/******/ (() => { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ "./js/canvas/Tracker.js":
/*!******************************!*\
  !*** ./js/canvas/Tracker.js ***!
  \******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   canvasTracker: () => (/* binding */ canvasTracker)
/* harmony export */ });
/* harmony import */ var _canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./canvas.js */ "./js/canvas/canvas.js");
// CanvasTracker utility for tracking canvas operations


const canvasObject = _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvasObject;

class CanvasTracker {
  constructor() {
    this.history = [];
    this.historyIndex = -1;
    this.maxHistory = 50;
    this.isRestoring = false;
    this.isInteractionActive = false;
    this.historyChangeCallbacks = [];
    this.saveStateTimer = null;

    // Initialize with empty state or current state if canvas is ready
    setTimeout(() => {
        if (_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas) {
            this.saveState();
            this.setupEventListeners();
        }
    }, 100);
  }

  setupEventListeners() {
      const canvas = _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas;
      if (!canvas) return;

      const saveHandler = () => {
          if (!this.isRestoring) {
              this.saveState();
          }
      };

      // Track mouse interaction state to group drag operations
      canvas.on('mouse:down', () => {
          this.isInteractionActive = true;
      });
      
      // mouse:up and object:modified are handled by mouseEvents.js calling endDrag()
      // We keep object:added/removed here for other operations (like paste, delete key)
      
      canvas.on('object:added', (e) => {
          if (!this.isRestoring && e.target && e.target.id !== 'grid') {
              saveHandler();
          }
      });
      canvas.on('object:removed', (e) => {
          if (!this.isRestoring && e.target && e.target.id !== 'grid') {
              saveHandler();
          }
      });
  }

  addHistoryChangeCallback(callback) {
    if (typeof callback === 'function' && !this.historyChangeCallbacks.includes(callback)) {
      this.historyChangeCallbacks.push(callback);
    }
  }

  removeHistoryChangeCallback(callback) {
    const index = this.historyChangeCallbacks.indexOf(callback);
    if (index !== -1) {
      this.historyChangeCallbacks.splice(index, 1);
    }
  }

  notifyHistoryChanged() {
    this.historyChangeCallbacks.forEach(callback => {
      try {
        callback();
      } catch (error) {
        console.error('Error in history change callback:', error);
      }
    });
  }

  saveState() {
    if (this.isRestoring) return;
    
    // Debounce saveState to prevent multiple saves in the same tick (e.g. mouseup + modified)
    if (this.saveStateTimer) {
        clearTimeout(this.saveStateTimer);
    }
    
    this.saveStateTimer = setTimeout(() => {
        this._performSaveState();
    }, 50);
  }

  _performSaveState() {
    const objects = canvasObject.filter(obj => obj.id !== 'grid');
    const state = objects.map(obj => {
        if (typeof obj.serializeToJSON === 'function') {
            return obj.serializeToJSON();
        }
    });

    if (this.historyIndex < this.history.length - 1) {
      this.history = this.history.slice(0, this.historyIndex + 1);
    }

    const stateStr = JSON.stringify(state);
    let description = this.history.length === 0 ? "Initial State" : `State ${this.history.length + 1}`;

    if (this.historyIndex >= 0) {
        const lastEntry = this.history[this.historyIndex];
        const lastStateStr = JSON.stringify(lastEntry.state);
        
        if (stateStr === lastStateStr) {
            return;
        }
        
        description = this._generateDescription(lastEntry.state, state);
    }

    this.history.push({
        state: state,
        timestamp: new Date().toLocaleTimeString(),
        description: description
    });
    this.historyIndex++;

    if (this.history.length > this.maxHistory) {
      this.history.shift();
      this.historyIndex--;
    }

    console.log(`State saved. History: ${this.historyIndex + 1}/${this.history.length}`);
    this.notifyHistoryChanged();
  }

  _generateDescription(oldState, newState) {
    const oldMap = new Map(oldState.map(obj => [obj.canvasID, obj]));
    const newMap = new Map(newState.map(obj => [obj.canvasID, obj]));
    const changes = [];

    const created = [];
    const deleted = [];
    const updated = [];

    // Check for created
    newState.forEach(obj => {
        if (!oldMap.has(obj.canvasID)) {
            created.push(obj.objectType || 'Object');
        }
    });

    // Check for deleted
    oldState.forEach(obj => {
        if (!newMap.has(obj.canvasID)) {
            deleted.push(obj.objectType || 'Object');
        }
    });

    // Check for updated
    newState.forEach(obj => {
        if (oldMap.has(obj.canvasID)) {
            const oldObj = oldMap.get(obj.canvasID);
            if (JSON.stringify(obj) !== JSON.stringify(oldObj)) {
                updated.push(obj.objectType || 'Object');
            }
        }
    });

    if (created.length > 0) changes.push(`Created ${this._summarizeTypes(created)}`);
    if (deleted.length > 0) changes.push(`Deleted ${this._summarizeTypes(deleted)}`);
    if (updated.length > 0) changes.push(`Updated ${this._summarizeTypes(updated)}`);

    if (changes.length === 0) {
         return "Reordered Objects";
    }
    
    return changes.join(', ');
  }

  _summarizeTypes(types) {
    const counts = {};
    types.forEach(t => counts[t] = (counts[t] || 0) + 1);
    return Object.entries(counts).map(([type, count]) => {
        const label = count > 1 ? `${type}s` : type;
        return count > 1 ? `${count} ${label}` : label;
    }).join(', ');
  }

  async restoreState(index) {
    if (index < 0 || index >= this.history.length) return;

    this.isRestoring = true;
    const stateEntry = this.history[index];
    const canvas = _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas;

    // Save current active object's canvasID
    const activeObject = canvas.getActiveObject();
    const activeCanvasID = activeObject ? activeObject.canvasID : null;

      // Remove all objects except grid
      const objects = canvas.getObjects();
      const objectsToRemove = objects.filter(obj => obj.id !== 'grid');
      objectsToRemove.forEach(obj => canvas.remove(obj));
      
      // Clear global object list
      if (_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvasObject) {
          _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvasObject.length = 0;
      }
      
      // Clear global anchor tree to prevent circular references and stale object references
      const { globalAnchorTree } = await Promise.resolve(/*! import() */).then(__webpack_require__.bind(__webpack_require__, /*! ../objects/anchor.js */ "./js/objects/anchor.js"));
      if (globalAnchorTree && typeof globalAnchorTree.clear === 'function') {
          globalAnchorTree.clear();
      }

      const stateClone = JSON.parse(JSON.stringify(stateEntry.state));
      
      // Dynamic import to avoid circular dependency
      const { buildObjectsFromJSON } = await Promise.resolve(/*! import() */).then(__webpack_require__.bind(__webpack_require__, /*! ../objects/build.js */ "./js/objects/build.js"));
      await buildObjectsFromJSON(stateClone);

      // Ensure grid is present and correct
      (0,_canvas_js__WEBPACK_IMPORTED_MODULE_0__.DrawGrid)();

      this.historyIndex = index;
      canvas.renderAll();
      
      // Dynamic import for property panel to avoid circular dependency
      const { showPropertyPanel } = await Promise.resolve(/*! import() */).then(__webpack_require__.bind(__webpack_require__, /*! ../sidebar/property.js */ "./js/sidebar/property.js"));

      // Restore selection if possible
      let restored = false;
      if (activeCanvasID !== null && activeCanvasID !== undefined) {
          const restoredObj = _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvasObject.find(obj => obj.canvasID === activeCanvasID);
          if (restoredObj) {
              canvas.setActiveObject(restoredObj);
              showPropertyPanel(restoredObj);
              restored = true;
          }
      }
      
      if (!restored) {
          canvas.discardActiveObject();
          showPropertyPanel(null);
      }

      console.log(`State restored. History: ${this.historyIndex + 1}/${this.history.length}`);
      this.notifyHistoryChanged();


      this.isRestoring = false;
    
  }

  async undo() {
    if (this.historyIndex > 0) {
      await this.restoreState(this.historyIndex - 1);
      return true;
    }
    return false;
  }

  async redo() {
    if (this.historyIndex < this.history.length - 1) {
      await this.restoreState(this.historyIndex + 1);
      return true;
    }
    return false;
  }
  
  clearHistory() {
      this.history = [];
      this.historyIndex = -1;
      this.saveState(); // Save current state as initial
      this.notifyHistoryChanged();
  }

  track(action, params) {
      // If we are in the middle of an interaction (like dragging), don't save state yet.
      // We wait for endDrag() or object:modified.
      if (action === 'modifyObject' && this.isInteractionActive) {
          return;
      }
      this.saveState();
  }
  
  // Alias for compatibility with older code or user expectations
  completeDragOperations() {
      this.endDrag();
  }

  endDrag() {
      this.isInteractionActive = false;
      this.saveState();
  }
  
  trackMetadataChange(type, description) {
      this.saveState();
  }
  
  // Mock methods for UI compatibility if needed
  getUniqueChainIds() { return []; }
  getChainObjectCount() { return 0; }
}

const canvasTracker = new CanvasTracker();
window.canvasTracker = canvasTracker;



/***/ }),

/***/ "./js/canvas/canvas.js":
/*!*****************************!*\
  !*** ./js/canvas/canvas.js ***!
  \*****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   CanvasGlobals: () => (/* binding */ CanvasGlobals),
/* harmony export */   DrawGrid: () => (/* binding */ DrawGrid)
/* harmony export */ });
var canvas = new fabric.Canvas('canvas', { fireMiddleClick: true, fireRightClick: true, preserveObjectStacking: true, enableRetinaScaling: true });
const ctx = canvas.getContext("2d")
let activeObject = null
let canvasObject = []
let activeVertex = null
canvas.isDragging = false;
canvas.lastPosX = 0;
canvas.lastPosY = 0;

// Global render scheduler to prevent excessive renderAll calls
let __renderQueued = false;
function scheduleRender() {
  if (__renderQueued) return;
  __renderQueued = true;
  requestAnimationFrame(() => {
    __renderQueued = false;
    if (canvas && typeof canvas.requestRenderAll === 'function') {
      canvas.requestRenderAll();
    } else if (canvas && typeof canvas.renderAll === 'function') {
      canvas.renderAll();
    }
  });
}

//window.canvas = canvas; // Expose canvas to the global scope for debugging

canvas.setZoom(0.2);

window.addEventListener('resize', resizeCanvas, false);

function resizeCanvas() {
  const canvasContainer = document.getElementById('canvas-container')
  canvas.setDimensions({ width: canvasContainer.clientWidth, height: canvasContainer.clientHeight })
  canvas.absolutePan({ x: -canvas.width / 2, y: -canvas.height / 2 })
  // Prefer scheduled render to coalesce redraws
  scheduleRender();
  DrawGrid()
}

function DrawGrid() {
  // Check if grid should be visible
  if (typeof GeneralSettings === 'undefined' || GeneralSettings === null) { return; }
  if (GeneralSettings && GeneralSettings.showGrid === false) {
    // Remove the existing grid if it exists
    const obj = canvas.getObjects().find(obj => obj.id === 'grid');
    if (obj) canvas.remove(obj);
    return;
  }

  // Calculate the appropriate grid distance based on the viewport boundaries
  const corners = canvas.calcViewportBoundaries();
  var xmin = corners.tl.x;
  var xmax = corners.br.x;
  var ymin = corners.tl.y;
  var ymax = corners.br.y;
  const width = xmax - xmin;
  const height = ymax - ymin;
  const maxDimension = Math.max(width, height);
  const zoom = canvas.getZoom();

  // Get grid color from settings, default to '#ffffff' if not available
  const gridColor = GeneralSettings.gridColor || '#ffffff';

  // Get grid size from settings, default to 20 if not available
  const gridSize = GeneralSettings.gridSize || 20;

  // Determine the grid distance based on the max dimension AND zoom level
  let gridDistance = gridSize; // Use setting value, default to 20 if not set
  if (zoom < 0.05) {
    gridDistance = 1000;
  } else if (zoom < 0.1) {
    gridDistance = 500;
  } else if (zoom < 0.25) {
    gridDistance = 200;
  } else if (zoom < 0.5) {
    gridDistance = 100;
  } else if (zoom < 1) {
    gridDistance = 50;
  } else if (zoom < 2) {
    gridDistance = 20;
  } else if (zoom < 5) {
    gridDistance = 10;
  } else {
    gridDistance = 5;
  }

  xmin = Math.floor(corners.tl.x / gridDistance) * gridDistance;
  xmax = Math.ceil(corners.br.x / gridDistance) * gridDistance;
  ymin = Math.floor(corners.tl.y / gridDistance) * gridDistance;
  ymax = Math.ceil(corners.br.y / gridDistance) * gridDistance;

  const options = {
    distance: gridDistance,
    param: {
      stroke: gridColor, // Use settings color instead of hardcoded '#ebebeb'
      strokeWidth: 0.1 / zoom, // Scale stroke width inversely with zoom for consistent appearance
      selectable: false
    }
  };

  const grid_set = [];

  // Set a constant screen size for text (12px)
  const constantFontSize = 12;
  // Scale font size according to zoom to maintain consistent screen size
  const scaledFontSize = constantFontSize / zoom;

  // Text appearance threshold - don't show labels when too zoomed out
  const showLabels = zoom > 0.08;

  for (let x = xmin; x <= xmax; x += options.distance) {
    const vertical = new fabric.Line([x, ymin, x, ymax], options.param);
    if (Math.abs(x % (5 * options.distance)) < 1e-6) {
      vertical.set({ strokeWidth: 0.2 / zoom }); // Thicker lines for major grid lines

      if (showLabels) {
        const vText = new fabric.Text(String(x), {
          left: x + 2 / zoom, // Add a small offset
          top: 2 / zoom,
          fill: gridColor, // Use settings color instead of hardcoded '#a0a0a0'
          selectable: false,
          hoverCursor: 'default',
          fontSize: scaledFontSize,
          scaleX: 1,
          scaleY: 1,
          fontFamily: 'Arial'
        });
        grid_set.push(vText);
      }
    }
    grid_set.push(vertical);
  }

  for (let y = ymin; y <= ymax; y += options.distance) {
    const horizontal = new fabric.Line([xmin, y, xmax, y], options.param);
    if (Math.abs(y % (5 * options.distance)) < 1e-6) {
      horizontal.set({ strokeWidth: 0.2 / zoom }); // Thicker lines for major grid lines

      if (showLabels) {
        const hText = new fabric.Text(String(y), {
          left: 2 / zoom,
          top: y + 2 / zoom, // Add a small offset
          fill: gridColor, // Use settings color instead of hardcoded '#a0a0a0'
          selectable: false,
          hoverCursor: 'default',
          fontSize: scaledFontSize,
          scaleX: 1,
          scaleY: 1,
          fontFamily: 'Arial'
        });
        grid_set.push(hText);
      }
    }
    grid_set.push(horizontal);
  }

  // Ensure the origin (0, 0) is included in the grid
  const originLineX = new fabric.Line([0, ymin, 0, ymax], { stroke: '#ffffff', strokeWidth: 0.5 / zoom, selectable: false });
  const originLineY = new fabric.Line([xmin, 0, xmax, 0], { stroke: '#ffffff', strokeWidth: 0.5 / zoom, selectable: false });
  grid_set.push(originLineX);
  grid_set.push(originLineY);

  // Remove the existing grid if it exists
  const obj = canvas.getObjects().find(obj => obj.id === 'grid');
  canvas.remove(obj);

  const grid_group = new fabric.Group(grid_set, { id: 'grid', selectable: false, evented: false });
  canvas.add(grid_group);
  canvas.sendObjectToBack(grid_group);
}

function CenterCoord() {
  var zoom = canvas.getZoom()
  return {
    x: fabric.util.invertTransform(canvas.viewportTransform)[4] + (canvas.width / zoom) / 2,
    y: fabric.util.invertTransform(canvas.viewportTransform)[5] + (canvas.height / zoom) / 2
  }
}



resizeCanvas();

const CanvasGlobals = {
  canvas: canvas,
  ctx: ctx,
  activeObject: activeObject,
  activeVertex: activeVertex,
  canvasObject: canvasObject,
  CenterCoord: CenterCoord,
  scheduleRender: scheduleRender,
}





/***/ }),

/***/ "./js/canvas/contexMenu.js":
/*!*********************************!*\
  !*** ./js/canvas/contexMenu.js ***!
  \*********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   cursorClickMode: () => (/* binding */ cursorClickMode)
/* harmony export */ });
/* harmony import */ var _canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _sidebar_property_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../sidebar/property.js */ "./js/sidebar/property.js");

 // Import showPropertyPanel

const canvas = _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas; // Access the global canvas object
// Context menu
const contextMenu = document.getElementById('context-menu');
let cursorClickMode = 'normal'; // Default click mode

function clickModelHandler(event) {
  switch (cursorClickMode) {
    case 'normal': {
      if (event.e.button === 2 && event.target) { // Right click
        event.e.preventDefault();
        contextMenu.style.top = `${event.e.clientY}px`;
        contextMenu.style.left = `${event.e.clientX}px`;
        contextMenu.style.display = 'block';
        // Reset potential submenu flip state when showing
        const pivotItem = document.getElementById('pivot-anchor');
        if (pivotItem) pivotItem.classList.remove('open-left');
        contextMenu.selectedArrow = event.target;
      } else {
        contextMenu.style.display = 'none';
      }
    }
      break;
    case 'select': {
      if (event.e.button === 0 && event.target) {
        contextMenu.selectedArrow = event.target;
        cursorClickMode = 'normal';
        contextMenu.style.display = 'none'; // Ensure context menu is hidden
      }
    }
      break;
  }
}

canvas.on('mouse:down', function (event) {
  clickModelHandler(event)
});


document.addEventListener('contextmenu', function (event) {
  event.preventDefault();
});


// Add handlers for context-menu actions
const deleteMenuItem = document.getElementById('delete-object');
deleteMenuItem.addEventListener('click', function (e) {
  e.preventDefault();
  contextMenu.style.display = 'none';
  const obj = contextMenu.selectedArrow;
  if (obj && typeof obj.deleteObject === 'function') {
    obj.deleteObject(null, { target: obj });
  }
});

const editMenuItem = document.getElementById('edit-object');
editMenuItem.addEventListener('click', function (e) {
  e.preventDefault();
  contextMenu.style.display = 'none';
  const obj = contextMenu.selectedArrow;
  // if (obj && typeof obj.onDoubleClick === 'function') { // Old behavior
  //   obj.onDoubleClick();
  // }
  if (obj) { // New behavior
    (0,_sidebar_property_js__WEBPACK_IMPORTED_MODULE_1__.showPropertyPanel)(obj);
  }
});

// Dynamically flip submenu to left if it would overflow the viewport on the right
const pivotMenuItem = document.getElementById('pivot-anchor');
if (pivotMenuItem) {
  pivotMenuItem.addEventListener('mouseenter', () => {
    const submenu = pivotMenuItem.querySelector('.context-submenu');
    if (!submenu) return;

    // Compute after hover shows submenu (CSS handles display); delay a tick if needed
    requestAnimationFrame(() => {
      const submenuRect = submenu.getBoundingClientRect();
      const overflowRight = submenuRect.right > (window.innerWidth - 4);
      if (overflowRight) {
        pivotMenuItem.classList.add('open-left');
      } else {
        pivotMenuItem.classList.remove('open-left');
      }
    });
  });
}



/***/ }),

/***/ "./js/canvas/keyboardEvents.js":
/*!*************************************!*\
  !*** ./js/canvas/keyboardEvents.js ***!
  \*************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ShowHideSideBarEvent: () => (/* binding */ ShowHideSideBarEvent),
/* harmony export */   handleArrowKeys: () => (/* binding */ handleArrowKeys)
/* harmony export */ });
/* harmony import */ var _canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../sidebar/sb-settings.js */ "./js/sidebar/sb-settings.js");
/* harmony import */ var _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../sidebar/sbGeneral.js */ "./js/sidebar/sbGeneral.js");
/* harmony import */ var _Tracker_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./Tracker.js */ "./js/canvas/Tracker.js");
/* harmony import */ var _sidebar_property_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../sidebar/property.js */ "./js/sidebar/property.js");
/* harmony import */ var _objects_build_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../objects/build.js */ "./js/objects/build.js");







// Keyboard shortcut for showing/hiding sidebar
function ShowHideSideBarEvent(e) {
  switch (e.keyCode) {
    case 27: // esc
      GeneralHandler.ShowHideSideBar(e);
      break;
  }
}

// Method to handle arrow key presses for all active objects
function handleArrowKeys(event) {
  const activeObjects = _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.getActiveObjects();
  let moved = false;
  let deltaX = 0;
  let deltaY = 0;

  activeObjects.forEach(obj => {
    switch (event.key) {
      case 'ArrowUp':
        if (!obj.lockMovementY) {
          obj.top -= 1;
          deltaY = -1;
          moved = true;
        }
        break;
      case 'ArrowDown':
        if (!obj.lockMovementY) {
          obj.top += 1;
          deltaY = 1;
          moved = true;
        }
        break;
      case 'ArrowLeft':
        if (!obj.lockMovementX) {
          obj.left -= 1;
          deltaX = -1;
          moved = true;
        }
        break;
      case 'ArrowRight':
        if (!obj.lockMovementX) {
          obj.left += 1;
          deltaX = 1;
          moved = true;
        }
        break;
      case 'Delete':
        // Check if user is currently inputting something - if so, don't delete objects
        if (document.activeElement.tagName === 'INPUT' ||
          document.activeElement.tagName === 'TEXTAREA') {
          return; // Exit early if user is typing in an input field
        }

        if (obj.deleteObject) {
          _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.discardActiveObject(obj)
          _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.fire('object:deselected', { target: obj });
          obj.deleteObject(null, obj)
        }
        break;
    }    
    if (moved) {
      obj.updateAllCoord();
      obj.setCoords();
      obj.fire('moving');
      // Notify listeners (e.g., property panel) that object was modified
      _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.fire('object:modified', { target: obj });

      // Track the movement for undo/redo
      _Tracker_js__WEBPACK_IMPORTED_MODULE_3__.canvasTracker.track('modifyObject', [{
        type: 'BaseGroup',
        id: obj.canvasID,
        functionalType: obj.functionalType,
        deltaX: deltaX,
        deltaY: deltaY,
        isInitialMover:  true,
      }], 'Object moved with arrow keys');
    }
  });

  if (moved) {
  _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
  }
}

// Add event listener for arrow keys to the canvas
document.addEventListener('keydown', handleArrowKeys);

// Add event listener for Ctrl+S to save canvas state
document.addEventListener('keydown', function (event) {
  if (event.ctrlKey && event.key === 's') {
    event.preventDefault(); // Prevent the browser's default save action
    _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_1__.FormSettingsComponent.saveCanvasState();
    // Optionally, provide some feedback to the user, e.g., a console log or a small notification
    console.log('Canvas state saved (Ctrl+S)');
  }
});

// Add event listeners for undo/redo functionality
document.addEventListener('keydown', function (event) {
  // Check if user is currently inputting something - if so, don't trigger undo/redo
  if (document.activeElement.tagName === 'INPUT' ||
    document.activeElement.tagName === 'TEXTAREA') {
    return; // Exit early if user is typing in an input field
  }
  // Ctrl+Z for undo (try state-based first, fallback to original)
  if (event.ctrlKey && event.key === 'z' && !event.shiftKey) {
    event.preventDefault();
    if (typeof _Tracker_js__WEBPACK_IMPORTED_MODULE_3__.canvasTracker.undo === 'function') {
        _Tracker_js__WEBPACK_IMPORTED_MODULE_3__.canvasTracker.undo().then(success => {
            if (success) console.log('Undo performed');
            else console.log('Nothing to undo');
        });
    }
  }

  // Ctrl+Y or Ctrl+Shift+Z for redo (try state-based first, fallback to original)
  if ((event.ctrlKey && event.key === 'y') ||
    (event.ctrlKey && event.shiftKey && event.key === 'Z')) {
    event.preventDefault();
    if (typeof _Tracker_js__WEBPACK_IMPORTED_MODULE_3__.canvasTracker.redo === 'function') {
        _Tracker_js__WEBPACK_IMPORTED_MODULE_3__.canvasTracker.redo().then(success => {
            if (success) console.log('Redo performed');
            else console.log('Nothing to redo');
        });
    }
  }

  // Refresh property panel when arrow keys are pressed
    const panel = document.getElementById('property-panel');
    if (panel.style.display !== 'block') return;
    const arrowKeys = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight'];
    if (arrowKeys.includes(event.key)) {
      // Update content based on current active object
      const obj = _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.getActiveObject();
      if (obj) (0,_sidebar_property_js__WEBPACK_IMPORTED_MODULE_4__.showPropertyPanel)(obj);
    }
});

// Clipboard for copy/paste
let _clipboard = null;

function copy() {
  const activeObjects = _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.getActiveObjects();
  if (activeObjects.length > 0) {
    _clipboard = [];
    activeObjects.forEach(obj => {
      if (typeof obj.serializeToJSON === 'function') {
        _clipboard.push(obj.serializeToJSON());
      }
    });
  }
}

async function paste() {
  if (!_clipboard || _clipboard.length === 0) return;
  
  // Deep copy the clipboard data to avoid modifying the original clipboard
  const dataToPaste = JSON.parse(JSON.stringify(_clipboard));
  
  // Offset the objects
  dataToPaste.forEach(data => {
    if (data.left !== undefined) data.left += 20;
    if (data.top !== undefined) data.top += 20;
    // Also offset fixedWidthCoords/fixedHeightCoords if they exist (for BorderGroup)
    if (data.fixedWidthCoords) {
        data.fixedWidthCoords.x += 20;
        data.fixedWidthCoords.y += 20;
    }
    if (data.fixedHeightCoords) {
        data.fixedHeightCoords.x += 20;
        data.fixedHeightCoords.y += 20;
    }
  });

  _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.discardActiveObject();
  
  try {
    const newObjects = await (0,_objects_build_js__WEBPACK_IMPORTED_MODULE_5__.buildObjectsFromJSON)(dataToPaste);
    
    if (newObjects && newObjects.length > 0) {
      const selection = new fabric.ActiveSelection(newObjects, {
        canvas: _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas,
      });
      _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.setActiveObject(selection);
      _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.requestRenderAll();
    }
  } catch (error) {
    console.error("Error pasting objects:", error);
  }
}

document.addEventListener('keydown', function(e) {
    // Ctrl+C
    if (e.ctrlKey && (e.key === 'c' || e.key === 'C')) {
        // Check if input is focused
        if (document.activeElement.tagName === 'INPUT' || document.activeElement.tagName === 'TEXTAREA') return;
        copy();
    }
    // Ctrl+V
    if (e.ctrlKey && (e.key === 'v' || e.key === 'V')) {
        if (document.activeElement.tagName === 'INPUT' || document.activeElement.tagName === 'TEXTAREA') return;
        paste();
    }
    
    // F3 - Toggle Text Border
    if (e.key === 'F3') {
        e.preventDefault();
        _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_2__.GeneralSettings.showTextBorders = !_sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_2__.GeneralSettings.showTextBorders;
        _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_1__.FormSettingsComponent.applyTextBorderSettings();
        _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_1__.FormSettingsComponent.updateSettingsUI();
        _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_1__.FormSettingsComponent.saveSettings();
    }
    // F4 - Toggle Grid
    if (e.key === 'F4') {
        e.preventDefault();
        _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_2__.GeneralSettings.showGrid = !_sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_2__.GeneralSettings.showGrid;
        _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_1__.FormSettingsComponent.applyGridSettings();
        _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_1__.FormSettingsComponent.updateSettingsUI();
        _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_1__.FormSettingsComponent.saveSettings();
    }
    // F2 - Toggle Vertices
    if (e.key === 'F2') {
        e.preventDefault();
        _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_2__.GeneralSettings.showAllVertices = !_sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_2__.GeneralSettings.showAllVertices;
        _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_1__.FormSettingsComponent.applyVertexDisplaySettings();
        _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_1__.FormSettingsComponent.updateSettingsUI();
        _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_1__.FormSettingsComponent.saveSettings();
    }
    // F8 - Toggle Dimension Unit
    if (e.key === 'F8') {
        e.preventDefault();
        _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_2__.GeneralSettings.dimensionUnit = _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_2__.GeneralSettings.dimensionUnit === 'mm' ? 'sw' : 'mm';
        _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_1__.FormSettingsComponent.refreshDimensionDisplays();
        _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_1__.FormSettingsComponent.updateSettingsUI();
        _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_1__.FormSettingsComponent.saveSettings();
    }
});




/***/ }),

/***/ "./js/canvas/mouseEvents.js":
/*!**********************************!*\
  !*** ./js/canvas/mouseEvents.js ***!
  \**********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _Tracker_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./Tracker.js */ "./js/canvas/Tracker.js");


const canvas = _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas; // Access the global canvas object
const canvasObject = _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvasObject; // Access the global canvas object list

let currentSnapHoverObjects = [];

function clearSnapHoverEnvelope() {
  currentSnapHoverObjects.forEach(obj => {
    if (obj && typeof obj.hideHoverSnapVertexEnvelope === 'function') {
      obj.hideHoverSnapVertexEnvelope();
    }
  });
  currentSnapHoverObjects = [];
}

function updateSnapHoverEnvelope(opt) {
  const activeVertex = _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex;
  const isSnapMode = !!(activeVertex && activeVertex.isDragging && activeVertex.baseGroup);

  if (!isSnapMode) {
    clearSnapHoverEnvelope();
    return;
  }

  const pointer = canvas.getPointer(opt.e);
  const activeSnapObject = activeVertex.baseGroup;
  const hoveredObjects = [];

  // Loop through canvasObject and use bounding-box hit test.
  for (let i = canvasObject.length - 1; i >= 0; i--) {
    const obj = canvasObject[i];
    if (!obj || obj === activeSnapObject) continue;
    if (typeof obj.getBoundingRect !== 'function') continue;

    const bbox = obj.getBoundingRect();
    const insideBBox =
      pointer.x >= bbox.left &&
      pointer.x <= bbox.left + bbox.width &&
      pointer.y >= bbox.top &&
      pointer.y <= bbox.top + bbox.height;

    if (insideBBox) {
      hoveredObjects.push(obj);
    }
  }

  const removedObjects = currentSnapHoverObjects.filter(obj => !hoveredObjects.includes(obj));
  const addedObjects = hoveredObjects.filter(obj => !currentSnapHoverObjects.includes(obj));

  if (removedObjects.length === 0 && addedObjects.length === 0) {
    return;
  }

  removedObjects.forEach(obj => {
    if (obj && typeof obj.hideHoverSnapVertexEnvelope === 'function') {
      obj.hideHoverSnapVertexEnvelope();
    }
  });

  addedObjects.forEach(obj => {
    if (obj && typeof obj.showHoverSnapVertexEnvelope === 'function') {
      obj.showHoverSnapVertexEnvelope();
    }
  });

  currentSnapHoverObjects = hoveredObjects;
}

// Handle mousedown event
canvas.on('mouse:down', function (opt) {
  const e = opt.e;
  // skip multi-touch pan if pinch-to-zoom is active
  if (e.touches && e.touches.length > 1) return;
  if (e.button === 1) { // Middle mouse button
    canvas.isDragging = true;
    canvas.selection = false;
    canvas.lastPosX = e.clientX;
    canvas.lastPosY = e.clientY;
  }
});
canvas.on('mouse:move', function (opt) {
  // skip if multi-touch gesture (pinch/rotate)
  const e = opt.e;
  if (e.touches && e.touches.length > 1) return;
  if (canvas.isDragging) {
    const vpt = this.viewportTransform;
    vpt[4] += e.clientX - canvas.lastPosX;
    vpt[5] += e.clientY - canvas.lastPosY;
  _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
    canvas.lastPosX = e.clientX;
    canvas.lastPosY = e.clientY;
    (0,_canvas_js__WEBPACK_IMPORTED_MODULE_0__.DrawGrid)()
    // Update the coordinates of all objects to ensure controls are updated
    canvas.getObjects().forEach(obj => {
      obj.setCoords();
    });
  }

  updateSnapHoverEnvelope(opt);
});

canvas.on('mouse:up', function (opt) {
  // on mouse up we want to recalculate new interaction
  // for all objects, so we call setViewportTransform\
  canvas.isDragging = false;
  canvas.selection = true;
  clearSnapHoverEnvelope();
});


// Handle zoom events
canvas.on('mouse:wheel', function (opt) {
  var delta = opt.e.deltaY;
  var zoom = canvas.getZoom();
  zoom *= 0.999 ** delta;
  if (zoom > 20) zoom = 20;
  if (zoom < 0.01) zoom = 0.01;
  canvas.zoomToPoint({ x: opt.e.offsetX, y: opt.e.offsetY }, zoom);
  opt.e.preventDefault();
  opt.e.stopPropagation();
  (0,_canvas_js__WEBPACK_IMPORTED_MODULE_0__.DrawGrid)()

  // Check if active object exists and has lock indicators that need to be redrawn
  const activeObj = canvas.getActiveObject();
  if (activeObj && activeObj.anchorageLink && activeObj.anchorageLink.length > 0) {
    // Redraw anchor linkages to update with new zoom level
    activeObj.drawAnchorLinkage();
  }

  // Update dimension lines on zoom if they exist
  if (activeObj && activeObj.showDimensions) {
    // Refresh dimension lines with new zoom level
    activeObj.showDimensions();
  }

  // Update active vertex 
  const activeVertex = _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex;
  if (activeVertex) {
    activeVertex.clearSnapHighlight()
    activeVertex.addSnapHighlight()
  }

  canvas.getObjects().forEach(obj => {
    obj.setCoords();
  });
  _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
})

canvas.on({
  'selection:updated': lockGroupSelection,
  'selection:created': lockGroupSelection
});

function lockGroupSelection(event) {
  const activeObjects = canvas.getActiveObject();

  if (activeObjects.type === 'activeselection') {
    activeObjects.lockMovementX = true;
    activeObjects.lockMovementY = true;
    activeObjects.lockScalingX = true;
    activeObjects.lockScalingY = true;
    activeObjects.lockRotation = true;
    activeObjects.lockUniScaling = true;
    activeObjects.hasControls = false;
  }
}

// Add event listeners to detect the end of drag operations
canvas.on('mouse:up', function () {
  // When mouse is released, signal the end of any ongoing drag
  _Tracker_js__WEBPACK_IMPORTED_MODULE_1__.canvasTracker.endDrag();
});

canvas.on('object:modified', function (e) {
  // When an object is modified (e.g., after resizing or rotation is complete), signal the end of any drag
  _Tracker_js__WEBPACK_IMPORTED_MODULE_1__.canvasTracker.endDrag();
  
  // Also capture the detailed state for property tracking
  //if (e.target && e.target.id) {
  //  canvasTracker.trackMetadataChange('objectModified', `Object ${e.target.id} modified`);
  //}
});

/***/ }),

/***/ "./js/canvas/promptBox.js":
/*!********************************!*\
  !*** ./js/canvas/promptBox.js ***!
  \********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   addPromptHighlightTerms: () => (/* binding */ addPromptHighlightTerms),
/* harmony export */   hideTextBox: () => (/* binding */ hideTextBox),
/* harmony export */   selectObjectHandler: () => (/* binding */ selectObjectHandler),
/* harmony export */   setPromptHighlightTerms: () => (/* binding */ setPromptHighlightTerms),
/* harmony export */   showTextBox: () => (/* binding */ showTextBox)
/* harmony export */ });
/* harmony import */ var _canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _keyboardEvents_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./keyboardEvents.js */ "./js/canvas/keyboardEvents.js");
/* harmony import */ var _contexMenu_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./contexMenu.js */ "./js/canvas/contexMenu.js");
/* harmony import */ var _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../i18n/i18n.js */ "./js/i18n/i18n.js");





const canvas = _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas; // Access the global canvas object

// Configurable prompt keyword emphasis
const PromptHighlight = {
  terms: new Set(["width", "height", "寬度", "高度"]),
  set(terms) {
    this.terms = new Set((terms || []).map((s) => String(s).toLowerCase()));
  },
  add(...terms) {
    terms.forEach((t) => this.terms.add(String(t).toLowerCase()));
  },
  clear() {
    this.terms.clear();
  },
};

function escapeHtml(str) {
  return String(str)
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;")
    .replace(/'/g, "&#39;");
}

function emphasizePromptText(s) {
  if (!s) return "";
  const escaped = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_3__.i18n.t(escapeHtml(s));
  if (!PromptHighlight.terms || PromptHighlight.terms.size === 0) {
    return escaped;
  }
  const pattern = Array.from(PromptHighlight.terms)
    .map((t) => t.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"))
    .join("|");
  const regex = new RegExp(pattern, "gi");
  return escaped.replace(escaped.match(regex), (m) => `<span class="prompt-keyword">${m.toUpperCase()}</span>`);
}

function updatePosition(event) {
  const promptBox = document.getElementById('cursorBoxContainer');
  const promptText = document.getElementById('cursorTextBox');
  const answerBox = document.getElementById('cursorAnswerBox');

  // Get viewport dimensions
  const viewportWidth = window.innerWidth;
  const viewportHeight = window.innerHeight;

  // Get cursor box dimensions
  const boxWidth = promptBox.offsetWidth;
  const boxHeight = promptBox.offsetHeight;

  // Default offset
  let xOffset = 10;
  let yOffset = 10;

  // Check if box would overflow right edge
  if (event.clientX + xOffset + boxWidth > viewportWidth) {
    // Position to the left of cursor instead
    xOffset = -boxWidth - 10;
  }

  // Check if box would overflow bottom edge
  if (event.clientY + yOffset + boxHeight > viewportHeight) {
    // Position above cursor instead
    yOffset = -boxHeight - 10;
  }

  // Apply the position
  promptBox.style.left = `${event.clientX + xOffset}px`;
  promptBox.style.top = `${event.clientY + yOffset}px`;
}

document.addEventListener('mousemove', updatePosition);

function answerBoxFocus(event) {
  const answerBox = document.getElementById('cursorAnswerBox');
  if (answerBox.style.display === 'block') {
    answerBox.focus();
  }
}

document.addEventListener('mouseup', answerBoxFocus);


function showTextBox(text, withAnswerBox = null, event = 'keydown', callback = null, xHeight = null, unit = 'sw') {
  const promptBox = document.getElementById('cursorTextBox');
  const answerBox = document.getElementById('cursorAnswerBox');
  const answerWrapper = document.getElementById('cursorAnswerWrapper');
  const enterButton = document.getElementById('cursorEnterButton');
  const cancelButton = document.getElementById('cursorCancelButton');

  // Emphasize configured keywords (defaults to WIDTH/HEIGHT)
  promptBox.innerHTML = emphasizePromptText(_i18n_i18n_js__WEBPACK_IMPORTED_MODULE_3__.i18n.t(text));
  promptBox.style.display = 'block';
  document.removeEventListener('keydown', _keyboardEvents_js__WEBPACK_IMPORTED_MODULE_1__.ShowHideSideBarEvent);

  // Unit handling variables
  let currentUnit = unit;
  let unitDisplay = null;
  let inputValue = '';

  if (withAnswerBox !== null) {
    if (answerWrapper) answerWrapper.style.display = 'inline-flex';
    answerBox.style.display = 'block';
    if (enterButton && cancelButton) {
      if (window.innerWidth <= 600) { // Check for mobile screen width
        enterButton.style.display = 'block';
        cancelButton.style.display = 'block';
      } else {
        enterButton.style.display = 'none';
        cancelButton.style.display = 'none';
      }
    }
    answerBox.value = withAnswerBox;
    answerBox.focus();
    answerBox.select();

    // Set up unit display if xHeight is provided
    if (xHeight !== null) {
      unitDisplay = document.getElementById('unit-display');
      if (unitDisplay) {
        unitDisplay.textContent = currentUnit;
        unitDisplay.style.display = 'inline-block';
      }
      // Initial setup
      inputValue = withAnswerBox;
    }

    // Handle user input and resolve the answer
    return new Promise((resolve) => {
      const enterButton = document.getElementById('cursorEnterButton'); // Get buttons inside promise
      const cancelButton = document.getElementById('cursorCancelButton');

      const cleanupListeners = () => {
        answerBox.removeEventListener('keydown', handleKeyDown);
        if (enterButton) {
          enterButton.removeEventListener('click', handleEnterClick);
        }
        if (cancelButton) {
          cancelButton.removeEventListener('click', handleCancelClick);
        }
      };

      const handleEnterClick = () => {
        let result = answerBox.value;
        if (xHeight !== null && unitDisplay && !isNaN(parseFloat(result))) {
          if (currentUnit === 'sw') {
            result = String(parseFloat(result) * xHeight / 4);
          }
        }
        if (unitDisplay) {
          unitDisplay.style.display = 'none';
        }
        resolve(result);
        hideTextBox();
        cleanupListeners();
      };

      const handleCancelClick = () => {
        if (unitDisplay) {
          unitDisplay.style.display = 'none';
        }
        resolve(null);
        hideTextBox();
        cleanupListeners();
      };

      const handleKeyDown = function (event) {
        if (event.key === 'Enter' || event.key === ' ') {
          let result = answerBox.value;
          // Convert units if in unit mode
          if (xHeight !== null && unitDisplay && !isNaN(parseFloat(result))) {
            if (currentUnit === 'sw') {
              // Convert from sw to mm (1 mm = 1 sw * xHeight / 4)
              result = String(parseFloat(result) * xHeight / 4);
            }
          }
          if (unitDisplay) {
            unitDisplay.style.display = 'none';
          }
          resolve(result);
          hideTextBox();
          cleanupListeners();
        } else if (event.key === 'Escape') {
          if (unitDisplay) {
            unitDisplay.style.display = 'none';
          }
          resolve(null);
          hideTextBox();
          cleanupListeners();
        } else if (event.key === 'Tab' && xHeight !== null && unitDisplay) {
          // Switch between mm and sw units on Tab key press
          event.preventDefault();
          currentUnit = currentUnit === 'mm' ? 'sw' : 'mm';

          // Convert the current value between units
          if (!isNaN(parseFloat(answerBox.value))) {
            if (currentUnit === 'sw') {
              // Convert from mm to sw
              answerBox.value = String(parseFloat(answerBox.value) * 4 / xHeight);
            } else {
              // Convert from sw to mm
              answerBox.value = String(parseFloat(answerBox.value) * xHeight / 4);
            }
          }

          unitDisplay.innerText = currentUnit;
        }
      };

      answerBox.addEventListener('keydown', handleKeyDown);
      if (enterButton && window.innerWidth <= 600) { // Check for mobile here as well for adding listener
        enterButton.addEventListener('click', handleEnterClick);
      }
      if (cancelButton && window.innerWidth <= 600) { // Check for mobile here as well for adding listener
        cancelButton.addEventListener('click', handleCancelClick);
      }
    });
  } else {
    if (answerWrapper) answerWrapper.style.display = 'none';
    answerBox.style.display = 'none';
    if (enterButton && cancelButton) {
      enterButton.style.display = 'none';
      cancelButton.style.display = 'none';
    }
    document.addEventListener(event, callback);
  }
}

function hideTextBox() {
  const promptBox = document.getElementById('cursorTextBox');
  const answerBox = document.getElementById('cursorAnswerBox');
  const answerWrapper = document.getElementById('cursorAnswerWrapper');
  const enterButton = document.getElementById('cursorEnterButton');
  const cancelButton = document.getElementById('cursorCancelButton');
  const unitDisplay = document.getElementById('unit-display');

  promptBox.style.display = 'none';
  answerBox.style.display = 'none';
  if (answerWrapper) answerWrapper.style.display = 'none';
  if (enterButton && cancelButton) {
    enterButton.style.display = 'none';
    cancelButton.style.display = 'none';
  }
  // Hide unit display badge if present
  if (unitDisplay) {
    unitDisplay.style.display = 'none';
  }
  setTimeout(() => {
    document.addEventListener('keydown', _keyboardEvents_js__WEBPACK_IMPORTED_MODULE_1__.ShowHideSideBarEvent);
  }, 1000); // Delay in milliseconds (e.g., 1000ms = 1 second)
}

function selectObjectHandler(text, callback, options = null, xHeight = null, unit = 'mm',
  skipTextBox = true, requiredTypes = null) {
  /*
    Simplified behavior:
      - Do not wait for Enter/textbox input.
      - When there are active objects, wait for the user to release any dragging,
        then pass the active objects to the callback.
      - Filter by requiredTypes if provided (all active objects must match).
  */

  // Show prompt message near cursor without answer box
  try {
    const promptTextEl = document.getElementById('cursorTextBox');
    const answerBoxEl = document.getElementById('cursorAnswerBox');
    const enterBtn = document.getElementById('cursorEnterButton');
    const cancelBtn = document.getElementById('cursorCancelButton');
    if (promptTextEl) {
      promptTextEl.innerHTML = emphasizePromptText(text || 'Select object(s)');
      promptTextEl.style.display = 'block';
    }
    if (answerBoxEl) answerBoxEl.style.display = 'none';
    if (enterBtn) enterBtn.style.display = 'none';
    if (cancelBtn) cancelBtn.style.display = 'none';
    // Pause sidebar toggle while prompt is visible
    document.removeEventListener('keydown', _keyboardEvents_js__WEBPACK_IMPORTED_MODULE_1__.ShowHideSideBarEvent);
  } catch (e) {
    // Non-fatal if UI elements are missing
  }

  const matchesRequiredType = (obj) => {
    if (!requiredTypes) return true;
    if (Array.isArray(requiredTypes)) return requiredTypes.includes(obj.functionalType);
    return obj.functionalType === requiredTypes;
  };

  let isDragging = false;
  let processed = false;
  let dragDebounceTimer = null;
  let checkInterval = null;

  const cleanup = () => {
    if (processed) return;
    // no-op; actual cleanup happens after processing
  };

  const removeListeners = () => {
    canvas.off('object:moving', onObjectMoving);
    canvas.off('mouse:up', onMouseUp);
    document.removeEventListener('keydown', onKeyDown);
    if (dragDebounceTimer) {
      clearTimeout(dragDebounceTimer);
      dragDebounceTimer = null;
    }
    if (checkInterval) {
      clearInterval(checkInterval);
    }
  };

  const processSelection = () => {
    if (processed) return;
    const active = canvas.getActiveObjects();
    if (active && active.length > 0 && active.every(matchesRequiredType)) {
      processed = true;
      removeListeners();
      hideTextBox();
      const successSelected = [...active];
      canvas.discardActiveObject();
      _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
      // response is not used anymore; pass null for backward compatibility
      callback(successSelected, options, null, xHeight);
    }
  };

  const onObjectMoving = () => {
    isDragging = true;
    if (dragDebounceTimer) {
      clearTimeout(dragDebounceTimer);
      dragDebounceTimer = null;
    }
  };

  const onMouseUp = () => {
    isDragging = false;
    // Small delay to allow fabric to finalize selection geometry
    dragDebounceTimer = setTimeout(processSelection, 80);
  };

  // Attach listeners
  canvas.on('object:moving', onObjectMoving);
  canvas.on('mouse:up', onMouseUp);
  const onKeyDown = (e) => {
    if (e.key === 'Escape') {
      processed = true;
      removeListeners();
      hideTextBox();
      // Do not invoke callback; ESC acts as cancel
    }
  };
  document.addEventListener('keydown', onKeyDown);

  // If there is already an active selection and user isn't dragging,
  // process it after a brief idle delay as a fallback.
  checkInterval = setInterval(() => {
    if (!isDragging) {
      processSelection();
    }
  }, 150);
}


// Optional API to configure emphasized terms at runtime
const setPromptHighlightTerms = (terms) => PromptHighlight.set(terms);
const addPromptHighlightTerms = (...terms) => PromptHighlight.add(...terms);

/***/ }),

/***/ "./js/canvas/touchEvents.js":
/*!**********************************!*\
  !*** ./js/canvas/touchEvents.js ***!
  \**********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./canvas.js */ "./js/canvas/canvas.js");

const canvas = _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas; // Access the global canvas object
const activeVertex = _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex; // Access the global active vertex object
// —————————————————————————————
// native touch pinch-to-zoom & two-finger pan
// using Fabric pointer events (no external libs)
// —————————————————————————————
let lastTouchDistanceNative = 1;
let lastTouchCenterNative = null;
let isNativeTouching = false;

canvas.on('mouse:down', function(opt) {
  const e = opt.e;
  if (e.touches && e.touches.length === 2) {
    isNativeTouching = true;
    canvas.selection = false;
    const t1 = e.touches[0], t2 = e.touches[1];
    lastTouchDistanceNative = Math.hypot(t2.clientX - t1.clientX, t2.clientY - t1.clientY);
    lastTouchCenterNative = { x: (t1.clientX + t2.clientX) / 2, y: (t1.clientY + t2.clientY) / 2 };
    opt.e.preventDefault();
  }
});

canvas.on('mouse:move', function(opt) {
  const e = opt.e;
  if (!isNativeTouching || !e.touches || e.touches.length !== 2) return;
  const t1 = e.touches[0], t2 = e.touches[1];
  const currentDistance = Math.hypot(t2.clientX - t1.clientX, t2.clientY - t1.clientY);
  const currentCenter = { x: (t1.clientX + t2.clientX) / 2, y: (t1.clientY + t2.clientY) / 2 };

  // zoom
  let zoom = canvas.getZoom() * (currentDistance / lastTouchDistanceNative);
  zoom = Math.max(0.01, Math.min(zoom, 20));
  canvas.zoomToPoint(new fabric.Point(lastTouchCenterNative.x, lastTouchCenterNative.y), zoom);
  (0,_canvas_js__WEBPACK_IMPORTED_MODULE_0__.DrawGrid)(); // Update grid after zoom

  // pan
  const dx = currentCenter.x - lastTouchCenterNative.x;
  const dy = currentCenter.y - lastTouchCenterNative.y;
  canvas.relativePan(new fabric.Point(dx, dy));

  lastTouchDistanceNative = currentDistance;
  lastTouchCenterNative = currentCenter;

  _canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
  opt.e.preventDefault();
});

canvas.on('mouse:up', function(opt) {
  if (isNativeTouching) {
    isNativeTouching = false;
    canvas.selection = true;
  }
});

/***/ }),

/***/ "./js/exportUtils/export.js":
/*!**********************************!*\
  !*** ./js/exportUtils/export.js ***!
  \**********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   calculatePathUnion: () => (/* binding */ calculatePathUnion),
/* harmony export */   collectNestedPathObjects: () => (/* binding */ collectNestedPathObjects),
/* harmony export */   collectPathObjects: () => (/* binding */ collectPathObjects),
/* harmony export */   convertRectToPath: () => (/* binding */ convertRectToPath),
/* harmony export */   cubicBezierToArc: () => (/* binding */ cubicBezierToArc),
/* harmony export */   processPathForDXF: () => (/* binding */ processPathForDXF),
/* harmony export */   transformPath: () => (/* binding */ transformPath)
/* harmony export */ });
const SPECIAL_EXPORT_CHAR = '九炮功勁勞勢磡婆支波飛肉內';

function collectPathObjects(obj, pathObjects) {
  // Skip grid objects
  if (obj.id === 'grid') return;

  // Special handling for TextObjects - collect both frames and character paths
  if (obj.functionalType === 'Text') {
    // Process text frames if available
    if (obj.txtFrameList) {
      obj.txtFrameList.forEach(frame => {
        // Get proper frame position relative to text object
        const frameOffsetX = frame.left + obj.width / 2 + obj.left || 0;
        const frameOffsetY = frame.top + obj.height / 2 + obj.top || 0;

        // Create a path representing the text frame
        const framePoints = [
          { x: frameOffsetX - 1, y: frameOffsetY - 1 },
          { x: frameOffsetX + 1 + frame.width, y: frameOffsetY - 1 },
          { x: frameOffsetX + 1 + frame.width, y: frameOffsetY + frame.height + 1 },
          { x: frameOffsetX - 1, y: frameOffsetY + frame.height + 1 }
        ];

        // Create a synthetic path object from the frame
        const framePath = {
          type: 'path',
          path: [
            ['M', framePoints[0].x, framePoints[0].y],
            ['L', framePoints[1].x, framePoints[1].y],
            ['L', framePoints[2].x, framePoints[2].y],
            ['L', framePoints[3].x, framePoints[3].y],
            ['Z']
          ],
        };

        pathObjects.push(framePath);
      });
    }
    // Process text character paths if available
    if (obj.txtCharList && obj.txtCharList.length > 0) {
      // Collect all character paths for union calculation
      const characterPaths = [];

      obj.txtCharList.forEach(charObj => {
        if (charObj && charObj.type === 'path' && charObj.path) {
          // Create a copy of the path to preserve the original
          const pathCopy = {
            type: 'path',
            path: charObj.path.slice(),
            left: charObj.left,
            top: charObj.top,
            scaleX: charObj.scaleX || 1,
            scaleY: charObj.scaleY || 1,
            angle: charObj.angle || 0,
            objectType: 'text' // Tag character paths as text
          };

          // Apply transformations based on the character's position within the text object
          const transformedPath = transformPath(
            pathCopy,
            obj.left + obj.width / 2,
            obj.top + obj.height / 2
          );

          if (transformedPath.path && transformedPath.path.length > 0) {
            const unionedPath = calculatePathUnion(transformedPath, charObj);
            unionedPath.objectType = 'text'; // Tag as text object
            pathObjects.push(unionedPath);
          }
        }
      });
    }
    return;
  }

  // Handle SVG paths with potential holes
  if (obj.basePolygon && obj.basePolygon.type === 'path' && obj.basePolygon.path) {
    // Add the complete path object from basePolygon
    const rawPath = obj.basePolygon;

    // Add as a regular path
    pathObjects.push({
      type: 'path',
      path: rawPath.path,
      left: rawPath.left || 0,
      top: rawPath.top || 0,
      scaleX: rawPath.scaleX || 1,
      scaleY: rawPath.scaleY || 1
    });
  }
  // If the object has a basePolygon with _objects
  else if (obj.basePolygon && obj.basePolygon._objects) {
    obj.basePolygon._objects.forEach(nestedObj => {
      collectNestedPathObjects(nestedObj, pathObjects, obj);
    });
  }
  // If it's a fabric Group object that might contain paths
  else if (obj.type === 'group' && obj._objects) {
    obj._objects.forEach(nestedObj => {
      collectNestedPathObjects(nestedObj, pathObjects, obj);
    });
  }
  // Direct path objects
  else if (obj.type === 'path') {
    // Add path direction info (clockwise/counterclockwise) for hole detection
    const pathWithDirection = {
      ...obj,
    };
    pathObjects.push(pathWithDirection);
  }
  // Direct rect objects
  else if (obj.type === 'rect') {
    const rectPathObj = convertRectToPath(obj);
    if (rectPathObj) {
      pathObjects.push(rectPathObj);
    }
  }
}

// Function to recursively collect path objects from nested structures
function collectNestedPathObjects(obj, collection, parentObj) {
  // Skip nulls or undefined
  if (!obj) return;

  if (obj.type === 'path') {
    // Add the complete path object from basePolygon
    const rawPath = {
      type: 'path',
      path: obj.path,
      left: obj.left || 0,
      top: obj.top || 0,
      scaleX: obj.scaleX || 1,
      scaleY: obj.scaleY || 1,
      width: obj.width,
      height: obj.height,
      pathOffset: obj.pathOffset,
      originX: obj.originX,
      originY: obj.originY
    };
    // Add as a regular path
    // Apply transformations based on the character's position within the text object
    const transformedPath = transformPath(
      rawPath,
      parentObj.left + parentObj.width / 2,
      parentObj.top + parentObj.height / 2
    );

    collection.push(transformedPath);


  }
  else if (obj.type === 'rect') {
    const rectPathObj = convertRectToPath(obj);
    if (rectPathObj) {
      rectPathObj.parentObj = parentObj;
      collection.push(rectPathObj);
    }
  }
  else if (obj.type === 'group' && obj._objects) {
    obj._objects.forEach(nestedObj => {
      collectNestedPathObjects(nestedObj, collection, parentObj || obj);
    });
  }
}

// Helper function to convert a rectangle to a path object
function convertRectToPath(rectObj) {
  if (!rectObj || !rectObj.width || !rectObj.height) return null;

  // Get the actual position of the rect considering its parent transformations
  let left = rectObj.left || 0;
  let top = rectObj.top || 0;
  let width = rectObj.width;
  let height = rectObj.height;

  // If the rect is part of a group, we need to apply the group's transformations
  if (rectObj.group) {
    const transformMatrix = rectObj.group.calcTransformMatrix();
    const tl = fabric.util.transformPoint({ x: left, y: top }, transformMatrix);
    const tr = fabric.util.transformPoint({ x: left + width, y: top }, transformMatrix);
    const br = fabric.util.transformPoint({ x: left + width, y: top + height }, transformMatrix);
    const bl = fabric.util.transformPoint({ x: left, y: top + height }, transformMatrix);

    // Create a pathData array using the transformed points
    return {
      type: 'path',
      path: [
        ['M', tl.x, tl.y],
        ['L', tr.x, tr.y],
        ['L', br.x, br.y],
        ['L', bl.x, bl.y],
        ['Z']
      ]
    };
  }

  // For standalone rects, create a simple path
  return {
    type: 'path',
    path: [
      ['M', left, top],
      ['L', left + width, top],
      ['L', left + width, top + height],
      ['L', left, top + height],
      ['Z']
    ]
  };
}

// Helper method to process a path object for DXF export
function processPathForDXF(pathObj, dxf, offsetX, offsetY) {
  // Process SVG path data for DXF export
  const pathData = pathObj.path || [];

  // First, we'll analyze the path to find subpaths (segments starting with 'M')
  const subpaths = [];
  let currentSubpath = [];
  let firstPointInSubpath = null;

  // Group commands by subpaths
  pathData.forEach(cmd => {
    if (cmd[0] === 'M') {
      // Start a new subpath
      if (currentSubpath.length > 0) {
        subpaths.push({
          commands: currentSubpath,
          firstPoint: firstPointInSubpath,
          closed: false
        });
      }
      currentSubpath = [cmd];
      firstPointInSubpath = [
        cmd[1] + offsetX,
        -(cmd[2] + offsetY) // Flip Y coordinate for DXF
      ];
    } else {
      currentSubpath.push(cmd);
      // Mark if path is closed
      if (cmd[0] === 'Z') {
        subpaths.push({
          commands: currentSubpath,
          firstPoint: firstPointInSubpath,
          closed: false
        });
        currentSubpath = [];
        firstPointInSubpath = null;
      }
    }
  });

  // Add the last subpath if it exists and wasn't closed with Z
  if (currentSubpath.length > 0) {
    subpaths.push({
      commands: currentSubpath,
      firstPoint: firstPointInSubpath,
      closed: false
    });
  }

  // Now process each subpath
  subpaths.forEach(subpath => {
    // For each subpath, we'll build spline control points and polyline points
    let currentX, currentY;
    let polylinePoints = [];
    let currentCmd;

    // Process the commands in this subpath
    for (let i = 0; i < subpath.commands.length; i++) {
      currentCmd = subpath.commands[i];
      const command = currentCmd[0];
      const values = currentCmd.slice(1);

      switch (command) {
        case 'M': // moveTo
          currentX = values[0] + offsetX;
          currentY = -(values[1] + offsetY); // Flip Y coordinate for DXF
          polylinePoints = [[currentX, currentY]];
          break;

        case 'L': // lineTo
          currentX = values[0] + offsetX;
          currentY = -(values[1] + offsetY); // Flip Y coordinate for DXF
          polylinePoints.push([currentX, currentY]);
          break; case 'C': { // bezierCurveTo - convert to DXF arc for better representation (except for text)
            const cp1x = values[0] + offsetX;
            const cp1y = -(values[1] + offsetY); // Flip Y coordinate for DXF
            const cp2x = values[2] + offsetX;
            const cp2y = -(values[3] + offsetY); // Flip Y coordinate for DXF
            const endX = values[4] + offsetX;
            const endY = -(values[5] + offsetY); // Flip Y coordinate for DXF

            // If we have accumulated polyline points, draw them first
            if (polylinePoints.length > 1) {
              dxf.drawPolyline(polylinePoints, false);
              // Start new polyline with the endpoint of the existing one
              polylinePoints = [[polylinePoints[polylinePoints.length - 1][0], polylinePoints[polylinePoints.length - 1][1]]];
            }

            // For text objects, use splines directly without arc conversion
            if (pathObj.objectType === 'text') {
              const cubicControlPoints = [
                [currentX, currentY],   // Start point
                [cp1x, cp1y],          // First control point
                [cp2x, cp2y],          // Second control point
                [endX, endY]           // End point
              ];
              dxf.drawSpline(cubicControlPoints, 3);
            } else {
              // For non-text objects, try to convert cubic Bézier curve to arc parameters
              const arcParams = cubicBezierToArc(currentX, currentY, cp1x, cp1y, cp2x, cp2y, endX, endY);

              if (arcParams) {
                // Draw as arc if conversion is successful
                dxf.drawArc(arcParams.centerX, arcParams.centerY, arcParams.radius, arcParams.startAngle, arcParams.endAngle);
              } else {
                // Fallback to spline if curve cannot be approximated as arc
                const cubicControlPoints = [
                  [currentX, currentY],   // Start point
                  [cp1x, cp1y],          // First control point
                  [cp2x, cp2y],          // Second control point
                  [endX, endY]           // End point
                ];
                dxf.drawSpline(cubicControlPoints, 3);
              }
            }

            // Update current position
            currentX = endX;
            currentY = endY;

            // Continue the polyline from here
            polylinePoints = [[currentX, currentY]];
            break;
          }

        case 'Q': { // quadraticCurveTo - use spline for better representation
          const qCpx = values[0] + offsetX;
          const qCpy = -(values[1] + offsetY); // Flip Y coordinate for DXF
          const qEndX = values[2] + offsetX;
          const qEndY = -(values[3] + offsetY); // Flip Y coordinate for DXF

          // If we have accumulated polyline points, draw them first
          if (polylinePoints.length > 1) {
            dxf.drawPolyline(polylinePoints, false);
            // Start new polyline with the endpoint of the existing one
            polylinePoints = [[polylinePoints[polylinePoints.length - 1][0], polylinePoints[polylinePoints.length - 1][1]]];
          }

          // Create control points for a quadratic spline (degree 2)
          const quadraticControlPoints = [
            [currentX, currentY],  // Start point
            [qCpx, qCpy],          // Control point
            [qEndX, qEndY]         // End point
          ];

          // Draw the spline
          dxf.drawSpline(quadraticControlPoints, 2);

          // Update current position
          currentX = qEndX;
          currentY = qEndY;

          // Continue the polyline from here
          polylinePoints = [[currentX, currentY]];
          break;
        }

        case 'Z': // closePath
          // If we have a first point and it's different from current position,
          // add it to close the path
          if (subpath.firstPoint &&
            (subpath.firstPoint[0] !== currentX || subpath.firstPoint[1] !== currentY)) {
            polylinePoints.push([subpath.firstPoint[0], subpath.firstPoint[1]]);
          }
          break;
      }
    }

    // Draw any remaining polyline points
    if (polylinePoints.length > 1) {
      dxf.drawPolyline(polylinePoints, subpath.closed);
    }

    // Close the path if it was marked as closed
    if (polylinePoints && polylinePoints.length > 0) {

      if (polylinePoints[[polylinePoints.length - 1]][0] !== subpath.firstPoint[0] ||
        polylinePoints[[polylinePoints.length - 1]][1] !== subpath.firstPoint[1]) {
        dxf.drawPolyline([polylinePoints[polylinePoints.length - 1], subpath.firstPoint], false);
      }
    }
  });
}

// Helper method to transform a path based on parent position
function transformPath(pathObj, parentLeft, parentTop) {
  const path = pathObj.path.slice();

  // Calculate absolute position of the path in the canvas
  // Extract all x,y coordinates from all command types
  let coordinates = [];
  path.forEach(cmd => {
    if (cmd.length >= 3 && cmd[1] !== undefined && cmd[2] !== undefined) {
      // Handle M and L commands
      coordinates.push({ x: cmd[1], y: cmd[2] });
    }

    if (cmd[0] === 'C' && cmd.length >= 7) {
      // Handle Bézier curve control points
      coordinates.push({ x: cmd[1], y: cmd[2] }); // First control point
      coordinates.push({ x: cmd[3], y: cmd[4] }); // Second control point
      coordinates.push({ x: cmd[5], y: cmd[6] }); // End point
    }

    if (cmd[0] === 'Q' && cmd.length >= 5) {
      // Handle quadratic curve control points
      coordinates.push({ x: cmd[1], y: cmd[2] }); // Control point
      coordinates.push({ x: cmd[3], y: cmd[4] }); // End point
    }
  });

  // Find minimum x and y from all collected coordinates
  const minX = coordinates.length > 0 ? Math.min(...coordinates.map(p => p.x)) : 0;
  const minY = coordinates.length > 0 ? Math.min(...coordinates.map(p => p.y)) : 0;

  let absLeft, absTop;

  // If pathOffset is available, use it for more accurate positioning
  if (pathObj.pathOffset) {
    const centerXOffset = pathObj.originX === 'center' ? 0 : (pathObj.width || 0) / 2;
    const centerYOffset = pathObj.originY === 'center' ? 0 : (pathObj.height || 0) / 2;

    absLeft = parentLeft + pathObj.left + centerXOffset - pathObj.pathOffset.x;
    absTop = parentTop + pathObj.top + centerYOffset - pathObj.pathOffset.y;
  } else {
    absLeft = parentLeft + pathObj.left - minX;
    absTop = parentTop + pathObj.top - minY;
  }

  // Apply transformations directly to path commands
  const transformedPath = {
    type: 'path',
    path: []
  };

  for (let i = 0; i < path.length; i++) {
    const command = path[i][0];
    const values = path[i].slice(1);

    switch (command) {
      case 'M': // moveTo
        if (i !== 0) {
          transformedPath.path.push(['Z']);
        }
        transformedPath.path.push([
          command,
          absLeft + values[0],
          absTop + values[1]
        ]);
        break;
      case 'L': // lineTo
        transformedPath.path.push([
          command,
          absLeft + values[0],
          absTop + values[1]
        ]);
        break;

      case 'C': // bezierCurveTo
        transformedPath.path.push([
          command,
          absLeft + values[0],
          absTop + values[1],
          absLeft + values[2],
          absTop + values[3],
          absLeft + values[4],
          absTop + values[5]
        ]);
        break;

      case 'Q': // quadraticCurveTo
        transformedPath.path.push([
          command,
          absLeft + values[0],
          absTop + values[1],
          absLeft + values[2],
          absTop + values[3]
        ]);
        break;

      case 'Z': // closePath
        transformedPath.path.push([command]);
        break;

      default:
        transformedPath.path.push(path[i].slice());
        break;
    }
  }

  return transformedPath;
}

// Helper function to convert cubic Bézier curve to arc parameters
function cubicBezierToArc(startX, startY, cp1x, cp1y, cp2x, cp2y, endX, endY) {
  // For a cubic Bézier to represent a circular arc, the control points must satisfy specific conditions
  // This function attempts to detect and convert such cases

  // Check if this could be a circular arc by examining control point relationships
  // For a circular arc, the control points should be tangent to the circle at start/end points

  // Calculate tangent vectors at start and end points
  const startTangentX = cp1x - startX;
  const startTangentY = cp1y - startY;
  const endTangentX = cp2x - endX;
  const endTangentY = cp2y - endY;

  // Normalize tangent vectors
  const startTangentLen = Math.sqrt(startTangentX * startTangentX + startTangentY * startTangentY);
  const endTangentLen = Math.sqrt(endTangentX * endTangentX + endTangentY * endTangentY);

  if (startTangentLen < 1e-6 || endTangentLen < 1e-6) return null;

  const startTangentNormX = startTangentX / startTangentLen;
  const startTangentNormY = startTangentY / startTangentLen;
  const endTangentNormX = endTangentX / endTangentLen;
  const endTangentNormY = endTangentY / endTangentLen;

  // For a circular arc, find center by intersecting perpendiculars to tangents
  // Perpendicular to start tangent (normal vector)
  const startNormalX = -startTangentNormY;
  const startNormalY = startTangentNormX;

  // Perpendicular to end tangent (normal vector)  
  const endNormalX = -endTangentNormY;
  const endNormalY = endTangentNormX;

  // Find intersection of two lines:
  // Line 1: start point + t1 * start_normal
  // Line 2: end point + t2 * end_normal

  // Solve: startX + t1 * startNormalX = endX + t2 * endNormalX
  //        startY + t1 * startNormalY = endY + t2 * endNormalY

  const det = startNormalX * endNormalY - startNormalY * endNormalX;

  // If lines are parallel, not a valid arc
  if (Math.abs(det) < 1e-6) return null;

  const dx = endX - startX;
  const dy = endY - startY;

  const t1 = (dx * endNormalY - dy * endNormalX) / det;

  // Calculate center point
  const centerX = startX + t1 * startNormalX;
  const centerY = startY + t1 * startNormalY;

  // Calculate radius from center to start point
  const radius = Math.sqrt((startX - centerX) * (startX - centerX) + (startY - centerY) * (startY - centerY));

  // Verify end point is at same distance (should be for a true arc)
  const endRadius = Math.sqrt((endX - centerX) * (endX - centerX) + (endY - centerY) * (endY - centerY));
  const radiusTolerance = radius * 0.05; // 5% tolerance

  if (Math.abs(radius - endRadius) > radiusTolerance) {
    return null; // Not a good arc approximation
  }

  // Additional validation: check if control points are roughly at the right distance
  // For a circular arc, control points should be at distance ≈ radius * k where k depends on arc angle
  const cp1Dist = Math.sqrt((cp1x - centerX) * (cp1x - centerX) + (cp1y - centerY) * (cp1y - centerY));
  const cp2Dist = Math.sqrt((cp2x - centerX) * (cp2x - centerX) + (cp2y - centerY) * (cp2y - centerY));

  // For small arcs (< 90°), control points should be close to the circle
  // For larger arcs, they can be further out
  const maxControlDist = radius * 2; // Allow some flexibility

  if (cp1Dist > maxControlDist || cp2Dist > maxControlDist) {
    return null; // Control points too far from expected positions
  }
  // Calculate start and end angles
  let startAngle = Math.atan2(startY - centerY, startX - centerX) * 180 / Math.PI;
  let endAngle = Math.atan2(endY - centerY, endX - centerX) * 180 / Math.PI;

  // Determine arc direction by checking which way the curve bends
  // Sample the curve at multiple points to ensure correct direction
  const sampleT1 = 0.25;
  const sampleT2 = 0.75;

  const sampleX1 = Math.pow(1 - sampleT1, 3) * startX + 3 * Math.pow(1 - sampleT1, 2) * sampleT1 * cp1x +
    3 * (1 - sampleT1) * Math.pow(sampleT1, 2) * cp2x + Math.pow(sampleT1, 3) * endX;
  const sampleY1 = Math.pow(1 - sampleT1, 3) * startY + 3 * Math.pow(1 - sampleT1, 2) * sampleT1 * cp1y +
    3 * (1 - sampleT1) * Math.pow(sampleT1, 2) * cp2y + Math.pow(sampleT1, 3) * endY;

  const sampleX2 = Math.pow(1 - sampleT2, 3) * startX + 3 * Math.pow(1 - sampleT2, 2) * sampleT2 * cp1x +
    3 * (1 - sampleT2) * Math.pow(sampleT2, 2) * cp2x + Math.pow(sampleT2, 3) * endX;
  const sampleY2 = Math.pow(1 - sampleT2, 3) * startY + 3 * Math.pow(1 - sampleT2, 2) * sampleT2 * cp1y +
    3 * (1 - sampleT2) * Math.pow(sampleT2, 2) * cp2y + Math.pow(sampleT2, 3) * endY;

  const sampleAngle1 = Math.atan2(sampleY1 - centerY, sampleX1 - centerX) * 180 / Math.PI;
  const sampleAngle2 = Math.atan2(sampleY2 - centerY, sampleX2 - centerX) * 180 / Math.PI;

  // Helper function to normalize angle to [-180, 180]
  const normalizeAngle = (angle) => {
    while (angle > 180) angle -= 360;
    while (angle < -180) angle += 360;
    return angle;
  };

  // Calculate angular progression to determine direction
  const diff1 = normalizeAngle(sampleAngle1 - startAngle);
  const diff2 = normalizeAngle(sampleAngle2 - sampleAngle1);
  const diff3 = normalizeAngle(endAngle - sampleAngle2);

  // Check if angles progress consistently in one direction
  const clockwise = (diff1 < 0 && diff2 < 0 && diff3 < 0);
  const counterClockwise = (diff1 > 0 && diff2 > 0 && diff3 > 0);

  // DXF arcs are always drawn counter-clockwise
  // If the original curve is clockwise, we need to swap start and end angles
  let finalStartAngle = startAngle;
  let finalEndAngle = endAngle;

  if (clockwise) {
    // Swap angles for clockwise curves to make DXF draw them correctly
    finalStartAngle = endAngle;
    finalEndAngle = startAngle;
  } else if (!counterClockwise) {
    // If direction is ambiguous, check total angular span and choose shorter arc
    let totalSpan = normalizeAngle(endAngle - startAngle);

    if (totalSpan < 0) {
      // Original direction is clockwise, swap angles
      finalStartAngle = endAngle;
      finalEndAngle = startAngle;
    }
    // If totalSpan > 0, keep original order (counter-clockwise)
  }

  // Ensure final angles are in [0, 360) range for DXF
  if (finalStartAngle < 0) finalStartAngle += 360;
  if (finalEndAngle < 0) finalEndAngle += 360;

  // Helper function to round to 3 decimal places or integer if no meaningful decimals
  const smartRound = (value) => {
    const rounded = Math.round(value * 1000) / 1000;
    return rounded % 1 === 0 ? Math.round(rounded) : rounded;
  };

  return {
    centerX: smartRound(centerX),
    centerY: smartRound(centerY),
    radius: smartRound(radius),
    startAngle: finalStartAngle,
    endAngle: finalEndAngle
  };
}

function splitToChunkedPaths(pathObjects) {
  // Split path objects into chunks based on character
  const chunkedPaths = [];
  let currentChunk = [];
  let unionPaths;
  pathObjects.path.forEach((pathObj, index) => {
    if (pathObj[0] === 'M' && currentChunk.length > 0) {
      // If we encounter a new 'M' command and current chunk is not empty,
      // push the current chunk to chunkedPaths and start a new one
      chunkedPaths.push(currentChunk);
      currentChunk = [];
    }
    // Add the current path object to the current chunk
    currentChunk.push(pathObj);
  });
  // If there's any remaining paths in the current chunk, add it
  if (currentChunk.length > 0) {
    chunkedPaths.push(currentChunk);
  }

  // Convert each chunk to a path object
  chunkedPaths.map(chunk => {
    let pathString = chunk.map(cmd => {
      if (Array.isArray(cmd)) {
        return cmd.join(' ');
      }
      return cmd;
    }).join(' ');

    if (pathString.slice(-1).toUpperCase() !== 'Z') {
      // Ensure the path ends with 'Z' to close it
      pathString += ' Z';
    }

    if (unionPaths === undefined) {
      // Create Paper.js path
      unionPaths = new paper.CompoundPath(pathString);
      unionPaths.windingRule = 'nonzero';
    } else {
      // Create Paper.js path for the chunk
      const paperPath = new paper.Path(pathString);
      paperPath.windingRule = 'nonzero';
      // Add the chunk path to the union paths
      if (paperPath.clockwise) {
        unionPaths = unionPaths.unite(paperPath);
      } else {
        unionPaths = unionPaths.subtract(paperPath);
      }
    }

  });
  return unionPaths;
}

function calculatePathUnion(pathObjects, char) {
  let pathData;

  // Process each subpath and determine if it should be united or subtracted
  // for some text characters it is better to split them into chunks then union them
  if (SPECIAL_EXPORT_CHAR.includes(char._textChar)) {
    const unionPath = splitToChunkedPaths(pathObjects);
    pathData = unionPath.pathData;
  } else {

    // Convert subpath to path string
    let pathString = pathObjects.path.map(cmd => {
      if (Array.isArray(cmd)) {
        return cmd.join(' ');
      }
      return cmd;
    }).join(' ');

    if (pathString.slice(-1).toUpperCase() !== 'Z') {
      // Ensure the path ends with 'Z' to close it
      pathString += ' Z';
    }

    // Create Paper.js path
    const paperPath = new paper.CompoundPath(pathString);
    paperPath.windingRule = 'nonzero';
    const unionPaths = paperPath.unite(); // Perform union operation

    pathData = unionPaths.pathData;
  }

  let resultPath = [];

  if (pathData) {
    // Parse SVG path data string into array format
    const commands = pathData.match(/[MmLlHhVvCcSsQqTtAaZz][^MmLlHhVvCcSsQqTtAaZz]*/g);
    let currentX = 0, currentY = 0;
    for (let command of commands) {
      const type = command[0];
      const coords = command.slice(1).trim().split(/[\s,]+/).filter(n => n !== '').map(parseFloat);

      switch (type) {
        case 'M':
          currentX = coords[0];
          currentY = coords[1];
          resultPath.push(['M', currentX, currentY]);
          break;
        case 'm':
          currentX += coords[0];
          currentY += coords[1];
          resultPath.push(['M', currentX, currentY]);
          break;
        case 'L':
          currentX = coords[0];
          currentY = coords[1];
          resultPath.push(['L', currentX, currentY]);
          break;
        case 'l':
          const newX = currentX + coords[0];
          const newY = currentY + coords[1];
          resultPath.push(['L', newX, newY]);
          currentX = newX;
          currentY = newY;
          break;
        case 'H':
          currentX = coords[0];
          resultPath.push(['L', currentX, currentY]);
          break;
        case 'h':
          currentX += coords[0];
          resultPath.push(['L', currentX, currentY]);
          break;
        case 'V':
          currentY = coords[0];
          resultPath.push(['L', currentX, currentY]);
          break;
        case 'v':
          currentY += coords[0];
          resultPath.push(['L', currentX, currentY]);
          break;
        case 'C':
          currentX = coords[4];
          currentY = coords[5];
          resultPath.push(['C', coords[0], coords[1], coords[2], coords[3], coords[4], coords[5]]);
          break;
        case 'c':
          const cp1X = currentX + coords[0];
          const cp1Y = currentY + coords[1];
          const cp2X = currentX + coords[2];
          const cp2Y = currentY + coords[3];
          const endX = currentX + coords[4];
          const endY = currentY + coords[5];
          resultPath.push(['C', cp1X, cp1Y, cp2X, cp2Y, endX, endY]);
          currentX = endX;
          currentY = endY;
          break;
        case 'Z':
        case 'z':
          resultPath.push(['Z']);
          break;
      }
    }
  }
  return {
    type: 'path',
    path: resultPath,
    objectType: pathObjects[0]?.objectType // Preserve objectType from input paths
  };
}




/***/ }),

/***/ "./js/i18n/i18n.js":
/*!*************************!*\
  !*** ./js/i18n/i18n.js ***!
  \*************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   i18n: () => (/* binding */ i18n)
/* harmony export */ });
// Simple i18n utility for runtime language switching

// Base English fallback: only include entries where the key is NOT the actual English text.
// This lets us skip a full "en" dictionary and use keys-as-English for simple labels.
const base = {
    // Property panel (English differs from technical key)
  'Left (geom)': 'Left',
  'Top (geom)': 'Top',
  'Right (geom)': 'Right',
  'Bottom (geom)': 'Bottom',
  'Width (geom)': 'Width',
  'Height (geom)': 'Height',
  // Font modal example text
  'e.g., 屯門元朗天水圍': 'e.g., Tuen Mun Yuen Long Tin Shui Wai',
  // Export/donation overlay
  'Donation Thanks Title': 'Thank you for using Road Sign Factory!',
  'Donation Message': 'If you find this tool helpful, please consider supporting its continued development and new features.',
   // Region labels (note: key has a typo kept for compatibility)
  'New Territories': 'New Territories',
};

const dictionaries = {
  zh: {
     // General UI
    'Toggle Sidebar': '切換側欄',
    'Draw Symbol': '繪製符號',
    'Add Text': '新增文字',
    'Add Border': '新增邊框',
    'Add Route Map': '新增路線圖',
    'Measure Tool': '量度工具',
    'Template Signs': '範例標誌',
    'Import/Export': '匯入/匯出',
    'History Tracker': '歷史追蹤',
    'Information': '資訊',
    'Settings': '設定',
    'Loading resources...': '載入資源中…',
    'Follow the cursor': '跟隨游標',
    'Enter': '確定',
    'Cancel': '取消',
    'Canvas Objects': '畫布物件',
    // Context menu
    'Set Anchor': '鎖定圖形',
    'Pivot Anchor': '交換鎖定點',
    'X-axis': 'X 軸',
    'Y-axis': 'Y 軸',
    'Both': '兩者',
    'Edit': '編輯',
    'Delete': '刪除',
    'Property': '屬性',
    // Settings
    'Keyboard Shortcuts': '鍵盤快速鍵',
    'Arrow Keys': '方向鍵',
    'Nudge Selected Object': '移動選取物件',
    'Delete Selected Object': '刪除選取物件',
    'Escape': 'Esc',
    'Cancel Action / Toggle / Close Panel': '取消動作/切換/關閉側欄',
    'Enter (Key)': 'Enter (鍵)',
    'Confirm Input': '確認輸入',
    'Tab': 'Tab',
    'Switch Vertex / Unit': '切換頂點/單位',
    'Ctrl + Z': 'Ctrl + Z',
    'Undo': '復原',
    'Ctrl + S': 'Ctrl + S',
    'Save': '儲存',
    'Show Text Borders': '顯示文字邊框',
    'Show Grid': '顯示格線',
    'Show All Vertices': '顯示所有頂點',
    'Dimension Unit': '尺寸單位',
    'Background Color': '背景色',
    'Grid Color': '格線色',
    'Grid Size': '格線尺寸',
    'Auto Save': '自動儲存',
    'Auto Save Interval (seconds)': '自動儲存間隔（秒）',
    'Save Canvas': '儲存畫布',
    'Clear Saved Canvas': '清除已儲存畫布',
    'Reset Settings': '重設設定',
    'Run Tests': '執行測試',
    'Run Tests on Start': '啟動時執行測試',
    'App Language': '介面語言',
    'English': '英文',
    'Chinese': '中文',
    'Yes': '是',
    'No': '否',
    // Text panel
    'x Height': 'x 高度',
    'mm': '毫米',
    'sw': '筆畫粗細',
    'Enter spacing in X \n (Leave empty if no need for axis):': '輸入X軸間隔(如無需鎖定則留空白):',
    'Enter spacing in Y \n (Leave empty if no need for axis):': '輸入Y軸間隔(如無需鎖定則留空白):',
    'Black': '黑色',
    'White': '白色',
    'Message Colour': '訊息顏色',
    'Text Font': '英文字體',
    'Chinese Font Setting': '中文字體設定',
    'Underline': '底線',
    'Add New Destination': '新增目的地',
    'Language': '語言',
    '2Liner': '雙行',
    'Justification': '對齊方式',
    'Left': '靠左',
    'Middle': '置中',
    'Right': '靠右',
    'Region': '地區',
    '-- Select Location --': '-- 請選擇地點 --',
    'Text input is disabled in 2Liner mode. Select the location in the destination panel.': '雙行模式不支援直接輸入文字，請於目的地面板選擇地點。',
    'Street Name Plate': '街道名稱牌',
    'Eng St Name': '英文街道名稱',
    'Chin St Name': '中文街道名稱',
    'Left Num 1': '左側數字 1',
    'Left Num 2': '左側數字 2',
    'Right Num 1': '右側數字 1',
    'Right Num 2': '右側數字 2',
    'Add Street Name Plate Text': '新增街道名稱牌文字',
    '(optional)': '(選填)',
    // Property  panel
    'Object Properties': '物件屬性',
    'Geometry': '幾何',
    'Basic': '基本',
    'Special': '特殊',
    'Left (geom)': '左座標',
    'Top (geom)': '上座標',
    'Right (geom)': '右座標',
    'Bottom (geom)': '下座標',
    'Width (geom)': '寬度',
    'Height (geom)': '高度',
    'Color': '顏色',
    'Fill Color': '填充顏色',
    'Text': '文字',
    'Font': '字型',
    'Symbol Type': '符號類型',
    'Angle': '角度',
    'Road Type': '道路類型',
    'Main Line': '主線',
    'Roundabout': '迴旋處',
    'Conventional': '傳統',
    'Spiral': '螺旋',
    'Oval': '橢圓',
    'Double': '雙迴旋',
    'Roundel Shape': '迴旋形狀',
    'Approach Length': '引道長度',
    'Exit Length': '出口長度',
    'Route Width': '路徑寬度',
    'Inner Corner Radius': '內彎半徑',
    'Outer Corner Radius': '外彎半徑',
    'Side Roads': '支路段',
    'Parent Road': '主路段',
    'Branch Index': '分支序號',
    'Shape': '形狀',
    'Arrow': '箭頭',
    'Stub': '平頭',
    'RedBar' : '路線封閉',
    'Circular Sign' : '圓形標誌',
    'Circular Sign (with Arrow)' : '圓形標誌(箭頭)',
    'LaneDrop' : '只往出口',
    'T-Junction' : 'T字路口',
    'Y-Junction' : 'Y字路口',
    'Straight' : '直路',
    'Border Type': '邊框類型',
    'Frame Width': '框厚',
    'Width Objects': '寬度物件數',
    'Height Objects': '高度物件數',
    'HDivider Count': '水平分隔數',
    'VDivider Count': '垂直分隔數',
    'BBox': '邊界盒',
    // Font modal
    'Chinese Font Priority Management': '中文字型優先順序管理',
    'Fonts are tried in order from top to bottom.': '字型會依照由上而下的順序嘗試使用。',
    'Upload Custom Font': '上傳自訂字型',
    'Choose Font File': '選擇字型檔案',
    'Character Override Settings': '特殊字元覆蓋設定',
    'Specify characters that should use a specific font. Enter characters directly without spaces or commas.': '指定需使用特定字型的字元，請直接輸入字元，勿加入空白或逗號。',
    'Override Font:': '覆蓋字型：',
    'Characters:': '字元：',
    'e.g., 屯門元朗天水圍': '例如：屯門元朗天水圍',
    'Apply Changes': '套用變更',
    // Import modal
    'Import JSON from Text': '從文字匯入 JSON',
    'Paste your JSON here...': '在此貼上 JSON 內容…',
    'Import': '匯入',
    // Export panel
    'Filename': '檔名',
    'Quality': '品質',
    'PDF Paper Size': 'PDF 紙張尺寸',
    'Scale Multiplier (PNG/SVG)': '縮放倍率（PNG/SVG）',
    'Include Grid': '包含格線',
    'Include Background': '包含背景',
    'Export as PNG': '匯出為 PNG',
    'Export as SVG': '匯出為 SVG',
    'Export as PDF': '匯出為 PDF',
    'Export as DXF (Outline Only)': '匯出為 DXF（僅外框）',
    'Export as JSON': '匯出為 JSON',
    'Import JSON file': '匯入 JSON 檔案',
    'Import JSON text': '匯入 JSON 文字',
    'Exporting': '正在匯出',
    'Donation Thanks Title': '感謝使用 Road Sign Factory！',
    'Donation Message': '如果本工具對你有所幫助，歡迎支持我們持續開發與新增功能。',
    // Border panel
    'Select Color Scheme': '選擇色彩方案',
    'Add Stack Divider': '新增長型標誌分隔線',
    'Add Gantry Divider': '新增架空標誌分隔線',
    'Add Gantry Line': '新增門架水平線',
    'Add Lane Line': '新增行車線分隔線',
    'Select Border Type': '選擇邊框類型',
    'Fixed Width': '固定寬度',
    'Fixed Height': '固定高度',
    '(optional) mm': '(選填) 毫米',
    'Select Template': '選擇範本',
    'Select shape(s) to calculate border width': '選擇圖形用以計算邊框 寬度',
    'Select shape(s) to calculate border height': '選擇圖形用以計算邊框 高度',
  // New border selection and divider placement prompts
  'Select shape(s) to contain inside the border': '選擇要包含於邊框內的圖形',
  'Click inside the border to place divider': '在邊框內點擊以放置分隔線',
    // Border color scheme labels
    'Blue Background': '藍底',
    'Green Background': '綠底',
    'White Background': '白底',
    'White Background - Parking': '白底－停車場',
    'Yellow Background': '黃底',
    'Brown Background': '棕底',
    'Red Background': '紅底',
    // Measure panel
    'Instructions': '使用說明',
    'Measurement Control': '量度控制',
    'Start Measuring': '開始量度',
    'Stop Measuring': '停止量度',
    'Click on vertices to measure distances. First click selects the starting vertex, second click measures to the end vertex.': '點選頂點以量度距離。第一次點選為起點，第二次點選為終點。',
    'Measurement Results': '量度結果',
    'Distance': '距離',
    'From': '起點',
    'To': '終點',
    'Press Enter to continue': '按 Enter 繼續',
    // Map panel
    'Main Road Type': '主路類型',
    'Main Road Shape': '主路形狀',
    'Main Road Width': '主路寬度',
    'Main Road Approach Length': '主路引道長度',
    'Main Road Exit Length': '主路出口長度',
    'Roundabout Type': '迴旋處類型',
    'Roundabout Approach Length': '迴旋處引道長度',
    'Normal': '一般',
    'Auxiliary': '輔助車道',
    'U-turn': '掉頭',
    'Draw Main Road Symbol': '繪製主路符號',
    '+ Another Route Destination': '+ 新增支路目的地',
    'Side Road width': '支路寬度',
    'Side Road Width': '支路寬度',
    'Side Road Shape': '支路形狀',
    // Tracker
    'Clear History': '清除歷程',
    'Undo Mode: Off': '復原模式：關',
    'Undo Mode: On': '復原模式：開',
    'No actions recorded yet': '尚未記錄任何操作',
    'Created': '已建立',
    'Deleted': '已刪除',
    'Modified': '已修改',
    'Anchored': '已錨定',
    'Property Changed': '屬性已變更',
    'Unlocked': '已解除鎖定',
    // Region group labels
    'Hong Kong Island': '香港島',
    'Kowloon': '九龍',
    'New Terriitories': '新界',

    // ===== Hints: Common =====
    'Usage': '用法',
    'Notes': '備註',
    'Design Reference': '設計參考',
    'Quick reference': '快速參考',
    'Scheme': '方案',
    'Background': '底色',
    'Message': '訊息',
    'Typical purpose': '常見用途',
    'When to use each scheme': '各色彩方案的使用時機',
    'Green': '綠色',
    'Blue': '藍色',
    'White': '白色',
    'Black': '黑色',
    'Yellow': '黃色',
    'Brown': '棕色',
    'Red': '紅色',
    'Language': '語言',
    'Font Family': '字型家族',
    'Background Type': '底色類型',
    'Background Colors': '底色',
    'Transport Alphabet': 'Transport 字體',
    'Dark background': '深色底',
    'Light background': '淺色底',
    'Dark Background': '深色底',
    'Light Background': '淺色底',
    'All Backgrounds': '所有底色',
    'All Colors': '所有顏色',
    'Gantry': '門架',
    'Road Side': '路旁',
    'Design Speed': '設計車速',
    'Typical Road Type': '常見道路類型',
    'Advance Information / Direction Sign': '先導資訊／方向標誌',
    'Final Advance Direction Sign': '最終先導方向標誌',
    'Direction Sign': '方向標誌',
    'RCS': 'RCS',
    '≥ 80 km/h': '≥ 80 公里/小時',
    '70–80 km/h': '70–80 公里/小時',
    '50–70 km/h': '50–70 公里/小時',
    '≤ 50 km/h': '≤ 50 公里/小時',
    'Expressway': '快速公路',
    'Trunk Road': '幹道',
    'Primary / District Distributor / Rural': '主幹／分區幹道／鄉郊',
    'Others': '其他',

    // Fallback/help messages
    'No help available for this item.': '此項目暫無說明。',
    'Failed to load help content.': '無法載入說明內容。',

    // ===== Hints: Border Colour Purpose =====
    'Border Colour Purpose': '邊框色彩用途',
    'TPDM Vol.3 – Section 3.2.7 (Colour formats), 3.5.4.8 (Sign Border Details)': 'TPDM 第3卷－第3.2.7節（顏色格式）、3.5.4.8節（標誌邊框詳情）',
    'Signs within expressway; panels directing to expressways.': '設於快速公路範圍內之標誌；或指向快速公路的導向面板。',
    'Non-expressway and regional destination route guidance.': '非快速公路之道路及區域性目的地的路線引導。',
    'Local destination blocks and general information panels.': '本地目的地區塊與一般資訊面板。',
    'Parking direction only border.': '僅用於停車場方向指示的邊框。',
    'Temporary traffic management or special event directions.': '臨時交通管理或特別活動之指示用途。',
    'Tourist attractions and recreational destinations.': '旅遊景點及康樂設施目的地。',
    'Special cases for warning message. Should not be used in normal situations.': '特殊警示用途；一般情況不應使用。',
    'Border colour matches the message colour unless specified (e.g., Dark border background use white message colour).': '邊框顏色通常與訊息字色一致，除非另有指定（例如：深色底邊框使用白色字）。',
    'Message colour follows background luminance: dark backgrounds → white message; light backgrounds → black message.': '訊息字色依背景明暗而定：深色底用白字；淺色底用黑字。',
    'For text alphabet choice, see Message Colour hint (Transport Medium/Heavy usage).': '字體粗細請參見「訊息顏色」提示（對應 Transport Medium/Heavy）。',

    // ===== Hints: Border types =====
    'Exit Border': '出口編號邊框',
    'TPDM Vol.3 - Section 3.5.7.27-3.5.7.33 (Exit Symbols), Section 3.5.4.8 (Sign Border Details)': 'TPDM 第3卷－第3.5.7.27–3.5.7.33節（出口符號）、第3.5.4.8節（標誌邊框詳情）',
    'Special border for exit numbering on signs.': '配合出口編號使用的專用邊框。',

    'Flag Border': '旗型邊框',
    'TPDM Vol.3 - Section 3.5.5.19-3.5.5.21 (Roadside Directional Signs), Section 3.5.4.8 (Sign Border Details), Section 3.5.4.16 (Border Rounding)': 'TPDM 第3卷－第3.5.5.19–3.5.5.21節（路旁方向標誌）、第3.5.4.8節（邊框詳情）、第3.5.4.16節（邊角圓角）',
    'Asymmetric flag-style border used where a pointer or offset legend is required.': '非對稱旗型邊框，適用於需要指示箭角或偏移文字的情況。',

    'Green Panel Border': '綠底面板邊框',
    'TPDM Vol.3 - Section 3.5.4.6 (Green Panel), Section 3.5.4.8 (Sign Border Details)': 'TPDM 第3卷－第3.5.4.6節（綠底面板）、第3.5.4.8節（標誌邊框詳情）',
    'Green background panel variant typically used for directional guidance to expressways.': '綠底面板，多用於指向快速公路的導向資訊。',
    'Use only on signs erected not on expressway.': '僅適用於設置於非快速公路上的標誌。',

    'Panel Border': '白底面板邊框',
    'TPDM Vol.3 - Section 3.2.7.9-3.2.7.12 (Local Destination Panel), Section 3.5.4.8 (Sign Border Details)': 'TPDM 第3卷－第3.2.7.9–3.2.7.12節（本地目的地面板）、第3.5.4.8節（標誌邊框詳情）',
    'Standard rectangular border used for non-regional destination blocks.': '標準矩形邊框，適用於非區域性目的地區塊。',
    'Whether a destination is local or regional refer to Section TPDM Chapter 3.7.': '目的地屬「本地」或「區域」的判別，請參見 TPDM 第3.7章。',

    'Stack Border': '長型邊框',
    'TPDM Vol.3 - Section 3.5.5 (Roadside Directional Signs), Section 3.5.6 (Gantry Signs), Section 3.5.4.8 (Sign Border Details), Section 3.5.4.16 (Border Rounding)': 'TPDM 第3卷－第3.5.5節（路旁方向標誌）、第3.5.6節（門架標誌）、第3.5.4.8節（邊框詳情）、第3.5.4.16節（邊角圓角）',
    'General border format for stack type, gantry type and map type sign.': '適用於長型、門架及地圖式標誌的一般邊框格式。',
    'Combine with Stack Divider when multiple horizontal blocks are needed.': '當需要多層水平區塊時，配合「長型分隔線」。',
    'Also can combine with Gantry Divider for vertical lane sectioning.': '亦可配合「門架分隔線」作垂直車道分區。',

    // ===== Hints: Divider =====
    'Gantry Border Divider': '門架邊框分隔線',
    'Gantry Border Divider Usage': '門架邊框分隔線用法',
    'TPDM Vol.3 - Section 3.5.6 (Gantry Signs), Section 3.5.4.8 (Sign Border Details)': 'TPDM 第3卷－第3.5.6節（門架標誌）、第3.5.4.8節（邊框詳情）',
    'Used to separate content horizontally within gantry-type borders for overhead directional signs': '用於門架邊框內作水平分隔（架空方向標誌）',
    'Each of the divided compartment of gantry shall represent approximate the traffic lane width': '分隔後的每一格高度，宜近似實際車道寬度',
  'Follows center arrangement principles for gantry signs in mainline or lane drop format (Section 3.5.4.10)': '主線或「車道減少」形式的門架標誌，遵循置中編排原則（詳見 3.5.4.10 節）',
    'Select the divider when placing Gantry border to assign the divider height': '放置門架邊框時選取分隔線以設定分隔高度',

    'Lane Separation Line': '車道分隔線',
    'TPDM Vol.3 - Section 3.5.6.14 (Gantry Line), Diagram 3.5.5.20 (Lane Line sign)': 'TPDM 第3卷－第3.5.6.14節（門架水平線）、圖 3.5.5.20（車道線標誌）',
    'When two or more lanes are serving the same destination with more than one location names': '當兩條或以上車道通往同一目的地且目的地名稱超過一個時',
    'Horizontal Line is added to prevent confusion of associating lanes to destinations': '加入水平線以避免車道與目的地對應關係混淆',

    'Lane Separation Line Usage': '車道分隔線用法',
    'TPDM Vol.3 - Section 3.5.6 (Gantry Signs), Section 3.5.4.9 (Border Spacings)': 'TPDM 第3卷－第3.5.6節（門架標誌）、第3.5.4.9節（邊框間距）',
    'Used to separate different lanes vertically within a lane drop sign': '在「車道減少」標誌中，用於垂直分隔不同車道',
    'Select the line when placing Stack border to assign the lane line strokes and height': '放置長型邊框時選取該線以設定線粗與高度',

    // ===== Hints: Route (Map) =====
    'Main Road Shape Guide': '主路形狀指引',
    'TPDM Vol.3 - Section 3.5.5 (Roadside Directional Signs)': 'TPDM 第3卷－第3.5.5節（路旁方向標誌）',
    'Different termination shapes follow TPDM design standards for traffic flow and road geometry:': '各種末端形狀依 TPDM 設計標準，配合交通流向及道路幾何：',
    'Shape selection should align with actual road configuration and driver expectation': '形狀選擇應符合實際道路配置與駕駛者預期',
    'Taper Diverge Junction where Main Road destination is not shown': '主路目的地未顯示之漸縮分岔路口',
    'Taper Diverge Junction where Main Road destination is shown': '主路目的地已顯示之漸縮分岔路口',
    'Main Road destination not shown here for clarity. Refer to text placement hints.': '為清晰起見，此處不顯示主路目的地。請參考文字擺放提示。',
    'Lane Drop Junction': '車道減少路口',
    'Crossroad Junction': '十字路口',

    'Roundabout Shape Guide': '迴旋處形狀指引',
    'Roundabout shape placement according to Diagram 3.5.5.8:': '依圖 3.5.5.8 之迴旋處形狀擺放：',

    // ===== Hints: Symbols =====
    'Airport Symbol': '機場符號',
    'TPDM Vol.3 - Section 3.5.7.22-3.5.7.25 (Airport Symbols), Section 3.5.4.15 (Symbol Spacing)': 'TPDM 第3卷－第3.5.7.22–3.5.7.25節（機場符號）、第3.5.4.15節（符號間距）',
    'Airport Symbol Usage': '機場符號用法',
    'Refer to general symbols (see Tunnel Symbol) for placement and spacing': '擺放與間距請參考通用符號（見隧道符號）',
    'Orientation should rotate to align with destination direction for clarity': '符號方向應轉至與目的地方向一致以提高清晰度',
    'However, airplane direction should never point downwards for safety recognition': '惟飛機機頭方向不應朝下，以免誤導與安全辨識',
  'Spacing: 2 s/w between symbols, 1.5 s/w between message and symbol (Section 3.5.4.15)': '間距：符號彼此 2 s/w；訊息與符號 1.5 s/w（詳見 3.5.4.15 節）',

    'Exit Symbol': '出口符號',
    'TPDM Vol.3 - Section 3.5.7.27-3.5.7.33 (Exit Symbols)': 'TPDM 第3卷－第3.5.7.27–3.5.7.33節（出口符號）',
    'Exit Symbol Usage': '出口符號用法',
    'Used in conjunction with exit numbers to indicate expressway exits': '與出口編號一併使用，表示快速公路之出口',
    'Positioned on the same side of the direction sign as that of the exit on the road': '應位於與道路實際出口同側之指示牌一側',

    'Expressway Symbol': '快速公路符號（綠底面板）',
    'TPDM Vol.3 - Section 3.5.7.18-3.5.7.21 (Expressway Symbols), Section 3.5.4.6 (Green Panels)': 'TPDM 第3卷－第3.5.7.18–3.5.7.21節（快速公路符號）、第3.5.4.6節（綠底面板）',
    'Expressway symbols should only be used for signs located not in an expressway.': '此符號只適用於設置於「非」快速公路之標誌。',
    'i.e. the sign is in blue background and contains a green panel directing to the expressway.': '即：標誌為藍底，並含有指向快速公路的綠底面板。',
    'This sign is used to warn motorist the start of expressway, which additional regulations will be applied.': '用以提示將進入快速公路，屆時會有額外交通規例生效。',
    'Placement inside green panel:': '放置於綠底面板內：',

    'Red Expressway Symbol': '快速公路符號（紅色，門架）',
    'TPDM Vol.3 - Section 3.5.6 (Gantry Signs), Section 3.5.6.11-18 (Expressway Symbol Placement)': 'TPDM 第3卷－第3.5.6節（門架標誌）、第3.5.6.11–18節（快速公路符號擺放）',
    'Red Expressway symbols should only be used for signs located on the expressway.': '紅色快速公路符號僅用於設置於快速公路上的標誌。',
    'i.e. the sign is in green background.': '即：標誌為綠底。',
    'The Expressway symbols should normally be located in the top right-hand corner of the sign': '符號一般位於標誌之右上角',
    'Whenever there are more than one panel on the gantry for lane drop or diverge, the symbol need only be located on the mainline part of the sign.': '若門架上有多塊面板（如車道減少或分流），僅需置於主線部分之面板即可。',
    'Placement above map type sign:': '置於地圖式標誌之上：',
    'Placement to the right of text in gantry sign:': '置於門架標誌文字右側：',
    'Placement to the left of text in gantry sign:': '置於門架標誌文字左側：',
    'Placement to the right of text in wide gantry sign:': '置於寬門架標誌文字右側：',

    'Gantry Type Sign Arrow Symbol': '門架式箭頭符號',
    'TPDM Vol.3 - Section 3.5.6 (Gantry Signs), Section 3.5.6.7 (Symbol Placement)': 'TPDM 第3卷－第3.5.6節（門架標誌）、第3.5.6.7節（符號擺放）',
    'Gantry Arrow Usage': '門架箭頭用法',
    'Downward pointing arrows must be positioned above the center of the traffic lane they refer to': '向下箭頭必須位於所指車道的正上方',
    'Used in gantry signs for lane guidance and direction indication': '用於門架標誌的車道引導與方向指示',
    'Placement below Horizontal line:': '置於水平線下：',

    'Stack Type Sign Arrow Symbol': '長型標誌箭頭符號',
    'TPDM Vol.3 - Section 3.5.5.22-3.5.5.31 (Stack Type Signs), Section 3.5.4.15 (Symbol Spacing)': 'TPDM 第3卷－第3.5.5.22–3.5.5.31節（長型標誌）、第3.5.4.15節（符號間距）',
    'Stack Arrow Symbol Usage': '長型箭頭用法',
    'Used in roadside stack-type directional signs for indicating direction of travel': '用於路旁長型方向標誌指示行駛方向',
    'Placement above Route Shield for Horizontal Arrows:': '水平箭頭置於路線盾形標誌之上：',
    'Placement when used in lane lines sign:': '用於車道線標誌時之擺放：',

    'Left Pedestrian, Disabled and Cycle Symbol': '行人（左向）、殘疾人士與單車符號',
    'TPDM Vol.3 - Section 3.5.5.34-3.5.5.35 (Pedestrian and Disabled Symbols), Section 3.5.5.47-3.5.5.49 (Cycle Symbols), Section 6.4 (Cycling signs)': 'TPDM 第3卷－第3.5.5.34–3.5.5.35節（行人與殘疾人士符號）、第3.5.5.47–3.5.5.49節（單車符號）、第6.4節（單車設施標誌）',
    'Left Pedestrian Symbol Usage': '行人（左向）符號用法',
    'Used to indicate pedestrian facilities or pedestrian-related destinations': '用以表示行人設施或與行人相關之目的地',
    'Typically placed adjacent to text indicating pedestrian areas or walkways': '通常置於顯示行人區或步行道之文字旁',
    'Symbol facing left or right should align with the sign direction': '符號朝向（左/右）應與指示方向一致',

    'Regulatory sign Symbol': '規例標誌符號',
    'TPDM Vol.3 - Section 3.5.5.13 (Route Symbol Arrangement), Section 3.5.7.42-3.5.5.46 (Cycle Symbols), Section 6.4 (Traffic Sign Symbols)': 'TPDM 第3卷－第3.5.5.13節（路線符號編排）、第3.5.7.42–3.5.5.46節（單車符號）、第6.4節（交通符號）',
    'Regulatory Sign Symbol Usage': '規例標誌符號用法',
    'Used to indicate restriction of the road ahead': '表示前方道路之限制',
    'Restrictions applied might be height, width, no turning left/right, no entry': '可能包括高度/寬度限制、禁止左/右轉、禁止進入等',

    'Route Shield Symbol': '路線盾形標誌',
    'TPDM Vol.3 - Section 3.5.7.2-3.5.7.9 (Route Shield Symbol), Section 3.5.4.15 (Symbol Spacing)': 'TPDM 第3卷－第3.5.7.2–3.5.7.9節（路線盾形）、第3.5.4.15節（符號間距）',
    'Route Shield Symbol Usage': '路線盾形標誌用法',
    'Used to identify specific route numbers and highway designations': '用以識別特定路線編號及道路等級',
    'Can be positioned adjacent to text, below text, or with directional arrows': '可置於文字旁、文字下方，或與方向箭頭搭配',
  'Spacing: 1.5 s/w bottom spacing extension applies to route shields (Section 3.5.4.9)': '下方延伸間距 1.5 s/w 適用於路線盾形（詳見 3.5.4.9 節）',
    'Symbol spacing: 2 s/w between symbols, proper alignment with destination text': '符號間距 2 s/w，並與目的地文字妥善對齊',
    'Placement below text': '置於文字下方',
    'Placement below another symbol': '置於另一符號之下',
    'Placement at the end of main road destination in map type sign': '置於地圖式標誌主路目的地文字之末端',
    'Placement to the side road destination in map type sign': '置於地圖式標誌支路目的地文字旁',
    'Placement with the stack arrow in stack type sign': '於長型標誌與箭頭同置',
    'Alternative placement with the stack arrow in stack type sign': '長型標誌與箭頭之替代擺放方式',
    'Placement with the gantry arrow in gantry type sign': '於門架標誌與箭頭同置',

    'General Symbol': '通用符號',
    'TPDM Vol.3 - Section 3.5.4.15 (Symbol Placement), Section 3.5.7.10-3.5.7.17 (Tunnel Symbols), Section 3.5.7.26 (MTR Symbol), Section 3.5.7.41 (Hospital Symbol)': 'TPDM 第3卷－第3.5.4.15節（符號擺放）、第3.5.7.10–3.5.7.17節（隧道符號）、第3.5.7.26節（港鐵符號）、第3.5.7.41節（醫院符號）',
    'Applicable for Tunnel, Airport, Hospital, Parking, Disney, MTR symbols': '適用於隧道、機場、醫院、停車、迪士尼、港鐵等符號',
    'Symbol placement follows general symbol guidelines with proper spacing and alignment': '符號擺放遵循通用規範並保持適當間距與對齊',
    'Placement next to one line of Text in stack type sign:': '長型標誌單行文字旁之擺放：',
    'Placement next to one line of Text in flag type sign:': '旗型標誌單行文字旁之擺放：',
    'Placement there are multiple lines of Text:': '多行文字之擺放：',
    'Alternative placement there are multiple lines of Text:': '多行文字之替代擺放：',
    'Placement there are multiple logos:': '多個圖標之擺放：',
    'Placement next to Text in gantry sign:': '門架標誌文字旁之擺放：',
    'Placement to the main road destination in map type sign:': '地圖式標誌主路目的地之擺放：',
    'Placement to the side road destination in map type sign:': '地圖式標誌支路目的地之擺放：',
    'Another placement to the side road destination in map type sign:': '地圖式標誌支路目的地之另一擺放：',

    // ===== Hints: Text =====
    'Message Colour Selection': '訊息顏色選擇',
  'TPDM Vol.3 – Section 3.5.3, 3.5.4.6–3.5.4.7; Colour formats per 3.2.7.1': 'TPDM 第3卷－第3.5.3、3.5.4.6–3.5.4.7；顏色格式詳見 3.2.7.1 節',
    'Choose text colour based on the panel/background': '依面板/背景選擇文字顏色',
    'Select the message colour to maintain contrast with the sign panel (border/background). Use this table:': '選擇訊息字色以維持與面板（邊框/背景）的對比。參考下表：',
    'Typical Background Colours': '常見底色',
    'Transport Medium': 'Transport 中黑',
    'Transport Heavy': 'Transport 粗黑',
    'For bilingual signs, follow the same contrast rule for both languages.': '雙語標誌亦應遵照同一對比原則。',
    'Green/Blue panels (e.g., expressway, trunk): use White text.': '綠/藍底面板（如快速公路、幹道）：使用白字。',
    'White/Yellow panels (local/temporary): use Black text.': '白/黃底面板（本地/臨時）：使用黑字。',
    'Refer to TPDM for full colour formats and panel rules. See also 3.5.7 for symbol colour behaviour on green panels.': '完整顏色格式與面板規則請參見 TPDM；另見 3.5.7（綠底面板上之符號顏色表現）。',

    'Text Placement Guide': '文字擺放指引',
    'TPDM Vol.3 - Section 3.5.4.1-3.5.4.5 (Legend Blocks), Section 3.5.4.10 (Alignment)': 'TPDM 第3卷－第3.5.4.1–3.5.4.5節（文字區塊）、第3.5.4.10節（對齊）',
    'Text Placement Guidelines': '文字擺放原則',
    'Text placement follows TPDM specifications for legend block formation and message alignment:': '文字擺放遵循 TPDM 對文字區塊與編排之規範：',
  'Words are formed by butting tiles together with 2 s/w space between words (Section 3.5.4.1)': '英文以字磚相接組成；單字間距 2 s/w（詳見 3.5.4.1 節）',
  'English messages are butted above corresponding Chinese messages (Section 3.5.4.3)': '英文置於相應中文之上方（詳見 3.5.4.3 節）',
  'Alignment: Left for stack/flag/map type signs, center for gantry signs (Section 3.5.4.10)': '對齊：長型/旗型/地圖式採靠左；門架採置中（詳見 3.5.4.10 節）',
    'Diverging Map Type Sign without Main Road Destination': '地圖式分流標誌（不含主路目的地）',
    'Diverging Map Type Sign with Main Road Route Number and expressway symbol': '地圖式分流標誌（含主路路線編號與快速公路符號）',
    'Diverging Map Type Sign with Main Road Destination': '地圖式分流標誌（含主路目的地）',
    'Roundabout Map Type Sign left and top destinations': '迴旋處地圖式標誌（左/上方向目的地）',
    'Roundabout Map Type Sign right and u-turn destinations': '迴旋處地圖式標誌（右/掉頭方向目的地）',
    'Stack Type Sign': '長型標誌',
    'Text above destination panel block (applicable both white and green panel)': '文字置於目的地面板上方（適用白底與綠底面板）',
    'Text below destination panel block (applicable both white and green panel)': '文字置於目的地面板下方（適用白底與綠底面板）',
    'Text placement in gantry sign': '門架標誌中文字擺放',
    'Alternative placement in gantry sign to reduce width': '門架標誌之替代擺放（縮減寬度）',

    'Text Font Guide': '文字字型指引',
    'TPDM Vol.3 - Section 3.5.3 (Lettering and Character Standards), Section 3.5.4.6-3.5.4.7 (Panel Typography)': 'TPDM 第3卷－第3.5.3節（字體及字符標準）、第3.5.4.6–3.5.4.7節（面板字體）',
    'Transport Font Family Selection (TPDM Vol.3 Section 3.5.3)': 'Transport 字型選擇（TPDM 第3卷 3.5.3）',
    'Font selection follows TPDM specifications for optimal legibility and consistency:': '字型選擇遵循 TPDM 規範，以確保可讀性與一致性：',
  'Dark colour panels use Transport Medium alphabet with white color (Section 3.5.4.6)': '深色底面板使用 Transport Medium 白字（詳見 3.5.4.6 節）',
  'Light colour use Transport Heavy alphabet with black color (Section 3.5.4.7)': '淺色底面板使用 Transport Heavy 黑字（詳見 3.5.4.7 節）',
    'Avector Chinese True Type Fonts (Hong Kong) - BlackBold': '全真字庫（港人版）- 粗黑',
    'Avector HK BlackBold (Chinese)': '全真字庫（港人版）- 粗黑',

    'x-Height Input Guide': 'x 高度輸入指南',
    'TPDM Vol.3 - Section 3.2 (Lettering Dimensions), Table 3.2.5.1': 'TPDM 第3卷－第3.2節（字高），表 3.2.5.1',
    'What is x-height?': '何謂 x 高度？',
    'x-height description': 'x 高度為交通標誌英文字母（Transport 字體）的小寫名義高度。此值決定整體設計尺寸，並與筆畫寬（s/w）共同用於換算實際標誌尺寸。常見關係式：mm = x 高度 × 筆畫寬 ÷ 4。',
    'How to choose x-height': '如何選擇 x 高度',
    'Refer to TPDM Table 3.2.5.1 for recommended x-heights by design speed and sign type. Summary:': '請參考 TPDM 表 3.2.5.1 按設計車速與標誌類型之建議 x 高度：',
  'X-height notes': '備註（摘自表下）：x 高度主要由設計車速決定；無括號數值為建議值；括號內為受限制時之較小替代值。中間值可依 5 mm 進位（詳見 3.2.5.2–3 節），另有例外情況。',
    'Minimum clear visibility': '最小清晰可見距離',
  'Table 3.2.5.1 also specifies minimum clear visibility distances for each case. Ensure siting achieves at least the stated minimums; strive for more where feasible (see 3.2.5.4–3.2.5.7).': '表 3.2.5.1 同時列出各情況之最小清晰可見距離。設置位置應至少達到所列最小值，並在可行情況下爭取更大距離（詳見 3.2.5.4–3.2.5.7 節）。',

    'Tunnel Closed Symbol': '隧道封閉符號',
    'TPDM Vol.3 - Section 4.5.5 (Symbol Placement)': 'TPDM 第三卷 - 第 4.5.5 節（符號位置）',
    'Applicable for Tunnel Symbol when the sign is within Trunk Road or Primary Distributor Road': '適用於位於主幹道或市區幹道標誌上的隧道符號',
    'Placement in gantry sign with flashing light above and outside sign:': '門架標誌上的位置（閃燈位於標誌上外方）：',
    'Placement with flashing light together within sign:': '與閃燈同時位於標誌內的位置：',
  },
};

let currentLocale = 'en';

const i18n = {
  t(key) {
    if (!key) return '';
    const dict = dictionaries[currentLocale] || {};
    // Prefer current locale, then base (English), then the key itself
    return dict[key] || base[key] || key;
  },
  setLocale(locale) {
    // Allow 'en' even without a dictionary (it will use base/keys)
    if (locale === 'en' || (locale && dictionaries[locale])) {
      currentLocale = locale;
    } else {
      currentLocale = 'en';
    }
  },
  getLocale() {
    return currentLocale;
  },
  applyTranslations(root = document) {
    if (!root) return;
    // Text content
    root.querySelectorAll('[data-i18n]')?.forEach(el => {
      const key = el.getAttribute('data-i18n');
      if (key) el.textContent = i18n.t(key);
    });
    // HTML content
    root.querySelectorAll('[data-i18n-html]')?.forEach(el => {
      const key = el.getAttribute('data-i18n-html');
      if (key) el.innerHTML = i18n.t(key);
    });
    // Placeholders
    root.querySelectorAll('[data-i18n-placeholder]')?.forEach(el => {
      const key = el.getAttribute('data-i18n-placeholder');
      if (key) el.setAttribute('placeholder', i18n.t(key));
    });
    // Tooltips (data-tooltip and title)
    root.querySelectorAll('[data-i18n-tooltip]')?.forEach(el => {
      const key = el.getAttribute('data-i18n-tooltip');
      if (key) {
        const val = i18n.t(key);
        el.setAttribute('data-tooltip', val);
        el.setAttribute('title', val);
      }
    });
  }
};



// Also expose globally for inline scripts that cannot import modules
if (typeof window !== 'undefined') {
  window.i18n = i18n;
}


/***/ }),

/***/ "./js/main.js":
/*!********************!*\
  !*** ./js/main.js ***!
  \********************/
/***/ ((module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.a(module, async (__webpack_handle_async_dependencies__, __webpack_async_result__) => { try {
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./canvas/keyboardEvents.js */ "./js/canvas/keyboardEvents.js");
/* harmony import */ var _canvas_mouseEvents_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./canvas/mouseEvents.js */ "./js/canvas/mouseEvents.js");
/* harmony import */ var _canvas_touchEvents_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./canvas/touchEvents.js */ "./js/canvas/touchEvents.js");
/* harmony import */ var _objects_vertex_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./objects/vertex.js */ "./js/objects/vertex.js");
/* harmony import */ var _objects_draw_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./objects/draw.js */ "./js/objects/draw.js");
/* harmony import */ var _objects_template_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./objects/template.js */ "./js/objects/template.js");
/* harmony import */ var _objects_symbols_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./objects/symbols.js */ "./js/objects/symbols.js");
/* harmony import */ var _objects_text_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./objects/text.js */ "./js/objects/text.js");
/* harmony import */ var _objects_routeBase_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./objects/routeBase.js */ "./js/objects/routeBase.js");
/* harmony import */ var _objects_mainRoute_js__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./objects/mainRoute.js */ "./js/objects/mainRoute.js");
/* harmony import */ var _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./objects/sideRoute.js */ "./js/objects/sideRoute.js");
/* harmony import */ var _objects_border_js__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./objects/border.js */ "./js/objects/border.js");
/* harmony import */ var _objects_divider_js__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ./objects/divider.js */ "./js/objects/divider.js");
/* harmony import */ var _objects_anchor_js__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ./objects/anchor.js */ "./js/objects/anchor.js");
/* harmony import */ var _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ./canvas/Tracker.js */ "./js/canvas/Tracker.js");
/* harmony import */ var _sidebar_sidebar_js__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ./sidebar/sidebar.js */ "./js/sidebar/sidebar.js");
/* harmony import */ var _tests_test_js__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! ./tests/test.js */ "./js/tests/test.js");
/* harmony import */ var _sidebar_tooltip_js__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! ./sidebar/tooltip.js */ "./js/sidebar/tooltip.js");
/* harmony import */ var _sidebar_tooltip_js__WEBPACK_IMPORTED_MODULE_18___default = /*#__PURE__*/__webpack_require__.n(_sidebar_tooltip_js__WEBPACK_IMPORTED_MODULE_18__);
/* harmony import */ var _sidebar_property_js__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! ./sidebar/property.js */ "./js/sidebar/property.js");
/* harmony import */ var _preload_js__WEBPACK_IMPORTED_MODULE_20__ = __webpack_require__(/*! ./preload.js */ "./js/preload.js");
var __webpack_async_dependencies__ = __webpack_handle_async_dependencies__([_preload_js__WEBPACK_IMPORTED_MODULE_20__]);
_preload_js__WEBPACK_IMPORTED_MODULE_20__ = (__webpack_async_dependencies__.then ? (await __webpack_async_dependencies__)() : __webpack_async_dependencies__)[0];
// Import canvas.js as a classic script to ensure global variables are set up





// Import other objects modules (these will use the global canvas)
// import './objects/path.js'; // Removed generic import, using specific above










// Import anchor and tracker logic if needed


// Import sidebar logic (UI, event handlers)

// Import any additional utilities or test logic





(0,_preload_js__WEBPACK_IMPORTED_MODULE_20__.preload)();

__webpack_async_result__();
} catch(e) { __webpack_async_result__(e); } });

/***/ }),

/***/ "./js/modal/md-font.js":
/*!*****************************!*\
  !*** ./js/modal/md-font.js ***!
  \*****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   FontPriorityManager: () => (/* binding */ FontPriorityManager)
/* harmony export */ });
/* harmony import */ var _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./mdGeneral.js */ "./js/modal/mdGeneral.js");
/* harmony import */ var _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../sidebar/sbGeneral.js */ "./js/sidebar/sbGeneral.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../i18n/i18n.js */ "./js/i18n/i18n.js");
/**
 * md-font.js - Font Priority Management Modal
 * This module handles the Chinese font priority management functionality
 */






const FontPriorityManager = {
  fontPriorityList: ['parsedFontKorean', 'parsedFontChinese', 'parsedFontChocolate', 'parsedFontHK'], // Default priority
  /**
   * Initialize font priority system
   */
  initialize: function () {
    FontPriorityManager.loadFontPriorityFromStorage();

  },

  /**
   * Show the font priority management modal
   */
  showModal: function () {
    FontPriorityManager.currentOverrideRules = null; // Reset temporary rules
    const modalId = 'font-priority-modal';
  const { modal, modalContent } = _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.createModal(modalId, 'Chinese Font Priority Management');

    // Info text
  const infoText = _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.createInfoText('Fonts are tried in order from top to bottom.');

    // Font list container
    const fontListContainer = document.createElement('div');
    fontListContainer.className = 'font-list-container';
    fontListContainer.id = 'font-list-container';

    // Populate font list
    FontPriorityManager.updateFontListDisplay(fontListContainer);

    // Upload font section
  const uploadSection = _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.createSection('upload-section');

  const uploadTitle = document.createElement('h4');
  uploadTitle.setAttribute('data-i18n', 'Upload Custom Font');
  uploadTitle.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_3__.i18n.t('Upload Custom Font');
    const uploadInput = document.createElement('input');
    uploadInput.type = 'file';
    uploadInput.accept = '.ttf,.otf,.woff';
    uploadInput.className = 'font-upload-input';
    uploadInput.onchange = FontPriorityManager.handleFontUpload;

  const uploadButton = _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.createButton('Choose Font File', 'upload-button', () => uploadInput.click());

    uploadSection.appendChild(uploadTitle);
    uploadSection.appendChild(uploadButton);
    uploadSection.appendChild(uploadInput);

    // Character Override section
    const overrideSection = _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.createSection('override-section');

    const overrideTitle = document.createElement('h4');
    overrideTitle.setAttribute('data-i18n', 'Character Override Settings');
    overrideTitle.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_3__.i18n.t('Character Override Settings');
    
    const overrideInfo = _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.createInfoText('Specify characters that should use a specific font. You can add multiple rules.');

    const rulesContainer = document.createElement('div');
    rulesContainer.id = 'override-rules-container';
    rulesContainer.className = 'override-rules-container';

    // Function to render rules
    const renderRules = () => {
        rulesContainer.innerHTML = '';
        const rules = FontPriorityManager.currentOverrideRules || FontPriorityManager.getOverrideRules();
        FontPriorityManager.currentOverrideRules = rules; // Cache for editing

        rules.forEach((rule, index) => {
            const ruleDiv = document.createElement('div');
            ruleDiv.className = 'override-rule';
            ruleDiv.style.display = 'flex';
            ruleDiv.style.gap = '10px';
            ruleDiv.style.marginBottom = '10px';
            ruleDiv.style.alignItems = 'center';

            // Font Select
            const fontSelect = document.createElement('select');
            fontSelect.className = 'font-select';
            FontPriorityManager.populateFontSelect(fontSelect);
            fontSelect.value = rule.font;
            fontSelect.onchange = (e) => {
                rule.font = e.target.value;
            };

            // Char Input
            const charInput = document.createElement('input');
            charInput.type = 'text';
            charInput.className = 'char-input';
            charInput.value = rule.characters;
            charInput.placeholder = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_3__.i18n.t('Characters');
            charInput.style.flex = '1';
            charInput.oninput = (e) => {
                rule.characters = e.target.value;
            };

            // Remove Button
            const removeBtn = document.createElement('button');
            removeBtn.textContent = '×';
            removeBtn.className = 'remove-button';
            removeBtn.onclick = () => {
                rules.splice(index, 1);
                renderRules();
            };

            ruleDiv.appendChild(fontSelect);
            ruleDiv.appendChild(charInput);
            ruleDiv.appendChild(removeBtn);
            rulesContainer.appendChild(ruleDiv);
        });
    };

    renderRules();

    const addRuleBtn = _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.createButton('Add Rule', 'add-rule-button', () => {
        if (!FontPriorityManager.currentOverrideRules) {
             FontPriorityManager.currentOverrideRules = FontPriorityManager.getOverrideRules();
        }
        FontPriorityManager.currentOverrideRules.push({ font: 'parsedFontChinese', characters: '' });
        renderRules();
    });
    addRuleBtn.style.marginTop = '10px';

    overrideSection.appendChild(overrideTitle);
    overrideSection.appendChild(overrideInfo);
    overrideSection.appendChild(rulesContainer);
    overrideSection.appendChild(addRuleBtn);

    // Create apply button using ModalUtils
    const applyButton = _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.createButton('Apply Changes', 'apply-button', () => {
      // Save character override settings
      if (FontPriorityManager.currentOverrideRules) {
          FontPriorityManager.saveOverrideRules(FontPriorityManager.currentOverrideRules);
      }

      // Apply other changes and close modal
      FontPriorityManager.applyChanges();
      _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.removeModal('font-priority-modal');
    });

    // Create buttons container using ModalUtils
    const buttonsContainer = _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.createButtonsContainer([applyButton]);

    // Assemble modal
    modalContent.appendChild(infoText);
    modalContent.appendChild(fontListContainer);
    modalContent.appendChild(uploadSection);
    modalContent.appendChild(overrideSection);
    modalContent.appendChild(buttonsContainer);

    // Show modal
    _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.showModal(modal);
  },

  /**
   * Update the font list display in the modal
   */
  updateFontListDisplay: function (container) {
    container.innerHTML = '';

    // Validate and clean up font priority list before displaying
    const validFonts = [];
    let listChanged = false;

    FontPriorityManager.fontPriorityList.forEach((fontName) => {
      // Check if it's a built-in font
      const isBuiltInFont = ['parsedFontChinese', 'parsedFontChocolate', 'parsedFontHK', 'parsedFontKorean', 'parsedFontMedium', 'parsedFontHeavy', 'parsedFontKai'].includes(fontName);

      if (isBuiltInFont) {
        validFonts.push(fontName);
      } else {
        // For custom fonts, check if they still exist in window object
        if (window[fontName]) {
          validFonts.push(fontName);
        } else {
          // Font no longer exists, mark for removal
          console.warn(`Custom font "${fontName}" no longer exists in window object, removing from list`);
          listChanged = true;
        }
      }
    });

    // Update the font priority list if any fonts were removed
    if (listChanged) {
      FontPriorityManager.fontPriorityList = validFonts;
      FontPriorityManager.saveFontPriorityToStorage();
    }

    // Display the validated font list
    validFonts.forEach((fontName, index) => {
      const fontItem = document.createElement('div');
      fontItem.className = 'font-item';
      fontItem.dataset.fontName = fontName;

      const fontLabel = document.createElement('span');
      fontLabel.className = 'font-label';
      fontLabel.textContent = FontPriorityManager.getFontDisplayName(fontName);

      const controlsContainer = document.createElement('div');
      controlsContainer.className = 'font-controls';

      // Up arrow
      const upButton = document.createElement('button');
      upButton.textContent = '↑';
      upButton.className = 'move-button';
      upButton.disabled = index === 0;
      upButton.onclick = () => FontPriorityManager.moveFontUp(index);

      // Down arrow
      const downButton = document.createElement('button');
      downButton.textContent = '↓';
      downButton.className = 'move-button';
      downButton.disabled = index === FontPriorityManager.fontPriorityList.length - 1;
      downButton.onclick = () => FontPriorityManager.moveFontDown(index);

      // Remove button (only for custom fonts)
      if (fontName.startsWith('custom_')) {
        const removeButton = document.createElement('button');
        removeButton.textContent = '×';
        removeButton.className = 'remove-button';
        removeButton.onclick = () => FontPriorityManager.removeCustomFont(fontName);
        controlsContainer.appendChild(removeButton);
      }

      controlsContainer.appendChild(upButton);
      controlsContainer.appendChild(downButton);

      fontItem.appendChild(fontLabel);
      fontItem.appendChild(controlsContainer);
      container.appendChild(fontItem);
    });
  },

  /**
   * Get display name for font
   */
  getFontDisplayName: function (fontName) {
    const displayNames = {
      'parsedFontKorean': 'Noto Sans KR (Default)',
      'parsedFontChinese': 'Noto Sans Trad Chinese',
      'parsedFontChocolate': 'Noto Sans Chocolate',
      'parsedFontHK': 'Noto Sans HK',
    };

    if (fontName.startsWith('custom_')) {
      return fontName.replace('custom_', 'Custom: ');
    }

    return displayNames[fontName] || fontName;
  },

  /**
   * Move font up in priority list
   */
  moveFontUp: function (index) {
    if (index > 0) {
      const list = FontPriorityManager.fontPriorityList;
      [list[index - 1], list[index]] = [list[index], list[index - 1]];
      FontPriorityManager.updateFontListDisplay(document.getElementById('font-list-container'));
    }
  },

  /**
   * Move font down in priority list
   */
  moveFontDown: function (index) {
    const list = FontPriorityManager.fontPriorityList;
    if (index < list.length - 1) {
      [list[index], list[index + 1]] = [list[index + 1], list[index]];
      FontPriorityManager.updateFontListDisplay(document.getElementById('font-list-container'));
    }
  },

  /**
   * Handle font file upload
   */  handleFontUpload: function (event) {
    const file = event.target.files[0];
    if (!file) return;

    const reader = new FileReader();
    reader.onload = function (e) {
      try {
        // Parse the font using opentype.js
        const font = opentype.parse(e.target.result);
        const customFontName = `custom_${file.name.replace(/\.[^/.]+$/, "")}`;

        // Store the font globally (you might want to add this to your font storage system)
        window[customFontName] = font;

        // Add to priority list
        FontPriorityManager.fontPriorityList.unshift(customFontName);

        // Update display
        FontPriorityManager.updateFontListDisplay(document.getElementById('font-list-container'));

        // Check for text objects that need this font and update them
        FontPriorityManager.updateTextObjectsWithUploadedFont(customFontName);

        // Save the updated priority list
        FontPriorityManager.saveFontPriorityToStorage();

      } catch (error) {
        console.error('Error parsing font:', error);
        if (_sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralHandler && _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralHandler.showToast) {
          _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralHandler.showToast(
            'Failed to parse font file. Please ensure it\'s a valid font file.',
            'warning',
            4000
          );
        } else {
          alert('Failed to parse font file. Please ensure it\'s a valid font file.');
        }
      }
    };
    reader.readAsArrayBuffer(file);
  },

  /**
   * Remove custom font from priority list
   */
  removeCustomFont: function (fontName) {
    const index = FontPriorityManager.fontPriorityList.indexOf(fontName);
    if (index > -1) {
      FontPriorityManager.fontPriorityList.splice(index, 1);
      // Remove from global scope
      if (window[fontName]) {
        delete window[fontName];
      }
      FontPriorityManager.updateFontListDisplay(document.getElementById('font-list-container'));
    }
  },
  /**
   * Apply font priority changes
   */
  applyChanges: function () {
    // Store in localStorage for persistence
    FontPriorityManager.saveFontPriorityToStorage();

    console.log('Font priority updated:', FontPriorityManager.fontPriorityList);
  },
  /**
   * Save font priority to localStorage
   */
  saveFontPriorityToStorage: function () {
    localStorage.setItem('fontPriorityList', JSON.stringify(FontPriorityManager.fontPriorityList));
    // Validate fonts after saving
    setTimeout(() => FontPriorityManager.validateFonts(), 100);
  },

  /**
   * Load font priority from localStorage
   */
  loadFontPriorityFromStorage: function () {
    const stored = localStorage.getItem('fontPriorityList');
    if (stored) {
      try {
        FontPriorityManager.fontPriorityList = JSON.parse(stored);
        // Validate fonts after loading
        setTimeout(() => FontPriorityManager.validateFonts(), 100);
      } catch (e) {
        console.warn('Failed to parse stored font priority list');
      }
    }
  },

  /**
   * Get the current font priority list for use by other modules
   */
  getFontPriorityList: function () {
    return FontPriorityManager.fontPriorityList;
  },
  /**
   * Get override rules from localStorage
   * Migrates old format if necessary
   */
  getOverrideRules: function () {
    const storedRules = localStorage.getItem('overrideRules');
    if (storedRules) {
      try {
        return JSON.parse(storedRules);
      } catch (e) {
        console.warn('Failed to parse override rules');
        return [];
      }
    }

    // Migration from old format
    const oldChars = localStorage.getItem('specialCharacters');
    const oldFont = localStorage.getItem('overrideFont');
    
    if (oldChars || oldFont) {
      const rules = [{
        font: oldFont || 'parsedFontChinese',
        characters: oldChars || '彩天輸勝都愉朗'
      }];
      // Save in new format and clear old
      FontPriorityManager.saveOverrideRules(rules);
      localStorage.removeItem('specialCharacters');
      localStorage.removeItem('overrideFont');
      return rules;
    }

    // Default if nothing exists
    return [{
        font: 'parsedFontHK',
        characters: '彩天輸勝都愉朗'
      },{
        font: 'parsedFontChinese',
        characters: '啟嶺'
      },{
        font: 'parsedFontChocolate',
        characters: '尚'
      }];
  },

  /**
   * Save override rules to localStorage
   */
  saveOverrideRules: function (rules) {
    localStorage.setItem('overrideRules', JSON.stringify(rules));
  },

  /**
   * Get special characters from localStorage or return default
   * @deprecated Use getOverrideRules instead
   */
  getSpecialCharacters: function () {
    const rules = FontPriorityManager.getOverrideRules();
    // Return all characters from all rules concatenated
    return rules.map(r => r.characters).join('');
  },

  /**
   * Get the override font from localStorage or return default
   * @deprecated Use getOverrideRules instead
   */
  getOverrideFont: function () {
    const rules = FontPriorityManager.getOverrideRules();
    return rules.length > 0 ? rules[0].font : 'parsedFontChinese';
  },

  /**
   * Save special characters and override font to localStorage
   * @deprecated Use saveOverrideRules instead
   */
  saveCharacterOverride: function (font, characters) {
    // This is kept for backward compatibility but should ideally not be used
    // It will overwrite the first rule or create a new one
    const rules = FontPriorityManager.getOverrideRules();
    if (rules.length > 0) {
      rules[0].font = font;
      rules[0].characters = characters;
    } else {
      rules.push({ font, characters });
    }
    FontPriorityManager.saveOverrideRules(rules);
  },
  /**
   * Populate font select dropdown with available fonts
   */
  populateFontSelect: function (selectElement) {
    // Clear existing options
    selectElement.innerHTML = '';

    const currentOverride = FontPriorityManager.getOverrideFont();
    // Use all available fonts including Chinese/HK fonts
    const availableFonts = FontPriorityManager.getAllAvailableFonts(true);

    // Create options from the available fonts list
    availableFonts.forEach(fontInfo => {
      const option = document.createElement('option');
      option.value = fontInfo.value;
      option.textContent = fontInfo.label;
      
      if (fontInfo.value === currentOverride) {
        option.selected = true;
      }
      selectElement.appendChild(option);
    });
  },

  /**
   * Get special characters as array for use in path.js
   * @deprecated Use getOverrideRules instead
   */
  getSpecialCharactersArray: function () {
    const charactersString = FontPriorityManager.getSpecialCharacters();
    // Split into individual characters and filter out whitespace
    return charactersString.split('').filter(char => char.trim().length > 0);
  },

  /**
   * Reset font manager settings to default values
   */
  resetToDefaults: function () {
    try {
      // Reset font priority list to default
      FontPriorityManager.fontPriorityList = ['parsedFontKorean', 'parsedFontChinese', 'parsedFontHK'];

      // Clear character override settings
      localStorage.removeItem('characterOverrideFont');
      localStorage.removeItem('specialCharacters');
      localStorage.removeItem('overrideFont');
      localStorage.removeItem('overrideRules');
      
      // Default rules
      const defaultRules = [{
        font: 'parsedFontHK',
        characters: '彩天輸勝都愉朗'
      },{
        font: 'parsedFontChinese',
        characters: '啟嶺'
      },{
        font: 'parsedFontChocolate',
        characters: '尚'
      }];
      FontPriorityManager.saveOverrideRules(defaultRules);

      // Remove custom fonts from window object
      Object.keys(window).forEach(key => {
        if (key.startsWith('custom_') && typeof window[key] === 'object' && window[key]?.getPath) {
          delete window[key];
        }
      });

      // Save the reset font priority to storage
      FontPriorityManager.saveFontPriorityToStorage();

      console.log('Font manager settings reset to defaults');
      return true;
    } catch (error) {
      console.error('Error resetting font manager settings:', error);
      return false;
    }
  },
  /**
   * Get all available fonts (built-in + custom) with user-friendly labels
   * @param {boolean} containsNonEnglish - Whether the text contains non-English characters
   * @returns {Array} Array of {value, label} objects for font selection
   */
  getAllAvailableFonts: function (containsNonEnglish = false) {
    const fonts = [];

    if (containsNonEnglish) {
      // For text with non-English characters, show Chinese/Asian fonts
      fonts.push({ value: 'parsedFontChinese', label: 'Noto Sans Trad Chinese' });
      fonts.push({ value: 'parsedFontChocolate', label: 'Noto Sans Chocolate' });
      fonts.push({ value: 'parsedFontHK', label: 'Noto Sans HK' });
      fonts.push({ value: 'parsedFontKorean', label: 'Noto Sans KR' });

      // Add custom fonts from fontPriorityList for non-English text
      FontPriorityManager.fontPriorityList.forEach(fontName => {
        if (fontName.startsWith('custom_')) {
          // Check if custom font is still available in window
          if (window[fontName]) {
            const label = fontName.replace('custom_', '').replace(/([A-Z])/g, ' $1').trim() || fontName;
            fonts.push({ value: fontName, label: `${label} (Custom)` });
          }
        }
      });
    } else {
      // For English-only text, show standard English fonts
      fonts.push({ value: 'TransportMedium', label: 'Transport Medium' });
      fonts.push({ value: 'TransportHeavy', label: 'Transport Heavy' });
    }

    return fonts;
  },

  /**
   * Validate that all fonts in the priority list exist
   * Show warning toast for missing fonts
   */
  validateFonts: function () {
    const missingFonts = [];

    FontPriorityManager.fontPriorityList.forEach(fontName => {
      // Check if font exists (either as built-in or in window)
      const isBuiltInFont = ['parsedFontKorean', 'parsedFontChinese', 'parsedFontChocolate', 'parsedFontHK', 'TransportMedium', 'TransportHeavy', 'TW-MOE-Std-Kai'].includes(fontName);
      const existsInWindow = window[fontName] !== undefined;

      if (!isBuiltInFont && !existsInWindow) {
        missingFonts.push(fontName);
      }
    });

    if (missingFonts.length > 0 && _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralHandler && _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralHandler.showToast) {
      _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralHandler.showToast(
        `Warning: ${missingFonts.length} font(s) not found: ${missingFonts.join(', ')}. Please upload or remove them from the priority list.`,
        'warning',
        7000
      );
    }
    return missingFonts;
  },

  /**
   * Check all fonts periodically and show warnings for missing ones
   * This is useful after font uploads or changes
   */
  checkAllFonts: function () {
    // Check if any fonts in the current priority list are missing
    const missingFonts = FontPriorityManager.validateFonts();

    // Also check all custom fonts that might be referenced by text objects
    if (window.CanvasGlobals && window.CanvasGlobals.canvas) {
      const textObjects = window.CanvasGlobals.canvas.getObjects().filter(obj =>
        obj.functionalType === 'Text'
      );

      const uniqueFonts = new Set();
      textObjects.forEach(textObj => {
        if (textObj.fontFamily) uniqueFonts.add(textObj.fontFamily);
        if (textObj.fontNames) uniqueFonts.add(textObj.fontNames);
      });

      // Check each unique font
      uniqueFonts.forEach(fontName => {
        const isBuiltInFont = ['parsedFontKorean', 'parsedFontChinese', 'parsedFontChocolate', 'parsedFontHK', 'TransportMedium', 'TransportHeavy', 'TW-MOE-Std-Kai'].includes(fontName);
        const existsInWindow = window[fontName] !== undefined;

        if (!isBuiltInFont && !existsInWindow && !missingFonts.includes(fontName)) {
          missingFonts.push(fontName);
        }
      });
    }

    return missingFonts;
  },

  /**
   * Get all text objects from the canvas
   * @returns {Array} Array of text objects
   */
  getAllTextObjects: function () {

    return _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.canvasObject.filter(obj =>
      obj.functionalType === 'Text'
    );
  },
  /**
   * Update all text objects to use a fallback font
   * @param {string} missingFont - The font that was not found
   * @param {string} fallbackFont - The font to use instead
   * @returns {number} Number of text objects updated
   */
  updateAllTextObjectsFont: function (missingFont, fallbackFont) {
    const textObjects = FontPriorityManager.getAllTextObjects();
    let updatedCount = 0;

    textObjects.forEach(textObj => {
      if (textObj.fontFamily === missingFont || textObj.fontNames === missingFont) {
        // Store the original font name so we can restore it later when the font becomes available
        if (!textObj.originalFontFamily) {
          textObj.originalFontFamily = missingFont;
        }

        // Update the font property
        if (textObj.fontFamily) textObj.fontFamily = fallbackFont;
        if (textObj.fontNames) textObj.fontNames = fallbackFont;

        // Force re-render
        textObj.dirty = true;
        updatedCount++;
      }
    });

    if (updatedCount > 0) {
      window.CanvasGlobals.canvas.renderAll();
    }

    return updatedCount;
  },

  /**
   * Get the first available font from the system
   * @returns {string} Name of an available font
   */
  getFirstAvailableFont: function () {
    // Check built-in fonts first by accessing global font variables
    const builtInFonts = [
      { name: 'TransportMedium', check: () => window.parsedFontMedium },
      { name: 'TransportHeavy', check: () => window.parsedFontHeavy },
      { name: 'TW-MOE-Std-Kai', check: () => window.parsedFontKai },
      { name: 'parsedFontKorean', check: () => window.parsedFontKorean },
      { name: 'parsedFontChinese', check: () => window.parsedFontChinese },
      { name: 'parsedFontChocolate', check: () => window.parsedFontChocolate },
      { name: 'parsedFontHK', check: () => window.parsedFontHK }
    ];

    for (const font of builtInFonts) {
      if (font.check && font.check()) {
        return font.name;
      }
    }

    // Fallback to system font
    return 'TransportMedium';
  },

  /**
   * Handle missing font - show toast and update all text objects
   * @param {string} missingFontName - The name of the missing font
   * @returns {string} The fallback font name used
   */
  handleMissingFont: function (missingFontName) {
    const fallbackFont = FontPriorityManager.getFirstAvailableFont();
    const updatedCount = FontPriorityManager.updateAllTextObjectsFont(missingFontName, fallbackFont);

    // Show toast message instead of console warning
    if (_sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralHandler && _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralHandler.showToast) {
      _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralHandler.showToast(
        `Font "${missingFontName}" not found. ${updatedCount} text object(s) updated to use "${fallbackFont}".`,
        'warning',
        5000
      );
    }
    return fallbackFont;
  },
  /**
   * Check for text objects that need the uploaded font and update them
   * @param {string} uploadedFontName - The name of the newly uploaded font
   * @returns {number} Number of text objects updated
   */
  updateTextObjectsWithUploadedFont: function (uploadedFontName) {
    const textObjects = FontPriorityManager.getAllTextObjects();
    let updatedCount = 0;

    // Get the original font filename without the "custom_" prefix
    const originalFontName = uploadedFontName.replace(/^custom_/, '');

    textObjects.forEach(textObj => {
      // Check if this text object was looking for this font
      // This could happen if the font was missing before and now it's available
      const needsUpdate = (
        (textObj.font === uploadedFontName) ||
        (textObj.font === originalFontName));

      if (needsUpdate) {
        // Update to use the newly uploaded font
        textObj.font = uploadedFontName;

        // Force re-render
        textObj.removeAll();
        textObj.initialize();
        textObj.updateAllCoord();
      }
    });

    if (updatedCount > 0) {

      // Show success toast
      if (_sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralHandler && _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralHandler.showToast) {
        _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralHandler.showToast(
          `Font "${originalFontName}" uploaded successfully! ${updatedCount} text object(s) updated to use the new font.`,
          'success',
          4000
        );
      }
    } else {
      // Show info toast even if no objects were updated
      if (_sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralHandler && _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralHandler.showToast) {
        _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralHandler.showToast(
          `Font "${originalFontName}" uploaded successfully and added to priority list.`,
          'success',
          3000
        );
      }
    }
    return updatedCount;
  },


};

// Initialize the font priority system when the module loads
FontPriorityManager.initialize();

// Make FontPriorityManager globally accessible for path.js and other modules
if (typeof window !== 'undefined') {
  window.FontPriorityManager = FontPriorityManager;
}




/***/ }),

/***/ "./js/modal/md-import.js":
/*!*******************************!*\
  !*** ./js/modal/md-import.js ***!
  \*******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ImportManager: () => (/* binding */ ImportManager)
/* harmony export */ });
/* harmony import */ var _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./mdGeneral.js */ "./js/modal/mdGeneral.js");
/**
 * md-import.js - Import Modal Management
 * This module handles the JSON import modal functionality
 */



const ImportManager = {
  /**
   * Show the JSON import modal
   * @param {Function} importCallback - Function to call when importing JSON text
   */
  showImportJSONTextModal: function (importCallback) {
    const modalId = 'import-json-text-modal';
    const { modal, modalContent } = _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.createModal(modalId, 'Import JSON from Text');

    // Create textarea for JSON input
    const textArea = _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.createTextArea('json-text-area-input', 'Paste your JSON here...');

    // Create import button
    const importButton = _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.createButton('Import', 'import-button', async () => {
      const jsonText = textArea.value;
      if (importCallback) {
        try {
          await importCallback(jsonText);
          // Modal removal is handled by the callback if successful
        } catch (error) {
          console.error('Import failed:', error);
          // Keep modal open on error so user can try again
        }
      }
    });

    // Create buttons container
    const buttonsContainer = _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.createButtonsContainer([importButton]);

    // Assemble modal content
    modalContent.appendChild(textArea);
    modalContent.appendChild(buttonsContainer);

    // Show modal and focus textarea
    _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.showModal(modal, textArea);
  },

  /**
   * Close the import modal
   */
  closeImportModal: function () {
    _mdGeneral_js__WEBPACK_IMPORTED_MODULE_0__.ModalUtils.removeModal('import-json-text-modal');
  }
};

// Make ImportManager globally accessible
if (typeof window !== 'undefined') {
  window.ImportManager = ImportManager;
}




/***/ }),

/***/ "./js/modal/mdGeneral.js":
/*!*******************************!*\
  !*** ./js/modal/mdGeneral.js ***!
  \*******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ModalUtils: () => (/* binding */ ModalUtils)
/* harmony export */ });
/* harmony import */ var _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../i18n/i18n.js */ "./js/i18n/i18n.js");
/**
 * mdGeneral.js - Common Modal Utilities
 * This module provides shared functions for modal creation and management
 */



const ModalUtils = {
  /**
   * Create a basic modal structure
   * @param {string} modalId - Unique ID for the modal
   * @param {string} title - Modal title text
   * @returns {Object} - Object containing modal and modalContent elements
   */
  createModal: function (modalId, title) {
    // Remove existing modal if any
    let existingModal = document.getElementById(modalId);
    if (existingModal) {
      existingModal.remove();
    }

    // Create modal container
    const modal = document.createElement('div');
    modal.id = modalId;

    // Create modal content box
    const modalContent = document.createElement('div');
    modalContent.className = 'modal-content';

    // Modal Title
  const titleElement = document.createElement('h3');
  titleElement.setAttribute('data-i18n', title);
  titleElement.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_0__.i18n.t(title);

    // Close button
  const closeButton = document.createElement('button');
  closeButton.textContent = '×';
    closeButton.className = 'close-button';
    closeButton.onclick = () => modal.remove();

    // Assemble basic structure
    modalContent.appendChild(closeButton);
    modalContent.appendChild(titleElement);
    modal.appendChild(modalContent);

    return { modal, modalContent, titleElement, closeButton };
  },

  /**
   * Create a button with specified properties
   * @param {string} text - Button text
   * @param {string} className - CSS class name
   * @param {Function} onClick - Click handler function
   * @returns {HTMLButtonElement} - Created button element
   */
  createButton: function (text, className, onClick) {
    const button = document.createElement('button');
    button.setAttribute('data-i18n', text);
    button.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_0__.i18n.t(text);
    button.className = className;
    if (onClick) {
      button.onclick = onClick;
    }
    return button;
  },

  /**
   * Create a buttons container
   * @param {Array} buttons - Array of button elements to add
   * @returns {HTMLDivElement} - Container with buttons
   */
  createButtonsContainer: function (buttons = []) {
    const buttonsContainer = document.createElement('div');
    buttonsContainer.className = 'buttons-container';
    buttons.forEach(button => {
      if (button) {
        buttonsContainer.appendChild(button);
      }
    });
    return buttonsContainer;
  },

  /**
   * Create an info text paragraph
   * @param {string} text - Text content
   * @returns {HTMLParagraphElement} - Info text element
   */
  createInfoText: function (text) {
    const infoText = document.createElement('p');
    infoText.setAttribute('data-i18n', text);
    infoText.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_0__.i18n.t(text);
    infoText.className = 'info-text';
    return infoText;
  },

  /**
   * Create a text area with specified properties
   * @param {string} id - Element ID
   * @param {string} placeholder - Placeholder text
   * @returns {HTMLTextAreaElement} - Created textarea element
   */
  createTextArea: function (id, placeholder) {
    const textArea = document.createElement('textarea');
    textArea.id = id;
    if (placeholder) {
      try {
        textArea.setAttribute('data-i18n-placeholder', placeholder);
        // Use i18n if available
        textArea.placeholder = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_0__.i18n.t(placeholder);
      } catch (_) {
        textArea.placeholder = placeholder;
      }
    }
    return textArea;
  },

  /**
   * Create a section container
   * @param {string} className - CSS class name
   * @returns {HTMLDivElement} - Section container element
   */
  createSection: function (className) {
    const section = document.createElement('div');
    section.className = className;
    return section;
  },

  /**
   * Show modal by appending to document body and optionally focus an element
   * @param {HTMLElement} modal - Modal element to show
   * @param {HTMLElement} focusElement - Element to focus after showing modal
   * @param {boolean} closeOnOutsideClick - Whether to close modal when clicking outside (default: true)
   */
  showModal: function (modal, focusElement = null, closeOnOutsideClick = true) {


    // Add click outside to close functionality (if enabled)
    if (closeOnOutsideClick) {
      modal.onclick = function(event) {
        // Only close if clicking directly on the modal overlay (not the modal content)
        if (event.target === modal) {
          modal.remove();
        }
      };
    }

    // Prevent clicks on modal content from bubbling up to the overlay
    const modalContent = modal.querySelector('.modal-content');
    if (modalContent) {
      modalContent.onclick = function(event) {
        event.stopPropagation();
      };
    }

    // Add ESC key support to close modal
    const handleKeyDown = function(event) {
      if (event.key === 'Escape') {
        modal.remove();
        document.removeEventListener('keydown', handleKeyDown);
      }
    };
    document.addEventListener('keydown', handleKeyDown);

    // Clean up event listener when modal is removed
    const originalRemove = modal.remove.bind(modal);
    modal.remove = function() {
      document.removeEventListener('keydown', handleKeyDown);
      originalRemove();
    };

    document.body.appendChild(modal);
    // Apply translations to any new modal content
    try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_0__.i18n.applyTranslations(modal); } catch (_) {}
    if (focusElement) {
      focusElement.focus();
    }
  },

  /**
   * Remove modal from DOM
   * @param {string} modalId - ID of modal to remove
   */
  removeModal: function (modalId) {
    const modal = document.getElementById(modalId);
    if (modal) {
      modal.remove();
    }
  }
};




/***/ }),

/***/ "./js/objects/anchor.js":
/*!******************************!*\
  !*** ./js/objects/anchor.js ***!
  \******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   anchorShape: () => (/* binding */ anchorShape),
/* harmony export */   globalAnchorTree: () => (/* binding */ globalAnchorTree),
/* harmony export */   processUpdateCycle: () => (/* binding */ processUpdateCycle)
/* harmony export */ });
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../canvas/Tracker.js */ "./js/canvas/Tracker.js");
/* harmony import */ var _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../canvas/keyboardEvents.js */ "./js/canvas/keyboardEvents.js");
/* harmony import */ var _canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../canvas/promptBox.js */ "./js/canvas/promptBox.js");
//TODO: check updateAllCoord for anchor object inside border / divider not working
// AnchorTree class to manage anchoring relationships between objects

 // Import canvasTracker for tracking changes

 // Import selectObjectHandler for object selection

const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas; // Get the global canvas instance

class AnchorTree {
  constructor() {
    this.xTree = {}; // Object to store X-axis anchor relationships
    this.yTree = {}; // Object to store Y-axis anchor relationships
    this.updateInProgressX = false; // Flag to track if an X-axis update cycle is in progress
    this.updateInProgressY = false; // Flag to track if a Y-axis update cycle is in progress
    this.updatedObjectsX = new Set(); // Track objects that have been updated in X-axis
    this.updatedObjectsY = new Set(); // Track objects that have been updated in Y-axis
    this.starterObjectX = null; // Track which object started the X update chain
    this.starterObjectY = null; // Track which object started the Y update chain
    this.updateDepthX = 0; // Track nesting level of X updates
    this.updateDepthY = 0; // Track nesting level of Y updates
  }

  clear() {
    this.xTree = {};
    this.yTree = {};
    this.updateInProgressX = false;
    this.updateInProgressY = false;
    this.updatedObjectsX.clear();
    this.updatedObjectsY.clear();
    this.starterObjectX = null;
    this.starterObjectY = null;
    this.updateDepthX = 0;
    this.updateDepthY = 0;
  }

  // Start a new update cycle for a specific direction
  startUpdateCycle(direction, starterId) {
    if (starterId === null) {
      console.warn("No starter ID provided to startUpdateCycle");
    }

    if (direction === 'x') {
      // Only set the starter object if this is the first level of the update
      if (!this.updateInProgressX) {
        this.starterObjectX = starterId;
        this.updatedObjectsX.clear();
      }
      this.updateInProgressX = true;
      this.updateDepthX++;
    } else {
      // Only set the starter object if this is the first level of the update
      if (!this.updateInProgressY) {
        this.starterObjectY = starterId;
        this.updatedObjectsY.clear();
      }
      this.updateInProgressY = true;
      this.updateDepthY++;
    }
  }

  // Check if the object is the starter of the current update chain
  isUpdateStarter(direction, objectId) {
    if (direction === 'x') {
      return this.starterObjectX === objectId;
    } else {
      return this.starterObjectY === objectId;
    }
  }

  // End the current update cycle for a specific direction
  endUpdateCycle(direction) {
    if (direction === 'x') {
      // Add safety check to prevent negative depth
      if (this.updateDepthX <= 0) {
        //console.warn("Attempted to end X update cycle when none is in progress");
        this.updateDepthX = 0;
        this.updateInProgressX = false;
        this.updatedObjectsX.clear();
        this.starterObjectX = null;
        return;
      }

      this.updateDepthX--;
      // Only fully end the cycle when we're back at the top level
      if (this.updateDepthX === 0) {
        this.updateInProgressX = false;
        this.updatedObjectsX.clear();
        this.starterObjectX = null;
      }
    } else {
      // Add safety check to prevent negative depth
      if (this.updateDepthY <= 0) {
        //console.warn("Attempted to end Y update cycle when none is in progress");
        this.updateDepthY = 0;
        this.updateInProgressY = false;
        this.updatedObjectsY.clear();
        this.starterObjectY = null;
        return;
      }

      this.updateDepthY--;
      // Only fully end the cycle when we're back at the top level
      if (this.updateDepthY === 0) {
        this.updateInProgressY = false;
        this.updatedObjectsY.clear();
        this.starterObjectY = null;
      }
    }
  }

  // Add a node to the tree (either X or Y direction)
  addNode(direction, sourceObj, targetObj) {
    const tree = direction === 'x' ? this.xTree : this.yTree;

    // Create entries for both objects if they don't exist
    if (!tree[sourceObj.canvasID]) {
      tree[sourceObj.canvasID] = {
        object: sourceObj,
        parents: [],
        children: []
      };
    }

    if (!tree[targetObj.canvasID]) {
      tree[targetObj.canvasID] = {
        object: targetObj,
        parents: [],
        children: []
      };
    }

    // Add parent-child relationship
    if (!tree[sourceObj.canvasID].parents.includes(targetObj.canvasID)) {
      tree[sourceObj.canvasID].parents.push(targetObj.canvasID);
    }

    if (!tree[targetObj.canvasID].children.includes(sourceObj.canvasID)) {
      tree[targetObj.canvasID].children.push(sourceObj.canvasID);
    }
  }

  // Remove a node from the tree (either X or Y direction)
  removeNode(direction, objId) {
    const tree = direction === 'x' ? this.xTree : this.yTree;

    if (!tree[objId]) return;

    // Remove this node from all its parents' children lists
    for (const parentId of tree[objId].parents) {
      if (tree[parentId]) {
        tree[parentId].children = tree[parentId].children.filter(id => id !== objId);
      }
    }
    // Remove this node from all its children's parents lists
    for (const childId of tree[objId].children) {
      if (tree[childId]) {
        tree[childId].parents = tree[childId].parents.filter(id => id !== objId);
      }
    }

    delete tree[objId]; // Finally, delete the node itself

  }

  // Helper function to find the upward chain based on current locks
  // Returns an array of objects: [{id: currentId, parentId: foundParentId}, ...]
  findUpwardChain(direction, startId) {
    const tree = direction === 'x' ? this.xTree : this.yTree;
    const chain = [];
    let currentId = startId;
    const visited = new Set(); // Prevent infinite loops

    while (currentId !== -1 && tree[currentId] && !visited.has(currentId)) {
      visited.add(currentId);
      const currentNode = tree[currentId];
      const currentObject = currentNode.object;
      let foundParentId = -1; // The parent ID determined for this step
      let lockinfo = null; // The lock information for this step

      // Find the parent based on the current lock state and verify in tree
      if (direction === 'x' && currentObject.lockXToPolygon?.TargetObject) {
        const potentialParentId = currentObject.lockXToPolygon.TargetObject.canvasID;
        if (currentNode.parents.includes(potentialParentId) && tree[potentialParentId]) {
          foundParentId = potentialParentId;
          lockinfo = currentObject.lockXToPolygon;
        }
      } else if (direction === 'y' && currentObject.lockYToPolygon?.TargetObject) {
        const potentialParentId = currentObject.lockYToPolygon.TargetObject.canvasID;
        if (currentNode.parents.includes(potentialParentId) && tree[potentialParentId]) {
          foundParentId = potentialParentId;
          lockinfo = currentObject.lockYToPolygon;
        }
      }

      // Store the current ID and the parent ID found *in this step*
      chain.push({ id: currentId, parentId: foundParentId, lockinfo: lockinfo });

      // Move to the found parent for the next iteration
      currentId = foundParentId;
    }

    if (currentId !== -1 && visited.has(currentId)) {
      console.warn(`Cycle detected during chain identification starting from ${startId} in ${direction}. Chain may be incomplete.`);
    }

    return chain; // Returns [{id: startId, parentId: p1}, {id: p1, parentId: p2}, ...]
  }

  // Helper function to update lockMovement flags and focus mode based on current locks
  updateLockAndFocus(obj) {
    if (!obj) return;

    obj.lockMovementX = !(Object.keys(obj.lockXToPolygon).length === 0);
    obj.lockMovementY = !(Object.keys(obj.lockYToPolygon).length === 0);

    if (obj.lockMovementX || obj.lockMovementY) {
      obj.enterFocusMode();
    } else {
      // Only exit focus mode if it's not locked by other means (like group selection)
      // Assuming exitFocusMode handles internal checks or is safe to call
      obj.exitFocusMode();
    }
  }

  // Reverse the anchor dependency chain starting from a pivot object
  reverseAnchorChain(direction, startPivotId) {
    const tree = direction === 'x' ? this.xTree : this.yTree;

    if (!tree[startPivotId]) {
      console.warn(`Start pivot object with ID ${startPivotId} not found in ${direction} tree.`);
      return;
    }

    // 1. Identify the entire chain to be reversed first, capturing parent links
    const chainInfo = this.findUpwardChain(direction, startPivotId);

    // If the chain only has the start node (no parents found in this direction), nothing to reverse
    if (chainInfo.length === 0 || (chainInfo.length === 1 && chainInfo[0].parentId === -1)) {
      return;
    }

    // 2. Iterate through the identified chain links and perform reversals
    // The chainInfo array gives us pairs of (childId, parentId) for each link
    for (const link of chainInfo) {
      const childId = link.id;
      const parentId = link.parentId;
      const oldLock = link.lockinfo; // The lock information for this link

      // Stop if we reach the end of a chain segment (no further parent)
      if (parentId === -1) {
        break;
      }

      // Double-check nodes exist
      if (!tree[childId] || !tree[parentId]) {
        console.warn(`Node missing during reversal (${childId} or ${parentId}). Stopping chain reversal.`);
        break;
      }

      const childNode = tree[childId];
      const childObject = childNode.object;
      const parentNode = tree[parentId];
      const parentObject = parentNode.object;


      // If the expected lock for this specific link isn't found now, it might have been changed
      // concurrently or there's an inconsistency. We rely on the chainInfo structure.
      if (!oldLock) {
        console.warn(`Could not find original lock information for link ${childId} -> ${parentId} in ${direction} during reversal. Attempting reversal based on identified chain.`);
        // Construct a placeholder oldLock if needed, assuming standard points if missing
        // This might be risky if lock points are critical and vary.
        // Alternatively, skip this link if consistency is paramount.
        // Let's try skipping for now:
        console.warn(`Skipping reversal for link ${childId} -> ${parentId} due to missing lock info.`);
        continue;
      }

      // --- Perform the reversal for this specific link ---

      // Clear the old lock on the child object for this direction
      if (direction === 'x' && childObject.canvasID === Object.values(chainInfo)[0].id) {
        childObject.lockXToPolygon = {};
      } else if (childObject.canvasID === Object.values(chainInfo)[0].id) { // direction === 'y'
        childObject.lockYToPolygon = {};
      }

      // Remove the old relationship (Parent -> Child) from tree structure
      parentNode.children = parentNode.children.filter(id => id !== childId);
      childNode.parents = childNode.parents.filter(id => id !== parentId);

      // Add the new relationship (Child -> Parent) to tree structure
      if (!childNode.children.includes(parentId)) {
        childNode.children.push(parentId);
      }
      if (!parentNode.parents.includes(childId)) {
        parentNode.parents.push(childId);
      }

      // Set the new lock on the PARENT object, making it dependent on the CHILD
      const newLock = {
        sourcePoint: oldLock.targetPoint, // Use default if missing
        targetPoint: oldLock.sourcePoint, // Use default if missing
        sourceObject: parentObject,
        TargetObject: childObject,
        spacing: -oldLock.spacing // Use default if missing
      };

      if (direction === 'x') {
        parentObject.lockXToPolygon = newLock;
      } else { // direction === 'y'
        parentObject.lockYToPolygon = newLock;
      }

      // Update lockMovement and focus mode for BOTH objects involved in this link
      this.updateLockAndFocus(childObject);
      this.updateLockAndFocus(parentObject);

    } // End for loop through the chain links
  }

  // Get all affected objects (in proper update order) when an object is moved
  getUpdateOrder(direction, startObjId) {
    const tree = direction === 'x' ? this.xTree : this.yTree;
    const visited = new Set();
    const updateOrder = [];

    // Skip if object isn't in the tree
    if (!tree[startObjId]) return updateOrder;

    // Special case: If this is the starting object of the movement, 
    // put it at the beginning of the update order
    updateOrder.push(startObjId);
    visited.add(startObjId);

    // Helper function for depth-first traversal
    const visit = (nodeId) => {
      if (visited.has(nodeId)) return;
      visited.add(nodeId);

      // Add this node to the update order
      updateOrder.push(nodeId);

      // Then process all children (dependencies)
      for (const childId of (tree[nodeId]?.children || [])) {
        visit(childId);
      }
    };

    // Process all direct children of the starting object
    for (const childId of (tree[startObjId]?.children || [])) {
      visit(childId);
    }

    // Return the objects in the correct update order (starter object first, then its dependencies)
    return updateOrder.map(id => tree[id].object);
  }

  // Check for circular dependencies
  hasCircularDependency(direction, sourceId, targetId) {
    const tree = direction === 'x' ? this.xTree : this.yTree;

    // If either object is not in the tree, there's no circular dependency
    if (!tree[sourceId] || !tree[targetId]) return false;

    // Check if adding this edge would create a cycle
    const visited = new Set();

    // Helper function for cycle detection
    const hasCycle = (currentId) => {
      if (currentId === sourceId) return true; // Found a cycle
      if (visited.has(currentId)) return false;

      visited.add(currentId);

      // Check all parents
      for (const parentId of tree[currentId].parents) {
        if (hasCycle(parentId)) return true;
      }

      return false;
    };

    // Start from the target and see if we can reach the source
    return hasCycle(targetId);
  }

  // Get pending updates for an object (nodes that would be affected by moving this object)
  getPendingUpdates(direction, objId) {
    const tree = direction === 'x' ? this.xTree : this.yTree;

    // If the object isn't in the tree, there are no pending updates
    if (!tree[objId]) return [];

    const visited = new Set();
    const pendingUpdates = [];

    // Helper function to recursively collect all child nodes that haven't been updated yet
    const collectPendingUpdates = (nodeId) => {
      if (visited.has(nodeId)) return;
      visited.add(nodeId);

      // If this node hasn't been updated yet in the current cycle, add it to pending updates
      if ((direction === 'x' && this.updateInProgressX && !this.updatedObjectsX.has(nodeId)) ||
        (direction === 'y' && this.updateInProgressY && !this.updatedObjectsY.has(nodeId))) {
        // Get the actual object reference from the node
        const obj = tree[nodeId].object;
        pendingUpdates.push(obj);
      }

      // Process all children recursively
      for (const childId of (tree[nodeId]?.children || [])) {
        collectPendingUpdates(childId);
      }
    };

    // Start collecting from the given object
    collectPendingUpdates(objId);

    return pendingUpdates;
  }

  // Get the root objects (objects with no parents)
  getRoots(direction) {
    const tree = direction === 'x' ? this.xTree : this.yTree;
    return Object.entries(tree)
      .filter(([_, node]) => node.parents.length === 0)
      .map(([_, node]) => node.object);
  }

  // Handle updates to the tree structure when an object is deleted
  updateOnDelete(objectId, newIdMapping) {

    // Update canvas IDs in both trees
    ['x', 'y'].forEach(direction => {
      // Remove the object from both X and Y trees
      this.removeNode(direction, objectId);
      const tree = direction === 'x' ? this.xTree : this.yTree;

      // Create a new tree with updated IDs
      const updatedTree = {};

      // Process each node in the tree
      Object.keys(tree).forEach(id => {
        const numId = parseInt(id);
        let newId;
        // If the ID needs to be updated (it's greater than the deleted object's ID)
        if (newIdMapping[numId] !== undefined) {
          newId = newIdMapping[numId];
        } else {
          newId = numId;
        }

        // Copy the node with the new ID
        updatedTree[newId] = tree[numId];
        updatedTree[newId].object = tree[numId].object;

        // Update parent references
        updatedTree[newId].parents = tree[numId].parents.map(parentId =>
          newIdMapping[parentId] !== undefined ? newIdMapping[parentId] : parentId
        );

        // Update child references
        updatedTree[newId].children = tree[numId].children.map(childId =>
          newIdMapping[childId] !== undefined ? newIdMapping[childId] : childId
        );
      });

      // Replace the old tree with the updated one
      if (direction === 'x') {
        this.xTree = updatedTree;
      } else {
        this.yTree = updatedTree;
      }
    });
  }
}

// Create a global instance of the AnchorTree
const globalAnchorTree = new AnchorTree();

document.getElementById('set-anchor').addEventListener('click', function (event) {
  const selectedArrow = event.target.parentElement.parentElement.selectedArrow; // The object we want to MOVE (shape2)
  if (!selectedArrow) return;

  // Hide context menu (container is parentElement.parentElement per existing code)
  this.parentElement.parentElement.style.display = 'none';
  document.removeEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent);

  // Wrapper to adapt selectObjectHandler's callback signature to anchorShape(expected: targetShape, movingShape)
  const handleAnchorSelection = (selectedTargets, movingShape /* comes from options param */) => {
    if (!selectedTargets || selectedTargets.length === 0) return; // nothing selected

    // Use first selected object as target (shape1). selectObjectHandler returns an array.
    const targetShape = Array.isArray(selectedTargets) ? selectedTargets[0] : selectedTargets;

    // Prevent anchoring an object to itself.
    if (targetShape === movingShape) {
      // Simply re-enable sidebar key listener and exit; could add user feedback if desired.
      document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent);
      return;
    }

    // Call original anchorShape with (targetShape, movingShape)
    anchorShape(targetShape, movingShape)
  };

  // Invoke selection flow. selectObjectHandler will pass (successSelectedArray, optionsSelectedArrow, ...)
  // First clear any existing active selection so user makes a fresh target choice.
    canvas.discardActiveObject();
  (0,_canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_3__.selectObjectHandler)('Select shape to anchor to', handleAnchorSelection, selectedArrow);
});

// Parent "Pivot Anchor" acts only as hover target to open submenu; no click action

function handlePivot(axis) {
  const contextMenu = document.getElementById('context-menu');
  contextMenu.style.display = 'none';
  const obj = contextMenu.selectedArrow;
  if (obj && typeof globalAnchorTree !== 'undefined') {
    if (axis === 'x' || axis === 'both') {
      globalAnchorTree.reverseAnchorChain('x', obj.canvasID);
    }
    if (axis === 'y' || axis === 'both') {
      globalAnchorTree.reverseAnchorChain('y', obj.canvasID);
    }
    if (typeof obj.updateAllCoord === 'function') obj.updateAllCoord();
    obj.setCoords();
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
    canvas.fire('object:modified', { target: obj });
  } else {
    console.warn('Pivot Anchor: Target object or canvasID not found.');
  }
}

// Submenu handlers
const pivotX = document.getElementById('pivot-anchor-x');
const pivotY = document.getElementById('pivot-anchor-y');
const pivotBoth = document.getElementById('pivot-anchor-both');

if (pivotX) pivotX.addEventListener('click', () => handlePivot('x'));
if (pivotY) pivotY.addEventListener('click', () => handlePivot('y'));
if (pivotBoth) pivotBoth.addEventListener('click', () => handlePivot('both'));


// Helper function to process anchor updates for a specific axis
function processUpdateCycle(direction, starterObj, updateOrder, delta, sourceList) {
  if (updateOrder.length === 0 || isNaN(delta)) {
    return; // No updates needed or invalid delta
  }

  const updatedObjectsSet = direction === 'x' ? globalAnchorTree.updatedObjectsX : globalAnchorTree.updatedObjectsY;
  const axis = direction === 'x' ? 'left' : 'top';

  // Start an update cycle with the starterObj as the starter
  globalAnchorTree.startUpdateCycle(direction, starterObj.canvasID);

  // First, explicitly update the starterObj itself to ensure proper anchoring propagation
  // This step is key to fixing the issue with one-axis movement not propagating
  if (!updatedObjectsSet.has(starterObj.canvasID)) {
    updatedObjectsSet.add(starterObj.canvasID);
    // Apply the delta directly to the starter object before calling updateAllCoord
    // This ensures the initial movement is reflected before propagation
    // No need to apply delta here as it was already applied before calling this function
    // if (delta !== 0) {
    //     starterObj.set({ [axis]: starterObj[axis] + delta });
    //     starterObj.setCoords(); // Update coordinates after direct movement
    // }
    starterObj.updateAllCoord(null, sourceList); // Propagate changes
  }

  // Update objects in the proper order (excluding the starterObj which is already processed)
  updateOrder.forEach(obj => {
    if (obj.canvasID !== starterObj.canvasID && !updatedObjectsSet.has(obj.canvasID)) {
      // Mark this object as updated
      updatedObjectsSet.add(obj.canvasID);

      // Apply the delta to each sub-anchored object first
      // This ensures they have changes to propagate to their own sub-anchored objects
      // No need to apply delta here as it was already applied before calling this function
      if (delta !== 0) {
        obj.set({ [axis]: obj[axis] + delta });
        obj.setCoords(); // Update coordinates after direct movement
      }

      // Update coordinates - this may involve both X and Y changes if anchored in both axes
      obj.updateAllCoord(null, sourceList);
    }
  });

  // End the update cycle
  globalAnchorTree.endUpdateCycle(direction);
}

async function anchorShape(inputShape1, inputShape2, options = {}, sourceList = []) {

  const shape1 = Array.isArray(inputShape1) ? inputShape1[0] : inputShape1
  const shape2 = Array.isArray(inputShape2) ? inputShape2[0] : inputShape2
  const xHeight = shape2.xHeight || shape1.xHeight || parseInt(document.getElementById("input-xHeight").value)


  const vertexIndex1 = options.vertexIndex1 ? options.vertexIndex1 : await (0,_canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_3__.showTextBox)('Enter vertex index for First Polygon:', 'E1')
  if (!vertexIndex1) {
    document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent);
    return Promise.reject('anchor_cancelled_vertex1');
  }
  const vertexIndex2 = options.vertexIndex2 ? options.vertexIndex2 : await (0,_canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_3__.showTextBox)('Enter vertex index for Second Polygon:', 'E1')
  if (!vertexIndex2) {
    document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent);
    return Promise.reject('anchor_cancelled_vertex2');
  }

  // Check if object is already anchored in X axis
  const horizontalDividerTypes = ['HDivider',];
  const verticalDividerTypes = ['VDivider', 'VLane'];
  const isHorizontalDivider = horizontalDividerTypes.includes(shape2.functionalType);
  const isVerticalDivider = verticalDividerTypes.includes(shape2.functionalType);

  let isAlreadyAnchoredInX = Object.keys(shape2.lockXToPolygon || {}).length > 0;
  let isAlreadyAnchoredInY = Object.keys(shape2.lockYToPolygon || {}).length > 0;

  // Horizontal dividers' X position is managed elsewhere; treat as anchored in X
  if (isHorizontalDivider) {
    isAlreadyAnchoredInX = true;
  }
  // Vertical dividers' Y position is managed elsewhere; treat as anchored in Y
  if (isVerticalDivider) {
    isAlreadyAnchoredInY = true;
  }

  const spacingX = options.spacingX != null ? options.spacingX :
    (isAlreadyAnchoredInX ? '' : await (0,_canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_3__.showTextBox)('Enter spacing in X \n (Leave empty if no need for axis):', 0, 'keydown', null, xHeight));
  if (spacingX == null) { document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent); return Promise.reject('anchor_cancelled_spacingX'); }

  // Check if object is already anchored in Y axis
  const spacingY = options.spacingY != null ? options.spacingY :
    (isAlreadyAnchoredInY ? '' : await (0,_canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_3__.showTextBox)('Enter spacing in Y \n (Leave empty if no need for axis):', 0, 'keydown', null, xHeight));
  if (spacingY == null) { document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent); return Promise.reject('anchor_cancelled_spacingY'); }

  const movingPoint = shape2.getBasePolygonVertex(vertexIndex1.toUpperCase())
  const targetPoint = shape1.getBasePolygonVertex(vertexIndex2.toUpperCase())

  if (!movingPoint) {
    console.log(`Vertex ${vertexIndex1.toUpperCase()} not found in shape to be moved (ID: ${shape2._showName})`);
    document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent);
    //return Promise.reject($`anchor_invalid_vertex1, Shape ID: ${shape2._showName}, Vertex: ${vertexIndex1.toUpperCase()}`);
  }
  if (!targetPoint) {
    console.log(`Vertex ${vertexIndex2.toUpperCase()} not found in target shape (ID: ${shape1._showName})`);
    document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent);
    //return Promise.reject($`anchor_invalid_vertex2, Shape ID: ${shape1._showName}, Vertex: ${vertexIndex2.toUpperCase()}`);
  }

  // Track anchor operations - prepare tracking params
  const anchorTrackParams = {
    type: 'Anchor',
    source: {
      id: shape2.canvasID,
      functionalType: shape2.functionalType,
      vertex: vertexIndex1
    },
    target: {
      id: shape1.canvasID,
      functionalType: shape1.functionalType,
      vertex: vertexIndex2
    },
    spacingX: spacingX,
    spacingY: spacingY
  };

  const parsedSpacingX = isNaN(parseInt(spacingX)) ? 0 : parseInt(spacingX);
  const parsedSpacingY = isNaN(parseInt(spacingY)) ? 0 : parseInt(spacingY);

  const deltaX = targetPoint.x - movingPoint.x + parsedSpacingX;
  const deltaY = targetPoint.y - movingPoint.y + parsedSpacingY;

  let appliedDeltaX = false;
  let appliedDeltaY = false;

  // Add X axis anchoring
  if (!isNaN(parseInt(spacingX)) && spacingX !== '' && !isHorizontalDivider) { // Skip X anchoring for horizontal dividers
    if (globalAnchorTree.hasCircularDependency('x', shape2.canvasID, shape1.canvasID)) {
      alert("Cannot create anchor: would create a circular dependency in X axis");
    } else {
      // Snap shape2 to shape1 with the specified spacing
      shape2.set({
        left: shape2.left + deltaX,
        lockMovementX: true,
      });
      appliedDeltaX = true; // Mark that deltaX was applied
      const anchor = { sourcePoint: vertexIndex1, targetPoint: vertexIndex2, sourceObject: shape2, TargetObject: shape1, spacing: parseInt(spacingX) }
      shape2.lockXToPolygon = anchor

      // Add to the anchor tree
      globalAnchorTree.addNode('x', shape2, shape1);

      if (shape1.functionalType == 'Border') {
        if (shape1.widthObjects.includes(shape2)) {
          shape1.widthObjects.splice(shape1.widthObjects.indexOf(shape2), 1)
        }
      }
    }
  }

  // Add Y axis anchoring
  if (!isNaN(parseInt(spacingY)) && spacingY !== '' && !isVerticalDivider) { // Skip Y anchoring for vertical dividers
    if (globalAnchorTree.hasCircularDependency('y', shape2.canvasID, shape1.canvasID)) {
      alert("Cannot create anchor: would create a circular dependency in Y axis");
    } else {
      // Snap shape2 to shape1 with the specified spacing
      shape2.set({
        top: shape2.top + deltaY,
        lockMovementY: true,
      });
      appliedDeltaY = true; // Mark that deltaY was applied
      const anchor = { sourcePoint: vertexIndex1, targetPoint: vertexIndex2, sourceObject: shape2, TargetObject: shape1, spacing: parseInt(spacingY) }
      shape2.lockYToPolygon = anchor

      // Add to the anchor tree
      globalAnchorTree.addNode('y', shape2, shape1);

      if (shape1.functionalType == 'Border') {
        if (shape1.heightObjects.includes(shape2)) {
          shape1.heightObjects.splice(shape1.heightObjects.indexOf(shape2), 1)
        }
      }
    }
  }

  // Update coordinates after initial placement and adding to tree
  shape2.setCoords();

  // Use the AnchorTree to get the proper update order for anchors
  const updateOrderX = globalAnchorTree.getUpdateOrder('x', shape2.canvasID);
  const updateOrderY = globalAnchorTree.getUpdateOrder('y', shape2.canvasID);

  // Process X axis updates using the new helper function
  // Pass the delta only if it was actually applied during anchoring setup
  processUpdateCycle('x', shape2, updateOrderX, appliedDeltaX ? deltaX : NaN, sourceList);

  // Process Y axis updates using the new helper function
  // Pass the delta only if it was actually applied during anchoring setup
  processUpdateCycle('y', shape2, updateOrderY, appliedDeltaY ? deltaY : NaN, sourceList);

  shape2.updateAllCoord();

  // If no updates were triggered by anchoring (e.g., only one axis anchored, or no spacing),
  // ensure the object itself is updated if it wasn't part of an ongoing cycle.
  //if (!globalAnchorTree.isUpdating('x', shape2.canvasID) && !globalAnchorTree.isUpdating('y', shape2.canvasID)) {
  //    // Check if the object is currently part of an update cycle started elsewhere
  //    // This check might be redundant now with the processUpdateCycle logic, but kept for safety
  //    const isUpdatingX = globalAnchorTree.updateInProgressX && globalAnchorTree.updatedObjectsX.has(shape2.canvasID);
  //    const isUpdatingY = globalAnchorTree.updateInProgressY && globalAnchorTree.updatedObjectsY.has(shape2.canvasID);
  //
  //    // Only call updateAllCoord if it hasn't been updated in an ongoing cycle initiated by this function
  //    if (!isUpdatingX && !isUpdatingY) {
  //        shape2.updateAllCoord(null, sourceList);
  //    }
  //}

  if (!shape1.anchoredPolygon.includes(shape2)) {
    shape1.anchoredPolygon.push(shape2)
  }

  if (!shape1.borderType) {
    canvas.bringObjectToFront(shape1)
  }

  // Set focus mode to anchored object if applicable
  if (shape2.lockMovementX && shape2.lockMovementY) {
    shape2.enterFocusMode();
  } else {
    shape2.exitFocusMode();
  }

  document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent);

  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();

  // Track the regular anchoring operation in history
  // Only track if both X and Y aren't "EQ" (those are tracked separately)
  if (_canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_1__.canvasTracker && !(spacingX.toString().toUpperCase() === 'EQ' && spacingY.toString().toUpperCase() === 'EQ')) {
    _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_1__.canvasTracker.track('anchorObject', [anchorTrackParams]);
  }

  // Return a resolved promise to allow for chaining
  return Promise.resolve();
}




/***/ }),

/***/ "./js/objects/border.js":
/*!******************************!*\
  !*** ./js/objects/border.js ***!
  \******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   BorderGroup: () => (/* binding */ BorderGroup),
/* harmony export */   BorderUtilities: () => (/* binding */ BorderUtilities)
/* harmony export */ });
/* harmony import */ var _draw_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./draw.js */ "./js/objects/draw.js");
/* harmony import */ var _dimension_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./dimension.js */ "./js/objects/dimension.js");
/* harmony import */ var _anchor_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./anchor.js */ "./js/objects/anchor.js");
/* harmony import */ var _template_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./template.js */ "./js/objects/template.js");
/* harmony import */ var _path_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./path.js */ "./js/objects/path.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _divider_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./divider.js */ "./js/objects/divider.js");








const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_5__.CanvasGlobals.canvas; // Access the global canvas object
const canvasObject = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_5__.CanvasGlobals.canvasObject // Get all objects on the canvas

// Add function to remove anchor between objects
function removeAnchor(sourceObject, targetObject) {
  // Save the source and target objects before delinking
  const savedSourceObject = sourceObject;
  const savedTargetObject = targetObject;

  // Check if the source object has the targetObject in its anchoredPolygon array
  if (sourceObject.anchoredPolygon && Array.isArray(sourceObject.anchoredPolygon)) {
    // Remove the target object from the source's anchoredPolygon array
    sourceObject.anchoredPolygon = sourceObject.anchoredPolygon.filter(obj => obj !== targetObject);
  }

  // Clean up the target object's lock properties
  if (targetObject.lockXToPolygon && targetObject.lockXToPolygon.TargetObject === sourceObject) {
    targetObject.lockXToPolygon = {};
  }

  if (targetObject.lockYToPolygon && targetObject.lockYToPolygon.TargetObject === sourceObject) {
    targetObject.lockYToPolygon = {};
  }

  // Return the saved objects for reference
  return { source: savedSourceObject, target: savedTargetObject };
}

// Add BorderUtilities object to hold functions moved from FormBorderWrapComponent
const BorderUtilities = {

  getExtremeObject: function (objects, direction) {
    let extremeObject = null;
    let extremeValue = direction === 'bottom' || direction === 'right' ? -Infinity : Infinity;

    objects.forEach(obj => {
      let value;

      switch (direction) {
        case 'bottom':
          value = obj.top + obj.height * obj.scaleY;
          if (value > extremeValue) {
            extremeValue = value;
            extremeObject = obj;
          }
          break;
        case 'top':
          value = obj.top;
          if (value < extremeValue) {
            extremeValue = value;
            extremeObject = obj;
          }
          break;
        case 'right':
          value = obj.left + obj.width * obj.scaleX;
          if (value > extremeValue) {
            extremeValue = value;
            extremeObject = obj;
          }
          break;
        case 'left':
          value = obj.left;
          if (value < extremeValue) {
            extremeValue = value;
            extremeObject = obj;
          }
          break;
      }
    });

    return extremeObject;
  },

  // For backward compatibility
  getBottomMostObject: function (objects) {
    return this.getExtremeObject(objects, 'bottom');
  },

  getTopMostObject: function (objects) {
    return this.getExtremeObject(objects, 'top');
  },

  // Function to get the bounding box of specific objects
  getBoundingBox: function (objects) {
    // Loop through each object and its basePolygon
    let combinedBBox = { left: Infinity, top: Infinity, right: -Infinity, bottom: -Infinity };
    objects.forEach(obj => {
      const coord = obj.getEffectiveCoords()
      // Update the combined bounding box
      combinedBBox.left = Math.min(combinedBBox.left, coord[0].x,);
      combinedBBox.top = Math.min(combinedBBox.top, coord[0].y,);
      combinedBBox.right = Math.max(combinedBBox.right, coord[2].x,);
      combinedBBox.bottom = Math.max(combinedBBox.bottom, coord[2].y,);
    })

    // Return the bounding box coordinates
    return combinedBBox
  },

  // Function to calculate padded coordinates 
  paddingCoords: function getPaddedCoords(coords, xHeight, padLeft, padRight, padTop, padBottom) {
    return {
      left: coords.left - padLeft * xHeight / 4,
      top: coords.top - padTop * xHeight / 4,
      right: coords.right + padRight * xHeight / 4,
      bottom: coords.bottom + padBottom * xHeight / 4
    }
  },

  calcBorder: function (heightObjects, widthObjects, xHeight, borderType) {
    // Get the bounding box of the active selection 
    const coordsWidth = BorderUtilities.getBoundingBox(widthObjects)
    const coordsHeight = BorderUtilities.getBoundingBox(heightObjects)
    const coords = { left: coordsWidth.left, top: coordsHeight.top, right: coordsWidth.right, bottom: coordsHeight.bottom }

    /*
      coords[0]: Top-left corner
      coords[1]: Top-right corner
      coords[2]: Bottom-right corner
      coords[3]: Bottom-left corner
    */

    innerBorderCoords = BorderUtilities.paddingCoords(coords, xHeight, borderType.PaddingLeft, borderType.PaddingRight, borderType.PaddingTop, borderType.PaddingNBottom,)
    outerBorderCoords = BorderUtilities.paddingCoords(innerBorderCoords, xHeight, borderType.FrameWidth, borderType.FrameWidth, borderType.FrameWidth, borderType.FrameWidth,)

    // Border Rounding
    borderWidth = outerBorderCoords.right - outerBorderCoords.left
    borderHeight = outerBorderCoords.bottom - outerBorderCoords.top
    if (borderWidth > 2000 || borderHeight > 2000) {
      roundingX = (50.0 * Math.round(borderWidth / 50.0) - borderWidth) / 2
      roundingY = (50.0 * Math.round(borderHeight / 50.0) - borderHeight) / 2
    } else {
      roundingX = (25.0 * Math.round(borderWidth / 25.0) - borderWidth) / 2
      roundingY = (25.0 * Math.round(borderHeight / 25.0) - borderHeight) / 2
      roundingX = roundingX < -2.5 ? roundingX = 25 / 2 + roundingX : roundingX
      roundingY = roundingY < -2.5 ? roundingY = 25 / 2 + roundingY : roundingY
    }

    outerBorderCoords = {
      left: outerBorderCoords.left - roundingX,
      top: outerBorderCoords.top - roundingY,
      right: outerBorderCoords.right + roundingX,
      bottom: outerBorderCoords.bottom + roundingY
    }

    innerBorderCoords = {
      left: innerBorderCoords.left - roundingX,
      top: innerBorderCoords.top - roundingY,
      right: innerBorderCoords.right + roundingX,
      bottom: innerBorderCoords.bottom + roundingY
    }
    console.log(outerBorderCoords.top - innerBorderCoords.top)
    return { in: innerBorderCoords, out: outerBorderCoords }
  },

  calcBorderRounding: function (borderType, xHeight, bbox) {
    const block = { width: bbox.right - bbox.left, height: bbox.bottom - bbox.top }
    const rounding = { x: 0, y: 0 }
    const shapeMeta = _template_js__WEBPACK_IMPORTED_MODULE_3__.BorderTypeScheme[borderType](xHeight, block, rounding)
    let roundingX = 0, roundingY = 0

    let minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;
    // Create polygon with labeled vertices
    shapeMeta.path.forEach(p => {
      p.vertex.forEach((vertex) => {
        vertex.x += bbox.left;
        vertex.y += bbox.top;
        minX = Math.min(minX, vertex.x);
        minY = Math.min(minY, vertex.y);
        maxX = Math.max(maxX, vertex.x);
        maxY = Math.max(maxY, vertex.y);
      });
    });
    let borderWidth = maxX - minX;
    let borderHeight = maxY - minY;

    if (borderWidth > 2000 || borderHeight > 2000) {
      roundingX = (50.0 * Math.round(borderWidth / 50.0) - borderWidth) / 2
      roundingY = (50.0 * Math.round(borderHeight / 50.0) - borderHeight) / 2
    } else {
      roundingX = (25.0 * Math.round(borderWidth / 25.0) - borderWidth) / 2
      roundingY = (25.0 * Math.round(borderHeight / 25.0) - borderHeight) / 2
      roundingX = roundingX < -2.5 ? roundingX = 25 / 2 + roundingX : roundingX
      roundingY = roundingY < -2.5 ? roundingY = 25 / 2 + roundingY : roundingY
    }
    return { x: roundingX, y: roundingY }
  },

  FilterDivider: function (heightObjects, widthObjects, VDivider, HDivider) {
    let HDividerObject = []
    if (HDivider) {
      HDividerObject.push(...HDivider)
    }
    let VDividerObject = []
    if (VDivider) {
      VDividerObject.push(...VDivider)
    }
    let borderedObjects = []
    let fwidthObjects = widthObjects.filter(obj => {
      if (obj.functionalType == 'HDivider' || obj.functionalType == 'HLine') {
        HDividerObject.push(obj);
        return false; // Remove the object from original array
      }
      return true; // Keep the object in original array
    });

    let fheightObjects = heightObjects.filter(obj => {
      if (obj.functionalType == 'VDivider' || obj.functionalType == 'VLane') {
        VDividerObject.push(obj);
        return false; // Remove the object from original array
      }
      return true; // Keep the object in original array
    });
    return [fheightObjects, fwidthObjects, VDividerObject, HDividerObject, borderedObjects]
  },


  getBorderObjectCoords: function (fheightObjects, fwidthObjects) {
    const coordsWidth = BorderUtilities.getBoundingBox(fwidthObjects)
    const coordsHeight = BorderUtilities.getBoundingBox(fheightObjects)
    return { left: coordsWidth.left, top: coordsHeight.top, right: coordsWidth.right, bottom: coordsHeight.bottom }
  },

  BorderGroupCreate: function (borderType, heightObjects, widthObjects, widthText, heightText, options = null) {
    const xHeight = options ? options.xHeight : parseInt(document.getElementById("input-xHeight").value)
    //const borderType = options ? options.borderType : document.getElementById("input-type").value
    const colorType = options ? options.colorType : document.getElementById("input-color").value



    try {
      //const BaseBorder = drawLabeledBorder(borderType, xHeight, coords, colorType)

      // Use the new BorderGroup class instead of BaseGroup
      const borderGroup = new BorderGroup({
        borderType: borderType,
        widthObjects: [...widthObjects],
        heightObjects: [...heightObjects],
        fixedWidth: widthText,
        fixedHeight: heightText,
        xHeight: xHeight,
        color: colorType,
        frame: _template_js__WEBPACK_IMPORTED_MODULE_3__.BorderFrameWidth[borderType],
        fixedWidthCoords: null,
        fixedHeightCoords: null
      });


      // Add mid points and assign widths to dividers
      //borderGroup.assignWidthToDivider(); // Ensure this await is always present
      //borderGroup.addMidPointToDivider();


      // Send border to back and update object references
      //canvas.sendObjectToBack(borderGroup);


      //canvas.renderAll();
      return borderGroup;
    } catch (error) {
      console.error("Error creating border group:", error);
      throw error; // Re-throw to allow calling function to handle errors
    }
  },

  // Find the object closest to a border edge
  findClosestObject(objects, direction, innerBorder) {
    if (!objects || objects.length === 0) return null;

    let closestObject = null;
    let minDistance = Number.MAX_VALUE;

    objects.forEach(obj => {
      if (!obj.getBoundingRect) return;

      const objRect = obj.getBoundingRect();
      let distance = 0;

      switch (direction) {
        case 'top':
          distance = objRect.top - innerBorder.top;
          break;
        case 'bottom':
          distance = innerBorder.bottom - (objRect.top + objRect.height);
          break;
        case 'left':
          distance = objRect.left - innerBorder.left;
          break;
        case 'right':
          distance = innerBorder.right - (objRect.left + objRect.width);
          break;
      }

      // Only consider positive distances (objects inside the border)
      if (distance >= 0 && distance < minDistance) {
        minDistance = distance;
        closestObject = obj;
      }
    });

    return closestObject;
  },

  // Additional border utility methods would go here
}

// Define BorderGroup class that extends BaseGroup
class BorderGroup extends _draw_js__WEBPACK_IMPORTED_MODULE_0__.BaseGroup {
  constructor(options = {}) {
    // Call the parent constructor with the base border and 'Border' functional type
    super(null, 'Border', 'BorderGroup', options);

    // Initialize border-specific properties
    this.widthObjects = options.widthObjects || [];
    this.heightObjects = options.heightObjects || [];
    this.fixedWidth = options.fixedWidth || null;
    this.fixedHeight = options.fixedHeight || null;
    this.borderType = options.borderType;
    this.xHeight = options.xHeight || 100;
    this.color = options.color;
    this.left = options.left || 0;
    this.top = options.top || 0;
    this.dimensionAnnotations = [];
    this.frame = _template_js__WEBPACK_IMPORTED_MODULE_3__.BorderFrameWidth[this.borderType]; // Frame width for the border
    this.defaultPadding = _template_js__WEBPACK_IMPORTED_MODULE_3__.BorderPaddingWidth[this.borderType] || { left: 0, top: 0, right: 0, bottom: 0 };
    this.inbbox = null; // Inner border bounding box
    this.rounding = { x: 0, y: 0 }; // Rounding values for the border
    this.compartmentBboxes = []; // Array of compartment bounding boxes
    this.VDivider = options.VDivider || [];
    this.HDivider = options.HDivider || [];

    // Cache for fixed dimension coordinates
    this.fixedWidthCoords = options.fixedWidthCoords || null;
    this.fixedHeightCoords = options.fixedHeightCoords || null;
    this._metadataKeys.push("fixedWidthCoords", "fixedHeightCoords");

    // Add status flag to track border updates
    this.isUpdating = false; // Flag to track if the border is currently being updated

    // Lock movement for border
    if (this.fixedWidth == null) {
      this.lockMovementX = true;
    }
    if (this.fixedHeight == null) {
      this.lockMovementY = true;
    }

    this.initialize(); // Call the initialize method to set up the border

    // Calculate initial bbox
    this.updateBboxes();
  }

  initialize() {
    if (this.inbbox == null) {
      this.filterBorderObjects();
      this.calcfixedBboxes(); // true indicates this is initialization
      this.rounding = BorderUtilities.calcBorderRounding(this.borderType, this.xHeight, this.inbbox);
    }

    this.setBasePolygon(this.drawBorder());

    //this.RoundingToDivider()
    this.assignWidthToDivider();
    canvas.sendObjectToBack(this);
    //DrawGrid();

    this.widthObjects.forEach(obj => {
      // Update the borderGroup reference in each width object
      obj.borderGroup = this;
    });
    this.heightObjects.forEach(obj => {
      // Update the borderGroup reference in each height object
      obj.borderGroup = this;
    });

  }

  drawBorder() {
    const rounding = JSON.parse(JSON.stringify(this.rounding)); // Deep copy rounding to avoid mutation
    const block = {
      width: this.inbbox.right - this.inbbox.left,
      height: this.inbbox.bottom - this.inbbox.top,
    };
    if (!isNaN(parseFloat(this.fixedWidth))) { rounding.x = 0 }
    if (!isNaN(parseFloat(this.fixedHeight))) { rounding.y = 0 }
    const shapeMeta = _template_js__WEBPACK_IMPORTED_MODULE_3__.BorderTypeScheme[this.borderType](this.xHeight, block, rounding);
    const baseGroup = [];

    // Create polygon with labeled vertices
    shapeMeta.path.forEach((p) => {

      p.vertex.forEach((vertex) => {
        vertex.x = vertex.x + this.inbbox.left;
        vertex.y = vertex.y + this.inbbox.top;
      });

      const pathData = (0,_path_js__WEBPACK_IMPORTED_MODULE_4__.vertexToPath)({ path: [p] });
      // Extract the d attribute if pathData is a full SVG string
      const dValue = pathData.includes('<path')
        ? pathData.match(/d="([^"]+)"/)?.[1] || pathData
        : pathData;

      const borderShift = p.fill == 'background' ? this.frame * this.xHeight / 4 : 0;

      const vertexleft = Math.min(...p.vertex.map(v => v.x));
      const vertextop = Math.min(...p.vertex.map(v => v.y));

      baseGroup.push(
        new fabric.Path(dValue, {
          left: vertexleft,
          top: vertextop,
          fill: (p['fill'] == 'background') || (p['fill'] == 'symbol') || (p['fill'] == 'border') ? _template_js__WEBPACK_IMPORTED_MODULE_3__.BorderColorScheme[this.color][p['fill']] : p['fill'],
          objectCaching: false,
          strokeWidth: 0,
        })
      );
    });

    const GroupedBorder = new fabric.Group(baseGroup);
    GroupedBorder.vertex = shapeMeta.path.map(p => p.vertex).flat();

    return GroupedBorder;
  }
  // Show border dimensions when selected
  showDimensions() {
    // Clean up any existing dimension annotations
    this.hideDimensions();

    // Get border coordinates
    const borderRect = this.getBoundingRect();
    const frame = this.frame * this.xHeight / 4;

    // Find closest objects in each direction to show dimensions
    this.createDimensionAnnotations(borderRect);

    // Find closest objects in each direction to show dimensions
    this.createBorderDimensionAnnotations(borderRect, frame);

    // Show radius dimension for one corner
    this.createCornerRadiusDimension(borderRect);
  }

  // Override hideDimensions to handle both BorderDimensionDisplay and RadiusDimensionDisplay
  hideDimensions() {
    // Remove all dimension annotations from canvas
    this.dimensionAnnotations.forEach(annotation => {
      if (annotation.remove && typeof annotation.remove === 'function') {
        // For RadiusDimensionDisplay which has a remove method
        annotation.remove();
      } else if (annotation.objects && Array.isArray(annotation.objects)) {
        // For BorderDimensionDisplay which has objects array
        canvas.remove(...annotation.objects);
      }
    });
    this.dimensionAnnotations = [];
  }

  // Create dimension annotations for the border and contained objects
  createBorderDimensionAnnotations(borderRect, frame) {
    // Calculate inner content area of border (accounting for frame)
    const innerBorder = {
      left: borderRect.left + frame,
      top: borderRect.top + frame,
      right: borderRect.left + borderRect.width - frame,
      bottom: borderRect.top + borderRect.height - frame,
      width: borderRect.width - (2 * frame),
      height: borderRect.height - (2 * frame)
    };

    const frameDimension = new _dimension_js__WEBPACK_IMPORTED_MODULE_1__.BorderDimensionDisplay({
      direction: 'vertical',
      startX: innerBorder.left + (innerBorder.width / 3),
      startY: innerBorder.top,
      endY: innerBorder.top - frame,
      color: 'red',
      offset: 30 / canvas.getZoom(),
      baseObject: this
    });
    this.dimensionAnnotations.push(frameDimension);

    // Create horizontal dimensions (left and right)
    if (this.widthObjects.length > 0) {
      // Find object closest to left border
      const leftObject = this.findClosestObject(this.widthObjects, 'left', innerBorder);
      if (leftObject) {
        const leftObjectRect = leftObject.getBoundingRect();
        // Create left dimension annotation
        const leftDimension = new _dimension_js__WEBPACK_IMPORTED_MODULE_1__.BorderDimensionDisplay({
          direction: 'horizontal',
          startX: innerBorder.left,
          startY: leftObjectRect.top + (leftObjectRect.height / 2),
          endX: leftObjectRect.left,
          color: '#46C147',
          offset: 30 / canvas.getZoom(),
          baseObject: this
        });
        this.dimensionAnnotations.push(leftDimension);
      }

      // Find object closest to right border
      const rightObject = this.findClosestObject(this.widthObjects, 'right', innerBorder);
      if (rightObject) {
        const rightObjectRect = rightObject.getBoundingRect();
        // Create right dimension annotation
        const rightDimension = new _dimension_js__WEBPACK_IMPORTED_MODULE_1__.BorderDimensionDisplay({
          direction: 'horizontal',
          startX: rightObjectRect.left + rightObjectRect.width,
          startY: rightObjectRect.top + (rightObjectRect.height / 2),
          endX: innerBorder.right,
          color: '#46C147',
          offset: 30 / canvas.getZoom(),
          baseObject: this
        });
        this.dimensionAnnotations.push(rightDimension);
      }
    }

    // Create vertical dimensions (top and bottom)
    if (this.heightObjects.length > 0) {
      // Find object closest to top border
      const topObject = this.findClosestObject(this.heightObjects, 'top', innerBorder);
      if (topObject) {
        const topObjectRect = topObject.getBoundingRect();
        // Create top dimension annotation
        const topDimension = new _dimension_js__WEBPACK_IMPORTED_MODULE_1__.BorderDimensionDisplay({
          direction: 'vertical',
          startX: topObjectRect.left + (topObjectRect.width / 2),
          startY: innerBorder.top,
          endY: topObjectRect.top,
          color: 'red',
          offset: 30 / canvas.getZoom(),
          baseObject: this
        });
        this.dimensionAnnotations.push(topDimension);
      }

      // Find object closest to bottom border
      const bottomObject = this.findClosestObject(this.heightObjects, 'bottom', innerBorder);
      if (bottomObject) {
        const bottomObjectRect = bottomObject.getBoundingRect();
        // Create bottom dimension annotation
        const bottomDimension = new _dimension_js__WEBPACK_IMPORTED_MODULE_1__.BorderDimensionDisplay({
          direction: 'vertical',
          startX: bottomObjectRect.left + (bottomObjectRect.width / 2),
          startY: bottomObjectRect.top + bottomObjectRect.height,
          endY: innerBorder.bottom,
          color: 'red',
          offset: 30 / canvas.getZoom(),
          baseObject: this
        });
        this.dimensionAnnotations.push(bottomDimension);
      }
    }
  }

  // Create radius dimension for one corner of the border
  createCornerRadiusDimension(borderRect) {
    // Get the border template to check for corner radius information
    const block = { width: borderRect.width, height: borderRect.height };
    const shapeMeta = _template_js__WEBPACK_IMPORTED_MODULE_3__.BorderTypeScheme[this.borderType](this.xHeight, block, this.rounding);

    // Look for the first vertex with a radius property (typically top-left corner)
    let cornerRadius = null;
    let cornerVertex = null;

    if (shapeMeta && shapeMeta.path && shapeMeta.path.length > 0) {
      for (const path of shapeMeta.path) {
        if (path.vertex && path.vertex.length > 0) {
          for (const vertex of path.vertex) {
            if (vertex.radius && vertex.radius > 0) {
              cornerRadius = vertex.radius; // Convert to actual size
              cornerVertex = vertex;
              break;
            }
          }
          if (cornerRadius) break;
        }
      }
    }

    // If we found a corner with radius, create radius dimension
    if (cornerRadius && cornerRadius > 0) {
      // For most borders, the top-left corner would be at borderRect position
      // We'll show the radius dimension for the top-left corner
      const isFlagRight = this.borderType === 'flagRight';
      const centerX = borderRect.left + (isFlagRight ? cornerRadius : borderRect.width - cornerRadius);
      const centerY = borderRect.top + borderRect.height - cornerRadius;

      const radiusDimension = new _dimension_js__WEBPACK_IMPORTED_MODULE_1__.RadiusDimensionDisplay({
        centerX: centerX,
        centerY: centerY,
        radius: cornerRadius,
        color: 'teal',
        startAngle: (isFlagRight ? 120 : 60)  // 45 degrees (pointing toward top-left from center)
      });

      this.dimensionAnnotations.push(radiusDimension);
    }
  }

  // Find the object closest to a border edge
  findClosestObject(objects, direction, innerBorder) {
    if (!objects || objects.length === 0) return null;

    let closestObject = null;
    let minDistance = Number.MAX_VALUE;

    objects.forEach(obj => {
      if (!obj.getBoundingRect) return;

      const objRect = obj.getBoundingRect();
      let distance = 0;

      switch (direction) {
        case 'top':
          distance = objRect.top - innerBorder.top;
          break;
        case 'bottom':
          distance = innerBorder.bottom - (objRect.top + objRect.height);
          break;
        case 'left':
          distance = objRect.left - innerBorder.left;
          break;
        case 'right':
          distance = innerBorder.right - (objRect.left + objRect.width);
          break;
      }

      // Only consider positive distances (objects inside the border)
      if (distance >= 0 && distance < minDistance) {
        minDistance = distance;
        closestObject = obj;
      }
    });

    return closestObject;
  }

  // Add mid points to divider
  addMidPointToDivider() {
    // Make sure bbox is updated - using existing code
    //this.updateBboxes();

    const bbox = this.inbbox;
    let i = 0;

    // First clear any existing mid points from previous calculations
    this.basePolygon.vertex = this.basePolygon.vertex.filter(v => !v.label || !v.label.startsWith('C'));

    // Loop through each compartment and add midpoints on each edge
    this.compartmentBboxes.forEach((compartment, index) => {
      // Calculate midpoints for each edge of the compartment
      const midX = (compartment.left + compartment.right) / 2;
      const midY = (compartment.top + compartment.bottom) / 2;

      // Add midpoint on top edge
      if (compartment.top == bbox.top) {
        this.basePolygon.vertex.push({ x: midX, y: compartment.top, label: `C${i += 1}`, display: 1 });
      }

      // Add midpoint on right edge
      if (compartment.right == bbox.right) {
        this.basePolygon.vertex.push({ x: compartment.right, y: midY, label: `C${i += 1}`, display: 1 });
      }

      // Add midpoint on bottom edge
      if (compartment.bottom == bbox.bottom) {
        this.basePolygon.vertex.push({ x: midX, y: compartment.bottom, label: `C${i += 1}`, display: 1 });
      }

      // Add midpoint on left edge
      if (compartment.left == bbox.left) {
        this.basePolygon.vertex.push({ x: compartment.left, y: midY, label: `C${i += 1}`, display: 1 });
      }
    });
  }

  RoundingToDivider() {
    // No longer need sourceList parameter
    this.rounding.x /= (this.VDivider.length + 1) * 2;
    this.rounding.y /= (this.HDivider.length + 1) * 2;

    this.HDivider.forEach(h => {
      // Skip rounding if divider has fixed distance values
      if (h.fixedTopValue || h.fixedBottomValue) {
        return;
      }

      // Check if the target object is already being updated in the Y cycle
      const targetObj = h.lockYToPolygon.TargetObject;
      if (targetObj && h.functionalType == 'HDivider') {
        // Directly update anchor properties instead of removing and recreating
        if (h.lockYToPolygon && Object.keys(h.lockYToPolygon).length > 0) {
          // Update spacing value
          h.lockYToPolygon.spacingY = _template_js__WEBPACK_IMPORTED_MODULE_3__.DividerMargin[h.functionalType]['top'] * h.xHeight / 4 + this.rounding.y;

          // Update object position
          const sourcePoint = h.lockYToPolygon.sourcePoint;
          const targetPoint = h.lockYToPolygon.targetPoint;

          // Calculate new position
          const sourceVertexY = targetObj.getBasePolygonVertex(targetPoint).y;
          const newY = sourceVertexY + h.lockYToPolygon.spacingY;
          const deltaY = newY - h.getBasePolygonVertex(sourcePoint).y;

          // Update object position and vertices
          h.set('top', h.top + deltaY);
          if (h.basePolygon && h.basePolygon.vertex) {
            h.basePolygon.vertex.forEach(v => {
              v.y += deltaY;
            });
          }
        }
      }

      // Check the next anchored object (assuming it's the one below)
      const nextAnchor = h.anchoredPolygon && h.anchoredPolygon.length > 0 ? h.anchoredPolygon[0] : null;
      if (nextAnchor) {
        // Directly update anchor properties instead of removing and recreating
        if (nextAnchor.lockYToPolygon && Object.keys(nextAnchor.lockYToPolygon).length > 0) {
          // Update spacing value
          nextAnchor.lockYToPolygon.spacingY = _template_js__WEBPACK_IMPORTED_MODULE_3__.DividerMargin[h.functionalType]['bottom'] * h.xHeight / 4 + this.rounding.y;

          // Update object position
          const sourcePoint = nextAnchor.lockYToPolygon.sourcePoint;
          const targetPoint = nextAnchor.lockYToPolygon.targetPoint; // Target point on the *divider* (h)

          // Calculate new position based on the divider (h)
          const sourceVertexY = h.getBasePolygonVertex(targetPoint).y;
          const newY = sourceVertexY + nextAnchor.lockYToPolygon.spacingY;
          const deltaY = newY - nextAnchor.getBasePolygonVertex(sourcePoint).y;

          // Update object position and vertices
          nextAnchor.set('top', nextAnchor.top + deltaY);
          if (nextAnchor.basePolygon && nextAnchor.basePolygon.vertex) {
            nextAnchor.basePolygon.vertex.forEach(v => {
              v.y += deltaY;
            });
          }
          // Mark as updated in this cycle if part of an ongoing update
          if (!_anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.updateInProgressY) {
            const updateOrderY = _anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.getUpdateOrder('y', nextAnchor.canvasID);
            (0,_anchor_js__WEBPACK_IMPORTED_MODULE_2__.processUpdateCycle)('y', nextAnchor, updateOrderY, null, deltaY);
          }
        }
      }
    });

    this.VDivider.forEach(v => {
      // Skip rounding if divider has fixed distance values
      if (v.fixedLeftValue || v.fixedRightValue) {
        return;
      }

      // Check if the target object is already being updated in the X cycle
      const targetObj = v.lockXToPolygon.TargetObject;
      if (targetObj && (v.functionalType == 'VDivider' || v.functionalType == 'VLane')) {
        // Directly update anchor properties instead of removing and recreating
        if (v.lockXToPolygon && Object.keys(v.lockXToPolygon).length > 0) {
          // Update spacing value
          v.lockXToPolygon.spacingX = _template_js__WEBPACK_IMPORTED_MODULE_3__.DividerMargin[v.functionalType]['left'] * v.xHeight / 4 + this.rounding.x;

          // Update object position
          const sourcePoint = v.lockXToPolygon.sourcePoint;
          const targetPoint = v.lockXToPolygon.targetPoint;

          // Calculate new position
          const sourceVertexX = targetObj.getBasePolygonVertex(targetPoint).x;
          const newX = sourceVertexX + v.lockXToPolygon.spacingX;
          const deltaX = newX - v.getBasePolygonVertex(sourcePoint).x;

          // Update object position and vertices
          v.set('left', v.left + deltaX);
          if (v.basePolygon && v.basePolygon.vertex) {
            v.basePolygon.vertex.forEach(vertex => {
              vertex.x += deltaX;
            });
          }
        }
      }

      // Check the next anchored object (assuming it's the one to the right)
      const nextAnchor = v.anchoredPolygon && v.anchoredPolygon.length > 0 ? v.anchoredPolygon[0] : null;
      if (nextAnchor) {
        // Directly update anchor properties instead of removing and recreating
        if (nextAnchor.lockXToPolygon && Object.keys(nextAnchor.lockXToPolygon).length > 0) {
          // Update spacing value
          nextAnchor.lockXToPolygon.spacingX = _template_js__WEBPACK_IMPORTED_MODULE_3__.DividerMargin[v.functionalType]['right'] * v.xHeight / 4 + this.rounding.x;

          // Update object position
          const sourcePoint = nextAnchor.lockXToPolygon.sourcePoint;
          const targetPoint = nextAnchor.lockXToPolygon.targetPoint; // Target point on the *divider* (v)

          // Calculate new position based on the divider (v)
          const sourceVertexX = v.getBasePolygonVertex(targetPoint).x;
          const newX = sourceVertexX + nextAnchor.lockXToPolygon.spacingX;
          const deltaX = newX - nextAnchor.getBasePolygonVertex(sourcePoint).x;

          // Update object position and vertices
          nextAnchor.set('left', nextAnchor.left + deltaX);
          if (nextAnchor.basePolygon && nextAnchor.basePolygon.vertex) {
            nextAnchor.basePolygon.vertex.forEach(vertex => {
              vertex.x += deltaX;
            });
          }
          // Mark as updated in this cycle if part of an ongoing update
          if (!_anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.updateInProgressX) {
            const updateOrderX = _anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.getUpdateOrder('x', nextAnchor.canvasID);
            (0,_anchor_js__WEBPACK_IMPORTED_MODULE_2__.processUpdateCycle)('x', nextAnchor, updateOrderX, deltaX, null);
          }
        }
        // nextAnchor.rounded = { x: rounding.x, y: 0 } // This seems out of place, commenting out for now. Re-evaluate if needed.
      }
    });
  }

  // Helper method to update divider coordinates without triggering recursive updates
  updateDividerCoords(divider) {
    if (!divider.basePolygon || !divider.basePolygon.vertex) return;


    divider.updateAllCoord()
  }

  filterBorderObjects() {
    const [fheightObjects, fwidthObjects, VDivider, HDivider, bordered] = BorderUtilities.FilterDivider(this.heightObjects, this.widthObjects, this.VDivider, this.HDivider)
    this.widthObjects = fwidthObjects.filter(obj => obj.borderGroup == null);
    this.heightObjects = fheightObjects.filter(obj => obj.borderGroup == null);
    this.VDivider = VDivider
    this.HDivider = HDivider
  }

  calcfixedBboxes() {
    // Get the bounding box of the active selection for dynamic dimensions (content-driven)
    let coords = BorderUtilities.getBorderObjectCoords(this.heightObjects, this.widthObjects);

    // Debug: check for NaN values in initial coords
    if (isNaN(coords.left) || isNaN(coords.top) || isNaN(coords.right) || isNaN(coords.bottom)) {
      console.error('calcfixedBboxes: Initial coords contain NaN values:', coords);
      console.error('heightObjects:', this.heightObjects);
      console.error('widthObjects:', this.widthObjects);
      // Set default values to prevent further NaN propagation
      coords = { left: 0, top: 0, right: 100, bottom: 100 };
    }

    this.innerPadding = { x: 0, y: 0 };

    // Apply inner padding
    //coords.left -= this.innerPadding.x / (this.VDivider.length + 1) / 2;
    //coords.right += this.innerPadding.x / (this.VDivider.length + 1) / 2;
    //coords.top -= this.innerPadding.y / (this.HDivider.length + 1) / 2;
    //coords.bottom += this.innerPadding.y / (this.HDivider.length + 1) / 2;

    // Determine if width and height are fixed
    const hasFixedWidth = !isNaN(parseInt(this.fixedWidth));
    const hasFixedHeight = !isNaN(parseInt(this.fixedHeight));

    // Handle width calculation
    if (hasFixedWidth) {
      const leftPadding = (this.frame + this.defaultPadding.left) * this.xHeight / 4;
      const rightPadding = (this.frame + this.defaultPadding.right) * this.xHeight / 4;
      if (!this.fixedWidthCoords) {
        // Calculate fixed width coordinates only during initialization or if not cached
        if (this.widthObjects.length === 0) {
          const borderCoords = canvas.calcViewportBoundaries();

          if (!borderCoords || !borderCoords.tl || !borderCoords.br) {
            console.error('calcfixedBboxes: Invalid borderCoords:', borderCoords);
          } else {
            const centerX = (borderCoords.tl.x + borderCoords.br.x) / 2;

            this.fixedWidthCoords = {
              left: centerX - parseInt(this.fixedWidth) / 2 + leftPadding,
              right: centerX + parseInt(this.fixedWidth) / 2 - rightPadding
            };

          }
        } else {
          // If there are widthObjects, center the fixed width around their midpoint
          const midX = (coords.left + coords.right) / 2;
          this.fixedWidthCoords = {
            left: midX - parseInt(this.fixedWidth) / 2 + leftPadding,
            right: midX + parseInt(this.fixedWidth) / 2 - rightPadding
          };
        }
      } else {
        // For non-initialization calls, ensure coords are updated to current fixed values
        this.fixedWidthCoords.left = this.left + leftPadding;
        this.fixedWidthCoords.right = this.left + this.fixedWidth - rightPadding;

      }

      // Use cached fixed width coordinates
      if (this.fixedWidthCoords) {
        coords.left = this.fixedWidthCoords.left;
        coords.right = this.fixedWidthCoords.right;
      }
    }
    // If width is not fixed, use the calculated dynamic coordinates (already set above)

    // Handle height calculation
    if (hasFixedHeight) {
      const topPadding = (this.frame + this.defaultPadding.top) * this.xHeight / 4;
      const bottomPadding = (this.frame + this.defaultPadding.bottom) * this.xHeight / 4;

      if (!this.fixedHeightCoords) {
        // Calculate fixed height coordinates only during initialization or if not cached
        if (this.heightObjects.length === 0) {
          const borderCoords = canvas.calcViewportBoundaries();

          if (!borderCoords || !borderCoords.tl || !borderCoords.br) {
            console.error('calcfixedBboxes: Invalid borderCoords:', borderCoords);
          } else {
            const centerY = (borderCoords.tl.y + borderCoords.br.y) / 2;

            this.fixedHeightCoords = {
              top: centerY - parseInt(this.fixedHeight) / 2 + topPadding,
              bottom: centerY + parseInt(this.fixedHeight) / 2 - bottomPadding
            };

          }
        } else {
          // If there are heightObjects, center the fixed height around their midpoint
          const midY = (coords.top + coords.bottom) / 2;
          this.fixedHeightCoords = {
            top: midY - parseInt(this.fixedHeight) / 2 + topPadding,
            bottom: midY + parseInt(this.fixedHeight) / 2 - bottomPadding
          };
        }
      } else {
        // For non-initialization calls, ensure coords are updated to current fixed values
        this.fixedHeightCoords.top = this.top + topPadding;
        this.fixedHeightCoords.bottom = this.top + this.fixedHeight - bottomPadding;

      }


      // Use cached fixed height coordinates
      if (this.fixedHeightCoords) {
        coords.top = this.fixedHeightCoords.top;
        coords.bottom = this.fixedHeightCoords.bottom;
      }
    }
    // If height is not fixed, use the calculated dynamic coordinates (already set above)

    // Final check for NaN values before setting
    if (isNaN(coords.left) || isNaN(coords.top) || isNaN(coords.right) || isNaN(coords.bottom)) {
      console.error('calcfixedBboxes: Final coords contain NaN values:', coords);
      // Set default values to prevent further issues
      coords = { left: 0, top: 0, right: 100, bottom: 100 };
    }

    this.inbbox = coords;
  }

  // Update the bbox property and compartmentBboxes array
  updateBboxes() {
    const borderSize = this.getBoundingRect();
    const frame = 1.5 * this.xHeight / 4;

    // Calculate main bbox (inner area of the border)
    this.inbbox = {
      left: borderSize.left + frame,
      top: borderSize.top + frame,
      right: borderSize.left + borderSize.width - frame,
      bottom: borderSize.top + borderSize.height - frame,
      width: borderSize.width - (2 * frame),
      height: borderSize.height - (2 * frame)
    };

    if (this.fixedHeight !== null) {
      const topPadding = (this.frame + this.defaultPadding.top) * this.xHeight / 4;
      const bottomPadding = (this.frame + this.defaultPadding.bottom) * this.xHeight / 4;
      this.fixedHeightCoords.top = this.top + topPadding;
      this.fixedHeightCoords.bottom = this.top + this.fixedHeight - bottomPadding;
    }

    if (this.fixedWidth !== null) {
      const leftPadding = (this.frame + this.defaultPadding.left) * this.xHeight / 4;
      const rightPadding = (this.frame + this.defaultPadding.right) * this.xHeight / 4;
      this.fixedWidthCoords.left = this.left + leftPadding;
      this.fixedWidthCoords.right = this.left + this.fixedWidth - rightPadding;
    }

    // Reset compartment bboxes
    this.compartmentBboxes = [];

    // If there are no dividers, the entire inner area is one compartment
    if (this.VDivider.length === 0 && this.HDivider.length === 0) {
      this.compartmentBboxes.push({ ...this.inbbox });
      return;
    }

    // Calculate compartments based on dividers
    this.calculateCompartments();
  }

  // Calculate compartments created by dividers
  calculateCompartments() {
    // Get sorted dividers
    const sortedVDividers = this.VDivider.length > 0 ?
      [...this.VDivider].sort((a, b) => (a.getEffectiveCoords()[0].x + a.width / 2) - (b.getEffectiveCoords()[0].x + b.width / 2)) :
      [];

    const sortedHDividers = this.HDivider.length > 0 ?
      [...this.HDivider].sort((a, b) => (a.getEffectiveCoords()[0].y + a.height / 2) - (b.getEffectiveCoords()[0].y + b.height / 2)) :
      [];

    // Get divider positions, only considering them as position points, not actual width
    const xPositions = [this.inbbox.left];
    const yPositions = [this.inbbox.top];

    // Add all vertical divider center positions
    for (const vd of sortedVDividers) {
      const leftX = vd.getEffectiveCoords()[0].x; // top-left x
      const centerX = leftX + vd.width / 2;
      xPositions.push(centerX);
    }
    xPositions.push(this.inbbox.right);

    // Add all horizontal divider center positions
    for (const hd of sortedHDividers) {
      const topY = hd.getEffectiveCoords()[0].y; // top-left y
      const centerY = topY + hd.height / 2;
      yPositions.push(centerY);
    }
    yPositions.push(this.inbbox.bottom);

    // Create compartment bboxes based on divider positions
    for (let i = 0; i < xPositions.length - 1; i++) {
      for (let j = 0; j < yPositions.length - 1; j++) {
        // Get the adjacent dividers to calculate adjusted compartment boundaries
        const leftDivider = i > 0 ? sortedVDividers[i - 1] : null;
        const rightDivider = i < sortedVDividers.length ? sortedVDividers[i] : null;
        const topDivider = j > 0 ? sortedHDividers[j - 1] : null;
        const bottomDivider = j < sortedHDividers.length ? sortedHDividers[j] : null;

        // Get base positions
        let left = xPositions[i];
        let top = yPositions[j];
        let right = xPositions[i + 1];
        let bottom = yPositions[j + 1];

        // Adjust boundaries using half divider thickness if shadowWidth provided (centerline scheme)
        if (rightDivider && rightDivider.shadowWidth) {
          const shadowX = rightDivider.shadowWidth.x * rightDivider.xHeight / 4;
          right += shadowX / 2; // extend to include half of right divider's shadow influence
        }
        if (leftDivider && leftDivider.shadowWidth) {
          const shadowX = leftDivider.shadowWidth.x * leftDivider.xHeight / 4;
          left -= shadowX / 2; // retract to include half of left divider's shadow influence
        }
        if (bottomDivider && bottomDivider.shadowWidth) {
          const shadowY = bottomDivider.shadowWidth.y * bottomDivider.xHeight / 4;
          bottom += shadowY / 2;
        }
        if (topDivider && topDivider.shadowWidth) {
          const shadowY = topDivider.shadowWidth.y * topDivider.xHeight / 4;
          top -= shadowY / 2;
        }

        // Add the compartment with adjusted dimensions
        this.compartmentBboxes.push({
          left,
          top,
          right,
          bottom,
          width: right - left,
          height: bottom - top,
          index: this.compartmentBboxes.length
        });
      }
    }
  }

  // Assign width / position to dividers using border inner bbox only
  assignWidthToDivider() {
    this.isUpdating = true;
    // Always rebuild bboxes and compartments first
    this.updateBboxes();

    // Vertical dividers
    for (const d of this.VDivider) {
      const initialLeft = d.getEffectiveCoords()[0].x;
      const res = (0,_divider_js__WEBPACK_IMPORTED_MODULE_6__.drawDivider)(
        d.xHeight,
        d.color,
        { left: d.left - _template_js__WEBPACK_IMPORTED_MODULE_3__.DividerMargin[d.functionalType].left * d.xHeight / 4, top: d.top },
        this.inbbox,
        d.functionalType
      );
      d.replaceBasePolygon(res, false);
      d.set({
        top: this.inbbox.bottom - d.height - _template_js__WEBPACK_IMPORTED_MODULE_3__.DividerMargin[d.functionalType]['bottom'] * d.xHeight / 4,
      });
      const minLeft = this.inbbox.left + (this.frame) * d.xHeight / 4;
      const maxLeft = this.inbbox.right - (this.frame) * d.xHeight / 4 - d.width;
      const clampedLeft = maxLeft >= minLeft ? Math.min(Math.max(initialLeft, minLeft), maxLeft) : minLeft;
      d.set({ left: clampedLeft });
      d.lockMovementY = true;
    }

    // Horizontal dividers
    for (const d of this.HDivider) {
      if (d.functionalType !== 'HLine') {
        const initialTop = d.getEffectiveCoords()[0].y;
        const res = (0,_divider_js__WEBPACK_IMPORTED_MODULE_6__.drawDivider)(
          d.xHeight,
          d.color,
          { left: d.left, top: d.top - _template_js__WEBPACK_IMPORTED_MODULE_3__.DividerMargin[d.functionalType].top * d.xHeight / 4 },
          this.inbbox,
          d.functionalType
        );
        d.replaceBasePolygon(res, false);
        d.set({
          left: this.inbbox.left + _template_js__WEBPACK_IMPORTED_MODULE_3__.DividerMargin[d.functionalType]['left'] * d.xHeight / 4
        });
        const minTop = this.inbbox.top + (this.frame) * d.xHeight / 4;
        const maxTop = this.inbbox.bottom - (this.frame) * d.xHeight / 4 - d.height;
        const clampedTop = maxTop >= minTop ? Math.min(Math.max(initialTop, minTop), maxTop) : minTop;
        d.set({ top: clampedTop });
        d.lockMovementX = true;
      }
    }

    // Refresh compartments & midpoints after reposition
    this.updateBboxes();
    this.assignWidthToUnderline();
    this.addMidPointToDivider();
    this.drawVertex();
    this.isUpdating = false;
  }

  assignWidthToUnderline() {
    // Collect unique text objects that have an underline DividerObject attached
    const underlineObjects = new Set();
    this.HDivider.forEach(obj => {
      if (obj.functionalType === 'HLine') underlineObjects.add(obj);
    });


    // Helper to get left/right X from an object's effective coords
    const getLeftRightX = (obj) => {
      const coords = obj.getEffectiveCoords();
      const xs = [coords[0].x, coords[1].x, coords[2].x, coords[3].x];
      return { left: Math.min(...xs), right: Math.max(...xs) };
    };

    // Only consider actual vertical dividers for boundaries
    const verticalDividers = (this.VDivider || []).filter(d => d.functionalType === 'VDivider');

    underlineObjects.forEach(underline => {

      // One stroke width in canvas units (length = xHeight/4)
      const stroke = underline.xHeight / 4;
      const initialTop = underline.getEffectiveCoords()[0].y;

      // Determine the search reference from the text's bounds
      const { left: textLeft, right: textRight } = getLeftRightX(underline.textObject);

      // Find nearest left/right VDivider in a single pass
      let leftBoundary = this.inbbox.left;
      let rightBoundary = this.inbbox.right;
      let maxRightEdgeLeftOfText = -Infinity;
      let minLeftEdgeRightOfText = Infinity;
      verticalDividers.forEach(vd => {
        const vdCoords = vd.getEffectiveCoords();
        const vdLeft = Math.min(vdCoords[0].x, vdCoords[1].x, vdCoords[2].x, vdCoords[3].x);
        const vdRight = Math.max(vdCoords[0].x, vdCoords[1].x, vdCoords[2].x, vdCoords[3].x);

        // Consider margins when determining adjacency
        const rightMargin = _template_js__WEBPACK_IMPORTED_MODULE_3__.DividerMargin[vd.functionalType].right * vd.xHeight / 4;
        const leftMargin = _template_js__WEBPACK_IMPORTED_MODULE_3__.DividerMargin[vd.functionalType].left * vd.xHeight / 4;

        // Candidate for left boundary: nearest right edge to the left of text
        if (vdRight - rightMargin <= textLeft && vdRight - rightMargin > maxRightEdgeLeftOfText) {
          maxRightEdgeLeftOfText = vdRight - rightMargin;
        }

        // Candidate for right boundary: nearest left edge to the right of text
        if (vdLeft + leftMargin >= textRight && vdLeft + leftMargin < minLeftEdgeRightOfText) {
          minLeftEdgeRightOfText = vdLeft + leftMargin;
        }
      });
      if (maxRightEdgeLeftOfText !== -Infinity) leftBoundary = maxRightEdgeLeftOfText;
      if (minLeftEdgeRightOfText !== Infinity) rightBoundary = minLeftEdgeRightOfText;

      // Inset by one stroke width from both boundaries
      const targetLeft = leftBoundary;
      const targetRight = rightBoundary;
      const targetWidth = Math.max(0, targetRight - targetLeft);

      // Redraw and position the underline to span the computed width
      const res = (0,_divider_js__WEBPACK_IMPORTED_MODULE_6__.drawDivider)(
        underline.xHeight,
        underline.color,
        { left: targetLeft, top: initialTop - stroke },
        { width: targetWidth, height: stroke },
        'HLine'
      );
      underline.replaceBasePolygon(res, false);
      //underline.set({ left: targetLeft + 1.5 * stroke,  });
      underline.drawVertex(false)
      underline.setCoords();
    });

  }

  // Optimized method to process border resize without causing infinite loops
  processResize() {
    const BG = this;
    const prevInbbox = this.inbbox ? { left: this.inbbox.left, top: this.inbbox.top, right: this.inbbox.right, bottom: this.inbbox.bottom } : null;
    // Compute potential new bbox first
    this.calcfixedBboxes(false);
    const newInbbox = this.inbbox;
    const noChange = prevInbbox &&
      prevInbbox.left === newInbbox.left &&
      prevInbbox.top === newInbbox.top &&
      prevInbbox.right === newInbbox.right &&
      prevInbbox.bottom === newInbbox.bottom;
    if (noChange) {
      return; // Skip expensive resize if dimensions unchanged
    }

    BG.removeAll();
    this.rounding = BorderUtilities.calcBorderRounding(this.borderType, this.xHeight, this.inbbox);
    const borderObject = this.drawBorder();
    BG.add(borderObject);
    BG.basePolygon = borderObject;
    BG.assignWidthToDivider();
    let startedXCycle = false;
    let startedYCycle = false;
    try {
      if (!_anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.updateInProgressX) { _anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.startUpdateCycle('x', BG.canvasID); startedXCycle = true; }
      if (!_anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.updateInProgressY) { _anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.startUpdateCycle('y', BG.canvasID); startedYCycle = true; }
      BG.updateAllCoord(null, [], false);
      BG.drawVertex();
    } finally {
      if (startedXCycle) _anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.endUpdateCycle('x');
      if (startedYCycle) _anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.endUpdateCycle('y');
    }
    BG.refTopLeft = { top: BG.basePolygon.getCoords()[0].y, left: BG.basePolygon.getCoords()[0].x };
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_5__.CanvasGlobals.scheduleRender();
  }

  // Override updateAllCoord - need to make sure trees are updated correctly
  updateAllCoord(event, _sourceList = [], _selfOnly = false) {
    // Call parent implementation first
    super.updateAllCoord(event, _sourceList, _selfOnly);
    this.updateBboxes();

    // Mark that a border update is in progress
    this.isUpdating = true;

    try {
      // Only process groups that we own (for multi-box borders)
      if (this.compartmentBboxes && this.compartmentBboxes.length > 0) {

        // First handle horizontal dividers
        if (this.HDivider && this.HDivider.length > 0) {
          // Start an X update cycle for this object's ID
          _anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.startUpdateCycle('x', this.canvasID);

          // Update each divider
          this.HDivider.forEach(divider => {
            // Mark divider as updated in X axis to prevent cycles
            _anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.updatedObjectsX.add(divider.canvasID);

            // Update divider position and coordinates
            divider.set('left', divider.left);
            divider.setCoords();
            divider.updateAllCoord(null, [this]);
          });

          // End the X update cycle
          _anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.endUpdateCycle('x');
        }

        // Then handle vertical dividers
        if (this.VDivider && this.VDivider.length > 0) {
          // Start a Y update cycle for this object's ID
          _anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.startUpdateCycle('y', this.canvasID);

          // Update each divider
          this.VDivider.forEach(divider => {
            // Mark divider as updated in Y axis to prevent cycles
            _anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.updatedObjectsY.add(divider.canvasID);

            // Update divider position and coordinates
            divider.set('top', divider.top);
            divider.setCoords();
            divider.updateAllCoord(null, [this]);
          });

          // End the Y update cycle
          _anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.endUpdateCycle('y');
        }
      }
    } finally {
      // Always mark that the update is complete
      this.isUpdating = false;
    }
  }

}









/***/ }),

/***/ "./js/objects/build.js":
/*!*****************************!*\
  !*** ./js/objects/build.js ***!
  \*****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ObjectBuilderFactory: () => (/* binding */ ObjectBuilderFactory),
/* harmony export */   buildObjectsFromJSON: () => (/* binding */ buildObjectsFromJSON)
/* harmony export */ });
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _draw_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./draw.js */ "./js/objects/draw.js");
/* harmony import */ var _symbols_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./symbols.js */ "./js/objects/symbols.js");
/* harmony import */ var _text_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./text.js */ "./js/objects/text.js");
/* harmony import */ var _mainRoute_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./mainRoute.js */ "./js/objects/mainRoute.js");
/* harmony import */ var _sideRoute_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./sideRoute.js */ "./js/objects/sideRoute.js");
/* harmony import */ var _border_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./border.js */ "./js/objects/border.js");
/* harmony import */ var _divider_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./divider.js */ "./js/objects/divider.js");
/* harmony import */ var _anchor_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./anchor.js */ "./js/objects/anchor.js");
/**
 * @file build.js
 * Contains functions to reconstruct Fabric.js objects from serialized JSON.
 */

// Assume BaseGroup, LockIcon, and other necessary classes (e.g., for basePolygon types)
// are globally available (e.g., window.BaseGroup, window.LockIcon) or will be
// when this script is executed.
// Also assumes 'fabric' is global.
// Assumes 'canvas' and 'canvasObject' (e.g. window.canvas, window.canvasObject) are the global
// Fabric canvas instance and the array storing canvas objects, respectively.







 // Assuming this is defined in this file or imported correctly

 // For registering anchor relationships and creating anchors

const ObjectBuilderFactory = {
    creators: {},

    register(objectType, creatorFunction) {
        this.creators[objectType] = creatorFunction;
    },

    /**
     * Creates an instance of a registered object type.
     * @param {string} objectType - The type of object to create (e.g., "BaseGroup").
     * @param {object} data - The serialized data for the object.
     * @param {fabric.Object | null} reconstructedBasePolygon - The reconstructed basePolygon if any.
     * @param {object} constructorOptions - Options to pass to the constructor.
     * @returns {fabric.Object | null} The created Fabric object or null.
     */
    create(objectType, data, reconstructedBasePolygon, constructorOptions) {
        if (this.creators[objectType]) {
            return this.creators[objectType](data, reconstructedBasePolygon, constructorOptions);
        }
        console.error(`No creator registered for objectType: ${objectType}`);
        return null;
    }
};

const ObjectType = {
    'BaseGroup': _draw_js__WEBPACK_IMPORTED_MODULE_1__.BaseGroup,
    'SymbolObject': _symbols_js__WEBPACK_IMPORTED_MODULE_2__.SymbolObject,
    'TextObject': _text_js__WEBPACK_IMPORTED_MODULE_3__.TextObject,
    'MainRoadSymbol': _mainRoute_js__WEBPACK_IMPORTED_MODULE_4__.MainRoadSymbol,
    'SideRoadSymbol': _sideRoute_js__WEBPACK_IMPORTED_MODULE_5__.SideRoadSymbol,
    'BorderGroup': _border_js__WEBPACK_IMPORTED_MODULE_6__.BorderGroup,
    'DividerObject': _divider_js__WEBPACK_IMPORTED_MODULE_7__.DividerObject,
    // Add other object types as needed
}

// Register a creator for BaseGroup
// This assumes BaseGroup constructor: new BaseGroup(basePolygon, functionalType, options)
// and that it handles adding itself to the global canvas and canvasObject array.
for (const [key, value] of Object.entries(ObjectType)) {
    ObjectBuilderFactory.register(key, (data, reconstructedBasePolygon, constructorOptions) => {
        // Create a new instance of the object type
        const objectInstance = new value(constructorOptions);
        return objectInstance;
    });
}


/**
 * Reconstructs a single Fabric object from its serialized data.
 * This function handles the creation and initial property setting.
 * Linking to other objects is done in a subsequent pass.
 *
 * @param {object} data - The parsed JSON data of the serialized object.
 * @param {fabric.Canvas} fabricCanvas - The Fabric canvas instance.
 * @param {Object.<string, fabric.Object>} allDeserializedObjectsMap - Map to store/retrieve objects by their original canvasID.
 * @returns {Promise<fabric.Object|null>} A promise that resolves to the reconstructed Fabric object or null.
 */
async function reconstructSingleObjectInternal(data, fabricCanvas, allDeserializedObjectsMap) {
    if (!data || !data.objectType) {
        console.error("Invalid data or missing objectType for reconstruction", data);
        return null;
    }

    let newFabricObject;
    let reconstructedBasePolygon = null;


    // 2. Prepare constructor options
    const constructorOptions = { ...data };
    constructorOptions.isLoading = true;
    // Ensure functionalType is preserved in options
    if (data.functionalType) {
        constructorOptions.functionalType = data.functionalType;
    }
    delete constructorOptions.basePolygon; // Handled separately
    delete constructorOptions.basePolygonVertex; // Handled separately
    delete constructorOptions.objectType; // Used by factory


    // originalCanvasID is not an option for constructor, but useful for map
    const originalCanvasID = data.canvasID;
    delete constructorOptions.canvasID; // Let constructor assign new one, or handle if restoring

    // 3. Instantiate the object using the factory
    newFabricObject = ObjectBuilderFactory.create(data.objectType, data, reconstructedBasePolygon, constructorOptions);


    if (!newFabricObject) {
        return null;
    }

    // 4. Apply top-level Fabric.js properties that might not be covered by options or toObject/constructor
    //const fabricPropsToSet = [
    //    'left', 'top', 'width', 'height', 'angle', 'scaleX', 'scaleY',
    //    'flipX', 'flipY', 'skewX', 'skewY', 'visible', 'opacity',
    //    'originX', 'originY', 'borderColor', 'cornerColor', 'cornerSize',
    //    'transparentCorners', 'stroke', 'strokeWidth', 'fill',
    //    // BaseGroup specific properties from serialization if not handled by constructor options:
    //    'isTemporary', 'focusMode'
    //];
    //
    //fabricPropsToSet.forEach(prop => {
    //    if (typeof data[prop] !== 'undefined') {
    //        newFabricObject.set(prop, data[prop]);
    //    }
    //});

    // Ensure basePolygon is correctly set if the constructor didn't fully handle it with options
    // or if it needs re-setting after other properties.
    // The BaseGroup's setBasePolygon also calls drawVertex.
    if (reconstructedBasePolygon && newFabricObject.setBasePolygon) {
        // setBasePolygon(polygon, calcVertex)
        // Assuming vertices are already in reconstructedBasePolygon from data.basePolygonVertex, so calcVertex = false
        newFabricObject.setBasePolygon(reconstructedBasePolygon, false);
    }


    // Store in the map using its *original* canvasID for the linking pass.
    // The newFabricObject itself will have a *new* canvasID assigned by BaseGroup's constructor.
    if (typeof originalCanvasID !== 'undefined') {
        allDeserializedObjectsMap[originalCanvasID] = newFabricObject;
    }

    // newFabricObject.setCoords(); // Called by setBasePolygon, or should be called after all props set.

    // If this is a BorderGroup and we have cached coords / rounding, restore them after construction
    if (newFabricObject instanceof _border_js__WEBPACK_IMPORTED_MODULE_6__.BorderGroup) {
        if (data.fixedWidthCoords) newFabricObject.fixedWidthCoords = { ...data.fixedWidthCoords };
        if (data.fixedHeightCoords) newFabricObject.fixedHeightCoords = { ...data.fixedHeightCoords };
        if (data.rounding) newFabricObject.rounding = { ...data.rounding }; // Use stored rounding to avoid drift
    }

    return newFabricObject;
}

/**
 * Main function to reconstruct a scene or a set of objects from JSON strings.
 * Assumes `canvas` and `canvasObject` are global (e.g., window.canvas, window.canvasObject).
 * This version interleaves creation and linking.
 * IMPORTANT: Assumes `jsonStringsArray` is ordered such that dependencies appear before
 * the objects that depend on them.
 *
 * @param {Array<object|string>} jsonStringsArray - Array of serialized objects.
 *   New format: array of plain objects (already deserialized).
 *   Legacy format: array of JSON strings (each an object), possibly double-stringified.
 *   MUST BE IN DEPENDENCY ORDER.
 * @returns {Promise<Array<fabric.Object>>} A promise resolving to an array of the top-level reconstructed Fabric objects.
 */
async function buildObjectsFromJSON(jsonStringsArray) {
    const fabricCanvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas;
    // It's assumed that BaseGroup's constructor (and similar for other types)
    // adds the object to CanvasGlobals.canvasObject and the fabricCanvas.

    // Normalize items (simple version):
    // - If already an object, use it.
    // - If a string, parse once; if result is a string, parse again.
    const allDeserializedData = [];
    for (const entry of jsonStringsArray) {
        if (!entry) continue;
        if (typeof entry === 'object') {
            allDeserializedData.push(entry);
            continue;
        }
        if (typeof entry === 'string') {
            try {
                let parsed = JSON.parse(entry);
                if (parsed && typeof parsed === 'object') {
                    allDeserializedData.push(parsed);
                }
            } catch (_) { /* ignore invalid entries */ }
        }
    }
    const allDeserializedObjectsMap = {}; // Maps originalID -> new FabricObject
    const finalReconstructedObjects = []; // Stores the fabric objects in the order they are fully processed

    const propertiesToRemapById = ['borderGroup', 'mainRoad', 'textObject', 'underline'];
    const arrayPropertiesToRemapItemsById = ['anchoredPolygon', 'sideRoad', 'widthObjects', 'heightObjects',  'leftObjects', 'aboveObjects', 'rightObjects', 'belowObjects', 'VDivider', 'HDivider'];

    // First pass: Create all objects and store them in the map
    for (const data of allDeserializedData) {
        const originalID = data.canvasID;
        // Create the object using the existing internal function.
        // reconstructSingleObjectInternal will add the created object to allDeserializedObjectsMap.
        // We pass a clean copy of data for object creation, separate from linking data.
        const creationData = { ...data };


        // Link direct dependencies that are needed for constructor or initial setup
        // Remap single ID references to objects
        propertiesToRemapById.forEach(propName => {
            if (creationData[propName] !== undefined && allDeserializedObjectsMap[creationData[propName]]) {
                creationData[propName] = allDeserializedObjectsMap[creationData[propName]];
            }
        });

        // Remap arrays of IDs to arrays of objects
        arrayPropertiesToRemapItemsById.forEach(propName => {
            if (Array.isArray(creationData[propName])) {
                creationData[propName] = creationData[propName]
                    .map(id => allDeserializedObjectsMap[id])
                    .filter(Boolean); // Filter out any undefined if an ID wasn't found
            }
        });

        const fabricObject = await reconstructSingleObjectInternal(creationData, fabricCanvas, allDeserializedObjectsMap);

        if (!fabricObject) {
            console.warn(`Failed to reconstruct object for data (originalID: ${originalID}):`, data);
            continue; // Skip if object creation failed
        }
        finalReconstructedObjects.push(fabricObject);
    }

    // Second pass: Link anchors using anchorShape
    for (const data of allDeserializedData) {
        const fabricObject = allDeserializedObjectsMap[data.canvasID];
        if (!fabricObject) continue;

        if (data.LockXInfo) {
            const targetObjectX = allDeserializedObjectsMap[data.LockXInfo.TargetObjectID];
            if (targetObjectX) {
                const options = {
                    vertexIndex1: data.LockXInfo.sourcePoint,
                    vertexIndex2: data.LockXInfo.targetPoint,
                    spacingX: data.LockXInfo.spacingX,
                    spacingY: '' // Or undefined, depending on anchorShape's expectation
                };
                try{
                    await (0,_anchor_js__WEBPACK_IMPORTED_MODULE_8__.anchorShape)(targetObjectX, fabricObject, options);
                    fabricObject.updateAllCoord(); // Update coordinates after linking
                } catch (error) {
                    console.error(`Error linking X-lock for ${data.canvasID} (TargetID: ${data.LockXInfo.TargetObjectID}):`, error);
                }
            } else {
                console.warn(`TargetObject for X-lock not found for ${data.canvasID} (TargetID: ${data.LockXInfo.TargetObjectID})`);
            }
        }

        if (data.LockYInfo) {
            const targetObjectY = allDeserializedObjectsMap[data.LockYInfo.TargetObjectID];
            if (targetObjectY) {
                const options = {
                    vertexIndex1: data.LockYInfo.sourcePoint,
                    vertexIndex2: data.LockYInfo.targetPoint,
                    spacingX: '', // Or undefined
                    spacingY: data.LockYInfo.spacingY
                };
                try {
                    await (0,_anchor_js__WEBPACK_IMPORTED_MODULE_8__.anchorShape)(targetObjectY, fabricObject, options);
                    fabricObject.updateAllCoord(); // Update coordinates after linking
                } catch (error) {
                    console.error(`Error linking Y-lock for ${data.canvasID} (TargetID: ${data.LockYInfo.TargetObjectID}):`, error);
                }
            } else {
                console.warn(`TargetObject for Y-lock not found for ${data.canvasID} (TargetID: ${data.LockYInfo.TargetObjectID})`);
            }
        }

        // Call drawAnchorLinkage if it exists, after anchors are set.
        // anchorShape should handle the logic for globalAnchorTree.addNode
        if (typeof fabricObject.drawAnchorLinkage === 'function') {
            // fabricObject.drawAnchorLinkage(); // Potentially redundant if anchorShape updates visuals
        }
        fabricObject.setCoords(); // Ensure coordinates are updated after all linking and property setting.
    }

    fabricCanvas.renderAll();
    return finalReconstructedObjects; // Returns array of newly constructed fabric objects
}

// To use:
// 1. Ensure BaseGroup, LockIcon, etc., are defined globally or imported.
// 2. Prepare an array of JSON strings: `const DUMMY_JSON_ARRAY = [jsonString1, jsonString2];`
// 3. Call:
//    buildObjectsFromJSON(DUMMY_JSON_ARRAY)
//      .then(objects => console.log("Reconstructed:", objects))
//      .catch(err => console.error("Reconstruction error:", err));
//
// Before calling, if you want a clean slate:
// window.canvas.clear();
// window.canvasObject.length = 0;
// // Potentially reset other global states related to canvas objects if necessary.




/***/ }),

/***/ "./js/objects/dimension.js":
/*!*********************************!*\
  !*** ./js/objects/dimension.js ***!
  \*********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   BorderDimensionDisplay: () => (/* binding */ BorderDimensionDisplay),
/* harmony export */   RadiusDimensionDisplay: () => (/* binding */ RadiusDimensionDisplay)
/* harmony export */ });
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../sidebar/sbGeneral.js */ "./js/sidebar/sbGeneral.js");


const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas;

// Class to handle engineering style dimension displays for border objects
class BorderDimensionDisplay {
  constructor(options = {}) {
    this.direction = options.direction || 'horizontal'; // 'horizontal' or 'vertical'
    this.startX = options.startX;
    this.startY = options.startY;
    this.endX = options.endX !== undefined ? options.endX : this.startX;
    this.endY = options.endY !== undefined ? options.endY : this.startY;
    this.color = options.color || 'blue';
    this.offset = options.offset || 30;
    this.objects = [];
    this.baseObject = options.baseObject || null;
    this.createDimension();
  }

  createDimension() {
    // Scale adjustments based on zoom
    const zoom = canvas.getZoom();
    const lineWidth = 1 / zoom;
    const fontSize = 12 / zoom;
    const arrowSize = 8 / zoom;
    const extensionLength = 8 / zoom;

    // Create dimension lines based on direction
    if (this.direction === 'horizontal') {
      this.createHorizontalDimension(lineWidth, fontSize, arrowSize, extensionLength);
    } else {
      this.createVerticalDimension(lineWidth, fontSize, arrowSize, extensionLength);
    }

    // Add all objects to the canvas
    canvas.add(...this.objects);
  }

  createHorizontalDimension(lineWidth, fontSize, arrowSize, extensionLength) {
    // Position of the dimension line
    const dimLineY = this.startY - this.offset;
    const distance = Math.abs(this.endX - this.startX);

    // Extension lines
    this.objects.push(new fabric.Line(
      [this.startX, this.startY, this.startX, dimLineY - extensionLength],
      {
        stroke: this.color,
        strokeWidth: lineWidth,
        selectable: false,
        evented: false
      }
    ));

    this.objects.push(new fabric.Line(
      [this.endX, this.startY, this.endX, dimLineY - extensionLength],
      {
        stroke: this.color,
        strokeWidth: lineWidth,
        selectable: false,
        evented: false
      }
    ));

    // Main dimension line
    this.objects.push(new fabric.Line(
      [this.startX, dimLineY, this.endX, dimLineY],
      {
        stroke: this.color,
        strokeWidth: lineWidth,
        selectable: false,
        evented: false
      }
    ));

    // Add arrows
    this.addArrow(this.startX, dimLineY, this.endX > this.startX ? 'right' : 'left', arrowSize);
    this.addArrow(this.endX, dimLineY, this.endX > this.startX ? 'left' : 'right', arrowSize);

    // Add dimension text
    const midX = (this.startX + this.endX) / 2;
    this.objects.push(new fabric.Text(
      _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralSettings.formatDimension(distance, this.baseObject ? this.baseObject.xHeight : 100), // Use 100 as default xHeight for border dimensions
      {
        left: midX,
        top: dimLineY - (8 / canvas.getZoom()),
        fontSize: fontSize,
        fill: this.color,
        fontFamily: 'Arial',
        textAlign: 'center',
        originX: 'center',
        originY: 'bottom',
        selectable: false,
        evented: false,
        stroke: '#fff',
        strokeWidth: 3 / canvas.getZoom(),
        paintFirst: 'stroke'
      }
    ));
  }

  createVerticalDimension(lineWidth, fontSize, arrowSize, extensionLength) {
    // Position of the dimension line
    const dimLineX = this.startX - this.offset;
    const distance = Math.abs(this.endY - this.startY);

    // Extension lines
    this.objects.push(new fabric.Line(
      [this.startX, this.startY, dimLineX - extensionLength, this.startY],
      {
        stroke: this.color,
        strokeWidth: lineWidth,
        selectable: false,
        evented: false
      }
    ));

    this.objects.push(new fabric.Line(
      [this.startX, this.endY, dimLineX - extensionLength, this.endY],
      {
        stroke: this.color,
        strokeWidth: lineWidth,
        selectable: false,
        evented: false
      }
    ));

    // Main dimension line
    this.objects.push(new fabric.Line(
      [dimLineX, this.startY, dimLineX, this.endY],
      {
        stroke: this.color,
        strokeWidth: lineWidth,
        selectable: false,
        evented: false
      }
    ));

    // Add arrows
    this.addArrow(dimLineX, this.startY, this.endY > this.startY ? 'down' : 'up', arrowSize);
    this.addArrow(dimLineX, this.endY, this.endY > this.startY ? 'up' : 'down', arrowSize);

    // Add dimension text
    const midY = (this.startY + this.endY) / 2;

    // Create text with rotation for vertical dimension
    this.objects.push(new fabric.Text(
      _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralSettings.formatDimension(distance, this.baseObject ? this.baseObject.xHeight : 100), // Use 100 as default xHeight for border dimensions
      {
        left: dimLineX - (15 / canvas.getZoom()),
        top: midY,
        fontSize: fontSize,
        fill: this.color,
        fontFamily: 'Arial',
        angle: -90, // Rotate for vertical text
        originX: 'center',
        originY: 'center',
        selectable: false,
        evented: false,
        stroke: '#fff',
        strokeWidth: 3 / canvas.getZoom(),
        paintFirst: 'stroke'
      }
    ));
  }

  addArrow(x, y, direction, size) {
    let points;

    switch (direction) {
      case 'right':
        points = [
          { x: x, y: y },
          { x: x + size, y: y - size / 4 },
          { x: x + size, y: y + size / 4 }
        ];
        break;
      case 'left':
        points = [
          { x: x, y: y },
          { x: x - size, y: y - size / 4 },
          { x: x - size, y: y + size / 4 }
        ];
        break;
      case 'up':
        points = [
          { x: x, y: y },
          { x: x - size / 4, y: y - size },
          { x: x + size / 4, y: y - size }
        ];
        break;
      case 'down':
        points = [
          { x: x, y: y },
          { x: x - size / 4, y: y + size },
          { x: x + size / 4, y: y + size }
        ];
        break;
    }

    const arrow = new fabric.Polygon(points, {
      fill: this.color,
      stroke: this.color,
      strokeWidth: 0,
      selectable: false,
      evented: false
    });

    this.objects.push(arrow);
  }
}

// Class to handle radius dimension displays for corner radius
class RadiusDimensionDisplay {
  constructor(options = {}) {
    this.centerX = options.centerX;
    this.centerY = options.centerY;
    this.radius = options.radius;
    this.color = options.color || 'purple';
    this.startAngle = options.startAngle || 0; // Angle in degrees where to start the dimension
    this.objects = [];

    this.createRadiusDimension();
  }

  createRadiusDimension() {
    // Scale adjustments based on zoom
    const zoom = canvas.getZoom();
    const lineWidth = 1 / zoom;
    const fontSize = 12 / zoom;
    const arrowSize = 6 / zoom;

    // Convert start angle to radians
    const startAngleRad = (this.startAngle * Math.PI) / 180;
    
    // Calculate radius line end point
    const radiusEndX = this.centerX + this.radius * Math.cos(startAngleRad);
    const radiusEndY = this.centerY + this.radius * Math.sin(startAngleRad);

    // Create radius line from center to edge
    this.objects.push(new fabric.Line(
      [this.centerX, this.centerY, radiusEndX, radiusEndY],
      {
        stroke: this.color,
        strokeWidth: lineWidth,
        selectable: false,
        evented: false
      }
    ));

    // Add center point marker
    this.objects.push(new fabric.Circle({
      left: this.centerX,
      top: this.centerY,
      radius: 2 / zoom,
      fill: this.color,
      originX: 'center',
      originY: 'center',
      selectable: false,
      evented: false
    }));

    // Add arrow at the radius end
    this.addRadiusArrow(radiusEndX, radiusEndY, startAngleRad, arrowSize);

    // Add radius dimension text
    const textX = this.centerX - (this.radius * 0.2) * Math.cos(startAngleRad);
    const textY = this.centerY - (this.radius * 0.2) * Math.sin(startAngleRad);
    
    this.objects.push(new fabric.Text(
      `R${_sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralSettings.formatDimension(this.radius, 100).replace('mm', '').replace('sw', '')}${_sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_1__.GeneralSettings.dimensionUnit}`,
      {
        left: textX,
        top: textY,
        fontSize: fontSize,
        fill: this.color,
        fontFamily: 'Arial',
        textAlign: 'center',
        originX: 'center',
        originY: 'center',
        selectable: false,
        evented: false,
        stroke: '#fff',
        strokeWidth: 3 / canvas.getZoom(),
        paintFirst: 'stroke'
      }
    ));


    // Add all objects to the canvas
    canvas.add(...this.objects);
  }

  addRadiusArrow(x, y, angle, size) {
    // Calculate arrow direction (pointing away from center)
    const arrowAngle1 = angle + Math.PI + Math.PI / 6;
    const arrowAngle2 = angle + Math.PI - Math.PI / 6;

    const points = [
      { x: x, y: y },
      { x: x + size * Math.cos(arrowAngle1), y: y + size * Math.sin(arrowAngle1) },
      { x: x + size * Math.cos(arrowAngle2), y: y + size * Math.sin(arrowAngle2) }
    ];

    const arrow = new fabric.Polygon(points, {
      fill: this.color,
      stroke: this.color,
      strokeWidth: 0,
      selectable: false,
      evented: false
    });

    this.objects.push(arrow);
  }


  // Remove radius dimension from canvas
  remove() {
    this.objects.forEach(obj => canvas.remove(obj));
    this.objects = [];
  }
}



/***/ }),

/***/ "./js/objects/divider.js":
/*!*******************************!*\
  !*** ./js/objects/divider.js ***!
  \*******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   DividerObject: () => (/* binding */ DividerObject),
/* harmony export */   drawDivider: () => (/* binding */ drawDivider)
/* harmony export */ });
/* harmony import */ var _draw_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./draw.js */ "./js/objects/draw.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _template_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./template.js */ "./js/objects/template.js");
/* harmony import */ var _border_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./border.js */ "./js/objects/border.js");
/* harmony import */ var _anchor_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./anchor.js */ "./js/objects/anchor.js");
/* harmony import */ var _canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../canvas/promptBox.js */ "./js/canvas/promptBox.js");







const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas; // Access the global canvas object

function drawDivider(xHeight, color, position, size, type) {

    // Choose the template based on the horizontal parameter
    let dividerTemplate = _template_js__WEBPACK_IMPORTED_MODULE_2__.DividerScheme[type](xHeight, position, size, { x: 0, y: 0 }).path;

    dividerTemplate.map(path => {
        path.fill = color;
    });

    const arrowOptions1 = {
        left: 0,
        top: 0,
        fill: color,
        angle: 0,
        objectCaching: false,
        strokeWidth: 0
    };

    const dividerShape = new _draw_js__WEBPACK_IMPORTED_MODULE_0__.GlyphPath();
    dividerShape.initialize({ path: dividerTemplate }, arrowOptions1);
    return dividerShape;
}

class DividerObject extends _draw_js__WEBPACK_IMPORTED_MODULE_0__.BaseGroup {
    constructor(options = {}) {
        super(null, options.dividerType, 'DividerObject', options); // Call BaseGroup constructor

        // Store divider-specific properties
        this.xHeight = options.xHeight;
        this.colorType = options.colorType;
        this.color = options.color || _template_js__WEBPACK_IMPORTED_MODULE_2__.BorderColorScheme[this.colorType]['border'];
        this.dividerType = options.dividerType; // 'VDivider', 'HDivider', 'HLine', 'VLane'
        this.borderGroup = options.borderGroup || null;

        // HLine divider adhere to text
        this.textObject = options.textObject || null;

        this.leftValue = options.left;
        this.aboveValue = options.top;

        // Legacy properties for compatibility with existing create functions
        this.leftObjects = options.leftObjects || [];
        this.rightObjects = options.rightObjects || [];
        this.aboveObjects = options.aboveObjects || [];
        this.belowObjects = options.belowObjects || [];
        this.rightValue = options.rightValue;
        this.belowValue = options.belowValue;

        // Properties to be set by the respective create functions
        // These will now be set directly in initialize
        this.fixedLeftValue = undefined;
        this.fixedRightValue = undefined;
        this.fixedTopValue = undefined;
        this.fixedBottomValue = undefined;


        this.initialize();
    }

    initialize() {
        if (this.dividerType !== 'HLine') {
            // placeholder meta, resize later in border assignWidthToDivider; if compartmentBox supplied, seed with its center
            let objectBBox = { left: this.leftValue, top: this.aboveValue,  };
            let objectSize = { width: 0, height: 0 };
            const basePoly = drawDivider(this.xHeight, this.color, objectBBox, objectSize, this.dividerType);
            this.setBasePolygon(basePoly, false);
            switch (this.dividerType) {
                case 'HDivider': {
                    this.borderGroup.HDivider.push(this);
                    break;
                }
                case 'VDivider':
                case 'VLane': {
                    this.borderGroup.VDivider.push(this);
                    break;
                }
                default:
                    console.error('Unknown divider type:', this.dividerType);
                    return this;
            }
            this.borderGroup.assignWidthToDivider();
        } else {
            let objectBBox = { left: this.textObject.left, top: 0, right: 0, bottom: 0 };
            let objectSize = { width: this.textObject.width, height: this.xHeight / 4 };
            const basePoly = drawDivider(this.xHeight, this.color, objectBBox, objectSize, this.dividerType);
            this.setBasePolygon(basePoly, false);
        }

        this.setCoords();
        this.updateAllCoord();
        // } // tempGroup is no longer used
        return this;
    }

    SS_initialize() { // Save the original initialize method
        let objectBBox;
        let objectSize;
        let primaryAnchorObject, secondaryAnchorObject;
        let hasFixedPrimary = false, hasFixedSecondary = false;

        switch (this.dividerType) {
            case 'HDivider':
            case 'HLine': {
                let aboveObject = null;
                let belowObject = null;

                // Determine aboveObject
                if (this.dividerType === 'HDivider' && !isNaN(parseInt(this.aboveValue))) {
                    hasFixedPrimary = true;
                    const fixedDistanceFromTop = parseInt(this.aboveValue);
                    const borderCoords = canvas.calcViewportBoundaries();
                    const centerX = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.CenterCoord().x;
                    aboveObject = { top: (borderCoords.tl.y + borderCoords.br.y) / 2, left: centerX, getBoundingRect: () => false };
                    this.fixedTopValue = fixedDistanceFromTop;
                } else if (Array.isArray(this.aboveObjects) && this.aboveObjects.length) {
                    aboveObject = _border_js__WEBPACK_IMPORTED_MODULE_3__.BorderUtilities.getBottomMostObject(this.aboveObjects);
                }
                primaryAnchorObject = aboveObject;

                // Determine belowObject
                if (this.dividerType === 'HDivider' && !isNaN(parseInt(this.belowValue))) {
                    hasFixedSecondary = true;
                    const fixedDistanceFromBottom = parseInt(this.belowValue);
                    const borderCoords = canvas.calcViewportBoundaries();
                    const centerX = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.CenterCoord().x;
                    belowObject = { top: (borderCoords.tl.y + borderCoords.br.y) / 2, left: centerX, getBoundingRect: () => false };
                    this.fixedBottomValue = fixedDistanceFromBottom;
                } else if (Array.isArray(this.belowObjects) && this.belowObjects.length) {
                    belowObject = _border_js__WEBPACK_IMPORTED_MODULE_3__.BorderUtilities.getTopMostObject(this.belowObjects);
                }
                secondaryAnchorObject = belowObject;

                if (!primaryAnchorObject || !secondaryAnchorObject) {
                    (0,_canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_5__.showTextBox)('Please provide valid objects or values for both above and below positions for HDivider/HLine.', '');
                    return this;
                }
                if (!hasFixedSecondary && secondaryAnchorObject.getBoundingRect && secondaryAnchorObject.getBoundingRect() && secondaryAnchorObject.lockYToPolygon && Object.keys(secondaryAnchorObject.lockYToPolygon).length !== 0) {
                    (0,_canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_5__.showTextBox)('Unlock the object below divider in Y axis', '');
                    return this;
                }

                if (this.dividerType === 'HLine') {
                    objectBBox = _border_js__WEBPACK_IMPORTED_MODULE_3__.BorderUtilities.getBoundingBox(this.aboveObjects);
                } else { // HDivider
                    if (primaryAnchorObject.getBoundingRect && primaryAnchorObject.getBoundingRect()) {
                        objectBBox = _border_js__WEBPACK_IMPORTED_MODULE_3__.BorderUtilities.getBoundingBox([primaryAnchorObject]);
                    } else {
                        objectBBox = { left: primaryAnchorObject.left - 50, top: primaryAnchorObject.top - 10, right: primaryAnchorObject.left + 50, bottom: primaryAnchorObject.top + 10 };
                    }
                }
                objectSize = { width: objectBBox.right - objectBBox.left, height: objectBBox.bottom - objectBBox.top };

                const basePoly = drawDivider(this.xHeight, this.color, objectBBox, objectSize, this.dividerType);
                this.setBasePolygon(basePoly);

                this.shadowWidth = (this.dividerType === 'HLine') ? { x: 0, y: 0 } : { x: 0, y: 1.5 };
                this.position = objectBBox;
                this.bbox = objectSize;

                const marginTypeH = this.dividerType; // 'HDivider' or 'HLine'
                if (this.dividerType === 'HDivider') {
                    if (!hasFixedPrimary && primaryAnchorObject.getBoundingRect && primaryAnchorObject.getBoundingRect()) {
                        (0,_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(primaryAnchorObject, this, { vertexIndex1: 'E2', vertexIndex2: 'E6', spacingX: '', spacingY: _template_js__WEBPACK_IMPORTED_MODULE_2__.DividerMargin[marginTypeH]['top'] * this.xHeight / 4 });
                    } else if (hasFixedPrimary) {
                        this.set({ top: primaryAnchorObject.top + _template_js__WEBPACK_IMPORTED_MODULE_2__.DividerMargin[marginTypeH]['top'] * this.xHeight / 4 });
                    }
                    if (!hasFixedSecondary && secondaryAnchorObject.getBoundingRect && secondaryAnchorObject.getBoundingRect()) {
                        (0,_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(this, secondaryAnchorObject, { vertexIndex1: 'E2', vertexIndex2: 'E6', spacingX: '', spacingY: _template_js__WEBPACK_IMPORTED_MODULE_2__.DividerMargin[marginTypeH]['bottom'] * this.xHeight / 4 });
                    }
                } else { // HLine
                    if (primaryAnchorObject.getBoundingRect && primaryAnchorObject.getBoundingRect()) {
                        (0,_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(primaryAnchorObject, this, { vertexIndex1: 'E2', vertexIndex2: 'E6', spacingX: '', spacingY: _template_js__WEBPACK_IMPORTED_MODULE_2__.DividerMargin[marginTypeH]['top'] * this.xHeight / 4 });
                    }
                    if (secondaryAnchorObject.getBoundingRect && secondaryAnchorObject.getBoundingRect()) {
                        (0,_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(this, secondaryAnchorObject, { vertexIndex1: 'E2', vertexIndex2: 'E6', spacingX: '', spacingY: _template_js__WEBPACK_IMPORTED_MODULE_2__.DividerMargin[marginTypeH]['bottom'] * this.xHeight / 4 });
                    }
                }
                break;
            }
            case 'VDivider':
            case 'VLane': {
                let leftObject = null;
                let rightObject = null;

                if (!isNaN(parseInt(this.leftValue))) {
                    hasFixedPrimary = true;
                    const fixedDistanceFromLeft = parseInt(this.leftValue);
                    const borderCoords = canvas.calcViewportBoundaries();
                    const centerY = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.CenterCoord().y;
                    leftObject = { left: (borderCoords.tl.x + borderCoords.br.x) / 2, top: centerY, getBoundingRect: () => false };
                    this.fixedLeftValue = fixedDistanceFromLeft;
                } else if (Array.isArray(this.leftObjects) && this.leftObjects.length) {
                    leftObject = _border_js__WEBPACK_IMPORTED_MODULE_3__.BorderUtilities.getExtremeObject(this.leftObjects, 'left');
                }
                primaryAnchorObject = leftObject;

                if (!isNaN(parseInt(this.rightValue))) {
                    hasFixedSecondary = true;
                    const fixedDistanceFromRight = parseInt(this.rightValue);
                    const borderCoords = canvas.calcViewportBoundaries();
                    const centerY = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.CenterCoord().y;
                    rightObject = { left: (borderCoords.tl.x + borderCoords.br.x) / 2, top: centerY, getBoundingRect: () => false };
                    this.fixedRightValue = fixedDistanceFromRight;
                } else if (Array.isArray(this.rightObjects) && this.rightObjects.length) {
                    rightObject = _border_js__WEBPACK_IMPORTED_MODULE_3__.BorderUtilities.getExtremeObject(this.rightObjects, 'right');
                }
                secondaryAnchorObject = rightObject;

                if (!primaryAnchorObject || !secondaryAnchorObject) {
                    (0,_canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_5__.showTextBox)('Please provide valid objects or values for both left and right positions for VDivider/VLane.', '');
                    return this;
                }
                if (!hasFixedPrimary && primaryAnchorObject.getBoundingRect && primaryAnchorObject.getBoundingRect() && primaryAnchorObject.lockXToPolygon && Object.keys(primaryAnchorObject.lockXToPolygon).length !== 0) {
                    (0,_canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_5__.showTextBox)('Unlock the object left of divider in X axis', '');
                    return this;
                }

                if (primaryAnchorObject.getBoundingRect && primaryAnchorObject.getBoundingRect()) {
                    objectBBox = _border_js__WEBPACK_IMPORTED_MODULE_3__.BorderUtilities.getBoundingBox([primaryAnchorObject]);
                } else {
                    objectBBox = { left: primaryAnchorObject.left - 10, top: primaryAnchorObject.top - 50, right: primaryAnchorObject.left + 10, bottom: primaryAnchorObject.top + 50 };
                }
                objectSize = { width: objectBBox.right - objectBBox.left, height: objectBBox.bottom - objectBBox.top };

                const basePoly = drawDivider(this.xHeight, this.color, objectBBox, objectSize, this.dividerType);
                this.setBasePolygon(basePoly);

                this.shadowWidth = (this.dividerType === 'VLane') ? { x: 0, y: 0 } : { x: 1.5, y: 0 };
                this.position = objectBBox;
                this.bbox = objectSize;

                const marginTypeV = this.dividerType; // 'VDivider' or 'VLane'
                if (!hasFixedPrimary && primaryAnchorObject.getBoundingRect && primaryAnchorObject.getBoundingRect()) {
                    (0,_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(primaryAnchorObject, this, { vertexIndex1: 'E1', vertexIndex2: 'E3', spacingX: _template_js__WEBPACK_IMPORTED_MODULE_2__.DividerMargin[marginTypeV]['left'] * this.xHeight / 4, spacingY: '' });
                } else if (hasFixedPrimary) {
                    this.set({ left: primaryAnchorObject.left + _template_js__WEBPACK_IMPORTED_MODULE_2__.DividerMargin[marginTypeV]['left'] * this.xHeight / 4 });
                }

                if (!hasFixedSecondary && secondaryAnchorObject.getBoundingRect && secondaryAnchorObject.getBoundingRect()) {
                    (0,_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(this, secondaryAnchorObject, { vertexIndex1: 'E1', vertexIndex2: 'E3', spacingX: _template_js__WEBPACK_IMPORTED_MODULE_2__.DividerMargin[marginTypeV]['right'] * this.xHeight / 4, spacingY: '' });
                }
                break;
            }
            default:
                console.error('Unknown divider type:', this.dividerType);
                return this;
        }

        // if (tempGroup) { // tempGroup is no longer used
        // Copy properties from the created group to this instance
        // this.setBasePolygon(tempGroup.basePolygon); // Assuming basePolygon is the visual element
        // All visual setup and property setting is now done directly on 'this'

        this.setCoords();
        this.updateAllCoord();
        // } // tempGroup is no longer used
        return this;
    }

    // It might be beneficial to move the logic from VDividerCreate, HDividerCreate, etc.
    // into methods of this class, e.g., createVDividerVisuals(), createHDividerVisuals().
    // For now, the existing functions are leveraged.

    updateAllCoord(event, _sourceList = [], _selfOnly = false) {
        // Call base implementation for normal coordinate / anchor updates
        super.updateAllCoord(event, _sourceList, _selfOnly);

        // After divider movement/update, refresh its border group's compartment + midpoints
        const bg = this.borderGroup;
        if (bg && !bg.isUpdating && typeof bg.updateBboxes === 'function' && typeof bg.addMidPointToDivider === 'function') {
            try {
                bg.updateBboxes();
                bg.addMidPointToDivider();
            } catch (e) {
                console.warn('DividerObject.updateAllCoord borderGroup refresh failed:', e);
            }
        }
    }
}



/***/ }),

/***/ "./js/objects/draw.js":
/*!****************************!*\
  !*** ./js/objects/draw.js ***!
  \****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   BaseGroup: () => (/* binding */ BaseGroup),
/* harmony export */   GlyphPath: () => (/* binding */ GlyphPath)
/* harmony export */ });
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _path_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./path.js */ "./js/objects/path.js");
/* harmony import */ var _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../canvas/Tracker.js */ "./js/canvas/Tracker.js");
/* harmony import */ var _vertex_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./vertex.js */ "./js/objects/vertex.js");
/* harmony import */ var _dimension_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./dimension.js */ "./js/objects/dimension.js");
/* harmony import */ var _lock_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./lock.js */ "./js/objects/lock.js");
/* harmony import */ var _anchor_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./anchor.js */ "./js/objects/anchor.js");
/* harmony import */ var _sidebar_sb_inspector_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../sidebar/sb-inspector.js */ "./js/sidebar/sb-inspector.js");
/* harmony import */ var _sidebar_property_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../sidebar/property.js */ "./js/sidebar/property.js");











const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas; // Assuming canvas is a global variable in canvas.js
const canvasObject = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvasObject; // Assuming canvasObject is a global variable in canvas.js

const deleteIcon =
  "data:image/svg+xml,%3C%3Fxml version='1.0' encoding='utf-8'%3F%3E%3C!DOCTYPE svg PUBLIC '-//W3C//DTD SVG 1.1//EN' 'http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd'%3E%3Csvg version='1.1' id='Ebene_1' xmlns='http://www.w3.org/2000/svg' xmlns:xlink='http://www.w3.org/1999/xlink' x='0px' y='0px' width='595.275px' height='595.275px' viewBox='200 215 230 470' xml:space='preserve'%3E%3Ccircle style='fill:%23F44336;' cx='299.76' cy='439.067' r='218.516'/%3E%3Cg%3E%3Crect x='267.162' y='307.978' transform='matrix(0.7071 -0.7071 0.7071 0.7071 -222.6202 340.6915)' style='fill:white;' width='65.545' height='262.18'/%3E%3Crect x='266.988' y='308.153' transform='matrix(0.7071 0.7071 -0.7071 0.7071 398.3889 -83.3116)' style='fill:white;' width='65.544' height='262.179'/%3E%3C/g%3E%3C/svg%3E";


// additional property for fabric object
//const originalToObject = fabric.Object.prototype.toObject;
//const myAdditional = ['functionalType'];
//fabric.Object.prototype.toObject = function (additionalProperties) {
//  return originalToObject.call(this, myAdditional.concat(additionalProperties));
//}


class GlyphPath extends fabric.Group {
  constructor(options) {
    super([], options); // Call the parent class constructor first

    //this.initialize(shapeMeta, options);
  }

  initialize(shapeMeta, options) {
    shapeMeta.path.map((p) => {
      let transformed = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.calculateTransformedPoints)(p.vertex, {
        x: options.left,
        y: options.top,
        angle: options.angle
      });
      p.vertex = transformed;
    });

    if (shapeMeta.text) {
      shapeMeta.text.map((p) => {
        let transformed = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.calculateTransformedPoints)([{ x: p.x, y: -p.y, label: '' }], {
          x: options.left,
          y: options.top,
          angle: options.angle
        });
        p.x = transformed[0].x;
        p.y = -transformed[0].y;
      });
    }

    const vertexleft = Math.min(...shapeMeta.path.map(p => p.vertex).flat().map(v => v.x));
    const vertextop = Math.min(...shapeMeta.path.map(p => p.vertex).flat().map(v => v.y));

    options.left = vertexleft;
    options.top = vertextop;
    options.angle = 0;
    options.strokeWidth = 0;

    // Store vertex data for reference
    this.vertex = shapeMeta.path.map(p => p.vertex).flat();
    this.insertPoint = shapeMeta.path[0].vertex[0];

    // Create fabric.Path objects directly from vertex data
    shapeMeta.path.forEach(path => {
      const pathCommands = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.convertVertexToPathCommands)(path);
      const pathObj = new fabric.Path(pathCommands, {
        fill: path.fill || options.fill || 'white',
        stroke: options.stroke || 'none',
        strokeWidth: options.strokeWidth || 0,
        objectCaching: options.objectCaching,
        originX: 'left',
        originY: 'top'
      });
      this.add(pathObj);
    });

    // Add text elements if present
    if (shapeMeta.text && shapeMeta.text.length > 0) {
      shapeMeta.text.forEach(textElem => {
        let fontGlyphs;
        switch (textElem.fontFamily) {
          case 'TransportMedium':
            fontGlyphs = _path_js__WEBPACK_IMPORTED_MODULE_1__.parsedFontMedium;
            break;
          case 'TransportHeavy':
            fontGlyphs = _path_js__WEBPACK_IMPORTED_MODULE_1__.parsedFontHeavy;
            break;
          default:
            fontGlyphs = _path_js__WEBPACK_IMPORTED_MODULE_1__.parsedFontKorean;
        }
        // Access font metrics
        const fontMetrics = {
          unitsPerEm: fontGlyphs.unitsPerEm,
          ascender: fontGlyphs.ascender,
          descender: fontGlyphs.descender,
        };

        // Scale metrics to desired font size
        const fontScale = textElem.fontSize / fontMetrics.unitsPerEm;
        const scaledAscender = (fontMetrics.unitsPerEm - fontMetrics.ascender) * fontScale;

        const yOffset = scaledAscender;
        textElem.y = textElem.y - yOffset;
        const charPath = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.getFontPath)(textElem);
        if (charPath && charPath.commands) {
          // Convert font path commands to fabric.Path format
          const pathCommands = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.convertFontPathToFabricPath)(charPath.commands, textElem);
          const textPathObj = new fabric.Path(pathCommands, {
            fill: textElem.fill || options.fill || 'black',
            stroke: 'none',
            strokeWidth: 0,
            objectCaching: options.objectCaching,
            originX: 'left',
            originY: 'top'
          });
          this.add(textPathObj);
        }
      });
    }

    this.setCoords();
  }

}


// Define the BaseGroup class using ES6 class syntax
class BaseGroup extends fabric.Group {
  /**
   * List of property names to include in metadata.
   * @type {string[]}
   */
  _metadataKeys = [];
  _arrayPropertiesToSerializeItemByCanvasID = ['heightObjects', 'widthObjects'];

  constructor(basePolygon, functionalType, className, options = {}) {
    super([], {
      subTargetCheck: true,
      lockScalingX: true,// lock scaling
      lockScalingY: true
    });

    // Initialize metadata keys with default properties
    this._metadataKeys = ['functionalType', 'className', 'canvasID'];
    Object.keys(options).forEach(key => {
      if (!this._metadataKeys.includes(key)) {
        this._metadataKeys.push(key);
      }
    });

    this.functionalType = functionalType;
    this.className = className || 'BaseGroup';
    this.anchoredPolygon = [];
    this.anchorageLink = [];
    this.lockXToPolygon = {};
    this.lockYToPolygon = {};

    this.refTopLeft = { top: 0, left: 0 }; // Initialize even without basePolygon

    this.dimensionAnnotations = []; // Array to hold dimension line objects
    this.hoverSnapEnvelopes = []; // Temporary hover-only vertex envelopes during snapping

    this.isTemporary = false;
    this.focusMode = false; // Add focus mode flag

    canvasObject.push(this);
    this.canvasID = canvasObject.length - 1;
    this._showName = `<Group ${this.canvasID}> ${functionalType}`;

    // Add to canvas regardless of basePolygon
    canvas.add(this);

    // Track object creation
    _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_2__.canvasTracker.track('createObject', [{
      type: 'BaseGroup',
      id: this.canvasID,
      functionalType: functionalType,
      hasBasePolygon: !!basePolygon
    }]);

    // remove default fabric control
    Object.values(this.controls).forEach((control) => {
      control.visible = false;
    })

    // If basePolygon is provided, initialize with it
    if (basePolygon) {
      this.setBasePolygon(basePolygon, options.calcVertex);
    }

    // add delete control
    this.controls.deleteControl = new fabric.Control({
      x: 0.5,
      y: -0.5,
      offsetY: 40,
      cursorStyle: 'pointer',
      mouseUpHandler: this.deleteObject,
      render: this.renderIcon,
      cornerSize: 24,
    });

    _sidebar_sb_inspector_js__WEBPACK_IMPORTED_MODULE_7__.CanvasObjectInspector.createObjectListPanelInit();

    this.on('selected', () => {
      if (canvas.getActiveObjects().length > 2) return;
      this.drawAnchorLinkage();
      _sidebar_sb_inspector_js__WEBPACK_IMPORTED_MODULE_7__.CanvasObjectInspector.SetActiveObjectList(this);
      this.showLockHighlights();

      // Redraw vertices when selected to apply current vertex display settings
      if (this.basePolygon && this.basePolygon.vertex) {
        this.drawVertex(false);
      }

      // Show dimension lines when object is selected

      this.showDimensions();

    });

    this.on('deselected', () => {
      setTimeout(() => {
        this.anchorageLink.forEach(obj => {
          obj.objects.forEach(o => {
            o.set('opacity', 0);
          })
        });
        this.hideLockHighlights();
        this.hideHoverSnapVertexEnvelope();

        // Hide dimension lines when object is deselected
        this.hideDimensions();
      }, 0)
      _sidebar_sb_inspector_js__WEBPACK_IMPORTED_MODULE_7__.CanvasObjectInspector.SetActiveObjectList(null)
    });

    this.on('mouseover', function () {
      this.set({
        opacity: 0.5
      });

      const activeVertex = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex;
      const isVertexSnappingMode = !!(activeVertex && activeVertex.isDragging);
      const isHoveredObjectBeingSnapped = !!(isVertexSnappingMode && activeVertex.baseGroup === this);
      if (isVertexSnappingMode && !isHoveredObjectBeingSnapped) {
        this.showHoverSnapVertexEnvelope();
      }

      if (this.__corner) {
        if (this.controls[this.__corner].onHover) {
          this.controls[this.__corner].onHover()
        } else {
          Object.values(this.controls).forEach(control => { if (control.onMouseOut) { control.onMouseOut() } })
        }
      } else {
        Object.values(this.controls).forEach(control => { if (control.onMouseOut) { control.onMouseOut() } })
      }
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
    });


    this.on('mouseout', function () {
      this.set({
        opacity: 1
      });
      //this.hideHoverSnapVertexEnvelope();
      Object.values(this.controls).forEach(control => { if (control.onMouseOut) { control.onMouseOut() } })
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
    });


    this.on('mousedblclick', (e) => {
      _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_2__.canvasTracker.isDragging = false;
      (0,_sidebar_property_js__WEBPACK_IMPORTED_MODULE_8__.showPropertyPanel)(this); //-> handled globally by object:dblclick event
    });

    this.on('modified', this.updateAllCoord.bind(this));
    this.on('moving', this.updateAllCoord.bind(this));


    this.on('moving', () => {

      this.showDimensions();

    });
    this.on('modified', () => {

      this.showDimensions();

    });
  }


  /**
   * Registers property names to include in metadata.
   * @param {...string} keys
   */
  registerMetadataKeys(...keys) {
    this._metadataKeys.push(...keys);
  }

  /**
   * Returns an object containing the registered metadata.
   */
  getMetadata() {
    const meta = {};
    this._metadataKeys.forEach(key => {
      if (Object.prototype.hasOwnProperty.call(this, key)) {
        meta[key] = this[key];
      }
    });
    return meta;
  }

  /**
   * Serializes the BaseGroup object based on its metadata and returns a plain object.
   * (Exporter will stringify; this method never returns a string.)
   * @returns {Object}
   */
  serializeToJSON() {
    const meta = this.getMetadata();
    // Add any other properties that are essential for reconstruction but might not be in _metadataKeys by default
    // For example, the class type itself, or specific geometric properties if not covered.
    // For now, we rely on what's populated in _metadataKeys and direct properties.

    const dataToSerialize = { ...meta };
    dataToSerialize.objectType = this.className; // Store the class name for reconstruction
    dataToSerialize.left = this.left
    dataToSerialize.top = this.top
    dataToSerialize.refTopLeft = this.refTopLeft


    // Serialize references to other BaseGroup objects by their canvasID
    const propertiesToSerializeById = ['borderGroup', 'mainRoad', 'textObject', 'underline'];
    propertiesToSerializeById.forEach(propName => {
      if (this[propName] && typeof this[propName].canvasID !== 'undefined') {
        dataToSerialize[propName] = this[propName].canvasID;
      } else if (this[propName]) {
        // If it's not a BaseGroup but some other object, serialize as is (or define specific logic)
        dataToSerialize[propName] = JSON.parse(JSON.stringify(this[propName]));
      }
    });

    const arrayPropertiesToSerializeById = ['anchoredPolygon', 'sideRoad', 'widthObjects', 'heightObjects', 'leftObjects', 'aboveObjects', 'rightObjects', 'belowObjects', 'VDivider', 'HDivider'];
    arrayPropertiesToSerializeById.forEach(propName => {
      if (this[propName] && Array.isArray(this[propName])) {
        dataToSerialize[propName] = this[propName].map(item => {
          if (item && typeof item.canvasID !== 'undefined') {
            return item.canvasID;
          }
          // If item is not a BaseGroup or doesn't have canvasID, serialize as is or handle specifically
          return JSON.parse(JSON.stringify(item));
        });
      }
    });

    // Handle lockXToPolygon and lockYToPolygon TargetObject
    if (this.lockXToPolygon && typeof this.lockXToPolygon.TargetObject?.canvasID !== 'undefined') {
      dataToSerialize.LockXInfo = {
        TargetObjectID: this.lockXToPolygon.TargetObject.canvasID,
        sourcePoint: this.lockXToPolygon.sourcePoint, // Assuming AnchorPoint on this object is the source
        targetPoint: this.lockXToPolygon.targetPoint, // Assuming this new property will hold the target's vertex index
        spacingX: this.lockXToPolygon.spacing, // Assuming this property exists or will be added
      };
    }


    if (this.lockYToPolygon && typeof this.lockYToPolygon.TargetObject?.canvasID !== 'undefined') {
      dataToSerialize.LockYInfo = {
        TargetObjectID: this.lockYToPolygon.TargetObject.canvasID,
        sourcePoint: this.lockYToPolygon.sourcePoint, // Assuming AnchorPoint on this object is the source
        targetPoint: this.lockYToPolygon.targetPoint, // Assuming this new property will hold the target's vertex index
        spacingY: this.lockYToPolygon.spacing, // Assuming this property exists or will be added
      };
    }

    // Serialize other direct properties that might have been missed by _metadataKeys but are important
    // Example: this.left, this.top, this.width, this.height, this.angle, etc.
    // FabricJS's toObject() usually handles these, but if you're not using it for the group itself:
    dataToSerialize.left = this.left;
    dataToSerialize.top = this.top;
    dataToSerialize.width = this.width;
    dataToSerialize.height = this.height;
    dataToSerialize.angle = this.angle;
    // Add any other fabric.Object properties you need to preserve


    // You'll need to expand this based on how you plan to reconstruct the objects.
    // Consider if sub-objects (like those in anchoredPolygon) need their own serializeToJSON methods.
    delete dataToSerialize['lockXToPolygon']
    delete dataToSerialize['lockYToPolygon']
    const replacer = (key, value) => {
      // Handle fabric object references safely
      if (value instanceof fabric.Object && value !== this.basePolygon) {
        if (typeof value.canvasID !== 'undefined') return `ref:${value.canvasID}`;
        return `fabricObject:${value.type}`;
      }
      return value;
    };

    // Return a plain object with fabric references replaced so that the exporter can stringify once.
    return JSON.parse(JSON.stringify(dataToSerialize, replacer));
  }

  // Show border dimensions when selected
  showDimensions() {
    // Clean up any existing dimension annotations
    this.hideDimensions();

    // Get border coordinates
    const borderRect = this.getBoundingRect();

    // Find closest objects in each direction to show dimensions
    if (!this.isTemporary) {
      this.createDimensionAnnotations(borderRect);
    }
  }

  // Hide all dimension annotations
  hideDimensions() {
    // Remove all dimension annotations from canvas
    this.dimensionAnnotations.forEach(annotation => {
      canvas.remove(...annotation.objects);
    });
    this.dimensionAnnotations = [];
  }

  // Create dimension annotations for the border and contained objects
  createDimensionAnnotations(borderRect) {


    // Create horizontal dimensions (left and right)
    const leftDimension = new _dimension_js__WEBPACK_IMPORTED_MODULE_4__.BorderDimensionDisplay({
      direction: 'horizontal',
      startX: borderRect.left,
      startY: borderRect.top - 8 / canvas.getZoom(),
      endX: borderRect.left + borderRect.width,
      color: 'green',
      offset: 30 / canvas.getZoom(),
      baseObject: this
    });
    this.dimensionAnnotations.push(leftDimension);



    // Create vertical dimensions (top and bottom)
    const topDimension = new _dimension_js__WEBPACK_IMPORTED_MODULE_4__.BorderDimensionDisplay({
      direction: 'vertical',
      startX: borderRect.left - 12 / canvas.getZoom(),
      startY: borderRect.top,
      endY: borderRect.top + borderRect.height,
      color: 'red',
      offset: 30 / canvas.getZoom(),
      baseObject: this
    });
    this.dimensionAnnotations.push(topDimension);


  }

  /**
   * Sets the basePolygon after construction
   * @param {Object} basePolygon - The polygon to set as base
   * @param {boolean} calcVertex - Whether to calculate vertices
   */
  setBasePolygon(basePolygon, calcVertex = true) {
    this.basePolygon = basePolygon;

    if (this.basePolygon) {
      // Update name with additional info if available
      this._showName = `<Group ${this.canvasID}> ${this.functionalType}${basePolygon.text ? ' - ' + basePolygon.text : ''}${basePolygon.symbol ? ' - ' + basePolygon.symbol : ''}${this.roadType ? ' - ' + this.roadType : ''}${this.borderType ? ' - ' + this.borderType : ''}`;

      this.basePolygon.insertPoint = this.basePolygon.vertex ? this.basePolygon.vertex[0] : null;
      const oldLeft = this.left; // Store old left position since symbols path have negative left values
      //canvas.remove(this.basePolygon);
      this.add(this.basePolygon);
      if (this.functionalType == 'Symbol') {
        this.basePolygon.vertex.forEach((v) => {
          v.x += oldLeft - this.left;
        })
        this.left = oldLeft
      }

      // Basepolygon async not loaded, make a temp dimension
      if (this.width == 0 || this.height == 0) {
        let left = Infinity
        let top = Infinity
        let right = -Infinity
        let bottom = -Infinity
        this.basePolygon.vertex.forEach((v) => {
          right = Math.max(right, v.x)
          bottom = Math.max(bottom, v.y)
          left = Math.min(left, v.x)
          top = Math.min(top, v.y)
        }
        )
        this.tempWidth = left - right
        this.tempHeight = bottom - top
        this.tempLeft = left
        this.tempTop = top
      }

      this.drawVertex(calcVertex);

      // Set reference top-left corner
      if (this.basePolygon.getCoords) {
        this.refTopLeft = {
          top: this.basePolygon.getCoords()[0].y,
          left: this.basePolygon.getCoords()[0].x
        };
      }
    }
  }

  replaceBasePolygon(newBasePolygon, calcVertex = true) {
    this.removeAll();
    this.setBasePolygon(newBasePolygon, calcVertex);
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
  }

  drawVertex(calc = true) {
    // If basePolygon doesn't exist, exit early
    if (!this.basePolygon) return;

    if (!this.basePolygon.vertex) {
      this.basePolygon.vertex = [];
    }

    if (calc) {
      this.basePolygon.vertex = this.basePolygon.vertex.filter(v => !v.label.startsWith('E'));
      let basePolygonCoords = Object.values(this.basePolygon.getCoords());
      basePolygonCoords.forEach((p, i) => {
        this.basePolygon.vertex.push({ x: p.x, y: p.y, label: `E${i * 2 + 1}`, display: 1 });
        const midpoint = {
          x: (p.x + basePolygonCoords[(i + 1 === basePolygonCoords.length) ? 0 : i + 1].x) / 2,
          y: (p.y + basePolygonCoords[(i + 1 === basePolygonCoords.length) ? 0 : i + 1].y) / 2,
          label: `E${(i + 1) * 2}`,
          display: 1
        };
        this.basePolygon.vertex.push(midpoint);
      });
      //if (this.addMidPointToDivider) {
      //  this.addMidPointToDivider(this);
      //}
    }


    // Always check current GeneralSettings, not just when toggled
    const showAllVertices = GeneralSettings && GeneralSettings.showAllVertices;

    // Process vertices according to hierarchy and create/update controls
    this.basePolygon.vertex.forEach(v => {
      const vertexLabel = v.label;
      let shouldDisplay = false;

      // Rule 1: Native controls (ml, tl, br, etc.) should always be hidden
      if (/^(ml|mr|mt|mb|mtr|tl|tr|bl|br)$/.test(vertexLabel)) {
        shouldDisplay = false;
      }
      // Rule 2: Always no show side road vertices
      else if (this.functionalType === 'SideRoad' && v.label.startsWith('E')) {
        shouldDisplay = false;
      }
      // Rule 3: Focus mode - only show active vertex
      else if (this.focusMode && _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex) {
        shouldDisplay = (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.vertex.label === vertexLabel);
      }
      // Rule 4: ShowAllVertices setting overrides other display logic
      else if (showAllVertices) {
        shouldDisplay = true;
      }
      // Rule 5: Base on display property or default to showing vertex with labels
      else {
        shouldDisplay = (v.display == 1);
      }

      // Update or create control for this vertex
      if (!this.controls[vertexLabel]) {
        // Create new control for vertices that don't have one
        this.controls[vertexLabel] = new _vertex_js__WEBPACK_IMPORTED_MODULE_3__.VertexControl(v, this);
        this.controls[vertexLabel].visible = shouldDisplay;
      } else {
        // Update existing control position and visibility
        this.controls[vertexLabel].vertex = v;
        this.controls[vertexLabel].x = (v.x - this.left) / this.width - 0.5;
        this.controls[vertexLabel].y = (v.y - this.top) / this.height - 0.5;
      }

      // For SideRoad objects, set the control location to match the basePolygon location
      if (this.functionalType === 'SideRoad') {
        // Update control position to match the vertex position
        this.controls[vertexLabel].x = (v.x - this.left) / this.width - 0.5;
        this.controls[vertexLabel].y = (v.y - this.top) / this.height - 0.5;

        // Ensure the offsets are reset
        this.controls[vertexLabel].offsetX = 0;
        this.controls[vertexLabel].offsetY = 0;
      }

      this.controls[vertexLabel].visible = shouldDisplay;
    });


    this.setCoords();
  }
  // Method to emit deltaX and deltaY to anchored groups
  emitDelta(deltaX, deltaY, _sourceList = []) {
    // For X axis changes
    if (deltaX !== 0) {
      // Get all objects that should be updated in X axis
      const xUpdateOrder = _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.getUpdateOrder('x', this.canvasID);

      // Start an X-axis update cycle if needed with this object as the starter
      if (!_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressX) {
        _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.startUpdateCycle('x', this.canvasID);
      }

      // Mark this object as updated in X axis
      _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsX.add(this.canvasID);

      // Update each object in X axis
      xUpdateOrder.forEach(obj => {
        if (!_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsX.has(obj.canvasID)) {
          _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsX.add(obj.canvasID);
          obj.set({ left: obj.left + deltaX });
          obj.setCoords();
          obj.updateAllCoord(null, [], true); // selfOnly=true to prevent recursion
        }
      });

      // End the X-axis update cycle if this object started it
      if (_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.isUpdateStarter('x', this.canvasID)) {
        _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.endUpdateCycle('x');
      }
    }

    // For Y axis changes
    if (deltaY !== 0) {
      // Get all objects that should be updated in Y axis
      const yUpdateOrder = _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.getUpdateOrder('y', this.canvasID);

      // Start a Y-axis update cycle if needed with this object as the starter
      if (!_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressY) {
        _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.startUpdateCycle('y', this.canvasID);
      }

      // Mark this object as updated in Y axis
      _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsY.add(this.canvasID);

      // Update each object in Y axis
      yUpdateOrder.forEach(obj => {
        if (!_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsY.has(obj.canvasID)) {
          _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsY.add(obj.canvasID);
          obj.set({ top: obj.top + deltaY });
          obj.setCoords();
          obj.updateAllCoord(null, [], true); // selfOnly=true to prevent recursion
        }
      });

      // End the Y-axis update cycle if this object started it
      if (_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.isUpdateStarter('y', this.canvasID)) {
        _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.endUpdateCycle('y');
      }
    }


  }

  // Method to receive deltaX and deltaY and update position
  receiveDelta(caller, deltaX, deltaY, _sourceList = []) {
    // Only update axis if the caller is the target of our lock
    const newDeltaX = this.lockXToPolygon.TargetObject == caller ? deltaX : 0;
    const newDeltaY = this.lockYToPolygon.TargetObject == caller ? deltaY : 0;

    // Update position
    this.set({
      left: this.left + newDeltaX,
      top: this.top + newDeltaY
    });

    this.setCoords();

    // Track if we start update cycles that need to be closed
    let startedXCycle = false;
    let startedYCycle = false;

    try {
      // Use the AnchorTree to update coordinates properly
      if (newDeltaX !== 0) {
        // Start an X-axis update cycle if needed
        if (!_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressX) {
          _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.startUpdateCycle('x');
          startedXCycle = true;
        }
        _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsX.add(this.canvasID);
      }

      if (newDeltaY !== 0) {
        // Start a Y-axis update cycle if needed
        if (!_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressY) {
          _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.startUpdateCycle('y');
          startedYCycle = true;
        }
        _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsY.add(this.canvasID);
      }

      // Update coordinates without using sourceList
      this.updateAllCoord(null, [], false);
    } finally {
      // Always end update cycles if we started them, even if an error occurs
      if (startedXCycle) {
        _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.endUpdateCycle('x');
      }

      if (startedYCycle) {
        _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.endUpdateCycle('y');
      }
    }
  }



  // Method to call for border resizing
  borderResize(_sourceList = []) {
    if (this.borderGroup && this.borderGroup.canvasID && !_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsX.has(this.borderGroup.canvasID) &&
      !_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsY.has(this.borderGroup.canvasID)) {

      this.borderGroup.processResize()
    }
  }

  getBasePolygonVertex(label) {
    // Add null check
    if (!this.basePolygon || !this.basePolygon.vertex) return null;
    return this.basePolygon.vertex.find(v => v.label === label.toUpperCase());
  }

  drawAnchorLinkage() {
    for (let i = this.anchorageLink.length - 1; i >= 0; i--) {
      const obj = this.anchorageLink[i];

      this.anchorageLink.splice(i, 1); // Remove the element from the array
      obj.objects.forEach(e => {
        this.remove(e); // Remove the object from the group
        canvas.remove(e); // Remove the object from the canvas
      })

    }
    if (Object.keys(this.lockXToPolygon).length) {
      let lockAnno1 = new _lock_js__WEBPACK_IMPORTED_MODULE_5__.LockIcon(this, this.lockXToPolygon, 'x')
      this.anchorageLink.push(lockAnno1);
      canvas.add(...lockAnno1.objects);

    }
    if (Object.keys(this.lockYToPolygon).length) {
      let lockAnno2 = new _lock_js__WEBPACK_IMPORTED_MODULE_5__.LockIcon(this, this.lockYToPolygon, 'y')
      this.anchorageLink.push(lockAnno2);
      canvas.add(...lockAnno2.objects);

    }
    const isActive = canvas.getActiveObject() === this;
    const opacity = isActive ? 1 : 0;
    this.anchorageLink.forEach(obj => {
      obj.objects.forEach(o => {
        o.set('opacity', opacity);
      })
    });
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();

  }
  // Method to update coordinates and emit delta
  updateAllCoord(event, _sourceList = [], _selfOnly = false) {
    // Check for basePolygon before calculating deltas
    if (!this.basePolygon || !this.basePolygon.getCoords) {
      // If basePolygon doesn't exist yet, just return
      return;
    }
    const initialCoords = this.basePolygon.getCoords();
    if (!initialCoords || initialCoords.length === 0) {
      // console.warn("updateAllCoord: basePolygon.getCoords() returned empty or null for object:", this.canvasID);
      return;
    }

    const deltaX = initialCoords[0].x - this.refTopLeft.left;
    const deltaY = initialCoords[0].y - this.refTopLeft.top;

    // Only track modifications if actual movement occurred for this object
    if (deltaX !== 0 || deltaY !== 0) {
      _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_2__.canvasTracker.track('modifyObject', [{
        type: 'BaseGroup',
        id: this.canvasID,
        functionalType: this.functionalType,
        deltaX: deltaX,
        deltaY: deltaY,
        isInitialMover: !(_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressX || _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressY) || (_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.starterObjectX === this.canvasID || _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.starterObjectY === this.canvasID)
      }]);
      this.updateCoord(deltaX, deltaY);
      this.refTopLeft = { top: initialCoords[0].y, left: initialCoords[0].x };
    }


    // Check for route-specific methods
    if (this.onMove) {
      this.onMove();
    }
    if (canvas.getActiveObject() === this) {
      this.drawAnchorLinkage();
      this.showLockHighlights();
      this.showDimensions();
    }
    // Check if this object is already being processed in the current update cycle
    const alreadyProcessedX = _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressX && _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsX.has(this.canvasID);
    const alreadyProcessedY = _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressY && _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsY.has(this.canvasID);

    // Check if this object has been directly moved through anchoring
    // If either lockXToPolygon or lockYToPolygon has values, it's an anchored object
    const isAnchoredObjectX = Object.keys(this.lockXToPolygon).length > 0;
    const isAnchoredObjectY = Object.keys(this.lockYToPolygon).length > 0;

    // Determine if this specific call to updateAllCoord should initiate propagation.
    // This happens if the object itself moved (deltaX or deltaY != 0)
    // or if it's the designated starter of an anchor update chain.
    // let shouldInitiatePropagation = (deltaX !== 0 || deltaY !== 0); // Old logic

    let startedXCycle = false;
    let startedYCycle = false;

    // Start cycles if this object moved and no cycle is active for that axis
    if (!_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressX) {
      _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.startUpdateCycle('x', this.canvasID);
      startedXCycle = true;
    }
    if (!_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressY) {
      _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.startUpdateCycle('y', this.canvasID);
      startedYCycle = true;
    }

    // Mark current object as processed in active cycles, to prevent re-processing it via getUpdateOrder if it's already handled.
    if (_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressX && !alreadyProcessedX) {
      _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsX.add(this.canvasID);
    }
    if (_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressY && !alreadyProcessedY) {
      _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsY.add(this.canvasID);
    }

    // Proceed with updating anchored children if:
    // 1. This object itself moved (deltaX or deltaY is non-zero), OR
    // 2. An anchor update cycle is currently in progress for X or Y axis.
    if (_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressX || _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressY) {

      const xUpdateOrder = (_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressX) ? _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.getUpdateOrder('x', this.canvasID) : [];
      const yUpdateOrder = (_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateInProgressY) ? _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.getUpdateOrder('y', this.canvasID) : [];

      const allObjectsToUpdate = new Map();

      xUpdateOrder.forEach(obj => {
        if (!_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsX.has(obj.canvasID)) {
          allObjectsToUpdate.set(obj.canvasID, { object: obj, fromXChain: true, fromYChain: false });
        }
      });

      yUpdateOrder.forEach(obj => {
        if (!_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsY.has(obj.canvasID)) {
          if (allObjectsToUpdate.has(obj.canvasID)) {
            allObjectsToUpdate.get(obj.canvasID).fromYChain = true;
          } else {
            allObjectsToUpdate.set(obj.canvasID, { object: obj, fromXChain: false, fromYChain: true });
          }
        }
      });

      allObjectsToUpdate.forEach(info => {
        const currentAnchoredObject = info.object;
        const { desiredLeft, desiredTop } = currentAnchoredObject.determineAnchoredPosition();

        const actualDeltaX = desiredLeft - currentAnchoredObject.left;
        const actualDeltaY = desiredTop - currentAnchoredObject.top;

        currentAnchoredObject.left = desiredLeft;
        currentAnchoredObject.top = desiredTop;

        _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsX.add(currentAnchoredObject.canvasID);
        _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updatedObjectsY.add(currentAnchoredObject.canvasID);

        //canvasTracker.track('modifyObject', [{
        //  type: 'BaseGroup',
        //  id: currentAnchoredObject.canvasID,
        //  functionalType: currentAnchoredObject.functionalType,
        //  deltaX: actualDeltaX,
        //  deltaY: actualDeltaY,
        //  triggeredByAnchor: true
        //}]);

        currentAnchoredObject.updateAllCoord(null, []); // Recursive call

      });

      if (startedXCycle && _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.isUpdateStarter('x', this.canvasID)) {
        _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.endUpdateCycle('x');
      }
      if (startedYCycle && _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.isUpdateStarter('y', this.canvasID)) {
        _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.endUpdateCycle('y');
      }
    }

    // Check if border resize is needed
    const firstXBorder = _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.starterObjectX !== null ? canvasObject[_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.starterObjectX] == null ? null : canvasObject[_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.starterObjectX].borderGroup : null;
    const firstYBorder = _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.starterObjectY !== null ? canvasObject[_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.starterObjectY] == null ? null : canvasObject[_anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.starterObjectY].borderGroup : null;
    if (
      (this.borderGroup !== firstXBorder ||
        this.canvasID == _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.starterObjectX
      ) &&
      (this.borderGroup !== firstYBorder ||
        this.canvasID == _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.starterObjectY)) {
      this.borderResize();
    }
    this.setCoords();
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
  }

  // Method to update coordinates
  updateCoord(updateX, updateY) {
    // Check for basePolygon
    if (!this.basePolygon || !this.basePolygon.vertex) {
      return;
    }

    const polygon = this.basePolygon;

    const transformedPoints = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.calculateTransformedPoints)(polygon.vertex, {
      x: updateX,
      y: updateY,
      angle: 0
    });

    // Update customList with new coordinates
    transformedPoints.forEach((point, index) => {
      polygon.vertex[index].x = point.x;
      polygon.vertex[index].y = point.y;
    });

    if (this.routeList) {
      this.routeList.forEach((item) => {
        item.x += updateX;
        item.y += updateY;
      });
    }

    polygon.insertPoint = transformedPoints[0];

  }

  getEffectiveCoords() {
    // Add null check
    if (!this.basePolygon) {
      return [
        { x: this.left, y: this.top },
        { x: this.left + this.width, y: this.top },
        { x: this.left + this.width, y: this.top + this.height },
        { x: this.left, y: this.top + this.height }
      ];
    }

    return this.basePolygon.getCoords();
  }
  // Method to delete the object
  deleteObject(_eventData, transform) {
    const deleteObj = transform?.target || transform || this
    if (typeof deleteObj.hideHoverSnapVertexEnvelope === 'function') {
      deleteObj.hideHoverSnapVertexEnvelope();
    }

    // Track object deletion before actually deleting it
    _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_2__.canvasTracker.track('deleteObject', [{
      type: 'BaseGroup',
      id: deleteObj.canvasID,
      functionalType: deleteObj.functionalType
    }]);

    // Close property panel if it is open for the object being deleted
    (0,_sidebar_property_js__WEBPACK_IMPORTED_MODULE_8__.handleClear)(null);

    // Delete Side road first as they affects the canvasID numbering
    if (deleteObj.sideRoad) {
      const sideRoad = deleteObj.sideRoad
      sideRoad.forEach(side => {
        side.mainRoad = null
        side.deleteObject()
      })
    }

    // Store the original canvasID before removing the object
    const originalCanvasID = deleteObj.canvasID;

    // Now remove from the canvasObject array and create ID mapping
    const index = canvasObject.indexOf(deleteObj)
    if (index > -1) {
      canvasObject.splice(index, 1);

      // Create a mapping of old IDs to new IDs for anchor tree update
      const idMapping = {};

      // Update IDs for remaining objects
      for (let i = index; i < canvasObject.length; i++) {
        const oldID = canvasObject[i].canvasID;
        canvasObject[i].canvasID -= 1;
        idMapping[oldID] = canvasObject[i].canvasID;
      }

      // Update the anchor tree with the new ID mapping
      if (typeof _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree !== 'undefined') {
        _anchor_js__WEBPACK_IMPORTED_MODULE_6__.globalAnchorTree.updateOnDelete(originalCanvasID, idMapping);
      }
    }

    //delete route branch
    if (deleteObj.mainRoad) {
      const mainRoad = deleteObj.mainRoad
      const branchIndex = mainRoad.sideRoad.indexOf(deleteObj)
      if (branchIndex >= 0) {
        mainRoad.sideRoad.splice(branchIndex, 1)
        // Find and remove the vertices with matching labels for the branch being deleted
        const vertexLabels = [`C${branchIndex}`];
        mainRoad.basePolygon.vertex = mainRoad.basePolygon.vertex.filter(vertex =>
          !vertexLabels.includes(vertex.label)
        );
      }

      mainRoad.receiveNewRoute()
      mainRoad.setCoords()

    }

    // Free anchored Polygon
    if (deleteObj.anchoredPolygon) {
      deleteObj.anchoredPolygon.forEach(anchoredGroup => {

        if (anchoredGroup.lockXToPolygon.TargetObject == deleteObj) {
          anchoredGroup.lockXToPolygon = {}
          anchoredGroup.set({ lockMovementX: false });
        }
        if (anchoredGroup.lockYToPolygon.TargetObject == deleteObj) {
          anchoredGroup.lockYToPolygon = {}
          anchoredGroup.set({ lockMovementY: false });
        }
        anchoredGroup.focusMode = false;
        anchoredGroup.drawAnchorLinkage()
      })
    }

    // Free border
    if (deleteObj.borderGroup) {
      ['widthObjects', 'heightObjects', 'HDivider', 'VDivider'].forEach(prop => {
        const arr = deleteObj.borderGroup[prop];
        if (Array.isArray(arr)) {
          const idx = arr.indexOf(deleteObj);
          if (idx > -1) arr.splice(idx, 1);
        }
      });
      if (deleteObj.functionalType !== 'HDivider' && deleteObj.functionalType !== 'VDivider') {
        deleteObj.borderResize();
      }
    }

    // If this is a borderGroup
    if (deleteObj.widthObjects) {
      deleteObj.widthObjects.forEach(obj => obj.borderGroup = null)
    }
    if (deleteObj.heightObjects) {
      deleteObj.heightObjects.forEach(obj => obj.borderGroup = null)
    }

    // Copy arrays to avoid mutation during iteration
    const allDividers = [
      ...(deleteObj.HDivider ? [...deleteObj.HDivider.filter(obj => obj.functionalType !== 'HLine')] : []),
      ...(deleteObj.VDivider ? [...deleteObj.VDivider] : [])
    ];
    allDividers.forEach(div => {
      if (div && typeof div.deleteObject === 'function') {
        div.deleteObject(null, div);
      }
    });
    canvas.remove(deleteObj);
    _sidebar_sb_inspector_js__WEBPACK_IMPORTED_MODULE_7__.CanvasObjectInspector.createObjectListPanelInit()
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
  }

  renderIcon(ctx, left, top, _styleOverride, fabricObject) {
    const size = this.cornerSize;

    var deleteImg = document.createElement('img');
    deleteImg.src = deleteIcon;
    ctx.save();
    ctx.translate(left, top);
    ctx.rotate(fabric.util.degreesToRadians(fabricObject.angle));
    ctx.drawImage(deleteImg, -size / 2, -size / 2, size, size);
    ctx.restore();
  }

  // New method to determine position based on anchors
  determineAnchoredPosition() {
    let desiredLeft = this.left;
    let desiredTop = this.top;

    const getAbsoluteVertex = (obj, label) => {
      if (!obj || !obj.basePolygon || !obj.basePolygon.vertex) return null;
      // basePolygon.vertex stores absolute canvas coordinates
      return obj.basePolygon.vertex.find(v => v.label === label);
    };

    if (this.lockXToPolygon && this.lockXToPolygon.TargetObject) {
      this.lockXToPolygon.TargetObject = canvasObject[this.lockXToPolygon.TargetObject.canvasID];
      const targetObj = this.lockXToPolygon.TargetObject;

      const targetVertex = getAbsoluteVertex(targetObj, this.lockXToPolygon.targetPoint);
      const sourceVertex = getAbsoluteVertex(this, this.lockXToPolygon.sourcePoint);
      const spacing = this.lockXToPolygon.spacing || 0;

      if (targetVertex && sourceVertex) {
        const offsetSourceX = sourceVertex.x - this.left;
        desiredLeft = targetVertex.x + spacing - offsetSourceX;
      } else {
        // console.warn('X Lock: Target or source vertex not found for object.', {thisId: this.canvasID, targetId: targetObj?.canvasID, targetPoint: this.lockXToPolygon.targetPoint, sourcePoint: this.lockXToPolygon.sourcePoint});
      }
    }

    if (this.lockYToPolygon && this.lockYToPolygon.TargetObject) {
      this.lockYToPolygon.TargetObject = canvasObject[this.lockYToPolygon.TargetObject.canvasID];
      const targetObj = this.lockYToPolygon.TargetObject;

      const targetVertex = getAbsoluteVertex(targetObj, this.lockYToPolygon.targetPoint);
      const sourceVertex = getAbsoluteVertex(this, this.lockYToPolygon.sourcePoint);
      const spacing = this.lockYToPolygon.spacing || 0;

      if (targetVertex && sourceVertex) {
        const offsetSourceY = sourceVertex.y - this.top;
        desiredTop = targetVertex.y + spacing - offsetSourceY;
      } else {
        // console.warn('Y Lock: Target or source vertex not found for object.', {thisId: this.canvasID, targetId: targetObj?.canvasID, targetPoint: this.lockYToPolygon.targetPoint, sourcePoint: this.lockYToPolygon.sourcePoint});
      }
    }
    return { desiredLeft, desiredTop };
  }


  // New method: get final lock target along given axis
  getFinalLockTarget(axis) {
    const lockProp = axis === 'x' ? 'lockXToPolygon' : 'lockYToPolygon';
    let current = this;
    const visited = new Set();
    while (current[lockProp] && current[lockProp].TargetObject) {
      let next = current[lockProp].TargetObject;
      if (visited.has(next)) break;
      visited.add(next);
      current = next;
    }
    return current !== this ? current : null;
  }

  // New method: display temporary highlight border for lock targets
  showLockHighlights() {
    this.hideLockHighlights()
    if (this.lockXToPolygon && Object.keys(this.lockXToPolygon).length) {
      const finalX = this.getFinalLockTarget('x');
      if (finalX) {
        const rect = finalX.getBoundingRect ? finalX.getBoundingRect() : finalX.getCoords();
        this.lockHighlightX = new fabric.Rect({
          left: rect.left - 2,
          top: rect.top - 2,
          width: rect.width - 2,
          height: rect.height - 2,
          fill: 'transparent',
          stroke: 'green',
          strokeWidth: 4,
          selectable: false,
          evented: false,
        });
        canvas.add(this.lockHighlightX);
      }
    }
    if (this.lockYToPolygon && Object.keys(this.lockYToPolygon).length) {
      const finalY = this.getFinalLockTarget('y');
      if (finalY) {
        const rect = finalY.getBoundingRect ? finalY.getBoundingRect() : finalY.getCoords();
        this.lockHighlightY = new fabric.Rect({
          left: rect.left - 2,
          top: rect.top - 2,
          width: rect.width - 2,
          height: rect.height - 2,
          fill: 'transparent',
          stroke: 'red',
          strokeWidth: 4,
          selectable: false,
          evented: false,
        });
        canvas.add(this.lockHighlightY);
      }
    }
  }

  // New method: remove the temporary highlight borders
  hideLockHighlights() {
    if (this.lockHighlightX) {
      canvas.remove(this.lockHighlightX);
      this.lockHighlightX = null;
    }
    if (this.lockHighlightY) {
      canvas.remove(this.lockHighlightY);
      this.lockHighlightY = null;
    }
  }

  showHoverSnapVertexEnvelope() {
    this.hideHoverSnapVertexEnvelope();

    if (!this.basePolygon || !this.basePolygon.vertex) return;

    const showAllVertices = GeneralSettings && GeneralSettings.showAllVertices;
    const zoomFactor = canvas.getZoom() || 1;
    const radius = 12 / zoomFactor;
    const strokeWidth = 2 / zoomFactor;

    this.basePolygon.vertex.forEach(v => {
      const vertexLabel = v.label || '';
      let shouldDisplay = false;

      if (/^(ml|mr|mt|mb|mtr|tl|tr|bl|br)$/.test(vertexLabel)) {
        shouldDisplay = false;
      } else if (this.functionalType === 'SideRoad' && vertexLabel.startsWith('E')) {
        shouldDisplay = false;
      } else if (showAllVertices) {
        shouldDisplay = true;
      } else {
        shouldDisplay = (v.display == 1);
      }

      if (!shouldDisplay) return;

      const envelope = new fabric.Circle({
        left: v.x,
        top: v.y,
        radius: radius,
        fill: 'transparent',
        stroke: '#00B7FF',
        strokeWidth: strokeWidth,
        selectable: false,
        evented: false,
        originX: 'center',
        originY: 'center'
      });

      this.hoverSnapEnvelopes.push(envelope);
      canvas.add(envelope);
    });

    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
  }

  hideHoverSnapVertexEnvelope() {
    if (!this.hoverSnapEnvelopes || this.hoverSnapEnvelopes.length === 0) return;
    this.hoverSnapEnvelopes.forEach(envelope => canvas.remove(envelope));
    this.hoverSnapEnvelopes = [];
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
  }

  // New method to enter focus mode
  enterFocusMode(activeVertexControl) {
    this.focusMode = true;
    //this.hideDimensions();
    //this.drawVertex(false);
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
  }

  // New method to exit focus mode
  exitFocusMode() {
    this.focusMode = false;
    //this.drawVertex(false);
    //if (canvas.getActiveObject() === this) {
    //  this.showDimensions();
    //}
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
  }
}

// Register the custom class with Fabric.js
fabric.BaseGroup = BaseGroup;





/***/ }),

/***/ "./js/objects/lock.js":
/*!****************************!*\
  !*** ./js/objects/lock.js ***!
  \****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   LockIcon: () => (/* binding */ LockIcon)
/* harmony export */ });
/* harmony import */ var _anchor_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./anchor.js */ "./js/objects/anchor.js");
/* harmony import */ var _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../canvas/Tracker.js */ "./js/canvas/Tracker.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../sidebar/sbGeneral.js */ "./js/sidebar/sbGeneral.js");




const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.canvas;

class LockIcon {
  constructor(baseGroup, lockParam, direction) {
    this.objectType = 'LockIcon'; // Added objectType for serialization
    this.baseGroup = baseGroup; // This will be serialized as baseGroupCanvasID
    this.lockParam = lockParam; // This will be serialized carefully
    this.direction = direction
    this.lines = []
    this.dimensionTexts = []
    this.icons = []

    let sourcePoint = lockParam.sourceObject.getBasePolygonVertex(lockParam.sourcePoint)
    let targetPoint = lockParam.TargetObject.getBasePolygonVertex(lockParam.targetPoint)
    this.createLock(sourcePoint, targetPoint)

    if (lockParam.secondSourceObject) {
      let sourcePoint = lockParam.secondSourceObject.getBasePolygonVertex(
        lockParam.secondSourcePoint)
      let targetPoint = lockParam.secondTargetObject.getBasePolygonVertex(lockParam.secondTargetPoint)
      this.createLock(sourcePoint, targetPoint)
    }

    this.icons.forEach(i => {
      // Add hover and click event listeners
      i.on('mouseover', this.onHover.bind(this));
      i.on('mouseout', this.onMouseOut.bind(this));
      i.on('mousedown', this.onClick.bind(this));
    })

    // Add lock lines and lock icon to the canvas
    this.objects = [...this.lines, ...this.dimensionTexts, ...this.icons,]
  }

  createLock(sourcePoint, targetPoint) {
    // Fixed offset distances for engineering dimension style
    const offsetDistance = 30; // Fixed pixel offset for dimension line
    const iconOffset = 15;     // Fixed pixel offset for lock icon

    // Scale adjustments based on zoom
    const zoom = canvas.getZoom();
    const lineWidth = 1 / zoom;        // Thinner lines for engineering style
    const fontSize = 12 / zoom;        // Fixed 12px font size
    const arrowSize = 12 / zoom;        // Size of dimension arrows
    const extensionLineLength = 8 / zoom; // Length of extension lines

    // Create dimension lines and position lock icon based on direction
    if (this.direction == 'x') {
      // Position of dimension line, offset from source and target points
      const dimLineY = sourcePoint.y + offsetDistance / zoom;

      // Extension lines (vertical lines from source and target points to dimension line)
      this.lines.push(new fabric.Line(
        [sourcePoint.x, sourcePoint.y, sourcePoint.x, dimLineY + extensionLineLength],
        {
          stroke: '#46C147',
          strokeWidth: lineWidth,
          selectable: false,
          functionalType: 'anchorLine'
        }
      ));

      this.lines.push(new fabric.Line(
        [targetPoint.x, targetPoint.y, targetPoint.x, dimLineY + extensionLineLength],
        {
          stroke: '#46C147',
          strokeWidth: lineWidth,
          selectable: false,
          functionalType: 'anchorLine'
        }
      ));

      // Main dimension line
      this.lines.push(new fabric.Line(
        [sourcePoint.x, dimLineY, targetPoint.x, dimLineY],
        {
          stroke: '#46C147',
          strokeWidth: lineWidth,
          selectable: false,
          functionalType: 'anchorLine',
        }
      ));

      // Add arrow endpoints
      this.addArrow(sourcePoint.x, dimLineY, 'right', '#46C147', arrowSize);
      this.addArrow(targetPoint.x, dimLineY, 'left', '#46C147', arrowSize);

      // Calculate the midpoint for dimension text and icon
      const midX = (sourcePoint.x + targetPoint.x) / 2;
      const midY = dimLineY - (iconOffset / zoom);

      // Dimension text
      this.dimensionTexts.push(new fabric.Text(
        _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_3__.GeneralSettings.formatDimension(targetPoint.x - sourcePoint.x, this.baseGroup.xHeight),
        {
          left: midX,
          top: dimLineY + (25 / canvas.getZoom()),
          fontSize: fontSize,
          fill: '#46C147',
          fontFamily: 'Arial',
          textAlign: 'center',
          originX: 'center',
          originY: 'bottom',
          selectable: false,
          evented: false,
          stroke: '#fff',
          strokeWidth: 3 / canvas.getZoom(),
          paintFirst: 'stroke'
        }
      ));

      // Lock icon at fixed position relative to dimension line
      this.icons.push(new fabric.Text('\uf023', {
        fontFamily: 'Font Awesome 5 Free',
        fontWeight: 900,
        left: midX,
        top: midY,
        fontSize: fontSize,
        fill: 'gold',
        stroke: 'black',
        strokeWidth: 0.5 / zoom,
        originX: 'center',
        originY: 'center',
        selectable: false,
        functionalType: 'lockIcon',
      }));

    } else { // Y direction
      // Position of dimension line, offset from source and target points
      const dimLineX = sourcePoint.x + offsetDistance / zoom;

      // Extension lines (horizontal lines from source and target points to dimension line)
      this.lines.push(new fabric.Line(
        [sourcePoint.x, sourcePoint.y, dimLineX + extensionLineLength, sourcePoint.y],
        {
          stroke: 'red',
          strokeWidth: lineWidth,
          selectable: false,
          functionalType: 'anchorLine'
        }
      ));

      this.lines.push(new fabric.Line(
        [targetPoint.x, targetPoint.y, dimLineX + extensionLineLength, targetPoint.y],
        {
          stroke: 'red',
          strokeWidth: lineWidth,
          selectable: false,
          functionalType: 'anchorLine'
        }
      ));

      // Main dimension line
      this.lines.push(new fabric.Line(
        [dimLineX, sourcePoint.y, dimLineX, targetPoint.y],
        {
          stroke: 'red',
          strokeWidth: lineWidth,
          selectable: false,
          functionalType: 'anchorLine',
        }
      ));

      // Add arrow endpoints
      this.addArrow(dimLineX, sourcePoint.y, 'down', 'red', arrowSize);
      this.addArrow(dimLineX, targetPoint.y, 'up', 'red', arrowSize);

      // Calculate the midpoint for dimension text and icon
      const midX = dimLineX - (iconOffset / zoom);
      const midY = (sourcePoint.y + targetPoint.y) / 2;

      // Dimension text
      this.dimensionTexts.push(new fabric.Text(
        _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_3__.GeneralSettings.formatDimension(targetPoint.y - sourcePoint.y, this.baseGroup.xHeight),
        {
          left: midX + 45 / canvas.getZoom(),
          top: midY + 8 / canvas.getZoom(),
          fontSize: fontSize,
          fill: 'red',
          fontFamily: 'Arial',
          textAlign: 'center',
          originX: 'center',
          originY: 'bottom',
          selectable: false,
          evented: false,
          stroke: '#fff',
          strokeWidth: 3 / canvas.getZoom(),
          paintFirst: 'stroke'
        }
      ));

      // Lock icon at fixed position relative to dimension line
      this.icons.push(new fabric.Text('\uf023', {
        fontFamily: 'Font Awesome 5 Free',
        fontWeight: 900,
        left: midX,
        top: midY,
        fontSize: fontSize,
        fill: 'gold',
        stroke: 'black',
        strokeWidth: 0.5 / zoom,
        originX: 'center',
        originY: 'center',
        selectable: false,
        functionalType: 'lockIcon',
      }));
    }
  }

  // Helper method to add arrow endpoints to the dimension lines
  addArrow(x, y, direction, color, size) {
    let points;

    switch (direction) {
      case 'right':
        points = [
          { x: x, y: y },
          { x: x + size, y: y - size / 4 },
          { x: x + size, y: y + size / 4 }
        ];
        break;
      case 'left':
        points = [
          { x: x, y: y },
          { x: x - size, y: y - size / 4 },
          { x: x - size, y: y + size / 4 }
        ];
        break;
      case 'up':
        points = [
          { x: x, y: y },
          { x: x - size / 4, y: y - size },
          { x: x + size / 4, y: y - size }
        ];
        break;
      case 'down':
        points = [
          { x: x, y: y },
          { x: x - size / 4, y: y + size },
          { x: x + size / 4, y: y + size }
        ];
        break;
    }

    const arrow = new fabric.Polygon(points, {
      fill: color,
      stroke: color,
      strokeWidth: 0,
      selectable: false,
      functionalType: 'anchorArrow'
    });

    this.lines.push(arrow);
  }

  onHover(event) {
    const i = this.icons.indexOf(event.target)
    if (i !== -1) {
      this.icons[i].set('text', '\uf3c1');
      this.icons[i].set('fill', 'brown');
      this.dimensionTexts[i].set('fill', 'brown');
      this.icons[i].set('hoverCursor', 'pointer')
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.scheduleRender();
    }
  }

  onMouseOut(event) {
    const i = this.icons.indexOf(event.target)
    if (i !== -1) {
      this.icons[i].set('text', '\uf023');
      this.icons[i].set('fill', 'gold');
      this.dimensionTexts[i].set('fill', this.direction === 'x' ? '#46C147' : 'red');
      this.icons[i].set('hoverCursor', 'default')
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.scheduleRender();
    }
  }

  onClick() {
    // Remove lock lines and lock icon from the canvas and baseGroup
    canvas.remove(...this.objects);

    // Remove this lock icon from the baseGroup's anchorageLink list
    const anchorageIndex = this.baseGroup.anchorageLink.indexOf(this);
    if (anchorageIndex !== -1) {
      this.baseGroup.anchorageLink.splice(anchorageIndex, 1);
    }

    // Get references to both target objects (if they exist)
    const anchorX = this.baseGroup.lockXToPolygon.TargetObject;
    const anchorY = this.baseGroup.lockYToPolygon.TargetObject;

    if (this.direction == 'x') {
      // Handle X direction unlock
      this.baseGroup.lockMovementX = false;

      // Remove from globalAnchorTree x tree before clearing lockXToPolygon
      if (anchorX) {
        const tree = _anchor_js__WEBPACK_IMPORTED_MODULE_0__.globalAnchorTree.xTree;
        const nodeId = this.baseGroup.canvasID;
        const parentId = anchorX.canvasID;

        if (tree[nodeId] && tree[nodeId].children.length > 0) {
          // If the object has children, preserve the node and only remove the parent link
          if (tree[parentId]) {
            tree[parentId].children = tree[parentId].children.filter(id => id !== nodeId);
          }
          tree[nodeId].parents = tree[nodeId].parents.filter(id => id !== parentId);
        } else {
          _anchor_js__WEBPACK_IMPORTED_MODULE_0__.globalAnchorTree.removeNode('x', this.baseGroup.canvasID);
        }
      }

      // Clear the lockXToPolygon object
      this.baseGroup.lockXToPolygon = {};

      // Only remove from anchoredPolygon if this object is not also Y-anchored to the same target
      if (anchorX && (anchorY !== anchorX)) {
        const anchoredIndex = anchorX.anchoredPolygon.indexOf(this.baseGroup);
        if (anchoredIndex !== -1) {
          anchorX.anchoredPolygon.splice(anchoredIndex, 1);
        }
      }
    } else {
      // Handle Y direction unlock
      this.baseGroup.lockMovementY = false;

      // Remove from globalAnchorTree y tree before clearing lockYToPolygon
      if (anchorY) {
        const tree = _anchor_js__WEBPACK_IMPORTED_MODULE_0__.globalAnchorTree.yTree;
        const nodeId = this.baseGroup.canvasID;
        const parentId = anchorY.canvasID;

        if (tree[nodeId] && tree[nodeId].children.length > 0) {
          // If the object has children, preserve the node and only remove the parent link
          if (tree[parentId]) {
            tree[parentId].children = tree[parentId].children.filter(id => id !== nodeId);
          }
          tree[nodeId].parents = tree[nodeId].parents.filter(id => id !== parentId);
        } else {
          _anchor_js__WEBPACK_IMPORTED_MODULE_0__.globalAnchorTree.removeNode('y', this.baseGroup.canvasID);
        }
      }

      // Clear the lockYToPolygon object
      this.baseGroup.lockYToPolygon = {};

      // Only remove from anchoredPolygon if this object is not also X-anchored to the same target
      if (anchorY && (anchorX !== anchorY)) {
        const anchoredIndex = anchorY.anchoredPolygon.indexOf(this.baseGroup);
        if (anchoredIndex !== -1) {
          anchorY.anchoredPolygon.splice(anchoredIndex, 1);
        }
      }
    }

    // If the object is no longer anchored to anything, ensure it's removed from all anchoredPolygon lists
    if (Object.keys(this.baseGroup.lockXToPolygon).length === 0 &&
      Object.keys(this.baseGroup.lockYToPolygon).length === 0) {

      // Check if we need to remove from anchorX's list
      if (anchorX) {
        const anchoredIndex = anchorX.anchoredPolygon.indexOf(this.baseGroup);
        if (anchoredIndex !== -1) {
          anchorX.anchoredPolygon.splice(anchoredIndex, 1);
        }
      }

      // Check if we need to remove from anchorY's list (if different from anchorX)
      if (anchorY && anchorY !== anchorX) {
        const anchoredIndex = anchorY.anchoredPolygon.indexOf(this.baseGroup);
        if (anchoredIndex !== -1) {
          anchorY.anchoredPolygon.splice(anchoredIndex, 1);
        }
      }
    }

    // Record this unlock operation in history (if canvasTracker exists)
    //if (canvasTracker) {
    //  canvasTracker.track('unlockObject', [{
    //    type: 'Unlock',
    //    objectId: this.baseGroup.canvasID,
    //    functionalType: this.baseGroup.functionalType,
    //    direction: this.direction
    //  }]);
    //}

    // Reshow the vertexes and dimensions
    this.baseGroup.focusMode = false;

  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.scheduleRender();
  }

  // Add a method to serialize LockIcon state
  serializeToJSON() {
    return {
      objectType: this.objectType,
      baseGroupCanvasID: this.baseGroup ? this.baseGroup.canvasID : null,
      lockParam: this.lockParam ? {
        TargetObject: this.lockParam.TargetObject ? this.lockParam.TargetObject.canvasID : null,
        spacingX: this.lockParam.spacingX,
        spacingY: this.lockParam.spacingY,
        sourcePoint: this.lockParam.sourcePoint,
        targetPoint: this.lockParam.targetPoint
      } : null,
      direction: this.direction
    };
  }

  // Method to refresh dimension text when unit setting changes
  refreshDimensions() {
    if (!this.dimensionTexts || this.dimensionTexts.length === 0) return;
    
    // Get the current spacing values
    const sourcePoint = this.baseGroup.getBasePolygonVertex(this.lockParam.sourcePoint);
    const targetPoint = this.lockParam.TargetObject.getBasePolygonVertex(this.lockParam.targetPoint);
    
    if (sourcePoint && targetPoint) {
      // Update dimension text based on direction
      this.dimensionTexts.forEach(text => {
        if (this.direction === 'x') {
          text.set('text', _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_3__.GeneralSettings.formatDimension(targetPoint.x - sourcePoint.x, this.baseGroup.xHeight));
        } else {
          text.set('text', _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_3__.GeneralSettings.formatDimension(targetPoint.y - sourcePoint.y, this.baseGroup.xHeight));
        }
      });
    }
  }
}



/***/ }),

/***/ "./js/objects/mainRoute.js":
/*!*********************************!*\
  !*** ./js/objects/mainRoute.js ***!
  \*********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   MainRoadSymbol: () => (/* binding */ MainRoadSymbol),
/* harmony export */   calcMainRoadVertices: () => (/* binding */ calcMainRoadVertices),
/* harmony export */   calcMainRoadVerticesMultipleArms: () => (/* binding */ calcMainRoadVerticesMultipleArms),
/* harmony export */   calcMainRoadVerticesSingleArm: () => (/* binding */ calcMainRoadVerticesSingleArm),
/* harmony export */   calcRoundaboutVertices: () => (/* binding */ calcRoundaboutVertices),
/* harmony export */   calcVertexType: () => (/* binding */ calcVertexType),
/* harmony export */   calculateMainRoadBottomY: () => (/* binding */ calculateMainRoadBottomY),
/* harmony export */   roadMapOnDeselect: () => (/* binding */ roadMapOnDeselect),
/* harmony export */   roadMapOnSelect: () => (/* binding */ roadMapOnSelect)
/* harmony export */ });
/* harmony import */ var _draw_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./draw.js */ "./js/objects/draw.js");
/* harmony import */ var _path_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./path.js */ "./js/objects/path.js");
/* harmony import */ var _template_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./template.js */ "./js/objects/template.js");
/* harmony import */ var _symbols_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./symbols.js */ "./js/objects/symbols.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _routeBase_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./routeBase.js */ "./js/objects/routeBase.js");
/* harmony import */ var _sideRoute_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./sideRoute.js */ "./js/objects/sideRoute.js");
/* harmony import */ var _anchor_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./anchor.js */ "./js/objects/anchor.js");
/* harmony import */ var _version_upgrades_v1_3_1_roundabout_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../version_upgrades/v1_3_1_roundabout.js */ "./js/version_upgrades/v1_3_1_roundabout.js");










const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_4__.CanvasGlobals.canvas;

const calcVertexType = {
    'Main Line': (xHeight, routeList, innerCornerRadius = null, outerCornerRadius = null) => calcMainRoadVertices(xHeight, routeList, innerCornerRadius, outerCornerRadius),
    'Conventional Roundabout': (xHeight, routeList, innerCornerRadius = null, outerCornerRadius = null) => calcRoundaboutVertices('Conventional', xHeight, routeList),
    'Spiral Roundabout': (xHeight, routeList, innerCornerRadius = null, outerCornerRadius = null) => calcRoundaboutVertices('Spiral', xHeight, routeList),
    'Oval Roundabout': (xHeight, routeList, innerCornerRadius = null, outerCornerRadius = null) => calcRoundaboutVertices('Oval', xHeight, routeList),
    'Double Roundabout': (xHeight, routeList, innerCornerRadius = null, outerCornerRadius = null) => calcRoundaboutVertices('Double', xHeight, routeList)
}

// Extract drawing functions from FormDrawMapComponent

/**
 * Calculates the correct bottom Y coordinate for a main road based on pivot points
 * @param {Object} RootTop - Top route object with angle, length, etc.
 * @param {Object} RootBottom - Bottom route object
 * @param {number} length - Calculated length (xHeight / 4)
 * @return {number} The calculated bottom Y coordinate
 */
function calculateMainRoadBottomY(RootTop, RootBottom, length) {
    // Get the angle in radians
    const angleRad = RootTop.angle === 0 ? 0 : RootTop.angle / Math.abs(RootTop.angle) * (90 - Math.abs(RootTop.angle)) * Math.PI / 180;

    // Get top vertices to calculate pivot points
    let RootTopVertex = (0,_routeBase_js__WEBPACK_IMPORTED_MODULE_5__.getSideRoadCoords)(RootTop, length);
    const topVertices = RootTopVertex.path[0].vertex;
    const topLeftVertex = topVertices[0];
    const topRightVertex = topVertices[topVertices.length - 1];
    const topVertex = topVertices.reduce((min, vertex) => vertex.y < min.y ? vertex : min, topVertices[0]);

    // Calculate pivot points
    const leftPivotX = RootBottom.x - RootBottom.width * length / 2;
    const rightPivotX = RootBottom.x + RootBottom.width * length / 2;
    let leftPivotY, rightPivotY;

    // Handle special cases for vertical angles (±90°)
    if (Math.abs(RootTop.angle) === 90) {
        leftPivotY = topLeftVertex.y;
        rightPivotY = topRightVertex.y;
    } else if (RootTop.angle == 0) {
        leftPivotY = topVertex.y + RootTop.length * length;
        rightPivotY = topVertex.y + RootTop.length * length;
    } else {
        // Normal case: calculate intersection using tangent
        leftPivotY = topLeftVertex.y + (leftPivotX - topLeftVertex.x) * Math.tan(-angleRad);
        rightPivotY = topRightVertex.y + (rightPivotX - topRightVertex.x) * Math.tan(-angleRad);
    }

    // Return the maximum of original bottom Y, and pivot points plus bottom length
    return Math.max(rightPivotY + RootBottom.length * length, leftPivotY + RootBottom.length * length);
}

/**
 * Calculates pivot points for a single arm
 * @param {Object} RootTop - Top route object
 * @param {Object} RootBottom - Bottom route object
 * @param {number} length - Calculated length (xHeight / 4)
 * @param {number} innerCornerRadius - Inner corner radius value
 * @param {number} outerCornerRadius - Outer corner radius value
 * @return {Object} Object containing left and right pivot points
 */
function calculatePivotPoints(RootTop, RootBottom, length, innerCornerRadius = null, outerCornerRadius = null) {
    let RootTopVertex = (0,_routeBase_js__WEBPACK_IMPORTED_MODULE_5__.getSideRoadCoords)(RootTop, length);
    const topVertices = RootTopVertex.path[0].vertex;

    // Get the angle in radians
    const angleRad = RootTop.angle === 0 ? 0 : RootTop.angle / Math.abs(RootTop.angle) * (90 - Math.abs(RootTop.angle)) * Math.PI / 180;

    // Get key vertices
    const topLeftVertex = topVertices[0];
    const topRightVertex = topVertices[topVertices.length - 1];
    const topVertex = topVertices.reduce((min, vertex) => vertex.y < min.y ? vertex : min, topVertices[0]);

    // Calculate pivot X coordinates
    const leftPivotX = RootBottom.x - RootBottom.width * length / 2;
    const midPivotX = RootBottom.x;
    const rightPivotX = RootBottom.x + RootBottom.width * length / 2;
    let leftPivotY, midPivotY, rightPivotY;

    // Handle special cases for vertical angles (±90°)
    if (Math.abs(RootTop.angle) === 90) {
        leftPivotY = topLeftVertex.y;
        midPivotY = topVertex.y;
        rightPivotY = topRightVertex.y;
    } else if (RootTop.angle == 0) {
        leftPivotY = topVertex.y + RootTop.length * length;
        midPivotY = topVertex.y + RootTop.length * length;
        rightPivotY = topVertex.y + RootTop.length * length;
    } else {
        // Normal case: calculate intersection using tangent
        leftPivotY = topLeftVertex.y + (leftPivotX - topLeftVertex.x) * Math.tan(-angleRad);
        midPivotY = topRightVertex.y + (midPivotX - topRightVertex.x) * Math.tan(-angleRad);
        rightPivotY = topRightVertex.y + (rightPivotX - topRightVertex.x) * Math.tan(-angleRad);
    }

    // Calculate radii - use provided values if available, otherwise use default calculation
    let leftRadius = null;
    let rightRadius = null;

    if (innerCornerRadius !== null && outerCornerRadius !== null) {
        // Use provided radius values
        switch (Math.sign(RootTop.angle)) {
            case 0:
                leftRadius = null;
                rightRadius = null;
                break;
            case 1:
                leftRadius = outerCornerRadius * length;
                rightRadius = innerCornerRadius * length;
                break;
            case -1:
                leftRadius = innerCornerRadius * length;
                rightRadius = outerCornerRadius * length;
                break;
        }
    } else {
        // Use default calculation
        switch (Math.sign(RootTop.angle)) {
            case 0:
                leftRadius = null;
                rightRadius = null;
                break;
            case 1:
                leftRadius = 4 * length;
                rightRadius = length;
                break;
            case -1:
                leftRadius = length;
                rightRadius = 4 * length;
                break;
        }
    }

    return {
        leftPivot: { x: leftPivotX, y: leftPivotY, display: 0, radius: leftRadius },
        midPivot: { x: midPivotX, y: midPivotY, display: 0, radius: 2 * length },
        rightPivot: { x: rightPivotX, y: rightPivotY, display: 0, radius: rightRadius },
        topVertex: RootTopVertex,
        angleRad
    };
}

/**
 * Processes vertex list and arcs for final output
 * @param {Array} vertexList - List of vertices
 * @param {Array} arcList - List of arcs
 * @param {Array} remainingPath - Additional paths to include
 * @return {Object} Processed vertex list object
 */
function processVertexListAndArcs(vertexList, arcList, remainingPath = []) {
    // Move the first vertex to the end of the list
    if (vertexList.length > 0) {
        const firstVertex = vertexList.shift();
        vertexList.push(firstVertex);
    }
    (0,_routeBase_js__WEBPACK_IMPORTED_MODULE_5__.assignVertexLabel)(vertexList);

    // remap the arc vertex
    function shiftV(v, vertexList) {
        let id = v.match(/\d+/)[0];
        id = id - 1 == 0 ? vertexList.length : id - 1;
        return `V${id}`;
    }
    arcList.map(arc => {
        arc.start = shiftV(arc.start, vertexList);
        arc.end = shiftV(arc.end, vertexList);
    });

    return { path: [{ 'vertex': vertexList, 'arcs': [...arcList] }, ...remainingPath] };
}

/**
 * Calculates vertices for Main Road with single arm (current implementation)
 * @param {number} xHeight - X-height value
 * @param {Array}   - List of routes
 * @return {Object} Vertex list object
 */
function calcMainRoadVerticesSingleArm(xHeight, routeList, innerCornerRadius = null, outerCornerRadius = null) {
    const length = xHeight / 4;
    let RootBottom = routeList.filter(item => item.angle === 180)[0];
    let RootTop = routeList.filter(item => item.angle !== 180)[0];

    // Use common function to calculate pivot points
    const { leftPivot, rightPivot, topVertex: RootTopVertex } = calculatePivotPoints(RootTop, RootBottom, length, innerCornerRadius, outerCornerRadius);

    let RootBottomVertex = (0,_routeBase_js__WEBPACK_IMPORTED_MODULE_5__.getSideRoadCoords)(RootBottom, length);

    const vertexList = [...RootTopVertex.path[0].vertex, rightPivot, ...RootBottomVertex.path[0].vertex, leftPivot];
    const arcList = RootTopVertex.path[0].arcs;

    const remainingPath = [];
    if (RootTopVertex.path.length > 1) {
        remainingPath.push(...RootTopVertex.path.slice(-RootTopVertex.path.length + 1));
    }

    return processVertexListAndArcs(vertexList, arcList, remainingPath);
}

/**
 * Calculates vertices for Main Road with multiple arms
 * @param {number} xHeight - X-height value
 * @param {Array} routeList - List of routes
 * @return {Object} Vertex list object
 */
function calcMainRoadVerticesMultipleArms(xHeight, routeList, innerCornerRadius = null, outerCornerRadius = null) {
    const length = xHeight / 4;
    let RootBottom = routeList.filter(item => item.angle === 180)[0];
    let RootTops = routeList.filter(item => item.angle !== 180);

    // For multiple arms, we need to calculate vertices for each arm and combine them
    let allVertices = [];
    let allArcs = [];
    let lastRightPivot = null;

    // Process each arm using the common pivot calculation function
    RootTops.forEach((RootTop, index) => {
        const { leftPivot, midPivot, rightPivot, topVertex: RootTopVertex } = calculatePivotPoints(RootTop, RootBottom, length, innerCornerRadius, outerCornerRadius);

        // Add vertices for this arm
        allVertices.push(...RootTopVertex.path[0].vertex);
        if (index === RootTops.length - 1) {
            // Only add right pivot after the last arm
            allVertices.push(rightPivot);
            lastRightPivot = rightPivot;
        } else {
            // Add mid pivot for all but the last arm
            allVertices.push(midPivot);
        }

        // Collect arcs from this arm
        allArcs.push(...RootTopVertex.path[0].arcs);
    });

    // Add bottom vertices
    let RootBottomVertex = (0,_routeBase_js__WEBPACK_IMPORTED_MODULE_5__.getSideRoadCoords)(RootBottom, length);
    allVertices.push(...RootBottomVertex.path[0].vertex);

    // Add left pivot at the end (calculate based on first arm)
    if (RootTops.length > 0) {
        const { leftPivot } = calculatePivotPoints(RootTops[0], RootBottom, length, innerCornerRadius, outerCornerRadius);
        allVertices.push(leftPivot);
    }

    return processVertexListAndArcs(allVertices, allArcs);
}

/**
 * Calculates vertices for Main Road
 * @param {number} xHeight - X-height value
 * @param {Array} routeList - List of routes
 * @return {Object} Vertex list object
 */
function calcMainRoadVertices(xHeight, routeList, innerCornerRadius = null, outerCornerRadius = null) {
    const topRoutes = routeList.filter(item => item.angle !== 180);

    if (topRoutes.length === 1) {
        // Single arm - use existing implementation
        return calcMainRoadVerticesSingleArm(xHeight, routeList, innerCornerRadius, outerCornerRadius);
    } else {
        // Multiple arms - use new implementation
        return calcMainRoadVerticesMultipleArms(xHeight, routeList, innerCornerRadius, outerCornerRadius);
    }
}

function generateOutlineFromCenterline(centerLine, centerArc) {
    const rightPath = [];
    const leftPath = [];
    const newArcs = [];

    // Helper to find arc
    const findArc = (startLabel, endLabel) => {
        if (!centerArc) return null;
        return centerArc.find(a => a.start === startLabel && a.end === endLabel);
    };

    for (let i = 0; i < centerLine.length - 1; i++) {
        const curr = centerLine[i];
        const next = centerLine[i + 1];
        const width = curr.width || 4; // Default width
        const offset = width / 2;

        const arc = findArc(curr.label, next.label);

        if (arc) {
            // Handle Arc
            const d2 = Math.pow(next.x - curr.x, 2) + Math.pow(next.y - curr.y, 2);
            const det = Math.sqrt(Math.max(0, arc.radius * arc.radius / d2 - 0.25));

            const dx = next.x - curr.x;
            const dy = next.y - curr.y;

            let hx = (curr.x + next.x) / 2;
            let hy = (curr.y + next.y) / 2;

            let cx, cy;

            if (arc.direction === 1) { // CW
                cx = hx + dy * det;
                cy = hy - dx * det;
            } else { // CCW
                cx = hx - dy * det;
                cy = hy + dx * det;
            }

            const getOffsetPoint = (p, c, r_new) => {
                const v_x = p.x - c.x;
                const v_y = p.y - c.y;
                const len = Math.sqrt(v_x * v_x + v_y * v_y);
                return {
                    x: c.x + (v_x / len) * r_new,
                    y: c.y + (v_y / len) * r_new
                };
            };

            let r_right, r_left;
            if (arc.direction === 1) {
                r_right = arc.radius - offset;
                r_left = arc.radius + offset;
            } else {
                r_right = arc.radius + offset;
                r_left = arc.radius - offset;
            }

            const currRight = getOffsetPoint(curr, { x: cx, y: cy }, r_right);
            const nextRight = getOffsetPoint(next, { x: cx, y: cy }, r_right);
            const currLeft = getOffsetPoint(curr, { x: cx, y: cy }, r_left);
            const nextLeft = getOffsetPoint(next, { x: cx, y: cy }, r_left);

            if (i === 0) {
                rightPath.push({ ...currRight, label: `R${i}` });
                leftPath.push({ ...currLeft, label: `L${i}` });
            }
            rightPath.push({ ...nextRight, label: `R${i + 1}` });
            leftPath.push({ ...nextLeft, label: `L${i + 1}` });

            newArcs.push({
                start: `R${i}`,
                end: `R${i + 1}`,
                radius: r_right,
                direction: arc.direction,
                sweep: arc.sweep
            });

            newArcs.push({
                start: `L${i + 1}`,
                end: `L${i}`,
                radius: r_left,
                direction: 1 - arc.direction,
                sweep: arc.sweep
            });

        } else {
            // Handle Line
            const dx = next.x - curr.x;
            const dy = next.y - curr.y;
            const len = Math.sqrt(dx * dx + dy * dy);
            const nx = -dy / len;
            const ny = dx / len;

            const currRight = { x: curr.x + nx * offset, y: curr.y + ny * offset };
            const nextRight = { x: next.x + nx * offset, y: next.y + ny * offset };
            const currLeft = { x: curr.x - nx * offset, y: curr.y - ny * offset };
            const nextLeft = { x: next.x - nx * offset, y: next.y - ny * offset };

            if (i === 0) {
                rightPath.push({ ...currRight, label: `R${i}` });
                leftPath.push({ ...currLeft, label: `L${i}` });
            }
            rightPath.push({ ...nextRight, label: `R${i + 1}` });
            leftPath.push({ ...nextLeft, label: `L${i + 1}` });
        }
    }

    const vertexList = [...rightPath];
    for (let i = leftPath.length - 1; i >= 0; i--) {
        vertexList.push(leftPath[i]);
    }

    (0,_routeBase_js__WEBPACK_IMPORTED_MODULE_5__.assignVertexLabel)(vertexList);

    const labelMap = {};
    rightPath.forEach((p, idx) => labelMap[`R${idx}`] = vertexList[idx].label);
    leftPath.forEach((p, idx) => labelMap[`L${idx}`] = vertexList[rightPath.length + (leftPath.length - 1 - idx)].label);

    newArcs.forEach(a => {
        a.start = labelMap[a.start];
        a.end = labelMap[a.end];
    });

    return { vertex: vertexList, arcs: newArcs };
}

/**
 * Calculates vertices for a conventional roundabout
 * @param {number} xHeight - X-height value
 * @param {Array} routeList - List of routes with the center point
 * @return {Object} Vertex list object for roundabout
 */
function calcRoundaboutVertices(type, xHeight, routeList) {
    const length = xHeight / 4
    const center = routeList[1] // use tip location
    const templateName = routeList[0].shape + ' ' + type
    let roundel = (0,_template_js__WEBPACK_IMPORTED_MODULE_2__.roundelTemplate)(templateName, routeList[1].length)

    if (templateName.includes('Double')) {
        // For double roundabout, we need to rotate the inner roundabout by the specified angle
        const angle = routeList[0].angle || 0;
        const bottomCenter = { x: 0, y: 0 }
        const topCenter = { x: routeList[0].length * Math.sin(angle * Math.PI / 180), y: - routeList[0].length * Math.cos(angle * Math.PI / 180) }

        function rotatePoint(x, y, angleDeg, center) {
            const angleRad = angleDeg * Math.PI / 180;
            const cos = Math.cos(angleRad);
            const sin = Math.sin(angleRad);

            // Translate point to origin
            const translatedX = x - center.x;
            const translatedY = y - center.y;

            // Rotate 
            const newX = translatedX * cos - translatedY * sin;
            const newY = translatedX * sin + translatedY * cos;

            // Translate back
            const finalX = newX + center.x;
            const finalY = newY + center.y;

            return { x: finalX, y: finalY };
        }

        const excludeTopNotchPoints = ['V3', 'V4', 'V5', 'V6'];
        const excludeBottomNotchPoints = ['V10', 'V11', 'V12', 'V13'];

        roundel.path.map((p) => {
            if (p.vertex) {
                p.vertex.map((v) => {
                    let rotated = rotatePoint(v.x, v.y, angle, bottomCenter);
                    v.x = parseFloat(rotated.x.toFixed(4));
                    v.y = parseFloat(rotated.y.toFixed(4));
                });
            }
        });

        // rotate back the notch points to original position by its center
        // rotate more angle when the roundabout angle is negative
        let angleAdjustment = angle == -30 ? 20 : (angle == -60 ? 50 : (angle == -90 ? 80 : 0));
        roundel.path.map((p) => {
            if (p.vertex) {
                p.vertex.map((v) => {
                    if (excludeTopNotchPoints.includes(v.label)) {
                        let rotated = rotatePoint(v.x, v.y, -angle - angleAdjustment, topCenter);
                        v.x = parseFloat(rotated.x.toFixed(4));
                        v.y = parseFloat(rotated.y.toFixed(4));
                    }
                    if (excludeBottomNotchPoints.includes(v.label)) {
                        let rotated = rotatePoint(v.x, v.y, -angle, bottomCenter);
                        v.x = parseFloat(rotated.x.toFixed(4));
                        v.y = parseFloat(rotated.y.toFixed(4));
                    }
                }
                );
            }
        });

        // need to adjust the arc from V13 to V22 when angle is negative
        if (routeList[0].shape == 'Conventional') {
            roundel.path.map((p) => {
                if (p.arcs) {
                    p.arcs.map((a) => {
                        if (a.start === 'V13' && a.end === 'V22' && angle < 0) {
                            a.sweep = 0;
                        }
                        if (a.start === 'V21' && a.end === 'V10' && angle == -90) {
                            a.sweep = 1;
                        }
                    });
                }
            });
        }
        if (routeList[0].shape == 'Spiral') {
            roundel.path.map((p) => {
                if (p.arcs) {
                    p.arcs.map((a) => {
                        if (a.start === 'V13' && a.end === 'V14' && angle > 0) {
                            a.sweep = 1;
                        }
                        if (a.start === 'V8' && a.end === 'V10' && angle == -90) {
                            a.sweep = 1;
                        }
                    });
                }
            });
        }
    }

        roundel = (0,_symbols_js__WEBPACK_IMPORTED_MODULE_3__.calcSymbol)(roundel, length)
        roundel.path.map((p) => {
            let transformed = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.calculateTransformedPoints)(p.vertex, {
                x: center.x,
                y: center.y,
                angle: 0
            });
            p.vertex = transformed

            if (p.centerLine) {
                let transformedCenterLine = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.calculateTransformedPoints)(p.centerLine, {
                    x: center.x,
                    y: center.y,
                    angle: 0
                });
                p.centerLine = transformedCenterLine;

                if (type === 'Conventional') {
                    const outline = generateOutlineFromCenterline(p.centerLine, p.centerArc);
                    p.vertex = outline.vertex;
                    p.arcs = outline.arcs;
                }
            }
        });

        return roundel;
    }

    function addUTurnToMainRoad(mainRoad) {
        const center = mainRoad.routeList[1] // use tip location

        // Create options for the arm
        const options = {
            mainRoad: mainRoad,
            color: mainRoad.color,
            x: center.x,
            y: center.y,
            isBase: true,
            routeList: [{
                x: 6 + center.x, y: 33.4 + (mainRoad.roadType == 'Spiral Roundabout' ? 2 : 0) + center.y,
                angle: 0,
                shape: 'UArrow ' + mainRoad.roadType.split(' ')[0],
                width: 4
            }],
            xHeight: mainRoad.xHeight,
        };

        // Declare variables outside the if-else blocks 
        let routeList = options.routeList;
        let arrow = JSON.parse(JSON.stringify(_template_js__WEBPACK_IMPORTED_MODULE_2__.roadMapTemplate[routeList[0].shape]))
        arrow = (0,_symbols_js__WEBPACK_IMPORTED_MODULE_3__.calcSymbol)(arrow, mainRoad.xHeight / 4)

        // Create and initialize the side road
        const sideRoad = new _sideRoute_js__WEBPACK_IMPORTED_MODULE_6__.SideRoadSymbol(options);
        //sideRoad.initialize(arrow);


        // Update main road to show how it would look with the new side road
        //mainRoad.receiveNewRoute(tempVertexList);
        //mainRoad.setCoords();
        mainRoad.sideRoad.push(sideRoad);
        sideRoad.mainRoad = mainRoad;
    }

    function addBaseToRoundabout(mainRoad) {
        if (mainRoad.isLoading) return;

        const shapeName = 'Base ' + mainRoad.roadType.split(' ')[0] + ' ' + mainRoad.RAfeature; 

        const isBaseExists = mainRoad.sideRoad.some(side => 
            side.routeList[0].shape === shapeName
        );
        
        if (isBaseExists) return;

        const center = mainRoad.routeList[1];
        const length = mainRoad.xHeight / 4;
        const rootLengthPixels = mainRoad.rootLength * length;

        const options = {
            mainRoad: mainRoad,
            color: mainRoad.color,
            x: center.x,
            y: center.y,
            isBase: true,
            routeList: [{
                x: center.x, 
                y: center.y + rootLengthPixels,
                angle: 0,
                shape: 'Base ' + mainRoad.roadType.split(' ')[0] + ' ' + mainRoad.RAfeature,
                width: 6,
            }],
            xHeight: mainRoad.xHeight,
        };

        const sideRoad = new _sideRoute_js__WEBPACK_IMPORTED_MODULE_6__.SideRoadSymbol(options);

        //sideRoad.onMove(null, false);
        (0,_anchor_js__WEBPACK_IMPORTED_MODULE_7__.anchorShape)(mainRoad, sideRoad, { vertexIndex1: 'V1', vertexIndex2: 'C1', spacingX: 0, spacingY: rootLengthPixels});
    }

    /**
     * MainRoute class extends baseGroup to create route objects
     */
    class MainRoadSymbol extends _draw_js__WEBPACK_IMPORTED_MODULE_0__.BaseGroup {
        constructor(options = {}) {
            // We need to pass null as basePolygon initially, as we'll set it after initialize()
            super(null, 'MainRoad', 'MainRoadSymbol', options);

            // Initialize route-specific properties
            this.routeList = options.routeList || [];
            this.routeCenter = options.routeCenter || [];
            this.xHeight = options.xHeight || 100;
            this.rootLength = options.rootLength ?? 7;
            this.tipLength = options.tipLength ?? 12;
            this.routeWidth = options.routeWidth ?? 6;
            this.color = options.color || 'white';
            this.roadType = options.roadType || 'Main Line';
            this.sideRoad = [];
            this.mainAngle = options.mainAngle || 0;
            this.RAfeature = options.RAfeature || 'Normal';
            this.innerCornerRadius = options.innerCornerRadius || null;
            this.outerCornerRadius = options.outerCornerRadius || null;
            this.isLoading = options.isLoading || false;

            this.initialize();

            // Bind events
            this.on('selected', roadMapOnSelect);
            this.on('deselected', roadMapOnDeselect);
            this.on('moving', this.onMove.bind(this));
            this.on('moved', this.onMove.bind(this));
            this.on('modified', this.onMove.bind(this));
        }

    upgradeBaseRoute() {
        const wasLoading = this.isLoading;
        this.isLoading = false;

        this.RAfeature = this.routeList[0].shape || 'Normal';
        
        // 1. Remap existing anchors (arms) to new vertex labels
        (0,_version_upgrades_v1_3_1_roundabout_js__WEBPACK_IMPORTED_MODULE_8__.remapAnchors)(this);

        // 2. Add the base route (entry)
        addBaseToRoundabout(this);
        
        this.isLoading = wasLoading;
    }

    /**
     * Initialize the route with a GlyphPath
     * @param {Object} vertexList - Vertex data for the route
     * @return {MainRoadSymbol} - The initialized route
     */
        initialize() {
            const vertexList = calcVertexType[this.roadType](this.xHeight, this.routeList, this.innerCornerRadius, this.outerCornerRadius)
            const arrow = new _draw_js__WEBPACK_IMPORTED_MODULE_0__.GlyphPath();
            arrow.initialize(vertexList, {
                left: 0,
                top: 0,
                angle: 0,
                fill: this.color,
                objectCaching: false,
                dirty: true,
                strokeWidth: 0
            });

            // Set the basePolygon that was initially null in the constructor
            this.setBasePolygon(arrow, false)

            if (vertexList.path) {
                vertexList.path.forEach(p => {
                    if (p.centerLine) {
                        const centerLineData = {
                            vertex: p.centerLine,
                            arcs: p.centerArc
                        };

                        const centerLineCommands = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.convertVertexToPathCommands)(centerLineData, false);

                        const centerLinePath = new fabric.Path(centerLineCommands, {
                            fill: '',
                            stroke: 'red',
                            strokeWidth: 2,
                            strokeDashArray: [5, 5],
                            objectCaching: false,
                            originX: 'left',
                            originY: 'top'
                        });

                        this.add(centerLinePath);
                    }
                });
            }

            // Add special features based on RAfeature
            if (this.RAfeature === 'U-turn') {
                addUTurnToMainRoad(this);
            }
            if (this.roadType.includes('Roundabout')) {
                addBaseToRoundabout(this);
            }

            this.isLoading = false;

            return this;
        }


        /**
         * Updates base route object when receiving new route additions
         * @param {Object} branchRouteList - Optional route object to add (legacy)
         * @return {void}
         */
        receiveNewRoute(newSideRoad = null) {
            if (this.roadType !== 'Main Line') {
                if (newSideRoad) {
                    this.sideRoad.push(newSideRoad);
                }
                return;
            }
            const topList = this.routeList.filter(route => route.angle !== 180)[0];
            const bottomList = this.routeList.filter(route => route.angle === 180)[0];
            const length = this.xHeight / 4;

            if (newSideRoad) {
                this.sideRoad.push(newSideRoad);
            }

            // Use the extracted function to calculate the proper bottom Y coordinate
            let newBottom = calculateMainRoadBottomY(topList, bottomList, length);

            // Also consider side roads when calculating bottom position
            this.sideRoad.forEach(side => {
                const sideBottom = side.side ? side.basePolygon.vertex[5].y : side.basePolygon.vertex[2].y;
                if (sideBottom + bottomList.length * length > newBottom) {
                    newBottom = sideBottom + bottomList.length * length;
                }
            });

            this.routeList.forEach(route => {
                if (route.angle === 180) {
                    route.y = newBottom;
                }
            });
            let newVertexList = calcVertexType[this.roadType](this.xHeight, this.routeList, this.innerCornerRadius, this.outerCornerRadius);
            const newPolygon = new _draw_js__WEBPACK_IMPORTED_MODULE_0__.GlyphPath();
            newPolygon.initialize(newVertexList, {
                left: 0,
                top: 0,
                angle: 0,
                fill: this.color,
                objectCaching: false,
                dirty: true,
                strokeWidth: 0
            });

            this.replaceBasePolygon(newPolygon, false);
            this.setCoords();
            //this.drawVertex(false);
            _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_4__.CanvasGlobals.scheduleRender()
        }

        /**
         * Updates route positions when root is moved
         * @param {Event} event - Move event
         * @return {void}
         */
        onMove(event) {
            this.sideRoad.forEach((side, index) => {
                side.onMove(null, true);
            });
            this.receiveNewRoute();
            this.setCoords();
        }
    }

    /**
     * Handles route object selection
     * @param {Event} event - Selection event
     * @return {void}
     */
    function roadMapOnSelect(event) {
        const panel = document.getElementById("button-DrawMap");
        const parent = document.getElementById("input-form");
        const existingRoute = canvas.getActiveObjects()
        if (panel && parent && existingRoute.length == 1 && existingRoute[0].functionalType === 'MainRoad') {
            //GeneralHandler.createButton('button-addRoute', '+ Another Route Destination', parent, 'input', FormDrawMapComponent.addRouteInput, 'click')
        }

        // Enable side road button and make it active
        toggleButtonState('button-addRoute', true);
    }

    /**
     * Handles route object deselection
     * @param {Event} event - Deselection event
     * @return {void}
     */
    function roadMapOnDeselect(event) {
        const panel = document.getElementById("button-addRoute");
        if (panel) {
            //panel.parentNode.parentNode.removeChild(panel.parentNode)
        }
        //canvas.off('mouse:move', drawBranchRouteOnCursor)
        //existingRoute.routeList = existingRoute.tempRootList || existingRoute.routeList

        // Disable side road button
        toggleButtonState('button-addRoute', false);
    }

    /**
     * Toggles the state of a button to either active or deactive
     * @param {string} buttonId - The ID of the button to toggle
     * @param {boolean} isActive - Whether to make the button active (true) or deactive (false)
     */
    function toggleButtonState(buttonId, isActive) {
        const button = document.getElementById(buttonId);
        if (!button) return;

        if (isActive) {
            button.classList.remove('deactive');
            button.disabled = false;
        } else {
            button.classList.add('deactive');
            button.disabled = true;
        }
    }

    


/***/ }),

/***/ "./js/objects/path.js":
/*!****************************!*\
  !*** ./js/objects/path.js ***!
  \****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   assignVertexLabel: () => (/* binding */ assignVertexLabel),
/* harmony export */   calculateAngle: () => (/* binding */ calculateAngle),
/* harmony export */   calculateArcCenter: () => (/* binding */ calculateArcCenter),
/* harmony export */   calculateBoundingBox: () => (/* binding */ calculateBoundingBox),
/* harmony export */   calculateTangentPoint: () => (/* binding */ calculateTangentPoint),
/* harmony export */   calculateTransformedPoints: () => (/* binding */ calculateTransformedPoints),
/* harmony export */   combinePaths: () => (/* binding */ combinePaths),
/* harmony export */   convertFontPathToFabricPath: () => (/* binding */ convertFontPathToFabricPath),
/* harmony export */   convertVertexToPathCommands: () => (/* binding */ convertVertexToPathCommands),
/* harmony export */   drawSegment: () => (/* binding */ drawSegment),
/* harmony export */   ensureOpenTypePatched: () => (/* binding */ ensureOpenTypePatched),
/* harmony export */   getArcDirection: () => (/* binding */ getArcDirection),
/* harmony export */   getFontPath: () => (/* binding */ getFontPath),
/* harmony export */   getInsertOffset: () => (/* binding */ getInsertOffset),
/* harmony export */   intersectLines: () => (/* binding */ intersectLines),
/* harmony export */   offsetPoint: () => (/* binding */ offsetPoint),
/* harmony export */   parseFont: () => (/* binding */ parseFont),
/* harmony export */   parsedFontChinese: () => (/* binding */ parsedFontChinese),
/* harmony export */   parsedFontChocolate: () => (/* binding */ parsedFontChocolate),
/* harmony export */   parsedFontHK: () => (/* binding */ parsedFontHK),
/* harmony export */   parsedFontHeavy: () => (/* binding */ parsedFontHeavy),
/* harmony export */   parsedFontKai: () => (/* binding */ parsedFontKai),
/* harmony export */   parsedFontKorean: () => (/* binding */ parsedFontKorean),
/* harmony export */   parsedFontMedium: () => (/* binding */ parsedFontMedium),
/* harmony export */   parsedFontSans: () => (/* binding */ parsedFontSans),
/* harmony export */   vertexToPath: () => (/* binding */ vertexToPath)
/* harmony export */ });
/* harmony import */ var _modal_md_font_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../modal/md-font.js */ "./js/modal/md-font.js");
/**
 * Path.js - Contains all vertex and path related functions for traffic sign drawing
 * This file centralizes path manipulation logic that was previously scattered across
 * draw.js and symbols.js files.
 */



// Store parsed fonts (assuming opentype.js objects)
let parsedFontMedium = null;
let parsedFontHeavy = null;
let parsedFontKorean = null; // Korean font has closer appearance to the true sign fonts
let parsedFontChinese = null; // Chinese font for supplement for special characters
let parsedFontHK = null; // Hong Kong font
let parsedFontChocolate = null; // Chocolate font
let parsedFontKai = null;
let parsedFontSans = null; // Sans serif fallback for punctuation characters
let fontParsingPromise = null; // To store the promise
let opentypePatched = false; // Track whether we've patched opentype.js rounding

// Safe rounding function to replace/override opentype.js roundDecimal behavior
// Fixes import/rounding issues (see opentype.js PR #797) and avoids -0 results
function safeRoundDecimal(value, decimals = 3) {
  if (!Number.isFinite(value)) return 0;
  const factor = Math.pow(10, decimals);
  // Add tiny epsilon to mitigate floating point artifacts
  const n = Math.round((value + Number.EPSILON) * factor) / factor;
  // Normalize negative zero to zero for cleaner SVG output
  return Object.is(n, -0) ? 0 : n;
}

// Patch opentype.js to ensure it uses our safe rounder. If the library exposes
// roundDecimal, override it; otherwise, override Path.prototype.toPathData to
// build the SVG path string using our own rounding. This is done lazily when
// opentype is available in the global scope.
function ensureOpenTypePatched() {
  if (opentypePatched) return;
  if (typeof opentype === 'undefined') return; // Not yet loaded

  try {
    // If roundDecimal is exposed, override it directly
    if (opentype && typeof opentype.roundDecimal === 'function') {
      opentype.roundDecimal = safeRoundDecimal;
    }

    // Always ensure Path.prototype.toPathData uses our rounding logic to be safe
    if (opentype.Path && opentype.Path.prototype && typeof opentype.Path.prototype.toPathData === 'function') {
      const proto = opentype.Path.prototype;

      if (!proto._toPathDataOriginal) {
        // Keep original around for debugging if needed
        proto._toPathDataOriginal = proto.toPathData;
      }

      // Re-implement toPathData to avoid relying on internal roundDecimal imports
      proto.toPathData = function toPathData(options = {}) {
        const decimals = (options.decimals ?? options.precision ?? 3);
        const flipY = !!options.flipY; // default false in our usage
        const r = (v) => safeRoundDecimal(v, decimals);

        let d = '';
        for (const cmd of this.commands || []) {
          switch (cmd.type) {
            case 'M': {
              const x = r(cmd.x);
              const y = r(flipY ? -cmd.y : cmd.y);
              d += `M ${x} ${y} `;
              break;
            }
            case 'L': {
              const x = r(cmd.x);
              const y = r(flipY ? -cmd.y : cmd.y);
              d += `L ${x} ${y} `;
              break;
            }
            case 'C': {
              const x1 = r(cmd.x1);
              const y1 = r(flipY ? -cmd.y1 : cmd.y1);
              const x2 = r(cmd.x2);
              const y2 = r(flipY ? -cmd.y2 : cmd.y2);
              const x = r(cmd.x);
              const y = r(flipY ? -cmd.y : cmd.y);
              d += `C ${x1} ${y1} ${x2} ${y2} ${x} ${y} `;
              break;
            }
            case 'Q': {
              const x1 = r(cmd.x1);
              const y1 = r(flipY ? -cmd.y1 : cmd.y1);
              const x = r(cmd.x);
              const y = r(flipY ? -cmd.y : cmd.y);
              d += `Q ${x1} ${y1} ${x} ${y} `;
              break;
            }
            case 'Z':
              d += 'Z ';
              break;
            default:
              // Ignore unsupported command types to be resilient across versions
              break;
          }
        }
        return d.trim();
      };
    }

    opentypePatched = true;
    console.log('opentype.js rounding patched (roundDecimal/toPathData)');
  } catch (e) {
    console.warn('Failed to patch opentype.js rounding; continuing without patch.', e);
  }
}


/**
 * Calculate transformed points based on position and rotation
 * @param {Array|Object} pointsList - Array of points or object with path property
 * @param {Object} options - Transform options with x, y, and angle
 * @returns {Array} - Transformed points
 */
function calculateTransformedPoints(pointsList, options) {
  const { x, y, angle } = options;
  const radians = angle * (Math.PI / 180); // Convert angle to radians
  const points = pointsList.path ? pointsList.path[0].vertex : pointsList;

  return points.map(point => {
    // Translate point to origin
    const translatedX = point.x;
    const translatedY = point.y;

    // Apply rotation
    const rotatedX = translatedX * Math.cos(radians) - translatedY * Math.sin(radians);
    const rotatedY = translatedX * Math.sin(radians) + translatedY * Math.cos(radians);

    // Translate point back to the specified position
    return {
      ...point,
      x: rotatedX + x,
      y: rotatedY + y,
      label: point.label
    };
  });
}

/**
 * Calculate the angle between three points
 * @param {Object} p1 - First point with x, y coordinates
 * @param {Object} p2 - Center point with x, y coordinates
 * @param {Object} p3 - Third point with x, y coordinates
 * @returns {number} - Angle in radians
 */
function calculateAngle(prev, current, next) {
  const v1 = { x: current.x - prev.x, y: current.y - prev.y };
  const v2 = { x: next.x - current.x, y: next.y - current.y };
  const dotProduct = v1.x * v2.x + v1.y * v2.y;
  const magnitude1 = Math.sqrt(v1.x * v1.x + v1.y * v1.y);
  const magnitude2 = Math.sqrt(v2.x * v2.x + v2.y * v2.y);
  return Math.acos(dotProduct / (magnitude1 * magnitude2));
}

/**
 * Calculate tangent point for arc
 * @param {Object} p1 - Reference point with x, y coordinates
 * @param {Object} p2 - Center point with x, y coordinates
 * @param {number} offsetDistance - Distance to offset
 * @returns {Object} - Tangent point coordinates
 */
function calculateTangentPoint(point, center, offsetDistance) {
  const angle = Math.atan2(point.y - center.y, point.x - center.x);
  const offsetX = Math.round(offsetDistance * Math.cos(angle) * 100) / 100;
  const offsetY = Math.round(offsetDistance * Math.sin(angle) * 100) / 100;
  return {
    x: center.x + offsetX,
    y: center.y + offsetY
  };
}

/**
 * Calculate bounding box for a set of vertices
 * @param {Array} vertices - Array of vertex points
 * @return {Object} - Bounding box {left, top, width, height}
 */
function calculateBoundingBox(vertices) {
  if (!vertices || vertices.length === 0) {
    return { left: 0, top: 0, width: 0, height: 0 };
  }

  let minX = Infinity;
  let minY = Infinity;
  let maxX = -Infinity;
  let maxY = -Infinity;

  // Find min and max coordinates
  vertices.forEach(vertex => {
    minX = Math.min(minX, vertex.x);
    minY = Math.min(minY, vertex.y);
    maxX = Math.max(maxX, vertex.x);
    maxY = Math.max(maxY, vertex.y);
  });

  return {
    left: minX,
    top: minY,
    width: maxX - minX,
    height: maxY - minY
  };
}

/**
 * Determine the arc direction (clockwise or counterclockwise)
 * @param {Object} p1 - First point with x, y coordinates
 * @param {Object} p2 - Center point with x, y coordinates
 * @param {Object} p3 - Third point with x, y coordinates
 * @returns {number} - 1 for clockwise, 0 for counterclockwise
 */
function getArcDirection(p1, p2, p3) {
  // Cross product to determine clockwise/counterclockwise direction
  const crossProduct = (p2.x - p1.x) * (p3.y - p2.y) - (p2.y - p1.y) * (p3.x - p2.x);
  return crossProduct < 0 ? 1 : 0;
}

/**
 * Calculate the arc center by offsetting both edges by the radius
 * @param {Object} prev - Previous point
 * @param {Object} current - Current point (corner)
 * @param {Object} next - Next point
 * @param {number} radius - Arc radius
 * @returns {Object} - Arc center coordinates
 */
function calculateArcCenter(prev, current, next, radius) {
  // Calculate the perpendicular vectors
  const v1 = offsetPoint(prev, current, radius);
  const v2 = offsetPoint(next, current, radius);

  // Calculate the intersection
  return intersectLines(
    v1.p1.x, v1.p1.y, v1.p2.x, v1.p2.y,
    v2.p1.x, v2.p1.y, v2.p2.x, v2.p2.y
  );
}

/**
 * Offset a point by the radius
 * @param {Object} point - Point to offset
 * @param {Object} center - Center point
 * @param {number} radius - Offset radius
 * @returns {Object} - Object with p1 and p2 representing the offset line
 */
function offsetPoint(point, center, radius) {
  const dx = point.x - center.x;
  const dy = point.y - center.y;
  const distance = Math.sqrt(dx * dx + dy * dy);
  const unitX = dx / distance;
  const unitY = dy / distance;

  // Rotate 90 degrees for perpendicular
  const perpX = -unitY;
  const perpY = unitX;

  return {
    p1: {
      x: center.x + perpX * radius,
      y: center.y + perpY * radius
    },
    p2: {
      x: point.x + perpX * radius,
      y: point.y + perpY * radius
    }
  };
}

/**
 * Calculate the intersection point of two lines
 * @param {number} x1 - First line, first point x
 * @param {number} y1 - First line, first point y
 * @param {number} x2 - First line, second point x
 * @param {number} y2 - First line, second point y
 * @param {number} x3 - Second line, first point x
 * @param {number} y3 - Second line, first point y
 * @param {number} x4 - Second line, second point x
 * @param {number} y4 - Second line, second point y
 * @returns {Object} - Intersection point coordinates
 */
function intersectLines(x1, y1, x2, y2, x3, y3, x4, y4) {
  // Check if either of the lines are vertical
  const isLine1Vertical = Math.abs(x1 - x2) < 0.001;
  const isLine2Vertical = Math.abs(x3 - x4) < 0.001;

  // Handle vertical lines
  if (isLine1Vertical && isLine2Vertical) {
    return null; // Parallel vertical lines
  }

  if (isLine1Vertical) {
    const m2 = (y4 - y3) / (x4 - x3);
    const b2 = y3 - m2 * x3;
    return {
      x: x1,
      y: m2 * x1 + b2
    };
  }

  if (isLine2Vertical) {
    const m1 = (y2 - y1) / (x2 - x1);
    const b1 = y1 - m1 * x1;
    return {
      x: x3,
      y: m1 * x3 + b1
    };
  }

  // Calculate the slopes and y-intercepts
  const m1 = (y2 - y1) / (x2 - x1);
  const b1 = y1 - m1 * x1;

  const m2 = (y4 - y3) / (x4 - x3);
  const b2 = y3 - m2 * x3;

  // Check if the lines are parallel (same slope)
  if (Math.abs(m1 - m2) < 0.001) {
    return null; // Parallel lines
  }

  // Calculate the intersection point
  const x = (b2 - b1) / (m1 - m2);
  const y = m1 * x + b1;

  return { x, y };
}

/**
 * Draw a path segment between vertices, handling arcs and rounded corners
 * @param {Object} current - Current vertex
 * @param {Object} next - Next vertex
 * @param {Object} previous - Previous vertex
 * @param {Object} prevArc - Arc definition if it exists
 * @param {boolean} final - Whether this is the final segment
 * @returns {string} - SVG path string segment
 */
function drawSegment(current, next, previous, prevArc, final = false) {
  let pathString = '';
  if (current.radius) {
    // Calculate the exterior angle θ
    const angle = calculateAngle(previous, current, next);

    // Calculate the offset distance d = r × tan(θ/2)
    const offsetDistance = current.radius * Math.tan(angle / 2);

    // Calculate the tangent points for the arc
    const prevTangent = calculateTangentPoint(previous, current, offsetDistance);
    const nextTangent = calculateTangentPoint(next, current, offsetDistance);

    // Determine the arc direction (clockwise or counterclockwise)
    const arcDirection = getArcDirection(previous, current, next);

    // Line to the start of the arc
    pathString += ` ${current.start ? 'M' : 'L'} ${prevTangent.x} ${prevTangent.y}`;

    // Arc to the end of the arc
    pathString += ` A ${current.radius} ${current.radius} 0 0 ${1 - arcDirection} ${nextTangent.x} ${nextTangent.y}`;

  } else if (prevArc && (!current.start || final)) {
    pathString += ` A ${prevArc.radius} ${prevArc.radius2 ? prevArc.radius2 : prevArc.radius} 0 ${prevArc.sweep} ${prevArc.direction} ${current.x} ${current.y}`;
  } else {
    // Line to the next point
    pathString += ` ${current.start ? 'M' : 'L'} ${current.x} ${current.y}`;
  }
  return pathString;
}

/**
 * Convert vertex data to SVG path string
 * @param {Object} shapeMeta - Object with path and text data
 * @param {string} color - Fill color for paths
 * @returns {string} - Complete SVG path string
 */
function vertexToPath(shapeMeta, color) {
  let svgContent = '<svg>';
  let pathString = '';

  shapeMeta.path.forEach((path) => {
    let fillColor = path.fill || color || 'white';
    let pathStart = path.vertex[0];
    let pathNext = path.vertex[1];

    for (let i = 0; i < path.vertex.length; i++) {
      const current = path.vertex[i];
      const next = path.vertex[(i + 1) % path.vertex.length];
      const previous = path.vertex.at(i - 1);
      const prevArc = path.arcs && path.arcs.find(arc => (arc.end == current.label));

      pathString += drawSegment(current, next, previous, prevArc);

      // at end of path
      if (next.start == 1) {
        // Handle the last corner (which is also the first corner)
        const finalArc = path.arcs && path.arcs.find(arc => (arc.start == current.label));

        if (finalArc) {
          pathString += drawSegment(pathStart, pathNext, current, finalArc, true);
        }
        pathString += ' Z';
        pathStart = next;
        pathNext = path.vertex[(i + 2) % path.vertex.length];
      }
    }

    svgContent += `<path d="${pathString}" fill="${fillColor}" />`;
    pathString = '';
  });

  // Handle text objects in path
  if (shapeMeta.text && shapeMeta.text.length > 0) {
    shapeMeta.text.forEach(t => {
      let fillColor = t.fill || color || 'white';
      const origY = t.y;

      t.y = 0;
      let charPath = getFontPath(t);
      const minTop = Math.min(...charPath.commands.map(c => c.y));

      charPath.commands.forEach((command) => {
        command.y = command.y - origY - minTop;
        if (command.y1 !== undefined) {
          command.y1 = command.y1 - origY - minTop;
        }
      });

      const charSVG = charPath.toPathData({ flipY: false });
      svgContent += `<path d="${charSVG}" fill="${fillColor}" />`;
    });
  }

  svgContent += '</svg>';
  return svgContent;
}

/**
 * Get the insertion offset for a shape
 * @param {Object} shapeMeta - Object containing path data
 * @param {number} angle - Rotation angle in degrees
 * @returns {Object} - Left and top offset values
 */
function getInsertOffset(shapeMeta, angle = 0) {
  const vertexleft = Math.min(...shapeMeta.path.map(p => p.vertex).flat().map(v => v.x));
  const vertextop = Math.min(...shapeMeta.path.map(p => p.vertex).flat().map(v => v.y));
  return { left: vertexleft, top: vertextop };
}

/**
 * Convert vertex data directly to Fabric.js path commands
 * @param {Object} path - Path object with vertices and arcs
 * @param {boolean} isClosed - Whether to close the path (default: true)
 * @returns {string} - Fabric.js path command string
 */
function convertVertexToPathCommands(path, isClosed = true) {
  let pathCommands = '';
  const vertices = path.vertex || [];

  if (vertices.length === 0) return '';

  // Start with the first vertex
  let pathStart = vertices[0];
  pathCommands += `M ${pathStart.x} ${pathStart.y} `;

  // Process each vertex and its connections
  for (let i = 0; i < vertices.length; i++) {
    const current = vertices[i];
    const next = vertices[(i + 1) % vertices.length];
    const previous = i > 0 ? vertices[i - 1] : vertices[vertices.length - 1];

    // Find if there's an arc that ends at the current vertex
    const prevArc = path.arcs ? path.arcs.find(arc => arc.end === current.label) : null;

    // If this is a start point (other than the first vertex), move to it
    if (current.start && i > 0) {
      pathCommands += `M ${current.x} ${current.y} `;
      pathStart = current; // Update path start for next segments
    }
    // If there's a previous arc, draw an arc
    else if (prevArc && (!current.start || i === vertices.length - 1)) {
      // Find the vertices referenced by the arc
      const startVertex = vertices.find(v => v.label === prevArc.start);
      const endVertex = vertices.find(v => v.label === prevArc.end);

      if (startVertex && endVertex) {
        // Calculate arc parameters for fabric.Path
        const radius = prevArc.radius2 ? prevArc.radius2 : prevArc.radius;
        const largeArcFlag = prevArc.sweep ? 1 : 0;
        const sweepFlag = prevArc.direction ? 1 : 0;

        pathCommands += `A ${prevArc.radius} ${radius} 0 ${largeArcFlag} ${sweepFlag} ${current.x} ${current.y} `;
      } else {
        // Fallback to line if vertices not found
        pathCommands += `L ${current.x} ${current.y} `;
      }
    }
    // If this vertex has a radius property, create rounded corner
    else if (current.radius) {
      // Check if next starts a new curve, if yes trace back this path start
      let curveEnd = next;
      if (next.start && i < vertices.length - 1) {
        curveEnd = pathStart; // Use the path start for the arc end
      }
      // Calculate the angle between previous-current and current-next
      const angle = calculateAngle(previous, current, curveEnd);

      // Calculate offset distance based on radius and angle
      const offsetDistance = Math.abs(current.radius * Math.tan(angle / 2));

      // Calculate tangent points for the arc
      const prevTangent = calculateTangentPoint(previous, current, offsetDistance);
      const nextTangent = calculateTangentPoint(curveEnd, current, offsetDistance);

      // Determine arc direction (0 = counterclockwise, 1 = clockwise)
      const arcDirection = getArcDirection(previous, current, curveEnd);

      // Line to start of arc
      pathCommands += `L ${prevTangent.x} ${prevTangent.y} `;

      // Arc to end of arc
      pathCommands += `A ${current.radius} ${current.radius} 0 0 ${1 - arcDirection} ${nextTangent.x} ${nextTangent.y} `;
    }
    // Otherwise, draw a line to the next point
    else {
      pathCommands += `L ${current.x} ${current.y} `;
    }

    // If we've reached a vertex that starts a new subpath
    if (next.start) {
      // Find if there's an arc that starts at the current vertex
      const finalArc = path.arcs ? path.arcs.find(arc => arc.start === current.label) : null;

      if (finalArc) {
        // Similar arc calculation as above
        const endVertex = vertices.find(v => v.label === finalArc.end);
        if (endVertex && endVertex === pathStart) {
          const radius = finalArc.radius2 ? finalArc.radius2 : finalArc.radius;
          const largeArcFlag = finalArc.sweep ? 1 : 0;
          const sweepFlag = finalArc.direction ? 1 : 0;

          pathCommands += `A ${finalArc.radius} ${radius} 0 ${largeArcFlag} ${sweepFlag} ${pathStart.x} ${pathStart.y} `;
        }
      }

      // Close the path
      if (isClosed) pathCommands += 'Z ';

      // Reset path start for next subpath
      if (i < vertices.length - 1) {
        pathStart = next;
        pathCommands += `M ${pathStart.x} ${pathStart.y} `;
      }
    }
  }

  // Close the final subpath if needed
  if (isClosed && !pathCommands.endsWith('Z ')) {
    pathCommands += 'Z';
  }

  return pathCommands;
}

/**
 * Fetches and parses font files using opentype.js.
 * Returns a promise that resolves when all fonts are loaded and parsed.
 * Should be called once during application initialization.
 * @returns {Promise<void>} A promise that resolves when all fonts are fetched and parsed.
 */
function parseFont() {
  // If already parsing or parsed, return the existing promise
  if (fontParsingPromise) {
    return fontParsingPromise;
  }

  const loadFont = async (url, name, assignFn) => {
    const res = await fetch(url);
    if (!res.ok) throw new Error(`HTTP error! status: ${res.status}`);
    const buffer = await res.arrayBuffer();
    if (typeof opentype !== 'undefined') {
      const font = opentype.parse(buffer);
      assignFn(font);
    } else {
      throw new Error(`opentype.js not loaded. Cannot parse ${name} font.`);
    }
  };

  const assetBase = (typeof window !== 'undefined' && window.location && window.location.pathname.startsWith('/design'))
    ? '/design/'
    : '/';

  const localAsset = (relativePath) => `${assetBase}${relativePath.replace(/^\/+/, '')}`;

  // Critical fonts: TransportMedium, TransportHeavy, NotoSansHK
  const criticalFonts = [
    loadFont(localAsset('css/font/TransportMedium.woff'), 'TransportMedium', (f) => parsedFontMedium = f)
      .catch(e => { console.error("Error fetching/parsing TransportMedium:", e); throw e; }),
    loadFont(localAsset('css/font/TransportHeavy.woff'), 'TransportHeavy', (f) => parsedFontHeavy = f)
      .catch(e => { console.error("Error fetching/parsing TransportHeavy:", e); throw e; }),
    loadFont('https://fonts.gstatic.com/s/notosanskr/v36/PbyxFmXiEBPT4ITbgNA5Cgms3VYcOA-vvnIzztgyeLTq8H4hfeE.ttf', 'NotoSansKR-Medium', (f) => parsedFontKorean = f)
      .catch(e => { console.error("Error fetching/parsing NotoSansKR-Medium:", e); throw e; })
  ];

  // Deferred fonts loading function
  const loadDeferred = () => {
    const deferredTasks = [
      () => loadFont('https://fonts.gstatic.com/s/notosanstc/v38/-nFuOG829Oofr2wohFbTp9ifNAn722rq0MXz75Ky_CpOtma3uNQ.ttf', 'parsedFontChinese', (f) => {
        parsedFontChinese = f;
        window.dispatchEvent(new CustomEvent('fontLoaded', { detail: { fontName: 'parsedFontChinese' } }));
      }),
      () => loadFont('https://fonts.gstatic.com/s/chocolateclassicalsans/v14/nuFqD-PLTZX4XIgT-P2ToCDudWHHflqUpTpfjWdDPI2J9mHITw.ttf', 'parsedFontChocolate', (f) => {
        parsedFontChocolate = f;
        if (typeof window !== 'undefined') window.parsedFontChocolate = parsedFontChocolate;
        window.dispatchEvent(new CustomEvent('fontLoaded', { detail: { fontName: 'parsedFontChocolate' } }));
      }),
      () => loadFont('https://fonts.gstatic.com/s/notosanshk/v32/nKKF-GM_FYFRJvXzVXaAPe97P1KHynJFP716qEJ--oWTiYjNvVA.ttf', 'parsedFontHK', (f) => {
        parsedFontHK = f;
        if (typeof window !== 'undefined') window.parsedFontHK = parsedFontHK;
        window.dispatchEvent(new CustomEvent('fontLoaded', { detail: { fontName: 'parsedFontHK' } }));
      }),
      () => loadFont(localAsset('css/font/TW-MOE-Std-Kai-compact.ttf'), 'TW-MOE-Std-Kai-compact', (f) => {
         parsedFontKai = f
         window.dispatchEvent(new CustomEvent('fontLoaded', { detail: { fontName: 'TW-MOE-Std-Kai' } }));
      }),
      () => loadFont('https://fonts.gstatic.com/s/notosans/v39/o-0IIpQlx3QUlC5A4PNb4j5Ba_2c7A.ttf', 'parsedFontSans', (f) => {
         parsedFontSans = f
         window.dispatchEvent(new CustomEvent('fontLoaded', { detail: { fontName: 'parsedFontSans' } }));
      })
    ];

    console.log("Starting deferred font loading...");
    deferredTasks.forEach(task => {
      // We don't bubble up errors from deferred fonts to the main promise
      task().catch(e => console.warn("Deferred font failed to load:", e));
    });
  };

  // Start parsing and store the promise for critical fonts only
  fontParsingPromise = Promise.all(criticalFonts).then(() => {
    console.log("Critical fonts parsed successfully.");
    // Start deferred fonts loading after a short delay to ensure UI is responsive
    setTimeout(loadDeferred, 100);
  }).catch(error => {
    console.error("One or more critical fonts failed to load:", error);
    fontParsingPromise = null; // Reset promise if parsing failed
    throw error; // Re-throw so the caller knows about the failure
  });

  return fontParsingPromise;
}

/**
 * Gets the pre-parsed font path object for a given text character.
 * Assumes parseFont() has been called and resolved successfully beforehand.
 * Requires opentype.js for font parsing and path generation.
 * @param {Object} t - Text object containing character, position, fontSize, and fontFamily.
 * @returns {Object|null} The font path object (from opentype.js) or null if the font is not available or parsing failed.
 */
function getFontPath(t) {

  let font = null;
  // Select the appropriate pre-parsed font object
  switch (t.fontFamily) {
    case 'TransportMedium':
      font = parsedFontMedium;
      break;
    case 'TransportHeavy':
      font = parsedFontHeavy;
      break;
    case 'TW-MOE-Std-Kai':
      font = parsedFontKai;
      break;
    case 'parsedFontChinese': // Fallback to Chinese font for special characters
      font = parsedFontChinese;
      break;
    case 'parsedFontSans': // Fallback to Sans font for punctuation characters
      font = parsedFontSans;
      break;
    case 'parsedFontKorean': // Fallback to Korean font for special characters
      font = parsedFontKorean;
      break; 
    default: // Check for custom fonts in window
      const windowFont = window[t.fontFamily];
      // Validate that it's actually a font object with getPath method
      if (windowFont && typeof windowFont.getPath === 'function') {
        font = windowFont;
      } else {
        font = null; // Font not found or invalid
      }
      break;
  }
  // Check if the selected font is actually loaded/parsed
  if (!font) {
    // Handle missing font using FontPriorityManager
    const fallbackFont = _modal_md_font_js__WEBPACK_IMPORTED_MODULE_0__.FontPriorityManager.handleMissingFont(t.fontFamily);

    // Use the fallback font for this specific call
    switch (fallbackFont) {
      case 'TransportMedium':
        font = parsedFontMedium;
        break;
      case 'TransportHeavy':
        font = parsedFontHeavy;
        break;
      case 'TW-MOE-Std-Kai':
        font = parsedFontKai;
        break;
      case 'parsedFontChinese':
        font = parsedFontChinese;
        break;
      case 'parsedFontKorean':
        font = parsedFontKorean;
        break;
      case 'parsedFontSans':
        font = parsedFontSans;
        break;
      default:
        font = parsedFontKorean; // Final fallback
        break;
    }

    if (!font) {
      console.error(`Critical error: No fonts available. Make sure parseFont() was called and completed successfully.`);
      return null;
    }
  }
  // Check if opentype.js is available and the font object has getPath
  if (typeof opentype === 'undefined' || typeof font.getPath !== 'function') {
    console.error("opentype.js not loaded or font object is invalid."); return null;
  }  // Define font priority list for fallback
  let fontPriorityList = [];
  // Get user-configured font priority list
  const getUserFontPriority = () => {
    try {
      // Try to get from FontPriorityManager
      const priorityNames = _modal_md_font_js__WEBPACK_IMPORTED_MODULE_0__.FontPriorityManager.getFontPriorityList();
      return priorityNames.map(name => {
        switch (name) {
          case 'parsedFontChinese': return parsedFontChinese;
          case 'parsedFontKorean': return parsedFontKorean;
          case 'parsedFontSans': return parsedFontSans;
          default:
            // Handle custom fonts
            return window[name] || null;
        }
      }).filter(f => f !== null);
    } catch (e) {
      console.warn('Failed to load font priority from FontPriorityManager, falling back to localStorage');

      // Fallback to localStorage
      try {
        const stored = localStorage.getItem('fontPriorityList');
        if (stored) {
          const priorityNames = JSON.parse(stored);
          return priorityNames.map(name => {
            switch (name) {
              case 'parsedFontChinese': return parsedFontChinese;
              case 'parsedFontKorean': return parsedFontKorean;
              case 'parsedFontSans': return parsedFontSans;
              default:
                // Handle custom fonts
                return window[name] || null;
            }
          }).filter(f => f !== null);
        }
      } catch (storageError) {
        console.warn('Failed to load font priority from localStorage as well');
      }
    }
    // Default fallback - include the new sans font for punctuation
    return [parsedFontSans, parsedFontChinese, parsedFontKorean];
  };
  // Generate and return the path object using opentype.js getPath method
  try {
    // Helper function to check if character exists in font
    const hasCharacter = (font, character) => {
      if (!font) return false;

      // Method 1: Check using charToGlyph
      if (font.charToGlyph) {
        const glyph = font.charToGlyph(character);
        if (glyph && glyph.unicode !== undefined && glyph.unicode !== 0) {
          return true;
        }
      }

      // Method 2: Check using stringToGlyphs
      if (font.stringToGlyphs) {
        const glyphs = font.stringToGlyphs(character);
        if (glyphs && glyphs.length > 0 && glyphs[0].unicode !== undefined && glyphs[0].unicode !== 0) {
          return true;
        }
      }

      return false;
    };

    // First, check if the character exists in the current font
    if (hasCharacter(font, t.character)) {
      // Character exists in current font, use it
      const path = font.getPath(t.character, t.x, t.y, t.fontSize);
      path._resolvedFont = font;
      return path;
    } else {
      // Character not found in current font, try font priority list
      console.warn(`Character "${t.character}" not found in font ${t.fontFamily}, trying fallback fonts...`);

      const fallbackFonts = getUserFontPriority();
      for (const fallbackFont of fallbackFonts) {
        if (hasCharacter(fallbackFont, t.character)) {
          console.log(`Found character "${t.character}" in fallback font`);
          const path = fallbackFont.getPath(t.character, t.x, t.y, t.fontSize);
          path._resolvedFont = fallbackFont;
          return path;
        }
      }

      // If character not found in any font, try with the original font anyway (might render as missing character box)
      console.warn(`Character "${t.character}" not found in any available font, using original font`);
      const path = font.getPath(t.character, t.x, t.y, t.fontSize);
      path._resolvedFont = font;
      return path;
    }
  } catch (error) {
    console.error(`Error getting path for character "${t.character}" with font ${t.fontFamily}:`, error);
    return null; // Return null on error
  }
}

/**
 * Convert font path commands to Fabric.js path command string
 * @param {Array} commands - Array of font path commands
 * @param {Object} textElem - Text element with position data
 * @returns {string} - Fabric.js path command string
 */
function convertFontPathToFabricPath(commands, textElem) {
  let pathCommands = '';
  const offsetY = textElem.y;

  // Find minimum Y value for proper positioning
  const minY = Math.min(...commands
    .filter(cmd => cmd.y !== undefined)
    .map(cmd => cmd.y));

  commands.forEach(cmd => {
    switch (cmd.type) {
      case 'M':
        pathCommands += `M ${cmd.x} ${cmd.y - offsetY - minY} `;
        break;
      case 'L':
        pathCommands += `L ${cmd.x} ${cmd.y - offsetY - minY} `;
        break;
      case 'C':
        pathCommands += `C ${cmd.x1} ${cmd.y1 - offsetY - minY} ${cmd.x2} ${cmd.y2 - offsetY - minY} ${cmd.x} ${cmd.y - offsetY - minY} `;
        break;
      case 'Q':
        pathCommands += `Q ${cmd.x1} ${cmd.y1 - offsetY - minY} ${cmd.x} ${cmd.y - offsetY - minY} `;
        break;
      case 'Z':
        pathCommands += 'Z ';
        break;
    }
  });

  return pathCommands;
}

/**
 * Assigns labels to vertices
 * @param {Array} vertexList - List of vertices to label
 * @return {void}
 */
function assignVertexLabel(vertexList) {
  vertexList.map((vertex, index) => {
    vertex.label = `V${index + 1}`;
    vertex.start = index === 0 ? 1 : 0;
  });
}

/**
 * Combines multiple paths into one, reindexing vertex labels and updating arc references
 * @param {Array<Object>} pathsArray - Array of path objects or objects containing path arrays
 * @returns {Object} - Object with combined paths array, properly reindexed vertices and updated arc references
 */
function combinePaths(pathsArray) {
  // Handle edge cases
  if (!pathsArray || pathsArray.length === 0) return null;
  if (pathsArray.length === 1) return JSON.parse(JSON.stringify(pathsArray[0]));

  // Initialize the result object that will hold all combined paths
  let result = {
    path: [],
    text: []
  };

  // Track the highest vertex number across all paths to properly reindex
  let highestVertexNumber = 0;

  // Process each path object and add it to the result
  pathsArray.forEach((pathObj) => {
    // Deep copy to avoid mutations
    const pathObjCopy = JSON.parse(JSON.stringify(pathObj));

    // Check if this is a symbol object with a path array or a direct path object
    const pathsToCombine = pathObjCopy.path || [pathObjCopy];

    // Process each individual path in the array
    pathsToCombine.forEach(path => {
      // Create a mapping from old labels to new labels
      const labelMap = {};

      // Reindex all vertex labels in this path
      if (path.vertex) {
        path.vertex.forEach(vertex => {
          if (vertex.label && vertex.label.startsWith('V')) {
            const oldLabel = vertex.label;
            const numericPart = parseInt(oldLabel.substring(1), 10) || 0;
            const newLabel = `V${numericPart + highestVertexNumber}`;
            labelMap[oldLabel] = newLabel;
            vertex.label = newLabel;
          }
        });

        // Update arc references in this path
        if (path.arcs) {
          path.arcs.forEach(arc => {
            if (arc.start && labelMap[arc.start]) {
              arc.start = labelMap[arc.start];
            }
            if (arc.end && labelMap[arc.end]) {
              arc.end = labelMap[arc.end];
            }
          });
        }

        // Set start flag for the first vertex in each path
        if (path.vertex.length > 0) {
          path.vertex[0].start = 1;
        }

        // Update the highest vertex number for the next path
        const pathMaxNumber = Math.max(...path.vertex
          .filter(v => v.label && v.label.startsWith('V'))
          .map(v => parseInt(v.label.substring(1), 10) || 0));

        highestVertexNumber = Math.max(highestVertexNumber, pathMaxNumber);
      }

      // Add the processed path to the result
      result.path.push(path);
    });

    // If there are text elements, add them to the result
    if (pathObjCopy.text) {
      result.text = result.text.concat(pathObjCopy.text);
    }
  });

  return result;
}

// Export the functions so they can be imported elsewhere



/***/ }),

/***/ "./js/objects/routeBase.js":
/*!*********************************!*\
  !*** ./js/objects/routeBase.js ***!
  \*********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   assignVertexLabel: () => (/* binding */ assignVertexLabel),
/* harmony export */   getSideRoadCoords: () => (/* binding */ getSideRoadCoords)
/* harmony export */ });
/* harmony import */ var _template_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./template.js */ "./js/objects/template.js");
/* harmony import */ var _symbols_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./symbols.js */ "./js/objects/symbols.js");
/* harmony import */ var _path_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./path.js */ "./js/objects/path.js");




/**
 * Assigns labels to route vertices
 * @param {Array} vertexList - List of vertices to label
 * @return {void}
 */
function assignVertexLabel(vertexList) {
    vertexList.map((vertex, index) => {
        vertex.label = `V${index + 1}`
        vertex.start = index == 0 ? 1 : 0
    })
}

/**
 * Gets coordinates for side road endpoints
 * @param {Object} route - main road object
 * @param {number} length - Route length
 * @param {number} left - Left boundary
 * @param {number} right - Right boundary
 * @return {Array} Array of vertex coordinates
 */
function getSideRoadCoords(route, length, left, right) {
    let arrowTipPath = JSON.parse(JSON.stringify(_template_js__WEBPACK_IMPORTED_MODULE_0__.roadMapTemplate[route.shape]))
    // Apply vertex and arc scaling to each path
    arrowTipPath.path.forEach((path) => {
        path.vertex.map((v) => { v.x *= route.width / 2; v.y *= route.width / 2; v.radius *= route.width / 2 })
        if (path.arcs) {
            path.arcs.map((a) => { a.radius *= route.width / 2 })
        }
    })
    arrowTipPath = (0,_symbols_js__WEBPACK_IMPORTED_MODULE_1__.calcSymbol)(arrowTipPath, length)
    arrowTipPath.path.map((p) => {
        let transformed = (0,_path_js__WEBPACK_IMPORTED_MODULE_2__.calculateTransformedPoints)(p.vertex, {
            x: route.x,
            y: route.y,
            angle: route.angle
        });
        p.vertex = transformed
    });
    let arrowTipVertex = arrowTipPath.path[0].vertex
    if (route.angle != 0 && route.angle != 180) {
        if (route.x < left) {
            const i1 = { x: left, y: arrowTipVertex[0].y - (left - arrowTipVertex[0].x) / Math.tan(route.angle / 180 * Math.PI), radius: length, display: 1 }
            const i2 = { x: left, y: arrowTipVertex[2].y - (left - arrowTipVertex[2].x) / Math.tan(route.angle / 180 * Math.PI), radius: length, display: 1 }
            const offsetDistance = Math.tan(Math.abs(route.angle * Math.PI / 180) / 2);
            const i0 = { x: left, y: i1.y + offsetDistance * length, display: 0 }
            const i3 = { x: left, y: i2.y - offsetDistance * length, display: 0 }
            arrowTipVertex = [i0, i1, ...arrowTipVertex, i2, i3]
        } else if (route.x > right) {
            const i1 = { x: right, y: arrowTipVertex[0].y + (arrowTipVertex[0].x - right) / Math.tan(route.angle / 180 * Math.PI), radius: length, display: 1 }
            const i2 = { x: right, y: arrowTipVertex[2].y + (arrowTipVertex[2].x - right) / Math.tan(route.angle / 180 * Math.PI), radius: length, display: 1 }
            const offsetDistance = Math.tan(Math.abs(route.angle * Math.PI / 180) / 2);
            const i0 = { x: right, y: i1.y - offsetDistance * length, display: 0 }
            const i3 = { x: right, y: i2.y + offsetDistance * length, display: 0 }
            arrowTipVertex = [i0, i1, ...arrowTipVertex, i2, i3]
        }
    }

    //assignVertexLabel(arrowTipVertex)
    arrowTipPath.path[0].vertex = arrowTipVertex
    return arrowTipPath
}


/***/ }),

/***/ "./js/objects/sideRoute.js":
/*!*********************************!*\
  !*** ./js/objects/sideRoute.js ***!
  \*********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   SideRoadSymbol: () => (/* binding */ SideRoadSymbol)
/* harmony export */ });
/* harmony import */ var _draw_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./draw.js */ "./js/objects/draw.js");
/* harmony import */ var _path_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./path.js */ "./js/objects/path.js");
/* harmony import */ var _template_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./template.js */ "./js/objects/template.js");
/* harmony import */ var _symbols_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./symbols.js */ "./js/objects/symbols.js");
/* harmony import */ var _routeBase_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./routeBase.js */ "./js/objects/routeBase.js");






/**
 * Calculates vertices for side road
 * @param {number} xHeight - X-height value
 * @param {Array} mainRouteList - List of main road routes
 * @param {Array} routeList - List of routes
 * @return {Object} Vertex list object
 */
function calcSideRoadVertices(xHeight, mainRouteList, routeList) {
    const length = xHeight / 4

    // Calculate the direction vector of the arrow
    let RootLeft = mainRouteList[1].x - mainRouteList[1].width * xHeight / 8
    let RootRight = mainRouteList[1].x + mainRouteList[1].width * xHeight / 8

    // Calculate the arrowhead vertices
    let arrowTipPath = (0,_routeBase_js__WEBPACK_IMPORTED_MODULE_4__.getSideRoadCoords)(routeList[0], length, RootLeft, RootRight)
    let arrowTipVertex = arrowTipPath.path[0].vertex
    for (var i = 0; i < 3; i++) {
        arrowTipVertex.push(arrowTipVertex.shift());
    }
    (0,_routeBase_js__WEBPACK_IMPORTED_MODULE_4__.assignVertexLabel)(arrowTipVertex)
    return arrowTipPath;
}

/**
 * Common transformation logic for roundabout side roads
 * @param {Object} arrowTipPath - The path object to transform
 * @param {Object} route - The route object
 * @param {number} length - Route length
 * @param {number} angle - Rotation angle
 * @param {Object} center - Center point
 * @param {string} excludeShape - Shape name to exclude from standard rotation
 * @return {Object} Transformed path
 */
function transformRoundaboutPath(arrowTipPath, route, length, angle, center, excludeShape) {
    arrowTipPath = (0,_symbols_js__WEBPACK_IMPORTED_MODULE_3__.calcSymbol)(arrowTipPath, length);
    const transform = route.shape !== excludeShape ? {
        x: route.x,
        y: route.y,
        angle: angle / Math.PI * 180 + 90
    } : {
        x: center.x,
        y: center.y,
        angle: 0
    };
    // Apply transform to all paths
    arrowTipPath.path.forEach((p) => {
        p.vertex = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.calculateTransformedPoints)(p.vertex, transform);
    });
    return arrowTipPath;
}

/**
 * Gets coordinates for side road endpoints
 * @param {Object} route - main road object
 * @param {number} length - Route length
 * @param {number} left - Left boundary
 * @param {number} right - Right boundary
 * @return {Array} Array of vertex coordinates
 */
function getConvRdAboutSideRoadCoords(route, length, arm, radius, angle, center) {

    let arrowTipPath;
    if (route.isBase) {
        arrowTipPath = (0,_template_js__WEBPACK_IMPORTED_MODULE_2__.baseSideRoadTemplate)(route.shape, arm / length);
    }

    if (!arrowTipPath) {
        arrowTipPath = JSON.parse(JSON.stringify(_template_js__WEBPACK_IMPORTED_MODULE_2__.roadMapTemplate[route.shape]));
    }
    if (!route.isBase) {
        const width = route.width;
        // Scale vertices for all paths
        arrowTipPath.path.forEach(p => {
            p.vertex.forEach(v => { v.x *= width / 2; v.y *= width / 2; });
            p.arcs.forEach(a => { a.radius *= width / 2; });
        });

        // Compute trimming/extension points based on geometry
        const trimCenter = { x: width / 2 + 1, y: Math.sqrt((radius + 1) ** 2 - (width / 2 + 1) ** 2) };
        const tCenterAngle = Math.atan2(trimCenter.y, trimCenter.x);
        const i2 = { x: width / 2, y: arm / length - trimCenter.y, display: 0 };
        const i3 = { x: trimCenter.x - Math.cos(tCenterAngle), y: arm / length - trimCenter.y + Math.sin(tCenterAngle), display: 1 };
        const i0 = { x: -i3.x, y: i3.y, display: 1 };
        const i1 = { x: -i2.x, y: i2.y, display: 0 };

        // Modify the first path with additional vertices/arcs, keep others intact
        if (arrowTipPath.path.length > 0) {
            let vtx = arrowTipPath.path[0].vertex;
            vtx = [...vtx, i2, i3, i0, i1];
            vtx.push(vtx.shift()); // rotate start point
            (0,_routeBase_js__WEBPACK_IMPORTED_MODULE_4__.assignVertexLabel)(vtx);
            arrowTipPath.path[0].vertex = vtx;

            arrowTipPath.path[0].arcs.push(
                { start: 'V3', end: 'V4', radius: 1, direction: 0, sweep: 0 },
                { start: 'V4', end: 'V5', radius: 12, direction: 0, sweep: 0 },
                { start: 'V5', end: 'V6', radius: 1, direction: 0, sweep: 0 },
            );
        }
    }

    return transformRoundaboutPath(arrowTipPath, route, length, angle, center, 'UArrow Conventional');
}
/**
 * Gets coordinates for side road endpoints
 * @param {Object} route - main road object
 * @param {number} length - Route length
 * @param {number} left - Left boundary
 * @param {number} right - Right boundary
 * @return {Array} Array of vertex coordinates
 */
function getSpirRdAboutSideRoadCoords(route, length, angle, center) {
    if (route.isBase) {
        const distToCenter = Math.sqrt(Math.pow(route.x - center.x, 2) + Math.pow(route.y - center.y, 2));
        const arm = distToCenter;
        let arrowTipPath = (0,_template_js__WEBPACK_IMPORTED_MODULE_2__.baseSideRoadTemplate)(route.shape, arm / length);
        if (!arrowTipPath) {
            arrowTipPath = JSON.parse(JSON.stringify(_template_js__WEBPACK_IMPORTED_MODULE_2__.roadMapTemplate[route.shape]));
        }
        return transformRoundaboutPath(arrowTipPath, route, length, angle, center, 'UArrow Spiral');
    }

    const rootShape = route.shape == 'Spiral Arrow' ? 'Arrow' : route.shape; // legacy shape for spiral legs
    let arrowTipPath = JSON.parse(JSON.stringify(_template_js__WEBPACK_IMPORTED_MODULE_2__.roadMapTemplate[rootShape]));

    if (route.shape == 'UArrow Spiral') {
        return transformRoundaboutPath(arrowTipPath, route, length, angle, center, 'UArrow Spiral');
    }

    const width = route.width;
    // Scale vertices for all paths
    arrowTipPath.path.forEach(p => {
        p.vertex.forEach(v => { v.x *= width / 2; v.y *= width / 2; });
        p.arcs.forEach(a => { a.radius *= width / 2; });
    });

    // vector operations for arrow tips
    // https://www.desmos.com/calculator/6wj0d4ppmi

    // for rotate 5 deg relative to roundabout center
    const polarVectors1 = {
        rCenter: { x: 0, y: 24 },
        rAngle: 5 * Math.PI / 180
    }
    // for rotate stub end along side road middle line
    const polarVectors2 = {
        rCenter: { x: -13.9035, y: -0.3 },
        rAngle: 21.3 * Math.PI / 180
    }
    // circular sign arrow only, rotate arrow tip
    const polarVectors3 = {
        rCenter: { x: 4.2789, y: -2.881 },
        rAngle: -8.15 * Math.PI / 180
    }

    let polarVector

    switch (rootShape) {
        case 'Arrow':
            polarVector = [polarVectors1];
            break;
        case 'Stub':
            polarVector = [polarVectors1, polarVectors2];
            break;
        case 'Circular Sign':
            polarVector = [polarVectors1, polarVectors2];
            break;
        case 'RedBar':
            polarVector = [polarVectors1, polarVectors2];
            break;
        default:
            polarVector = [polarVectors1, polarVectors2];
            break;
    }


    arrowTipPath.path.forEach(p => {
        p.vertex.forEach(v => {
            if (rootShape === 'Circular Sign') {
                v.y -= 9;
            }
            if (rootShape === 'Circular Sign (with Arrow)') {
                v.y -= 24;
            }
        });
    });


    polarVector.forEach(({ rCenter, rAngle }) => {
        arrowTipPath.path.forEach(p => {
            p.vertex.forEach(v => {
                const dx = v.x - rCenter.x;
                const dy = v.y - rCenter.y;
                v.x = rCenter.x + dx * Math.cos(rAngle) - dy * Math.sin(rAngle);
                v.y = rCenter.y + dx * Math.sin(rAngle) + dy * Math.cos(rAngle);
            });
        });
    });

    if (rootShape === 'Circular Sign (with Arrow)') {
        arrowTipPath.path[1].vertex.forEach(v => {
            const { rCenter, rAngle } = polarVectors3;
            const dx = v.x - rCenter.x;
            const dy = v.y - rCenter.y;
            v.x = rCenter.x + dx * Math.cos(rAngle) - dy * Math.sin(rAngle);
            v.y = rCenter.y + dx * Math.sin(rAngle) + dy * Math.cos(rAngle);
        });
    }

    const i0 = { x: 0.841, y: 10.025, label: 'V3', start: 0, display: 1 }
    const i1 = { x: -6.949, y: 11.846, label: 'V4', start: 0, display: 1 }

    // Modify the first path with additional vertices/arcs, keep others intact
    if (arrowTipPath.path.length > 0) {
        let vtx = arrowTipPath.path[0].vertex;
        vtx = [...vtx, i0, i1];
        vtx.push(vtx.shift()); // rotate start point
        (0,_routeBase_js__WEBPACK_IMPORTED_MODULE_4__.assignVertexLabel)(vtx);
        arrowTipPath.path[0].vertex = vtx;

        // remap the end curve vertex V7 to V5 since spiral has 2 vertex less
        arrowTipPath.path[0].arcs.filter(a => a.start === 'V7').map(a => { a.start = 'V5' });

        arrowTipPath.path[0].arcs.push(
            { start: 'V2', end: 'V3', radius: 18, direction: 1, sweep: 0 },
            { start: 'V3', end: 'V4', radius: 14, direction: 0, sweep: 0 },
            { start: 'V4', end: 'V5', radius: 14, direction: 0, sweep: 0 },
        );


    }


    return transformRoundaboutPath(arrowTipPath, route, length, angle, center, 'UArrow Spiral');
}

/**
 * Gets coordinates for side road endpoints on straight section
 * @param {Object} route - main road object
 * @param {number} length - Route length
 * @param {number} arm - Arm length
 * @param {number} angle - Rotation angle
 * @param {Object} center - anchor point
 * @return {Array} Array of vertex coordinates
 */
function getOvalStraightSideRoadCoords(route, length, arm, angle, center) {
    let arrowTipPath = JSON.parse(JSON.stringify(_template_js__WEBPACK_IMPORTED_MODULE_2__.roadMapTemplate[route.shape]));
    if (!route.isBase) {
        const width = route.width;
        arrowTipPath.path.forEach(p => {
            p.vertex.forEach(v => { v.x *= width / 2; v.y *= width / 2; });
            p.arcs.forEach(a => { a.radius *= width / 2; });
        });

        const i2 = { x: width / 2, y: arm / length - 1, display: 0 };
        const i3 = { x: width / 2 + 1, y: arm / length, display: 1 };
        const i0 = { x: -i3.x, y: i3.y, display: 1 };
        const i1 = { x: -i2.x, y: i2.y, display: 0 };

        if (arrowTipPath.path.length > 0) {
            let vtx = arrowTipPath.path[0].vertex;
            vtx = [...vtx, i2, i3, i0, i1];
            vtx.push(vtx.shift());
            (0,_routeBase_js__WEBPACK_IMPORTED_MODULE_4__.assignVertexLabel)(vtx);
            arrowTipPath.path[0].vertex = vtx;

            arrowTipPath.path[0].arcs.push(
                { start: 'V3', end: 'V4', radius: 1, direction: 0, sweep: 0 },
                { start: 'V5', end: 'V6', radius: 1, direction: 0, sweep: 0 },
            );
        }
    }
    return transformRoundaboutPath(arrowTipPath, route, length, angle, center, 'UArrow Conventional');
}

/**
 * SideRoadSymbol class extends baseGroup for side roads
 */
class SideRoadSymbol extends _draw_js__WEBPACK_IMPORTED_MODULE_0__.BaseGroup {
    constructor(options = {}) {
        // Initialize with null basePolygon, will set it later
        super(null, 'SideRoad', 'SideRoadSymbol', options);

        // Branch-specific properties
        this.routeList = options.routeList || [];
        this.isBase = options.isBase || false;
        this.routeList.forEach((r) => {
            if (r.isBase === undefined) {
                r.isBase = this.isBase;
            }
        });
        this.xHeight = options.xHeight || 100;
        this.color = options.color || 'white';
        this.mainRoad = options.mainRoad || null;
        this.side = options.side || false; // false = right, true = left
        this.branchIndex = options.branchIndex || 0;

        this.initialize();

        // Bind events
        this.on('moving', this.onMove.bind(this));
        this.on('moved', this.onMove.bind(this));
        this.on('modified', this.onMove.bind(this));
    }    /**
     * Initialize the branch with a GlyphPath
     * @param {Object} vertexList - Vertex data for the branch
     * @return {SideRoadSymbol} - The initialized branch
     */
    initialize() {
        const { routeList, tempVertexList } = this.applySideRoadConstraints(
            this,
            this.mainRoad,
            this.routeList,
            this.side,
            this.xHeight
        );
        this.routeList = routeList;
        const branch = new _draw_js__WEBPACK_IMPORTED_MODULE_0__.GlyphPath();
        branch.initialize(tempVertexList, {
            left: 0,
            top: 0,
            angle: 0,
            fill: this.color,
            objectCaching: false,
            dirty: true,
            strokeWidth: 0
        });

        // Set the basePolygon that was initially null in the constructor
        this.setBasePolygon(branch, false);

        this.mainRoad.receiveNewRoute(this);

        return this;
    }


    /**
     * Applies position constraints to side road coordinates
     * @param {Object} sideRoad - The side road object
     * @param {Object} mainRoad - The parent road object
     * @param {Array} sideRouteList - The side road list to constrain
     * @param {boolean} isSideLeft - Whether branch is on left side
     * @param {number} xHeight - X-height value for measurements
     * @return {Object} - Updated routeList and vertexList
     */
    applySideRoadConstraints(sideRoad, mainRoad, sideRouteList, isSideLeft, xHeight) {
        // Make a copy to avoid modifying the original
        const routeList = JSON.parse(JSON.stringify(sideRouteList));

        switch (mainRoad.roadType) {
            case 'Main Line':
                return this.applyConstraintsMainLine(sideRoad, mainRoad, routeList, isSideLeft, xHeight)
            case 'Conventional Roundabout':
                return this.applySideRoadConstraintsRoundabout(sideRoad, mainRoad, routeList, xHeight)
            case 'Spiral Roundabout':
                return this.applySideRoadConstraintsSpiralRoundabout(sideRoad, mainRoad, routeList, xHeight)
            case 'Oval Roundabout':
                return this.applySideRoadConstraintsOvalRoundabout(sideRoad, mainRoad, routeList, xHeight)
            case 'Double Roundabout':
                return this.applySideRoadConstraintsDoubleRoundabout(sideRoad, mainRoad, routeList, xHeight)
        }

    }

    applyConstraintsMainLine(sideRoad, mainRoad, routeList, isSideLeft, xHeight) {
        // Horizontal constraint based on side
        const rootLeft = mainRoad.routeList[1].x - mainRoad.routeList[1].width * mainRoad.xHeight / 8;
        const rootRight = mainRoad.routeList[1].x + mainRoad.routeList[1].width * mainRoad.xHeight / 8;
        const minBranchXDeltaShape = { 'Arrow': 13, 'Stub': 4, 'RedBar': 4, 'Circular Sign': 13, 'Circular Sign (with Arrow)': 28 }[routeList[0].shape] || 12;
        const minBranchShapeXDelta = (Math.abs(routeList[0].angle) == 90 && routeList[0].shape == 'Arrow') ? 12 : minBranchXDeltaShape;
        const minBranchXDelta = minBranchShapeXDelta * xHeight / 4;

        // Constrain movement based on side (left or right)
        if (routeList[0].x + minBranchXDelta > rootLeft && isSideLeft) {
            // Left side branch constraint
            routeList[0].x = rootLeft - minBranchXDelta;
            //sideRoad.left = routeList[0].x;
        } else if (routeList[0].x - minBranchXDelta < rootRight && !isSideLeft) {
            // Right side branch constraint
            routeList[0].x = rootRight + minBranchXDelta;
            //sideRoad.left = rootRight
        }

        // Vertical constraint based on main road top
        const mainVertex = mainRoad.basePolygon.vertex
        const leftPivot = mainVertex[mainVertex.length - 2].y
        const rightPivot = mainVertex[mainVertex.length - 6].y
        const bottomPivotY = Math.max(leftPivot, rightPivot)

        // Calculate vertices with current position
        let tempVertexList = calcSideRoadVertices(mainRoad.xHeight, mainRoad.routeList, routeList);

        // Get the vertex that should touch the root (depends on side)
        const rootTopTouchY = tempVertexList.path[0].vertex[isSideLeft ? 3 : 4];

        // Ensure branch doesn't go above root + tip length
        if (rootTopTouchY.y < bottomPivotY) {
            // Push branch down if needed
            const adjustment = bottomPivotY - rootTopTouchY.y;
            routeList[0].y += adjustment;

            // Recalculate vertices with new position
            tempVertexList = calcSideRoadVertices(mainRoad.xHeight, mainRoad.routeList, routeList);

            // Only set top if sideRoad is not null
            if (sideRoad) {
                sideRoad.top = Math.min(...tempVertexList.path[0].vertex.map(v => v.y));
            }
        }

        return { routeList, tempVertexList };
    }

    applySideRoadConstraintsRoundabout(sideRoad, mainRoad, routeList, xHeight) {
        // Horizontal constraint based on side
        const radius = 12
        const length = xHeight / 4
        const minBranchXDelta = { 'Arrow': 24, 'Base Conventional Normal': 22.9, 'Base Conventional Auxiliary': 30, 'Base Conventional U-turn': 45, 'Circular Sign': 25, 'Circular Sign (with Arrow)': 40 }[routeList[0].shape] || 16;
        const center = mainRoad.routeList[1]

        let angleToCenter, distToCenter;

        if (routeList[0].isBase) {
            angleToCenter = 90 * Math.PI / 180;
            const rootLength = mainRoad.routeList[1].length || 7;
        } else {
            const rawAngleToCenter = Math.atan2(routeList[0].y - center.y, routeList[0].x - center.x)
            // Convert to degrees, round to nearest 15 degrees, then back to radians
            const angleInDegrees = rawAngleToCenter * 180 / Math.PI
            const roundedDegrees = Math.round(angleInDegrees / 15) * 15
            angleToCenter = roundedDegrees * Math.PI / 180

        }

        distToCenter = Math.max(minBranchXDelta * length, Math.sqrt((routeList[0].y - center.y) ** 2 + (routeList[0].x - center.x) ** 2))

        if (!routeList[0].isBase) {
            routeList[0].x = center.x + distToCenter * Math.cos(angleToCenter)
            routeList[0].y = center.y + distToCenter * Math.sin(angleToCenter)
        } else {
            routeList[0].x = center.x + distToCenter * Math.cos(angleToCenter)
            routeList[0].y = center.y + distToCenter * Math.sin(angleToCenter)
        }
        const tempVertexList = getConvRdAboutSideRoadCoords(routeList[0], length, distToCenter, radius, angleToCenter, center);

        // Only set left/top if sideRoad is a real object with those properties
        if (sideRoad && sideRoad.left !== undefined) {
            const offset = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.getInsertOffset)(tempVertexList);
            sideRoad.left = offset.left;
            sideRoad.top = offset.top;
        }

        return { routeList, tempVertexList };
    }

    applySideRoadConstraintsSpiralRoundabout(sideRoad, mainRoad, routeList, xHeight) {
        const center = mainRoad.routeList[1]
        const length = xHeight / 4 // Use the parameter directly for temp objects without sideRoad object
        const radius = 12;
        const minBranchXDelta = { 'Arrow': 24, 'Base Spiral Normal': 24, 'Base Spiral Auxiliary': 30, 'Base Spiral U-turn': 45, 'Circular Sign': 24, 'Circular Sign (with Arrow)': 24 }[routeList[0].shape] || 24;

        let angleToCenter, distToCenter;

        if (routeList[0].isBase) {
            angleToCenter = 90 * Math.PI / 180;
            const rootLength = mainRoad.routeList[1].length || 7;
            distToCenter = Math.max(minBranchXDelta * length, Math.sqrt((routeList[0].y - center.y) ** 2 + (routeList[0].x - center.x) ** 2))
        } else {
            const rawAngleToCenter = Math.atan2(routeList[0].y - center.y, routeList[0].x - center.x)
            // Convert to degrees, round to nearest 15 degrees, then back to radians
            const angleInDegrees = rawAngleToCenter * 180 / Math.PI
            const roundedDegrees = Math.round(angleInDegrees / 15) * 15
            angleToCenter = roundedDegrees * Math.PI / 180
            distToCenter = minBranchXDelta * length
        }


        routeList[0].x = center.x + distToCenter * Math.cos(angleToCenter)
        routeList[0].y = center.y + distToCenter * Math.sin(angleToCenter)


        const tempVertexList = getSpirRdAboutSideRoadCoords(routeList[0], length, angleToCenter, center);

        // Only set left/top if sideRoad is a real object with those properties
        if (sideRoad && sideRoad.left !== undefined) {
            const offset = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.getInsertOffset)(tempVertexList);
            sideRoad.left = offset.left;
            sideRoad.top = offset.top;
        }

        return { routeList, tempVertexList };
    }

    applySideRoadConstraintsOvalRoundabout(sideRoad, mainRoad, routeList, xHeight) {
        const center = mainRoad.routeList[1];
        const length = xHeight / 4;
        const radius = 12; // 12 units radius
        const minBranchXDelta = { 'Arrow': 12, 'Stub': 4, 'RedBar': 4, 'Circular Sign': 13, 'Circular Sign (with Arrow)': 16 }[routeList[0].shape] || 22.9;

        // Main road rotation
        const mainAngle = (mainRoad.mainAngle || 0) * Math.PI / 180;

        if (routeList[0].isBase) {
            const rootLength = mainRoad.routeList[1].length || 7;

            // Global Normal Angle (for drawing direction) - Always 90 degrees (Down)
            const globalNormalAngle = Math.PI / 2;

            // Virtual Center is just the center because Oval pivots at (0,0) local
            const globalVirtualCenter = {
                x: center.x,
                y: center.y
            };

            const distToCenter = Math.max(minBranchXDelta * length, Math.sqrt((routeList[0].y - center.y) ** 2 + (routeList[0].x - center.x) ** 2))
            routeList[0].x = center.x + distToCenter * Math.cos(globalNormalAngle);
            routeList[0].y = center.y + distToCenter * Math.sin(globalNormalAngle);

            const totalArm = distToCenter;
            const tempVertexList = getConvRdAboutSideRoadCoords(routeList[0], length, totalArm, 12, globalNormalAngle, globalVirtualCenter);

            if (sideRoad && sideRoad.left !== undefined) {
                const offset = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.getInsertOffset)(tempVertexList);
                sideRoad.left = offset.left;
                sideRoad.top = offset.top;
            }
            return { routeList, tempVertexList };
        }

        // Global to Local (Rotate by -mainAngle) relative to Bottom Center (routeList[1])
        const dxGlobal = (routeList[0].x - center.x) / length;
        const dyGlobal = (routeList[0].y - center.y) / length;

        const dx = dxGlobal * Math.cos(-mainAngle) - dyGlobal * Math.sin(-mainAngle);
        const dy = dxGlobal * Math.sin(-mainAngle) + dyGlobal * Math.cos(-mainAngle);

        // Define Zones in Local Space
        // Bottom Center is (0,0). Top Center is (0, -24).
        // Straight is between Y=0 and Y=-24.

        let localNormalAngle; // Direction of arm in Local Space
        let localVirtualCenter; // Pivot point for arm in Local Space
        let isStraight = false;
        let sideSign = 1; // 1 for right, -1 for left

        if (dy < -24) {
            // TOP CIRCLE ZONE (Center at 0, -24)
            const vY = dy - (-24);
            const vX = dx;
            const rawAngle = Math.atan2(vY, vX); // Angle from (0, -24)
            // Round to 15 deg
            const deg = rawAngle * 180 / Math.PI;
            const roundedDeg = Math.round(deg / 15) * 15;
            localNormalAngle = roundedDeg * Math.PI / 180;
            localVirtualCenter = { x: 0, y: -24 };

        } else if (dy > 0) {
            // BOTTOM CIRCLE ZONE (Center at 0, 0)
            const vY = dy;
            const vX = dx;
            const rawAngle = Math.atan2(vY, vX); // Angle from (0,0)
            // Round to 15 deg
            const deg = rawAngle * 180 / Math.PI;
            const roundedDeg = Math.round(deg / 15) * 15;
            localNormalAngle = roundedDeg * Math.PI / 180;
            localVirtualCenter = { x: 0, y: 0 };

        } else {
            // STRAIGHT ZONE (between 0 and -24)
            isStraight = true;
            if (dx >= 0) {
                // Right Side
                localNormalAngle = 0;
                sideSign = 1;
            } else {
                // Left Side
                localNormalAngle = Math.PI;
                sideSign = -1;
            }
            // For straight, virtualCenter acts as the anchor on the hull
            localVirtualCenter = { x: sideSign * 12, y: dy };
        }

        // Calculate Arm Length
        let distFromAnchor = 0;
        if (isStraight) {
            // Projected distance from the hull line (X = 12 or -12)
            distFromAnchor = (sideSign === 1) ? (dx - 12) : (-12 - dx);
        } else {
            // Radial distance from virtual center minus radius
            const distToVC = Math.sqrt((dx - localVirtualCenter.x) ** 2 + (dy - localVirtualCenter.y) ** 2);
            distFromAnchor = distToVC - radius;
        }

        // Min length constraint
        const minBranchShapeXDelta = minBranchXDelta;
        if (distFromAnchor < minBranchShapeXDelta) distFromAnchor = minBranchShapeXDelta;

        // Calculate Constrained Position in LOCAL space
        let constrainedLocalX, constrainedLocalY;
        if (isStraight) {
            constrainedLocalX = (sideSign * 12) + (sideSign * distFromAnchor);
            constrainedLocalY = dy; // Y is preserved (sliding)
        } else {
            constrainedLocalX = localVirtualCenter.x + (radius + distFromAnchor) * Math.cos(localNormalAngle);
            constrainedLocalY = localVirtualCenter.y + (radius + distFromAnchor) * Math.sin(localNormalAngle);
        }

        // Transform back to GLOBAL space
        // 1. Constrained Point
        const finalGlobalX = constrainedLocalX * Math.cos(mainAngle) - constrainedLocalY * Math.sin(mainAngle);
        const finalGlobalY = constrainedLocalX * Math.sin(mainAngle) + constrainedLocalY * Math.cos(mainAngle);

        if (routeList[0].shape !== 'UArrow Conventional') {
            routeList[0].x = center.x + finalGlobalX * length;
            routeList[0].y = center.y + finalGlobalY * length;
        }

        // 2. Helper Virtual Center (for drawing)
        // Note: For straight, we used sliding VCenter (x, dy).
        // For curved, we used static VCenter (x, y).
        const vcGlobalX = localVirtualCenter.x * Math.cos(mainAngle) - localVirtualCenter.y * Math.sin(mainAngle);
        const vcGlobalY = localVirtualCenter.x * Math.sin(mainAngle) + localVirtualCenter.y * Math.cos(mainAngle);
        const globalVirtualCenter = {
            x: center.x + vcGlobalX * length,
            y: center.y + vcGlobalY * length
        };

        // 3. Global Normal Angle (for drawing direction)
        const globalNormalAngle = localNormalAngle + mainAngle;

        // Generate Vertices
        let tempVertexList;
        if (isStraight) {
            tempVertexList = getOvalStraightSideRoadCoords(routeList[0], length, distFromAnchor * length, globalNormalAngle, globalVirtualCenter);
        } else {
            const totalArm = (radius + distFromAnchor) * length;
            tempVertexList = getConvRdAboutSideRoadCoords(routeList[0], length, totalArm, 12, globalNormalAngle, globalVirtualCenter);
        }

        // Only set left/top if sideRoad is a real object with those properties
        if (sideRoad && sideRoad.left !== undefined) {
            const offset = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.getInsertOffset)(tempVertexList);
            sideRoad.left = offset.left;
            sideRoad.top = offset.top;
        }

        return { routeList, tempVertexList };
    }

    applySideRoadConstraintsDoubleRoundabout(sideRoad, mainRoad, routeList, xHeight) {
        const center = mainRoad.routeList[1];
        const length = xHeight / 4;
        const radius = 12; // 12 units radius

        // Check for Spiral feature
        const isSpiral = mainRoad.RAfeature && mainRoad.RAfeature.includes('Spiral');
        const spacing = isSpiral ? 38 : 28;

        // Main road rotation
        const mainAngle = (mainRoad.mainAngle || 0) * Math.PI / 180;

        if (routeList[0].isBase) {
            const localVirtualCenter = { x: 0, y: 0 };
            const localNormalAngle = Math.PI / 2;
            const globalNormalAngle = 90 * Math.PI / 180;
            const globalVirtualCenter = {
                x: center.x,
                y: center.y
            };

            let tempVertexList;
            if (isSpiral) {
                const minBranchXDelta = { 'Base Spiral Normal': 24, 'Base Spiral Auxiliary': 30, 'Base Spiral U-turn': 45 }[routeList[0].shape] || 24;
                const distToCenter = Math.max(minBranchXDelta * length, Math.sqrt((routeList[0].y - center.y) ** 2 + (routeList[0].x - center.x) ** 2))

                routeList[0].x = center.x + distToCenter * Math.cos(globalNormalAngle)
                routeList[0].y = center.y + distToCenter * Math.sin(globalNormalAngle)

                tempVertexList = getSpirRdAboutSideRoadCoords(routeList[0], length, globalNormalAngle, globalVirtualCenter);
            } else {
                const minBranchXDelta = { 'Base Double Conventional': 22.9, }[routeList[0].shape] || 16;

                const distToCenter = Math.max(minBranchXDelta * length, Math.sqrt((routeList[0].y - center.y) ** 2 + (routeList[0].x - center.x) ** 2))

                routeList[0].x = center.x + distToCenter * Math.cos(globalNormalAngle)
                routeList[0].y = center.y + distToCenter * Math.sin(globalNormalAngle)
                tempVertexList = getConvRdAboutSideRoadCoords(routeList[0], length, distToCenter, radius, globalNormalAngle, globalVirtualCenter);
            }

            if (sideRoad && sideRoad.left !== undefined) {
                const offset = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.getInsertOffset)(tempVertexList);
                sideRoad.left = offset.left;
                sideRoad.top = offset.top;
            }
            return { routeList, tempVertexList };
        }

        // Global to Local (Rotate by -mainAngle) relative to Bottom Center (routeList[1])
        const dxGlobal = (routeList[0].x - center.x) / length;
        const dyGlobal = (routeList[0].y - center.y) / length;

        const dx = dxGlobal * Math.cos(-mainAngle) - dyGlobal * Math.sin(-mainAngle);
        const dy = dxGlobal * Math.sin(-mainAngle) + dyGlobal * Math.cos(-mainAngle);

        let localNormalAngle; // Direction of arm in Local Space
        let localVirtualCenter; // Pivot point for arm in Local Space

        // Two centers at (0,0) and (0, -spacing).
        const dist1 = dx * dx + dy * dy; // Distance squared to (0,0)
        const dist2 = dx * dx + (dy + spacing) * (dy + spacing); // Distance squared to (0, -spacing)

        if (dist1 < dist2) {
            // Closer to bottom center (0,0)
            localVirtualCenter = { x: 0, y: 0 };
        } else {
            // Closer to top center (0, -spacing)
            localVirtualCenter = { x: 0, y: -spacing };
        }

        const vY = dy - localVirtualCenter.y;
        const vX = dx - localVirtualCenter.x;
        const rawAngle = Math.atan2(vY, vX); // Angle from Virtual Center
        // Round to 15 deg
        const deg = rawAngle * 180 / Math.PI;
        let roundedDeg = Math.round(deg / 15) * 15;

        // Apply directional constraints (Double Roundabout splay)
        if (localVirtualCenter.y === -spacing) {
            // Top Center: Exclude downward cone (45 to 135)
            if (roundedDeg > 45 && roundedDeg < 135) {
                roundedDeg = (roundedDeg <= 90) ? 45 : 135;
            }
        } else {
            // Bottom Center: Exclude upward cone (-135 to -45)
            if (roundedDeg > -135 && roundedDeg < -45) {
                roundedDeg = (roundedDeg <= -90) ? -135 : -45;
            }
        }

        localNormalAngle = roundedDeg * Math.PI / 180;

        // Helper Virtual Center (for drawing)
        const vcGlobalX = localVirtualCenter.x * Math.cos(mainAngle) - localVirtualCenter.y * Math.sin(mainAngle);
        const vcGlobalY = localVirtualCenter.x * Math.sin(mainAngle) + localVirtualCenter.y * Math.cos(mainAngle);
        const globalVirtualCenter = {
            x: center.x + vcGlobalX * length,
            y: center.y + vcGlobalY * length
        };

        // Global Normal Angle (for drawing direction)
        const globalNormalAngle = localNormalAngle + mainAngle;

        let tempVertexList;

        if (isSpiral) {
            // Calculate Constrained Position in LOCAL space for Spiral (fixed radius 24)
            const constrainedLocalX = localVirtualCenter.x + 24 * Math.cos(localNormalAngle);
            const constrainedLocalY = localVirtualCenter.y + 24 * Math.sin(localNormalAngle);

            // Transform back to GLOBAL space
            const finalGlobalX = constrainedLocalX * Math.cos(mainAngle) - constrainedLocalY * Math.sin(mainAngle);
            const finalGlobalY = constrainedLocalX * Math.sin(mainAngle) + constrainedLocalY * Math.cos(mainAngle);

            if (!routeList[0].isBase) {
                routeList[0].x = center.x + finalGlobalX * length;
                routeList[0].y = center.y + finalGlobalY * length;
            }
            tempVertexList = getSpirRdAboutSideRoadCoords(routeList[0], length, globalNormalAngle, globalVirtualCenter);

        } else {
            // Calculate Arm Length
            // Radial distance from virtual center minus radius
            const distToVC = Math.sqrt((dx - localVirtualCenter.x) ** 2 + (dy - localVirtualCenter.y) ** 2);
            let distFromAnchor = distToVC - radius;

            // Min length constraint
            const minBranchXDelta = { 'Arrow': 12, 'Stub': 4, 'RedBar': 4, 'Circular Sign': 13, 'Circular Sign (with Arrow)': 28 }[routeList[0].shape] || 4;
            if (distFromAnchor < minBranchXDelta) distFromAnchor = minBranchXDelta;

            // Calculate Constrained Position in LOCAL space
            const constrainedLocalX = localVirtualCenter.x + (radius + distFromAnchor) * Math.cos(localNormalAngle);
            const constrainedLocalY = localVirtualCenter.y + (radius + distFromAnchor) * Math.sin(localNormalAngle);

            // Transform back to GLOBAL space
            // 1. Constrained Point
            const finalGlobalX = constrainedLocalX * Math.cos(mainAngle) - constrainedLocalY * Math.sin(mainAngle);
            const finalGlobalY = constrainedLocalX * Math.sin(mainAngle) + constrainedLocalY * Math.cos(mainAngle);

            if (!routeList[0].isBase) {
                routeList[0].x = center.x + finalGlobalX * length;
                routeList[0].y = center.y + finalGlobalY * length;
            }

            const totalArm = (radius + distFromAnchor) * length;
            tempVertexList = getConvRdAboutSideRoadCoords(routeList[0], length, totalArm, 12, globalNormalAngle, globalVirtualCenter);
        }

        // Only set left/top if sideRoad is a real object with those properties
        if (sideRoad && sideRoad.left !== undefined) {
            const offset = (0,_path_js__WEBPACK_IMPORTED_MODULE_1__.getInsertOffset)(tempVertexList);
            sideRoad.left = offset.left;
            sideRoad.top = offset.top;
        }

        return { routeList, tempVertexList };
    }

    /**
     * Constrains branch positions based on main road
     * @param {Object} mainRoad - The parent route
     * @return {Object} tempVertexList - Updated vertex list
     */
    constrainSideRoadPosition(mainRoad) {
        // Use the common constraint function
        const result = this.applySideRoadConstraints(
            this,
            mainRoad,
            this.routeList,
            this.side,
            this.xHeight
        );

        // Update instance properties with constrained values
        this.routeList = result.routeList;
        if (mainRoad.roadType == 'Main Line') {
            this.left = this.side ? this.routeList[0].x : mainRoad.left + mainRoad.width;
        }
        this.refTopLeft.left = this.left;
        //this.top = this.routeList[0].y;
        this.refTopLeft.top = this.top;

        return result.tempVertexList;
    }    /**
     * Updates route positions when branch is moved
     * @param {Event} event - Move event
     * @param {boolean} updateRoot - Whether to update the main road
     * @return {void}
     */
    onMove(event, updateRoot = true) {
        if (!this.mainRoad) return;

        const mainRoad = this.mainRoad;

        // Apply position constraints and get the updated vertex list
        const tempVertexList = this.constrainSideRoadPosition(mainRoad);

        // Create new polygon with constrained position
        const polygon1 = new _draw_js__WEBPACK_IMPORTED_MODULE_0__.GlyphPath();
        polygon1.initialize(tempVertexList, {
            left: 0,
            top: 0,
            angle: 0,
            fill: this.color,
            objectCaching: false,
            dirty: true,
            strokeWidth: 0
        });

        this.replaceBasePolygon(polygon1, false);

        // Update root if needed
        if (updateRoot) {
            mainRoad.receiveNewRoute();
            mainRoad.setCoords();
        }
        this.basePolygon.setCoords();
        this.drawVertex()
    }
}


/***/ }),

/***/ "./js/objects/symbols.js":
/*!*******************************!*\
  !*** ./js/objects/symbols.js ***!
  \*******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   SymbolObject: () => (/* binding */ SymbolObject),
/* harmony export */   calcSymbol: () => (/* binding */ calcSymbol),
/* harmony export */   drawSymbolDirectly: () => (/* binding */ drawSymbolDirectly)
/* harmony export */ });
/* harmony import */ var _draw_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./draw.js */ "./js/objects/draw.js");
/* harmony import */ var _template_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./template.js */ "./js/objects/template.js");
/* harmony import */ var _path_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./path.js */ "./js/objects/path.js");
/**
 * symbols.js - Functions for handling symbols in traffic sign drawings
 * Uses path.js for all path-related functionality
 */





function calcSymbol(type, length, color = 'white') {
  let symbol;
  if (typeof type === 'string') {
    const symbolsT = JSON.parse(JSON.stringify((color.toLocaleLowerCase() == 'black' && Object.keys(_template_js__WEBPACK_IMPORTED_MODULE_1__.symbolsTemplateAlt).includes(type)) ? _template_js__WEBPACK_IMPORTED_MODULE_1__.symbolsTemplateAlt : _template_js__WEBPACK_IMPORTED_MODULE_1__.symbolsTemplate)); // Deep copy to avoid mutation
    const backup = JSON.parse(JSON.stringify(_template_js__WEBPACK_IMPORTED_MODULE_1__.symbolsTemplate));
    symbol = symbolsT[type] || backup[type];
  } else {
    symbol = JSON.parse(JSON.stringify(type));
  }

  symbol.path.forEach(path => {
    path.vertex.forEach(vertex => {
      vertex.x *= length;
      vertex.y *= length;
      if (vertex.radius) vertex.radius *= length;
    });
    if (path.arcs){
      path.arcs.forEach(arc => {
        arc.radius *= length;
        if (arc.radius2) { arc.radius2 *= length; }
      });
    }
    if (path.centerLine) {
      path.centerLine.forEach(vertex => {
        vertex.x *= length;
        vertex.y *= length;
        if (vertex.radius) vertex.radius *= length;
        if (vertex.width) vertex.width *= length;
      });
    }
    if (path.centerArc) {
      path.centerArc.forEach(arc => {
        arc.radius *= length;
        if (arc.radius2) { arc.radius2 *= length; }
      });
    }
  });

  if (symbol.text) {
    symbol.text.forEach(t => {
      t.x *= length;
      t.y *= length;
      t.fontSize *= length;
    });
  }

  return symbol;
}

/**
 * SymbolObject extends BaseGroup to create symbols with editing functionality
 */
class SymbolObject extends _draw_js__WEBPACK_IMPORTED_MODULE_0__.BaseGroup {
  constructor(shapeMeta = {}) {
    // Ensure symbolType is passed in options so it gets added to _metadataKeys
    super(null, 'Symbol', 'SymbolObject', shapeMeta ); // Call BaseGroup constructor

    // Store shapeMeta properties
    this.symbolType = shapeMeta.symbolType;
    this.xHeight = shapeMeta.xHeight || 100;
    this.color = shapeMeta.color || 'White';
    this.symbolAngle = shapeMeta.symbolAngle || 0;
    this.left = shapeMeta.left || 0;
    this.top = shapeMeta.top || 0;

    this.initialize();

  }

  initialize() {

    // Set the basePolygon
    this.setBasePolygon(this.drawSymbol());

    return this;
  }

  /**
   * LEGACY Update symbol properties and visuals
   */
  updateSymbol(options) {
    // Store the new properties
        // Store shapeMeta properties
    this.symbolType = options.symbolType;
    this.xHeight = options.xHeight || 100;
    this.color = options.color || 'White';
    this.symbolAngle = options.angle || 0;
    this.left = options.x || 0;
    this.top = options.y || 0;
    
    // Initialize the new path with transformed coordinates and replace the current one
    this.replaceBasePolygon(this.drawSymbol());

    // Update the group's angle if necessary, though typically handled by rotating vertices
    // this.set('angle', this.symbolAngle); 

    this.showDimensions();
  }

  drawSymbol() {
    // Calculate symbol data
    const symbolData = calcSymbol(this.symbolType, this.xHeight / 4, this.color);

    // Create symbol options
    const symbolOptions = {
      left: this.left,
      top: this.top,
      fill: this.color.toLowerCase() === 'white' ? '#ffffff' : '#000000',
      angle: 0, // Angle is applied to vertices, not the fabric object initially
      objectCaching: false,
      strokeWidth: 0
    };

    // Apply the rotation transform to vertex coordinates before creating the path
    symbolData.path.forEach(p => {
      p.vertex = (0,_path_js__WEBPACK_IMPORTED_MODULE_2__.calculateTransformedPoints)(p.vertex, {
        x: 0,
        y: 0,
        angle: this.symbolAngle
      });
    });

    // Create the GlyphPath for the symbol
    const symbolPath = new _draw_js__WEBPACK_IMPORTED_MODULE_0__.GlyphPath(symbolOptions);
    symbolPath.initialize(symbolData, symbolOptions);
    symbolPath.symbol = this.symbolType; // Keep track of the symbol type

    return symbolPath;
  }
}

/**
 * Legacy function to draw a symbol directly on the canvas.
 * Creates a symbol directly on the canvas.
 * @param {string} symbolType - Type of symbol to create
 * @param {Object} options - Configuration options: x, y, xHeight, angle, color
 * @return {SymbolObject} The created symbol object
 */
function drawSymbolDirectly(symbolType, options) {
  const shapeMeta = {
    symbolType: symbolType,
    xHeight: options.xHeight,
    color: options.color,
    symbolAngle: options.angle || 0,
    x: options.x || 0,
    y: options.y || 0
  };

  const symbolGroup = new SymbolObject(shapeMeta);
  return symbolGroup.initialize();
}




/***/ }),

/***/ "./js/objects/template.js":
/*!********************************!*\
  !*** ./js/objects/template.js ***!
  \********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   BorderColorScheme: () => (/* binding */ BorderColorScheme),
/* harmony export */   BorderFrameWidth: () => (/* binding */ BorderFrameWidth),
/* harmony export */   BorderPaddingWidth: () => (/* binding */ BorderPaddingWidth),
/* harmony export */   BorderTypeScheme: () => (/* binding */ BorderTypeScheme),
/* harmony export */   ChtDestinations: () => (/* binding */ ChtDestinations),
/* harmony export */   DividerMargin: () => (/* binding */ DividerMargin),
/* harmony export */   DividerScheme: () => (/* binding */ DividerScheme),
/* harmony export */   EngDestinations: () => (/* binding */ EngDestinations),
/* harmony export */   HDividerTemplate: () => (/* binding */ HDividerTemplate),
/* harmony export */   HLineTemplate: () => (/* binding */ HLineTemplate),
/* harmony export */   VDividerTemplate: () => (/* binding */ VDividerTemplate),
/* harmony export */   VLaneTemplate: () => (/* binding */ VLaneTemplate),
/* harmony export */   baseSideRoadTemplate: () => (/* binding */ baseSideRoadTemplate),
/* harmony export */   roadMapTemplate: () => (/* binding */ roadMapTemplate),
/* harmony export */   roundelTemplate: () => (/* binding */ roundelTemplate),
/* harmony export */   routePermittedAngle: () => (/* binding */ routePermittedAngle),
/* harmony export */   symbolsPermittedAngle: () => (/* binding */ symbolsPermittedAngle),
/* harmony export */   symbolsTemplate: () => (/* binding */ symbolsTemplate),
/* harmony export */   symbolsTemplateAlt: () => (/* binding */ symbolsTemplateAlt),
/* harmony export */   textWidthHeavy: () => (/* binding */ textWidthHeavy),
/* harmony export */   textWidthMedium: () => (/* binding */ textWidthMedium)
/* harmony export */ });
// Important: 
// vertex 'start' property: 1 means the start of the path, 0 means not start. 
// If cutout is needed, the paths with different start point should be defined in the same object path array.
// radius property in vertex is only apply for straight line intersection, to indicate the fillet radius at that vertex.
// display property in vertex: 1 means the vertex is to be displayed, 0 means not displayed.
// display property also control the snapping behavior when try to anchor objects.

const roadMapTemplate = {
    'Arrow': {
        path: [{
            'vertex': [
                { x: -1, y: 1, label: 'V1', start: 1, display: 1 },
                { x: 0, y: 0, label: 'V2', start: 0, display: 1 },
                { x: 1, y: 1, label: 'V3', start: 0, display: 1 },
            ], 'arcs': []
        }],
    },
    'Stub': {
        path: [{
            'vertex': [
                { x: -1, y: 0, label: 'V1', start: 0, display: 1 },
                { x: 0, y: 0, label: 'V2', start: 1, display: 1 },
                { x: 1, y: 0, label: 'V3', start: 0, display: 1 },
            ], 'arcs': []
        }],
    },
    'RedBar': {
        path: [{
            'vertex': [
                { x: -1, y: 0, label: 'V1', start: 0, display: 1 },
                { x: 0, y: 0, label: 'V2', start: 1, display: 1 },
                { x: 1, y: 0, label: 'V3', start: 0, display: 1 },
            ], 'arcs': []
        }, {
            'vertex': [
                { x: -3, y: -2.667, label: 'V14', start: 1, display: 1 },
                { x: 3, y: -2.667, label: 'V15', start: 0, display: 1 },
                { x: 3, y: -0.667, label: 'V16', start: 0, display: 1 },
                { x: -3, y: -0.667, label: 'V17', start: 0, display: 1 },
            ], 'arcs': [], fill: 'rgb(224, 0, 0)'
        }],
    },
    'Circular Sign': {
        path: [{
            'vertex': [
                { x: -1, y: 4.3875, label: 'V1', start: 0, display: 1 },
                { x: 0, y: 4.5, label: 'V2', start: 1, display: 1 },
                { x: 1, y: 4.3875, label: 'V3', start: 0, display: 1 },
            ], 'arcs': [
                { start: 'V7', end: 'V1', radius: 4.5, direction: 0, sweep: 0 },
                { start: 'V1', end: 'V2', radius: 4.5, direction: 0, sweep: 0 },]
        }, {
            'vertex': [
                { x: 0, y: 0, label: 'C1', start: 1, display: 1 },
            ], 'arcs': [],
        }],
    },
    'Circular Sign (with Arrow)': {
        path: [{
            'vertex': [
                { x: -1, y: 11.8875, label: 'V1', start: 1, display: 1 },
                { x: 0, y: 12, label: 'V2', start: 0, display: 1 },
                { x: 1, y: 11.8875, label: 'V3', start: 0, display: 1 },
            ], 'arcs': [
                { start: 'V7', end: 'V1', radius: 4.5, direction: 0, sweep: 0 },
                { start: 'V1', end: 'V2', radius: 4.5, direction: 0, sweep: 0 },]
        }, {
            'vertex': [
                { x: -1, y: 1, label: 'V8', start: 1, display: 1 },
                { x: 0, y: 0, label: 'V9', start: 0, display: 1 },
                { x: 1, y: 1, label: 'V10', start: 0, display: 1 },
                { x: 1, y: 3.1125, label: 'V11', start: 0, display: 1 },
                { x: -1, y: 3.1125, label: 'V12', start: 0, display: 1 },
            ], 'arcs': [
                { start: 'V11', end: 'V12', radius: 4.5, direction: 0, sweep: 0 },]
        }, {
            'vertex': [
                { x: 0, y: 7.5, label: 'C1', start: 1, display: 1 },
            ], 'arcs': [],
        }],
    },
    'Round': {
        path: [{
            'vertex': [
                { x: 0, y: 0, label: 'V1', start: 1 }, // to be calculated by function
            ], 'arcs': []
        }],
    },
    'Root': {
        path: [{
            'vertex': [
                { x: 1, y: 24, label: 'V1', start: 1, display: 0 },
                { x: -1, y: 24, label: 'V2', start: 0, display: 0 },
            ], 'arcs': []
        }],
    },
    'Left': {
        path: [{
            'vertex': [
                { x: -1, y: 2.5, label: 'V1', start: 1, display: 0 },
                { x: -1, y: 2, label: 'V2', start: 0, display: 0, radius: 0.5 },
                { x: -15, y: 2, label: 'V3', start: 0, display: 0 },
                { x: -16, y: 1, label: 'V4', start: 0, display: 0 },
                { x: -15, y: 0, label: 'V5', start: 0, display: 0 },
                { x: 1, y: 0, label: 'V6', start: 0, display: 0, radius: 2 },
                { x: 1, y: 2.5, label: 'V7', start: 0, display: 0 },
            ], 'arcs': [
                //{ start: 'V1', end: 'V2', radius: 0.5, direction: 0, sweep: 0 },
                //{ start: 'V6', end: 'V7', radius: 2, direction: 1, sweep: 0 },
            ]
        }],
    },
    'Right': {
        path: [{
            'vertex': [
                { x: -1, y: 2.5, label: 'V1', start: 1, display: 0 },
                { x: -1, y: 0, label: 'V2', start: 0, display: 0, radius: 2 },
                { x: 15, y: 0, label: 'V3', start: 0, display: 0 },
                { x: 16, y: 1, label: 'V4', start: 0, display: 0 },
                { x: 15, y: 2, label: 'V5', start: 0, display: 0 },
                { x: 1, y: 2, label: 'V6', start: 0, display: 0, radius: 0.5 },
                { x: 1, y: 2.5, label: 'V7', start: 0, display: 0 },
            ], 'arcs': []
        }],
    },
    /*'Tee': {
        path: [{
            'vertex': [
                { x: -1, y: 2.5, label: 'V1', start: 1, display: 0 },
                { x: -1, y: 2, label: 'V2', start: 0, display: 0, radius: 0.5 },
                { x: -15, y: 2, label: 'V3', start: 0, display: 0 },
                { x: -16, y: 1, label: 'V4', start: 0, display: 0 },
                { x: -15, y: 0, label: 'V5', start: 0, display: 0 },
                { x: 15, y: 0, label: 'V6', start: 0, display: 0 },
                { x: 16, y: 1, label: 'V7', start: 0, display: 0 },
                { x: 15, y: 2, label: 'V8', start: 0, display: 0 },
                { x: 1, y: 2, label: 'V9', start: 0, display: 0, radius: 0.5 },
                { x: 1, y: 2.5, label: 'V10', start: 0, display: 0 },
            ], 'arcs': []
        }],
    },*/
    'T-Junction': {
        path: [{
            'vertex': [
                { x: -1, y: 1, label: 'V1', start: 1, display: 1 },
                { x: 0, y: 0, label: 'V2', start: 0, display: 1 },
                { x: 1, y: 1, label: 'V3', start: 0, display: 1 },
            ], 'arcs': []
        }],
    },
    /*'LaneDrop': {
        path: [{
            'vertex': [
                { x: -1, y: 6.712, label: 'V1', start: 1, display: 0 },
                { x: -1.5, y: 5.846, label: 'V2', start: 0, display: 0 },
                { x: -8.625, y: 1.732, label: 'V3', start: 0, display: 0 },
                { x: -8.991, y: 0.366, label: 'V4', start: 0, display: 0 },
                { x: -7.625, y: 0, label: 'V5', start: 0, display: 0 },
                { x: -0.5, y: 4.114, label: 'V6', start: 0, display: 0 },
                { x: 1, y: 6.712, label: 'V7', start: 0, display: 0 },
            ], 'arcs': [
                { start: 'V1', end: 'V2', radius: 1, direction: 0, sweep: 0 },
                { start: 'V6', end: 'V7', radius: 3, direction: 1, sweep: 0 },
            ]
        }],
    },*/
    'LaneDrop': {
        path: [{
            'vertex': [
                { x: -1, y: 1, label: 'V1', start: 1, display: 1 },
                { x: 0, y: 0, label: 'V2', start: 0, display: 1 },
                { x: 1, y: 1, label: 'V3', start: 0, display: 1 },
            ], 'arcs': []
        }],
    },
    /*'Bifurcation': {
        path: [{
            'vertex': [
                { x: -1, y: 8.262, label: 'V1', start: 1, display: 0, radius: 0.333 },
                { x: -5.244, y: 0.911, label: 'V2', start: 0, display: 0 },
                { x: -5, y: 0, label: 'V3', start: 0, display: 0 },
                { x: -4.089, y: 0.244, label: 'V4', start: 0, display: 0 },
                { x: 0, y: 7.327, label: 'V5', start: 0, display: 0, radius: 0.667 },
                { x: 4.089, y: 0.244, label: 'V6', start: 0, display: 0 },
                { x: 5, y: 0, label: 'V7', start: 0, display: 0 },
                { x: 5.244, y: 0.911, label: 'V8', start: 0, display: 0 },
                { x: 1, y: 8.262, label: 'V9', start: 0, display: 0, radius: 0.333 },

            ], 'arcs': [
            ]
        }],
    },*/
    'Y-Junction': {
        path: [{
            'vertex': [
                { x: -1, y: 1, label: 'V1', start: 1, display: 1 },
                { x: 0, y: 0, label: 'V2', start: 0, display: 1 },
                { x: 1, y: 1, label: 'V3', start: 0, display: 1 },
            ], 'arcs': []
        }],
    },
    'UArrow Conventional': {
        path: [{
            'vertex': [
                { x: 6, y: 33.4, label: 'V31', start: 1, display: 1 },
                { x: 8, y: 31.4, label: 'V32', start: 0, display: 1 },
                { x: 8, y: 9.3808, label: 'V33', start: 0, display: 1 },
                { x: 8.3077, y: 8.6592, label: 'V34', start: 0, display: 0 },
                { x: 2.7692, y: 11.6761, label: 'V35', start: 0, display: 0 },
                { x: 4, y: 12.6491, label: 'V36', start: 0, display: 0 },
                { x: 4, y: 31.4, label: 'V37', start: 0, display: 1 },
            ], 'arcs': [
                { start: 'V33', end: 'V34', radius: 1, direction: 1, sweep: 0 },
                { start: 'V34', end: 'V35', radius: 12, direction: 1, sweep: 0 },
                { start: 'V35', end: 'V36', radius: 1, direction: 1, sweep: 0 },
            ]
        },],
    },
    'UArrow Spiral': {
        path: [{
            'vertex': [
                { x: 6, y: 35.4, label: 'V31', start: 1, display: 1 },
                { x: 8, y: 33.4, label: 'V32', start: 0, display: 1 },
                { x: 8, y: 15.8745, label: 'V33', start: 0, display: 1 },
                { x: 10.5, y: 9.2601, label: 'V34', start: 0, display: 1 },
                { x: 4.2303, y: 13.3456, label: 'V35', start: 0, display: 0 },
                { x: 4, y: 15.8745, label: 'V36', start: 0, display: 0 },
                { x: 4, y: 33.4, label: 'V37', start: 0, display: 1 },
            ], 'arcs': [
                { start: 'V33', end: 'V34', radius: 10, direction: 1, sweep: 0 },
                { start: 'V34', end: 'V35', radius: 14, direction: 1, sweep: 0 },
                { start: 'V35', end: 'V36', radius: 16, direction: 0, sweep: 0 },
            ]
        },
        ],
    },

    'Spiral Arrow': {
        path: [{
            'vertex': [
                { x: 2.092, y: 0.091, label: 'V1', start: 1, display: 1 },
                { x: 3.917, y: 2.266, label: 'V2', start: 0, display: 1 },
                { x: 0.841, y: 10.025, label: 'V3', start: 0, display: 1 },
                { x: -6.949, y: 11.846, label: 'V4', start: 0, display: 1 },
                { x: -0.075, y: 1.909, label: 'V5', start: 0, display: 1 },
            ], 'arcs': [
                { start: 'V2', end: 'V3', radius: 18, direction: 1, sweep: 0 },
                { start: 'V3', end: 'V4', radius: 14, direction: 0, sweep: 0 },
                { start: 'V4', end: 'V5', radius: 14, direction: 0, sweep: 0 },
            ]
        }],
    },

};


function baseSideRoadTemplate(baseShape, rootLength) {
    // Shape here are rotated 180 degree pointing downwards to the roundel
    // origin is at the bottom of the side road tip
    switch (baseShape) {
        case 'Base Conventional Normal':
            return {
                path: [{
                    'vertex': [
                        { x: 0, y: 0, label: 'V1', start: 0, display: 1 },
                        { x: -3, y: 0, label: 'V2', start: 1, display: 1 },
                        { x: -3, y: rootLength - 12.3693, label: 'V3', start: 0, display: 0 },
                        { x: -3.6923, y: rootLength - 11.4178, label: 'V4', start: 0, display: 0 },
                        { x: 3.6923, y: rootLength - 11.4178, label: 'V5', start: 0, display: 0 },
                        { x: 3, y: rootLength - 12.3693, label: 'V6', start: 0, display: 0 },
                        { x: 3, y: 0, label: 'V7', start: 0, display: 1 },
                    ], 'arcs': [
                        { start: 'V3', end: 'V4', radius: 1, direction: 1, sweep: 0 },
                        { start: 'V4', end: 'V5', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V5', end: 'V6', radius: 1, direction: 1, sweep: 0 },
                    ]
                }]
            };
        case 'Base Conventional U-turn':
            return {
                path: [{
                    'vertex': [
                        { x: 6, y: 0, label: 'V1', start: 1, display: 1 },
                        { x: 8, y: 0, label: 'V2', start: 0, display: 1 },
                        { x: 8, y: rootLength - 9.3808, label: 'V13', start: 0, display: 0 },
                        { x: 8.3077, y: rootLength - 8.6592, label: 'V14', start: 0, display: 0 },
                        { x: 2.7692, y: rootLength - 11.6761, label: 'V15', start: 0, display: 0 },
                        { x: 4, y: rootLength - 12.6491, label: 'V16', start: 0, display: 0 },
                        { x: 4, y: 0, label: 'V17', start: 0, display: 1 },
                    ], 'arcs': [
                        { start: 'V13', end: 'V14', radius: 1, direction: 0, sweep: 0 },
                        { start: 'V14', end: 'V15', radius: 12, direction: 0, sweep: 0 },
                        { start: 'V15', end: 'V16', radius: 1, direction: 0, sweep: 0 },
                    ]
                },]
            }
        case 'Base Conventional Auxiliary':
            return {
                path: [
                    {
                        'vertex': [
                            { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                            { x: 3, y: 0, label: 'V2', start: 0, display: 1 },
                            { x: 3, y: rootLength - 20.3345, label: 'V3', start: 0, radius: 4, display: 1 },
                            { x: 19.3345, y: rootLength - 4, label: 'V4', start: 0, radius: 4, display: 1 },
                            { x: 27, y: rootLength - 4, label: 'V5', start: 0, display: 1 },
                            { x: 30, y: rootLength - 1, label: 'V6', start: 0, display: 1 },
                            { x: 27, y: rootLength + 2, label: 'V7', start: 0, display: 1 },
                            { x: 12.6491, y: rootLength + 2, label: 'V8', start: 0, display: 0 },
                            { x: 11.6761, y: rootLength + 2.7692, label: 'V9', start: 0, display: 0 },
                            { x: 11.6761, y: rootLength - 2.7692, label: 'V10', start: 0, display: 0 },
                            { x: 12.6491, y: rootLength - 2, label: 'V11', start: 0, display: 0 },
                            { x: 17.7990, y: rootLength - 2, label: 'V12', start: 0, display: 0 },
                            { x: 3, y: rootLength - 16.7990, label: 'V13', start: 0, display: 0 },
                            { x: 3, y: rootLength - 12.3693, label: 'V14', start: 0, display: 0 },
                            { x: 3.6923, y: rootLength - 11.4178, label: 'V15', start: 0, display: 0 },
                            { x: -3.6923, y: rootLength - 11.4178, label: 'V16', start: 0, display: 0 },
                            { x: -3, y: rootLength - 12.3693, label: 'V17', start: 0, display: 0 },
                            { x: -3, y: 0, label: 'V18', start: 0, display: 1 },
                        ], 'arcs': [
                            { start: 'V8', end: 'V9', radius: 1, direction: 0, sweep: 0 },
                            { start: 'V9', end: 'V10', radius: 12, direction: 0, sweep: 0 },
                            { start: 'V10', end: 'V11', radius: 1, direction: 0, sweep: 0 },
                            { start: 'V14', end: 'V15', radius: 1, direction: 0, sweep: 0 },
                            { start: 'V15', end: 'V16', radius: 12, direction: 0, sweep: 0 },
                            { start: 'V16', end: 'V17', radius: 1, direction: 0, sweep: 0 },
                        ]
                    },
                ]
            }
        case 'Base Conventional Auxiliary -45':
            return {
                path: [
                    {
                        'vertex': [
                            { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                            { x: 3, y: 0, label: 'V2', start: 0, display: 1 },
                            { x: 3, y: rootLength - 20.3345, label: 'V3', start: 0, display: 1 },
                            { x: 16.1806, y: rootLength - 18.7695, label: 'V4', start: 0, display: 1 },
                            { x: 22.708, y: rootLength - 26.3436, label: 'V5', start: 0, display: 1 },
                            { x: 26.950, y: rootLength - 26.3436, label: 'V6', start: 0, display: 1 },
                            { x: 26.950, y: rootLength - 22.1036, label: 'V7', start: 0, display: 1 },
                            { x: 10.3585, y: rootLength - 7.5301, label: "V8", start: 0, display: 0 },
                            { x: 10.2144, y: rootLength - 6.2981, label: "V9", start: 0, display: 0 },
                            { x: 6.2981, y: rootLength - 10.2144, label: "V10", start: 0, display: 0 },
                            { x: 7.5301, y: rootLength - 10.3585, label: "V11", start: 0, display: 0 },
                            { x: 11.1716, y: rootLength - 14, label: "V12", start: 0, display: 0 },
                            { x: 3, y: rootLength - 16.7990, label: 'V13', start: 0, display: 0 },
                            { x: 3, y: rootLength - 12.3693, label: 'V14', start: 0, display: 0 },
                            { x: 3.6923, y: rootLength - 11.4178, label: 'V15', start: 0, display: 0 },
                            { x: -3.6923, y: rootLength - 11.4178, label: 'V16', start: 0, display: 0 },
                            { x: -3, y: rootLength - 12.3693, label: 'V17', start: 0, display: 0 },
                            { x: -3, y: 0, label: 'V18', start: 0, display: 1 },
                        ], 'arcs': [
                            { start: 'V3', end: 'V4', radius: 7.5, direction: 0, sweep: 0 },
                            { start: 'V8', end: 'V9', radius: 1, direction: 0, sweep: 0 },
                            { start: 'V9', end: 'V10', radius: 12, direction: 0, sweep: 0 },
                            { start: 'V10', end: 'V11', radius: 1, direction: 0, sweep: 0 },
                            { start: 'V12', end: 'V13', radius: 10, direction: 1, sweep: 0 },
                            { start: 'V14', end: 'V15', radius: 1, direction: 0, sweep: 0 },
                            { start: 'V15', end: 'V16', radius: 12, direction: 0, sweep: 0 },
                            { start: 'V16', end: 'V17', radius: 1, direction: 0, sweep: 0 },
                        ]
                    },
                ]
            }
        case 'Base Spiral Normal':
            return {
                path: [{
                    'vertex': [
                        { x: 2, y: 0, label: 'V1', start: 1, display: 1 },
                        { x: 2, y: rootLength - 20.785, label: 'V2', start: 0, display: 0 },
                        { x: 7, y: rootLength - 12.124, label: 'V3', start: 0, display: 0 },
                        { x: -2.392, y: rootLength - 13.794, label: 'V4', start: 0, display: 0 },
                        { x: -4, y: rootLength - 20.785, label: 'V5', start: 0, display: 0 },
                        { x: -4, y: 0, label: 'V6', start: 0, display: 1 },
                    ], 'arcs': [
                        { start: 'V2', end: 'V3', radius: 10, direction: 0, sweep: 0 },
                        { start: 'V3', end: 'V4', radius: 14, direction: 0, sweep: 0 },
                        { start: 'V4', end: 'V5', radius: 16, direction: 1, sweep: 0 },
                    ]
                },]
            }
        case 'Base Spiral U-turn':
            return {
                path: [{
                    'vertex': [
                        { x: 6, y: 0, label: 'V1', start: 1, display: 1 },
                        { x: 6, y: rootLength - 17.8885, label: 'V2', start: 0, display: 0 },
                        { x: 9.3333, y: rootLength - 10.4350, label: 'V3', start: 0, display: 0 },
                        { x: 2.6252, y: rootLength - 13.7517, label: 'V4', start: 0, display: 0 },
                        { x: 2, y: rootLength - 17.8885, label: 'V5', start: 0, display: 0 },
                        { x: 2, y: 0, label: 'V6', start: 0, display: 1 },
                    ], 'arcs': [
                        { start: 'V2', end: 'V3', radius: 10, direction: 0, sweep: 0 },
                        { start: 'V3', end: 'V4', radius: 14, direction: 0, sweep: 0 },
                        { start: 'V4', end: 'V5', radius: 16, direction: 1, sweep: 0 },
                    ]
                },]
            }
        case 'Base Spiral Auxiliary':
            return {
                path: [{
                    'vertex': [
                        { x: 2, y: 0, label: 'V1', start: 1, display: 1 },
                        { x: 2, y: rootLength - 24.1630, label: 'V2', start: 0, radius: 4, display: 1 },
                        { x: 24.3045, y: rootLength - 1.8584, label: 'V3', start: 0, radius: 4, display: 1 },
                        { x: 31.2484, y: rootLength - 1.2509, label: 'V4', start: 0, display: 1 },
                        { x: 34, y: rootLength + 1.9784, label: 'V5', start: 0, display: 1 },
                        { x: 30.7294, y: rootLength + 4.6809, label: 'V6', start: 0, display: 1 },
                        { x: 21.734, y: rootLength + 3.917, label: 'V7', start: 0, display: 0 },
                        { x: 13.975, y: rootLength + 0.841, label: 'V8', start: 0, display: 0 },
                        { x: 12.154, y: rootLength - 6.949, label: 'V9', start: 0, display: 0 },
                        { x: 22.6134, y: rootLength - 0.0140, label: 'V10', start: 0, display: 0 },
                        { x: 2, y: rootLength - 20.6274, label: 'V11', start: 0, display: 0 },
                        { x: 7, y: rootLength - 12.124, label: 'V12', start: 0, display: 0 },
                        { x: -2.392, y: rootLength - 13.794, label: 'V13', start: 0, display: 0 },
                        { x: -4, y: rootLength - 20.785, label: 'V14', start: 0, display: 0 },
                        { x: -4, y: 0, label: 'V15', start: 0, display: 1 },
                    ], 'arcs': [
                        { start: 'V7', end: 'V8', radius: 18, direction: 1, sweep: 0 },
                        { start: 'V8', end: 'V9', radius: 14, direction: 0, sweep: 0 },
                        { start: 'V9', end: 'V10', radius: 14, direction: 0, sweep: 0 },
                        { start: 'V11', end: 'V12', radius: 10, direction: 0, sweep: 0 },
                        { start: 'V12', end: 'V13', radius: 14, direction: 0, sweep: 0 },
                        { start: 'V13', end: 'V14', radius: 16, direction: 1, sweep: 0 },
                    ]
                },]
            }
        case 'Base Double Conventional':
            return {
                path: [{
                    'vertex': [
                        { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                        { x: 3, y: 0, label: 'V2', start: 0, display: 1 },
                        { x: 3, y: rootLength - 12.3693, label: 'V3', start: 0, display: 0 },
                        { x: 3.6923, y: rootLength - 11.4178, label: 'V4', start: 0, display: 0 },
                        { x: -3.6923, y: rootLength - 11.4178, label: 'V5', start: 0, display: 0 },
                        { x: -3, y: rootLength - 12.3693, label: 'V6', start: 0, display: 0 },
                        { x: -3, y: 0, label: 'V7', start: 0, display: 1 },
                        { x: 0, y: 0, label: 'V8', start: 0, display: 1 },
                    ], 'arcs': [
                        { start: 'V3', end: 'V4', radius: 1, direction: 0, sweep: 0 },
                        { start: 'V6', end: 'V5', radius: 12, direction: 0, sweep: 0 },
                        { start: 'V5', end: 'V6', radius: 1, direction: 0, sweep: 0 },
                    ],
                },]
            };

        case 'Base Double Spiral':
            return {
                path: [{
                    'vertex': [
                        { x: -1, y: 0, label: 'V1', start: 1, display: 1 },
                        { x: 2, y: 0, label: 'V2', start: 0, display: 1 },
                        { x: 2, y: rootLength - 20.785, label: 'V3', start: 0, display: 0 },
                        { x: 7, y: rootLength - 12.124, label: 'V4', start: 0, display: 1 },
                        { x: -2.392, y: rootLength - 13.794, label: 'V5', start: 0, display: 0 },
                        { x: -4, y: rootLength - 20.785, label: 'V6', start: 0, display: 0 },
                        { x: -4, y: 0, label: 'V7', start: 0, display: 1 },
                    ], 'arcs': [
                        { start: 'V3', end: 'V4', radius: 10, direction: 0, sweep: 0 },
                        { start: 'V4', end: 'V5', radius: 14, direction: 0, sweep: 0 },
                        { start: 'V5', end: 'V6', radius: 16, direction: 1, sweep: 0 },
                    ]
                },]
            };
        case 'Base Oval Normal 0':
        case 'Base Oval Normal 30':
        case 'Base Oval Normal 60':
        case 'Base Oval Normal -30':
        case 'Base Oval Normal -60':
            return {
                path: [{
                    'vertex': [
                        { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                        { x: 3, y: 0, label: 'V2', start: 0, display: 1 },
                        { x: 3, y: rootLength - 12.3693, label: 'V3', start: 0, display: 0 },
                        { x: 3.6923, y: rootLength - 11.4178, label: 'V4', start: 0, display: 0 },
                        { x: -3.6923, y: rootLength - 11.4178, label: 'V5', start: 0, display: 0 },
                        { x: -3, y: rootLength - 12.3693, label: 'V6', start: 0, display: 0 },
                        { x: -3, y: 0, label: 'V7', start: 0, display: 0 },
                        { x: -0, y: 0, label: 'V8', start: 0, display: 1 },
                    ], 'arcs': [
                        { start: 'V3', end: 'V4', radius: 1, direction: 0, sweep: 0 },
                        { start: 'V4', end: 'V5', radius: 12, direction: 0, sweep: 0 },
                        { start: 'V5', end: 'V6', radius: 1, direction: 0, sweep: 0 },
                    ],
                }]
            };
        case 'Base Oval Normal 90 Left':
            return {
                path: [{
                    'vertex': [
                        { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                        { x: 3, y: 0, label: 'V2', start: 0, display: 1 },
                        { x: 3, y: rootLength - 12.3693, label: 'V3', start: 0, display: 0 },
                        { x: 3.6923, y: rootLength - 11.4178, label: 'V4', start: 0, display: 0 },
                        { x: -0, y: rootLength - 12, label: "V5", start: 0, display: 0 },
                        { x: -4, y: rootLength - 12, label: 'V6', start: 0, display: 0 },
                        { x: -3, y: rootLength - 13, label: 'V7', start: 0, display: 0 },
                        { x: -3, y: 0, label: 'V8', start: 0, display: 1 },
                    ], 'arcs': [
                        { start: 'V3', end: 'V4', radius: 1, direction: 0, sweep: 0 },
                        { start: 'V4', end: 'V5', radius: 12, direction: 0, sweep: 0 },
                        { start: 'V6', end: 'V7', radius: 1, direction: 0, sweep: 0 },
                    ],
                }]
            };
        case 'Base Oval Normal 90 Middle':
            return {
                path: [{
                    'vertex': [
                        { x: -12, y: 0, label: 'V1', start: 1, display: 1 },
                        { x: -9, y: 0, label: 'V2', start: 0, display: 1 },
                        { x: -9, y: rootLength - 13, label: 'V3', start: 0, display: 0 },
                        { x: -8, y: rootLength - 12, label: 'V4', start: 0, display: 0 },
                        { x: -12, y: rootLength - 12, label: "V5", start: 0, display: 0 },
                        { x: -16, y: rootLength - 12, label: 'V6', start: 0, display: 0 },
                        { x: -15, y: rootLength - 13, label: 'V7', start: 0, display: 0 },
                        { x: -15, y: 0, label: 'V8', start: 0, display: 1 },
                    ], 'arcs': [
                        { start: 'V3', end: 'V4', radius: 1, direction: 0, sweep: 0 },
                        { start: 'V6', end: 'V7', radius: 1, direction: 0, sweep: 0 },
                    ],
                }]
            };
        case 'Base Oval Normal -90':
            return {
                path: [{
                    'vertex': [
                        { x: -24, y: 0, label: 'V1', start: 1, display: 1 },
                        { x: -27, y: 0, label: 'V2', start: 0, display: 1 },
                        { x: -27, y: rootLength - 12.3693, label: 'V3', start: 0, display: 0 },
                        { x: -27.6923, y: rootLength - 11.4178, label: 'V4', start: 0, display: 0 },
                        { x: -24, y: rootLength - 12, label: "V5", start: 0, display: 0 },
                        { x: -20, y: rootLength - 12, label: 'V6', start: 0, display: 0 },
                        { x: -21, y: rootLength - 13, label: 'V7', start: 0, display: 0 },
                        { x: -21, y: 0, label: 'V8', start: 0, display: 1 },
                    ], 'arcs': [
                        { start: 'V3', end: 'V4', radius: 1, direction: 1, sweep: 0 },
                        { start: 'V4', end: 'V5', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V6', end: 'V7', radius: 1, direction: 1, sweep: 0 },
                    ],
                }]
            };

    }

}




function roundelTemplate(type, rootLength) {
    switch (type) {
        case 'Normal Conventional':
        case 'Auxiliary Conventional':
        case 'Auxiliary -45 Conventional':
            return {
                path: [
                    {
                        'vertex': [
                            { x: 0, y: 0, label: 'C1', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: 0, y: 0, label: 'C1', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: -3, y: Math.sqrt(144 - 9), label: 'V1', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: 3, y: Math.sqrt(144 - 9), label: 'V3', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: 0, y: -12, label: 'V2', start: 1, display: 1 }, // Original center point
                            { x: 12, y: 0, label: 'V20', start: 0, display: 1 }, // Original center point
                            { x: 10.3923, y: 6, label: 'V21', start: 0, display: 0 },
                            { x: 6.0622, y: 3.5, label: 'V22', start: 0, display: 0 },
                            { x: 3.5, y: 6.0622, label: 'V23', start: 0, display: 0 },
                            { x: 6, y: 10.3923, label: 'V24', start: 0, display: 0 },
                            { x: -12, y: 0, label: 'V25', start: 0, display: 1 },
                        ], 'arcs': [
                            { start: 'V2', end: 'V20', radius: 12, direction: 1, sweep: 0 },
                            { start: 'V20', end: 'V21', radius: 12, direction: 1, sweep: 0 },
                            { start: 'V22', end: 'V23', radius: 7, direction: 0, sweep: 1 },
                            { start: 'V24', end: 'V25', radius: 12, direction: 1, sweep: 0 },
                            { start: 'V25', end: 'V2', radius: 12, direction: 1, sweep: 0 },
                        ], //'centerLine': [
                        // { x: 4.75, y: 8.2273, label: 'C2', width: 5, start: 1, display: 1 },
                        // { x: 8.2273, y: 4.75, label: 'C3', width: 5, start: 0, display: 1 },
                        // ], 'centerArc': [
                        // { start: 'C2', end: 'C3', radius: 9.5, direction: 1, sweep: 1 }]
                    },
                ],
            }
        case 'U-turn Conventional':
            return {
                path: [
                    {
                        'vertex': [
                            { x: 0, y: 0, label: 'C1', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: 8, y: Math.sqrt(144 - 64), label: 'V3', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: -8, y: Math.sqrt(144 - 64), label: 'V1', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: 0, y: -12, label: 'V2', start: 1, display: 1 }, // Original center point
                            { x: 12, y: 0, label: 'V20', start: 0, display: 1 },
                            { x: 2.0838, y: 11.8177, label: 'V21', start: 0, display: 0 },
                            { x: 1.2155, y: 6.8937, label: 'V22', start: 0, display: 0 },
                            { x: -1.2155, y: 6.8937, label: 'V23', start: 0, display: 0 },
                            { x: -2.0838, y: 11.8177, label: 'V24', start: 0, display: 0 },
                            { x: -12, y: 0, label: 'V25', start: 0, display: 1 },
                        ], 'arcs': [
                            { start: 'V2', end: 'V20', radius: 12, direction: 1, sweep: 0 },
                            { start: 'V20', end: 'V21', radius: 12, direction: 1, sweep: 0 },
                            { start: 'V22', end: 'V23', radius: 7, direction: 0, sweep: 1 },
                            { start: 'V24', end: 'V25', radius: 12, direction: 1, sweep: 0 },
                            { start: 'V25', end: 'V2', radius: 12, direction: 1, sweep: 0 },
                        ]
                    }
                ],
            }
        case 'Auxiliary Spiral':
        case 'Normal Spiral':
            return {
                path: [
                    {
                        'vertex': [
                            { x: 0, y: 0, label: 'C1', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: 2.392, y: 13.794, label: 'V1', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: -7, y: 12.124, label: 'V3', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: 0, y: -14, label: 'V11', start: 1, display: 1 }, // Original center point
                            { x: 14, y: 0, label: 'V12', start: 0, display: 1 },
                            { x: 12.1244, y: 7, label: 'V13', start: 0, display: 0 },
                            { x: 8.6603, y: 5, label: 'V14', start: 0, display: 0 },
                            { x: 5, y: 8.6603, label: 'V15', start: 0, display: 0 },
                            { x: 7, y: 12.1244, label: 'V16', start: 0, display: 0 },
                            { x: -14, y: 0, label: 'V17', start: 0, display: 1 },
                        ], 'arcs': [
                            { start: 'V11', end: 'V12', radius: 14, direction: 1, sweep: 0 },
                            { start: 'V12', end: 'V13', radius: 14, direction: 1, sweep: 0 },
                            { start: 'V14', end: 'V15', radius: 10, direction: 0, sweep: 1 },
                            { start: 'V16', end: 'V17', radius: 14, direction: 1, sweep: 0 },
                            { start: 'V17', end: 'V11', radius: 14, direction: 1, sweep: 0 },
                        ]
                    },
                ],
            }
        case 'U-turn Spiral':
            return {
                path: [
                    {
                        'vertex': [
                            { x: 0, y: 0, label: 'C1', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: 9.3333, y: 10.435, label: 'V1', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: -9.3333, y: 10.435, label: 'V3', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: 0, y: -14, label: 'V11', start: 1, display: 1 }, // Original center point
                            { x: 14, y: 0, label: 'V12', start: 0, display: 1 },
                            { x: 2, y: 13.8564, label: 'V13', start: 0, display: 0 },
                            { x: 2, y: 9.7980, label: 'V14', start: 0, display: 0 },
                            { x: 0, y: 10, label: 'V15', start: 0, display: 0 },
                            { x: 0, y: 14, label: 'V16', start: 0, display: 0 },
                            { x: -14, y: 0, label: 'V17', start: 0, display: 1 },
                        ], 'arcs': [
                            { start: 'V11', end: 'V12', radius: 14, direction: 1, sweep: 0 },
                            { start: 'V12', end: 'V13', radius: 14, direction: 1, sweep: 0 },
                            { start: 'V14', end: 'V15', radius: 10, direction: 0, sweep: 1 },
                            { start: 'V16', end: 'V17', radius: 14, direction: 1, sweep: 0 },
                            { start: 'V17', end: 'V11', radius: 14, direction: 1, sweep: 0 },
                        ]
                    },
                ],
            }

        case 'Normal 0 Oval':
            return {
                path: [{
                    'vertex': [
                        { x: 0, y: -36, label: 'V1', start: 1, display: 1 },
                        { x: 12, y: -24, label: 'V2', start: 0, display: 1 },
                        { x: 12, y: -12, label: 'V3', start: 0, display: 1 },
                        { x: 12, y: 0, label: 'V4', start: 0, display: 1 },
                        { x: 10.392, y: 6, label: 'V5', start: 0, display: 0 },  // notched
                        { x: 6.062, y: 3.5, label: 'V6', start: 0, display: 0 }, // notched
                        { x: 7, y: 0, label: 'V7', start: 0, display: 0 },
                        { x: 7, y: -24, label: 'V9', start: 0, display: 0 },
                        { x: -7, y: -24, label: 'V11', start: 0, display: 0 },
                        { x: -7, y: 0, label: 'V13', start: 0, display: 0 },
                        { x: 3.5, y: 6.062, label: 'V14', start: 0, display: 0 }, // notched
                        { x: 6, y: 10.392, label: 'V15', start: 0, display: 0 }, // notched
                        { x: 0, y: 12, label: 'V16', start: 0, display: 1 },
                        { x: -12, y: 0, label: 'V17', start: 0, display: 1 },
                        { x: -12, y: -12, label: 'V18', start: 0, display: 1 },
                        { x: -12, y: -24, label: 'V19', start: 0, display: 1 },
                    ], 'arcs': [
                        { start: 'V1', end: 'V2', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V4', end: 'V5', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V6', end: 'V7', radius: 7, direction: 0, sweep: 0 },
                        { start: 'V9', end: 'V11', radius: 7, direction: 0, sweep: 0 },
                        { start: 'V13', end: 'V14', radius: 7, direction: 0, sweep: 0 },
                        { start: 'V15', end: 'V16', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V16', end: 'V17', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V19', end: 'V1', radius: 12, direction: 1, sweep: 0 },
                    ]
                },
                {
                    'vertex': [
                        { x: 0, y: 0, label: 'C1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 0, y: -24, label: 'C2', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: -3, y: Math.sqrt(144 - 9), label: 'V1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 3, y: Math.sqrt(144 - 9), label: 'V3', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                ]
            }
        case 'Normal 30 Oval':
            return {
                path: [{
                    'vertex': [
                        { x: 18, y: -31.1769, label: "V1", start: 1, display: 1 },
                        { x: 22.3923, y: -14.7846, label: "V2", start: 0, display: 1 },
                        { x: 16.3923, y: -4.3923, label: "V3", start: 0, display: 1 },
                        { x: 10.392, y: 6, label: 'V5', start: 0, display: 1 },  // notched
                        { x: 6.062, y: 3.5, label: 'V6', start: 0, display: 0 }, // notched
                        { x: 18.0622, y: -17.2846, label: "V9", start: 0, display: 0 },
                        { x: 5.9378, y: -24.2846, label: "V11", start: 0, display: 0 },
                        { x: -6.0622, y: -3.5, label: "V13", start: 0, display: 0 },
                        { x: 3.5, y: 6.062, label: 'V14', start: 0, display: 0 }, // notched
                        { x: 6, y: 10.392, label: 'V15', start: 0, display: 0 }, // notched
                        { x: -6, y: 10.3923, label: "V16", start: 0, display: 1 },
                        { x: -10.3923, y: -6, label: "V17", start: 0, display: 1 },
                        { x: -4.3923, y: -16.3923, label: "V18", start: 0, display: 1 },
                        { x: 1.6077, y: -26.7846, label: "V19", start: 0, display: 1 }
                    ], 'arcs': [
                        { start: 'V1', end: 'V2', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V9', end: 'V11', radius: 7, direction: 0, sweep: 0 },
                        { start: 'V13', end: 'V14', radius: 7, direction: 0, sweep: 0 },
                        { start: 'V15', end: 'V16', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V16', end: 'V17', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V19', end: 'V1', radius: 12, direction: 1, sweep: 0 },
                    ]
                },
                {
                    'vertex': [
                        { x: 0, y: 0, label: 'C1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 12, y: -20.7846, label: 'C2', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: -3, y: Math.sqrt(144 - 9), label: 'V1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 3, y: Math.sqrt(144 - 9), label: 'V3', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                ]
            }
        case 'Normal 60 Oval':
            return {
                path: [{
                    'vertex': [
                        { x: 31.1769, y: -18, label: "V1", start: 1, display: 1 },
                        { x: 26.7846, y: -1.6077, label: "V2", start: 0, display: 1 },
                        { x: 16.3923, y: 4.3923, label: "V3", start: 0, display: 1 },
                        { x: 9.5, y: 8.372, label: 'V4', start: 0, display: 1 }, // notched
                        { x: 7, y: 4.042, label: 'V5', start: 0, display: 0 },  // notched
                        { x: 24.2846, y: -5.9378, label: "V9", start: 0, display: 0 },
                        { x: 17.2846, y: -18.0622, label: "V11", start: 0, display: 0 },
                        { x: -3.5, y: -6.0622, label: "V13", start: 0, display: 0 },
                        { x: 3.5, y: 6.062, label: 'V14', start: 0, display: 0 }, // notched
                        { x: 6, y: 10.392, label: 'V15', start: 0, display: 1 }, // notched
                        { x: -10.3923, y: 6, label: "V16", start: 0, display: 1 },
                        { x: -6, y: -10.3923, label: "V17", start: 0, display: 1 },
                        { x: 4.3923, y: -16.3923, label: "V18", start: 0, display: 1 },
                        { x: 14.7846, y: -22.3923, label: "V19", start: 0, display: 1 }
                    ], 'arcs': [
                        { start: 'V1', end: 'V2', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V6', end: 'V7', radius: 7, direction: 0, sweep: 0 },
                        { start: 'V9', end: 'V11', radius: 7, direction: 0, sweep: 0 },
                        { start: 'V13', end: 'V14', radius: 7, direction: 0, sweep: 0 },
                        { start: 'V15', end: 'V16', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V16', end: 'V17', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V19', end: 'V1', radius: 12, direction: 1, sweep: 0 },
                    ]
                },
                {
                    'vertex': [
                        { x: 0, y: 0, label: 'C1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 20.7846, y: -12, label: 'C2', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: -3, y: Math.sqrt(144 - 9), label: 'V1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 3, y: Math.sqrt(144 - 9), label: 'V3', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                ]
            }
        case 'Normal 90 Left Oval':
            return {
                path: [{
                    'vertex': [
                        { x: 36, y: 0, label: "V1", start: 1, display: 1 },
                        { x: 24, y: 12, label: "V2", start: 0, display: 1 },
                        { x: 14.5, y: 12, label: 'V5', start: 0, display: 0 },  // notched
                        { x: 14.5, y: 7, label: 'V6', start: 0, display: 0 }, // notched
                        { x: 24, y: 7, label: "V9", start: 0, display: 0 },
                        { x: 24, y: -7, label: "V11", start: 0, display: 0 },
                        { x: 0, y: -7, label: "V13", start: 0, display: 0 },
                        { x: 0, y: 7, label: "V7", start: 0, display: 0 },
                        { x: 9.5, y: 7, label: 'V14', start: 0, display: 0 }, // notched
                        { x: 9.5, y: 12, label: 'V15', start: 0, display: 0 }, // notched
                        { x: 0, y: 12, label: "V4", start: 0, display: 1 },
                        { x: -12, y: 0, label: "V16", start: 0, display: 1 },
                        { x: 0, y: -12, label: "V17", start: 0, display: 1 },
                        { x: 12, y: -12, label: "V18", start: 0, display: 1 },
                        { x: 24, y: -12, label: "V19", start: 0, display: 1 }
                    ], 'arcs': [
                        { start: 'V1', end: 'V2', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V9', end: 'V11', radius: 7, direction: 0, sweep: 0 },
                        { start: 'V13', end: 'V7', radius: 7, direction: 0, sweep: 0 },
                        { start: 'V4', end: 'V16', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V16', end: 'V17', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V19', end: 'V1', radius: 12, direction: 1, sweep: 0 },
                    ]
                },
                {
                    'vertex': [
                        { x: 0, y: 0, label: 'C1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 24, y: 0, label: 'C2', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 9, y: Math.sqrt(144 - 9), label: 'V1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 15, y: 12, label: 'V3', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                ]
            }
        case 'Normal 90 Middle Oval':
            return {
                path: [{
                    'vertex': [
                        { x: 36, y: 0, label: "V1", start: 1, display: 1 },
                        { x: 34.392, y: 6, label: 'V5', start: 0, display: 0 },  // notched
                        { x: 30.062, y: 3.5, label: 'V6', start: 0, display: 0 }, // notched
                        { x: 24, y: -7, label: "V11", start: 0, display: 0 },
                        { x: 0, y: -7, label: "V13", start: 0, display: 0 },
                        { x: 0, y: 7, label: "V7", start: 0, display: 0 },
                        { x: 24, y: 7, label: "V9", start: 0, display: 0 },
                        { x: 27.5, y: 6.062, label: 'V14', start: 0, display: 0 }, // notched
                        { x: 30, y: 10.392, label: 'V15', start: 0, display: 0 }, // notched
                        { x: 24, y: 12, label: "V2", start: 0, display: 1 },
                        { x: 12, y: 12, label: "V3", start: 0, display: 1 },
                        { x: 0, y: 12, label: "V4", start: 0, display: 1 },
                        { x: -12, y: 0, label: "V16", start: 0, display: 1 },
                        { x: 0, y: -12, label: "V17", start: 0, display: 1 },
                        { x: 12, y: -12, label: "V18", start: 0, display: 1 },
                        { x: 24, y: -12, label: "V19", start: 0, display: 1 }
                    ], 'arcs': [
                        { start: 'V1', end: 'V2', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V4', end: 'V5', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V6', end: 'V7', radius: 7, direction: 0, sweep: 0 },
                        { start: 'V9', end: 'V11', radius: 7, direction: 0, sweep: 0 },
                        { start: 'V13', end: 'V14', radius: 7, direction: 0, sweep: 0 },
                        { start: 'V15', end: 'V16', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V16', end: 'V17', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V19', end: 'V1', radius: 12, direction: 1, sweep: 0 },
                    ]
                },
                {
                    'vertex': [
                        { x: 0, y: 0, label: 'C1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 24, y: 0, label: 'C2', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: -3, y: 12, label: 'V1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 3, y: 12, label: 'V3', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                ]
            }
        case 'Normal -30 Oval':
            return {
                path: [{
                    'vertex': [
                        { x: -18, y: -31.1769, label: "V1", start: 1, display: 1 },
                        { x: -22.3923, y: -14.7846, label: "V2", start: 0, display: 1 },
                        { x: -16.3923, y: -4.3923, label: "V3", start: 0, display: 1 },
                        { x: -10.3923, y: 6, label: "V4", start: 0, display: 1 },
                        { x: 6, y: 10.392, label: 'V5', start: 0, display: 0 }, // notched
                        { x: 3.5, y: 6.062, label: 'V6', start: 0, display: 0 }, // notched
                        { x: -6.0622, y: 3.5, label: "V7", start: 0, display: 0 },
                        { x: -18.0622, y: -17.2846, label: "V9", start: 0, display: 0 },
                        { x: -5.9378, y: -24.2846, label: "V11", start: 0, display: 0 },
                        { x: 6.0622, y: -3.5, label: "V13", start: 0, display: 0 },
                        { x: 6.062, y: 3.5, label: 'V14', start: 0, display: 0 }, // notched
                        { x: 10.392, y: 6, label: 'V15', start: 0, display: 0 },  // notched
                        { x: 10.3923, y: -6, label: "V17", start: 0, display: 1 },
                        { x: 4.3923, y: -16.3923, label: "V18", start: 0, display: 1 },
                        { x: -1.6077, y: -26.7846, label: "V19", start: 0, display: 1 }
                    ], 'arcs': [
                        { start: 'V1', end: 'V2', radius: 12, direction: 0, sweep: 0 },
                        { start: 'V4', end: 'V5', radius: 12, direction: 0, sweep: 0 },
                        { start: 'V6', end: 'V7', radius: 7, direction: 1, sweep: 0 },
                        { start: 'V9', end: 'V11', radius: 7, direction: 1, sweep: 0 },
                        { start: 'V13', end: 'V14', radius: 7, direction: 1, sweep: 0 },
                        { start: 'V15', end: 'V17', radius: 12, direction: 0, sweep: 0 },
                        { start: 'V19', end: 'V1', radius: 12, direction: 0, sweep: 0 },
                    ]
                },
                {
                    'vertex': [
                        { x: 0, y: 0, label: 'C1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: -12, y: -20.7846, label: 'C2', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: -3, y: Math.sqrt(144 - 9), label: 'V1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 3, y: Math.sqrt(144 - 9), label: 'V3', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                ]
            }
        case 'Normal -60 Oval':
            return {
                path: [{
                    'vertex': [
                        { x: -31.1769, y: -18, label: "V1", start: 1, display: 1 },
                        { x: -26.7846, y: -1.6077, label: "V2", start: 0, display: 1 },
                        { x: -16.3923, y: 4.3923, label: "V3", start: 0, display: 1 },
                        { x: -6, y: 10.3923, label: "V4", start: 0, display: 1 },
                        { x: 6, y: 10.392, label: 'V5', start: 0, display: 0 }, // notched
                        { x: 3.5, y: 6.062, label: 'V6', start: 0, display: 0 }, // notched
                        { x: -3.5, y: 6.0622, label: "V7", start: 0, display: 0 },
                        { x: -24.2846, y: -5.9378, label: "V9", start: 0, display: 0 },
                        { x: -17.2846, y: -18.0622, label: "V11", start: 0, display: 0 },
                        { x: 3.5, y: -6.0622, label: "V13", start: 0, display: 0 },
                        { x: 7, y: 4.042, label: 'V14', start: 0, display: 0 },  // notched
                        { x: 10.3923, y: 6, label: "V16", start: 0, display: 1 },
                        { x: 6, y: -10.3923, label: "V17", start: 0, display: 1 },
                        { x: -4.3923, y: -16.3923, label: "V18", start: 0, display: 1 },
                        { x: -14.7846, y: -22.3923, label: "V19", start: 0, display: 1 }
                    ], 'arcs': [
                        { start: 'V1', end: 'V2', radius: 12, direction: 0, sweep: 0 },
                        { start: 'V4', end: 'V5', radius: 12, direction: 0, sweep: 0 },
                        { start: 'V6', end: 'V7', radius: 7, direction: 1, sweep: 0 },
                        { start: 'V9', end: 'V11', radius: 7, direction: 1, sweep: 0 },
                        { start: 'V13', end: 'V14', radius: 7, direction: 1, sweep: 0 },
                        { start: 'V16', end: 'V17', radius: 12, direction: 0, sweep: 0 },
                        { start: 'V19', end: 'V1', radius: 12, direction: 0, sweep: 0 },
                    ]
                },
                {
                    'vertex': [
                        { x: 0, y: 0, label: 'C1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: -20.7846, y: -12, label: 'C2', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: -3, y: Math.sqrt(144 - 9), label: 'V1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 3, y: Math.sqrt(144 - 9), label: 'V3', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                ]
            }

        case 'Normal -90 Oval':
            return {
                path: [{
                    'vertex': [
                        { x: 36, y: 0, label: "V1", start: 1, display: 1 },
                        { x: 24, y: 12, label: "V2", start: 0, display: 1 },
                        { x: 14.5, y: 12, label: 'V5', start: 0, display: 0 },  // notched
                        { x: 14.5, y: 7, label: 'V6', start: 0, display: 0 }, // notched
                        { x: 24, y: 7, label: "V9", start: 0, display: 0 },
                        { x: 24, y: -7, label: "V11", start: 0, display: 0 },
                        { x: 0, y: -7, label: "V13", start: 0, display: 0 },
                        { x: 0, y: 7, label: "V7", start: 0, display: 0 },
                        { x: 9.5, y: 7, label: 'V14', start: 0, display: 0 }, // notched
                        { x: 9.5, y: 12, label: 'V15', start: 0, display: 0 }, // notched
                        { x: 0, y: 12, label: "V4", start: 0, display: 1 },
                        { x: -12, y: 0, label: "V16", start: 0, display: 1 },
                        { x: 0, y: -12, label: "V17", start: 0, display: 1 },
                        { x: 12, y: -12, label: "V18", start: 0, display: 1 },
                        { x: 24, y: -12, label: "V19", start: 0, display: 1 }
                    ], 'arcs': [
                        { start: 'V1', end: 'V2', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V9', end: 'V11', radius: 7, direction: 0, sweep: 0 },
                        { start: 'V13', end: 'V7', radius: 7, direction: 0, sweep: 0 },
                        { start: 'V4', end: 'V16', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V16', end: 'V17', radius: 12, direction: 1, sweep: 0 },
                        { start: 'V19', end: 'V1', radius: 12, direction: 1, sweep: 0 },
                    ]
                },
                {
                    'vertex': [
                        { x: 24, y: 0, label: 'C1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 0, y: 0, label: 'C2', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 21, y: 12, label: 'V1', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                {
                    'vertex': [
                        { x: 27, y: Math.sqrt(144 - 9), label: 'V3', start: 1, display: 1 }, // Center point for tracking
                    ], 'arcs': []
                },
                ]
            }

        case 'Conventional Double':
            return {
                path: [
                    {
                        // Top Circle
                        'vertex': [
                            { x: 0, y: -40, label: 'V1', start: 1, display: 1 },
                            { x: 10.3923, y: -22, label: 'V3', start: 0, display: 0 },      // notched
                            { x: 6.0622, y: -24.5, label: 'V4', start: 0, display: 0 },     // notched
                            { x: 3.5, y: -21.9378, label: 'V5', start: 0, display: 0 },     // notched
                            { x: 6, y: -17.6077, label: 'V6', start: 0, display: 0 },       // notched
                            { x: 3.2334, y: -16.4438, label: 'V20', start: 0, display: 0 },
                            { x: 2.5, y: -15.48, label: 'V7', start: 0, display: 0 },
                            { x: 2.5, y: -12.52, label: 'V8', start: 0, display: 0 },
                            { x: 3.2334, y: -11.5562, label: 'V21', start: 0, display: 0 },
                            { x: 10.3923, y: 6, label: 'V10', start: 0, display: 0 },       // notched
                            { x: 6.0622, y: 3.5, label: 'V11', start: 0, display: 0 },      // notched
                            { x: 3.5, y: 6.0622, label: 'V12', start: 0, display: 0 },      // notched
                            { x: 6, y: 10.3923, label: 'V13', start: 0, display: 0 },       // notched
                            { x: -3.2334, y: -11.5562, label: 'V22', start: 0, display: 0 },
                            { x: -2.5, y: -12.52, label: 'V16', start: 0, display: 0 },
                            { x: -2.5, y: -15.48, label: 'V17', start: 0, display: 0 },
                            { x: -3.2334, y: -16.4438, label: 'V23', start: 0, display: 0 },
                        ],
                        'arcs': [
                            { start: 'V1', end: 'V3', radius: 12, direction: 1, sweep: 0 },
                            { start: 'V4', end: 'V5', radius: 7, direction: 0, sweep: 1 },
                            { start: 'V6', end: 'V20', radius: 12, direction: 1, sweep: 0 },
                            { start: 'V20', end: 'V7', radius: 1, direction: 0, sweep: 0 },
                            { start: 'V8', end: 'V21', radius: 1, direction: 0, sweep: 0 },
                            { start: 'V21', end: 'V10', radius: 12, direction: 1, sweep: 0 },
                            { start: 'V11', end: 'V12', radius: 7, direction: 0, sweep: 1 },
                            { start: 'V13', end: 'V22', radius: 12, direction: 1, sweep: 1 },
                            { start: 'V22', end: 'V16', radius: 1, direction: 0, sweep: 0 },
                            { start: 'V17', end: 'V23', radius: 1, direction: 0, sweep: 0 },
                            { start: 'V23', end: 'V1', radius: 12, direction: 1, sweep: 0 },
                        ]
                    },
                    {
                        'vertex': [
                            { x: 0, y: 0, label: 'C1', start: 1, display: 1 }, // Center point
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: 0, y: -28, label: 'C2', start: 1, display: 1 }, // Center point
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: 12, y: -28, label: 'V2', start: 1, display: 1 },
                        ],
                    },
                    {
                        'vertex': [
                            { x: 12, y: 0, label: 'V9', start: 0, display: 1 },
                        ],
                    },
                    {
                        'vertex': [
                            { x: 0, y: 12, label: 'V14', start: 0, display: 1 },
                        ],
                    },
                    {
                        'vertex': [
                            { x: -12, y: 0, label: 'V15', start: 0, display: 1 },
                        ],
                    },
                    {
                        'vertex': [
                            { x: -12, y: -28, label: 'V18', start: 0, display: 1 },
                        ],
                    },
                    {
                        'vertex': [
                            { x: -3, y: Math.sqrt(144 - 9), label: 'V31', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: 3, y: Math.sqrt(144 - 9), label: 'V33', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                ]
            };

        case 'Spiral Double':
            return {
                path: [
                    {
                        'vertex': [
                            { x: 0, y: -52, label: 'V1', start: 1, display: 1 }, // Original center point
                            { x: 12.1244, y: -31, label: 'V3', start: 0, display: 0 },  //notched
                            { x: 8.6603, y: -33, label: 'V4', start: 0, display: 0 }, //notched
                            { x: 5, y: -29.3397, label: 'V5', start: 0, display: 0 }, //notched
                            { x: 7, y: -25.8756, label: 'V6', start: 0, display: 0 }, //notched
                            { x: 3.991, y: -24.5833, label: 'V7', start: 0, display: 0 },
                            { x: 0.7835, y: -13.9781, label: 'V8', start: 0, display: 0 },
                            { x: 12.1244, y: 7, label: 'V10', start: 0, display: 0 },  // notched
                            { x: 8.6603, y: 5, label: 'V11', start: 0, display: 0 },  // notched
                            { x: 5, y: 8.6603, label: 'V12', start: 0, display: 0 },  // notched
                            { x: 7, y: 12.1244, label: 'V13', start: 0, display: 0 },  // notched
                            { x: -6.9989, y: -12.125, label: 'V14', start: 0, display: 1 },
                            { x: 0, y: -24, label: 'V15', start: 0, display: 1 },
                        ], 'arcs': [
                            { start: 'V1', end: 'V3', radius: 14, direction: 1, sweep: 0 },
                            { start: 'V4', end: 'V5', radius: 10, direction: 0, sweep: 1 },
                            { start: 'V6', end: 'V7', radius: 14, direction: 1, sweep: 0 },
                            { start: 'V7', end: 'V8', radius: 14, direction: 1, sweep: 0 },
                            { start: 'V8', end: 'V10', radius: 14, direction: 1, sweep: 0 },
                            { start: 'V11', end: 'V12', radius: 10, direction: 0, sweep: 1 },
                            { start: 'V13', end: 'V14', radius: 14, direction: 1, sweep: 0 },
                            { start: 'V14', end: 'V15', radius: 14, direction: 0, sweep: 0 },
                            { start: 'V15', end: 'V1', radius: 14, direction: 1, sweep: 0 },
                        ]
                    },
                    {
                        'vertex': [
                            { x: 0, y: 0, label: 'C1', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: 0, y: -38, label: 'C2', start: 1, display: 1 }, // Center point for tracking
                        ], 'arcs': []
                    },
                    {
                        'vertex': [
                            { x: 14, y: -38, label: 'V2', start: 1, display: 1 },
                        ],
                    },
                    {
                        'vertex': [
                            { x: 14, y: 0, label: 'V9', start: 0, display: 1 },
                        ],
                    },
                    {
                        'vertex': [
                            { x: 0, y: 14, label: 'V16', start: 0, display: 1 },
                        ],
                    },
                    {
                        'vertex': [
                            { x: -14, y: 0, label: 'V17', start: 0, display: 1 },
                        ],
                    },
                    {
                        'vertex': [
                            { x: -14, y: -38, label: 'V18', start: 0, display: 1 },
                        ],
                    },
                ],

            }
    }
}

const symbolsPermittedAngle = {
    'StackArrow': [-135, -90, -45, -22.5, 0, 22.5, 45, 90, 135],
    'Airport': [-90, -60, -45, 0, 45, 60, 90],

};

const routePermittedAngle = {
    'Main Line': [-90, -60, -45, -30, 0, 30, 45, 60, 90],
    'Oval Roundabout': [-90, -60, -30, 0, 30, 60, 90],
    'Double Roundabout': [-90, -60, -30, 0, 30, 60, 90]
};

const symbolsTemplate = {
    // TODO: permitted angle


    //'TestTriangle': [{
    //  'vertex': [
    //    { x: 0, y: 0, label: 'V1', radius: 50, start: 1 },
    //    { x: 300, y: 0, label: 'V2', radius: 50, start: 0 },
    //    { x: 300, y: 600, label: 'V3', radius: 50, start: 0 },
    //    { x: 0, y: 600, label: 'V3', radius: 50, start: 0 },
    //  ], 'arcs': []
    //}],

    //'TestX': {
    //  path: [{ 'vertex': [{ x: 0, y: 0, label: 'V1', start: 1 },], 'arcs': [] }],
    //  text: [{ character: 'x', x: 0, y: 0, fontSize: 100 * 0.075, fontFamily: 'TransportMedium' },
    //  { character: '中', x: 0, y: 0, fontSize: 5, fontFamily: 'Chinese' }]
    //},

    'StackArrow': {
        path: [{
            'vertex': [
                { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                { x: 4, y: 4, label: 'V2', start: 0, display: 0 },
                { x: 4, y: 8, label: 'V3', start: 0, display: 0 },
                { x: 4 / 3, y: 16 / 3, label: 'V4', start: 0, display: 0 },
                { x: 4 / 3, y: 16, label: 'V5', start: 0, display: 0 },
                { x: -4 / 3, y: 16, label: 'V6', start: 0, display: 0 },
                { x: -4 / 3, y: 16 / 3, label: 'V7', start: 0, display: 0 },
                { x: -4, y: 8, label: 'V8', start: 0, display: 0 },
                { x: -4, y: 4, label: 'V9', start: 0, display: 0 },
            ], 'arcs': []
        }],
    },

    'GantryArrow': {
        path: [{
            'vertex': [
                { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                { x: 3, y: 0, label: 'V2', start: 0, display: 0 },
                { x: 3, y: 4, label: 'V3', start: 0, display: 0 },
                { x: 9, y: 4, label: 'V4', start: 0, display: 0 },
                { x: 0, y: 8, label: 'V5', start: 0, display: 0 },
                { x: -9, y: 4, label: 'V6', start: 0, display: 0 },
                { x: -3, y: 4, label: 'V7', start: 0, display: 0 },
                { x: -3, y: 0, label: 'V8', start: 0, display: 0 },
            ], 'arcs': []
        }],
    },

    'Tunnel': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 9, y: 5, label: 'V3', start: 0, display: 0 },
                    { x: 9, y: 16, label: 'V4', start: 0, display: 0 },
                    { x: -9, y: 16, label: 'V5', start: 0, display: 0 },
                    { x: -9, y: 5, label: 'V6', start: 0, display: 0 },
                    { x: -4, y: 0, label: 'V7', start: 0, display: 0 },
                    { x: -5.25, y: 13, label: 'V8', start: 1, display: 0 },
                    { x: 5.25, y: 13, label: 'V9', start: 0, display: 0 },
                ], 'arcs': [{ start: 'V9', end: 'V8', radius: 6.5, direction: 0, sweep: 1 }]
            },
        ],
    },

    'TunnelClosed': {
        path: [ // 4.5.2.2
            {
                'vertex': [
                    { x: 0, y: 2, label: 'V1', start: 1, display: 1 },
                    { x: 3.25, y: 2, label: 'V2', start: 0, display: 0 },
                    { x: 7.25, y: 6, label: 'V3', start: 0, display: 0 },
                    { x: 7.25, y: 15, label: 'V4', start: 0, display: 0 },
                    { x: -7.25, y: 15, label: 'V5', start: 0, display: 0 },
                    { x: -7.25, y: 6, label: 'V6', start: 0, display: 0 },
                    { x: -3.25, y: 2, label: 'V7', start: 0, display: 0 },
                    { x: -4.25, y: 12.5, label: 'V8', start: 1, display: 0 },
                    { x: 4.25, y: 12.5, label: 'V9', start: 0, display: 0 },
                    { x: 0, y: 0, label: 'V10', start: 1, display: 0 },
                ], 'arcs': [{ start: 'V9', end: 'V8', radius: 5.25, direction: 0, sweep: 1 }],
            },
            {
                'vertex': [
                    { x: 8.116, y: 0, label: 'V91', start: 1, display: 1 },
                    { x: 9, y: 0, label: 'V92', start: 0, display: 0 },
                    { x: 9, y: 0.884, label: 'V93', start: 0, display: 0 },
                    { x: -8.116, y: 16, label: 'V94', start: 0, display: 1 },
                    { x: -9, y: 16, label: 'V95', start: 0, display: 0 },
                    { x: -9, y: 15.116, label: 'V96', start: 0, display: 0 },

                ], 'arcs': [], 'fill': '#ff0000'
            },
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V10', start: 1, display: 0 },
                    { x: 9, y: 0, label: 'V11', start: 0, display: 0 },
                    { x: 9, y: 16, label: 'V12', start: 0, display: 0 },
                    { x: -9, y: 16, label: 'V13', start: 0, display: 0 },
                    { x: -9, y: 0, label: 'V14', start: 0, display: 0 },
                    { x: 0, y: 0.2, label: 'V15', start: 1, display: 0 },
                    { x: -8.8, y: 0.2, label: 'V16', start: 0, display: 0 },
                    { x: -8.8, y: 1.9, label: 'V17', start: 0, display: 0 },
                    { x: 8.8, y: 1.9, label: 'V18', start: 0, display: 0 },
                    { x: 8.8, y: 0.2, label: 'V19', start: 0, display: 0 },

                    { x: 0, y: 2.1, label: 'V25', start: 1, display: 0 },
                    { x: -8.8, y: 2.1, label: 'V26', start: 0, display: 0 },
                    { x: -8.8, y: 3.9, label: 'V27', start: 0, display: 0 },
                    { x: 8.8, y: 3.9, label: 'V28', start: 0, display: 0 },
                    { x: 8.8, y: 2.1, label: 'V29', start: 0, display: 0 },

                    { x: 0, y: 4.1, label: 'V35', start: 1, display: 0 },
                    { x: -8.8, y: 4.1, label: 'V36', start: 0, display: 0 },
                    { x: -8.8, y: 5.9, label: 'V37', start: 0, display: 0 },
                    { x: 8.8, y: 5.9, label: 'V38', start: 0, display: 0 },
                    { x: 8.8, y: 4.1, label: 'V39', start: 0, display: 0 },

                    { x: 0, y: 6.1, label: 'V45', start: 1, display: 0 },
                    { x: -8.8, y: 6.1, label: 'V46', start: 0, display: 0 },
                    { x: -8.8, y: 7.9, label: 'V47', start: 0, display: 0 },
                    { x: 8.8, y: 7.9, label: 'V48', start: 0, display: 0 },
                    { x: 8.8, y: 6.1, label: 'V49', start: 0, display: 0 },

                    { x: 0, y: 8.1, label: 'V55', start: 1, display: 0 },
                    { x: -8.8, y: 8.1, label: 'V56', start: 0, display: 0 },
                    { x: -8.8, y: 9.9, label: 'V57', start: 0, display: 0 },
                    { x: 8.8, y: 9.9, label: 'V58', start: 0, display: 0 },
                    { x: 8.8, y: 8.1, label: 'V59', start: 0, display: 0 },

                    { x: 0, y: 10.1, label: 'V65', start: 1, display: 0 },
                    { x: -8.8, y: 10.1, label: 'V66', start: 0, display: 0 },
                    { x: -8.8, y: 11.9, label: 'V67', start: 0, display: 0 },
                    { x: 8.8, y: 11.9, label: 'V68', start: 0, display: 0 },
                    { x: 8.8, y: 10.1, label: 'V69', start: 0, display: 0 },

                    { x: 0, y: 12.1, label: 'V75', start: 1, display: 0 },
                    { x: -8.8, y: 12.1, label: 'V76', start: 0, display: 0 },
                    { x: -8.8, y: 13.9, label: 'V77', start: 0, display: 0 },
                    { x: 8.8, y: 13.9, label: 'V78', start: 0, display: 0 },
                    { x: 8.8, y: 12.1, label: 'V79', start: 0, display: 0 },

                    { x: 0, y: 14.1, label: 'V85', start: 1, display: 0 },
                    { x: -8.8, y: 14.1, label: 'V86', start: 0, display: 0 },
                    { x: -8.8, y: 15.8, label: 'V87', start: 0, display: 0 },
                    { x: 8.8, y: 15.8, label: 'V88', start: 0, display: 0 },
                    { x: 8.8, y: 14.1, label: 'V89', start: 0, display: 0 },
                ], 'arcs': [], 'fill': '#000000'
            },
        ],
    },

    'TunnelOpened': {
        path: [ // 4.5.2.2
            {
                'vertex': [
                    { x: 0, y: 2, label: 'V1', start: 1, display: 1 },
                    { x: 3.25, y: 2, label: 'V2', start: 0, display: 0 },
                    { x: 7.25, y: 6, label: 'V3', start: 0, display: 0 },
                    { x: 7.25, y: 15, label: 'V4', start: 0, display: 0 },
                    { x: -7.25, y: 15, label: 'V5', start: 0, display: 0 },
                    { x: -7.25, y: 6, label: 'V6', start: 0, display: 0 },
                    { x: -3.25, y: 2, label: 'V7', start: 0, display: 0 },
                    { x: -4.25, y: 12.5, label: 'V8', start: 1, display: 0 },
                    { x: 4.25, y: 12.5, label: 'V9', start: 0, display: 0 },
                    { x: 0, y: 0, label: 'V10', start: 1, display: 0 },
                ], 'arcs': [{ start: 'V9', end: 'V8', radius: 5.25, direction: 0, sweep: 1 }],
            },
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V10', start: 1, display: 0 },
                    { x: 9, y: 0, label: 'V11', start: 0, display: 0 },
                    { x: 9, y: 16, label: 'V12', start: 0, display: 0 },
                    { x: -9, y: 16, label: 'V13', start: 0, display: 0 },
                    { x: -9, y: 0, label: 'V14', start: 0, display: 0 },
                    { x: 0, y: 0.2, label: 'V15', start: 1, display: 0 },
                    { x: -8.8, y: 0.2, label: 'V16', start: 0, display: 0 },
                    { x: -8.8, y: 1.9, label: 'V17', start: 0, display: 0 },
                    { x: 8.8, y: 1.9, label: 'V18', start: 0, display: 0 },
                    { x: 8.8, y: 0.2, label: 'V19', start: 0, display: 0 },

                    { x: 0, y: 2.1, label: 'V25', start: 1, display: 0 },
                    { x: -8.8, y: 2.1, label: 'V26', start: 0, display: 0 },
                    { x: -8.8, y: 3.9, label: 'V27', start: 0, display: 0 },
                    { x: 8.8, y: 3.9, label: 'V28', start: 0, display: 0 },
                    { x: 8.8, y: 2.1, label: 'V29', start: 0, display: 0 },

                    { x: 0, y: 4.1, label: 'V35', start: 1, display: 0 },
                    { x: -8.8, y: 4.1, label: 'V36', start: 0, display: 0 },
                    { x: -8.8, y: 5.9, label: 'V37', start: 0, display: 0 },
                    { x: 8.8, y: 5.9, label: 'V38', start: 0, display: 0 },
                    { x: 8.8, y: 4.1, label: 'V39', start: 0, display: 0 },

                    { x: 0, y: 6.1, label: 'V45', start: 1, display: 0 },
                    { x: -8.8, y: 6.1, label: 'V46', start: 0, display: 0 },
                    { x: -8.8, y: 7.9, label: 'V47', start: 0, display: 0 },
                    { x: 8.8, y: 7.9, label: 'V48', start: 0, display: 0 },
                    { x: 8.8, y: 6.1, label: 'V49', start: 0, display: 0 },

                    { x: 0, y: 8.1, label: 'V55', start: 1, display: 0 },
                    { x: -8.8, y: 8.1, label: 'V56', start: 0, display: 0 },
                    { x: -8.8, y: 9.9, label: 'V57', start: 0, display: 0 },
                    { x: 8.8, y: 9.9, label: 'V58', start: 0, display: 0 },
                    { x: 8.8, y: 8.1, label: 'V59', start: 0, display: 0 },

                    { x: 0, y: 10.1, label: 'V65', start: 1, display: 0 },
                    { x: -8.8, y: 10.1, label: 'V66', start: 0, display: 0 },
                    { x: -8.8, y: 11.9, label: 'V67', start: 0, display: 0 },
                    { x: 8.8, y: 11.9, label: 'V68', start: 0, display: 0 },
                    { x: 8.8, y: 10.1, label: 'V69', start: 0, display: 0 },

                    { x: 0, y: 12.1, label: 'V75', start: 1, display: 0 },
                    { x: -8.8, y: 12.1, label: 'V76', start: 0, display: 0 },
                    { x: -8.8, y: 13.9, label: 'V77', start: 0, display: 0 },
                    { x: 8.8, y: 13.9, label: 'V78', start: 0, display: 0 },
                    { x: 8.8, y: 12.1, label: 'V79', start: 0, display: 0 },

                    { x: 0, y: 14.1, label: 'V85', start: 1, display: 0 },
                    { x: -8.8, y: 14.1, label: 'V86', start: 0, display: 0 },
                    { x: -8.8, y: 15.8, label: 'V87', start: 0, display: 0 },
                    { x: 8.8, y: 15.8, label: 'V88', start: 0, display: 0 },
                    { x: 8.8, y: 14.1, label: 'V89', start: 0, display: 0 },
                ], 'arcs': [], 'fill': '#000000'
            },
        ],
    },

    'AmberLightAbove': {
        path: [ // Diagram 4.5.5.1
            {
                'vertex': [
                    { x: 4.8, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 4.8, y: 3.2, label: 'V2', start: 0, display: 0 },
                ], 'arcs': [{ start: 'V1', end: 'V2', radius: 1.6, direction: 1, sweep: 0 },
                { start: 'V2', end: 'V1', radius: 1.6, direction: 1, sweep: 0 }],
                'fill': '#0a0a0a'
            },
            {
                'vertex': [
                    { x: -4.8, y: 0, label: 'V3', start: 1, display: 0 },
                    { x: -4.8, y: 3.2, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [{ start: 'V3', end: 'V4', radius: 1.6, direction: 1, sweep: 0 },
                { start: 'V4', end: 'V3', radius: 1.6, direction: 1, sweep: 0 }],
                'fill': '#0a0a0a'
            },
            {
                'vertex': [
                    { x: 4.8, y: 0.2, label: 'V11', start: 1, display: 0 },
                    { x: 4.8, y: 3, label: 'V12', start: 0, display: 0 },
                ], 'arcs': [{ start: 'V11', end: 'V12', radius: 1.4, direction: 1, sweep: 0 },
                { start: 'V12', end: 'V11', radius: 1.4, direction: 1, sweep: 0 }],
                'fill': '#ffbf00'
            },
            {
                'vertex': [
                    { x: -4.8, y: 0.2, label: 'V13', start: 1, display: 0 },
                    { x: -4.8, y: 3, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [{ start: 'V13', end: 'V14', radius: 1.4, direction: 1, sweep: 0 },
                { start: 'V14', end: 'V13', radius: 1.4, direction: 1, sweep: 0 }],
                'fill': '#ffbf00'
            },
        ],
    },

    'AmberLightBack': {
        path: [ // Diagram 4.5.5.3
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V21', start: 1, display: 0 },
                    { x: 9.6, y: 0, label: 'V23', start: 0, display: 0, radius: 1.6 },
                    { x: 9.6, y: 6.4, label: 'V24', start: 0, display: 0 },
                    { x: -9.6, y: 6.4, label: 'V25', start: 0, display: 0 },
                    { x: -9.6, y: 0, label: 'V26', start: 0, display: 0, radius: 1.6 },
                ], 'arcs': [],
                'fill': '#333333'
            },
            {
                'vertex': [
                    { x: 4.8, y: 1.6, label: 'V1', start: 1, display: 0 },
                    { x: 4.8, y: 4.8, label: 'V2', start: 0, display: 0 },
                ], 'arcs': [{ start: 'V1', end: 'V2', radius: 1.6, direction: 1, sweep: 0 },
                { start: 'V2', end: 'V1', radius: 1.6, direction: 1, sweep: 0 }],
                'fill': '#0a0a0a'
            },
            {
                'vertex': [
                    { x: -4.8, y: 1.6, label: 'V3', start: 1, display: 0 },
                    { x: -4.8, y: 4.8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [{ start: 'V3', end: 'V4', radius: 1.6, direction: 1, sweep: 0 },
                { start: 'V4', end: 'V3', radius: 1.6, direction: 1, sweep: 0 }],
                'fill': '#0a0a0a'
            },
            {
                'vertex': [
                    { x: 4.8, y: 1.8, label: 'V11', start: 1, display: 0 },
                    { x: 4.8, y: 4.6, label: 'V12', start: 0, display: 0 },
                ], 'arcs': [{ start: 'V11', end: 'V12', radius: 1.4, direction: 1, sweep: 0 },
                { start: 'V12', end: 'V11', radius: 1.4, direction: 1, sweep: 0 }],
                'fill': '#ffbf00'
            },
            {
                'vertex': [
                    { x: -4.8, y: 1.8, label: 'V13', start: 1, display: 0 },
                    { x: -4.8, y: 4.6, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [{ start: 'V13', end: 'V14', radius: 1.4, direction: 1, sweep: 0 },
                { start: 'V14', end: 'V13', radius: 1.4, direction: 1, sweep: 0 }],
                'fill': '#ffbf00'
            },
        ],
    },



    'Expressway': {
        path: [ // Diagram 3.5.7.11
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 4.5, y: 0, label: 'V2', radius: 0.75, start: 0, display: 0 },
                    { x: 4.5, y: 9, label: 'V3', radius: 0.75, start: 0, display: 0 },
                    { x: -4.5, y: 9, label: 'V4', radius: 0.75, start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V5', radius: 0.75, start: 0, display: 0 },
                    { x: 0, y: 0.25, label: 'V6', start: 1, display: 0 },
                    { x: -4.25, y: 0.25, label: 'V7', radius: 0.5, start: 0, display: 0 },
                    { x: -4.25, y: 8.75, label: 'V8', radius: 0.5, start: 0, display: 0 },
                    { x: 4.25, y: 8.75, label: 'V9', radius: 0.5, start: 0, display: 0 },
                    { x: 4.25, y: 0.25, label: 'V10', radius: 0.5, start: 0, display: 0 },
                ], 'arcs': []
            },
            {
                'vertex': [
                    { x: 0.25, y: 1.25, label: 'V11', start: 1, display: 0 },
                    { x: 0.75, y: 1.25, label: 'V12', start: 0, display: 0 },
                    { x: 0.972, y: 2.75, label: 'V13', start: 0, display: 0 },
                    { x: 0.25, y: 2.75, label: 'V14', start: 0, display: 0 },
                ], 'arcs': []
            },
            {
                'vertex': [
                    { x: -0.25, y: 1.25, label: 'V15', start: 1, display: 0 },
                    { x: -0.25, y: 2.75, label: 'V16', start: 0, display: 0 },
                    { x: -0.972, y: 2.75, label: 'V17', start: 0, display: 0 },
                    { x: -0.75, y: 1.25, label: 'V18', start: 0, display: 0 },
                ], 'arcs': []
            },
            {
                'vertex': [
                    { x: 0, y: 3.25, label: 'V19', start: 1, display: 0 },
                    { x: 3.5, y: 3.25, label: 'V20', start: 0, display: 0 },
                    { x: 3.5, y: 3.75, label: 'V21', start: 0, display: 0 },
                    { x: 2.75, y: 3.75, label: 'V22', radius: 0.25, start: 0, display: 0 },
                    { x: 2.75, y: 4.75, label: 'V23', start: 0, display: 0 },
                    { x: 2.25, y: 4.75, label: 'V24', start: 0, display: 0 },
                    { x: 2, y: 3.75, label: 'V25', radius: 0.25, start: 0, display: 0 },
                    { x: -2, y: 3.75, label: 'V26', radius: 0.25, start: 0, display: 0 },
                    { x: -2.25, y: 4.75, label: 'V27', start: 0, display: 0 },
                    { x: -2.75, y: 4.75, label: 'V28', start: 0, display: 0 },
                    { x: -2.75, y: 3.75, label: 'V29', radius: 0.25, start: 0, display: 0 },
                    { x: -3.5, y: 3.75, label: 'V30', start: 0, display: 0 },
                    { x: -3.5, y: 3.25, label: 'V31', start: 0, display: 0 },
                ], 'arcs': []
            },
            {
                'vertex': [
                    { x: 0.25, y: 4.25, label: 'V32', start: 1, display: 0 },
                    { x: 1.194, y: 4.25, label: 'V33', start: 0, display: 0 },
                    { x: 1.5, y: 8, label: 'V34', start: 0, display: 0 },
                    { x: 0.25, y: 8, label: 'V35', start: 0, display: 0 },
                ], 'arcs': []
            },
            {
                'vertex': [
                    { x: -0.25, y: 4.25, label: 'V36', start: 1, display: 0 },
                    { x: -0.25, y: 8, label: 'V371', start: 0, display: 0 },
                    { x: -1.5, y: 8, label: 'V38', start: 0, display: 0 },
                    { x: -1.194, y: 4.25, label: 'V39', start: 0, display: 0 },
                ], 'arcs': []
            },
        ],
    },

    'ExpresswayRed': {
        path: [ // Diagram 3.5.7.11
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', radius: 0.75, start: 0, display: 0 },
                    { x: 4.5, y: 9, label: 'V3', radius: 0.75, start: 0, display: 0 },
                    { x: -4.5, y: 9, label: 'V4', radius: 0.75, start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V5', radius: 0.75, start: 0, display: 0 },
                ], 'arcs': [], 'fill': '#ff0000'
            },
            {
                'vertex': [
                    { x: 0, y: 0.25, label: 'V6', start: 1, display: 0 },
                    { x: -4.25, y: 0.25, label: 'V7', radius: 0.5, start: 0, display: 0 },
                    { x: -4.25, y: 8.75, label: 'V8', radius: 0.5, start: 0, display: 0 },
                    { x: 4.25, y: 8.75, label: 'V9', radius: 0.5, start: 0, display: 0 },
                    { x: 4.25, y: 0.25, label: 'V10', radius: 0.5, start: 0, display: 0 },
                ], 'arcs': [], 'fill': '#ffffff'
            },
            {
                'vertex': [
                    { x: 0.25, y: 1.25, label: 'V11', start: 1, display: 0 },
                    { x: 0.75, y: 1.25, label: 'V12', start: 0, display: 0 },
                    { x: 0.972, y: 2.75, label: 'V13', start: 0, display: 0 },
                    { x: 0.25, y: 2.75, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [], 'fill': '#ff0000'
            },
            {
                'vertex': [
                    { x: -0.25, y: 1.25, label: 'V15', start: 1, display: 0 },
                    { x: -0.25, y: 2.75, label: 'V16', start: 0, display: 0 },
                    { x: -0.972, y: 2.75, label: 'V17', start: 0, display: 0 },
                    { x: -0.75, y: 1.25, label: 'V18', start: 0, display: 0 },
                ], 'arcs': [], 'fill': '#ff0000'
            },
            {
                'vertex': [
                    { x: 0, y: 3.25, label: 'V19', start: 1, display: 0 },
                    { x: 3.5, y: 3.25, label: 'V20', start: 0, display: 0 },
                    { x: 3.5, y: 3.75, label: 'V21', start: 0, display: 0 },
                    { x: 2.75, y: 3.75, label: 'V22', radius: 0.25, start: 0, display: 0 },
                    { x: 2.75, y: 4.75, label: 'V23', start: 0, display: 0 },
                    { x: 2.25, y: 4.75, label: 'V24', start: 0, display: 0 },
                    { x: 2, y: 3.75, label: 'V25', radius: 0.25, start: 0, display: 0 },
                    { x: -2, y: 3.75, label: 'V26', radius: 0.25, start: 0, display: 0 },
                    { x: -2.25, y: 4.75, label: 'V27', start: 0, display: 0 },
                    { x: -2.75, y: 4.75, label: 'V28', start: 0, display: 0 },
                    { x: -2.75, y: 3.75, label: 'V29', radius: 0.25, start: 0, display: 0 },
                    { x: -3.5, y: 3.75, label: 'V30', start: 0, display: 0 },
                    { x: -3.5, y: 3.25, label: 'V31', start: 0, display: 0 },
                ], 'arcs': [], 'fill': '#ff0000'
            },
            {
                'vertex': [
                    { x: 0.25, y: 4.25, label: 'V32', start: 1, display: 0 },
                    { x: 1.194, y: 4.25, label: 'V33', start: 0, display: 0 },
                    { x: 1.5, y: 8, label: 'V34', start: 0, display: 0 },
                    { x: 0.25, y: 8, label: 'V35', start: 0, display: 0 },
                ], 'arcs': [], 'fill': '#ff0000'
            },
            {
                'vertex': [
                    { x: -0.25, y: 4.25, label: 'V36', start: 1, display: 0 },
                    { x: -0.25, y: 8, label: 'V371', start: 0, display: 0 },
                    { x: -1.5, y: 8, label: 'V38', start: 0, display: 0 },
                    { x: -1.194, y: 4.25, label: 'V39', start: 0, display: 0 },
                ], 'arcs': [], 'fill': '#ff0000'
            },
        ],
    },

    'Airport': {
        path: [ // 3.5.7.14
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 1.27, y: 2.924, label: 'V2', start: 0, display: 0 },
                    { x: 1.27, y: 6.5, label: 'V3', start: 0, display: 0 },
                    { x: 9, y: 11, label: 'V4', radius: 0.5, start: 0, display: 0 },
                    { x: 9, y: 13, label: 'V5', radius: 0.5, start: 0, display: 0 },
                    { x: 2.5, y: 11, label: 'V6', start: 0, display: 0 },
                    { x: 1.25, y: 11, label: 'V7', start: 0, display: 0 },
                    { x: 1.25, y: 11.5, label: 'V8', start: 0, display: 0 },
                    { x: 0.75, y: 15.5, label: 'V9', start: 0, display: 0 },
                    { x: 4, y: 16.5, label: 'V10', radius: 0.5, start: 0, display: 0 },
                    { x: 4, y: 18, label: 'V11', radius: 0.5, start: 0, display: 0 },
                    // mirror
                    { x: -4, y: 18, label: 'V12', radius: 0.5, start: 0, display: 0 },
                    { x: -4, y: 16.5, label: 'V13', radius: 0.5, start: 0, display: 0 },
                    { x: -0.75, y: 15.5, label: 'V14', start: 0, display: 0 },
                    { x: -1.25, y: 11.5, label: 'V15', start: 0, display: 0 },
                    { x: -1.25, y: 11, label: 'V16', start: 0, display: 0 },
                    { x: -2.5, y: 11, label: 'V17', start: 0, display: 0 },
                    { x: -9, y: 13, label: 'V18', radius: 0.5, start: 0, display: 0 },
                    { x: -9, y: 11, label: 'V19', radius: 0.5, start: 0, display: 0 },
                    { x: -1.27, y: 6.5, label: 'V20', start: 0, display: 0 },
                    { x: -1.27, y: 2.924, label: 'V21', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 4, direction: 1, sweep: 0 },
                    { start: 'V21', end: 'V1', radius: 4, direction: 1, sweep: 0 },
                ]
            },
        ],
    },

    'Route1': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },],
        text: [
            { character: '1', x: -1.56, y: -0.7, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route2': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '2', x: -2.4, y: -0.7, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route3': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '3', x: -2.54, y: -0.6, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route4': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '4', x: -2.64, y: -0.7, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route5': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '5', x: -2.44, y: -0.7, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route6': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '6', x: -2.52, y: -0.6, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route7': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '7', x: -2.08, y: -0.7, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route8': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '8', x: -2.76, y: -0.6, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route9': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '9', x: -2.56, y: -0.6, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route10': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 6, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 6, y: 3.8295, label: 'V3', start: 0, display: 0 },
                    { x: 4.8, y: 6.6861, label: 'V4', start: 0, display: 0 }, //https://www.wolframalpha.com/input?i2d=true&i=81+%3D+Power%5B%5C%2840%29x-7.5%5C%2841%29%2C2%5D+%2B+Power%5B%5C%2840%29y%2B0.2588%5C%2841%29%2C2%5D%5C%2844%29+16+%3D+Power%5B%5C%2840%29x-4%5C%2841%29%2C2%5D+%2B+Power%5By%2B3.82952%2C2%5D
                    { x: 0, y: 9, label: 'V5', start: 0, display: 0 },
                    { x: -4.8, y: 6.6861, label: 'V6', start: 0, display: 0 },
                    { x: -6, y: 3.8295, label: 'V7', start: 0, display: 0 },
                    { x: -6, y: 0, label: 'V8', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 4, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 9, direction: 1, sweep: 0 },
                    { start: 'V5', end: 'V6', radius: 9, direction: 1, sweep: 0 },
                    { start: 'V6', end: 'V7', radius: 4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '10', x: -4.22, y: -0.5, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route11': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 6, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 6, y: 3.8295, label: 'V3', start: 0, display: 0 },
                    { x: 4.8, y: 6.6861, label: 'V4', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V5', start: 0, display: 0 },
                    { x: -4.8, y: 6.6861, label: 'V6', start: 0, display: 0 },
                    { x: -6, y: 3.8295, label: 'V7', start: 0, display: 0 },
                    { x: -6, y: 0, label: 'V8', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 4, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 9, direction: 1, sweep: 0 },
                    { start: 'V5', end: 'V6', radius: 9, direction: 1, sweep: 0 },
                    { start: 'V6', end: 'V7', radius: 4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '11', x: -3.12, y: -0.5, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route12': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 6, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 6, y: 3.8295, label: 'V3', start: 0, display: 0 },
                    { x: 4.8, y: 6.6861, label: 'V4', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V5', start: 0, display: 0 },
                    { x: -4.8, y: 6.6861, label: 'V6', start: 0, display: 0 },
                    { x: -6, y: 3.8295, label: 'V7', start: 0, display: 0 },
                    { x: -6, y: 0, label: 'V8', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 4, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 9, direction: 1, sweep: 0 },
                    { start: 'V5', end: 'V6', radius: 9, direction: 1, sweep: 0 },
                    { start: 'V6', end: 'V7', radius: 4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '12', x: -3.96, y: -0.5, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },


    'CHT': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 11, y: 0, label: 'V2', radius: 1.5, start: 0, display: 0 },
                    { x: 11, y: 16, label: 'V3', radius: 1.5, start: 0, display: 0 },
                    { x: -11, y: 16, label: 'V4', radius: 1.5, start: 0, display: 0 },
                    { x: -11, y: 0, label: 'V5', radius: 1.5, start: 0, display: 0 },
                    { x: 0, y: 1, label: 'V6', start: 1, display: 0 },
                    { x: -10, y: 1, label: 'V7', radius: 0.5, start: 0, display: 0 },
                    { x: -10, y: 15, label: 'V8', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 15, label: 'V9', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 1, label: 'V10', radius: 0.5, start: 0, display: 0 },
                ], 'arcs': []
            },
            {
                'vertex': [
                    { x: 0, y: 6, label: 'V11', start: 1, display: 0 },
                    { x: 2, y: 6, label: 'V12', start: 0, display: 0 },
                    { x: 4.5, y: 8.5, label: 'V13', start: 0, display: 0 },
                    { x: 4.5, y: 14, label: 'V14', start: 0, display: 0 },
                    { x: -4.5, y: 14, label: 'V15', start: 0, display: 0 },
                    { x: -4.5, y: 8.5, label: 'V16', start: 0, display: 0 },
                    { x: -2, y: 6, label: 'V17', start: 0, display: 0 },
                    { x: -2.625, y: 12.5, label: 'V18', start: 1, display: 0 },
                    { x: 2.625, y: 12.5, label: 'V19', start: 0, display: 0 },
                ], 'arcs': [{ start: 'V19', end: 'V18', radius: 3.25, direction: 0, sweep: 1 }]
            },
            {
                'vertex': [
                    { x: -9, y: 3, label: 'V21', start: 1, display: 0 },
                    { x: -8.034, y: 2.485, label: 'V22', start: 0, display: 0 },
                    { x: -4.5, y: 2.583, label: 'V23', start: 0, display: 0 },
                    { x: -1.5, y: 2.525, label: 'V24', start: 0, display: 0 },
                    { x: 1.5, y: 2.525, label: 'V25', start: 0, display: 0 },
                    { x: 4.5, y: 2.583, label: 'V26', start: 0, display: 0 },
                    { x: 8.034, y: 2.485, label: 'V27', start: 0, display: 0 },
                    { x: 9, y: 3, label: 'V28', start: 0, display: 0 },
                    { x: 9, y: 4, label: 'V29', start: 0, display: 0 },
                    { x: 8.034, y: 3.485, label: 'V30', start: 0, display: 0 },
                    { x: 4.5, y: 3.583, label: 'V31', start: 0, display: 0 },
                    { x: 1.5, y: 3.525, label: 'V32', start: 0, display: 0 },
                    { x: -1.5, y: 3.525, label: 'V33', start: 0, display: 0 },
                    { x: -4.5, y: 3.583, label: 'V34', start: 0, display: 0 },
                    { x: -8.034, y: 3.485, label: 'V35', start: 0, display: 0 },
                    { x: -9, y: 4, label: 'V36', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V22', end: 'V23', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V23', end: 'V24', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V24', end: 'V25', radius: 2.456, direction: 1, sweep: 0 },
                    { start: 'V25', end: 'V26', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V26', end: 'V27', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V30', end: 'V31', radius: 3.57, direction: 0, sweep: 0 },
                    { start: 'V31', end: 'V32', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V32', end: 'V33', radius: 2.456, direction: 0, sweep: 0 },
                    { start: 'V33', end: 'V34', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V34', end: 'V35', radius: 3.57, direction: 0, sweep: 0 },
                ]
            },
            {
                'vertex': [
                    { x: -9, y: 4.5, label: 'V41', start: 1, display: 0 },
                    { x: -8.034, y: 3.985, label: 'V42', start: 0, display: 0 },
                    { x: -4.5, y: 4.083, label: 'V43', start: 0, display: 0 },
                    { x: -1.5, y: 4.025, label: 'V44', start: 0, display: 0 },
                    { x: 1.5, y: 4.025, label: 'V45', start: 0, display: 0 },
                    { x: 4.5, y: 4.083, label: 'V46', start: 0, display: 0 },
                    { x: 8.034, y: 3.985, label: 'V47', start: 0, display: 0 },
                    { x: 9, y: 4.5, label: 'V48', start: 0, display: 0 },
                    { x: 9, y: 5.5, label: 'V49', start: 0, display: 0 },
                    { x: 8.034, y: 4.985, label: 'V50', start: 0, display: 0 },
                    { x: 4.5, y: 4.983, label: 'V51', start: 0, display: 0 },
                    { x: 1.5, y: 5.025, label: 'V52', start: 0, display: 0 },
                    { x: -1.5, y: 5.025, label: 'V53', start: 0, display: 0 },
                    { x: -4.5, y: 5.083, label: 'V54', start: 0, display: 0 },
                    { x: -8.034, y: 4.985, label: 'V55', start: 0, display: 0 },
                    { x: -9, y: 5.5, label: 'V56', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V42', end: 'V43', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V43', end: 'V44', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V44', end: 'V45', radius: 2.456, direction: 1, sweep: 0 },
                    { start: 'V45', end: 'V46', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V46', end: 'V47', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V50', end: 'V51', radius: 3.57, direction: 0, sweep: 0 },
                    { start: 'V51', end: 'V52', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V52', end: 'V53', radius: 2.456, direction: 0, sweep: 0 },
                    { start: 'V53', end: 'V54', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V54', end: 'V55', radius: 3.57, direction: 0, sweep: 0 },
                ]
            },
        ],
        text: [
            { character: 'C', x: 4.845, y: -9, fontSize: 6.5 * 0.94, fontFamily: 'TransportMedium' },
            { character: '中', x: -9.8, y: -9.3, fontSize: 5.7 * 0.9, fontFamily: 'parsedFontKorean' }
        ]
    },

    'EHC': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 11, y: 0, label: 'V2', radius: 1.5, start: 0, display: 0 },
                    { x: 11, y: 16, label: 'V3', radius: 1.5, start: 0, display: 0 },
                    { x: -11, y: 16, label: 'V4', radius: 1.5, start: 0, display: 0 },
                    { x: -11, y: 0, label: 'V5', radius: 1.5, start: 0, display: 0 },
                    { x: 0, y: 1, label: 'V6', start: 1, display: 0 },
                    { x: -10, y: 1, label: 'V7', radius: 0.5, start: 0, display: 0 },
                    { x: -10, y: 15, label: 'V8', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 15, label: 'V9', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 1, label: 'V10', radius: 0.5, start: 0, display: 0 },
                ], 'arcs': []
            },
            {
                'vertex': [
                    { x: 0, y: 6, label: 'V11', start: 1, display: 0 },
                    { x: 2, y: 6, label: 'V12', start: 0, display: 0 },
                    { x: 4.5, y: 8.5, label: 'V13', start: 0, display: 0 },
                    { x: 4.5, y: 14, label: 'V14', start: 0, display: 0 },
                    { x: -4.5, y: 14, label: 'V15', start: 0, display: 0 },
                    { x: -4.5, y: 8.5, label: 'V16', start: 0, display: 0 },
                    { x: -2, y: 6, label: 'V17', start: 0, display: 0 },
                    { x: -2.625, y: 12.5, label: 'V18', start: 1, display: 0 },
                    { x: 2.625, y: 12.5, label: 'V19', start: 0, display: 0 },
                ], 'arcs': [{ start: 'V19', end: 'V18', radius: 3.25, direction: 0, sweep: 1 }]
            },
            {
                'vertex': [
                    { x: -9, y: 3, label: 'V21', start: 1, display: 0 },
                    { x: -8.034, y: 2.485, label: 'V22', start: 0, display: 0 },
                    { x: -4.5, y: 2.583, label: 'V23', start: 0, display: 0 },
                    { x: -1.5, y: 2.525, label: 'V24', start: 0, display: 0 },
                    { x: 1.5, y: 2.525, label: 'V25', start: 0, display: 0 },
                    { x: 4.5, y: 2.583, label: 'V26', start: 0, display: 0 },
                    { x: 8.034, y: 2.485, label: 'V27', start: 0, display: 0 },
                    { x: 9, y: 3, label: 'V28', start: 0, display: 0 },
                    { x: 9, y: 4, label: 'V29', start: 0, display: 0 },
                    { x: 8.034, y: 3.485, label: 'V30', start: 0, display: 0 },
                    { x: 4.5, y: 3.583, label: 'V31', start: 0, display: 0 },
                    { x: 1.5, y: 3.525, label: 'V32', start: 0, display: 0 },
                    { x: -1.5, y: 3.525, label: 'V33', start: 0, display: 0 },
                    { x: -4.5, y: 3.583, label: 'V34', start: 0, display: 0 },
                    { x: -8.034, y: 3.485, label: 'V35', start: 0, display: 0 },
                    { x: -9, y: 4, label: 'V36', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V22', end: 'V23', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V23', end: 'V24', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V24', end: 'V25', radius: 2.456, direction: 1, sweep: 0 },
                    { start: 'V25', end: 'V26', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V26', end: 'V27', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V30', end: 'V31', radius: 3.57, direction: 0, sweep: 0 },
                    { start: 'V31', end: 'V32', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V32', end: 'V33', radius: 2.456, direction: 0, sweep: 0 },
                    { start: 'V33', end: 'V34', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V34', end: 'V35', radius: 3.57, direction: 0, sweep: 0 },
                ]
            },
            {
                'vertex': [
                    { x: -9, y: 4.5, label: 'V41', start: 1, display: 0 },
                    { x: -8.034, y: 3.985, label: 'V42', start: 0, display: 0 },
                    { x: -4.5, y: 4.083, label: 'V43', start: 0, display: 0 },
                    { x: -1.5, y: 4.025, label: 'V44', start: 0, display: 0 },
                    { x: 1.5, y: 4.025, label: 'V45', start: 0, display: 0 },
                    { x: 4.5, y: 4.083, label: 'V46', start: 0, display: 0 },
                    { x: 8.034, y: 3.985, label: 'V47', start: 0, display: 0 },
                    { x: 9, y: 4.5, label: 'V48', start: 0, display: 0 },
                    { x: 9, y: 5.5, label: 'V49', start: 0, display: 0 },
                    { x: 8.034, y: 4.985, label: 'V50', start: 0, display: 0 },
                    { x: 4.5, y: 4.983, label: 'V51', start: 0, display: 0 },
                    { x: 1.5, y: 5.025, label: 'V52', start: 0, display: 0 },
                    { x: -1.5, y: 5.025, label: 'V53', start: 0, display: 0 },
                    { x: -4.5, y: 5.083, label: 'V54', start: 0, display: 0 },
                    { x: -8.034, y: 4.985, label: 'V55', start: 0, display: 0 },
                    { x: -9, y: 5.5, label: 'V56', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V42', end: 'V43', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V43', end: 'V44', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V44', end: 'V45', radius: 2.456, direction: 1, sweep: 0 },
                    { start: 'V45', end: 'V46', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V46', end: 'V47', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V50', end: 'V51', radius: 3.57, direction: 0, sweep: 0 },
                    { start: 'V51', end: 'V52', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V52', end: 'V53', radius: 2.456, direction: 0, sweep: 0 },
                    { start: 'V53', end: 'V54', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V54', end: 'V55', radius: 3.57, direction: 0, sweep: 0 },
                ]
            },
        ],
        text: [
            { character: 'E', x: 4.945, y: -9, fontSize: 6.5 * 0.94, fontFamily: 'TransportMedium' },
            { character: '東', x: -9.8, y: - 9.3, fontSize: 5.7 * 0.9, fontFamily: 'parsedFontKorean' }
        ]
    },

    'WHC': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 11, y: 0, label: 'V2', radius: 1.5, start: 0, display: 0 },
                    { x: 11, y: 16, label: 'V3', radius: 1.5, start: 0, display: 0 },
                    { x: -11, y: 16, label: 'V4', radius: 1.5, start: 0, display: 0 },
                    { x: -11, y: 0, label: 'V5', radius: 1.5, start: 0, display: 0 },
                    { x: 0, y: 1, label: 'V6', start: 1, display: 0 },
                    { x: -10, y: 1, label: 'V7', radius: 0.5, start: 0, display: 0 },
                    { x: -10, y: 15, label: 'V8', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 15, label: 'V9', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 1, label: 'V10', radius: 0.5, start: 0, display: 0 },
                ], 'arcs': []
            },
            {
                'vertex': [
                    { x: 0, y: 6, label: 'V11', start: 1, display: 0 },
                    { x: 2, y: 6, label: 'V12', start: 0, display: 0 },
                    { x: 4.5, y: 8.5, label: 'V13', start: 0, display: 0 },
                    { x: 4.5, y: 14, label: 'V14', start: 0, display: 0 },
                    { x: -4.5, y: 14, label: 'V15', start: 0, display: 0 },
                    { x: -4.5, y: 8.5, label: 'V16', start: 0, display: 0 },
                    { x: -2, y: 6, label: 'V17', start: 0, display: 0 },
                    { x: -2.625, y: 12.5, label: 'V18', start: 1, display: 0 },
                    { x: 2.625, y: 12.5, label: 'V19', start: 0, display: 0 },
                ], 'arcs': [{ start: 'V19', end: 'V18', radius: 3.25, direction: 0, sweep: 1 }]
            },
            {
                'vertex': [
                    { x: -9, y: 3, label: 'V21', start: 1, display: 0 },
                    { x: -8.034, y: 2.485, label: 'V22', start: 0, display: 0 },
                    { x: -4.5, y: 2.583, label: 'V23', start: 0, display: 0 },
                    { x: -1.5, y: 2.525, label: 'V24', start: 0, display: 0 },
                    { x: 1.5, y: 2.525, label: 'V25', start: 0, display: 0 },
                    { x: 4.5, y: 2.583, label: 'V26', start: 0, display: 0 },
                    { x: 8.034, y: 2.485, label: 'V27', start: 0, display: 0 },
                    { x: 9, y: 3, label: 'V28', start: 0, display: 0 },
                    { x: 9, y: 4, label: 'V29', start: 0, display: 0 },
                    { x: 8.034, y: 3.485, label: 'V30', start: 0, display: 0 },
                    { x: 4.5, y: 3.583, label: 'V31', start: 0, display: 0 },
                    { x: 1.5, y: 3.525, label: 'V32', start: 0, display: 0 },
                    { x: -1.5, y: 3.525, label: 'V33', start: 0, display: 0 },
                    { x: -4.5, y: 3.583, label: 'V34', start: 0, display: 0 },
                    { x: -8.034, y: 3.485, label: 'V35', start: 0, display: 0 },
                    { x: -9, y: 4, label: 'V36', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V22', end: 'V23', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V23', end: 'V24', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V24', end: 'V25', radius: 2.456, direction: 1, sweep: 0 },
                    { start: 'V25', end: 'V26', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V26', end: 'V27', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V30', end: 'V31', radius: 3.57, direction: 0, sweep: 0 },
                    { start: 'V31', end: 'V32', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V32', end: 'V33', radius: 2.456, direction: 0, sweep: 0 },
                    { start: 'V33', end: 'V34', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V34', end: 'V35', radius: 3.57, direction: 0, sweep: 0 },
                ]
            },
            {
                'vertex': [
                    { x: -9, y: 4.5, label: 'V41', start: 1, display: 0 },
                    { x: -8.034, y: 3.985, label: 'V42', start: 0, display: 0 },
                    { x: -4.5, y: 4.083, label: 'V43', start: 0, display: 0 },
                    { x: -1.5, y: 4.025, label: 'V44', start: 0, display: 0 },
                    { x: 1.5, y: 4.025, label: 'V45', start: 0, display: 0 },
                    { x: 4.5, y: 4.083, label: 'V46', start: 0, display: 0 },
                    { x: 8.034, y: 3.985, label: 'V47', start: 0, display: 0 },
                    { x: 9, y: 4.5, label: 'V48', start: 0, display: 0 },
                    { x: 9, y: 5.5, label: 'V49', start: 0, display: 0 },
                    { x: 8.034, y: 4.985, label: 'V50', start: 0, display: 0 },
                    { x: 4.5, y: 4.983, label: 'V51', start: 0, display: 0 },
                    { x: 1.5, y: 5.025, label: 'V52', start: 0, display: 0 },
                    { x: -1.5, y: 5.025, label: 'V53', start: 0, display: 0 },
                    { x: -4.5, y: 5.083, label: 'V54', start: 0, display: 0 },
                    { x: -8.034, y: 4.985, label: 'V55', start: 0, display: 0 },
                    { x: -9, y: 5.5, label: 'V56', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V42', end: 'V43', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V43', end: 'V44', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V44', end: 'V45', radius: 2.456, direction: 1, sweep: 0 },
                    { start: 'V45', end: 'V46', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V46', end: 'V47', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V50', end: 'V51', radius: 3.57, direction: 0, sweep: 0 },
                    { start: 'V51', end: 'V52', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V52', end: 'V53', radius: 2.456, direction: 0, sweep: 0 },
                    { start: 'V53', end: 'V54', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V54', end: 'V55', radius: 3.57, direction: 0, sweep: 0 },
                ]
            },
        ],
        text: [
            { character: 'W', x: 4.2, y: -9, fontSize: 6.5 * 0.94, fontFamily: 'TransportMedium' },
            { character: '西', x: -9.8, y: - 9.3, fontSize: 5.7 * 0.9, fontFamily: 'parsedFontKorean' }
        ]
    },

    'JTIS': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 11, y: 0, label: 'V2', radius: 1.5, start: 0, display: 0 },
                    { x: 11, y: 15, label: 'V3', radius: 1.5, start: 0, display: 0 },
                    { x: -11, y: 15, label: 'V4', radius: 1.5, start: 0, display: 0 },
                    { x: -11, y: 0, label: 'V5', radius: 1.5, start: 0, display: 0 },
                    { x: 0, y: 1, label: 'V6', start: 1, display: 0 },
                    { x: -10, y: 1, label: 'V7', radius: 0.5, start: 0, display: 0 },
                    { x: -10, y: 14, label: 'V8', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 14, label: 'V9', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 1, label: 'V10', radius: 0.5, start: 0, display: 0 },
                ], 'arcs': []
            },
            {
                'vertex': [
                    { x: -10, y: 1, label: 'V11', start: 1, display: 1 },
                    { x: 3, y: 1, label: 'V12', start: 0, display: 0 },
                    { x: 3, y: 14, label: 'V13', start: 0, display: 0 },
                    { x: -10, y: 14, label: 'V14', start: 0, display: 0 }
                ], 'arcs': [], fill: '#000000'
            },

        ],
        text: [
            { character: 'mins', x: 3.25, y: -7.94, fontSize: 3 * 0.94, fontFamily: 'TransportMedium' },
            { character: '分鐘', x: 3.25, y: -10.94, fontSize: 3.5 * 0.9, fontFamily: 'parsedFontKorean' }
        ]
    },

    'JTIS-CHT': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 11, y: 0, label: 'V2', radius: 1.5, start: 0, display: 0 },
                    { x: 11, y: 30, label: 'V3', radius: 1.5, start: 0, display: 0 },
                    { x: -11, y: 30, label: 'V4', radius: 1.5, start: 0, display: 0 },
                    { x: -11, y: 0, label: 'V5', radius: 1.5, start: 0, display: 0 },
                    { x: 0, y: 1, label: 'V6', start: 1, display: 0 },
                    { x: -10, y: 1, label: 'V7', radius: 0.5, start: 0, display: 0 },
                    { x: -10, y: 15, label: 'V8', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 15, label: 'V9', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 1, label: 'V10', radius: 0.5, start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V66', start: 1, display: 0 },
                    { x: -10, y: 16, label: 'V67', radius: 0.5, start: 0, display: 0 },
                    { x: -10, y: 29, label: 'V68', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 29, label: 'V69', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 16, label: 'V70', radius: 0.5, start: 0, display: 0 },
                ], 'arcs': []
            },
            {
                'vertex': [
                    { x: 0, y: 6, label: 'V11', start: 1, display: 0 },
                    { x: 2, y: 6, label: 'V12', start: 0, display: 0 },
                    { x: 4.5, y: 8.5, label: 'V13', start: 0, display: 0 },
                    { x: 4.5, y: 14, label: 'V14', start: 0, display: 0 },
                    { x: -4.5, y: 14, label: 'V15', start: 0, display: 0 },
                    { x: -4.5, y: 8.5, label: 'V16', start: 0, display: 0 },
                    { x: -2, y: 6, label: 'V17', start: 0, display: 0 },
                    { x: -2.625, y: 12.5, label: 'V18', start: 1, display: 0 },
                    { x: 2.625, y: 12.5, label: 'V19', start: 0, display: 0 },
                ], 'arcs': [{ start: 'V19', end: 'V18', radius: 3.25, direction: 0, sweep: 1 }]
            },
            {
                'vertex': [
                    { x: -9, y: 3, label: 'V21', start: 1, display: 0 },
                    { x: -8.034, y: 2.485, label: 'V22', start: 0, display: 0 },
                    { x: -4.5, y: 2.583, label: 'V23', start: 0, display: 0 },
                    { x: -1.5, y: 2.525, label: 'V24', start: 0, display: 0 },
                    { x: 1.5, y: 2.525, label: 'V25', start: 0, display: 0 },
                    { x: 4.5, y: 2.583, label: 'V26', start: 0, display: 0 },
                    { x: 8.034, y: 2.485, label: 'V27', start: 0, display: 0 },
                    { x: 9, y: 3, label: 'V28', start: 0, display: 0 },
                    { x: 9, y: 4, label: 'V29', start: 0, display: 0 },
                    { x: 8.034, y: 3.485, label: 'V30', start: 0, display: 0 },
                    { x: 4.5, y: 3.583, label: 'V31', start: 0, display: 0 },
                    { x: 1.5, y: 3.525, label: 'V32', start: 0, display: 0 },
                    { x: -1.5, y: 3.525, label: 'V33', start: 0, display: 0 },
                    { x: -4.5, y: 3.583, label: 'V34', start: 0, display: 0 },
                    { x: -8.034, y: 3.485, label: 'V35', start: 0, display: 0 },
                    { x: -9, y: 4, label: 'V36', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V22', end: 'V23', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V23', end: 'V24', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V24', end: 'V25', radius: 2.456, direction: 1, sweep: 0 },
                    { start: 'V25', end: 'V26', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V26', end: 'V27', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V30', end: 'V31', radius: 3.57, direction: 0, sweep: 0 },
                    { start: 'V31', end: 'V32', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V32', end: 'V33', radius: 2.456, direction: 0, sweep: 0 },
                    { start: 'V33', end: 'V34', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V34', end: 'V35', radius: 3.57, direction: 0, sweep: 0 },
                ]
            },
            {
                'vertex': [
                    { x: -9, y: 4.5, label: 'V41', start: 1, display: 0 },
                    { x: -8.034, y: 3.985, label: 'V42', start: 0, display: 0 },
                    { x: -4.5, y: 4.083, label: 'V43', start: 0, display: 0 },
                    { x: -1.5, y: 4.025, label: 'V44', start: 0, display: 0 },
                    { x: 1.5, y: 4.025, label: 'V45', start: 0, display: 0 },
                    { x: 4.5, y: 4.083, label: 'V46', start: 0, display: 0 },
                    { x: 8.034, y: 3.985, label: 'V47', start: 0, display: 0 },
                    { x: 9, y: 4.5, label: 'V48', start: 0, display: 0 },
                    { x: 9, y: 5.5, label: 'V49', start: 0, display: 0 },
                    { x: 8.034, y: 4.985, label: 'V50', start: 0, display: 0 },
                    { x: 4.5, y: 4.983, label: 'V51', start: 0, display: 0 },
                    { x: 1.5, y: 5.025, label: 'V52', start: 0, display: 0 },
                    { x: -1.5, y: 5.025, label: 'V53', start: 0, display: 0 },
                    { x: -4.5, y: 5.083, label: 'V54', start: 0, display: 0 },
                    { x: -8.034, y: 4.985, label: 'V55', start: 0, display: 0 },
                    { x: -9, y: 5.5, label: 'V56', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V42', end: 'V43', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V43', end: 'V44', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V44', end: 'V45', radius: 2.456, direction: 1, sweep: 0 },
                    { start: 'V45', end: 'V46', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V46', end: 'V47', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V50', end: 'V51', radius: 3.57, direction: 0, sweep: 0 },
                    { start: 'V51', end: 'V52', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V52', end: 'V53', radius: 2.456, direction: 0, sweep: 0 },
                    { start: 'V53', end: 'V54', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V54', end: 'V55', radius: 3.57, direction: 0, sweep: 0 },
                ]
            },
            {
                'vertex': [
                    { x: -10, y: 16, label: 'V71', start: 1, display: 1 },
                    { x: 3, y: 16, label: 'V72', start: 0, display: 0 },
                    { x: 3, y: 29, label: 'V73', start: 0, display: 0 },
                    { x: -10, y: 29, label: 'V74', start: 0, display: 0 }
                ], 'arcs': [], fill: '#000000'
            },
        ],
        text: [
            { character: 'C', x: 4.845, y: -9, fontSize: 6.5 * 0.94, fontFamily: 'TransportMedium' },
            { character: '中', x: -9.8, y: -9.3, fontSize: 5.7 * 0.9, fontFamily: 'parsedFontKorean' },
            { character: 'mins', x: 3.25, y: -22.94, fontSize: 3 * 0.94, fontFamily: 'TransportMedium' },
            { character: '分鐘', x: 3.25, y: -25.94, fontSize: 3.5 * 0.9, fontFamily: 'parsedFontKorean' }
        ]
    },

    'JTIS-EHC': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 11, y: 0, label: 'V2', radius: 1.5, start: 0, display: 0 },
                    { x: 11, y: 30, label: 'V3', radius: 1.5, start: 0, display: 0 },
                    { x: -11, y: 30, label: 'V4', radius: 1.5, start: 0, display: 0 },
                    { x: -11, y: 0, label: 'V5', radius: 1.5, start: 0, display: 0 },
                    { x: 0, y: 1, label: 'V6', start: 1, display: 0 },
                    { x: -10, y: 1, label: 'V7', radius: 0.5, start: 0, display: 0 },
                    { x: -10, y: 15, label: 'V8', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 15, label: 'V9', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 1, label: 'V10', radius: 0.5, start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V66', start: 1, display: 0 },
                    { x: -10, y: 16, label: 'V67', radius: 0.5, start: 0, display: 0 },
                    { x: -10, y: 29, label: 'V68', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 29, label: 'V69', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 16, label: 'V70', radius: 0.5, start: 0, display: 0 },
                ], 'arcs': []
            },
            {
                'vertex': [
                    { x: 0, y: 6, label: 'V11', start: 1, display: 0 },
                    { x: 2, y: 6, label: 'V12', start: 0, display: 0 },
                    { x: 4.5, y: 8.5, label: 'V13', start: 0, display: 0 },
                    { x: 4.5, y: 14, label: 'V14', start: 0, display: 0 },
                    { x: -4.5, y: 14, label: 'V15', start: 0, display: 0 },
                    { x: -4.5, y: 8.5, label: 'V16', start: 0, display: 0 },
                    { x: -2, y: 6, label: 'V17', start: 0, display: 0 },
                    { x: -2.625, y: 12.5, label: 'V18', start: 1, display: 0 },
                    { x: 2.625, y: 12.5, label: 'V19', start: 0, display: 0 },
                ], 'arcs': [{ start: 'V19', end: 'V18', radius: 3.25, direction: 0, sweep: 1 }]
            },
            {
                'vertex': [
                    { x: -9, y: 3, label: 'V21', start: 1, display: 0 },
                    { x: -8.034, y: 2.485, label: 'V22', start: 0, display: 0 },
                    { x: -4.5, y: 2.583, label: 'V23', start: 0, display: 0 },
                    { x: -1.5, y: 2.525, label: 'V24', start: 0, display: 0 },
                    { x: 1.5, y: 2.525, label: 'V25', start: 0, display: 0 },
                    { x: 4.5, y: 2.583, label: 'V26', start: 0, display: 0 },
                    { x: 8.034, y: 2.485, label: 'V27', start: 0, display: 0 },
                    { x: 9, y: 3, label: 'V28', start: 0, display: 0 },
                    { x: 9, y: 4, label: 'V29', start: 0, display: 0 },
                    { x: 8.034, y: 3.485, label: 'V30', start: 0, display: 0 },
                    { x: 4.5, y: 3.583, label: 'V31', start: 0, display: 0 },
                    { x: 1.5, y: 3.525, label: 'V32', start: 0, display: 0 },
                    { x: -1.5, y: 3.525, label: 'V33', start: 0, display: 0 },
                    { x: -4.5, y: 3.583, label: 'V34', start: 0, display: 0 },
                    { x: -8.034, y: 3.485, label: 'V35', start: 0, display: 0 },
                    { x: -9, y: 4, label: 'V36', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V22', end: 'V23', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V23', end: 'V24', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V24', end: 'V25', radius: 2.456, direction: 1, sweep: 0 },
                    { start: 'V25', end: 'V26', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V26', end: 'V27', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V30', end: 'V31', radius: 3.57, direction: 0, sweep: 0 },
                    { start: 'V31', end: 'V32', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V32', end: 'V33', radius: 2.456, direction: 0, sweep: 0 },
                    { start: 'V33', end: 'V34', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V34', end: 'V35', radius: 3.57, direction: 0, sweep: 0 },
                ]
            },
            {
                'vertex': [
                    { x: -9, y: 4.5, label: 'V41', start: 1, display: 0 },
                    { x: -8.034, y: 3.985, label: 'V42', start: 0, display: 0 },
                    { x: -4.5, y: 4.083, label: 'V43', start: 0, display: 0 },
                    { x: -1.5, y: 4.025, label: 'V44', start: 0, display: 0 },
                    { x: 1.5, y: 4.025, label: 'V45', start: 0, display: 0 },
                    { x: 4.5, y: 4.083, label: 'V46', start: 0, display: 0 },
                    { x: 8.034, y: 3.985, label: 'V47', start: 0, display: 0 },
                    { x: 9, y: 4.5, label: 'V48', start: 0, display: 0 },
                    { x: 9, y: 5.5, label: 'V49', start: 0, display: 0 },
                    { x: 8.034, y: 4.985, label: 'V50', start: 0, display: 0 },
                    { x: 4.5, y: 4.983, label: 'V51', start: 0, display: 0 },
                    { x: 1.5, y: 5.025, label: 'V52', start: 0, display: 0 },
                    { x: -1.5, y: 5.025, label: 'V53', start: 0, display: 0 },
                    { x: -4.5, y: 5.083, label: 'V54', start: 0, display: 0 },
                    { x: -8.034, y: 4.985, label: 'V55', start: 0, display: 0 },
                    { x: -9, y: 5.5, label: 'V56', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V42', end: 'V43', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V43', end: 'V44', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V44', end: 'V45', radius: 2.456, direction: 1, sweep: 0 },
                    { start: 'V45', end: 'V46', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V46', end: 'V47', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V50', end: 'V51', radius: 3.57, direction: 0, sweep: 0 },
                    { start: 'V51', end: 'V52', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V52', end: 'V53', radius: 2.456, direction: 0, sweep: 0 },
                    { start: 'V53', end: 'V54', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V54', end: 'V55', radius: 3.57, direction: 0, sweep: 0 },
                ]
            },
            {
                'vertex': [
                    { x: -10, y: 16, label: 'V71', start: 1, display: 1 },
                    { x: 3, y: 16, label: 'V72', start: 0, display: 0 },
                    { x: 3, y: 29, label: 'V73', start: 0, display: 0 },
                    { x: -10, y: 29, label: 'V74', start: 0, display: 0 }
                ], 'arcs': [], fill: '#000000'
            },
        ],
        text: [
            { character: 'E', x: 4.945, y: -9, fontSize: 6.5 * 0.94, fontFamily: 'TransportMedium' },
            { character: '東', x: -9.8, y: - 9.3, fontSize: 5.7 * 0.9, fontFamily: 'parsedFontKorean' },
            { character: 'mins', x: 3.25, y: -22.94, fontSize: 3 * 0.94, fontFamily: 'TransportMedium' },
            { character: '分鐘', x: 3.25, y: -25.94, fontSize: 3.5 * 0.9, fontFamily: 'parsedFontKorean' }
        ]
    },

    'JTIS-WHC': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 11, y: 0, label: 'V2', radius: 1.5, start: 0, display: 0 },
                    { x: 11, y: 30, label: 'V3', radius: 1.5, start: 0, display: 0 },
                    { x: -11, y: 30, label: 'V4', radius: 1.5, start: 0, display: 0 },
                    { x: -11, y: 0, label: 'V5', radius: 1.5, start: 0, display: 0 },
                    { x: 0, y: 1, label: 'V6', start: 1, display: 0 },
                    { x: -10, y: 1, label: 'V7', radius: 0.5, start: 0, display: 0 },
                    { x: -10, y: 15, label: 'V8', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 15, label: 'V9', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 1, label: 'V10', radius: 0.5, start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V66', start: 1, display: 0 },
                    { x: -10, y: 16, label: 'V67', radius: 0.5, start: 0, display: 0 },
                    { x: -10, y: 29, label: 'V68', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 29, label: 'V69', radius: 0.5, start: 0, display: 0 },
                    { x: 10, y: 16, label: 'V70', radius: 0.5, start: 0, display: 0 },
                ], 'arcs': []
            },
            {
                'vertex': [
                    { x: 0, y: 6, label: 'V11', start: 1, display: 0 },
                    { x: 2, y: 6, label: 'V12', start: 0, display: 0 },
                    { x: 4.5, y: 8.5, label: 'V13', start: 0, display: 0 },
                    { x: 4.5, y: 14, label: 'V14', start: 0, display: 0 },
                    { x: -4.5, y: 14, label: 'V15', start: 0, display: 0 },
                    { x: -4.5, y: 8.5, label: 'V16', start: 0, display: 0 },
                    { x: -2, y: 6, label: 'V17', start: 0, display: 0 },
                    { x: -2.625, y: 12.5, label: 'V18', start: 1, display: 0 },
                    { x: 2.625, y: 12.5, label: 'V19', start: 0, display: 0 },
                ], 'arcs': [{ start: 'V19', end: 'V18', radius: 3.25, direction: 0, sweep: 1 }]
            },
            {
                'vertex': [
                    { x: -9, y: 3, label: 'V21', start: 1, display: 0 },
                    { x: -8.034, y: 2.485, label: 'V22', start: 0, display: 0 },
                    { x: -4.5, y: 2.583, label: 'V23', start: 0, display: 0 },
                    { x: -1.5, y: 2.525, label: 'V24', start: 0, display: 0 },
                    { x: 1.5, y: 2.525, label: 'V25', start: 0, display: 0 },
                    { x: 4.5, y: 2.583, label: 'V26', start: 0, display: 0 },
                    { x: 8.034, y: 2.485, label: 'V27', start: 0, display: 0 },
                    { x: 9, y: 3, label: 'V28', start: 0, display: 0 },
                    { x: 9, y: 4, label: 'V29', start: 0, display: 0 },
                    { x: 8.034, y: 3.485, label: 'V30', start: 0, display: 0 },
                    { x: 4.5, y: 3.583, label: 'V31', start: 0, display: 0 },
                    { x: 1.5, y: 3.525, label: 'V32', start: 0, display: 0 },
                    { x: -1.5, y: 3.525, label: 'V33', start: 0, display: 0 },
                    { x: -4.5, y: 3.583, label: 'V34', start: 0, display: 0 },
                    { x: -8.034, y: 3.485, label: 'V35', start: 0, display: 0 },
                    { x: -9, y: 4, label: 'V36', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V22', end: 'V23', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V23', end: 'V24', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V24', end: 'V25', radius: 2.456, direction: 1, sweep: 0 },
                    { start: 'V25', end: 'V26', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V26', end: 'V27', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V30', end: 'V31', radius: 3.57, direction: 0, sweep: 0 },
                    { start: 'V31', end: 'V32', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V32', end: 'V33', radius: 2.456, direction: 0, sweep: 0 },
                    { start: 'V33', end: 'V34', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V34', end: 'V35', radius: 3.57, direction: 0, sweep: 0 },
                ]
            },
            {
                'vertex': [
                    { x: -9, y: 4.5, label: 'V41', start: 1, display: 0 },
                    { x: -8.034, y: 3.985, label: 'V42', start: 0, display: 0 },
                    { x: -4.5, y: 4.083, label: 'V43', start: 0, display: 0 },
                    { x: -1.5, y: 4.025, label: 'V44', start: 0, display: 0 },
                    { x: 1.5, y: 4.025, label: 'V45', start: 0, display: 0 },
                    { x: 4.5, y: 4.083, label: 'V46', start: 0, display: 0 },
                    { x: 8.034, y: 3.985, label: 'V47', start: 0, display: 0 },
                    { x: 9, y: 4.5, label: 'V48', start: 0, display: 0 },
                    { x: 9, y: 5.5, label: 'V49', start: 0, display: 0 },
                    { x: 8.034, y: 4.985, label: 'V50', start: 0, display: 0 },
                    { x: 4.5, y: 4.983, label: 'V51', start: 0, display: 0 },
                    { x: 1.5, y: 5.025, label: 'V52', start: 0, display: 0 },
                    { x: -1.5, y: 5.025, label: 'V53', start: 0, display: 0 },
                    { x: -4.5, y: 5.083, label: 'V54', start: 0, display: 0 },
                    { x: -8.034, y: 4.985, label: 'V55', start: 0, display: 0 },
                    { x: -9, y: 5.5, label: 'V56', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V42', end: 'V43', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V43', end: 'V44', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V44', end: 'V45', radius: 2.456, direction: 1, sweep: 0 },
                    { start: 'V45', end: 'V46', radius: 2.8, direction: 0, sweep: 0 },
                    { start: 'V46', end: 'V47', radius: 3.57, direction: 1, sweep: 0 },
                    { start: 'V50', end: 'V51', radius: 3.57, direction: 0, sweep: 0 },
                    { start: 'V51', end: 'V52', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V52', end: 'V53', radius: 2.456, direction: 0, sweep: 0 },
                    { start: 'V53', end: 'V54', radius: 2.8, direction: 1, sweep: 0 },
                    { start: 'V54', end: 'V55', radius: 3.57, direction: 0, sweep: 0 },
                ]
            },
            {
                'vertex': [
                    { x: -10, y: 16, label: 'V71', start: 1, display: 1 },
                    { x: 3, y: 16, label: 'V72', start: 0, display: 0 },
                    { x: 3, y: 29, label: 'V73', start: 0, display: 0 },
                    { x: -10, y: 29, label: 'V74', start: 0, display: 0 }
                ], 'arcs': [], fill: '#000000'
            },
        ],
        text: [
            { character: 'W', x: 4.2, y: -9, fontSize: 6.5 * 0.94, fontFamily: 'TransportMedium' },
            { character: '西', x: -9.8, y: - 9.3, fontSize: 5.7 * 0.9, fontFamily: 'parsedFontKorean' },
            { character: 'mins', x: 3.25, y: -22.94, fontSize: 3 * 0.94, fontFamily: 'TransportMedium' },
            { character: '分鐘', x: 3.25, y: -25.94, fontSize: 3.5 * 0.9, fontFamily: 'parsedFontKorean' }
        ]
    },



    'MTR': {
        path: [ // 3.5.7.19
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 0, y: 22, label: 'V2', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 13, radius2: 11, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V1', radius: 13, radius2: 11, direction: 1, sweep: 0 },
                ], 'fill': '#ff0101'
            },
            {
                'vertex': [
                    { x: 0, y: 4, label: 'V3', start: 1, display: 0 },
                    { x: 1, y: 4, label: 'V4', start: 0, display: 0 },
                    { x: 1, y: 8, label: 'V5', start: 0, display: 0 },
                    { x: 4, y: 4.2, label: 'V6', start: 0, display: 0 },
                    { x: 6.2, y: 4.2, label: 'V7', start: 0, display: 0 },
                    { x: 1, y: 10, label: 'V8', start: 0, display: 0 },
                    { x: 1, y: 12, label: 'V9', start: 0, display: 0 },
                    { x: 6.2, y: 17.8, label: 'V10', start: 0, display: 0 },
                    { x: 4, y: 17.8, label: 'V11', start: 0, display: 0 },
                    { x: 1, y: 14, label: 'V12', start: 0, display: 0 },
                    { x: 1, y: 18, label: 'V13', start: 0, display: 0 },
                    { x: -1, y: 18, label: 'V14', start: 0, display: 0 },
                    { x: -1, y: 14, label: 'V15', start: 0, display: 0 },
                    { x: -4, y: 17.8, label: 'V16', start: 0, display: 0 },
                    { x: -6.2, y: 17.8, label: 'V17', start: 0, display: 0 },
                    { x: -1, y: 12, label: 'V18', start: 0, display: 0 },
                    { x: -1, y: 10, label: 'V19', start: 0, display: 0 },
                    { x: -6.2, y: 4.2, label: 'V20', start: 0, display: 0 },
                    { x: -4, y: 4.2, label: 'V21', start: 0, display: 0 },
                    { x: -1, y: 8, label: 'V22', start: 0, display: 0 },
                    { x: -1, y: 4, label: 'V23', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V5', end: 'V6', radius: 3, radius2: 4, direction: 0, sweep: 0 },
                    { start: 'V7', end: 'V8', radius: 5.2, radius2: 6, direction: 1, sweep: 0 },
                    { start: 'V9', end: 'V10', radius: 5.2, radius2: 6, direction: 1, sweep: 0 },
                    { start: 'V11', end: 'V12', radius: 3, radius2: 4, direction: 0, sweep: 0 },
                    { start: 'V15', end: 'V16', radius: 3, radius2: 4, direction: 0, sweep: 0 },
                    { start: 'V17', end: 'V18', radius: 5.2, radius2: 6, direction: 1, sweep: 0 },
                    { start: 'V19', end: 'V20', radius: 5.2, radius2: 6, direction: 1, sweep: 0 },
                    { start: 'V21', end: 'V22', radius: 3, radius2: 4, direction: 0, sweep: 0 },
                ], 'fill': '#ffffff'
            },
        ],
        text: []
    },

    'Hospital': {
        path: [ // 3.5.7.31
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 0, y: 16, label: 'V2', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': '#ffffff'
            },
            {
                'vertex': [
                    { x: 0, y: 1.5, label: 'V3', start: 1, display: 0 },
                    { x: 2, y: 1.5, label: 'V4', start: 0, display: 0 },
                    { x: 2, y: 6, label: 'V5', start: 0, display: 0 },
                    { x: 6.5, y: 6, label: 'V6', start: 0, display: 0 },
                    { x: 6.5, y: 10, label: 'V7', start: 0, display: 0 },
                    { x: 2, y: 10, label: 'V8', start: 0, display: 0 },
                    { x: 2, y: 14.5, label: 'V9', start: 0, display: 0 },
                    { x: -2, y: 14.5, label: 'V10', start: 0, display: 0 },
                    { x: -2, y: 10, label: 'V11', start: 0, display: 0 },
                    { x: -6.5, y: 10, label: 'V12', start: 0, display: 0 },
                    { x: -6.5, y: 6, label: 'V13', start: 0, display: 0 },
                    { x: -2, y: 6, label: 'V14', start: 0, display: 0 },
                    { x: -2, y: 1.5, label: 'V15', start: 0, display: 0 },
                ], 'arcs': [], 'fill': '#ff0101'
            },
        ],
        text: []
    },

    'CableCar': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 0.426, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 0.426, y: 3.407, label: 'V3', start: 0, display: 0 },
                    { x: 3.212, y: 3.407, label: 'V4', start: 0, display: 0 },
                    { x: 3.097, y: 3.044, label: 'V5', start: 0, display: 0 },
                    { x: 3.697, y: 3.044, label: 'V6', start: 0, display: 0 },
                    { x: 5.319, y: 8.271, label: 'V7', start: 0, display: 0 },
                    { x: 5.383, y: 11.477, label: 'V8', start: 0, display: 0 },
                    { x: 4.158, y: 16.070, label: 'V9', start: 0, display: 0 },
                    { x: 3.585, y: 16.070, label: 'V10', start: 0, display: 0 },
                    { x: 3.833, y: 15.133, label: 'V11', start: 0, display: 0 },
                    { x: -3.833, y: 15.133, label: 'V12', start: 0, display: 0 },
                    { x: -3.585, y: 16.070, label: 'V13', start: 0, display: 0 },
                    { x: -4.158, y: 16.070, label: 'V14', start: 0, display: 0 },
                    { x: -5.383, y: 11.477, label: 'V15', start: 0, display: 0 },
                    { x: -5.319, y: 8.271, label: 'V16', start: 0, display: 0 },
                    { x: -3.697, y: 3.044, label: 'V17', start: 0, display: 0 },
                    { x: -3.097, y: 3.044, label: 'V18', start: 0, display: 0 },
                    { x: -3.212, y: 3.407, label: 'V19', start: 0, display: 0 },
                    { x: -0.426, y: 3.407, label: 'V20', start: 0, display: 0 },
                    { x: -0.426, y: 0, label: 'V21', start: 0, display: 0 },
                    { x: 3.518, y: 4.390, label: 'V31', start: 1, display: 0 },
                    { x: 3.518, y: 10.656, label: 'V32', start: 0, display: 0 },
                    { x: 4.983, y: 10.656, label: 'V33', start: 0, display: 0 },
                    { x: 4.790, y: 8.437, label: 'V34', start: 0, display: 0 },
                    { x: 0.235, y: 4.390, label: 'V35', start: 1, display: 0 },
                    { x: 0.235, y: 10.656, label: 'V36', start: 0, display: 0 },
                    { x: 3.048, y: 10.656, label: 'V37', start: 0, display: 0 },
                    { x: 3.048, y: 4.390, label: 'V38', start: 0, display: 0 },
                    { x: -3.518, y: 4.390, label: 'V41', start: 1, display: 0 },
                    { x: -4.790, y: 8.437, label: 'V42', start: 0, display: 0 },
                    { x: -4.983, y: 10.656, label: 'V43', start: 0, display: 0 },
                    { x: -3.518, y: 10.656, label: 'V44', start: 0, display: 0 },
                    { x: -0.235, y: 4.390, label: 'V45', start: 1, display: 0 },
                    { x: -3.048, y: 4.390, label: 'V46', start: 0, display: 0 },
                    { x: -3.048, y: 10.656, label: 'V47', start: 0, display: 0 },
                    { x: -0.235, y: 10.656, label: 'V48', start: 0, display: 0 },

                ], 'arcs': [
                    { start: 'V7', end: 'V8', radius: 5.709, direction: 1, sweep: 0 },
                    { start: 'V15', end: 'V16', radius: 5.709, direction: 1, sweep: 0 },
                    { start: 'V33', end: 'V34', radius: 5.155, direction: 0, sweep: 0 },
                    { start: 'V42', end: 'V43', radius: 5.155, direction: 0, sweep: 0 },

                ],
            },
            {
                'vertex': [
                    { x: 0.641, y: 1.103, label: 'V51', start: 1, display: 0 },
                    { x: 7.147, y: 2.847, label: 'V52', start: 0, display: 0 },
                    { x: 7.147, y: 3.335, label: 'V53', start: 0, display: 0 },
                    { x: 0.641, y: 1.589, label: 'V54', start: 0, display: 0 },
                ], 'arcs': [],
            },
            {
                'vertex': [
                    { x: -0.641, y: 0.761, label: 'V55', start: 1, display: 0 },
                    { x: -7.147, y: -0.982, label: 'V56', start: 0, display: 0 },
                    { x: -7.147, y: -0.497, label: 'V57', start: 0, display: 0 },
                    { x: -0.641, y: 1.246, label: 'V58', start: 0, display: 0 },
                ], 'arcs': [],
            },
        ],
        text: []
    },

    'Disney': {
        path: [ // https://upload.wikimedia.org/wikipedia/commons/f/fe/Mickey_Mouse_head_and_ears.svg
            {
                'vertex': [
                    { x: 0, y: 3.7266, label: 'V1', start: 1, display: 1 },
                    { x: 2.7767, y: 4.4704, label: 'V2', start: 0, display: 0 },
                    { x: 4.6732, y: 6.2787, label: 'V3', start: 0, display: 0 },
                    { x: -4.6732, y: 6.2787, label: 'V4', start: 0, display: 0 },
                    { x: -2.7767, y: 4.4704, label: 'V5', start: 0, display: 0 },

                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 5.555, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 3.234, direction: 1, sweep: 1 },
                    { start: 'V3', end: 'V4', radius: 5.555, direction: 1, sweep: 1 },
                    { start: 'V4', end: 'V5', radius: 3.234, direction: 1, sweep: 1 },
                    { start: 'V5', end: 'V1', radius: 5.555, direction: 1, sweep: 0 },
                ],
            },
        ],
        text: []
    },

    'Parking': {
        path: [ // 3.5.7.26
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 9, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 9, y: 18, label: 'V3', start: 0, display: 0 },
                    { x: -9, y: 18, label: 'V4', start: 0, display: 0 },
                    { x: -9, y: 0, label: 'V5', start: 0, display: 0 },
                ], 'arcs': [], 'fill': '#0000FE'
            },
            {
                'vertex': [
                    { x: -5, y: 2, label: 'V6', start: 1, display: 0 },
                    { x: 2.125, y: 2, label: 'V7', start: 0, display: 0 },
                    { x: 2.125, y: 10.55, label: 'V8', start: 0, display: 0 },
                    { x: -1.58, y: 10.55, label: 'V9', start: 0, display: 0 },
                    { x: -1.58, y: 16.25, label: 'V10', start: 0, display: 0 },
                    { x: -5, y: 16.25, label: 'V11', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V7', end: 'V8', radius: 4.275, direction: 1, sweep: 0 },
                ], 'fill': '#ffffff'
            },
            {
                'vertex': [
                    { x: -1.58, y: 4.28, label: 'V12', start: 1, display: 0 },
                    { x: 1.27, y: 4.28, label: 'V13', start: 0, display: 0 },
                    { x: 2.98, y: 5.99, label: 'V14', start: 0, display: 0 },
                    { x: 2.98, y: 6.56, label: 'V15', start: 0, display: 0 },
                    { x: 1.27, y: 8.27, label: 'V16', start: 0, display: 0 },
                    { x: -1.58, y: 8.27, label: 'V17', start: 0, display: 0 },

                ], 'arcs': [
                    { start: 'V13', end: 'V14', radius: 1.71, direction: 1, sweep: 0 },
                    { start: 'V15', end: 'V16', radius: 1.71, direction: 1, sweep: 0 },
                ], 'fill': '#0000FE'
            },
        ],
        text: []
    },

    'Exit': {
        path: [ // 3.5.7.20
            {
                'vertex': [ //E
                    { x: -2, y: 0.2, label: 'V1', start: 1, display: 1 },
                    { x: -1.0667, y: 0.2, label: 'V2', start: 0, display: 0 },
                    { x: -1.0667, y: 0.5333, label: 'V3', start: 0, display: 0 },
                    { x: -1.6667, y: 0.5333, label: 'V4', start: 0, display: 0 },
                    { x: -1.6667, y: 1.0333, label: 'V5', start: 0, display: 0 },
                    { x: -1.2, y: 1.0333, label: 'V6', start: 0, display: 0 },
                    { x: -1.2, y: 1.3667, label: 'V7', start: 0, display: 0 },
                    { x: -1.6667, y: 1.3667, label: 'V8', start: 0, display: 0 },
                    { x: -1.6667, y: 1.8667, label: 'V9', start: 0, display: 0 },
                    { x: -1.0667, y: 1.8667, label: 'V10', start: 0, display: 0 },
                    { x: -1.0667, y: 2.2, label: 'V11', start: 0, display: 0 },
                    { x: -2, y: 2.2, label: 'V12', start: 0, display: 0 },
                ], 'arcs': [
                ], 'fill': '#ffffff'
            },
            {
                'vertex': [ //X
                    { x: -0.8, y: 0.2, label: 'V13', start: 1, display: 1 },
                    { x: -0.4469, y: 0.2, label: 'V14', start: 0, display: 0 },
                    { x: -0.3, y: 0.6197, label: 'V15', start: 0, display: 0 },
                    { x: -0.1532, y: 0.2, label: 'V16', start: 0, display: 0 },
                    { x: 0.2, y: 0.2, label: 'V17', start: 0, display: 0 },
                    { x: -0.15, y: 1.2, label: 'V18', start: 0, display: 0 },
                    { x: 0.2, y: 2.2, label: 'V19', start: 0, display: 0 },
                    { x: -0.1532, y: 2.2, label: 'V20', start: 0, display: 0 },
                    { x: -0.3, y: 1.7808, label: 'V21', start: 0, display: 0 },
                    { x: -0.4469, y: 2.2, label: 'V22', start: 0, display: 0 },
                    { x: -0.8, y: 2.2, label: 'V23', start: 0, display: 0 },
                    { x: -0.45, y: 1.2, label: 'V24', start: 0, display: 0 },
                ], 'arcs': [
                ], 'fill': '#ffffff'
            },
            {
                'vertex': [ //I
                    { x: 0.543, y: 0.2, label: 'V25', start: 1, display: 1 },
                    { x: 0.8, y: 0.2, label: 'V26', start: 0, display: 0 },
                    { x: 0.8, y: 2.2, label: 'V27', start: 0, display: 0 },
                    { x: 0.543, y: 2.2, label: 'V28', start: 0, display: 0 },
                ], 'arcs': [
                ], 'fill': '#ffffff'
            },
            {
                'vertex': [ //T
                    { x: 1.0667, y: 0.2, label: 'V29', start: 1, display: 1 },
                    { x: 2, y: 0.2, label: 'V30', start: 0, display: 0 },
                    { x: 2, y: 0.5333, label: 'V31', start: 0, display: 0 },
                    { x: 1.7, y: 0.5333, label: 'V32', start: 0, display: 0 },
                    { x: 1.7, y: 2.2, label: 'V33', start: 0, display: 0 },
                    { x: 1.3667, y: 2.2, label: 'V34', start: 0, display: 0 },
                    { x: 1.3667, y: 0.5333, label: 'V35', start: 0, display: 0 },
                    { x: 1.0667, y: 0.5333, label: 'V36', start: 0, display: 0 },], 'arcs': [
                    ], 'fill': '#ffffff'
            },
            {
                'vertex': [
                    { x: 0.4, y: 3.0, label: 'V37', start: 1, display: 1 },
                    { x: 0.4, y: 3.9333, label: 'V38', start: 0, display: 0 },
                    { x: 1.3333, y: 3.9333, label: 'V39', start: 0, display: 0 },
                    { x: 1.3333, y: 3.4, label: 'V40', start: 0, display: 0 },
                    { x: 1.8667, y: 3.4, label: 'V41', start: 0, display: 0 },
                    { x: 1.8667, y: 4.4667, label: 'V42', start: 0, display: 0 },
                    { x: 0.4, y: 4.4667, label: 'V43', start: 0, display: 0 },
                    { x: 0.4, y: 5.4, label: 'V44', start: 0, display: 0 },
                    { x: 1.4667, y: 5.4, label: 'V45', start: 0, display: 0 },
                    { x: 1.4667, y: 4.8667, label: 'V46', start: 0, display: 0 },
                    { x: 2, y: 4.8667, label: 'V47', start: 0, display: 0 },
                    { x: 2, y: 6.2, label: 'V48', start: 0, display: 0 },
                    { x: 1.4667, y: 6.2, label: 'V49', start: 0, display: 0 },
                    { x: 1.4667, y: 5.9333, label: 'V50', start: 0, display: 0 },
                    /////
                    { x: -1.4667, y: 5.9333, label: 'V51', start: 0, display: 0 },
                    { x: -1.4667, y: 6.2, label: 'V52', start: 0, display: 0 },
                    { x: -2, y: 6.2, label: 'V53', start: 0, display: 0 },
                    { x: -2, y: 4.8667, label: 'V54', start: 0, display: 0 },
                    { x: -1.4667, y: 4.8667, label: 'V55', start: 0, display: 0 },
                    { x: -1.4667, y: 5.4, label: 'V56', start: 0, display: 0 },
                    { x: -0.4, y: 5.4, label: 'V57', start: 0, display: 0 },
                    { x: -0.4, y: 4.4667, label: 'V58', start: 0, display: 0 },
                    { x: -1.8667, y: 4.4667, label: 'V59', start: 0, display: 0 },
                    { x: -1.8667, y: 3.4, label: 'V60', start: 0, display: 0 },
                    { x: -1.3333, y: 3.4, label: 'V61', start: 0, display: 0 },
                    { x: -1.3333, y: 3.9333, label: 'V62', start: 0, display: 0 },
                    { x: -0.4, y: 3.9333, label: 'V63', start: 0, display: 0 },
                    { x: -0.4, y: 3.0, label: 'V64', start: 0, display: 0 },

                ], 'arcs': [
                ], 'fill': '#ffffff'
            },
        ],
        text: []
    },

    'LeftArrow': {
        path: [{
            'vertex': [
                { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                { x: -4, y: 0, label: 'V2', start: 0, display: 0 },
                { x: -8, y: 4, label: 'V3', start: 0, display: 0 },
                { x: -4, y: 8, label: 'V4', start: 0, display: 0 },
                { x: -0, y: 8, label: 'V5', start: 0, display: 0 },
                { x: -2.667, y: 5.333, label: 'V6', start: 0, display: 0 },
                { x: -0.667, y: 5.333, label: 'V7', start: 0, display: 0 },
                { x: 3.333, y: 9.333, label: 'V8', start: 0, display: 0 },
                { x: 3.333, y: 18, label: 'V9', start: 0, display: 0 },
                { x: 6, y: 18, label: 'V10', start: 0, display: 0 },
                { x: 6, y: 9.333, label: 'V11', start: 0, display: 0 },
                { x: 0, y: 2.667, label: 'V12', start: 0, display: 0 },
                { x: -2.667, y: 2.667, label: 'V13', start: 0, display: 0 },

            ], 'arcs': [
                { start: 'V7', end: 'V8', radius: 4, direction: 1, sweep: 0 },
                { start: 'V11', end: 'V12', radius: 6.667, direction: 0, sweep: 0 },
            ]
        }],
    },
    'RightArrow': {
        path: [{
            'vertex': [
                { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                { x: 4, y: 0, label: 'V2', start: 0, display: 0 },
                { x: 8, y: 4, label: 'V3', start: 0, display: 0 },
                { x: 4, y: 8, label: 'V4', start: 0, display: 0 },
                { x: 0, y: 8, label: 'V5', start: 0, display: 0 },
                { x: 2.667, y: 5.333, label: 'V6', start: 0, display: 0 },
                { x: 0.667, y: 5.333, label: 'V7', start: 0, display: 0 },
                { x: -3.333, y: 9.333, label: 'V8', start: 0, display: 0 },
                { x: -3.333, y: 18, label: 'V9', start: 0, display: 0 },
                { x: -6, y: 18, label: 'V10', start: 0, display: 0 },
                { x: -6, y: 9.333, label: 'V11', start: 0, display: 0 },
                { x: 0, y: 2.667, label: 'V12', start: 0, display: 0 },
                { x: 2.667, y: 2.667, label: 'V13', start: 0, display: 0 },

            ], 'arcs': [
                { start: 'V7', end: 'V8', radius: 4, direction: 0, sweep: 0 },
                { start: 'V11', end: 'V12', radius: 6.667, direction: 1, sweep: 0 },
            ]
        }],
    },

    'LeftStraightArrow': {
        path: [{
            'vertex': [
                { x: -0, y: 0, label: 'V1', start: 1, display: 1 },
                { x: -4, y: 4, label: 'V2', start: 0, display: 0 },
                { x: -4, y: 8, label: 'V3', start: 0, display: 0 },
                { x: -1.333, y: 5.333, label: 'V4', start: 0, display: 0 },
                { x: -1.333, y: 14, label: 'V5', start: 0, display: 0 },
                { x: -5.327, y: 12.667, label: 'V6', start: 0, display: 0 },
                { x: -9.333, y: 12.668, label: 'V7', start: 0, display: 0 },
                { x: -6.667, y: 10, label: 'V8', start: 0, display: 0 },
                { x: -10.667, y: 10, label: 'V9', start: 0, display: 0 },
                { x: -14.667, y: 14, label: 'V10', start: 0, display: 0 },
                { x: -10.667, y: 18, label: 'V11', start: 0, display: 0 },
                { x: -6.667, y: 18, label: 'V12', start: 0, display: 0 },
                { x: -9.333, y: 15.333, label: 'V13', start: 0, display: 0 },
                { x: -5.340, y: 15.333, label: 'V14', start: 0, display: 0 },
                { x: -1.333, y: 19.333, label: 'V15', start: 0, display: 0 },
                { x: -1.333, y: 28, label: 'V16', start: 0, display: 0 },
                { x: 1.333, y: 28, label: 'V17', start: 0, display: 0 },
                { x: 1.333, y: 5.333, label: 'V18', start: 0, display: 0 },
                { x: 4, y: 8, label: 'V19', start: 0, display: 0 },
                { x: 4, y: 4, label: 'V20', start: 0, display: 0 },

            ], 'arcs': [
                { start: 'V5', end: 'V6', radius: 6.667, direction: 0, sweep: 0 },
                { start: 'V14', end: 'V15', radius: 4, direction: 1, sweep: 0 },
            ]
        }],
    },

    'RightStraightArrow': {
        path: [{
            'vertex': [
                { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                { x: 4, y: 4, label: 'V2', start: 0, display: 0 },
                { x: 4, y: 8, label: 'V3', start: 0, display: 0 },
                { x: 1.333, y: 5.333, label: 'V4', start: 0, display: 0 },
                { x: 1.333, y: 14, label: 'V5', start: 0, display: 0 },
                { x: 5.327, y: 12.667, label: 'V6', start: 0, display: 0 },
                { x: 9.333, y: 12.668, label: 'V7', start: 0, display: 0 },
                { x: 6.667, y: 10, label: 'V8', start: 0, display: 0 },
                { x: 10.667, y: 10, label: 'V9', start: 0, display: 0 },
                { x: 14.667, y: 14, label: 'V10', start: 0, display: 0 },
                { x: 10.667, y: 18, label: 'V11', start: 0, display: 0 },
                { x: 6.667, y: 18, label: 'V12', start: 0, display: 0 },
                { x: 9.333, y: 15.333, label: 'V13', start: 0, display: 0 },
                { x: 5.340, y: 15.333, label: 'V14', start: 0, display: 0 },
                { x: 1.333, y: 19.333, label: 'V15', start: 0, display: 0 },
                { x: 1.333, y: 28, label: 'V16', start: 0, display: 0 },
                { x: -1.333, y: 28, label: 'V17', start: 0, display: 0 },
                { x: -1.333, y: 5.333, label: 'V18', start: 0, display: 0 },
                { x: -4, y: 8, label: 'V19', start: 0, display: 0 },
                { x: -4, y: 4, label: 'V20', start: 0, display: 0 },

            ], 'arcs': [
                { start: 'V5', end: 'V6', radius: 6.667, direction: 1, sweep: 0 },
                { start: 'V14', end: 'V15', radius: 4, direction: 0, sweep: 0 },
            ]
        }],
    },

    'LeftPedestrian': {
        path: [ // 3.5.7.22
            {
                'vertex': [
                    { x: -0.384, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: -0.384, y: 2.565, label: 'V2', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 1.283, direction: 0, sweep: 0 },
                    { start: 'V2', end: 'V1', radius: 1.283, direction: 0, sweep: 0 },

                ],
            },
            {
                'vertex': [
                    { x: 2.016, y: 4.248, label: 'V3', start: 1, display: 1 },
                    { x: -2.378, y: 4.565, label: 'V4', start: 0, display: 0 },
                    { x: -2.86, y: 6.006, label: 'V5', start: 0, display: 0 },
                    { x: -4.498, y: 7.505, label: 'V6', start: 0, display: 0 },
                    { x: -4.035, y: 8.254, label: 'V7', start: 0, display: 0 },
                    { x: -2.136, y: 6.641, label: 'V8', start: 0, display: 0 },
                    { x: -1.552, y: 6, label: 'V9', start: 0, display: 0 },
                    { x: -1.343, y: 8.483, label: 'V10', start: 0, display: 0 },
                    { x: -2.803, y: 12.470, label: 'V11', start: 0, display: 0 },
                    { x: -3, y: 16, label: 'V12', start: 0, display: 0 },
                    { x: -2.003, y: 16, label: 'V13', start: 0, display: 0 },
                    { x: -1.343, y: 12.4, label: 'V14', start: 0, display: 0 },
                    { x: 0.181, y: 9.683, label: 'V15', start: 0, display: 0 },
                    { x: 0.181, y: 9.683, label: 'V16', start: 0, display: 0 },
                    { x: 0.981, y: 11.606, label: 'V17', start: 0, display: 0 },
                    { x: 3.73, y: 15.314, label: 'V18', start: 0, display: 0 },
                    { x: 4.498, y: 14.502, label: 'V19', start: 0, display: 0 },
                    { x: 4.498, y: 14.502, label: 'V20', start: 0, display: 0 },
                    { x: 2.943, y: 12.019, label: 'V21', start: 0, display: 0 },
                    { x: 1.171, y: 5.244, label: 'V22', start: 0, display: 0 },
                    { x: 1.698, y: 5.765, label: 'V23', start: 0, display: 0 },
                    { x: 2.498, y: 9.003, label: 'V24', start: 0, display: 0 },
                    { x: 3.292, y: 8.679, label: 'V25', start: 0, display: 0 },
                    { x: 2.702, y: 5.324, label: 'V26', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 2.467, direction: 0, sweep: 0 },

                ],
            },
        ],
    },

    'RightPedestrian': {
        path: [ // 3.5.7.22
            {
                'vertex': [
                    { x: 0.384, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 0.384, y: 2.565, label: 'V2', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 1.283, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V1', radius: 1.283, direction: 1, sweep: 0 },

                ],
            },
            {
                'vertex': [
                    { x: -2.016, y: 4.248, label: 'V3', start: 1, display: 1 },
                    { x: 2.378, y: 4.565, label: 'V4', start: 0, display: 0 },
                    { x: 2.86, y: 6.006, label: 'V5', start: 0, display: 0 },
                    { x: 4.498, y: 7.505, label: 'V6', start: 0, display: 0 },
                    { x: 4.035, y: 8.254, label: 'V7', start: 0, display: 0 },
                    { x: 2.136, y: 6.641, label: 'V8', start: 0, display: 0 },
                    { x: 1.552, y: 6, label: 'V9', start: 0, display: 0 },
                    { x: 1.343, y: 8.483, label: 'V10', start: 0, display: 0 },
                    { x: 2.803, y: 12.470, label: 'V11', start: 0, display: 0 },
                    { x: 3, y: 16, label: 'V12', start: 0, display: 0 },
                    { x: 2.003, y: 16, label: 'V13', start: 0, display: 0 },
                    { x: 1.343, y: 12.4, label: 'V14', start: 0, display: 0 },
                    { x: -0.181, y: 9.683, label: 'V15', start: 0, display: 0 },
                    { x: -0.181, y: 9.683, label: 'V16', start: 0, display: 0 },
                    { x: -0.981, y: 11.606, label: 'V17', start: 0, display: 0 },
                    { x: -3.73, y: 15.314, label: 'V18', start: 0, display: 0 },
                    { x: -4.498, y: 14.502, label: 'V19', start: 0, display: 0 },
                    { x: -4.498, y: 14.502, label: 'V20', start: 0, display: 0 },
                    { x: -2.943, y: 12.019, label: 'V21', start: 0, display: 0 },
                    { x: -1.171, y: 5.244, label: 'V22', start: 0, display: 0 },
                    { x: -1.698, y: 5.765, label: 'V23', start: 0, display: 0 },
                    { x: -2.498, y: 9.003, label: 'V24', start: 0, display: 0 },
                    { x: -3.292, y: 8.679, label: 'V25', start: 0, display: 0 },
                    { x: -2.702, y: 5.324, label: 'V26', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 2.467, direction: 1, sweep: 0 },

                ],
            },
        ],
    },

    'LeftDisabled': {
        path: [ // 3.5.7.24
            {
                'vertex': [
                    { x: 4.275, y: 2.032, label: 'V1', start: 1, display: 1 },
                    { x: 2.844, y: 3.547, label: 'V2', start: 0, display: 0 },
                    { x: 2.633, y: 6.637, label: 'V3', start: 0, display: 0 },
                    { x: -1.566, y: 6.637, label: 'V4', start: 0, display: 0 },
                    { x: -1.566, y: 7.992, label: 'V5', start: 0, display: 0 },
                    { x: 2.531, y: 7.992, label: 'V6', start: 0, display: 0 },
                    { x: 2.463, y: 9.075, label: 'V7', start: 0, display: 0 },
                    { x: -3.031, y: 9.075, label: 'V8', start: 0, display: 0 },
                    { x: -5.291, y: 14.002, label: 'V9', start: 0, display: 0 },
                    { x: -5.291, y: 14.002, label: 'V10', start: 0, display: 0 },
                    { x: -6.857, y: 13.350, label: 'V11', start: 0, display: 0 },
                    { x: -7.213, y: 14.281, label: 'V12', start: 0, display: 0 },
                    { x: -4.368, y: 15.458, label: 'V13', start: 0, display: 0 },
                    { x: -2.108, y: 10.523, label: 'V14', start: 0, display: 0 },
                    { x: 3.683, y: 10.523, label: 'V15', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 1.645, direction: 0, sweep: 1 },

                ],
            },
            {
                'vertex': [
                    { x: 4.605, y: 7.272, label: 'V16', start: 1, display: 1 },
                    { x: 4.495, y: 8.880, label: 'V17', start: 0, display: 0 },
                    { x: -1.998, y: 11.970, label: 'V18', start: 0, display: 0 },
                    { x: -2.980, y: 14.104, label: 'V19', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V17', end: 'V18', radius: 4, direction: 1, sweep: 1 },
                    { start: 'V19', end: 'V16', radius: 5.4, direction: 0, sweep: 1 },

                ],
            },
        ],
    },

    'RightDisabled': {
        path: [ // 3.5.7.24
            {
                'vertex': [
                    { x: -4.275, y: 2.032, label: 'V1', start: 1, display: 1 },
                    { x: -2.844, y: 3.547, label: 'V2', start: 0, display: 0 },
                    { x: -2.633, y: 6.637, label: 'V3', start: 0, display: 0 },
                    { x: 1.566, y: 6.637, label: 'V4', start: 0, display: 0 },
                    { x: 1.566, y: 7.992, label: 'V5', start: 0, display: 0 },
                    { x: -2.531, y: 7.992, label: 'V6', start: 0, display: 0 },
                    { x: -2.463, y: 9.075, label: 'V7', start: 0, display: 0 },
                    { x: 3.031, y: 9.075, label: 'V8', start: 0, display: 0 },
                    { x: 5.291, y: 14.002, label: 'V9', start: 0, display: 0 },
                    { x: 5.291, y: 14.002, label: 'V10', start: 0, display: 0 },
                    { x: 6.857, y: 13.350, label: 'V11', start: 0, display: 0 },
                    { x: 7.213, y: 14.281, label: 'V12', start: 0, display: 0 },
                    { x: 4.368, y: 15.458, label: 'V13', start: 0, display: 0 },
                    { x: 2.108, y: 10.523, label: 'V14', start: 0, display: 0 },
                    { x: -3.683, y: 10.523, label: 'V15', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 1.645, direction: 1, sweep: 1 },

                ],
            },
            {
                'vertex': [
                    { x: -4.605, y: 7.272, label: 'V16', start: 1, display: 1 },
                    { x: -4.495, y: 8.880, label: 'V17', start: 0, display: 0 },
                    { x: 1.998, y: 11.970, label: 'V18', start: 0, display: 0 },
                    { x: 2.980, y: 14.104, label: 'V19', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V17', end: 'V18', radius: 4, direction: 0, sweep: 1 },
                    { start: 'V19', end: 'V16', radius: 5.4, direction: 1, sweep: 1 },

                ],
            },
        ],
    }, 'LeftArrow': {
        path: [{
            'vertex': [
                { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                { x: -4, y: 0, label: 'V2', start: 0, display: 0 },
                { x: -8, y: 4, label: 'V3', start: 0, display: 0 },
                { x: -4, y: 8, label: 'V4', start: 0, display: 0 },
                { x: -0, y: 8, label: 'V5', start: 0, display: 0 },
                { x: -2.667, y: 5.333, label: 'V6', start: 0, display: 0 },
                { x: -0.667, y: 5.333, label: 'V7', start: 0, display: 0 },
                { x: 3.333, y: 9.333, label: 'V8', start: 0, display: 0 },
                { x: 3.333, y: 18, label: 'V9', start: 0, display: 0 },
                { x: 6, y: 18, label: 'V10', start: 0, display: 0 },
                { x: 6, y: 9.333, label: 'V11', start: 0, display: 0 },
                { x: 0, y: 2.667, label: 'V12', start: 0, display: 0 },
                { x: -2.667, y: 2.667, label: 'V13', start: 0, display: 0 },

            ], 'arcs': [
                { start: 'V7', end: 'V8', radius: 4, direction: 1, sweep: 0 },
                { start: 'V11', end: 'V12', radius: 6.667, direction: 0, sweep: 0 },
            ]
        }],
    },
    'RightArrow': {
        path: [{
            'vertex': [
                { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                { x: 4, y: 0, label: 'V2', start: 0, display: 0 },
                { x: 8, y: 4, label: 'V3', start: 0, display: 0 },
                { x: 4, y: 8, label: 'V4', start: 0, display: 0 },
                { x: 0, y: 8, label: 'V5', start: 0, display: 0 },
                { x: 2.667, y: 5.333, label: 'V6', start: 0, display: 0 },
                { x: 0.667, y: 5.333, label: 'V7', start: 0, display: 0 },
                { x: -3.333, y: 9.333, label: 'V8', start: 0, display: 0 },
                { x: -3.333, y: 18, label: 'V9', start: 0, display: 0 },
                { x: -6, y: 18, label: 'V10', start: 0, display: 0 },
                { x: -6, y: 9.333, label: 'V11', start: 0, display: 0 },
                { x: 0, y: 2.667, label: 'V12', start: 0, display: 0 },
                { x: 2.667, y: 2.667, label: 'V13', start: 0, display: 0 },

            ], 'arcs': [
                { start: 'V7', end: 'V8', radius: 4, direction: 0, sweep: 0 },
                { start: 'V11', end: 'V12', radius: 6.667, direction: 1, sweep: 0 },
            ]
        }],
    },

    'LeftBike': {
        path: [
            {
                'vertex': [
                    { x: -3.75, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: -3.75, y: 1, label: 'V2', start: 0, display: 0 },
                    { x: -6.2, y: 1, label: 'V3', start: 0, display: 0 },
                    { x: -5.17, y: 2.47, label: 'V4', start: 0, display: 0 },
                    { x: -5.14, y: 3.75, label: 'V5', start: 0, display: 0 },
                    { x: 4.30, y: 3.75, label: 'V6', start: 0, display: 0 },
                    { x: 5.15, y: 2.22, label: 'V7', start: 0, display: 0 },
                    { x: 2.50, y: 1.24, label: 'V8', start: 0, display: 0 },
                    { x: 2.50, y: 1, label: 'V9', start: 0, display: 0 },
                    { x: 7.25, y: 1, label: 'V10', start: 0, display: 0 },
                    { x: 7.25, y: 1.61, label: 'V11', start: 0, display: 0 },
                    { x: 6.14, y: 2.49, label: 'V12', start: 0, display: 0 },
                    { x: 5.45, y: 3.75, label: 'V13', start: 0, display: 0 },
                    { x: 6.97, y: 6.43, label: 'V14', start: 0, display: 0 },
                    { x: 4.03, y: 11.45, label: 'V15', start: 0, display: 0 },
                    { x: 2.42, y: 11.45, label: 'V16', start: 0, display: 0 },
                    { x: 1.65, y: 12.35, label: 'V17', start: 0, display: 0 },
                    { x: 1.84, y: 12.87, label: 'V18', start: 0, display: 0 },
                    { x: 2.25, y: 12.87, label: 'V19', start: 0, display: 0 },
                    { x: 2.25, y: 13.12, label: 'V20', start: 0, display: 0 },
                    { x: 1.93, y: 13.12, label: 'V21', start: 0, display: 0 },
                    { x: 1.93, y: 13.25, label: 'V22', start: 0, display: 0 },
                    { x: 1.62, y: 13.25, label: 'V23', start: 0, display: 0 },
                    { x: 1.62, y: 13.12, label: 'V24', start: 0, display: 0 },
                    { x: 1.25, y: 13.12, label: 'V25', start: 0, display: 0 },
                    { x: 1.25, y: 12.87, label: 'V26', start: 0, display: 0 },
                    { x: 1.53, y: 12.87, label: 'V27', start: 0, display: 0 },
                    { x: 1.37, y: 12.45, label: 'V28', start: 0, display: 0 },
                    { x: -0.5, y: 10.97, label: 'V29', start: 0, display: 0 },
                    { x: -6.03, y: 6.52, label: 'V30', start: 0, display: 0 },
                    { x: -6.15, y: 6.89, label: 'V31', start: 0, display: 0 },
                    { x: -7.05, y: 6.39, label: 'V32', start: 0, display: 0 },
                    { x: -6.01, y: 3.23, label: 'V33', start: 0, display: 0 },
                    { x: -6.05, y: 2.97, label: 'V34', start: 0, display: 0 },
                    { x: -7.43, y: 0.98, label: 'V35', start: 0, display: 0 },
                    { x: -6.93, y: 0, label: 'V36', start: 0, display: 0 },
                    { x: 4.03, y: 4.75, label: 'V37', start: 1, display: 0 },
                    { x: -5.46, y: 4.75, label: 'V38', start: 0, display: 0 },
                    { x: -5.70, y: 5.50, label: 'V39', start: 0, display: 0 },
                    { x: -0.11, y: 10.0, label: 'V40', start: 0, display: 0 },
                    { x: 0.35, y: 9.65, label: 'V41', start: 0, display: 0 },
                    { x: 0.16, y: 9.12, label: 'V42', start: 0, display: 0 },
                    { x: -0.25, y: 9.12, label: 'V43', start: 0, display: 0 },
                    { x: -0.25, y: 8.87, label: 'V44', start: 0, display: 0 },
                    { x: -0.07, y: 8.87, label: 'V45', start: 0, display: 0 },
                    { x: -0.07, y: 8.74, label: 'V46', start: 0, display: 0 },
                    { x: 0.39, y: 8.74, label: 'V47', start: 0, display: 0 },
                    { x: 0.39, y: 8.87, label: 'V48', start: 0, display: 0 },
                    { x: 0.75, y: 8.87, label: 'V49', start: 0, display: 0 },
                    { x: 0.75, y: 9.12, label: 'V50', start: 0, display: 0 },
                    { x: 0.48, y: 9.12, label: 'V51', start: 0, display: 0 },
                    { x: 0.63, y: 9.55, label: 'V52', start: 0, display: 0 },
                    { x: 1.27, y: 9.52, label: 'V53', start: 0, display: 0 },
                    { x: 5.03, y: 5.03, label: 'V54', start: 1, display: 0 },
                    { x: 2.14, y: 10.03, label: 'V55', start: 0, display: 0 },
                    { x: 2.41, y: 10.50, label: 'V56', start: 0, display: 0 },
                    { x: 4.03, y: 10.50, label: 'V57', start: 0, display: 0 },
                    { x: 6.10, y: 6.92, label: 'V58', start: 0, display: 0 },
                    { x: 6.60, y: 7.80, label: 'V60', start: 1, display: 0 },
                    { x: 5.04, y: 10.50, label: 'V61', start: 0, display: 0 },
                    { x: 8.14, y: 10.50, label: 'V62', start: 0, display: 0 },
                    { x: 7.47, y: 7.30, label: 'V63', start: 1, display: 0 },
                    { x: 9.45, y: 10.77, label: 'V64', start: 0, display: 0 },
                    { x: 9, y: 11.50, label: 'V65', start: 0, display: 0 },
                    { x: 5.04, y: 11.50, label: 'V66', start: 0, display: 0 },
                    { x: -6.48, y: 7.89, label: 'V67', start: 1, display: 0 },
                    { x: -6.84, y: 8.98, label: 'V68', start: 0, display: 0 },
                    { x: -8.71, y: 11.40, label: 'V69', start: 0, display: 0 },
                    { x: -9.24, y: 10.55, label: 'V70', start: 0, display: 0 },
                    { x: -7.79, y: 8.69, label: 'V71', start: 0, display: 0 },
                    { x: -7.36, y: 7.35, label: 'V72', start: 0, display: 0 },
                    { x: 1.01, y: 9.75, label: 'V73', start: 1, display: 0 },
                    { x: 1.01, y: 12.25, label: 'V74', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V4', end: 'V5', radius: 1.35, direction: 1, sweep: 0 },
                    { start: 'V11', end: 'V12', radius: 1, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V15', radius: 5, direction: 1, sweep: 1 },
                    { start: 'V16', end: 'V17', radius: 1.5, direction: 1, sweep: 0 },
                    { start: 'V28', end: 'V29', radius: 1.5, direction: 1, sweep: 0 },
                    { start: 'V31', end: 'V32', radius: 5, direction: 1, sweep: 1 },
                    { start: 'V33', end: 'V34', radius: 0.3, direction: 0, sweep: 0 },
                    { start: 'V35', end: 'V36', radius: 1.5, direction: 1, sweep: 0 },
                    { start: 'V40', end: 'V41', radius: 1.5, direction: 1, sweep: 0 },
                    { start: 'V45', end: 'V46', radius: 1.5, direction: 1, sweep: 0 },
                    { start: 'V55', end: 'V56', radius: 1.5, direction: 1, sweep: 0 },
                    { start: 'V57', end: 'V58', radius: 5, direction: 1, sweep: 0 },
                    { start: 'V60', end: 'V61', radius: 4, direction: 0, sweep: 0 },
                    { start: 'V64', end: 'V65', radius: 0.5, direction: 1, sweep: 0 },
                    { start: 'V66', end: 'V63', radius: 4, direction: 0, sweep: 1 },
                    { start: 'V68', end: 'V69', radius: 4.5, direction: 1, sweep: 0 },
                    { start: 'V69', end: 'V70', radius: 0.5, direction: 1, sweep: 0 },
                    { start: 'V70', end: 'V71', radius: 4.5, direction: 0, sweep: 0 },
                    { start: 'V72', end: 'V67', radius: 4, direction: 0, sweep: 1 },
                    { start: 'V73', end: 'V74', radius: 1.25, direction: 0, sweep: 0 },
                    { start: 'V74', end: 'V73', radius: 1.25, direction: 0, sweep: 0 },
                ]
            },
        ],
    },

    'RightBike': {
        path: [
            {
                'vertex': [
                    { x: 3.75, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 3.75, y: 1, label: 'V2', start: 0, display: 0 },
                    { x: 6.2, y: 1, label: 'V3', start: 0, display: 0 },
                    { x: 5.17, y: 2.47, label: 'V4', start: 0, display: 0 },
                    { x: 5.14, y: 3.75, label: 'V5', start: 0, display: 0 },
                    { x: -4.30, y: 3.75, label: 'V6', start: 0, display: 0 },
                    { x: -5.15, y: 2.22, label: 'V7', start: 0, display: 0 },
                    { x: -2.50, y: 1.24, label: 'V8', start: 0, display: 0 },
                    { x: -2.50, y: 1, label: 'V9', start: 0, display: 0 },
                    { x: -7.25, y: 1, label: 'V10', start: 0, display: 0 },
                    { x: -7.25, y: 1.61, label: 'V11', start: 0, display: 0 },
                    { x: -6.14, y: 2.49, label: 'V12', start: 0, display: 0 },
                    { x: -5.45, y: 3.75, label: 'V13', start: 0, display: 0 },
                    { x: -6.97, y: 6.43, label: 'V14', start: 0, display: 0 },
                    { x: -4.03, y: 11.45, label: 'V15', start: 0, display: 0 },
                    { x: -2.42, y: 11.45, label: 'V16', start: 0, display: 0 },
                    { x: -1.65, y: 12.35, label: 'V17', start: 0, display: 0 },
                    { x: -1.84, y: 12.87, label: 'V18', start: 0, display: 0 },
                    { x: -2.25, y: 12.87, label: 'V19', start: 0, display: 0 },
                    { x: -2.25, y: 13.12, label: 'V20', start: 0, display: 0 },
                    { x: -1.93, y: 13.12, label: 'V21', start: 0, display: 0 },
                    { x: -1.93, y: 13.25, label: 'V22', start: 0, display: 0 },
                    { x: -1.62, y: 13.25, label: 'V23', start: 0, display: 0 },
                    { x: -1.62, y: 13.12, label: 'V24', start: 0, display: 0 },
                    { x: -1.25, y: 13.12, label: 'V25', start: 0, display: 0 },
                    { x: -1.25, y: 12.87, label: 'V26', start: 0, display: 0 },
                    { x: -1.53, y: 12.87, label: 'V27', start: 0, display: 0 },
                    { x: -1.37, y: 12.45, label: 'V28', start: 0, display: 0 },
                    { x: 0.5, y: 10.97, label: 'V29', start: 0, display: 0 },
                    { x: 6.03, y: 6.52, label: 'V30', start: 0, display: 0 },
                    { x: 6.15, y: 6.89, label: 'V31', start: 0, display: 0 },
                    { x: 7.05, y: 6.39, label: 'V32', start: 0, display: 0 },
                    { x: 6.01, y: 3.23, label: 'V33', start: 0, display: 0 },
                    { x: 6.05, y: 2.97, label: 'V34', start: 0, display: 0 },
                    { x: 7.43, y: 0.98, label: 'V35', start: 0, display: 0 },
                    { x: 6.93, y: 0, label: 'V36', start: 0, display: 0 },
                    { x: -4.03, y: 4.75, label: 'V37', start: 1, display: 0 },
                    { x: 5.46, y: 4.75, label: 'V38', start: 0, display: 0 },
                    { x: 5.70, y: 5.50, label: 'V39', start: 0, display: 0 },
                    { x: 0.11, y: 10.0, label: 'V40', start: 0, display: 0 },
                    { x: -0.35, y: 9.65, label: 'V41', start: 0, display: 0 },
                    { x: -0.16, y: 9.12, label: 'V42', start: 0, display: 0 },
                    { x: 0.25, y: 9.12, label: 'V43', start: 0, display: 0 },
                    { x: -0.25, y: 8.87, label: 'V44', start: 0, display: 0 },
                    { x: 0.07, y: 8.87, label: 'V45', start: 0, display: 0 },
                    { x: 0.07, y: 8.74, label: 'V46', start: 0, display: 0 },
                    { x: -0.39, y: 8.74, label: 'V47', start: 0, display: 0 },
                    { x: -0.39, y: 8.87, label: 'V48', start: 0, display: 0 },
                    { x: -0.75, y: 8.87, label: 'V49', start: 0, display: 0 },
                    { x: -0.75, y: 9.12, label: 'V50', start: 0, display: 0 },
                    { x: -0.48, y: 9.12, label: 'V51', start: 0, display: 0 },
                    { x: -0.63, y: 9.55, label: 'V52', start: 0, display: 0 },
                    { x: -1.27, y: 9.52, label: 'V53', start: 0, display: 0 },
                    { x: -5.03, y: 5.03, label: 'V54', start: 1, display: 0 },
                    { x: -2.14, y: 10.03, label: 'V55', start: 0, display: 0 },
                    { x: -2.41, y: 10.50, label: 'V56', start: 0, display: 0 },
                    { x: -4.03, y: 10.50, label: 'V57', start: 0, display: 0 },
                    { x: -6.10, y: 6.92, label: 'V58', start: 0, display: 0 },
                    { x: -6.60, y: 7.80, label: 'V60', start: 1, display: 0 },
                    { x: -5.04, y: 10.50, label: 'V61', start: 0, display: 0 },
                    { x: -8.14, y: 10.50, label: 'V62', start: 0, display: 0 },
                    { x: -7.47, y: 7.30, label: 'V63', start: 1, display: 0 },
                    { x: -9.45, y: 10.77, label: 'V64', start: 0, display: 0 },
                    { x: -9, y: 11.50, label: 'V65', start: 0, display: 0 },
                    { x: -5.04, y: 11.50, label: 'V66', start: 0, display: 0 },
                    { x: 6.48, y: 7.89, label: 'V67', start: 1, display: 0 },
                    { x: 6.84, y: 8.98, label: 'V68', start: 0, display: 0 },
                    { x: 8.71, y: 11.40, label: 'V69', start: 0, display: 0 },
                    { x: 9.24, y: 10.55, label: 'V70', start: 0, display: 0 },
                    { x: 7.79, y: 8.69, label: 'V71', start: 0, display: 0 },
                    { x: 7.36, y: 7.35, label: 'V72', start: 0, display: 0 },
                    { x: -1.01, y: 9.75, label: 'V73', start: 1, display: 0 },
                    { x: -1.01, y: 12.25, label: 'V74', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V4', end: 'V5', radius: 1.35, direction: 0, sweep: 0 },
                    { start: 'V11', end: 'V12', radius: 1, direction: 0, sweep: 0 },
                    { start: 'V14', end: 'V15', radius: 5, direction: 0, sweep: 1 },
                    { start: 'V16', end: 'V17', radius: 1.5, direction: 0, sweep: 0 },
                    { start: 'V28', end: 'V29', radius: 1.5, direction: 0, sweep: 0 },
                    { start: 'V31', end: 'V32', radius: 5, direction: 0, sweep: 1 },
                    { start: 'V33', end: 'V34', radius: 0.3, direction: 1, sweep: 0 },
                    { start: 'V35', end: 'V36', radius: 1.5, direction: 0, sweep: 0 },
                    { start: 'V40', end: 'V41', radius: 1.5, direction: 0, sweep: 0 },
                    { start: 'V45', end: 'V46', radius: 1.5, direction: 0, sweep: 0 },
                    { start: 'V55', end: 'V56', radius: 1.5, direction: 0, sweep: 0 },
                    { start: 'V57', end: 'V58', radius: 5, direction: 0, sweep: 0 },
                    { start: 'V60', end: 'V61', radius: 4, direction: 1, sweep: 0 },
                    { start: 'V64', end: 'V65', radius: 0.5, direction: 0, sweep: 0 },
                    { start: 'V66', end: 'V63', radius: 4, direction: 1, sweep: 1 },
                    { start: 'V68', end: 'V69', radius: 4.5, direction: 0, sweep: 0 },
                    { start: 'V69', end: 'V70', radius: 0.5, direction: 0, sweep: 0 },
                    { start: 'V70', end: 'V71', radius: 4.5, direction: 1, sweep: 0 },
                    { start: 'V72', end: 'V67', radius: 4, direction: 1, sweep: 1 },
                    { start: 'V73', end: 'V74', radius: 1.25, direction: 1, sweep: 0 },
                    { start: 'V74', end: 'V73', radius: 1.25, direction: 1, sweep: 0 },
                ]
            },
        ],
    },

    'NoEntry': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8.25, y: 8.25, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16.5, label: 'V3', start: 0, display: 0 },
                    { x: -8.25, y: 8.25, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8.25, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8.25, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8.25, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8.25, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: 0, y: 0.25, label: 'V11', start: 1, display: 0 },
                    { x: 8, y: 8.25, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 16.25, label: 'V13', start: 0, display: 0 },
                    { x: -8, y: 8.25, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 6.75, label: 'V21', start: 1, display: 0 },
                    { x: 7.25, y: 6.75, label: 'V22', start: 0, display: 0, radius: 0.75 },
                    { x: 7.25, y: 9.75, label: 'V23', start: 0, display: 0, radius: 0.75 },
                    { x: -7.25, y: 9.75, label: 'V24', start: 0, display: 0, radius: 0.75 },
                    { x: -7.25, y: 6.75, label: 'V25', start: 0, display: 0, radius: 0.75 },
                ], 'arcs': [], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
    },

    'AllVehProhibited': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            }, {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },

        ],
    },

    'NoLeftTurn': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: -4.557, y: 5.173, label: 'V21', start: 1, display: 0 },
                    { x: -5.6, y: 5.973, label: 'V22', start: 0, display: 0 },
                    { x: -4.557, y: 6.773, label: 'V23', start: 0, display: 0 },
                    { x: 0.96, y: 6.773, label: 'V24', start: 0, display: 0, radius: 0.533 },
                    { x: 0.96, y: 13.333, label: 'V25', start: 0, display: 0 },
                    { x: 2.56, y: 13.333, label: 'V26', start: 0, display: 0 },
                    { x: 2.56, y: 5.173, label: 'V27', start: 0, display: 0, radius: 1.6 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                    { x: -5.056, y: 4.076, label: 'V11', start: 1, display: 0 },
                    { x: 3.924, y: 13.056, label: 'V12', start: 0, display: 0 },
                    { x: -3.924, y: 2.944, label: 'V13', start: 1, display: 0 },
                    { x: 5.056, y: 11.924, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 0, sweep: 0 },
                    { start: 'V14', end: 'V13', radius: 6.4, direction: 0, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            }, {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },

        ],
    },

    'NoRightTurn': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: 4.557, y: 5.173, label: 'V21', start: 1, display: 0 },
                    { x: 5.6, y: 5.973, label: 'V22', start: 0, display: 0 },
                    { x: 4.557, y: 6.773, label: 'V23', start: 0, display: 0 },
                    { x: -0.96, y: 6.773, label: 'V24', start: 0, display: 0, radius: 0.533 },
                    { x: -0.96, y: 13.333, label: 'V25', start: 0, display: 0 },
                    { x: -2.56, y: 13.333, label: 'V26', start: 0, display: 0 },
                    { x: -2.56, y: 5.173, label: 'V27', start: 0, display: 0, radius: 1.6 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                    { x: 5.056, y: 4.076, label: 'V11', start: 1, display: 0 },
                    { x: -3.924, y: 13.056, label: 'V12', start: 0, display: 0 },
                    { x: 3.924, y: 2.944, label: 'V13', start: 1, display: 0 },
                    { x: -5.056, y: 11.924, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V11', radius: 6.4, direction: 0, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 0, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            }, {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },

        ],
    },

    'NoUTurn': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: -3.467, y: 13.067, label: 'V21', start: 1, display: 0 },
                    { x: -3.467, y: 6.667, label: 'V22', start: 0, display: 0 },
                    { x: 3.467, y: 6.667, label: 'V23', start: 0, display: 0 },
                    { x: 3.467, y: 11.677, label: 'V24', start: 0, display: 0 },
                    { x: 2.4, y: 13.067, label: 'V25', start: 0, display: 0 },
                    { x: 1.333, y: 11.677, label: 'V26', start: 0, display: 0 },
                    { x: 1.333, y: 6.667, label: 'V27', start: 0, display: 0 },
                    { x: -1.333, y: 6.667, label: 'V28', start: 0, display: 0 },
                    { x: -1.333, y: 13.067, label: 'V29', start: 0, display: 0 },
                    { x: -2.4, y: 11.0667, label: 'V30', start: 0, display: 0 },

                ], 'arcs': [
                    { start: 'V22', end: 'V23', radius: 3.467, direction: 1, sweep: 0 },
                    { start: 'V27', end: 'V28', radius: 1.333, direction: 0, sweep: 0 },
                ], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                    { x: -5.899, y: 5.518, label: 'V11', start: 1, display: 0 },
                    { x: 5.099, y: 11.868, label: 'V12', start: 0, display: 0 },
                    { x: -5.099, y: 4.132, label: 'V13', start: 1, display: 0 },
                    { x: 5.899, y: 10.482, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 0, sweep: 0 },
                    { start: 'V14', end: 'V13', radius: 6.4, direction: 0, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            }, {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },

        ],
    },

    '2.3WidthLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: 4.516, y: 8, label: 'V11', start: 1, display: 0 },
                    { x: 5.867, y: 6.667, label: 'V12', start: 0, display: 0 },
                    { x: 5.867, y: 9.333, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -4.516, y: 8, label: 'V11', start: 1, display: 0 },
                    { x: -5.867, y: 6.667, label: 'V12', start: 0, display: 0 },
                    { x: -5.867, y: 9.333, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            }, {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },

        ],
        text: [
            { character: '2.3', x: -4.52, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 1.45, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    '2.5WidthLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: 4.466, y: 8, label: 'V11', start: 1, display: 0 },
                    { x: 5.817, y: 6.667, label: 'V12', start: 0, display: 0 },
                    { x: 5.817, y: 9.333, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -4.466, y: 8, label: 'V11', start: 1, display: 0 },
                    { x: -5.817, y: 6.667, label: 'V12', start: 0, display: 0 },
                    { x: -5.817, y: 9.333, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
        text: [
            { character: '2.5', x: -4.47, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 1.4, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    '2.7WidthLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: 4.25, y: 8, label: 'V11', start: 1, display: 0 },
                    { x: 5.60, y: 6.667, label: 'V12', start: 0, display: 0 },
                    { x: 5.60, y: 9.333, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -4.25, y: 8, label: 'V11', start: 1, display: 0 },
                    { x: -5.601, y: 6.667, label: 'V12', start: 0, display: 0 },
                    { x: -5.601, y: 9.333, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
        text: [
            { character: '2.7', x: -4.25, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 1.184, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    '2.9WidthLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: 4.466, y: 8, label: 'V11', start: 1, display: 0 },
                    { x: 5.817, y: 6.667, label: 'V12', start: 0, display: 0 },
                    { x: 5.817, y: 9.333, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -4.466, y: 8, label: 'V11', start: 1, display: 0 },
                    { x: -5.817, y: 6.667, label: 'V12', start: 0, display: 0 },
                    { x: -5.817, y: 9.333, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
        text: [
            { character: '2.9', x: -4.47, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 1.4, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    '2HeightLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 2.133, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 2.133, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 4.267, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 13.866, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 13.866, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 11.733, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
        text: [
            { character: '2', x: -3.111, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 0.017, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },
    '3HeightLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 2.133, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 2.133, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 4.267, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 13.866, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 13.866, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 11.733, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
        text: [
            { character: '3', x: -3.2, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 0.107, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },
    '3.5HeightLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 2.133, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 2.133, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 4.267, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 13.866, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 13.866, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 11.733, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
        text: [
            { character: '3.5', x: -4.853, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 1.76, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },
    '4HeightLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 2.133, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 2.133, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 4.267, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 13.866, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 13.866, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 11.733, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
        text: [
            { character: '4', x: -3.218, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 0.124, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },
    '4.1HeightLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 2.133, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 2.133, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 4.267, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 13.866, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 13.866, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 11.733, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
        text: [
            { character: '4.1', x: -4.462, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 1.369, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },
    '4.2HeightLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 2.133, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 2.133, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 4.267, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 13.866, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 13.866, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 11.733, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
        text: [
            { character: '4.2', x: -4.836, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 1.742, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },
    '4.3HeightLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 2.133, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 2.133, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 4.267, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 13.866, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 13.866, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 11.733, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
        text: [
            { character: '4.3', x: -4.924, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 1.831, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },
    '4.4HeightLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 2.133, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 2.133, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 4.267, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 13.866, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 13.866, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 11.733, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
        text: [
            { character: '4.4', x: -4.942, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 1.849, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },
    '4.5HeightLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 2.133, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 2.133, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 4.267, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 13.866, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 13.866, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 11.733, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
        text: [
            { character: '4.5', x: -4.871, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 1.778, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },
    '4.6HeightLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 2.133, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 2.133, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 4.267, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 13.866, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 13.866, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 11.733, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
        text: [
            { character: '4.6', x: -4.871, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 1.778, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },
    '4.7HeightLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 2.133, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 2.133, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 4.267, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 13.866, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 13.866, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 11.733, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
        text: [
            { character: '4.7', x: -4.853, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 1.76, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },
    '4.8HeightLimit': {
        path: [
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 0 },
                    { x: 8, y: 8, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 16, label: 'V3', start: 0, display: 0 },
                    { x: -8, y: 8, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V1', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(224, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 1.6, label: 'V11', start: 1, display: 0 },
                    { x: 6.4, y: 8, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 14.4, label: 'V13', start: 0, display: 0 },
                    { x: -6.4, y: 8, label: 'V14', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V11', end: 'V12', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V12', end: 'V13', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V13', end: 'V14', radius: 6.4, direction: 1, sweep: 0 },
                    { start: 'V14', end: 'V11', radius: 6.4, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(255, 255, 255)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 2.133, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 2.133, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 4.267, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: -1.6, y: 13.866, label: 'V11', start: 1, display: 0 },
                    { x: 1.6, y: 13.866, label: 'V12', start: 0, display: 0 },
                    { x: 0, y: 11.733, label: 'V13', start: 0, display: 0 },
                ], 'arcs': [], 'fill': 'rgb(0, 0, 0)'
            },
            {
                'vertex': [
                    { x: 0, y: 8, label: 'C1', start: 1, display: 1 },
                ],
            },
        ],
        text: [
            { character: '4.8', x: -4.942, y: -6.2, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
            { character: 'm', x: 1.849, y: -6.9, fontSize: 3.556, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Lozenge': {
        path: [
            {
                'vertex': [
                    { x: 0, y: -1, label: 'V1', start: 1, display: 1 },
                    { x: 2, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 0, y: 1, label: 'V3', start: 0, display: 0 },
                    { x: -2, y: 0, label: 'V4', start: 0, display: 0 },
                ], 'arcs': []
            }
        ]
    },

};

const symbolsTemplateAlt = {
    'Hospital': {
        path: [ // 3.5.7.31
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 0, y: 17, label: 'V2', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V1', end: 'V2', radius: 8.5, direction: 1, sweep: 0 },
                    { start: 'V2', end: 'V1', radius: 8.5, direction: 1, sweep: 0 },
                ], 'fill': '#000000'
            },
            {
                'vertex': [
                    { x: 0, y: 0.5, label: 'V3', start: 1, display: 1 },
                    { x: 0, y: 16.5, label: 'V4', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 8, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V3', radius: 8, direction: 1, sweep: 0 },
                ], 'fill': '#ffffff'
            },
            {
                'vertex': [
                    { x: 0, y: 2, label: 'V5', start: 1, display: 0 },
                    { x: 2, y: 2, label: 'V6', start: 0, display: 0 },
                    { x: 2, y: 6.5, label: 'V7', start: 0, display: 0 },
                    { x: 6.5, y: 6.5, label: 'V8', start: 0, display: 0 },
                    { x: 6.5, y: 10.5, label: 'V9', start: 0, display: 0 },
                    { x: 2, y: 10.5, label: 'V10', start: 0, display: 0 },
                    { x: 2, y: 15, label: 'V11', start: 0, display: 0 },
                    { x: -2, y: 15, label: 'V12', start: 0, display: 0 },
                    { x: -2, y: 10.5, label: 'V13', start: 0, display: 0 },
                    { x: -6.5, y: 10.5, label: 'V14', start: 0, display: 0 },
                    { x: -6.5, y: 6.5, label: 'V15', start: 0, display: 0 },
                    { x: -2, y: 6.5, label: 'V16', start: 0, display: 0 },
                    { x: -2, y: 2, label: 'V17', start: 0, display: 0 },
                ], 'arcs': [], 'fill': '#ff0101'
            },
        ],
        text: []
    },
    'Route1': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': '#000000'
            },
            {
                'vertex': [
                    { x: 0, y: 0.5, label: 'V1', start: 1, display: 1 },
                    { x: 4, y: 0.5, label: 'V2', start: 0, display: 0 },
                    { x: 4, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 8.5, label: 'V4', start: 0, display: 0 },
                    { x: -4, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4, y: 0.5, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 5.5, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 5.5, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '1', x: -1.56, y: -0.7, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },
    'Route2': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': '#000000'
            },
            {
                'vertex': [
                    { x: 0, y: 0.5, label: 'V1', start: 1, display: 1 },
                    { x: 4, y: 0.5, label: 'V2', start: 0, display: 0 },
                    { x: 4, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 8.5, label: 'V4', start: 0, display: 0 },
                    { x: -4, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4, y: 0.5, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 5.5, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 5.5, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '2', x: -2.4, y: -0.7, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route3': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': '#000000'
            },
            {
                'vertex': [
                    { x: 0, y: 0.5, label: 'V1', start: 1, display: 1 },
                    { x: 4, y: 0.5, label: 'V2', start: 0, display: 0 },
                    { x: 4, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 8.5, label: 'V4', start: 0, display: 0 },
                    { x: -4, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4, y: 0.5, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 5.5, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 5.5, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '3', x: -2.54, y: -0.6, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route4': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': '#000000'
            },
            {
                'vertex': [
                    { x: 0, y: 0.5, label: 'V1', start: 1, display: 1 },
                    { x: 4, y: 0.5, label: 'V2', start: 0, display: 0 },
                    { x: 4, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 8.5, label: 'V4', start: 0, display: 0 },
                    { x: -4, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4, y: 0.5, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 5.5, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 5.5, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '4', x: -2.64, y: -0.7, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route5': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': '#000000'
            },
            {
                'vertex': [
                    { x: 0, y: 0.5, label: 'V1', start: 1, display: 1 },
                    { x: 4, y: 0.5, label: 'V2', start: 0, display: 0 },
                    { x: 4, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 8.5, label: 'V4', start: 0, display: 0 },
                    { x: -4, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4, y: 0.5, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 5.5, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 5.5, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '5', x: -2.44, y: -0.7, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route6': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': '#000000'
            },
            {
                'vertex': [
                    { x: 0, y: 0.5, label: 'V1', start: 1, display: 1 },
                    { x: 4, y: 0.5, label: 'V2', start: 0, display: 0 },
                    { x: 4, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 8.5, label: 'V4', start: 0, display: 0 },
                    { x: -4, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4, y: 0.5, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 5.5, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 5.5, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '6', x: -2.52, y: -0.6, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route7': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': '#000000'
            },
            {
                'vertex': [
                    { x: 0, y: 0.5, label: 'V1', start: 1, display: 1 },
                    { x: 4, y: 0.5, label: 'V2', start: 0, display: 0 },
                    { x: 4, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 8.5, label: 'V4', start: 0, display: 0 },
                    { x: -4, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4, y: 0.5, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 5.5, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 5.5, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '7', x: -2.08, y: -0.7, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route8': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': '#000000'
            },
            {
                'vertex': [
                    { x: 0, y: 0.5, label: 'V1', start: 1, display: 1 },
                    { x: 4, y: 0.5, label: 'V2', start: 0, display: 0 },
                    { x: 4, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 8.5, label: 'V4', start: 0, display: 0 },
                    { x: -4, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4, y: 0.5, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 5.5, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 5.5, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '8', x: -2.76, y: -0.6, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route9': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 4.5, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 4.5, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V4', start: 0, display: 0 },
                    { x: -4.5, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4.5, y: 0, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 6, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 6, direction: 1, sweep: 0 },
                ], 'fill': '#000000'
            },
            {
                'vertex': [
                    { x: 0, y: 0.5, label: 'V1', start: 1, display: 1 },
                    { x: 4, y: 0.5, label: 'V2', start: 0, display: 0 },
                    { x: 4, y: 3, label: 'V3', start: 0, display: 0 },
                    { x: 0, y: 8.5, label: 'V4', start: 0, display: 0 },
                    { x: -4, y: 3, label: 'V5', start: 0, display: 0 },
                    { x: -4, y: 0.5, label: 'V6', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 5.5, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 5.5, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '9', x: -2.56, y: -0.6, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route10': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 6, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 6, y: 3.8295, label: 'V3', start: 0, display: 0 },
                    { x: 4.8, y: 6.6861, label: 'V4', start: 0, display: 0 }, //https://www.wolframalpha.com/input?i2d=true&i=72.25+%3D+Power%5B%5C%2840%29x-7%5C%2841%29%2C2%5D+%2B+Power%5B%5C%2840%29y-0.2412%5C%2841%29%2C2%5D%5C%2844%29+12.25%3D+Power%5B%5C%2840%29x-3.5%5C%2841%29%2C2%5D+%2B+Power%5By%2B3.32952%2C2%5D
                    { x: 0, y: 9, label: 'V5', start: 0, display: 0 },
                    { x: -4.8, y: 6.6861, label: 'V6', start: 0, display: 0 },
                    { x: -6, y: 3.8295, label: 'V7', start: 0, display: 0 },
                    { x: -6, y: 0, label: 'V8', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 4, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 9, direction: 1, sweep: 0 },
                    { start: 'V5', end: 'V6', radius: 9, direction: 1, sweep: 0 },
                    { start: 'V6', end: 'V7', radius: 4, direction: 1, sweep: 0 },
                ], 'fill': '#000000'
            },
            {
                'vertex': [
                    { x: 0, y: 0.5, label: 'V1', start: 1, display: 1 },
                    { x: 5.5, y: 0.5, label: 'V2', start: 0, display: 0 },
                    { x: 5.5, y: 3.8295, label: 'V3', start: 0, display: 0 },
                    { x: 4.45, y: 6.329, label: 'V4', start: 0, display: 0 },
                    { x: 0, y: 8.5, label: 'V5', start: 0, display: 0 },
                    { x: -4.45, y: 6.329, label: 'V6', start: 0, display: 0 },
                    { x: -5.5, y: 3.8295, label: 'V7', start: 0, display: 0 },
                    { x: -5.5, y: 0.5, label: 'V8', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 3.5, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 8.5, direction: 1, sweep: 0 },
                    { start: 'V5', end: 'V6', radius: 8.5, direction: 1, sweep: 0 },
                    { start: 'V6', end: 'V7', radius: 3.5, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '10', x: -4.22, y: -0.5, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route11': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 6, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 6, y: 3.8295, label: 'V3', start: 0, display: 0 },
                    { x: 4.8, y: 6.6861, label: 'V4', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V5', start: 0, display: 0 },
                    { x: -4.8, y: 6.6861, label: 'V6', start: 0, display: 0 },
                    { x: -6, y: 3.8295, label: 'V7', start: 0, display: 0 },
                    { x: -6, y: 0, label: 'V8', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 4, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 9, direction: 1, sweep: 0 },
                    { start: 'V5', end: 'V6', radius: 9, direction: 1, sweep: 0 },
                    { start: 'V6', end: 'V7', radius: 4, direction: 1, sweep: 0 },
                ], 'fill': '#000000'
            },
            {
                'vertex': [
                    { x: 0, y: 0.5, label: 'V1', start: 1, display: 1 },
                    { x: 5.5, y: 0.5, label: 'V2', start: 0, display: 0 },
                    { x: 5.5, y: 3.8295, label: 'V3', start: 0, display: 0 },
                    { x: 4.45, y: 6.329, label: 'V4', start: 0, display: 0 },
                    { x: 0, y: 8.5, label: 'V5', start: 0, display: 0 },
                    { x: -4.45, y: 6.329, label: 'V6', start: 0, display: 0 },
                    { x: -5.5, y: 3.8295, label: 'V7', start: 0, display: 0 },
                    { x: -5.5, y: 0.5, label: 'V8', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 3.5, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 8.5, direction: 1, sweep: 0 },
                    { start: 'V5', end: 'V6', radius: 8.5, direction: 1, sweep: 0 },
                    { start: 'V6', end: 'V7', radius: 3.5, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '11', x: -3.12, y: -0.5, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },

    'Route12': {
        path: [ // 3.5.7.7
            {
                'vertex': [
                    { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
                    { x: 6, y: 0, label: 'V2', start: 0, display: 0 },
                    { x: 6, y: 3.8295, label: 'V3', start: 0, display: 0 },
                    { x: 4.8, y: 6.6861, label: 'V4', start: 0, display: 0 },
                    { x: 0, y: 9, label: 'V5', start: 0, display: 0 },
                    { x: -4.8, y: 6.6861, label: 'V6', start: 0, display: 0 },
                    { x: -6, y: 3.8295, label: 'V7', start: 0, display: 0 },
                    { x: -6, y: 0, label: 'V8', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 4, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 9, direction: 1, sweep: 0 },
                    { start: 'V5', end: 'V6', radius: 9, direction: 1, sweep: 0 },
                    { start: 'V6', end: 'V7', radius: 4, direction: 1, sweep: 0 },
                ], 'fill': '#000000'
            },
            {
                'vertex': [
                    { x: 0, y: 0.5, label: 'V1', start: 1, display: 1 },
                    { x: 5.5, y: 0.5, label: 'V2', start: 0, display: 0 },
                    { x: 5.5, y: 3.8295, label: 'V3', start: 0, display: 0 },
                    { x: 4.45, y: 6.329, label: 'V4', start: 0, display: 0 },
                    { x: 0, y: 8.5, label: 'V5', start: 0, display: 0 },
                    { x: -4.45, y: 6.329, label: 'V6', start: 0, display: 0 },
                    { x: -5.5, y: 3.8295, label: 'V7', start: 0, display: 0 },
                    { x: -5.5, y: 0.5, label: 'V8', start: 0, display: 0 },
                ], 'arcs': [
                    { start: 'V3', end: 'V4', radius: 3.5, direction: 1, sweep: 0 },
                    { start: 'V4', end: 'V5', radius: 8.5, direction: 1, sweep: 0 },
                    { start: 'V5', end: 'V6', radius: 8.5, direction: 1, sweep: 0 },
                    { start: 'V6', end: 'V7', radius: 3.5, direction: 1, sweep: 0 },
                ], 'fill': 'rgb(233, 181, 0)'
            },
        ],
        text: [
            { character: '12', x: -3.96, y: -0.5, fontSize: 8 * 0.94, fontFamily: 'TransportHeavy', fill: '#000000' },
        ]
    },
}

const textWidthMedium = [{ char: ' ', width: 50, shortWidth: 0 }, { char: 'A', width: 136, shortWidth: 0 }, { char: 'B', width: 147, shortWidth: 0 }, { char: 'C', width: 148, shortWidth: 0 }, { char: 'D', width: 154, shortWidth: 0 }, { char: 'E', width: 132, shortWidth: 0 }, { char: 'F', width: 119, shortWidth: 0 }, { char: 'G', width: 155, shortWidth: 0 }, { char: 'H', width: 160, shortWidth: 0 }, { char: 'I', width: 73, shortWidth: 0 }, { char: 'J', width: 93, shortWidth: 0 }, { char: 'K', width: 138, shortWidth: 0 }, { char: 'L', width: 107, shortWidth: 0 }, { char: 'M', width: 184, shortWidth: 0 }, { char: 'N', width: 168, shortWidth: 0 }, { char: 'O', width: 156, shortWidth: 0 }, { char: 'P', width: 130, shortWidth: 0 }, { char: 'Q', width: 158, shortWidth: 0 }, { char: 'R', width: 141, shortWidth: 0 }, { char: 'S', width: 137, shortWidth: 0 }, { char: 'T', width: 109, shortWidth: 105 }, { char: 'U', width: 154, shortWidth: 0 }, { char: 'V', width: 130, shortWidth: 120 }, { char: 'W', width: 183, shortWidth: 189 }, { char: 'X', width: 128, shortWidth: 0 }, { char: 'Y', width: 123, shortWidth: 118 }, { char: 'Z', width: 119, shortWidth: 0 }, { char: 'a', width: 111, shortWidth: 104 }, { char: 'b', width: 117, shortWidth: 0 }, { char: 'c', width: 103, shortWidth: 0 }, { char: 'd', width: 119, shortWidth: 0 }, { char: 'e', width: 109, shortWidth: 102 }, { char: 'f', width: 75, shortWidth: 0 }, { char: 'g', width: 114, shortWidth: 107 }, { char: 'h', width: 112, shortWidth: 0 }, { char: 'i', width: 54, shortWidth: 0 }, { char: 'j', width: 58, shortWidth: 0 }, { char: 'k', width: 108, shortWidth: 0 }, { char: 'l', width: 62, shortWidth: 0 }, { char: 'm', width: 164, shortWidth: 0 }, { char: 'n', width: 112, shortWidth: 0 }, { char: 'o', width: 118, shortWidth: 111 }, { char: 'p', width: 118, shortWidth: 0 }, { char: 'q', width: 118, shortWidth: 0 }, { char: 'r', width: 73, shortWidth: 59 }, { char: 's', width: 97, shortWidth: 95 }, { char: 't', width: 81, shortWidth: 0 }, { char: 'u', width: 115, shortWidth: 101 }, { char: 'v', width: 98, shortWidth: 0 }, { char: 'w', width: 147, shortWidth: 145 }, { char: 'x', width: 104, shortWidth: 0 }, { char: 'y', width: 98, shortWidth: 96 }, { char: 'z', width: 97, shortWidth: 0 }, { char: '1', width: 78, shortWidth: 0 }, { char: '2', width: 120, shortWidth: 0 }, { char: '3', width: 127, shortWidth: 0 }, { char: '4', width: 132, shortWidth: 0 }, { char: '5', width: 122, shortWidth: 0 }, { char: '6', width: 126, shortWidth: 0 }, { char: '7', width: 104, shortWidth: 0 }, { char: '8', width: 130, shortWidth: 0 }, { char: '9', width: 128, shortWidth: 0 }, { char: '0', width: 133, shortWidth: 0 }, { char: '¼', width: 123, shortWidth: 0 }, { char: '½', width: 132, shortWidth: 0 }, { char: '¾', width: 148, shortWidth: 0 }, { char: ',', width: 53, shortWidth: 0 }, { char: '.', width: 53, shortWidth: 0 }, { char: "'", width: 39, shortWidth: 0 }, { char: ':', width: 53, shortWidth: 0 }, { char: '•', width: 53, shortWidth: 0 }, { char: '、', width: 53, shortWidth: 0 }, { char: '-', width: 66, shortWidth: 0 }, { char: '&', width: 126, shortWidth: 0 }, { char: '(', width: 105, shortWidth: 0 }, { char: ')', width: 105, shortWidth: 0 }, { char: '/', width: 85, shortWidth: 0 }, { char: '$', width: 100, shortWidth: 0 }, { char: '%', width: 160, shortWidth: 0 }, { char: '"', width: 92, shortWidth: 0 }, { char: '"', width: 92, shortWidth: 0 }, { char: '⧫', width: 100, shortWidth: 0 }]
const textWidthHeavy = [{ char: ' ', width: 50, shortWidth: 0 }, { char: 'A', width: 142, shortWidth: 0 }, { char: 'B', width: 146, shortWidth: 0 }, { char: 'C', width: 151, shortWidth: 0 }, { char: 'D', width: 150, shortWidth: 0 }, { char: 'E', width: 136, shortWidth: 0 }, { char: 'F', width: 121, shortWidth: 0 }, { char: 'G', width: 156, shortWidth: 0 }, { char: 'H', width: 159, shortWidth: 0 }, { char: 'I', width: 73, shortWidth: 0 }, { char: 'J', width: 95, shortWidth: 0 }, { char: 'K', width: 138, shortWidth: 0 }, { char: 'L', width: 118, shortWidth: 0 }, { char: 'M', width: 186, shortWidth: 0 }, { char: 'N', width: 168, shortWidth: 0 }, { char: 'O', width: 158, shortWidth: 0 }, { char: 'P', width: 134, shortWidth: 0 }, { char: 'Q', width: 161, shortWidth: 0 }, { char: 'R', width: 148, shortWidth: 0 }, { char: 'S', width: 146, shortWidth: 0 }, { char: 'T', width: 118, shortWidth: 113 }, { char: 'U', width: 157, shortWidth: 0 }, { char: 'V', width: 133, shortWidth: 127 }, { char: 'W', width: 193, shortWidth: 196 }, { char: 'X', width: 130, shortWidth: 0 }, { char: 'Y', width: 128, shortWidth: 125 }, { char: 'Z', width: 119, shortWidth: 0 }, { char: 'a', width: 111, shortWidth: 104 }, { char: 'b', width: 117, shortWidth: 0 }, { char: 'c', width: 107, shortWidth: 0 }, { char: 'd', width: 119, shortWidth: 0 }, { char: 'e', width: 110, shortWidth: 103 }, { char: 'f', width: 79, shortWidth: 0 }, { char: 'g', width: 117, shortWidth: 110 }, { char: 'h', width: 119, shortWidth: 0 }, { char: 'i', width: 55, shortWidth: 0 }, { char: 'j', width: 71, shortWidth: 0 }, { char: 'k', width: 114, shortWidth: 0 }, { char: 'l', width: 63, shortWidth: 0 }, { char: 'm', width: 173, shortWidth: 0 }, { char: 'n', width: 119, shortWidth: 0 }, { char: 'o', width: 115, shortWidth: 107 }, { char: 'p', width: 120, shortWidth: 0 }, { char: 'q', width: 120, shortWidth: 0 }, { char: 'r', width: 80, shortWidth: 67 }, { char: 's', width: 100, shortWidth: 98 }, { char: 't', width: 84, shortWidth: 0 }, { char: 'u', width: 120, shortWidth: 107 }, { char: 'v', width: 107, shortWidth: 0 }, { char: 'w', width: 160, shortWidth: 154 }, { char: 'x', width: 110, shortWidth: 0 }, { char: 'y', width: 106, shortWidth: 104 }, { char: 'z', width: 93, shortWidth: 0 }, { char: '1', width: 84, shortWidth: 0 }, { char: '2', width: 125, shortWidth: 0 }, { char: '3', width: 136, shortWidth: 0 }, { char: '4', width: 138, shortWidth: 0 }, { char: '5', width: 130, shortWidth: 0 }, { char: '6', width: 129, shortWidth: 0 }, { char: '7', width: 107, shortWidth: 0 }, { char: '8', width: 138, shortWidth: 0 }, { char: '9', width: 129, shortWidth: 0 }, { char: '0', width: 145, shortWidth: 0 }, { char: '¼', width: 130, shortWidth: 0 }, { char: '½', width: 136, shortWidth: 0 }, { char: '¾', width: 162, shortWidth: 0 }, { char: ',', width: 56, shortWidth: 0 }, { char: '.', width: 56, shortWidth: 0 }, { char: "'", width: 41, shortWidth: 0 }, { char: ':', width: 56, shortWidth: 0 }, { char: '•', width: 56, shortWidth: 0 }, { char: '、', width: 53, shortWidth: 0 }, { char: '-', width: 71, shortWidth: 0 }, { char: '&', width: 126, shortWidth: 0 }, { char: '(', width: 115, shortWidth: 0 }, { char: ')', width: 115, shortWidth: 0 }, { char: '/', width: 88, shortWidth: 0 }, { char: '$', width: 100, shortWidth: 0 }, { char: '%', width: 160, shortWidth: 0 }, { char: '"', width: 92, shortWidth: 0 }, { char: '"', width: 92, shortWidth: 0 }]

const EngDestinations = [{ "Hong Kong Island": ["Hong Kong", "Hong Kong(E)", "Hong Kong(C)", "Hong Kong(W)", "Aberdeen", "Ap Lei Chau", "Causeway Bay", "Central", "Chai Wan", "Chung Hom Kok", "Cyberport", "Hang Fa Tsuen", "Happy Valley", "Kennedy Town", "Kornhill", "Mid-levels", "North Point", "Pok Fu Lam", "Quarry Bay", "Repulse Bay", "Sai Wan", "Sai Wan Ho", "Sai Ying Pun", "Shau Kei Wan", "Shek O", "Shek Tong Tsui", "Sheung Wan", "Shouson Hill", "Stanley", "Siu Sai Wan", "Tai Hang", "Tai Koo Shing", "The Peak", "Wah Fu", "Wan Chai", "Wan Chai(N)", "Wong Chuk Hang"] },
{ "Kowloon": ["Kowloon", "Kowloon(C)", "Kowloon(E)", "Kowloon(W)", "Beacon Hill", "Cha Kwo Ling", "Cheung Sha Wan", "Cheung Sha Wan(W)", "Choi Hung", "Chuk Yuen", "Cruise Terminal", "Diamond Hill", "Hang Hau", "Ho Man Tin", "Hung Hom", "Hung Hom Bay", "Kai Tak", "King's Park", "Kowloon Bay", "Kowloon City", "Kowloon Tong", "Kwun Tong", "Kwun Tong Business Area", "Lai Chi Kok", "Lai Chi Kok(S)", "Lam Tin", "Lok Fu", "Ma Tau Wai", "Mei Foo", "Mong Kok", "Ngau Chi Wan", "Ngau Tau Kok", "Ngong Shuen Chau", "Ping Shek", "Po Lam", "San Po Kong", "Sham Shui Po", "Sham Shui Po(W)", "Shek Kip Mei", "Sau Mau Ping", "Tai Kok Tsui", "Tai Kok Tsui(W)", "Tiu Keng Leng", "To Kwan Wan", "Tsim Sha Tsui", "Tsim Sha Tsui East", "Tsz Wan Shan", "Wang Tau Hom", "West Kowloon Cultural District", "West Kowloon Terminus", "Wong Tai Sin", "Yau Ma Tei", "Yau Ma Tei(W)", "Yau Tong", "Yau Yat Chuen"] },
{ "New Terriitories": ["Lantau", "Airport", "Au Tau", "Clear Water Bay", "Discovery Bay", "Fairview Park", "Fanling", "Fo Tan", "Ha Tsuen", "Hin Tin", "Heung Yuen Wai", "Hong Lok Yuen", "Hung Shui Kiu", "Kam Tin", "Kwai Chung", "Kwai Fong", "Kwai Hing", "Kwu Tung", "Lai King", "Lam Tei", "Lantau(S)", "Lau Fau Shan", "Lei Muk Shue", "Lok Ma Chau", "Long Ping", "Luen Wo Hui", "Luk Keng", "Lung Kwu Tan", "Ma On Shan", "Ma On Shan Town Centre", "Ma Liu Sui", "Ma Wan", "Man Kam To", "Mui Wo", "Ngong Ping", "On Lok Tsuen", "Pak Shek Kok", "Pak Tam Chung", "Pat Heung", "Ping Che", "Ping Shan", "Pui O", "River Trade Terminal", "Sai Sha", "Sai Kung", "Sam Shing Hui", "San Tin", "San Hui", "Science Park", "Sha Tau Kok", "Sha Tin", "Sha Tin Central", "Sham Tseng", "Shek Kong", "Shek Lei", "Shek Pik", "Shek Wu Hui", "Shek Yam", "Shenzhen Bay", "Sheung Shui", "Sheung Tak", "Siu Lam", "Siu Lek Yuen", "So Kwun Wat", "Tai Hing", "Tai Mei Tuk", "Tai O", "Tai Po", "Tai Po Industrial Estate", "Tai Po Kau", "Tai Po Market", "Tai Po Town Centre", "Tai Po(N)", "Tai Po(S)", "Tai Shui Hang", "Tai Wai", "Tin Shui Wai", "Tai Lin Pai", "Tai Wo Hau", "Tin Sam", "Tin Shui Wai(N)", "Tin Shui Wai(S)", "Tin Shui Wai(C)", "Tin Shui Wai(W)", "Ting Kau", "Tiu Keng Leng", "Tseung Kwan O", "Tseung Kwan O(E)", "Tseung Kwan O Industrial Estate", "Tseung Kwan O Town Centre", "Tsing Lung Tau", "Tsing Yi", "Tsing Yi Town Centre", "Tsing Yi(N)", "Tsing Yi(S)", "Tsing Yi(E)", "Tsuen Wan", "Tsuen Wan Central", "Tsuen Wan(N)", "Tsuen Wan(S)", "Tsui Lam", "Tuen Mun", "Tuen Mun Town Centre", "Tuen Mun(W)", "Tung Chung", "Tung Chung Town Centre", "Tung Chung(W)", "Tung Chung(N)", "Tung Tau Industrial Area", "Wo Hop Shek", "Wu Kai Sha", "Yuen Chau Kok", "Yuen Long", "Yuen Long(C)", "Yuen Long(S)", "Yuen Long Industrial Estate"] },
]

const ChtDestinations = [{ "Hong Kong Island": ["香港", "香港(東)", "香港(中)", "香港(西)", "香港仔", "鴨脷洲", "銅鑼灣", "中區", "柴灣", "舂坎角", "數碼港", "杏花邨", "跑馬地", "堅尼地城", "康怡", "半山", "北角", "薄扶林", "鰂魚涌", "淺水灣", "西灣", "西灣河", "西營盤", "筲箕灣", "石澳", "石塘咀", "上環", "壽臣山", "赤柱", "小西灣", "大坑", "太古城", "山頂", "華富", "灣仔", "灣仔(北)", "黃竹坑"] },
{ "Kowloon": ["九龍", "九龍(中)", "九龍(東)", "九龍(西)", "筆架山", "荼果嶺", "長沙灣", "長沙灣(西)", "彩虹", "竹園", "郵輪碼頭", "鑽石山", "坑口", "何文田", "紅磡", "紅磡灣", "啟德", "京士柏", "九龍灣", "九龍城區", "九龍塘", "觀塘", "觀塘商貿區", "荔枝角", "荔枝角(南)", "藍田", "樂富", "馬頭圍", "美孚", "旺角", "牛池灣", "牛頭角", "昂船洲", "坪石", "寶琳", "新蒲崗", "深水埗", "深水埗(西)", "石硤尾", "秀茂坪", "大角咀", "大角咀(西)", "調景嶺", "土瓜灣", "尖沙咀", "尖沙咀東", "慈雲山", "橫頭磡", "西九文化區", "西九龍總站", "黃大仙", "油麻地", "油麻地(西)", "油塘", "又一村",] },
{ "New Terriitories": ["大嶼山", "機場", "凹頭", "清水灣", "愉景灣", "錦綉花園", "粉嶺", "火炭", "廈村", "顯田", "香園圍", "康樂園", "洪水橋", "錦田", "葵涌", "葵芳", "葵興", "古洞", "荔景", "藍地", "大嶼山", "流浮山", "梨木樹", "落馬洲", "朗屏", "聯和墟", "鹿頸", "龍鼓灘", "馬鞍山", "馬鞍山市中心", "馬料水", "馬灣", "文錦渡", "梅窩", "昂坪", "安樂村", "白石角", "北潭涌", "八鄉", "坪輋", "坪山", "貝澳", "內河碼頭", "西沙", "西貢", "三聖墟", "新田", "新墟", "科學園", "沙頭角", "沙田", "沙田巿中心", "深井", "石崗", "石籬", "石壁", "石湖墟", "石蔭", "深圳灣", "上水", "尚德", "小欖", "小瀝源", "掃管笏", "大興", "大美督", "大澳", "大埔", "大埔工業邨", "大埔滘", "大埔墟", "大埔市中心", "大埔(北)", "大埔(南)", "大水坑", "大圍", "天水圍", "大連排", "大窩口", "田心", "天水圍(北)", "天水圍(南)", "天水圍(中)", "天水圍(西)", "汀九", "調景嶺", "將軍澳", "將軍澳(東)", "將軍澳工業邨", "將軍澳市中心", "青龍頭", "青衣", "青衣市中心", "青衣(北)", "青衣(南)", "青衣(東)", "荃灣", "荃灣(中)", "荃灣(北)", "荃灣(南)", "翠林", "屯門", "屯門市中心", "屯門(西)", "東涌", "東涌市中心", "東涌(西)", "東涌(北)", "東頭工業區", "和合石", "烏溪沙", "圓洲角", "元朗", "元朗(中)", "元朗(南)", "元朗(南)", "元朗工業邨"] }
]


const BorderColorScheme = {
    "Blue Background": {
        'background': 'rgb(0, 51, 162)',
        'symbol': '#ffffff',
        'border': '#ffffff',
    },
    "Green Background": {
        'background': 'rgb(0, 105, 40)',
        'symbol': '#ffffff',
        'border': '#ffffff',
    },
    "White Background": {
        'background': '#ffffff',
        'symbol': '#000000',
        'border': '#000000',
    },
    "White Background - Parking": {
        'background': '#ffffff',
        'symbol': '#000000',
        'border': 'rgb(0, 15, 162)',
    },
    "Yellow Background": {
        'background': 'rgb(233, 181, 0)',
        'symbol': '#000000',
        'border': '#000000',
    },
    "Brown Background": {
        'background': 'rgb(117,75,42)',
        'symbol': '#ffffff',
        'border': '#ffffff',
    },
    "Red Background": {
        'background': 'rgb(224,0,0)',
        'symbol': '#ffffff',
        'border': '#ffffff',
    },
}

const BorderTypeScheme = {
    'stack': StackBorderTemplate,
    'flagLeft': FlagLeftBorderTemplate,
    'flagRight': FlagRightBorderTemplate,
    'exit': ExitBorderTemplate,
    'panel': PanelTemplate,
    'greenPanel': GreenPanelTemplate,
    'rectangle': RectTemplate,
    //'StreetName': StreetNameTemplate,
    'StreetName2Way': StreetName2WayTemplate,
    'StreetNameLeft': StreetNameLeftTemplate,
    'StreetNameRight': StreetNameRightTemplate
}

const BorderFrameWidth = {
    'stack': 1.5,
    'flagLeft': 1.5,
    'flagRight': 1.5,
    'exit': 1,
    'panel': 0,
    'greenPanel': 0.5,
    'rectangle': 0,
}

const BorderPaddingWidth = {
    'stack': {
        left: 2.5,
        top: 2.5,
        right: 2.5,
        bottom: 1.5,
    },
    'flagLeft': null, //to be calculated in function
    'flagRight': null,
    'exit': {
        left: 0.5,
        top: 0.3,
        right: 0.5,
        bottom: 0,
    },
    'panel': {
        left: 2.5,
        top: 2.5,
        right: 2.5,
        bottom: 1.5,
    },
    'greenPanel': {
        left: 2.5,
        top: 2.5,
        right: 2.5,
        bottom: 1.5,
    },
    'rectangle': {
        left: 1,
        top: 1,
        right: 1,
        bottom: 1,
    },
}


function applyLengthAndRounding(path, length) {
    path.vertex.forEach(vertex => {
        vertex.x *= length;
        vertex.y *= length;
        if (vertex.radius) vertex.radius *= length;
    });
    path.arcs.forEach(arc => {
        arc.radius *= length;
    });
}

function StreetName2WayTemplate(xHeight, block, rounding = { x: 0, y: 0 }) {
    const length = xHeight / 4;

    const baseWidth = block.width + 136 + 136; // V1 to V2 distance
    const totalWidth = Math.ceil(baseWidth / 50) * 50;
    const paddingX = (totalWidth - baseWidth) / 2;

    const returnBorder = [{
        'vertex': [
            { x: (-136 - paddingX) / length, y: -45 / length, label: 'V1', start: 1, radius: 25 / length },
            { x: (block.width + 136 + paddingX) / length, y: -45 / length, label: 'V2', start: 0, radius: 25 / length },
            { x: (block.width + 136 + paddingX) / length, y: 279 / length, label: 'V3', start: 0, radius: 25 / length },
            { x: (-136 - paddingX) / length, y: 279 / length, label: 'V4', start: 0, radius: 25 / length },
        ], 'arcs': [], 'fill': '#000000'
    }, {
        'vertex': [
            { x: (-6 - paddingX) / length, y: -33 / length, label: 'V1', start: 1, },
            { x: (block.width + 6 + paddingX) / length, y: -33 / length, label: 'V2', start: 0 },
            { x: (block.width + 111 + paddingX) / length, y: 117 / length, label: 'V3', start: 0 },
            { x: (block.width + 6 + paddingX) / length, y: 267 / length, label: 'V4', start: 0 },
            { x: (-6 - paddingX) / length, y: 267 / length, label: 'V5', start: 0 },
            { x: (-111 - paddingX) / length, y: 117 / length, label: 'V6', start: 0 },
        ], 'arcs': [], 'fill': '#FFFFFF'
    }];

    returnBorder.forEach(path => applyLengthAndRounding(path, length));
    return { path: returnBorder };
}

function StreetNameLeftTemplate(xHeight, block, rounding = { x: 0, y: 0 }) {
    const length = xHeight / 4;

    const baseWidth = 136 + block.width + 37;
    const totalWidth = Math.ceil(baseWidth / 50) * 50;
    const paddingX = (totalWidth - baseWidth) / 2;

    const returnBorder = [{
        'vertex': [
            { x: (-136 - paddingX) / length, y: -45 / length, label: 'V1', start: 1, radius: 25 / length },
            { x: (block.width + 37 + paddingX) / length, y: -45 / length, label: 'V2', start: 0, radius: 25 / length },
            { x: (block.width + 37 + paddingX) / length, y: 279 / length, label: 'V3', start: 0, radius: 25 / length },
            { x: (-136 - paddingX) / length, y: 279 / length, label: 'V4', start: 0, radius: 25 / length },
        ], 'arcs': [], 'fill': '#000000'
    }, {
        // Chevron on left, straight flat on right
        'vertex': [
            { x: (-6 - paddingX) / length, y: -33 / length, label: 'V1', start: 1, },
            { x: (block.width + 25 + paddingX) / length, y: -33 / length, label: 'V2', start: 0, radius: 12.5 / length },
            { x: (block.width + 25 + paddingX) / length, y: 267 / length, label: 'V3', start: 0, radius: 12.5 / length },
            { x: (-6 - paddingX) / length, y: 267 / length, label: 'V4', start: 0 },
            { x: (-111 - paddingX) / length, y: 117 / length, label: 'V5', start: 0 },
        ], 'arcs': [], 'fill': '#FFFFFF'
    }];

    returnBorder.forEach(path => applyLengthAndRounding(path, length));
    return { path: returnBorder };
}

function StreetNameRightTemplate(xHeight, block, rounding = { x: 0, y: 0 }) {
    const length = xHeight / 4;

    const baseWidth = 37 + block.width + 136;
    const totalWidth = Math.ceil(baseWidth / 50) * 50;
    const paddingX = (totalWidth - baseWidth) / 2;

    const returnBorder = [{
        'vertex': [
            { x: (-37 - paddingX) / length, y: -45 / length, label: 'V1', start: 1, radius: 25 / length },
            { x: (block.width + 136 + paddingX) / length, y: -45 / length, label: 'V2', start: 0, radius: 25 / length },
            { x: (block.width + 136 + paddingX) / length, y: 279 / length, label: 'V3', start: 0, radius: 25 / length },
            { x: (-37 - paddingX) / length, y: 279 / length, label: 'V4', start: 0, radius: 25 / length },
        ], 'arcs': [], 'fill': '#000000'
    }, {
        // Straight flat on left, Chevron on right
        'vertex': [
            { x: (-25 - paddingX) / length, y: -33 / length, label: 'V1', start: 1, radius: 12.5 / length },
            { x: (block.width + 6 + paddingX) / length, y: -33 / length, label: 'V2', start: 0 },
            { x: (block.width + 111 + paddingX) / length, y: 117 / length, label: 'V3', start: 0 },
            { x: (block.width + 6 + paddingX) / length, y: 267 / length, label: 'V4', start: 0 },
            { x: (-25 - paddingX) / length, y: 267 / length, label: 'V5', start: 0, radius: 12.5 / length },
        ], 'arcs': [], 'fill': '#FFFFFF'
    }];

    returnBorder.forEach(path => applyLengthAndRounding(path, length));
    return { path: returnBorder };
}

function RectTemplate(xHeight, block, rounding = { x: 0, y: 0 }) {
    const length = xHeight / 4;

    const padding = {
        left: 1,
        top: 1,
        right: 1,
        bottom: 1,
    };

    const returnBorder = [{
        'vertex': [
            { x: 0 - padding.left, y: 0 - padding.top, label: 'V1', start: 1 },
            { x: block.width / length + padding.right, y: 0 - padding.top, label: 'V2', start: 0 },
            { x: block.width / length + padding.right, y: block.height / length + padding.bottom, label: 'V3', start: 0 },
            { x: 0 - padding.right, y: block.height / length + padding.bottom, label: 'V4', start: 0 },
        ], 'arcs': [], 'fill': 'background'
    }];

    returnBorder.forEach(path => applyLengthAndRounding(path, length));
    return { path: returnBorder };
}

function PanelTemplate(xHeight, block, rounding = { x: 0, y: 0 }) {
    const length = xHeight / 4;

    const padding = {
        left: 2.5,
        top: 2.5,
        right: 2.5,
        bottom: 1.5,
    };

    const returnBorder = [{
        'vertex': [
            { x: 0 - padding.left, y: 0 - padding.top, label: 'V1', radius: 1, start: 1 },
            { x: block.width / length + padding.right, y: 0 - padding.top, label: 'V2', radius: 1, start: 0 },
            { x: block.width / length + padding.right, y: block.height / length + padding.bottom, label: 'V3', radius: 1, start: 0 },
            { x: 0 - padding.right, y: block.height / length + padding.bottom, label: 'V4', radius: 1, start: 0 },
        ], 'arcs': [], 'fill': 'background'
    }];

    returnBorder.forEach(path => applyLengthAndRounding(path, length));
    return { path: returnBorder };
}

function GreenPanelTemplate(xHeight, block, rounding = { x: 0, y: 0 }) {
    const length = xHeight / 4;

    const padding = {
        left: 2.5,
        top: 2.5,
        right: 2.5,
        bottom: 1.5,
    };

    const border = 0.5;
    const returnBorder = [{
        'vertex': [
            { x: 0 - padding.left - border, y: 0 - padding.top - border, label: 'V1', radius: 1.5, start: 1 },
            { x: block.width / length + padding.right + border, y: 0 - padding.top - border, label: 'V2', radius: 1.5, start: 0 },
            { x: block.width / length + padding.right + border, y: block.height / length + padding.bottom + border, label: 'V3', radius: 1.5, start: 0 },
            { x: 0 - padding.left - border, y: block.height / length + padding.bottom + border, label: 'V4', radius: 1.5, start: 0 },
        ], 'arcs': [], 'fill': '#ffffff'
    }, {
        'vertex': [
            { x: 0 - padding.left, y: 0 - padding.top, label: 'V5', radius: 1, start: 1 },
            { x: block.width / length + padding.right, y: 0 - padding.top, label: 'V6', radius: 1, start: 0 },
            { x: block.width / length + padding.right, y: block.height / length + padding.bottom, label: 'V7', radius: 1, start: 0 },
            { x: 0 - padding.right, y: block.height / length + padding.bottom, label: 'V8', radius: 1, start: 0 },
        ], 'arcs': [], 'fill': 'rgb(0, 105, 40)'
    }];

    returnBorder.forEach(path => applyLengthAndRounding(path, length));
    return { path: returnBorder };
}

function StackBorderTemplate(xHeight, block, rounding = { x: 0, y: 0 }) {
    const length = xHeight / 4;
    rounding.x /= length;
    rounding.y /= length;
    const padding = {
        left: 2.5 + rounding.x,
        top: 2.5 + rounding.y,
        right: 2.5 + rounding.x,
        bottom: 1.5 + rounding.y,
    };

    const border = 1.5;
    const returnBorder = [{
        'vertex': [
            { x: 0 - padding.left - border, y: 0 - padding.top - border, label: 'V1', radius: 3, start: 1 },
            { x: block.width / length + padding.right + border, y: 0 - padding.top - border, label: 'V2', radius: 3, start: 0, display: 0 },
            { x: block.width / length + padding.right + border, y: block.height / length + padding.bottom + border, label: 'V3', radius: 3, start: 0, display: 0 },
            { x: 0 - padding.left - border, y: block.height / length + padding.bottom + border, label: 'V4', radius: 3, start: 0, display: 0 },
        ], 'arcs': [], 'fill': 'border'
    }, {
        'vertex': [
            { x: 0 - padding.left, y: 0 - padding.top, label: 'V5', radius: 1.5, start: 1, display: 0 },
            { x: block.width / length + padding.right, y: 0 - padding.top, label: 'V6', radius: 1.5, start: 0, display: 0 },
            { x: block.width / length + padding.right, y: block.height / length + padding.bottom, label: 'V7', radius: 1.5, start: 0, display: 0 },
            { x: 0 - padding.right, y: block.height / length + padding.bottom, label: 'V8', radius: 1.5, start: 0, display: 0 },
        ], 'arcs': [], 'fill': 'background'
    }];

    returnBorder.forEach(path => applyLengthAndRounding(path, length));
    return { path: returnBorder };
}

function ExitBorderTemplate(xHeight, block, rounding = { x: 0, y: 0 }) {
    const length = xHeight / 4;
    const padding = {
        left: 0.5,
        top: 0.3,
        right: 0.5,
        bottom: 0,
    };

    const border = 0.5;
    const returnBorder = [{
        'vertex': [
            { x: 0 - padding.left - border, y: 0 - padding.top - border, label: 'V1', start: 1 },
            { x: block.width / length + padding.right + border, y: 0 - padding.top - border, label: 'V2', start: 0, display: 0 },
            { x: block.width / length + padding.right + border, y: 7.2, label: 'V3', start: 0, display: 0 },
            { x: 0 - padding.left - border, y: 7.2, label: 'V4', start: 0, display: 0 },
        ], 'arcs': [], 'fill': 'border'
    }, {
        'vertex': [
            { x: 0 - padding.left, y: 0 - padding.top, label: 'V5', start: 1, display: 0 },
            { x: block.width / length + padding.right, y: 0 - padding.top, label: 'V6', start: 0, display: 0 },
            { x: block.width / length + padding.right, y: 6.7, label: 'V7', start: 0, display: 0 },
            { x: 0 - padding.right, y: 6.7, label: 'V8', start: 0, display: 0 },
        ], 'arcs': [], 'fill': '#000000'
    }];

    returnBorder.forEach(path => applyLengthAndRounding(path, length));
    return { path: returnBorder };
}

function FlagLeftBorderTemplate(xHeight, block, rounding = { x: 0, y: 0 }) {
    const length = xHeight / 4;
    rounding.x /= length;
    rounding.y /= length;
    const variables = {
        '2Lines': {
            'A': 1.5,
            'B': 2.5,
            'C': 4.5,
            'D': 1.5,
            'E': 2.5,
            'F': 1.5,
            'G': 1.5,
            'H': 3,
        },
        '4Lines': {
            'A': 1.5,
            'B': 3.5,
            'C': 6,
            'D': 1.5,
            'E': 2.5,
            'F': 1.5,
            'G': 1.5,
            'H': 3,
        }
    };
    const v = block.height > 19.4 * xHeight / 4 + 1 ? variables['4Lines'] : variables['2Lines'];

    const padding = {
        left: v.D + (block.height / length + v.E + rounding.y / 2 - v.D) / 2 / Math.tan(Math.PI / 3) + (v.A + v.B + v.C) / Math.cos(Math.PI / 6),
        top: v.E + rounding.y,
        right: v.E,
        bottom: v.D + rounding.y,
    };

    const border = v.A;
    const panel = {
        height: (block.height / length + v.E + rounding.y * 2 + v.D + v.A * 2)
    };

    const returnBorder = [
        /*{
        'vertex': [{ x: 0, y: 0, label: 'V0', start: 1 }], 'arcs': [], 'fill': 'symbol'
    }, */{
            'vertex': [
                { x: 0 - padding.left + panel.height / 2 * Math.tan(Math.PI / 6), y: 0 - padding.top - border, radius: v.H, label: 'V1', start: 1 },
                { x: block.width / length + padding.right + border, y: 0 - padding.top - border, radius: v.H, label: 'V2', start: 0, display: 0 },
                { x: block.width / length + padding.right + border, y: block.height / length + padding.bottom + border, radius: v.H, label: 'V3', start: 0, display: 0 },
                { x: 0 - padding.left + panel.height / 2 * Math.tan(Math.PI / 6), y: block.height / length + padding.bottom + border, radius: v.H, label: 'V4', start: 0, display: 0 },
                { x: 0 - padding.left, y: 0 - v.E - rounding.y - border + panel.height / 2, radius: v.F, label: 'V5', start: 0, display: 0 }
            ], 'arcs': [], 'fill': 'border'
        }, {
            'vertex': [
                { x: 0 - padding.left + v.A + (panel.height - border * 2) / 2 * Math.tan(Math.PI / 6), y: 0 - padding.top, radius: v.G, label: 'V6', start: 1, display: 0 },
                { x: block.width / length + padding.right, y: 0 - padding.top, radius: v.G, label: 'V7', start: 0, display: 0 },
                { x: block.width / length + padding.right, y: block.height / length + padding.bottom, radius: v.G, label: 'V8', start: 0, display: 0 },
                { x: 0 - padding.left + v.A + (panel.height - border * 2) / 2 * Math.tan(Math.PI / 6), y: block.height / length + padding.bottom, radius: v.G, label: 'V9', start: 0, display: 0 },
                { x: 0 - padding.left + v.A, y: 0 - v.E - rounding.y - v.A + (panel.height) / 2, label: 'V10', start: 0, display: 0 }
            ], 'arcs': [], 'fill': 'background'
        }, {
            'vertex': [
                { x: 0 - v.D - v.C / Math.cos(Math.PI / 6), y: 0 - v.E - rounding.y + v.D, label: 'V11', start: 1, display: 0 },
                { x: 0 - v.D, y: 0 - v.E - rounding.y + v.D, label: 'V12', start: 0, display: 0 },
                { x: -padding.left + v.A + (v.B + v.C) / Math.cos(Math.PI / 6), y: 0 - v.E - rounding.y - v.A + (panel.height) / 2, label: 'V13', start: 0, display: 0 },
                { x: 0 - v.D, y: 0 + block.height / length, label: 'V14', start: 0, display: 0 },
                { x: 0 - v.D - v.C / Math.cos(Math.PI / 6), y: 0 + block.height / length, label: 'V15', start: 0, display: 0 },
                { x: -padding.left + v.A + v.B / Math.cos(Math.PI / 6), y: 0 - v.E - rounding.y - v.A + (panel.height) / 2, label: 'V16', start: 0, display: 0 },
            ], 'arcs': [], 'fill': 'symbol'
        }];

    returnBorder.forEach(path => applyLengthAndRounding(path, length));
    return { path: returnBorder };
}

function FlagRightBorderTemplate(xHeight, block, rounding = { x: 0, y: 0 }) {
    const length = xHeight / 4;
    rounding.x /= length;
    rounding.y /= length;
    const variables = {
        '2Lines': {
            'A': 1.5,
            'B': 2.5,
            'C': 4.5,
            'D': 1.5,
            'E': 2.5,
            'F': 1.5,
            'G': 1.5,
            'H': 3,
        },
        '4Lines': {
            'A': 1.5,
            'B': 3.5,
            'C': 6,
            'D': 1.5,
            'E': 2.5,
            'F': 1.5,
            'G': 1.5,
            'H': 3,
        }
    };
    const v = block.height > 4.85 * xHeight ? variables['4Lines'] : variables['2Lines'];

    const padding = {
        left: v.E,
        top: v.E + rounding.y,
        right: v.D + (block.height / length + v.E + rounding.y / 2 - v.D) / 2 / Math.tan(Math.PI / 3) + (v.A + v.B + v.C) / Math.cos(Math.PI / 6),
        bottom: v.D + rounding.y,
    };

    const border = v.A;
    const panel = {
        height: (block.height / length + v.E + rounding.y * 2 + v.D + v.A * 2)
    };

    const returnBorder = [
        /*{
        'vertex': [{ x: 0, y: 0, label: 'V0', start: 1 }], 'arcs': [], 'fill': 'symbol'
    }, */{
            'vertex': [
                { x: 0 - padding.left - border, y: 0 - padding.top - border, radius: v.H, label: 'V1', start: 1 },
                { x: block.width / length + padding.right - panel.height / 2 * Math.tan(Math.PI / 6), y: 0 - padding.top - border, radius: v.H, label: 'V2', start: 0, display: 0 },
                { x: block.width / length + padding.right, y: 0 - v.E - rounding.y - v.A + (panel.height) / 2, radius: v.F, label: 'V3', start: 0, display: 0 },
                { x: block.width / length + padding.right - panel.height / 2 * Math.tan(Math.PI / 6), y: block.height / length + padding.bottom + border, radius: v.H, label: 'V4', start: 0, display: 0 },
                { x: 0 - padding.left - border, y: block.height / length + padding.bottom + border, radius: v.H, label: 'V5', start: 0, display: 0 },
            ], 'arcs': [], 'fill': 'border'
        }, {
            'vertex': [
                { x: 0 - padding.left, y: 0 - padding.top, radius: v.G, label: 'V8', start: 1 },
                { x: block.width / length + padding.right - v.A - (panel.height - border * 2) / 2 * Math.tan(Math.PI / 6), y: 0 - padding.top, radius: v.G, label: 'V7', start: 0, display: 0 },
                { x: block.width / length + padding.right - v.A, y: 0 - v.E - rounding.y - v.A + (panel.height) / 2, label: 'V8', start: 0, display: 0 },
                { x: block.width / length + padding.right - v.A - (panel.height - border * 2) / 2 * Math.tan(Math.PI / 6), y: block.height / length + padding.bottom, radius: v.G, label: 'V9', start: 0, display: 0 },
                { x: 0 - padding.left, y: block.height / length + padding.bottom, radius: v.G, label: 'V10', start: 0, display: 0 },
            ], 'arcs': [], 'fill': 'background'
        }, {
            'vertex': [
                { x: block.width / length + v.D + v.C / Math.cos(Math.PI / 6), y: 0 - v.E - rounding.y + v.D, label: 'V11', start: 1, display: 0 },
                { x: block.width / length + v.D, y: 0 - v.E - rounding.y + v.D, label: 'V12', start: 0, display: 0 },
                { x: block.width / length + padding.right - v.A - (v.B + v.C) / Math.cos(Math.PI / 6), y: 0 - v.E - rounding.y - v.A + (panel.height) / 2, label: 'V13', start: 0, display: 0 },
                { x: block.width / length + v.D, y: 0 + block.height / length, label: 'V14', start: 0, display: 0 },
                { x: block.width / length + v.D + v.C / Math.cos(Math.PI / 6), y: 0 + block.height / length, label: 'V15', start: 0, display: 0 },
                { x: block.width / length + padding.right - v.A - v.B / Math.cos(Math.PI / 6), y: 0 - v.E - rounding.y - v.A + (panel.height) / 2, label: 'V16', start: 0, display: 0 },
            ], 'arcs': [], 'fill': 'symbol'
        }];

    returnBorder.forEach(path => applyLengthAndRounding(path, length));
    return { path: returnBorder };
}

function OvalRoundaboutTemplate(angle) {
    const innerRadius = 7
    const outerRadius = 12
    const linkLength = 24
    const cosA = Math.cos(angle * Math.PI() / 180)
    const sinA = Math.sin(angle * Math.PI() / 180)
    const innerCutStart = { x: innerRadius * Math.cos(60 * Math.PI() / 180), y: innerRadius * Math.sin(60 * Math.PI() / 180) }
    const innerFirstArc = { x: - innerRadius * cosA, y: - innerRadius * sinA }
    const innerFirstLine = { x: - innerRadius * cosA + linkLength * sinA, y: - innerRadius * sinA - linkLength * cosA }
    const innerSecondArc = { x: innerRadius * cosA + linkLength * sinA, y: innerRadius * sinA - linkLength * cosA }
    const innerSecondLine = { x: innerRadius * cosA, y: innerRadius * sinA }
    const innerCutEnd = { x: innerRadius * Math.cos(30 * Math.PI() / 180), y: innerRadius * Math.sin(30 * Math.PI() / 180) }
    const outerCutStart = { x: outerRadius * Math.cos(30 * Math.PI() / 180), y: outerRadius * Math.sin(30 * Math.PI() / 180) }
    const outerFirstArc = { x: outerRadius * cosA, y: outerRadius * sinA }
    const outerFirstLine = { x: outerRadius * cosA + linkLength * sinA, y: outerRadius * sinA - linkLength * cosA }
    const outerSecondArc = { x: - outerRadius * cosA + linkLength * sinA, y: - outerRadius * sinA - linkLength * cosA }
    const outerSecondLine = { x: - outerRadius * cosA, y: - outerRadius * sinA }
    const outerCutEnd = { x: outerRadius * Math.cos(60 * Math.PI() / 180), y: outerRadius * Math.sin(60 * Math.PI() / 180) }
}

const DividerScheme = {
    'HDivider': HDividerTemplate,
    'VDivider': VDividerTemplate,
    'HLine': HLineTemplate,
    'VLane': VLaneTemplate,
}

const DividerMargin = {
    'HDivider': { left: 0, top: 0, right: 0, bottom: 1 },
    'VDivider': { left: 1, top: 0, right: 1, bottom: 0 },
    'HLine': { left: 1.5, top: 1, right: 1.5, bottom: 1 },
    'VLane': { left: 2.5, top: 1.5, right: 2.5, bottom: 1.5 },
}

function HDividerTemplate(xHeight, position, block, rounding = { x: 0, y: 0 }) {
    const length = xHeight / 4;
    const Xwidth = block.width / length;
    rounding.x /= length;
    rounding.y /= length;

    const returnBorder = [{
        'vertex': [
            { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
            { x: Xwidth / 2, y: 0, label: 'V2', radius: 1.5, start: 0 },
            { x: Xwidth / 2, y: -1.5, label: 'V3', start: 0 },
            { x: Xwidth / 2, y: 2.5, label: 'V4', start: 0 },
            { x: Xwidth / 2, y: 1, label: 'V5', radius: 1.5, start: 0 },
            { x: 0, y: 1, label: 'V6', radius: 1.5, start: 0, display: 1 },
            { x: -Xwidth / 2, y: 1, label: 'V7', radius: 1.5, start: 0 },
            { x: -Xwidth / 2, y: 2.5, label: 'V8', start: 0 },
            { x: -Xwidth / 2, y: -1.5, label: 'V9', start: 0 },
            { x: -Xwidth / 2, y: 0, label: 'V10', radius: 1.5, start: 0 },
        ], 'arcs': [], 'fill': 'border'
    },];

    returnBorder.forEach(p => {
        p.vertex.forEach(vertex => {
            vertex.x *= length;
            vertex.x += position.left + (Xwidth / 2) * length;
            vertex.y *= length;
            vertex.y += position.top + (1.5) * length;
            if (vertex.radius) vertex.radius *= length;
        });
    });

    return { path: returnBorder };
}

function VDividerTemplate(xHeight, position, block, rounding = { x: 0, y: 0 }) {
    const length = xHeight / 4;
    const Xwidth = block.height / length;
    rounding.x /= length;
    rounding.y /= length;

    const returnBorder = [{
        'vertex': [
            { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
            { x: 0, y: Xwidth / 2, label: 'V2', radius: 1.5, start: 0 },
            { x: -1.5, y: Xwidth / 2, label: 'V3', start: 0 },
            { x: 2.5, y: Xwidth / 2, label: 'V4', start: 0 },
            { x: 1, y: Xwidth / 2, label: 'V5', radius: 1.5, start: 0 },
            { x: 1, y: 0, label: 'V6', radius: 1.5, start: 0, display: 1 },
            { x: 1, y: -Xwidth / 2, label: 'V7', radius: 1.5, start: 0 },
            { x: 2.5, y: -Xwidth / 2, label: 'V8', start: 0 },
            { x: -1.5, y: -Xwidth / 2, label: 'V9', start: 0 },
            { x: 0, y: -Xwidth / 2, label: 'V10', radius: 1.5, start: 0 },
        ], 'arcs': [], 'fill': 'border'
    },];

    returnBorder.forEach(p => {
        p.vertex.forEach(vertex => {
            vertex.x *= length;
            vertex.x += position.left + (1.5) * length;
            vertex.y *= length;
            vertex.y += position.top + (Xwidth / 2) * length;
            if (vertex.radius) vertex.radius *= length;
        });
    });
    return { path: returnBorder };
}

function HLineTemplate(xHeight, position, block, rounding = { x: 0, y: 0 }) {
    const length = xHeight / 4;
    const Xwidth = block.width / length;
    rounding.x /= length;
    rounding.y /= length;

    const returnBorder = [{
        'vertex': [
            { x: 0, y: 0, label: 'V1', start: 1, display: 1 },
            { x: Xwidth / 2 - 1.5, y: 0, label: 'V2', start: 0 },
            { x: Xwidth / 2 - 1.5, y: 1, label: 'V3', start: 0 },
            { x: 0, y: 1, label: 'V4', start: 0, display: 1 },
            { x: -Xwidth / 2 + 1.5, y: 1, label: 'V5', start: 0 },
            { x: -Xwidth / 2 + 1.5, y: 0, label: 'V6', start: 0 },
        ], 'arcs': [], 'fill': 'border'
    },];

    returnBorder.forEach(p => {
        p.vertex.forEach(vertex => {
            vertex.x *= length;
            vertex.x += position.left + (Xwidth / 2) * length;
            vertex.y *= length;
            vertex.y += position.top + (1) * length;
        });
    });

    return { path: returnBorder };
}

function VLaneTemplate(xHeight, position, block, rounding = { x: 0, y: 0 }) {
    const length = xHeight / 4;
    const BHeight = block.height / length;
    const strokeHeight = 8
    const strokeSpacing = 4
    rounding.x /= length;
    rounding.y /= length;
    const effHeight = Math.floor((BHeight - rounding.y / 2 - DividerMargin['VLane'].top - DividerMargin['VLane'].bottom + strokeSpacing) / (strokeHeight + strokeSpacing));

    const strokeCount = Math.max(2, effHeight);
    const diminishedStroke = (BHeight - (strokeHeight + strokeSpacing) * strokeCount) - (strokeSpacing)

    let returnBorder = [{
        'vertex': [], 'arcs': [], 'fill': 'border'
    },];

    let k = 0
    for (let i = 0; i < strokeCount + 1; i++) {
        if (i == 0 && diminishedStroke > 0) {
            returnBorder[0].vertex.push(...[
                { x: 0, y: BHeight - DividerMargin['VLane'].bottom - rounding.y / 2 + i * (strokeHeight + strokeSpacing), label: 'V1', start: 1 },
                { x: 1.5, y: BHeight - DividerMargin['VLane'].bottom - rounding.y / 2 + i * (strokeHeight + strokeSpacing), label: 'V2', start: 0 },
                { x: 1.5, y: BHeight - DividerMargin['VLane'].bottom - rounding.y / 2 + i * (strokeHeight + strokeSpacing) - diminishedStroke, label: 'V3', start: 0, display: 1 },
                { x: -1.5, y: BHeight - DividerMargin['VLane'].bottom - rounding.y / 2 + i * (strokeHeight + strokeSpacing) - diminishedStroke, label: 'V4', start: 0, display: 1 },
                { x: -1.5, y: BHeight - DividerMargin['VLane'].bottom - rounding.y / 2 + i * (strokeHeight + strokeSpacing), label: 'V5', start: 0 },
            ])
            k++
        } else if (i != 0) {
            returnBorder[0].vertex.push(...[
                { x: 0, y: BHeight - DividerMargin['VLane'].bottom - rounding.y / 2 + i * (strokeHeight + strokeSpacing), label: `V${k * 5 + 1}`, start: 1 },
                { x: 1.5, y: BHeight - DividerMargin['VLane'].bottom - rounding.y / 2 + i * (strokeHeight + strokeSpacing), label: `V${k * 5 + 2}`, start: 0 },
                { x: 1.5, y: BHeight - DividerMargin['VLane'].bottom - rounding.y / 2 + i * (strokeHeight + strokeSpacing) - strokeHeight, label: `V${k * 5 + 3}`, start: 0 },
                { x: -1.5, y: BHeight - DividerMargin['VLane'].bottom - rounding.y / 2 + i * (strokeHeight + strokeSpacing) - strokeHeight, label: `V${k * 5 + 4}`, start: 0 },
                { x: -1.5, y: BHeight - DividerMargin['VLane'].bottom - rounding.y / 2 + i * (strokeHeight + strokeSpacing), label: `V${k * 5 + 5}`, start: 0 },
            ])
            k++
        }
    }

    returnBorder[0].vertex.filter(v => v.label == 'V3' || v.label == `V4`).forEach(v => v.display = 1)

    returnBorder.forEach(p => {
        p.vertex.forEach(vertex => {
            vertex.x *= length;
            vertex.x += position.left + 1.5 * length;
            vertex.y *= length;
            vertex.y += position.top;
            if (vertex.radius) vertex.radius *= length;
        });
    });
    return { path: returnBorder };
}




/***/ }),

/***/ "./js/objects/text.js":
/*!****************************!*\
  !*** ./js/objects/text.js ***!
  \****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   TextObject: () => (/* binding */ TextObject),
/* harmony export */   containsNonEnglishCharacters: () => (/* binding */ containsNonEnglishCharacters),
/* harmony export */   isNonEnglishCharacter: () => (/* binding */ isNonEnglishCharacter)
/* harmony export */ });
/* harmony import */ var _draw_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./draw.js */ "./js/objects/draw.js");
/* harmony import */ var _divider_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./divider.js */ "./js/objects/divider.js");
/* harmony import */ var _template_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./template.js */ "./js/objects/template.js");
/* harmony import */ var _path_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./path.js */ "./js/objects/path.js");
/* harmony import */ var _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../sidebar/sbGeneral.js */ "./js/sidebar/sbGeneral.js");
/* harmony import */ var _modal_md_font_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../modal/md-font.js */ "./js/modal/md-font.js");
/**
 * TextObject extends BaseGroup to create text with proper vertex handling
 */









// Special characters that are not available in Transport fonts and should use fallback
const TRANSPORT_MISSING_CHARS = new Set([
  '$', '@', '#', '*', '~', '`',
  '€', '£', '¥', '¢', '©', '®', '™', '§',
  '¿', '¡', 'ñ', 'Ñ', 'ç', 'Ç', 'á', 'é', 'í', 'ó', 'ú',
  'À', 'È', 'Ì', 'Ò', 'Ù', 'â', 'ê', 'î', 'ô', 'û',
  'ä', 'ë', 'ï', 'ö', 'ü', 'Ä', 'Ë', 'Ï', 'Ö', 'Ü',
  '±', '×', '÷', '≠', '≤', '≥', '∞', '∑', '∏', '√',
  '°', 'µ', 'Ω', 'α', 'β', 'γ', 'δ', 'π', 'σ', 'τ',
  '♠', '♣', '♥', '♦', '★', '☆', '♪', '♫', '☂', '☀',
  '←', '→', '↑', '↓', '↔', '↕', '⇒', '⇐', '⇑', '⇓'
]);

class TextObject extends _draw_js__WEBPACK_IMPORTED_MODULE_0__.BaseGroup {
  constructor(options = {}) {
    options.color = options.color === 'White' ? '#ffffff' : (options.color === 'Black' ? '#000000' : options.color);
    // Call BaseGroup without base polygon
    super(null, 'Text', 'TextObject', options);
    // Store metadata properties
    this.text = options.text || '';
    this.xHeight = options.xHeight || 100;
    this.color = options.color || '#ffffff';
    this.left = options.left || 0;
    this.top = options.top || 0;
    this.containsNonAlphabetic = false;
    this.txtCharList = [];
    this.txtFrameList = [];
    this.underline = options.underline || null;
    this.charSpacing = options.charSpacing || 0;
    this.containsNonAlphabetic = containsNonEnglishCharacters(this.text);

    // Determine the correct font based on text content and priority system
    const defaultFont = options.font || 'TransportMedium';
    const chineseFontFromPriority = this._getChineseFontFromPriority(this.text, defaultFont);

    // Use Chinese font from priority system if text contains non-English characters,
    // otherwise use the provided font or default
    this.font = chineseFontFromPriority || defaultFont;

    this.initialize();
  }

  /**
 * Initialize the TextObject drawing after instantiation.
 * Creates character paths and frames, then sets the base polygon.
 * @returns {TextObject}
 */
  initialize() {
    // Generate elements using stored properties
    const { txtCharList, txtFrameList, containsNonAlphabetic } = TextObject.createTextElements(
      this.text,
      this.xHeight,
      this.color,
      this.font,
      false,
      this.charSpacing
    );
    // Build group
    const group = new fabric.Group([...txtCharList, ...txtFrameList], {
      left: this.left,
      top: this.top
    });
    // Custom bounding box calculation
    group.setCoords();
    // Store references
    this.txtCharList = txtCharList;
    this.txtFrameList = txtFrameList;
    this.containsNonAlphabetic = containsNonAlphabetic;
    // Set group as base polygon
    this.setBasePolygon(group);

    // Listen for font updates if we might be using fallback fonts
    if (this.containsNonAlphabetic && !this._fontLoadListener) {
      this._fontLoadListener = (e) => {
        // Debounce update to avoid multiple renders when multiple fonts load in sequence
        if (this._updateTimer) clearTimeout(this._updateTimer);
        this._updateTimer = setTimeout(() => {
          this.updateTextElements();
        }, 50);
      };
      window.addEventListener('fontLoaded', this._fontLoadListener);
    }

    // Update display name
    this._showName = `<Group ${this.canvasID}> Text - ${this.text}`;
    return this;
  }

  /**
   * Re-generate text elements (e.g. after a font has loaded)
   */
  updateTextElements() {
    // Generate elements using stored properties
    const { txtCharList, txtFrameList, containsNonAlphabetic } = TextObject.createTextElements(
      this.text,
      this.xHeight,
      this.color,
      this.font,
      false,
      this.charSpacing
    );

    // Remove old basePolygon
    if (this.basePolygon) {
      this.remove(this.basePolygon);
    }

    // Create a new group for the updated elements
    // We must position the new group at the current absolute position of this TextObject (the parent group)
    // so that when added, its relative position becomes (0,0).
    const group = new fabric.Group([...txtCharList, ...txtFrameList], {
      left: this.left,
      top: this.top,
      angle: this.angle,
      scaleX: this.scaleX,
      scaleY: this.scaleY
    });

    group.setCoords();

    // Store references
    this.txtCharList = txtCharList;
    this.txtFrameList = txtFrameList;
    this.containsNonAlphabetic = containsNonAlphabetic;

    // Update the base polygon
    this.setBasePolygon(group);

    // Force canvas render if possible
    this.canvas.requestRenderAll();

  }

  /**
   * Helper function to get the appropriate font for Chinese/non-English characters
   * @param {string} text - Text to check
   * @returns {string} - Font family name to use
   */
  _getChineseFontFromPriority(text, defaultFont = null) {
    try {
      // Check if text contains non-English characters
      if (!containsNonEnglishCharacters(text)) {
        return null; // Not Chinese text, don't override font
      }

      if (window[defaultFont]) {
        return defaultFont; // If a specific font is provided, don't override it
      } else {
        const fontPriorityList = _modal_md_font_js__WEBPACK_IMPORTED_MODULE_5__.FontPriorityManager.getFontPriorityList();
        const priorityFont = fontPriorityList.length > 0 ? fontPriorityList[0] : 'parsedFontKorean';
        return priorityFont; // Return the first font from the priority list
      }
    } catch (error) {
      console.warn('Could not get font from FontPriorityManager, falling back to default Chinese font:', error);
      return defaultFont; // Fallback to default Chinese font
    }
  }

  /**
   * Helper function to get character parameters based on type
   */
  static _getCharacterParameters(textChar, containsNonAlphabetic, font, xHeight, previousChar, nextChar, charSpacing = 0) {
    // Handle special character transformations
    let actualChar = textChar;
    if (textChar === ',' && containsNonAlphabetic) {
      actualChar = '、';
    }

    // Check if character is missing from Transport fonts and force fallback
    let effectiveFont = font;
    if ((font === 'TransportMedium' || font === 'TransportHeavy') && TRANSPORT_MISSING_CHARS.has(textChar)) {
      console.log(`Character "${textChar}" is not available in ${font}, switching to fallback font`);
      effectiveFont = 'parsedFontSans'; // Use sans serif fallback for special characters
    }

    if (containsNonAlphabetic) {
      return this._getNonEnglishCharacterParams(actualChar, effectiveFont, xHeight, previousChar, charSpacing);
    } else {
      return this._getEnglishCharacterParams(textChar, effectiveFont, xHeight, previousChar, nextChar);
    }
  }

  /**
   * Helper function for English character parameters
   */
  static _getEnglishCharacterParams(textChar, font, xHeight, previousChar, nextChar) {
    const isTransportHeavy = font === 'TransportHeavy';
    const fontWidth = isTransportHeavy ? _template_js__WEBPACK_IMPORTED_MODULE_2__.textWidthHeavy : _template_js__WEBPACK_IMPORTED_MODULE_2__.textWidthMedium;

    // Check if current character should use short width based on specific rules
    let hasShortWidth = false;

    // Rule 1: Characters following T, U, V use short width
    if (previousChar && ['T', 'U', 'V'].includes(previousChar)) {
      hasShortWidth = true;
    }

    // Rule 2: T, U, V use short width when followed by a character that has short width AND is lowercase
    if (['T', 'U', 'V'].includes(textChar) && nextChar) {
      const nextCharWidthObj = fontWidth.find(e => e.char === nextChar);
      const nextCharHasShortWidth = nextCharWidthObj && nextCharWidthObj.shortWidth !== 0;
      const nextCharIsLowercase = nextChar >= 'a' && nextChar <= 'z';

      if (nextCharHasShortWidth && nextCharIsLowercase) {
        hasShortWidth = true;
      }
    }

    // Rule 3: W uses short width when followed by uppercase letter
    if (textChar === 'W' && nextChar) {
      const nextCharIsUppercase = nextChar >= 'A' && nextChar <= 'Z';
      if (nextCharIsUppercase) {
        hasShortWidth = true;
      }
    }

    const charWidthObj = fontWidth.find(e => e.char === textChar);
    const charWidth = charWidthObj ?
      (hasShortWidth && charWidthObj.shortWidth !== 0 ? charWidthObj.shortWidth : charWidthObj.width) :
      100;

    return {
      actualChar: textChar,
      fontFamily: font,
      fontSize: xHeight * 1.88,
      charWidth: charWidth,
      bracketOffset: ['(', ')'].includes(textChar) ? 0.15 : 0,
      leftOffset: 0,
      topOffset: 0.1 * xHeight,
      frameWidth: charWidth * xHeight / 100 - 2,
      frameHeight: xHeight * 2 - 2,
      advanceWidth: charWidth * xHeight / 100
    };
  }
  /**
   * Helper function for non-English character parameters (Chinese, numbers, punctuation)
   */
  static _getNonEnglishCharacterParams(actualChar, font, xHeight, previousChar, charSpacing = 0) {
    const isKnownPunctuation = _template_js__WEBPACK_IMPORTED_MODULE_2__.textWidthHeavy.map(item => item.char).includes(actualChar);

    if (this._isNumber(actualChar)) {
      // Numbers in non-English text
      return this._getNumberParams(actualChar, xHeight, previousChar);
    } else if (isKnownPunctuation) {
      // English punctuation in non-English text - use Transport Heavy
      return this._getPunctuationParams(actualChar, xHeight, previousChar);
    } else {
      // Chinese characters - pass the font parameter
      return this._getChineseCharacterParams(actualChar, font, xHeight, charSpacing);
    }
  }

  /**
   * Helper function for punctuation parameters
   */
  static _getPunctuationParams(textChar, xHeight, previousChar) {
    const charWidthObj = _template_js__WEBPACK_IMPORTED_MODULE_2__.textWidthHeavy.find(e => e.char === textChar);
    const charWidth = charWidthObj?.width || xHeight * 100 / 4;

    // Check if this punctuation should use fallback font
    let fontFamily;
    if (textChar === '、') {
      fontFamily = 'TW-MOE-Std-Kai';
    } else if (TRANSPORT_MISSING_CHARS.has(textChar)) {
      fontFamily = 'parsedFontSans'; // Use sans serif for special punctuation
      console.log(`Punctuation "${textChar}" using fallback font`);
    } else {
      fontFamily = 'TransportHeavy';
    }

    return {
      actualChar: textChar,
      fontFamily: fontFamily,
      fontSize: xHeight * 1.88,
      charWidth: charWidth,
      bracketOffset: ['(', ')'].includes(textChar) ? -0.3125 : 0,
      leftOffset: 0.25 * xHeight,
      topOffset: 0.2 * xHeight,
      frameWidth: charWidth * xHeight / 100 + 0.5 * xHeight - 2,
      frameHeight: 2.85 * xHeight - 2,
      advanceWidth: charWidth * xHeight / 100 + 0.5 * xHeight
    };
  }

  /**
   * Helper function for number parameters
   */
  static _getNumberParams(textChar, xHeight, previousChar) {
    const charWidthObj = _template_js__WEBPACK_IMPORTED_MODULE_2__.textWidthMedium.find(e => e.char === textChar);
    const charWidth = charWidthObj.width * 1.375 * xHeight / 100;

    return {
      actualChar: textChar, fontFamily: 'TransportMedium',
      fontSize: xHeight * 1.88 * 1.375,
      charWidth: charWidth * xHeight,
      bracketOffset: 0,
      leftOffset: (containsNonEnglishCharacters(previousChar || '') ? 0.25 * xHeight : 0),
      topOffset: 0.1 * xHeight,
      frameWidth: charWidth - 2 + (containsNonEnglishCharacters(previousChar || '') ? 0.25 * xHeight : 0),
      frameHeight: 2.85 * xHeight - 2,
      advanceWidth: charWidth + (containsNonEnglishCharacters(previousChar || '') ? 0.25 * xHeight : 0)
    };
  }  /**
   * Helper function for Chinese character parameters
   */
  static _getChineseCharacterParams(actualChar, font, xHeight, charSpacing = 0) {
    // Check if character requires special override font
    let fontFamily = font;

    try {
      // Get override rules from FontPriorityManager
      const rules = _modal_md_font_js__WEBPACK_IMPORTED_MODULE_5__.FontPriorityManager.getOverrideRules();

      // Find a rule that contains this character
      const matchingRule = rules.find(rule => rule.characters.includes(actualChar));

      if (matchingRule) {
        fontFamily = matchingRule.font;
      }
    } catch (error) {
      console.warn('Could not check special characters, using default font:', error);
      // Continue with the originally passed font
    }

    return {
      actualChar: actualChar,
      fontFamily: fontFamily,
      fontSize: xHeight * 2.25,
      charWidth: 275,
      bracketOffset: 0,
      leftOffset: 0.25 * xHeight,
      topOffset: -0.2 * xHeight,
      frameWidth: 2.75 * xHeight - 2,
      frameHeight: 2.85 * xHeight - 2,
      advanceWidth: 2.75 * xHeight + charSpacing
    };
  }

  /**
   * Helper function to check if character is a number
   */
  static _isNumber(char) {
    return /\d/.test(char);
  }

  /**
   * Helper function to calculate character positioning
   */
  static _calculateCharacterPositioning(left_pos, charParams, xHeight, previousChar) {
    return {
      charLeftPos: left_pos + charParams.leftOffset,
      charTopPos: charParams.bracketOffset * xHeight + charParams.topOffset
    };
  }

  /**
   * Helper function to create character elements (path and frame)
   */
  static _createCharacterElements(charParams, positioning, fontGlyphs, color, xHeight, textChar, font, previousChar, nextChar) {
    // Access font metrics
    const fontMetrics = {
      unitsPerEm: fontGlyphs.unitsPerEm,
      ascender: fontGlyphs.ascender,
      descender: fontGlyphs.descender,
    };

    // Scale metrics to desired font size
    const fontScale = charParams.fontSize / fontMetrics.unitsPerEm;
    const scaledAscender = fontMetrics.ascender * fontScale;
    const scaledDescender = Math.abs(fontMetrics.descender) * fontScale;
    const fontHeight = scaledAscender + scaledDescender;

    // Calculate the vertical position to center the glyph
    const font2CanvasRatio = 1 / fontHeight * charParams.frameHeight;

    // Create the path parameters for this character
    const pathParams = {
      character: charParams.actualChar,
      x: 0,
      y: scaledAscender * font2CanvasRatio,
      fontSize: charParams.fontSize,
      fontFamily: charParams.fontFamily,
      fill: color
    };

    // Get the font path
    const charPath = (0,_path_js__WEBPACK_IMPORTED_MODULE_3__.getFontPath)(pathParams);
    charPath.fill = color;
    let resolvedFontGlyphs = fontGlyphs;
    if (charPath._resolvedFont) {
      resolvedFontGlyphs = charPath._resolvedFont;
    }
    // Ensure opentype rounding patch is applied at point of use in TextObject
    (0,_path_js__WEBPACK_IMPORTED_MODULE_3__.ensureOpenTypePatched)();
    const charSVG = charPath.toPathData({ flipY: false });
    const charGlyph = resolvedFontGlyphs.charToGlyph(charParams.actualChar);

    const minTop = Math.min(...charPath.commands.map(cmd => cmd.y));

    // Calculate left position adjustment for short width characters
    let leftAdjustment = 0;

    // Check if current character should use short width based on the same rules
    let shouldUseShortWidth = false;

    // Rule 1: Characters following T, U, V use short width
    if (previousChar && ['T', 'U', 'V'].includes(previousChar)) {
      shouldUseShortWidth = true;
    }

    // Rule 2: T, U, V use short width when followed by a character that has short width AND is lowercase
    if (['T', 'U', 'V'].includes(textChar) && nextChar) {
      const isTransportFont = font === 'TransportMedium' || font === 'TransportHeavy';
      if (isTransportFont) {
        const fontWidth = font === 'TransportHeavy' ? _template_js__WEBPACK_IMPORTED_MODULE_2__.textWidthHeavy : _template_js__WEBPACK_IMPORTED_MODULE_2__.textWidthMedium;
        const nextCharWidthObj = fontWidth.find(e => e.char === nextChar);
        const nextCharHasShortWidth = nextCharWidthObj && nextCharWidthObj.shortWidth !== 0;
        const nextCharIsLowercase = nextChar >= 'a' && nextChar <= 'z';

        if (nextCharHasShortWidth && nextCharIsLowercase) {
          shouldUseShortWidth = true;
        }
      }
    }

    // Rule 3: W uses short width when followed by uppercase letter
    if (textChar === 'W' && nextChar) {
      const nextCharIsUppercase = nextChar >= 'A' && nextChar <= 'Z';
      if (nextCharIsUppercase) {
        shouldUseShortWidth = true;
      }
    }

    if (shouldUseShortWidth && !TRANSPORT_MISSING_CHARS.has(textChar)) {
      // Check if short width was actually used for this character
      const isTransportFont = font === 'TransportMedium' || font === 'TransportHeavy';
      if (isTransportFont) {
        const fontWidth = font === 'TransportHeavy' ? _template_js__WEBPACK_IMPORTED_MODULE_2__.textWidthHeavy : _template_js__WEBPACK_IMPORTED_MODULE_2__.textWidthMedium;
        const charWidthObj = fontWidth.find(e => e.char === textChar);

        if (charWidthObj && charWidthObj.shortWidth !== 0 && charWidthObj.shortWidth < charWidthObj.width) {
          // Calculate the width difference and center the character
          const widthDifference = (charWidthObj.width - charWidthObj.shortWidth) * xHeight / 100;
          leftAdjustment = widthDifference / 2;
        }
      }
    }

    // Create a path from the font path data
    const txt_char = new fabric.Path(charSVG, {
      left: (charParams.actualChar === '、' ? charGlyph.leftSideBearing - 800 : charGlyph.leftSideBearing) * fontScale + positioning.charLeftPos - leftAdjustment,
      top: minTop + positioning.charTopPos - (charParams.actualChar === '、' ? 600 * fontScale : 0),
      fill: color,
      originX: 'left',
      originY: 'top'
    });

    // Set properties and return the created path
    txt_char.lockScalingX = txt_char.lockScalingY = true;
    txt_char._textChar = charParams.actualChar; // Store the character for reference

    // Create the frame rectangle
    const txt_frame = new fabric.Rect({
      left: positioning.charLeftPos - charParams.leftOffset,
      top: 0,
      width: charParams.frameWidth,
      height: charParams.frameHeight,
      fill: 'rgba(0,0,0,0)',
      stroke: _sidebar_sbGeneral_js__WEBPACK_IMPORTED_MODULE_4__.GeneralSettings.showTextBorders ? color : 'rgba(0,0,0,0)',
      strokeWidth: 2,
      strokeDashArray: [xHeight / 10, xHeight / 10],
    });

    return {
      textPath: txt_char,
      frame: txt_frame
    };
  }
  /**
   * Helper function to get appropriate font glyphs
   */
  static _getFontGlyphs(fontFamily, textChar, containsNonAlphabetic) {
    let glyphs = null;
    if (fontFamily === 'TransportMedium') {
      glyphs = _path_js__WEBPACK_IMPORTED_MODULE_3__.parsedFontMedium;
    } else if (fontFamily === 'TransportHeavy') {
      glyphs = _path_js__WEBPACK_IMPORTED_MODULE_3__.parsedFontHeavy;
    } else if (fontFamily === 'TW-MOE-Std-Kai') {
      glyphs = _path_js__WEBPACK_IMPORTED_MODULE_3__.parsedFontKai;
    } else if (fontFamily === 'parsedFontKorean') {
      glyphs = _path_js__WEBPACK_IMPORTED_MODULE_3__.parsedFontKorean;
    } else if (fontFamily === 'parsedFontHK') {
      glyphs = _path_js__WEBPACK_IMPORTED_MODULE_3__.parsedFontHK;
    } else if (fontFamily === 'parsedFontChocolate') {
      glyphs = _path_js__WEBPACK_IMPORTED_MODULE_3__.parsedFontChocolate;
    } else if (fontFamily === 'parsedFontSans') {
      glyphs = _path_js__WEBPACK_IMPORTED_MODULE_3__.parsedFontSans; // Return the sans serif fallback font
    } else if (fontFamily.startsWith('custom_')) {
      // Handle custom fonts - they should have their font object in window
      const customFont = window[fontFamily];
      glyphs = customFont || _path_js__WEBPACK_IMPORTED_MODULE_3__.parsedFontChinese; // Fallback to default
    } else {
      glyphs = _path_js__WEBPACK_IMPORTED_MODULE_3__.parsedFontChinese;
    }

    // Fallback if the selected font is not yet loaded
    if (!glyphs) {
      return _path_js__WEBPACK_IMPORTED_MODULE_3__.parsedFontMedium;
    }
    return glyphs;
  }
  /**
   * Static method to create text and frame elements
   * 
   * This function handles different character types with clear separation:
   * 
   * 1. English characters:
   *    - Uses TransportMedium or TransportHeavy font based on font parameter
   *    - Handles short width for characters following T, U, V
   * 
   * 2. Non-English text containing:
   *    a) English punctuation: Uses TransportHeavy font
   *    b) Numbers: Uses TransportMedium font with adjusted sizing
   *    c) Chinese characters: Uses font from FontPriorityManager priority system
   *       - Font determined by priority list (e.g., TW-MOE-Std-Kai, custom fonts)
   *       - Special handling for comma (,) → 、 transformation
   *
   * Each character type has specific parameters for:
   * - Font family and size
   * - Character width and positioning offsets
   * - Frame dimensions and advance width
   */
  static createTextElements(txt, xHeight, color, font, isCursor = false, charSpacing = 0) {
    let txtCharList = [];
    let txtFrameList = [];
    let left_pos = 0;    // Check if text contains any non-English characters
    const containsNonAlphabetic = containsNonEnglishCharacters(txt);

    for (let i = 0; i < txt.length; i++) {
      let textChar = txt.charAt(i);
      const previousChar = i > 0 ? txt[i - 1] : null;
      const nextChar = i < txt.length - 1 ? txt[i + 1] : null;

      // Get character parameters based on type
      const charParams = this._getCharacterParameters(textChar, containsNonAlphabetic, font, xHeight, previousChar, nextChar, charSpacing);
      // Calculate positioning
      const positioning = this._calculateCharacterPositioning(left_pos, charParams, xHeight, previousChar);

      // Get font glyphs
      const fontGlyphs = this._getFontGlyphs(charParams.fontFamily, charParams.actualChar, containsNonAlphabetic);

      // Create character path and frame
      const charElements = this._createCharacterElements(
        charParams,
        positioning,
        fontGlyphs,
        color,
        xHeight,
        textChar,
        font,
        previousChar,
        nextChar
      );

      txtCharList.push(charElements.textPath);
      txtFrameList.push(charElements.frame);

      // Update position for next character
      left_pos += charParams.advanceWidth;
    }

    return { txtCharList, txtFrameList, containsNonAlphabetic };
  }

  /**
   * Legacy Method to calculate combined bounding box from rectangle objects in a group
   */
  static getCombinedBoundingBoxOfRects() {
    let combinedBBox = { left: Infinity, top: Infinity, right: -Infinity, bottom: -Infinity };
    let points = [];
    this.forEachObject(obj => {
      if (obj.type === 'rect') {
        obj.setCoords();
        const aCoords = obj.aCoords;

        // Transform the coordinates to the canvas coordinate system
        Object.values(aCoords).forEach(point => {
          const absPoint = fabric.util.transformPoint(point, this.calcTransformMatrix());
          combinedBBox.left = Math.min(combinedBBox.left, absPoint.x);
          combinedBBox.top = Math.min(combinedBBox.top, absPoint.y);
          combinedBBox.right = Math.max(combinedBBox.right, absPoint.x);
          combinedBBox.bottom = Math.max(combinedBBox.bottom, absPoint.y);
        });
      }
    });

    // Calculate the 8 points (excluding the center point) from the combined bounding box
    const centerX = (combinedBBox.left + combinedBBox.right) / 2;
    const centerY = (combinedBBox.top + combinedBBox.bottom) / 2;

    points = [
      { x: combinedBBox.left, y: combinedBBox.top, label: 'E1' }, // Top-left corner
      { x: centerX, y: combinedBBox.top, label: 'E2' }, // Top-middle
      { x: combinedBBox.right, y: combinedBBox.top, label: 'E3' }, // Top-right corner
      { x: combinedBBox.right, y: centerY, label: 'E4' }, // Middle-right
      { x: combinedBBox.right, y: combinedBBox.bottom, label: 'E5' }, // Bottom-right corner
      { x: centerX, y: combinedBBox.bottom, label: 'E6' }, // Bottom-middle
      { x: combinedBBox.left, y: combinedBBox.bottom, label: 'E7' }, // Bottom-left corner
      { x: combinedBBox.left, y: centerY, label: 'E8' } // Middle-left
    ];

    return points;
  }

  /**
   * Method to update text properties
   */
  updateText(newText, newXHeight, newFont, newColor, newCharSpacing = this.charSpacing) {
    // Remove old objects
    this.removeAll();

    // Create new text elements
    const { txtCharList, txtFrameList, containsNonAlphabetic } = TextObject.createTextElements(
      newText,
      newXHeight,
      newColor,
      newFont,
      false,
      newCharSpacing
    );

    // Create a new group
    const group = new fabric.Group([...txtCharList, ...txtFrameList], {
      left: this.left,
      top: this.top
    });


    // Add vertex and text information to the group
    group.setCoords();
    group.text = newText;
    group.xHeight = newXHeight;

    // Update this text object with the new group
    this.replaceBasePolygon(group);

    // Update properties
    this.text = newText;
    this.xHeight = newXHeight;
    this.font = newFont;
    this.color = newColor;
    this.charSpacing = newCharSpacing;
    this.txtCharList = txtCharList;
    this.txtFrameList = txtFrameList;
    this.containsNonAlphabetic = containsNonAlphabetic;

    // Update the name for the object inspector
    this._showName = `<Group ${this.canvasID}> Text - ${newText}`;

    // Update underline if it exists
    if (this.underline) {
      const textWidth = (this.basePolygon && this.basePolygon.width)
        ? this.basePolygon.width * this.basePolygon.scaleX
        : this.width;

      const objectBBox = { left: this.left, top: this.top + this.height, right: 0, bottom: 0 };
      const objectSize = { width: textWidth, height: this.underline.xHeight / 4 };

      const basePoly = (0,_divider_js__WEBPACK_IMPORTED_MODULE_1__.drawDivider)(this.underline.xHeight, this.underline.color, objectBBox, objectSize, 'HLine');
      this.underline.replaceBasePolygon(basePoly, false);
      //this.underline.drawVertex(false);
      //this.underline.setCoords();
    }

    //this.canvas.renderAll();
  }
}

/**
 * Check if text contains non-English characters
 * @param {string} txt - Text to check
 * @returns {boolean} - True if text contains non-English characters
 */
function containsNonEnglishCharacters(txt) {
  if (!txt || typeof txt !== 'string') {
    return false;
  }

  return txt.split('').some(char => {
    // If the character is not in our English width dictionaries, consider it non-English
    return !_template_js__WEBPACK_IMPORTED_MODULE_2__.textWidthMedium.map(item => item.char).includes(char);
  });
}

/**
 * Check if a single character is non-English
 * @param {string} char - Single character to check
 * @returns {boolean} - True if character is non-English
 */
function isNonEnglishCharacter(char) {
  if (!char || typeof char !== 'string' || char.length !== 1) {
    return false;
  }

  return !_template_js__WEBPACK_IMPORTED_MODULE_2__.textWidthMedium.map(item => item.char).includes(char);
}




/***/ }),

/***/ "./js/objects/vertex.js":
/*!******************************!*\
  !*** ./js/objects/vertex.js ***!
  \******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   VertexControl: () => (/* binding */ VertexControl)
/* harmony export */ });
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _anchor_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./anchor.js */ "./js/objects/anchor.js");
/* harmony import */ var _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../canvas/keyboardEvents.js */ "./js/canvas/keyboardEvents.js");
// This file handles VertexControl class and keyboard navigation for vertices
// Implements Tab key cycling through vertices



 // Import the event handler for sidebar toggling

const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas; // Fabric.js canvas instance
const canvasObject = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvasObject; // All objects on the canvas
let vertexSnapInProgress = false; // Flag to indicate if a snap operation is in progress

class VertexControl extends fabric.Control {
    constructor(vertex, baseGroup) {
        const width = baseGroup.width || baseGroup.tempWidth
        const height = baseGroup.height || baseGroup.tempHeight
        const left = baseGroup.left
        const top = baseGroup.top
        super({
            x: (vertex.x - left) / width - 0.5,
            y: (vertex.y - top) / height - 0.5,
            offsetX: 0,
            offsetY: 0,
            cursorStyle: 'pointer',
            cornerSize: 20,
        });
        this.hover = false;
        this.isDragging = false; // New flag to indicate active dragging
        this.mouseUpHandler = this.onClick.bind(this);
        this.render = this.renderControl.bind(this);
        this.vertex = vertex;
        this.baseGroup = baseGroup;
        this.snapThreshold = 50; // Distance in pixels for snapping
        this.snapTarget = null; // Current snap target
        this.snapHighlight = null; // Visual highlight of snap target
        this.handleMouseMoveRef = this.handleMouseMove.bind(this);
        this.handleMouseUpRef = this.handleMouseUp.bind(this);
        this.cancelDragRef = this.cancelDrag.bind(this);
    }

    renderControl(ctx, left, top, styleOverride, fabricObject) {
        const size = this.cornerSize;

        // Draw the circle with different color based on state
        ctx.beginPath();
        ctx.arc(left, top, size / 2, 0, 2 * Math.PI, false);

        // Different fill colors based on state
        if (this.isDragging) {
            // Active dragging state - bright yellow
            ctx.fillStyle = this.snapTarget ? 'rgba(0, 255, 0, 0.7)' : 'rgba(255, 255, 0, 0.7)';
        } else if (this.baseGroup.focusMode) {
            // Focus mode - no color
            ctx.fillStyle = 'rgba(0, 0, 0, 0)';
        }
        else {
            // Normal or hover state
            ctx.fillStyle = `rgba(255, 20, 20, ${this.hover ? 0.7 : 0.2})`;
        }

        ctx.fill();
        ctx.lineWidth = 1;
        ctx.strokeStyle = this.baseGroup.focusMode ? 'rgba(0, 0, 0, 0)' : this.VertexColorPicker(this.vertex);
        ctx.stroke();

        // Draw the text
        ctx.font = '10px Arial, sans-serif';
        ctx.fillStyle = this.baseGroup.focusMode ? 'rgba(0, 0, 0, 0)' : this.VertexColorPicker(this.vertex);
        ctx.textAlign = 'center';
        ctx.textBaseline = 'middle';
        ctx.fillText(this.vertex.label, left, this.vertex.label.includes('E') ? top - 15 : top + 15);
    }

    VertexColorPicker(vertex) {
        switch (vertex.label.substring(0, 1)) {
            case 'E':
                return 'red'
            case 'V':
                return 'violet'
            case 'C':
                return 'orange'
        }
    }

    onClick(eventData, transform) {
        // Check if it's a left-click (button 1)
        if (eventData.button !== 0 && eventData.type !== 'touchend') return;

        // Prevent clicks during ongoing snap operations
        if (vertexSnapInProgress) return;

        // Prevent vertex activation if measure mode is active
          if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeMeasurement) {
        }

        const vertexX = this.vertex.x;
        const vertexY = this.vertex.y;

        if (!_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex) {
            _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex = this;
            this.isDown = true;
            this.isDragging = true; // Set dragging flag

            // Store original position and offset from vertex to group center
            this.originalPosition = {
                left: this.baseGroup.left,
                top: this.baseGroup.top
            };

            this.vertexOriginalPosition = {
                x: this.vertex.x,
                y: this.vertex.y
            };

            this.vertexOffset = {
                x: this.vertex.x - this.baseGroup.left,
                y: this.vertex.y - this.baseGroup.top
            };

            if (this.baseGroup.functionalType === 'SideRoad') {
                this.sideroadRouteOffset = {
                    x: this.vertex.x - this.baseGroup.routeList[0].x,
                    y: this.vertex.y - this.baseGroup.routeList[0].y
                };
            }

            // Set cursor style for canvas
            canvas.defaultCursor = 'move';

            // Add mouse move and click handlers for drag behavior
            document.removeEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent);
            document.addEventListener('keydown', this.cancelDragRef);
            canvas.on('mouse:move', this.handleMouseMoveRef);

            _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();

            this.baseGroup.enterFocusMode();
            setTimeout(() => {
                canvas.on('mouse:up', this.handleMouseUpRef);
            }, 50); // Delay to ensure mouse move is registered first
        }
    }

    handleMouseMove(event) {
        if (!this.isDown) return;

        const pointer = canvas.getPointer(event.e);

        // If Shift key is held, allow snap target detection/highlight, but don't move the group
        if (event && event.e && event.e.shiftKey) {
            this.checkForSnapTargets(pointer);
            _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
            return;
        }

        // Find nearest vertex for snapping
        this.checkForSnapTargets(pointer);

        // Calculate new position of group based on vertex position
        let newLeft, newTop;

        if (this.snapTarget) {
            // Snap to the target vertex
            const snapPoint = this.snapTarget.vertex;

            // Special handling for text objects during snapping

            // Calculate the offset from the current vertex to the object's center
            const currentVertex = this.baseGroup.getBasePolygonVertex(this.vertex.label);
            if (currentVertex) {
                // Calculate position by maintaining the same offset between vertex and object center
                const offsetX = this.baseGroup.left - currentVertex.x;
                const offsetY = this.baseGroup.top - currentVertex.y;
                newLeft = snapPoint.x + offsetX;
                newTop = snapPoint.y + offsetY;
            } else {
                // Fall back to standard calculation if vertex not found
                newLeft = snapPoint.x - this.vertexOffset.x;
                newTop = snapPoint.y - this.vertexOffset.y;
            }
        } else {
            // Regular movement (no snapping target)

            // For text objects, adjust movement to prevent shifting
            const currentVertex = this.baseGroup.getBasePolygonVertex(this.vertex.label);
            if (currentVertex) {
                // Move based on cursor position relative to vertex
                const dx = pointer.x - currentVertex.x;
                const dy = pointer.y - currentVertex.y;
                newLeft = this.baseGroup.left + dx;
                newTop = this.baseGroup.top + dy;
            } else {
                // Fall back to standard calculation if vertex not found
                newLeft = pointer.x - this.vertexOffset.x;
                newTop = pointer.y - this.vertexOffset.y;
            }

        }

        // Move the group
        if (this.baseGroup.functionalType !== 'SideRoad') {

            // Process both X and Y updates in a single atomic operation
            let positionChanged = false;

            if (!this.baseGroup.lockMovementX) {
                this.baseGroup.set({ left: newLeft });
                positionChanged = true;
            }

            if (!this.baseGroup.lockMovementY) {
                this.baseGroup.set({ top: newTop });
                positionChanged = true;
            }

            if (positionChanged) {


                // Update coordinates in one go
                this.baseGroup.setCoords();
                this.baseGroup.updateAllCoord();


            }
        } else {
            // Special handling for MainRoad and SideRoad
            // If we have a snap target, use its position instead of pointer
            const finalPointer = this.snapTarget ?
                { x: this.snapTarget.vertex.x, y: this.snapTarget.vertex.y } :
                pointer;
            if (this.baseGroup.functionalType === 'SideRoad') {
                if (this.sideroadRouteOffset) {
                    if (!this.baseGroup.lockMovementX) {
                        this.baseGroup.routeList[0].x = finalPointer.x - this.sideroadRouteOffset.x;
                    }
                    if (!this.baseGroup.lockMovementY) {
                        this.baseGroup.routeList[0].y = finalPointer.y - this.sideroadRouteOffset.y;
                    }
                } else {
                    // Fallback to V1-based logic if offset not available
                    if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex && _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.vertex) {
                        let v1Vertex = this.baseGroup.basePolygon.vertex.find(v => v.label === 'V1');
                        let offsetX = 0;
                        let offsetY = 0;

                        if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.vertex.label !== 'V1' && v1Vertex) {
                            const activeVertexObj = this.baseGroup.basePolygon.vertex.find(v => v.label === _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.vertex.label);
                            if (activeVertexObj) {
                                offsetX = v1Vertex.x - activeVertexObj.x;
                                offsetY = v1Vertex.y - activeVertexObj.y;
                            }
                        }

                        if (!this.baseGroup.lockMovementX) {
                            this.baseGroup.routeList[0].x = finalPointer.x + offsetX;
                        }
                        if (!this.baseGroup.lockMovementY) {
                            this.baseGroup.routeList[0].y = finalPointer.y + offsetY;
                        }
                    } else {
                        // Deep fallback
                        this.baseGroup.routeList.forEach(route => {
                            if (!this.baseGroup.lockMovementX) {
                                route.x = newLeft + this.vertexOffset.x;
                            }
                            if (!this.baseGroup.lockMovementY) {
                                route.y = newTop + this.vertexOffset.y;
                            }
                        });
                    }
                }
            } else {
                // For MainRoad, use original behavior but respect lock properties
            }

            // Update side-flag on SideRoad based on its position relative to the main road
            if (this.baseGroup.functionalType === 'SideRoad' && this.baseGroup.mainRoad) {
                // Compare current branch root to main road tip (routeList[1])
                const mainTipX = this.baseGroup.mainRoad.routeList[1].x;
                this.baseGroup.side = this.baseGroup.routeList[0].x < mainTipX;
                this.baseGroup.routeList[0].angle = this.baseGroup.side ? -Math.abs(this.baseGroup.routeList[0].angle) : Math.abs(this.baseGroup.routeList[0].angle);
            }
            // Process route changes in a single update cycle, but only for directions that aren't locked
            let updateX = !this.baseGroup.lockMovementX;
            let updateY = !this.baseGroup.lockMovementY;

            let cycleStartedX = false;
            let cycleStartedY = false;

            if (updateX && !_anchor_js__WEBPACK_IMPORTED_MODULE_1__.globalAnchorTree.updateInProgressX) {
                _anchor_js__WEBPACK_IMPORTED_MODULE_1__.globalAnchorTree.startUpdateCycle('x', this.baseGroup.canvasID);
                cycleStartedX = true;
            }
            if (updateY && !_anchor_js__WEBPACK_IMPORTED_MODULE_1__.globalAnchorTree.updateInProgressY) {
                _anchor_js__WEBPACK_IMPORTED_MODULE_1__.globalAnchorTree.startUpdateCycle('y', this.baseGroup.canvasID);
                cycleStartedY = true;
            }

            this.baseGroup.onMove();
            this.baseGroup.setCoords();
            this.baseGroup.updateAllCoord();

            if (cycleStartedX) {
                _anchor_js__WEBPACK_IMPORTED_MODULE_1__.globalAnchorTree.endUpdateCycle('x');
            }
            if (cycleStartedY) {
                _anchor_js__WEBPACK_IMPORTED_MODULE_1__.globalAnchorTree.endUpdateCycle('y');
            }
        }

        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
    }

    checkForSnapTargets(pointer) {
        // Clear any existing snap highlight
        this.clearSnapHighlight();

        this.snapTarget = null;

        // Check all canvas objects for potential snap targets
        let closestDistance = this.snapThreshold;
        let closestVertex = null;
        let closestObject = null;

        canvasObject.forEach(obj => {
            // Skip the current object and objects without basePolygon
            if (obj === this.baseGroup || !obj.basePolygon || !obj.basePolygon.vertex) return;

            // If current object is part of a two-liner pair, also skip its partner
            if (this.baseGroup.isTwoLiner && this.baseGroup.twoLinerPartner && obj === this.baseGroup.twoLinerPartner) return;

            // Check each vertex
            obj.basePolygon.vertex.forEach(vertex => {
                if (vertex.display === 1) {
                    const dx = vertex.x - (pointer.x);
                    const dy = vertex.y - (pointer.y);
                    const distance = Math.sqrt(dx * dx + dy * dy);

                    if (distance < closestDistance) {
                        closestDistance = distance;
                        closestVertex = vertex;
                        closestObject = obj;
                    }
                }
            });
        });

        // If we found a vertex within threshold, highlight it
        if (closestVertex) {
            this.snapTarget = {
                object: closestObject,
                vertex: closestVertex
            };
            this.addSnapHighlight(); // Add highlight for the snap target

        }
    }

    addSnapHighlight() {
        if (this.snapTarget) {
            // Match the circle size with the vertex control size
            const size = this.cornerSize + 5;
            const radius = size / 2;
            const zoomFactor = canvas.getZoom() || 1; // Get the current zoom factor

            // Create a hollow circle to indicate snap target, centered at the vertex position
            this.snapHighlight = new fabric.Circle({
                left: this.snapTarget.vertex.x,
                top: this.snapTarget.vertex.y,
                radius: radius / zoomFactor,
                fill: 'transparent',
                stroke: '#00FF00',
                strokeWidth: 2 / zoomFactor,
                selectable: false,
                evented: false,
                originX: 'center',
                originY: 'center'
            });

            // Add the highlight to the canvas
            canvas.add(this.snapHighlight);

            // Force a render to update vertex appearance
            _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
        }
    }


    // New method to clear snap highlight
    clearSnapHighlight() {
        if (this.snapHighlight) {
            canvas.remove(this.snapHighlight);
            this.snapHighlight = null;
            _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
        }
    }

    handleMouseUp(event) {
        // Check for right-click (button 2)
        if (event.e.button === 2 && this.isDown) {
            // Cancel drag on right-click
            this.restoreOriginalPosition();
            return;
        }

        if (!this.isDown) return;

        // Check for right-click (button 2)
        if (event.e.button === 2) {
            // Cancel drag on right-click
            this.restoreOriginalPosition();
            return;
        }

        // Only process left clicks (button 1) for object selection
        if (event.e.button !== 0 && event.e.type !== 'touchend') return;

        // If we have a snap target, use that for anchoring
        if (this.snapTarget) {
            // Set the flag to prevent additional onClick events
            vertexSnapInProgress = true;

            // Store the snap target before finishing the drag
            const savedSnapTarget = {
                object: this.snapTarget.object,
                vertex: this.snapTarget.vertex
            };
            const savedVertex = this.vertex;
            const savedBaseGroup = this.baseGroup;

            // Clear the snap highlight before finishing the drag
            this.clearSnapHighlight();

            this.finishDrag();

            // Start the anchor process with the saved snap target
            setTimeout(() => {
                const self = this; // preserve context
                const maybePromise = (0,_anchor_js__WEBPACK_IMPORTED_MODULE_1__.anchorShape)(
                    savedSnapTarget.object,
                    savedBaseGroup,
                    {
                        vertexIndex1: savedVertex.label,
                        vertexIndex2: savedSnapTarget.vertex.label
                    }
                );
                // If anchorShape returned a promise, attach handlers; else treat as cancelled
                if (maybePromise && typeof maybePromise.then === 'function') {
                    maybePromise.then(() => {
                        setTimeout(() => { vertexSnapInProgress = false; }, 300);
                    }).catch(() => {
                        // Restore original position on cancellation/error
                        self.restoreOriginalPosition();
                        vertexSnapInProgress = false;
                    });
                } else {
                    // Cancellation occurred before promise creation (early return in anchorShape)
                    self.restoreOriginalPosition();
                    vertexSnapInProgress = false;
                }
            }, 100);
            return;
        }

        const pointer = canvas.getPointer(event.e);
        const targetObject = canvas.findTarget(event.e);

        // Check if we clicked on another object with vertices
        if (targetObject && targetObject !== this.baseGroup && targetObject.basePolygon && targetObject.basePolygon.vertex) {
            // Find the closest vertex to the click point
            const vertices = targetObject.basePolygon.vertex;
            if (!vertices) {
                // Set flag before starting the finishing process
                vertexSnapInProgress = true;

                // Finish drag with a delay to ensure proper cleanup
                this.finishDrag();

                // Reset the flag after a delay
                setTimeout(() => {
                    vertexSnapInProgress = false;
                }, 300);
                return;
            }

            let closestVertex = null;
            let minDistance = 30; // Minimum distance to consider a hit

            for (const vertex of vertices) {
                const dx = vertex.x - pointer.x;
                const dy = vertex.y - pointer.y;
                const distance = Math.sqrt(dx * dx + dy * dy);

                if (distance < minDistance) {
                    minDistance = distance;
                    closestVertex = vertex;
                }
            }

            if (closestVertex) {
                // Found a vertex to anchor to
                vertexSnapInProgress = true;
                this.finishDrag();

                // Start the anchor process
                setTimeout(() => {
                    const self = this;
                    const maybePromise = (0,_anchor_js__WEBPACK_IMPORTED_MODULE_1__.anchorShape)(
                        targetObject,
                        this.baseGroup,
                        {
                            vertexIndex1: this.vertex.label,
                            vertexIndex2: closestVertex.label
                        }
                    );
                    if (maybePromise && typeof maybePromise.then === 'function') {
                        maybePromise.then(() => {
                            setTimeout(() => { vertexSnapInProgress = false; }, 300);
                        }).catch(() => {
                            self.restoreOriginalPosition();
                            vertexSnapInProgress = false;
                        });
                    } else {
                        self.restoreOriginalPosition();
                        vertexSnapInProgress = false;
                    }
                }, 100);
                return;
            }
        }    // If we click on empty space, set flag and finish the drag with a single operation
        vertexSnapInProgress = true;

        // Store reference to the current baseGroup before calling finishDrag
        const baseGroupRef = this.baseGroup;

        // Call finishDrag once to handle all cleanup consistently
        this.finishDrag(true); // Pass true to indicate empty space click

        // Reset the flag after a delay to prevent new clicks
        setTimeout(() => {
            vertexSnapInProgress = false;
            _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
        }, 300);
    }

    // New helper method to remove all mouse events immediately
    removeAllMouseEvents() {
        canvas.off('mouse:move', this.handleMouseMoveRef);
        canvas.off('mouse:up', this.handleMouseUpRef);
        document.removeEventListener('keydown', this.cancelDragRef);

        // Clear any snap highlight before removing events
        this.clearSnapHighlight();

        // Restore default behavior
        document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent);
        canvas.defaultCursor = 'default';

        // Reset internal state
        this.isDown = false;
        this.isDragging = false; // Reset dragging state
    }

    finishDrag(isEmptySpaceClick = false) {
        this.clearSnapHighlight();
        this.cleanupDrag();
        this.baseGroup.updateAllCoord(null, []);

        // Call the appropriate onMove method for special object types
        if (this.baseGroup.functionalType === 'MainRoad' && typeof this.baseGroup.onMove === 'function') {
            this.baseGroup.onMove();
        } else if (this.baseGroup.functionalType === 'SideRoad' && typeof this.baseGroup.onMove === 'function') {
            this.baseGroup.onMove();
        }

        _anchor_js__WEBPACK_IMPORTED_MODULE_1__.globalAnchorTree.endUpdateCycle('x');
        _anchor_js__WEBPACK_IMPORTED_MODULE_1__.globalAnchorTree.endUpdateCycle('y');

        // If it's an empty space click, exit focus mode
        if (isEmptySpaceClick) {
            this.baseGroup.exitFocusMode();
        }

        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex = null;
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
    }


    restoreOriginalPosition() {
        // Clear any snap highlight
        this.clearSnapHighlight();

        // Restore original position
        this.baseGroup.set(this.originalPosition);
        this.baseGroup.setCoords();
        this.baseGroup.updateAllCoord();
        this.baseGroup.exitFocusMode()

        this.cleanupDrag();
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex = null;
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
    }

    cancelDrag(event) {
        if (event && event.key === 'Escape') {
            this.restoreOriginalPosition();
        }
    }

    cleanupDrag() {
        // Clear any snap highlight
        this.clearSnapHighlight();

        // First reset object properties
        this.isDown = false;
        this.isDragging = false; // Reset dragging flag

        // Remove event listeners using stored references
        canvas.off('mouse:move', this.handleMouseMoveRef);
        canvas.off('mouse:up', this.handleMouseUpRef);
        document.removeEventListener('keydown', this.cancelDragRef);

        // Restore default behavior
        document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent);
        canvas.defaultCursor = 'default';

        // Make sure we're no longer active
        if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex === this) {
            _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex = null;
        }
    }

    onHover() {
        this.hover = true;
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
    }

    onMouseOut() {
        this.hover = false;
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
    }
}

// Register the Tab key handler for vertex cycling
document.addEventListener('keydown', function (event) {
    // Only handle Tab key when a vertex is active and we're not in a text input
    if (event.key === 'Tab' && _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex &&
        document.activeElement.tagName !== 'INPUT' &&
        document.activeElement.tagName !== 'TEXTAREA') {

        // Prevent default Tab behavior (focus change)
        event.preventDefault();

        // Check if Shift key is also pressed
        const shiftPressed = event.shiftKey;

        // Get reference to current baseGroup and the active vertex
        const currentBaseGroup = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.baseGroup;
        const currentVertex = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.vertex;        // Handle both V and E vertices
        // Get all E vertices in the group
        const eVertices = currentBaseGroup.basePolygon.vertex.filter(v => v.label.startsWith('E'));

        // Sort them by their label number
        eVertices.sort((a, b) => {
            const aNum = parseInt(a.label.substring(1));
            const bNum = parseInt(b.label.substring(1));
            return aNum - bNum;
        });

        // Determine which vertex to activate next/previous
        let nextVertex;

        // Special handling for V vertices
        if (currentVertex.label.startsWith('V')) {
            if (shiftPressed) {
                // When Shift+Tab on a V vertex, move to the last E vertex
                nextVertex = eVertices[eVertices.length - 1];
            } else {
                // When Tab on a V vertex, find E3 or default to the first E vertex
                nextVertex = eVertices.find(v => v.label === 'E3') || eVertices[0];
            }
        } else if (currentVertex.label.startsWith('E')) {
            // Normal case for E vertices - cycle through them
            const currentIndex = eVertices.findIndex(v => v.label === currentVertex.label);

            if (shiftPressed) {
                // Calculate the previous index (cycle back to end if at the beginning)
                const prevIndex = (currentIndex - 1 + eVertices.length) % eVertices.length;
                nextVertex = eVertices[prevIndex];
            } else {
                // Calculate the next index (cycle back to 0 if at the end)
                const nextIndex = (currentIndex + 1) % eVertices.length;
                nextVertex = eVertices[nextIndex];
            }
        } else {
            // For any other vertex type, default based on Shift key
            nextVertex = shiftPressed ? eVertices[eVertices.length - 1] : eVertices[0];
        }

        // Only proceed if we have a valid next vertex
        if (nextVertex) {
            // Use the current active vertex position instead of trying to get a pointer position
            // This works because we're cycling from one vertex to another
            const currentVertexPosition = {
                x: currentVertex.x,
                y: currentVertex.y
            };

            // Only proceed if we have valid vertex positions
            if (typeof currentVertexPosition.x === 'number' && !isNaN(currentVertexPosition.x) &&
                typeof currentVertexPosition.y === 'number' && !isNaN(currentVertexPosition.y)) {

                // Clean up the current active vertex
                _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.cleanupDrag();

                // Activate the next vertex control
                const nextVertexControl = currentBaseGroup.controls[nextVertex.label];
                if (nextVertexControl) {

                    // Make it the active vertex
                    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex = nextVertexControl;
                    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.isDown = true;
                    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.isDragging = true;

                    // Store original position for possible cancellation
                    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.originalPosition = {
                        left: currentBaseGroup.left,
                        top: currentBaseGroup.top
                    };

                    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.vertexOriginalPosition = {
                        x: nextVertex.x,
                        y: nextVertex.y
                    };
                    // Store the vertex's original position
                    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.vertexOriginalPosition = {
                        x: nextVertex.x,
                        y: nextVertex.y
                    };

                    // Calculate offset from vertex to group center - ensure values are numeric
                    const offsetX = nextVertex.x - currentBaseGroup.left;
                    const offsetY = nextVertex.y - currentBaseGroup.top;

                    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.vertexOffset = {
                        x: isNaN(offsetX) ? 0 : offsetX,
                        y: isNaN(offsetY) ? 0 : offsetY
                    };

                    // Calculate the delta needed to move the new vertex to the position of the current vertex
                    // This effectively keeps the object position stable while cycling through vertices
                    const deltaX = currentVertexPosition.x - nextVertex.x;
                    const deltaY = currentVertexPosition.y - nextVertex.y;

                    // Verify we have valid deltas before moving
                    const validDeltaX = typeof deltaX === 'number' && !isNaN(deltaX);
                    const validDeltaY = typeof deltaY === 'number' && !isNaN(deltaY);

                    // Move the group so that the new active vertex is at the pointer position
                    if (!currentBaseGroup.lockMovementX && validDeltaX) {
                        currentBaseGroup.set({ left: currentBaseGroup.left + deltaX });
                    }
                    if (!currentBaseGroup.lockMovementY && validDeltaY) {
                        currentBaseGroup.set({ top: currentBaseGroup.top + deltaY });
                    }
                    try {
                        // Update coordinates for the entire group
                        currentBaseGroup.setCoords();
                        currentBaseGroup.updateAllCoord();

                        // Set up event listeners for the new active vertex
                        document.removeEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent);
                        document.addEventListener('keydown', _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.cancelDragRef);
                        canvas.on('mouse:move', _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.handleMouseMoveRef);
                        canvas.on('mouse:up', _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.handleMouseUpRef);

                        // Keep the group in focus mode
                        currentBaseGroup.enterFocusMode();

                        // Set cursor style
                        canvas.defaultCursor = 'move';

                        // Render the updated canvas
                        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
                    } catch (err) {
                        console.error("Error during vertex cycling:", err);

                        // Clean up if there was an error
                        if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex) {
                            _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.cleanupDrag();
                            _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex = null;
                        }

                        // Restore default behavior
                        document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent);
                        canvas.defaultCursor = 'default';
                        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
                    }
                }
            }
        }
    }
});




/***/ }),

/***/ "./js/preload.js":
/*!***********************!*\
  !*** ./js/preload.js ***!
  \***********************/
/***/ ((module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.a(module, async (__webpack_handle_async_dependencies__, __webpack_async_result__) => { try {
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   preload: () => (/* binding */ preload)
/* harmony export */ });
/* harmony import */ var _objects_path_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./objects/path.js */ "./js/objects/path.js");
/* harmony import */ var _sidebar_sb_draw_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./sidebar/sb-draw.js */ "./js/sidebar/sb-draw.js");
/* harmony import */ var _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./sidebar/sb-settings.js */ "./js/sidebar/sb-settings.js");
/* harmony import */ var _sidebar_sb_text_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./sidebar/sb-text.js */ "./js/sidebar/sb-text.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _sidebar_sidebar_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./sidebar/sidebar.js */ "./js/sidebar/sidebar.js");
/* harmony import */ var _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./i18n/i18n.js */ "./js/i18n/i18n.js");
/* harmony import */ var _services_firebase_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./services/firebase.js */ "./js/services/firebase.js");
var __webpack_async_dependencies__ = __webpack_handle_async_dependencies__([_services_firebase_js__WEBPACK_IMPORTED_MODULE_7__]);
_services_firebase_js__WEBPACK_IMPORTED_MODULE_7__ = (__webpack_async_dependencies__.then ? (await __webpack_async_dependencies__)() : __webpack_async_dependencies__)[0];

// Import necessary functions and modules
 // Import parseFont specifically
 // Import FormDrawAddComponent specifically
 // Import FormSettingsComponent specifically
 // Import FormTextAddComponent for initialization
 // Import DrawGrid if needed
 // Import activatePanelFromHash if needed




// --- Initialization ---
// Use an async IIFE to handle the font loading promise
async function preload() {
    try {
        console.log("Initializing application, parsing fonts...");
        // Show a loading indicator here if desired
        await (0,_objects_path_js__WEBPACK_IMPORTED_MODULE_0__.parseFont)(); // Call parseFont and wait for it to complete
        (0,_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_4__.DrawGrid)(); // Initialize grid drawing
        (0,_sidebar_sidebar_js__WEBPACK_IMPORTED_MODULE_5__.activatePanelFromHash)(); // Initialize Draw panel by default
        console.log("Fonts parsed successfully. Application ready.");
        // Hide loading indicator here
        // Any initialization code that depends on fonts being loaded can go here.        await FormSettingsComponent.loadSettings();
        await _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_2__.FormSettingsComponent.loadSettings();

        const params = new URLSearchParams(window.location.search);
        const fileId = params.get('fileId');
        let cloudSignData = null;

        if (fileId) {
            try {
                console.log(`Cloud Load triggered: File ${fileId}`);
                cloudSignData = await _services_firebase_js__WEBPACK_IMPORTED_MODULE_7__.FirebaseService.getSign(fileId);
            } catch (error) {
                console.error('Failed to load sign from cloud, falling back to local canvas state:', error);
            }
        }

        await _sidebar_sb_settings_js__WEBPACK_IMPORTED_MODULE_2__.FormSettingsComponent.loadCanvasState(cloudSignData);

        // Initialize i18n locale from saved settings (if available)
        try {
            const savedSettings = localStorage.getItem('appSettings');
            const locale = savedSettings ? (JSON.parse(savedSettings).locale || 'en') : 'en';
            _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_6__.i18n.setLocale(locale);
            _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_6__.i18n.applyTranslations(document);
        } catch (_) {
            _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_6__.i18n.setLocale('en');
            _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_6__.i18n.applyTranslations(document);
        }

        // Initialize component settings listener after all modules are loaded
        _sidebar_sb_text_js__WEBPACK_IMPORTED_MODULE_3__.FormTextAddComponent.initializeSettingsListener();
        _sidebar_sb_draw_js__WEBPACK_IMPORTED_MODULE_1__.FormDrawAddComponent.initializeSettingsListener();

        window.jsPDF = window.jspdf.jsPDF; // Ensure jsPDF is available globally if needed
        paper.install(window); // Install paper.js into the global window object
        paper.setup(new paper.Size(1, 1)); // creates a virtual canvas
        paper.view.autoUpdate = false; // disables drawing any shape automatically

        setTimeout(function () {
            document.getElementById('loading-overlay').style.display = 'none';
        }, 1000); // 3 second backup timeout



    } catch (error) {
        console.error("Failed to initialize application due to font loading error:", error);
        // Handle the error appropriately, e.g., show an error message to the user.
    }
}

 // Export the preload function for use in other modules if needed
__webpack_async_result__();
} catch(e) { __webpack_async_result__(e); } });

/***/ }),

/***/ "./js/services/firebase.js":
/*!*********************************!*\
  !*** ./js/services/firebase.js ***!
  \*********************************/
/***/ ((module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.a(module, async (__webpack_handle_async_dependencies__, __webpack_async_result__) => { try {
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   FirebaseService: () => (/* binding */ FirebaseService),
/* harmony export */   app: () => (/* binding */ app),
/* harmony export */   auth: () => (/* binding */ auth),
/* harmony export */   db: () => (/* binding */ db)
/* harmony export */ });
/* harmony import */ var https_www_gstatic_com_firebasejs_12_15_0_firebase_app_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! https://www.gstatic.com/firebasejs/12.15.0/firebase-app.js */ "https://www.gstatic.com/firebasejs/12.15.0/firebase-app.js");
/* harmony import */ var https_www_gstatic_com_firebasejs_12_15_0_firebase_auth_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! https://www.gstatic.com/firebasejs/12.15.0/firebase-auth.js */ "https://www.gstatic.com/firebasejs/12.15.0/firebase-auth.js");
/* harmony import */ var https_www_gstatic_com_firebasejs_12_15_0_firebase_firestore_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! https://www.gstatic.com/firebasejs/12.15.0/firebase-firestore.js */ "https://www.gstatic.com/firebasejs/12.15.0/firebase-firestore.js");
var __webpack_async_dependencies__ = __webpack_handle_async_dependencies__([https_www_gstatic_com_firebasejs_12_15_0_firebase_app_js__WEBPACK_IMPORTED_MODULE_0__, https_www_gstatic_com_firebasejs_12_15_0_firebase_auth_js__WEBPACK_IMPORTED_MODULE_1__, https_www_gstatic_com_firebasejs_12_15_0_firebase_firestore_js__WEBPACK_IMPORTED_MODULE_2__]);
([https_www_gstatic_com_firebasejs_12_15_0_firebase_app_js__WEBPACK_IMPORTED_MODULE_0__, https_www_gstatic_com_firebasejs_12_15_0_firebase_auth_js__WEBPACK_IMPORTED_MODULE_1__, https_www_gstatic_com_firebasejs_12_15_0_firebase_firestore_js__WEBPACK_IMPORTED_MODULE_2__] = __webpack_async_dependencies__.then ? (await __webpack_async_dependencies__)() : __webpack_async_dependencies__);
/**
 * firebase.js - Firebase Configuration and Data Service
 * Handles Authentication and Firestore interactions
 */






const ENV_KEYS = {
  apiKey: 'NEXT_PUBLIC_FIREBASE_API_KEY',
  authDomain: 'NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN',
  projectId: 'NEXT_PUBLIC_FIREBASE_PROJECT_ID',
  storageBucket: 'NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET',
  messagingSenderId: 'NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID',
  appId: 'NEXT_PUBLIC_FIREBASE_APP_ID',
  measurementId: 'NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID'
};

function readConfigValue(key) {
  if (typeof process !== 'undefined' && process.env && process.env[key]) {
    return process.env[key];
  }

  if (typeof window !== 'undefined') {
    const runtimeConfig = window.__FIREBASE_CONFIG__ || {};
    if (runtimeConfig[key]) {
      return runtimeConfig[key];
    }

    if (window[key]) {
      return window[key];
    }
  }


  if (localStorage.getItem('fb_config')) {
    const config = JSON.parse(localStorage.getItem('fb_config'));
    if (config[key]) {
      return config[key];
    }
  }


  return undefined;
}

function buildFirebaseConfig() {
  return {
    apiKey: readConfigValue(ENV_KEYS.apiKey),
    authDomain: readConfigValue(ENV_KEYS.authDomain),
    projectId: readConfigValue(ENV_KEYS.projectId),
    storageBucket: readConfigValue(ENV_KEYS.storageBucket),
    messagingSenderId: readConfigValue(ENV_KEYS.messagingSenderId),
    appId: readConfigValue(ENV_KEYS.appId),
    measurementId: readConfigValue(ENV_KEYS.measurementId)
  };
}

function isConfigValid(config) {
  return Boolean(config.apiKey && config.authDomain && config.projectId && config.appId);
}

const firebaseConfig = JSON.parse(localStorage.getItem('fb_config')) || buildFirebaseConfig();

const validConfig = isConfigValid(firebaseConfig);

const app = validConfig ? (0,https_www_gstatic_com_firebasejs_12_15_0_firebase_app_js__WEBPACK_IMPORTED_MODULE_0__.initializeApp)(firebaseConfig) : null;
const auth = app ? (0,https_www_gstatic_com_firebasejs_12_15_0_firebase_auth_js__WEBPACK_IMPORTED_MODULE_1__.getAuth)(app) : null;
const db = app ? (0,https_www_gstatic_com_firebasejs_12_15_0_firebase_firestore_js__WEBPACK_IMPORTED_MODULE_2__.getFirestore)(app) : null;
// Note: getStorage was not imported in the original file but used in the if block. 
// I will keep the pattern consistent. If getStorage is needed, it should be imported.



const FirebaseService = {
  /**
   * Gets the currently logged-in user
   * @returns {Promise<User|null>}
   */
  getCurrentUser: async () => {
    if (!auth) return null;

    if (auth.currentUser) {
      return auth.currentUser;
    }

    const token = localStorage.getItem('fb_auth_token');
    if (token) {
      try {
        const userCredential = await (0,https_www_gstatic_com_firebasejs_12_15_0_firebase_auth_js__WEBPACK_IMPORTED_MODULE_1__.signInWithCustomToken)(auth, token);
        return userCredential.user;
      } catch (error) {
        console.error('Error signing in with custom token:', error);
      }
    }

    return new Promise((resolve) => {
      const unsubscribe = auth.onAuthStateChanged((user) => {
        unsubscribe();
        resolve(user);
      });
    });
  },

  /**
   * Fetches a sign document from Firestore
   * @param {string} fileId - The ID of the sign document
   * @returns {Promise<Array|null>} The sign data array or null
   */
  async getSign(fileId) {
    try {
      if (!db || !auth) {
        throw new Error('Firebase is not initialized. Provide Firebase config before calling FirebaseService.');
      }

      const user = await this.getCurrentUser();

      // Security Check: Ensure the authenticated user matches the requested userId
      if (!user) {
        throw new Error("Unauthorized: User session does not match requested userId");
      }

      console.log(`Fetching sign ${fileId} for user ${user.uid}...`);

      //const token = localStorage.getItem('fb_auth_token');

      //const authSignIn = await auth.signInWithCustomToken(token)

      const docRef = db.collection("designs").doc(fileId);
      const docSnap = await docRef.get();

      if (docSnap.exists) {
        const data = docSnap.data();
        // Assuming the sign data is stored in a field called 'objects' or as the document itself
        return data.objects || data;
      } else {
        console.error("No such sign document found!");
        return null;
      }
    } catch (error) {
      console.error("Error fetching sign from Firestore:", error);
      throw error;
    }
  }
};

__webpack_async_result__();
} catch(e) { __webpack_async_result__(e); } });

/***/ }),

/***/ "./js/sidebar/property.js":
/*!********************************!*\
  !*** ./js/sidebar/property.js ***!
  \********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   handleClear: () => (/* binding */ handleClear),
/* harmony export */   showPropertyPanel: () => (/* binding */ showPropertyPanel)
/* harmony export */ });
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _objects_template_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../objects/template.js */ "./js/objects/template.js");
/* harmony import */ var _modal_md_font_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../modal/md-font.js */ "./js/modal/md-font.js");
/* harmony import */ var _objects_text_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../objects/text.js */ "./js/objects/text.js");
/* harmony import */ var _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../canvas/Tracker.js */ "./js/canvas/Tracker.js");
/* harmony import */ var _objects_divider_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../objects/divider.js */ "./js/objects/divider.js");
/* harmony import */ var _objects_anchor_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../objects/anchor.js */ "./js/objects/anchor.js");
/* harmony import */ var _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../i18n/i18n.js */ "./js/i18n/i18n.js");
/* harmony import */ var _objects_mainRoute_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../objects/mainRoute.js */ "./js/objects/mainRoute.js");










// Add handler for 'Property' context-menu action
const propertyMenuItem = document.getElementById('property');
const contextMenu = document.getElementById('context-menu');

propertyMenuItem.addEventListener('click', function (e) {
  e.preventDefault();
  contextMenu.style.display = 'none';
  const obj = contextMenu.selectedArrow;
  showPropertyPanel(obj);
});

// Initialize property panel based on canvas selection events
_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.on('selection:created', handleSelection);
_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.on('selection:updated', handleSelection);
_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.on('object:modified', handleSelection);
_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.on('selection:cleared', handleClear);

function handleSelection(event) {
  const panel = document.getElementById('property-panel');
  // Only update panel if it was opened via context-menu
  if (panel.style.display !== 'block') return;
  // Prefer active objects from canvas to reliably detect multi-selection
  let active = [];
  try {
    active = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.getActiveObjects ? _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.getActiveObjects() : [];
  } catch (_) { /* noop */ }
  if (active && active.length > 1) {
    showPropertyPanel(active);
    return;
  }
  const obj = event?.target || (Array.isArray(event?.selected) ? event.selected[0] : active[0]);
  if (obj) showPropertyPanel(obj);
}

function handleClear() {
  const panel = document.getElementById('property-panel');
  panel.style.display = 'none';
  panel.innerHTML = '';
}

// Function to populate and display the property panel
function showPropertyPanel(object) {
  if (!object) {
    handleClear();
    return;
  }
  const panel = document.getElementById('property-panel');
  panel.innerHTML = '';
  // Close button
  const closeBtn = document.createElement('button');
  closeBtn.className = 'property-close';
  closeBtn.innerHTML = '\u00d7';
  closeBtn.addEventListener('click', () => { panel.style.display = 'none'; });
  panel.appendChild(closeBtn);

  panel.style.display = 'block';

  // Helper: normalize display name for an object
  const getObjDisplayName = (obj, idx) => {
    const base = obj?._showName || obj?.type || `#${idx + 1}`;
    const idHint = obj?.canvasID != null ? ` (${obj.canvasID})` : '';
    return `${base}${idHint}`;
  };

  // Title (supports multi-select with dropdown)
  const title = document.createElement('div');
  title.className = 'property-title';

  const isMulti = Array.isArray(object) && object.length > 1;
  if (isMulti) {
    // Build dropdown in title: first option is Group summary
    const select = document.createElement('select');
    // Style to match single-title appearance via CSS; keep minimal inline sizing
    select.className = 'property-title-select';
    select.style.maxWidth = '80%';
    const groupOption = document.createElement('option');
    groupOption.value = 'group';
    groupOption.setAttribute('data-i18n', 'Group');
    groupOption.text = `${_i18n_i18n_js__WEBPACK_IMPORTED_MODULE_7__.i18n.t('Group')} (${object.length})`;
    select.appendChild(groupOption);
    object.forEach((obj, idx) => {
      const opt = document.createElement('option');
      // value by canvasID if available, else index
      opt.value = obj?.canvasID != null ? String(obj.canvasID) : `idx:${idx}`;
      opt.text = getObjDisplayName(obj, idx);
      opt.setAttribute('background', '#444'); // Dark background for visibility
      select.appendChild(opt);
    });
    select.value = 'group';
    select.addEventListener('change', (e) => {
      const v = e.target.value;
      if (v === 'group') {
        showPropertyPanel(object);
        return;
      }
      const chosen = object.find((o, idx) => String(o.canvasID) === v || `idx:${idx}` === v);
      if (chosen) {
        // Optionally focus this object on canvas
        try {
          if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas && _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.setActiveObject) {
            _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.discardActiveObject();
            _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.setActiveObject(chosen);
            _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.requestRenderAll && _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.requestRenderAll();
          }
        } catch (_) { /* noop */ }
        showPropertyPanel(chosen);
      }
    });
    title.appendChild(select);
  } else {
    // If we fall back to generic title, translate it; otherwise use object-specific name
    if (object && (object._showName || object.type)) {
      title.innerText = object._showName || object.type;
    } else {
      title.setAttribute('data-i18n', 'Object Properties');
      title.innerText = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_7__.i18n.t('Object Properties');
    }
  }
  panel.appendChild(title);

  const PREDEFINED_COLORS = ['black', 'white',];
  // --- Helper functions for input changes ---
  function handleNumericInputChange(e, prop, targetObject) {
    let valueChanged = false;
    let numValue;
    let oldValue; // Track old value for undo

  const isBorder = targetObject.functionalType === 'Border';
  if (prop.key === 'xHeight') {
      numValue = parseFloat(e.target.value);
      if (!isNaN(numValue) && targetObject.xHeight !== numValue) {
        oldValue = targetObject.xHeight; // Store old value
        targetObject.xHeight = numValue;
        valueChanged = true;
      }
    } else { // Covers 'left', 'top', 'rootLength', 'tipLength'
      numValue = parseInt(e.target.value, 10);

      if (prop.key === 'rootLength' && targetObject.roadType == 'Main Line') {
        // edit the routeList
        targetObject.routeList[1].length = numValue
      } else if (prop.key === 'tipLength' && targetObject.roadType == 'Main Line') {
        // edit the routeList
        targetObject.routeList[0].length = numValue
      } else if (prop.key === 'routeWidth' && targetObject.roadType == 'Main Line') {
        targetObject.routeList.forEach(route => {
          route.width = numValue; // Update width for all routes
        });
      } else if (prop.key === 'innerCornerRadius' || prop.key === 'outerCornerRadius') {
        // Handle corner radius properties for MainRoad objects
        if (!isNaN(numValue) && targetObject[prop.key] !== numValue) {
          oldValue = targetObject[prop.key]; // Store old value
          targetObject[prop.key] = numValue;
          valueChanged = true;
        }
      }

      // Check lockMovement for left/top
      if (prop.key === 'left' && targetObject.lockMovementX && !targetObject.hasOwnProperty('fixedWidth')) {
        // Do not change value if movement is locked
      } else if (prop.key === 'top' && targetObject.lockMovementY && !targetObject.hasOwnProperty('fixedHeight')) {
        // Do not change value if movement is locked
      }
      else if (!isNaN(numValue) && targetObject[prop.key] !== numValue) {
        oldValue = targetObject[prop.key]; // Store old value
        targetObject.set(prop.key, numValue);
        valueChanged = true;
      }
    }

    // Reinitialize fixed width/height of borders
    if (isBorder && (prop.key === 'fixedWidth' || prop.key === 'fixedHeight')) {
      targetObject.calcfixedBboxes();
    }
    // If user moved left/top for border with fixed dimensions, update inbbox anchor using overrides
    if (isBorder && (prop.key === 'left' || prop.key === 'top')) {
      targetObject.calcfixedBboxes();
    }

    if (valueChanged) {
      // Track the property change for undo functionality
      _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_4__.canvasTracker.track('propertyChanged', [{
        functionalType: targetObject.functionalType,
        id: targetObject.canvasID,
        propertyKey: prop.key,
        oldValue: oldValue,
        newValue: numValue,
      }]);

      if (typeof targetObject.initialize === 'function') {
        try {
          targetObject.removeAll();
          targetObject.initialize();
          targetObject.updateAllCoord();
          if (targetObject.functionalType === 'Border') {
            targetObject.processResize();
          }
        } catch (initError) {
          console.error(`Error calling ${targetObject.type}.initialize() for ${prop.key} change:`, initError);
        }
      }
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
      _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_4__.canvasTracker.isDragging = false; // Reset dragging state
      showPropertyPanel(targetObject); // Refresh panel
    }
  }
  // Group-edit helpers for multi-select
  function isBorderType(obj) {
    return obj && obj.functionalType === 'Border';
  }
  function isDividerType(obj) {
    const ft = obj && obj.functionalType;
    return ft === 'HDivider' || ft === 'VDivider' || ft === 'VLane' || ft === 'HLine' || (typeof ft === 'string' && ft.includes('Divider'));
  }
  function rebuildObject(obj, changedKey) {
    if (typeof obj.initialize === 'function') {
      try {
        obj.removeAll();
        obj.initialize();
        obj.updateAllCoord();
        if (obj.functionalType === 'Border' && (changedKey === 'color' || changedKey === 'fill' || changedKey === 'fixedWidth' || changedKey === 'fixedHeight')) {
          obj.processResize();
        }
      } catch (err) {
        console.error(`Error rebuilding ${obj.type} after ${changedKey} change:`, err);
      }
    }
  }
  function handleGroupNumericChange(e, prop, selectedObjects) {
    const val = parseFloat(e.target.value);
    if (isNaN(val)) return;
    const changes = [];
    selectedObjects.forEach(o => {
      if (!o) return;
      if (prop.key === 'xHeight' && o.hasOwnProperty('xHeight')) {
        if (o.xHeight !== val) {
          const oldValue = o.xHeight;
          o.xHeight = val;
          changes.push({ functionalType: o.functionalType, id: o.canvasID, propertyKey: prop.key, oldValue, newValue: val });
          rebuildObject(o, prop.key);
        }
      }
    });
    if (changes.length) {
      try { _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_4__.canvasTracker.track('propertyChanged', changes); } catch (_) { }
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
      _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_4__.canvasTracker.isDragging = false;
      showPropertyPanel(selectedObjects);
    }
  }
  function handleGroupSelectChange(e, prop, selectedObjects) {
    const newValue = e.target.value;
    const changes = [];
    selectedObjects.forEach(o => {
      if (!o) return;
      if (prop.key === 'color') {
        // Skip Border and Divider types for color
        if (isBorderType(o) || isDividerType(o)) return;
        if (o.hasOwnProperty('color') && o.color !== newValue) {
          const oldValue = o.color;
          o.color = newValue;
          changes.push({ functionalType: o.functionalType, id: o.canvasID, propertyKey: prop.key, oldValue, newValue });
          rebuildObject(o, prop.key);
        }
      }
    });
    if (changes.length) {
      try { _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_4__.canvasTracker.track('propertyChanged', changes); } catch (_) { }
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
      _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_4__.canvasTracker.isDragging = false;
      showPropertyPanel(selectedObjects);
    }
  }
  function handleTextInputChange(e, prop, targetObject) {
    const newValue = e.target.value;
    if (targetObject[prop.key] !== newValue) {
      const oldValue = targetObject[prop.key]; // Store old value for tracking

      // Track the property change for undo functionality
      _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_4__.canvasTracker.track('propertyChanged', [{
        functionalType: targetObject.functionalType,
        id: targetObject.canvasID,
        propertyKey: prop.key,
        oldValue: oldValue,
        newValue: newValue,
      }]);

      targetObject.set(prop.key, newValue);
      if (targetObject.functionalType === 'Text' && prop.key === 'text') {
        targetObject._showName = newValue;
      }
      try {
        targetObject.updateText(newValue,targetObject.xHeight,targetObject.font,targetObject.color);
        //targetObject.removeAll();
        //targetObject.initialize();
        //targetObject.updateAllCoord();
      } catch (initError) {
        console.error(`Error calling ${targetObject.type}.initialize() for ${prop.key} change:`, initError);
      }
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
      _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_4__.canvasTracker.isDragging = false; // Reset dragging state
      showPropertyPanel(targetObject); // Refresh panel
    }
  }
  function handleSelectInputChange(e, prop, targetObject) {
    const newValue = e.target.value;
    let valueToSet = newValue;
    let valueChanged = false;
    let oldValue; // Store old value for tracking

    // Special handling: Text underline toggle
    if (prop.key === 'underline' && targetObject.functionalType === 'Text') {
      const wantUnderline = newValue === 'Yes';
      const currentlyUnderlined = !!targetObject.underline;
      if (wantUnderline === currentlyUnderlined) return; // No change

      oldValue = currentlyUnderlined ? 'Yes' : 'No';
      try {
        if (wantUnderline) {
          const underlineObject = new _objects_divider_js__WEBPACK_IMPORTED_MODULE_5__.DividerObject({
            xHeight: targetObject.xHeight,
            color: targetObject.color,
            dividerType: 'HLine',
            textObject: targetObject,
            borderGroup: null,
          });
          underlineObject.isTemporary = true;
          // Anchor underline to text: V1 of text to E6 of underline, gap = xHeight/4
          (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_6__.anchorShape)(targetObject, underlineObject, {
            vertexIndex1: 'V1',
            vertexIndex2: 'E6',
            spacingX: 0,
            spacingY: targetObject.xHeight / 4,
          });
          targetObject.underline = underlineObject;
          if (targetObject.borderGroup) {
            const border = targetObject.borderGroup;
            border.HDivider.push(underlineObject);
            border.heightObjects.push(underlineObject);
            underlineObject.borderGroup = border;
          }
        } else {
          if (targetObject.underline && typeof targetObject.underline.deleteObject === 'function') {
            const u = targetObject.underline;
            targetObject.underline = null;
            u.deleteObject(null, u);
          } else {
            targetObject.underline = null;
          }
        }
        valueChanged = true;
      } catch (err) {
        console.error('Failed to toggle underline:', err);
      }

    }
    else if (prop.key === 'color' || prop.key === 'fill') {
      if (targetObject[prop.key] !== newValue) {
        oldValue = targetObject[prop.key]; // Store old value
        targetObject[prop.key] = newValue; // Direct assignment for color/fill
        valueChanged = true;
      }
    } else if (prop.key === 'font') {
      if (targetObject[prop.key] !== newValue) {
        oldValue = targetObject[prop.key]; // Store old value
        targetObject.set(prop.key, newValue);
        valueChanged = true;
      }
    } else if (prop.key === 'symbolAngle' || prop.key === 'mainAngle') {
      valueToSet = parseInt(newValue, 10);
      if (targetObject[prop.key] !== valueToSet) {
        oldValue = targetObject[prop.key]; // Store old value
        targetObject.set(prop.key, valueToSet);
        valueChanged = true;
      }
    } else if (targetObject.functionalType === 'SideRoad' && (prop.key === 'shape' || prop.key === 'angle')) {
      if (prop.key === 'angle') {
        valueToSet = parseInt(newValue, 10);
      }
      // Specific update logic for SideRoad shape and angle
      if (targetObject.routeList && targetObject.routeList[0] && targetObject.routeList[0][prop.key] !== valueToSet) {
        oldValue = targetObject.routeList[0][prop.key]; // Store old value
        targetObject.routeList[0][prop.key] = valueToSet;
        valueChanged = true;
      }
    }

    if (valueChanged) {
      // Track the property change for undo functionality
      _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_4__.canvasTracker.track('propertyChanged', [{
        functionalType: targetObject.functionalType,
        id: targetObject.canvasID,
        propertyKey: prop.key,
        oldValue: oldValue,
        newValue: valueToSet,
      }]);

      if (typeof targetObject.initialize === 'function') {
        try {
          targetObject.removeAll();
          targetObject.initialize();
          targetObject.updateAllCoord();
          if (targetObject.functionalType === 'Border' && (prop.key === 'color' || prop.key === 'fill')) {
            targetObject.processResize();
          }
        } catch (initError) {
          console.error(`Error calling ${targetObject.type}.initialize() for ${prop.key} change:`, initError);
        }
      }
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
      _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_4__.canvasTracker.isDragging = false; // Reset dragging state
      showPropertyPanel(targetObject); // Refresh panel
    }
  }
  // --- End of helper functions ---

  // Helper to render a category box
  function renderCategory(name, props, targetObject) { // targetObject can be undefined or an array for group summaries
    if (!props.length) return;
    const box = document.createElement('div');
    box.className = 'input-group-container';
    const header = document.createElement('div');
    header.className = 'property-title';
    header.setAttribute('data-i18n', name);
    header.innerText = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_7__.i18n.t(name);
    box.appendChild(header);

    props.forEach(prop => {
      const item = document.createElement('div');
      item.className = 'property-item';
      // Align rows in a two-column grid where the control/value hugs the right edge
      item.style.display = 'grid';
      item.style.gridTemplateColumns = '1fr auto';
      item.style.columnGap = '8px';
      item.style.alignItems = 'center';

      const labelSpan = document.createElement('span');
      labelSpan.style.minWidth = '0';
      labelSpan.style.justifySelf = 'start';
      labelSpan.style.textAlign = 'left';
      // Separate i18n label text from colon so colon isn't overridden by i18n.applyTranslations
      const labelTextSpan = document.createElement('span');
      labelTextSpan.setAttribute('data-i18n', prop.label);
      labelTextSpan.innerText = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_7__.i18n.t(prop.label);
      const colonNode = document.createTextNode(':');
      labelSpan.appendChild(labelTextSpan);
      labelSpan.appendChild(colonNode);
      item.appendChild(labelSpan);

      if (prop.editable && targetObject && !Array.isArray(targetObject)) {
        let inputElement;
        if (prop.type === 'number') {
          inputElement = document.createElement('input');
          inputElement.type = 'number';
          const currentValue = targetObject[prop.key];
          if (prop.key === 'xHeight') {
            inputElement.value = (currentValue !== undefined ? parseFloat(currentValue) : 0).toFixed(0);
            inputElement.step = prop.step || '5';
          } else {
            inputElement.value = Math.round(currentValue !== undefined ? parseFloat(currentValue) : 0);
            inputElement.step = prop.step || '1';
          }
          inputElement.style.width = '80px';
          inputElement.style.maxWidth = '100%';
          inputElement.style.justifySelf = 'end';
          inputElement.addEventListener('change', (e) => {
            handleNumericInputChange(e, prop, targetObject);
          });
        } else if (prop.type === 'text') { // Added handler for text input
          inputElement = document.createElement('input');
          inputElement.type = 'text';
          inputElement.value = targetObject[prop.key] || '';
          inputElement.style.width = '200px';
          inputElement.style.maxWidth = '100%';
          inputElement.style.justifySelf = 'end';
          inputElement.addEventListener('change', (e) => {
            handleTextInputChange(e, prop, targetObject);
          });
        } else if (prop.type === 'select') {
          inputElement = document.createElement('select');
          inputElement.style.width = '200px';
          inputElement.style.maxWidth = '100%';
          inputElement.style.justifySelf = 'end';
          prop.options.forEach(opt => { // opt is a color/name e.g. 'Primary', 'white'
            const option = document.createElement('option');
            option.value = opt;
            // Translate option text when possible
            if (typeof opt === 'string') {
              option.setAttribute('data-i18n', opt);
              option.text = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_7__.i18n.t(opt);
            } else {
              option.text = String(opt);
            }
            inputElement.appendChild(option);
          });

          // inputElement.value = prop.value; // prop.value is the initial color name to select // Original line
          let valueToSet = prop.value; // Default to original prop.value
          if (typeof prop.value === 'string') {
            // Find an option that matches prop.value case-insensitively
            const matchedOption = prop.options.find(opt =>
              typeof opt === 'string' && opt.toLowerCase() === prop.value.toLowerCase()
            );
            if (matchedOption !== undefined) {
              valueToSet = matchedOption; // Use the casing from the option
            }
          }
          inputElement.value = valueToSet;

          inputElement.addEventListener('change', (e) => {
            handleSelectInputChange(e, prop, targetObject);
          });
        } else if (prop.type === 'select' && (prop.key === 'font' || prop.key === 'symbolAngle')) {
          inputElement = document.createElement('select');
          prop.options.forEach(opt => {
            const option = document.createElement('option');
            // Handle both string options and {value, label} objects
            if (typeof opt === 'object' && opt.value !== undefined) {
              option.value = opt.value;
              option.text = opt.label || opt.value;
            } else {
              option.value = opt;
              option.text = opt;
            }
            inputElement.appendChild(option);
          });
          inputElement.value = targetObject[prop.key]; // Current value

          inputElement.addEventListener('change', (e) => {
            handleSelectInputChange(e, prop, targetObject);
          });
        }
        if (inputElement) {
          item.appendChild(inputElement);
        }
      } else if (prop.editable && Array.isArray(targetObject)) {
        // Group-editable (multi-select) controls
        let inputElement;
        if (prop.type === 'number') {
          inputElement = document.createElement('input');
          inputElement.type = 'number';
          if (prop.value === 'varies') {
            inputElement.placeholder = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_7__.i18n.t('varies');
          } else {
            inputElement.value = (prop.value !== undefined ? parseFloat(prop.value) : 0).toFixed(0);
          }
          inputElement.step = prop.step || '1';
          inputElement.style.width = '80px';
          inputElement.style.maxWidth = '100%';
          inputElement.style.justifySelf = 'end';
          inputElement.addEventListener('change', (e) => {
            handleGroupNumericChange(e, prop, targetObject);
          });
        } else if (prop.type === 'select') {
          inputElement = document.createElement('select');
          inputElement.style.width = '200px';
          inputElement.style.maxWidth = '100%';
          inputElement.style.justifySelf = 'end';
          // If varies, include a disabled placeholder first option
          const hasVaries = prop.value === 'varies';
          if (hasVaries) {
            const placeholder = document.createElement('option');
            placeholder.value = '';
            placeholder.disabled = true;
            placeholder.selected = true;
            placeholder.setAttribute('data-i18n', 'varies');
            placeholder.text = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_7__.i18n.t('varies');
            inputElement.appendChild(placeholder);
          }
          (prop.options || []).forEach(opt => {
            const option = document.createElement('option');
            option.value = opt;
            option.setAttribute('data-i18n', opt);
            option.text = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_7__.i18n.t(opt);
            inputElement.appendChild(option);
          });
          if (!hasVaries) inputElement.value = prop.value;
          inputElement.addEventListener('change', (e) => {
            handleGroupSelectChange(e, prop, targetObject);
          });
        }
        if (inputElement) {
          item.appendChild(inputElement);
        }
      } else {
        const valueSpan = document.createElement('span');
        let displayValue = prop.value; // prop.value is now guaranteed for geometry & basic
        valueSpan.style.justifySelf = 'end';
        valueSpan.style.textAlign = 'right';
        valueSpan.style.maxWidth = '100%';

        // Handle color name conversion for display
        if (typeof displayValue === 'string' && (prop.label === 'Color' || prop.label === 'Fill Color')) {
          if (displayValue === '#ffffff') {
            displayValue = 'white';
          } else if (displayValue === '#000000') {
            displayValue = 'black';
          }
        }

        // Attempt to parse as float for formatting
        const numericValue = parseFloat(displayValue);

        // Check if it's a number or a string that purely represents a number
        if (!isNaN(numericValue) && typeof displayValue !== 'boolean' && (typeof displayValue === 'number' || (typeof displayValue === 'string' && /^-?\d+(\.\d+)?$/.test(displayValue.trim())))) {
          valueSpan.innerText = numericValue.toFixed(1);
        } else {
          // Otherwise, display as is (e.g., color names, text, boolean values)
          if (typeof displayValue === 'string') {
            valueSpan.setAttribute('data-i18n', displayValue);
            valueSpan.innerText = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_7__.i18n.t(displayValue);
          } else {
            valueSpan.innerText = displayValue;
          }
        }
        item.appendChild(valueSpan);
      }
      box.appendChild(item);
    });
    panel.appendChild(box);
  }

  // Helpers for bounds and common/basic comparisons
  const getBounds = (obj) => {
    const left = obj.left || 0;
    const top = obj.top || 0;
    const w = Math.round((obj.width || 0) * (obj.scaleX || 1));
    const h = Math.round((obj.height || 0) * (obj.scaleY || 1));
    return { left, top, right: left + w, bottom: top + h, width: w, height: h };
  };
  const normColor = (val, isBorderRel) => {
    if (!val) return val;
    if (isBorderRel) return val; // Border-related use color scheme names
    if (val === '#ffffff') return 'white';
    if (val === '#000000') return 'black';
    return val;
  };

  // Prepare Geometry and Basic properties depending on single vs multi
  let geometryProps = [];
  let basicProps = [];

  const isMultiSelect = Array.isArray(object) && object.length > 1;
  if (isMultiSelect) {
    // Geometry: show Left, Top, Width, Height (display-only; values are group bounds)
    let minL = Infinity, minT = Infinity, maxR = -Infinity, maxB = -Infinity;
    object.forEach(o => {
      const b = getBounds(o);
      if (b.left < minL) minL = b.left;
      if (b.top < minT) minT = b.top;
      if (b.right > maxR) maxR = b.right;
      if (b.bottom > maxB) maxB = b.bottom;
    });
    geometryProps.push({ label: 'Left (geom)', value: Math.round(minL) });
    geometryProps.push({ label: 'Top (geom)', value: Math.round(minT) });
    geometryProps.push({ label: 'Right (geom)', value: Math.round(maxR) });
    geometryProps.push({ label: 'Bottom (geom)', value: Math.round(maxB) });
    geometryProps.push({ label: 'Width (geom)', value: Math.round(maxR - minL) });
    geometryProps.push({ label: 'Height (geom)', value: Math.round(maxB - minT) });

    // Basic: editable in group; if all same, show value; else show 'varies'
    const allHaveXHeight = object.every(o => o.hasOwnProperty('xHeight'));
    if (allHaveXHeight) {
      const first = object[0].xHeight;
      const same = object.every(o => o.xHeight === first);
      basicProps.push({ label: 'x Height', key: 'xHeight', type: 'number', editable: true, step: 0.1, value: same ? first : 'varies' });
    }

    const allHaveColor = object.every(o => o.hasOwnProperty('color'));
    if (allHaveColor) {
      // Determine if all are border-related types
      const isBorderRel = object.every(o => (
        o.functionalType === 'Border' ||
        o.functionalType === 'HDivider' ||
        o.functionalType === 'VDivider' ||
        o.functionalType === 'VLane' ||
        o.functionalType === 'HLine'
      ));
      const first = normColor(object[0].color, isBorderRel);
      const same = object.every(o => normColor(o.color, isBorderRel) === first);
      // For group editing, provide a select for color, but applying will skip Border/Divider objects
      basicProps.push({ label: 'Color', key: 'color', type: 'select', options: PREDEFINED_COLORS, editable: true, value: same ? first : 'varies' });
    }
  } else {
    // Single object path (existing behavior)
    const obj = object;

    // Prepare Geometry properties
    const isNonMovable = obj.functionalType === 'Border' || obj.functionalType === 'HDivider' || obj.functionalType === 'VDivider' || obj.functionalType === 'VLane' || obj.functionalType === 'HLine';
    const hasEditableFixedWidth = obj.functionalType === 'Border' && obj.hasOwnProperty('fixedWidth') && obj.fixedWidth != null;
    const hasEditableFixedHeight = obj.functionalType === 'Border' && obj.hasOwnProperty('fixedHeight') && obj.fixedHeight != null;

    // Left
    geometryProps.push({
      label: 'Left (geom)',
      key: 'left',
      type: 'number',
      editable: (hasEditableFixedWidth && !obj.lockMovementX) || (!isNonMovable && !obj.lockMovementX),
      step: 1,
      value: obj.left
    });
    // Top remains same rules
    geometryProps.push({
      label: 'Top (geom)',
      key: 'top',
      type: 'number',
      editable: (hasEditableFixedHeight && !obj.lockMovementY) || (!isNonMovable && !obj.lockMovementY),
      step: 1,
      value: obj.top
    });
    // Right / Bottom display only
    geometryProps.push({ label: 'Right (geom)', value: Math.round(obj.left + (obj.width * (obj.scaleX || 1))) });
    geometryProps.push({ label: 'Bottom (geom)', value: Math.round(obj.top + (obj.height * (obj.scaleY || 1))) });
    // Width editable via fixedWidth if present, else display actual width
    if (hasEditableFixedWidth) {
      geometryProps.push({ label: 'Width (geom)', key: 'fixedWidth', type: 'number', editable: true, step: 1, value: obj.fixedWidth });
    } else {
      geometryProps.push({ label: 'Width (geom)', value: Math.round((obj.width || 0) * (obj.scaleX || 1)) });
    }
    // Height editable via fixedHeight if present, else display height
    if (hasEditableFixedHeight) {
      geometryProps.push({ label: 'Height (geom)', key: 'fixedHeight', type: 'number', editable: true, step: 1, value: obj.fixedHeight });
    } else {
      geometryProps.push({ label: 'Height (geom)', value: Math.round((obj.height || 0) * (obj.scaleY || 1)) });
    }

    // Prepare Basic properties (xHeight, Color)
    const isBorderRelatedType = obj.functionalType === 'Border' ||
      obj.functionalType === 'HDivider' ||
      obj.functionalType === 'VDivider' ||
      obj.functionalType === 'VLane' ||
      obj.functionalType === 'HLine';

    if (obj.hasOwnProperty('xHeight')) {
      // Use the existing translation key 'x Height'
      basicProps.push({ label: 'x Height', key: 'xHeight', type: 'number', editable: true, step: 0.1, value: obj.xHeight });
    }

    if (obj.hasOwnProperty('color')) {
      let colorOptions;
      let initialSelectValue = obj.color; // Actual color value (hex or name like 'white')

      if (isBorderRelatedType) {
        colorOptions = Object.keys(_objects_template_js__WEBPACK_IMPORTED_MODULE_1__.BorderColorScheme);
        const colorNameFromScheme = Object.keys(_objects_template_js__WEBPACK_IMPORTED_MODULE_1__.BorderColorScheme).find(name => name === obj.color);
        initialSelectValue = colorNameFromScheme || (colorOptions.length > 0 ? colorOptions[0] : obj.color);
      } else {
        colorOptions = PREDEFINED_COLORS;
        if (obj.color === '#ffffff') initialSelectValue = 'white';
        else if (obj.color === '#000000') initialSelectValue = 'black';
        // else initialSelectValue remains obj.color if not hex white/black
      }
      basicProps.push({ label: 'Color', key: 'color', type: 'select', options: colorOptions, editable: true, value: initialSelectValue });

    }
  }

  // Prepare special properties (remains display-only as per current structure)
  let specialProps = [];
  if (!isMultiSelect) switch (object.functionalType) {
    case 'Text':
      // Check if text contains non-English characters to determine appropriate font options
      const hasNonEnglish = (0,_objects_text_js__WEBPACK_IMPORTED_MODULE_3__.containsNonEnglishCharacters)(object.text);
      const fontOptions = _modal_md_font_js__WEBPACK_IMPORTED_MODULE_2__.FontPriorityManager.getAllAvailableFonts(hasNonEnglish);
      specialProps = [
        { label: 'Text', key: 'text', type: 'text', editable: true, value: object.text },
        { label: 'Font', key: 'font', type: 'select', options: fontOptions.map(f => f.value), editable: true, value: object.font },
        { label: 'Underline', key: 'underline', type: 'select', options: ['Yes', 'No'], editable: true, value: object.underline ? 'Yes' : 'No' }
      ];
      break;
    case 'Symbol':
      specialProps = [
        { label: 'Symbol Type', value: object.symbolType },
      ];
      // Determine permitted angles for the current symbolType
      const permittedAngles = _objects_template_js__WEBPACK_IMPORTED_MODULE_1__.symbolsPermittedAngle[object.symbolType];
      if (permittedAngles && permittedAngles.length > 0) {
        specialProps.push({ label: 'Angle', key: 'symbolAngle', type: 'select', options: permittedAngles, editable: true, value: object.symbolAngle });
      } else {
        // If no specific angles are defined, or if the symbolType is not in symbolsPermittedAngle,
        // default to 0 degrees and make it non-editable.
        specialProps.push({ label: 'Angle', value: 0, editable: false });
      }
      break;
    case 'MainRoad':
      specialProps = [
        { label: 'Road Type', value: object.roadType },
      ];
      // Check if this road type supports rotation
      if (_objects_template_js__WEBPACK_IMPORTED_MODULE_1__.routePermittedAngle[object.roadType]) {
        specialProps.push({ label: 'Main Angle', key: 'mainAngle', type: 'select', options: _objects_template_js__WEBPACK_IMPORTED_MODULE_1__.routePermittedAngle[object.roadType], editable: true, value: object.mainAngle || 0 });
      }

      if (object.roadType === 'Main Line') {
        specialProps.push(
          { label: 'Approach Length', key: 'rootLength', type: 'number', editable: true, step: 1, value: object.rootLength },
          { label: 'Exit Length', key: 'tipLength', type: 'number', editable: true, step: 1, value: object.tipLength },
          { label: 'Route Width', key: 'routeWidth', type: 'number', editable: true, step: 1, value: object.routeWidth },
        );

        // Add corner radius inputs for LaneDrop shape
        if (object.routeList && object.routeList[0] && object.routeList[0].shape === 'LaneDrop') {
          specialProps.push(
            { label: 'Inner Corner Radius', key: 'innerCornerRadius', type: 'number', editable: true, step: 0.1, value: object.innerCornerRadius || 1 },
            { label: 'Outer Corner Radius', key: 'outerCornerRadius', type: 'number', editable: true, step: 0.1, value: object.outerCornerRadius || 4 }
          );
        }
      } /*else {
        specialProps.push(
          { label: 'Root Length', value: object.rootLength },
          { label: 'Tip Length', value: object.tipLength }
        );
      }*/
      specialProps.push({ label: 'Side Roads', value: object.sideRoad.length });
      break;
    case 'SideRoad':
      const isBaseRoundabout = object.routeList && object.routeList[0] && object.routeList[0].shape === 'Base Roundabout';
      specialProps = [
        { label: 'Parent Road', value: object.mainRoad?.roadType || '' },
        { label: 'Branch Index', value: object.branchIndex },
      ];
      if (!isBaseRoundabout) {
        specialProps.push(
          { label: 'Shape', key: 'shape', type: 'select', options: ['Arrow', 'Stub'], editable: true, value: object.routeList[0].shape },
          { label: 'Angle', key: 'angle', type: 'select', options: [45, 60, 90], editable: true, value: object.routeList[0].angle }
        );
      }
      // Add event listener for SideRoad shape and angle directly if not covered by generic select
      // This part might be redundant if the generic select handler covers it.
      // We will rely on the handleSelectInputChange to manage SideRoad specific updates.
      break;
    case 'Border':
      specialProps = [
        { label: 'Border Type', value: object.borderType },
        { label: 'Frame Width', value: object.frame },
        { label: 'Width Objects', value: object.widthObjects.length },
        { label: 'Height Objects', value: object.heightObjects.length },
        { label: 'HDivider Count', value: object.HDivider.length },
        { label: 'VDivider Count', value: object.VDivider.length }
      ];
      if (object.bbox) {
        const b = object.bbox;
        specialProps.push({ label: 'BBox', value: `L:${Math.round(b.left)}, T:${Math.round(b.top)}, R:${Math.round(b.right)}, B:${Math.round(b.bottom)}` });
      }
      break;
  }

  // Render categories
  // Render categories
  if (isMultiSelect) {
    renderCategory('Geometry', geometryProps); // display-only
    renderCategory('Basic', basicProps, object); // group-editable for Basic
  } else {
    renderCategory('Geometry', geometryProps, object);
    renderCategory('Basic', basicProps, object);
    renderCategory(object.functionalType || 'Special', specialProps, object);
  }

  // Apply translations to the freshly built panel
  try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_7__.i18n.applyTranslations(panel); } catch (_) { }
}



// Export showPropertyPanel for context menu


/***/ }),

/***/ "./js/sidebar/sb-border.js":
/*!*********************************!*\
  !*** ./js/sidebar/sb-border.js ***!
  \*********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   FormBorderWrapComponent: () => (/* binding */ FormBorderWrapComponent)
/* harmony export */ });
/* harmony import */ var _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./sbGeneral.js */ "./js/sidebar/sbGeneral.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _objects_border_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../objects/border.js */ "./js/objects/border.js");
/* harmony import */ var _objects_divider_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../objects/divider.js */ "./js/objects/divider.js");
/* harmony import */ var _objects_template_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../objects/template.js */ "./js/objects/template.js");
/* harmony import */ var _objects_path_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../objects/path.js */ "./js/objects/path.js");
/* harmony import */ var _canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../canvas/promptBox.js */ "./js/canvas/promptBox.js");
/* harmony import */ var _utils_hintLoader_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../utils/hintLoader.js */ "./js/utils/hintLoader.js");
/* harmony import */ var _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../i18n/i18n.js */ "./js/i18n/i18n.js");
/* Border Panel */










let FormBorderWrapComponent = {
  BorderPanelInit: function () {
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.tabNum = 3;
    var parent = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.PanelInit()
    if (parent) {
      // Create a container for border parameters
      var borderParamsContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);
      const xHeightInput = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('input-xHeight', 'x Height', borderParamsContainer, _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.xHeight, FormBorderWrapComponent.handleXHeightChange, 'input', 'mm')

      // Attach x-height help icon (TPDM Table 3.2.5.1 guidance)
      try {
        const xhParent = xHeightInput.parentElement && xHeightInput.parentElement.classList.contains('input-wrapper')
          ? xHeightInput.parentElement.parentElement
          : xHeightInput.parentElement || basicParamsContainer;
        if (xhParent) {
          _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createHelpIconWithHint(xhParent, 'text/XHeight', { position: 'right' });
        }
      } catch (e) {
        console.warn('Failed to attach x-height help icon in Border panel:', e);
      }

      // Color scheme selection
  const colorOptions = Object.keys(_objects_template_js__WEBPACK_IMPORTED_MODULE_4__.BorderColorScheme).map(key => ({ value: key, label: key }));
  const colorSelect = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createSelect('input-color', 'Select Color Scheme', colorOptions, borderParamsContainer, null, FormBorderWrapComponent.handleColorChange, 'change')

      // Attach border color purpose help icon next to the color scheme label
      try {
        const colorParent = colorSelect && colorSelect.parentElement ? colorSelect.parentElement : borderParamsContainer;
        if (colorParent) {
          _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createHelpIconWithHint(colorParent, 'border/ColorPurpose', { position: 'right' });
        }
      } catch (e) {
        console.warn('Failed to attach color purpose help icon in Border panel:', e);
      }

      // (moved) Fixed width/height inputs will be added inside borderTypeContainer for better layout

  // Create a container for border actions
      var borderActionsContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);
  //GeneralHandler.createButton('input-border', 'Select Border', borderActionsContainer, 'input', FormBorderWrapComponent.BorderCreateHandler, 'click')
  // Use i18n keys that exist in dictionaries
  const stackDividerBtn = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('input-HDivider', 'Add Stack Divider', borderActionsContainer, 'input', FormBorderWrapComponent.StackDividerHandler, 'click')
  const gantryDividerBtn = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('input-VDivider', 'Add Gantry Divider', borderActionsContainer, 'input', FormBorderWrapComponent.GantryDividerHandler, 'click')
  //const gantryLineBtn = GeneralHandler.createButton('input-HLine', 'Add Gantry Line', borderActionsContainer, 'input', FormBorderWrapComponent.GantryLineHandler, 'click')
  const laneLineBtn = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('input-VLane', 'Add Lane Line', borderActionsContainer, 'input', FormBorderWrapComponent.LaneLineHandler, 'click')

      // Add tooltips to divider buttons
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createGeneralButtonTooltip(stackDividerBtn, 'divider/StackDivider', {
        position: 'right',
        showDelay: 500,
        hideDelay: 150
      });
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createGeneralButtonTooltip(gantryDividerBtn, 'divider/GantryDivider', {
        position: 'right',
        showDelay: 500,
        hideDelay: 150
      });
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createGeneralButtonTooltip(laneLineBtn, 'divider/LaneLine', {
        position: 'right',
        showDelay: 500,
        hideDelay: 150
      });

      // Create a container for border type selection with SVG buttons
  var borderTypeContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container', 'id': 'border-select-container' }, parent);
  // i18n-enabled header for border type selection
  _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createI18nNode("div", { 'class': 'placeholder' }, borderTypeContainer, 'Select Border Type', 'text');

      // Fixed width/height inputs inside the border type container, aligned in one row
      try {
        const fixedRow = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode('div', { 'class': 'input-row' }, borderTypeContainer);
        // inline styles to guarantee side-by-side layout without relying on external CSS
        fixedRow.style.display = 'flex';
        fixedRow.style.gap = '0px';
        fixedRow.style.alignItems = 'flex-end';

        const fw = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('input-fixedWidth', 'Fixed Width', fixedRow, '', null, 'input', '(optional) mm');
        const fh = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('input-fixedHeight', 'Fixed Height', fixedRow, '', null, 'input', '(optional) mm');
        // Make each input block share the row evenly
        if (fw && fw.parentElement) fw.parentElement.style.flex = '1 1 0';
        if (fh && fh.parentElement) fh.parentElement.style.flex = '1 1 0';

        // Update unit span to remove '(optional)' when a value is present
        const updateUnitSpan = (inputEl) => {
          if (!inputEl || !inputEl.parentElement) return;
          const unitSpan = inputEl.parentElement.querySelector('.input-unit');
          if (!unitSpan) return;
          const hasValue = inputEl.value && inputEl.value.toString().trim() !== '';
          unitSpan.textContent = hasValue ? 'mm' : '(optional) mm';
        };

        if (fw) {
          ['input', 'change', 'blur'].forEach(evt => fw.addEventListener(evt, () => updateUnitSpan(fw)));
          updateUnitSpan(fw);
        }
        if (fh) {
          ['input', 'change', 'blur'].forEach(evt => fh.addEventListener(evt, () => updateUnitSpan(fh)));
          updateUnitSpan(fh);
        }
      } catch (e) {
        console.warn('Failed to attach fixed width/height inputs:', e);
      }
  FormBorderWrapComponent.createBorderButtons()
  // Apply translations for all newly created elements in this panel
  try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_8__.i18n.applyTranslations(parent); } catch (_) {}
    }
  },

  // Handle xHeight change and update GeneralSettings
  handleXHeightChange: function (event) {
    const xHeight = parseInt(event.target.value);
    if (!isNaN(xHeight) && xHeight > 0) {
      // Update GeneralSettings
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.updateSetting('xHeight', xHeight);

      // Redraw the border buttons with the new xHeight
      FormBorderWrapComponent.createBorderButtons();
    }
  },

  // Handle color scheme change
  handleColorChange: function (event) {
    // Redraw the border buttons with the new color scheme
    FormBorderWrapComponent.createBorderButtons();
  },

  createBorderButtons: function () {
    const parent = document.getElementById('border-select-container');
    // Clear any existing border buttons
    const existingBorderButtons = parent.querySelectorAll('.border-container');
    if (existingBorderButtons.length > 0) {
      existingBorderButtons.forEach(button => {
        button.remove();
      });
    }

    // Create a mock bbox for the border preview
    const xHeight = 20; // Small size for the preview
    const bbox = {
      left: 0,
      top: 0,
      width: 161,
      height: 100
    };
    const bboxExit = {
      left: 0,
      top: 0,
      width: 60,
      height: 100
    };
    const bboxStreet = {
      left: 0,
      top: 0,
      width: 400,
      height: 100
    };

    // Create SVG buttons for each border type
    Object.keys(_objects_template_js__WEBPACK_IMPORTED_MODULE_4__.BorderTypeScheme).forEach(async (borderType) => {
      const shapeMeta = _objects_template_js__WEBPACK_IMPORTED_MODULE_4__.BorderTypeScheme[borderType](xHeight, borderType == 'exit' ? bboxExit : borderType.includes('Street') ? bboxStreet : bbox,);
      const svg = await FormBorderWrapComponent.createBorderSVG(shapeMeta,)
      // Create the button first
      const btn = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createSVGButton(`button-${borderType}`, svg, parent, 'border', FormBorderWrapComponent.BorderCreateHandler, 'click');

      // Map border types to their hint paths
      const hintMap = {
        panel: 'border/Panel',
        greenPanel: 'border/GreenPanel',
        stack: 'border/StackBorder',
        flagLeft: 'border/FlagBorder',
        flagRight: 'border/FlagBorder',
        exit: 'border/ExitBorder',
      };

      const hintPath = hintMap[borderType];
      if (hintPath) {
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createGeneralButtonTooltip(btn, hintPath, {
          position: 'right',
          showDelay: 500,
          hideDelay: 150
        });
      }
    });
  },

  createBorderSVG: async (shapeMeta,) => {
    const colorScheme = document.getElementById('input-color').value;
    const color = _objects_template_js__WEBPACK_IMPORTED_MODULE_4__.BorderColorScheme[colorScheme];
    let pathData = (0,_objects_path_js__WEBPACK_IMPORTED_MODULE_5__.vertexToPath)(shapeMeta, color);
    pathData = pathData.replace(/fill="border"/g, `fill="${color.border}"`);
    pathData = pathData.replace(/fill="symbol"/g, `fill="${color.symbol}"`);
    pathData = pathData.replace(/fill="background"/g, `fill="${color.background}"`);

    const svgWidth = 160;
    const svgHeight = 100;

    // Calculate the bounding box of the path  

    const result = await fabric.loadSVGFromString(pathData);
    const GroupedBorder = fabric.util.groupSVGElements(result.objects);
    let symbolSize = { width: GroupedBorder.width, height: GroupedBorder.height, left: GroupedBorder.left, top: GroupedBorder.top };

    const scaleX = svgWidth / symbolSize.width;
    const scaleY = svgHeight / symbolSize.height;
    const scale = Math.min(scaleX, scaleY);

    // Calculate the translation to center the path
    const translateX = (svgWidth - symbolSize.width * scale) / 2 - symbolSize.left * scale;
    const translateY = (svgHeight - symbolSize.height * scale) / 2 - symbolSize.top * scale;

    pathData = pathData.replace(/<svg>/g, `<svg style="width:${svgWidth};height:${svgHeight};">`)
    pathData = pathData.replace(/<path/g, `<path transform="translate(${translateX}, ${translateY}) scale(${scale})"`);
    return pathData;
  },

  BorderCreateHandler: async function (event) {
    const borderType = event.currentTarget.id.replace('button-', '');
    const xHeight = parseInt(document.getElementById("input-xHeight").value);
    const colorType = document.getElementById('input-color').value;

    // Read fixed width/height from sidebar
    const fixedWidthRaw = (document.getElementById('input-fixedWidth')?.value || '').trim();
    const fixedHeightRaw = (document.getElementById('input-fixedHeight')?.value || '').trim();
    const fixedWidth = fixedWidthRaw !== '' ? parseFloat(fixedWidthRaw) : null;
    const fixedHeight = fixedHeightRaw !== '' ? parseFloat(fixedHeightRaw) : null;

    const createBorder = (widthObjects = [], heightObjects = []) => {
      new _objects_border_js__WEBPACK_IMPORTED_MODULE_2__.BorderGroup({
        borderType: borderType,
        widthObjects: [...widthObjects],
        // heightObjects kept for legacy; mirror widthObjects if empty
        heightObjects: (heightObjects && heightObjects.length) ? [...heightObjects] : [...widthObjects],
        fixedWidth: fixedWidth,
        fixedHeight: fixedHeight,
        xHeight: xHeight,
        color: colorType,
        frame: _objects_template_js__WEBPACK_IMPORTED_MODULE_4__.BorderFrameWidth[borderType],
      });
    };

    // If both fixed values provided, skip object selection entirely
    if (fixedWidth !== null && !isNaN(fixedWidth) && fixedHeight !== null && !isNaN(fixedHeight)) {
      createBorder();
      return;
    }

    // New behavior: we only need one containment selection for both width and height.
    const needAnySelection = !(fixedWidth !== null && !isNaN(fixedWidth) && fixedHeight !== null && !isNaN(fixedHeight));
    if (needAnySelection) {
      (0,_canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_6__.selectObjectHandler)('Select shape(s) to contain inside the border', function (objects) {
        createBorder(objects, objects);
      }, null, xHeight, 'mm');
      return;
    }

    // Fallback: both fixed provided
    createBorder();
  },

  StackDividerHandler: function () {
    FormBorderWrapComponent.startDividerHoverMode('HDivider');
  },

  GantryDividerHandler: function () {
    FormBorderWrapComponent.startDividerHoverMode('VDivider');
  },

  GantryLineHandler: function () {
    FormBorderWrapComponent.startDividerHoverMode('HLine');
  },

  LaneLineHandler: function () {
    FormBorderWrapComponent.startDividerHoverMode('VLane');
  },

  // Start hover mode: user hovers a border to show compartments, then clicks to place divider
  startDividerHoverMode: function (dividerType) {
    // End any existing mode first
    FormBorderWrapComponent._exitDividerPlacementMode(true);
    const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas;
    FormBorderWrapComponent._dividerPlacementMode = { active: true, dividerType };
    // Instruction prompt (ESC to cancel)
    (0,_canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_6__.showTextBox)('Click inside the border to place divider', null, 'keydown', (e) => {
      if (e.key === 'Escape') {
        FormBorderWrapComponent._exitDividerPlacementMode();
      }
    });

    // Hover handler to detect border under pointer (no overlay)
    FormBorderWrapComponent._dividerPlacementHoverHandler = function (opt) {
      if (!FormBorderWrapComponent._dividerPlacementMode?.active) return;
      const target = opt.target;
      if (target && target.functionalType === 'Border') {
        FormBorderWrapComponent._hoveredBorder = target;
        // Show or update a subtle hover overlay over the border's inner area
        FormBorderWrapComponent._showBorderHoverOverlay(target);
      } else {
        FormBorderWrapComponent._hoveredBorder = null;
        FormBorderWrapComponent._removeBorderHoverOverlay();
      }
    };
    canvas.on('mouse:move', FormBorderWrapComponent._dividerPlacementHoverHandler);

    // Click to place divider inside the border under pointer
    FormBorderWrapComponent._dividerPlacementClickHandler = function (opt) {
      if (!FormBorderWrapComponent._dividerPlacementMode?.active) return;
      const target = opt.target;
      const border = (target && target.functionalType === 'Border') ? target : FormBorderWrapComponent._hoveredBorder;
      if (!border) return;
      border.updateBboxes();
      const pointer = canvas.getPointer(opt.e);
      const bbox = border.inbbox;
      const color = document.getElementById('input-color').value;
      // Pass the border inner bbox to the divider
      const dividerOptions = { dividerType, borderGroup: border, xHeight: border.xHeight, colorType: color, compartmentBox: bbox };
      const divider = new _objects_divider_js__WEBPACK_IMPORTED_MODULE_3__.DividerObject(dividerOptions);
      // Position divider near click point, will be clamped by assignWidthToDivider
      if (dividerType === 'HDivider' || dividerType === 'HLine') {
        divider.set({ top: pointer.y - divider.height / 2 });
        border.HDivider.push(divider)
      } else {
        divider.set({ left: pointer.x - divider.width / 2 });
        border.VDivider.push(divider)
      }
      divider.setCoords();
      // Clamp based on border inbbox using standard sizing logic
      if (border && typeof border.assignWidthToDivider === 'function') {
        border.assignWidthToDivider();
      }
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
      FormBorderWrapComponent._exitDividerPlacementMode();
    };
    canvas.on('mouse:down', FormBorderWrapComponent._dividerPlacementClickHandler);
  },

  _showCompartmentOverlay: function (border, dividerType) {
    const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas;
    // Remove existing overlay if any
    if (FormBorderWrapComponent._compartmentOverlay) {
      FormBorderWrapComponent._removeCompartmentOverlay();
    }
    const overlays = [];
    const majorColor = 'rgba(0,150,255,0.25)';
    const dimColor = 'rgba(0,150,255,0.08)';
    const strokeColor = 'rgba(0,150,255,0.5)';
    const zoom = canvas.getZoom ? canvas.getZoom() : 1;
    const strokeWidth = 1 / zoom;

    const onMouseMove = (opt) => {
      const pointer = canvas.getPointer(opt.e);
      overlays.forEach(o => {
        const b = o._metaBox;
        const inside = pointer.x >= b.left && pointer.x <= b.right && pointer.y >= b.top && pointer.y <= b.bottom;
        o.set('fill', inside ? majorColor : dimColor);
      });
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
    };

    const color = document.getElementById('input-color').value;

    const onClick = (opt) => {
      const pointer = canvas.getPointer(opt.e);
      let chosen = null;
      overlays.forEach(o => {
        const b = o._metaBox;
        if (pointer.x >= b.left && pointer.x <= b.right && pointer.y >= b.top && pointer.y <= b.bottom) {
          chosen = b;
        }
      });
      if (chosen) {
        // Remove overlay first
        FormBorderWrapComponent._removeCompartmentOverlay();
        // Capture the compartment dimensions (width/height limits) so the divider can clamp within this compartment
        const compartmentBox = {
          left: chosen.left,
          top: chosen.top,
          width: chosen.right - chosen.left,
          height: chosen.bottom - chosen.top,
          right: chosen.right,
          bottom: chosen.bottom
        };
        // Determine compartment order indices
        const columns = [...new Set(border.compartmentBboxes.map(b => b.left))].sort((a, b) => a - b);
        const rows = [...new Set(border.compartmentBboxes.map(b => b.top))].sort((a, b) => a - b);
        const compartmentColumn = columns.indexOf(chosen.left);
        const compartmentRow = rows.indexOf(chosen.top);
        const dividerOptions = { dividerType: dividerType, borderGroup: border, xHeight: border.xHeight, colorType: color, compartmentBox, compartmentColumn, compartmentRow };
        const divider = new _objects_divider_js__WEBPACK_IMPORTED_MODULE_3__.DividerObject(dividerOptions);
        // Position divider roughly at compartment center initially
        const centerX = (chosen.left + chosen.right) / 2;
        const centerY = (chosen.top + chosen.bottom) / 2;
        if (dividerType === 'HDivider' || dividerType === 'HLine') {
          divider.set({ top: centerY - divider.height / 2 });
        } else {
          divider.set({ left: centerX - divider.width / 2 });
        }
        divider.setCoords();
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
        // Exit placement mode after successful placement
        FormBorderWrapComponent._exitDividerPlacementMode();
      }
    };

    border.compartmentBboxes.forEach((b, idx) => {
      const rect = new fabric.Rect({
        left: b.left,
        top: b.top,
        width: b.right - b.left,
        height: b.bottom - b.top,
        fill: dimColor,
        stroke: strokeColor,
        strokeWidth: strokeWidth,
        selectable: false,
        evented: false,
        objectCaching: false
      });
      rect._metaBox = b;
      overlays.push(rect);
      canvas.add(rect);
    });

    FormBorderWrapComponent._compartmentOverlay = { overlays, border, onMouseMove, onClick };
    canvas.on('mouse:move', onMouseMove);
    canvas.on('mouse:down', onClick);
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
  },

  _showBorderHoverOverlay: function (border) {
    const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas;
    // Remove any existing overlay first
    if (FormBorderWrapComponent._borderHoverOverlay) {
      FormBorderWrapComponent._removeBorderHoverOverlay();
    }
    // Ensure latest inbbox
    if (!border.inbbox) {
      border.updateBboxes();
    }
    const bbox = border.inbbox;
    const zoom = canvas.getZoom ? canvas.getZoom() : 1;
    const rect = new fabric.Rect({
      left: bbox.left,
      top: bbox.top,
      width: bbox.right - bbox.left,
      height: bbox.bottom - bbox.top,
      fill: 'rgba(238, 255, 0, 0.6)',
      stroke: 'rgba(0,150,255,0.35)',
      strokeWidth: 1 / zoom,
      selectable: false,
      evented: false,
      objectCaching: false
    });
    FormBorderWrapComponent._borderHoverOverlay = { rect, border };
    canvas.add(rect);
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
  },

  _removeBorderHoverOverlay: function () {
    const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas;
    const overlay = FormBorderWrapComponent._borderHoverOverlay;
    if (!overlay) return;
    canvas.remove(overlay.rect);
    FormBorderWrapComponent._borderHoverOverlay = null;
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
  },

  _removeCompartmentOverlay: function () {
    const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas;
    const overlay = FormBorderWrapComponent._compartmentOverlay;
    if (!overlay) return;
    overlay.overlays.forEach(o => canvas.remove(o));
    canvas.off('mouse:move', overlay.onMouseMove);
    canvas.off('mouse:down', overlay.onClick);
    FormBorderWrapComponent._compartmentOverlay = null;
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
  },

  _exitDividerPlacementMode: function (silent = false) {
    const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas;
    if (FormBorderWrapComponent._dividerPlacementHoverHandler) {
      canvas.off('mouse:move', FormBorderWrapComponent._dividerPlacementHoverHandler);
      FormBorderWrapComponent._dividerPlacementHoverHandler = null;
    }
    if (FormBorderWrapComponent._dividerPlacementClickHandler) {
      canvas.off('mouse:down', FormBorderWrapComponent._dividerPlacementClickHandler);
      FormBorderWrapComponent._dividerPlacementClickHandler = null;
    }
    if (FormBorderWrapComponent._borderHoverOverlay) {
      FormBorderWrapComponent._removeBorderHoverOverlay();
    }
    if (FormBorderWrapComponent._compartmentOverlay) {
      FormBorderWrapComponent._removeCompartmentOverlay();
    }
    if (!silent) {
      (0,_canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_6__.hideTextBox)();
    }
    FormBorderWrapComponent._dividerPlacementMode = null;
  }
}

// Add listener for GeneralSettings changes
_sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.addListener(function (setting, value) {
  // Only update UI if we're in the Border panel
  if (_sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.tabNum === 3) {
    if (setting === 'xHeight') {
      const xHeightInput = document.getElementById('input-xHeight');
      if (xHeightInput && xHeightInput.value !== value.toString()) {
        xHeightInput.value = value;

        // Redraw border buttons with new xHeight
        FormBorderWrapComponent.createBorderButtons();
      }
    }
    // Note: We don't need to handle messageColor changes here as border uses its own color scheme
  }
});




/***/ }),

/***/ "./js/sidebar/sb-draw.js":
/*!*******************************!*\
  !*** ./js/sidebar/sb-draw.js ***!
  \*******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   FormDrawAddComponent: () => (/* binding */ FormDrawAddComponent)
/* harmony export */ });
/* harmony import */ var _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./sbGeneral.js */ "./js/sidebar/sbGeneral.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _objects_template_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../objects/template.js */ "./js/objects/template.js");
/* harmony import */ var _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../objects/symbols.js */ "./js/objects/symbols.js");
/* harmony import */ var _objects_path_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../objects/path.js */ "./js/objects/path.js");
/* Draw Symbol Panel */







let FormDrawAddComponent = {
  symbolAngle: 0,
  newSymbolObject: null,
  editingExistingSymbol: null, // Track when we're editing an existing symbol

  drawPanelInit: async function (e, existingSymbol = null) {
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.tabNum = 1
    var parent = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.PanelInit()
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.setActiveComponentOff(FormDrawAddComponent.DrawHandlerOff);

    // If an existing symbol was passed, set it as the editing symbol
    if (existingSymbol) {
      FormDrawAddComponent.editingExistingSymbol = existingSymbol;
      FormDrawAddComponent.symbolAngle = existingSymbol.symbolAngle;
    } else {
      FormDrawAddComponent.editingExistingSymbol = null;
      FormDrawAddComponent.symbolAngle = 0;
    }

    if (parent) {

      // Use provided defaults or fall back to GeneralSettings
      const xHeight = FormDrawAddComponent.editingExistingSymbol ?
        FormDrawAddComponent.editingExistingSymbol.xHeight :
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.xHeight;

      const color = FormDrawAddComponent.editingExistingSymbol ?
        FormDrawAddComponent.editingExistingSymbol.color.charAt(0).toUpperCase() + FormDrawAddComponent.editingExistingSymbol.color.slice(1) :
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.messageColor;

      // Create the basic parameters container using the shared function
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createBasicParamsContainer(parent, FormDrawAddComponent,
        xHeight,
        color,
        FormDrawAddComponent.handleXHeightChange, FormDrawAddComponent.handleColorChange);


      // Create a placeholder container for angle controls
      var angleControlContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'id': 'angle-control-container', 'class': 'input-group-container', 'style': 'display: none;' }, parent);

      // If we're editing an existing symbol, toggle the angle controls based on its type
      if (FormDrawAddComponent.editingExistingSymbol) {
        FormDrawAddComponent.toggleAngleControls(FormDrawAddComponent.editingExistingSymbol.symbolType);
      }

      FormDrawAddComponent.addAllSymbolsButton()
    }
  },

  // Function to update only angle and xHeight of an existing symbol
  updateExistingSymbol: function () {
    if (FormDrawAddComponent.editingExistingSymbol) {
      const xHeight = parseInt(document.getElementById('input-xHeight').value);
      const color = document.getElementById('Message Colour-container').selected.getAttribute('data-value');
      const angle = FormDrawAddComponent.symbolAngle;

      // Update the symbol with new properties, but keep the same symbol type
      FormDrawAddComponent.editingExistingSymbol.updateSymbol(
        FormDrawAddComponent.editingExistingSymbol.symbol,
        xHeight,
        color,
        angle
      );

      // Reset the editing state
      FormDrawAddComponent.editingExistingSymbol = null;
    }
  },

  addAllSymbolsButton: function () {
    const parent = document.getElementById("input-form");
    const color = document.getElementById('Message Colour-container').selected ? document.getElementById('Message Colour-container').selected.getAttribute('data-value') : 'white';

    // Clear any existing symbol containers
    const existingSymbolContainers = parent.querySelectorAll('.symbols-grid');
    if (existingSymbolContainers.length > 0) {
      existingSymbolContainers.forEach(container => {
        container.remove();
      });
    }

    const hintMapping = {
      'Route1': 'Route',
      'Route2': 'Route',
      'Route3': 'Route',
      'Route4': 'Route',
      'Route5': 'Route',
      'Route6': 'Route',
      'Route7': 'Route',
      'Route8': 'Route',
      'Route9': 'Route',
      'Route10': 'Route',
      'Route11': 'Route',
      'Route12': 'Route',
      'CHT': 'Tunnel',
      'EHC': 'Tunnel',
      'WHC': 'Tunnel',
      'JTIS': 'Tunnel',
      'JTIS-CHT': 'Tunnel',
      'JTIS-EHC': 'Tunnel',
      'JTIS-WHC': 'Tunnel',
      'MTR': 'Tunnel',
      'Hospital': 'Tunnel',
      'Disney': 'Tunnel',
      'Parking': 'Tunnel',
      'TunnelClosed': 'TunnelClosed',
      'TunnelOpen': 'TunnelClosed',
      'AmberLightAbove': 'TunnelClosed',
      'AmberLightBack': 'TunnelClosed',
      'LeftArrow': 'LaneArrow',
      'RightArrow': 'LaneArrow',
      'LeftStraightArrow': 'LaneArrow',
      'RightStraightArrow': 'LaneArrow',
      'RightPedestrian': 'LeftPedestrian',
      'LeftDisabled': 'LeftPedestrian',
      'RightDisabled': 'LeftPedestrian',
      'LeftBike': 'LeftPedestrian',
      'RightBike': 'LeftPedestrian',
      'NoEntry': 'Regulatory',
      'AllVehProhibited': 'Regulatory',
      'NoLeftTurn': 'Regulatory',
      'NoRightTurn': 'Regulatory',
      'NoUTurn': 'Regulatory',
      '2.3WidthLimit': 'Regulatory',
      '2.5WidthLimit': 'Regulatory',
      '2.7WidthLimit': 'Regulatory',
      '2.9WidthLimit': 'Regulatory',
      '2HeightLimit': 'Regulatory',
      '3HeightLimit': 'Regulatory',
      '3.5HeightLimit': 'Regulatory',
      '4HeightLimit': 'Regulatory',
      '4.1HeightLimit': 'Regulatory',
      '4.2HeightLimit': 'Regulatory',
      '4.3HeightLimit': 'Regulatory',
      '4.4HeightLimit': 'Regulatory',
      '4.5HeightLimit': 'Regulatory',
      '4.6HeightLimit': 'Regulatory',
      '4.7HeightLimit': 'Regulatory',
      '4.8HeightLimit': 'Regulatory',
    }

    // Create a container for all symbols with the proper grid layout
    const symbolsContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'symbols-grid' }, parent);

    // Add symbols to the grid - two in each row
    Object.keys(_objects_template_js__WEBPACK_IMPORTED_MODULE_2__.symbolsTemplate).forEach(async (symbol) => {
      if (symbol === 'Lozenge') return;
      const svg = await FormDrawAddComponent.createButtonSVG(symbol, 5, color);

      // Create the button
      const symbolBtn = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createSVGButton(
        `button-${symbol}`,
        svg,
        symbolsContainer,
        'symbol',
        FormDrawAddComponent.createSymbolObject,
        'click'
      );

      // Add tooltip functionality for all symbols
      // This will attempt to load hints/symbols/{symbol}.html
      const hintSymbol = Object.keys(hintMapping).includes(symbol) ? hintMapping[symbol] : symbol;
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createGeneralButtonTooltip(symbolBtn, `symbols/${hintSymbol}`, {
        position: 'right',
        showDelay: 500,  // Slightly longer delay for symbols
        hideDelay: 150
      });

      // Store the symbol type as data attribute
      symbolBtn.setAttribute('data-symbol-type', symbol);
    });
  },

  // New function to select a symbol when editing
  selectSymbolForEditing: function (event) {
    // Remove selection from all buttons
    document.querySelectorAll('.symbol-button').forEach(btn => {
      btn.classList.remove('selected');
    });

    // Add selection to clicked button
    event.currentTarget.classList.add('selected');
  },

  // Add a custom handler for color change to update SVG buttons immediately
  handleColorChange: function (event) {
    const color = event.getAttribute('data-value');

    // Update buttons immediately
    FormDrawAddComponent.addAllSymbolsButton();

    // Update the symbol with new color if one exists
    if (FormDrawAddComponent.newSymbolObject) {
      FormDrawAddComponent.updateSymbol(FormDrawAddComponent.newSymbolObject, { color: color.toLowerCase(), });
    } else if (FormDrawAddComponent.editingExistingSymbol) {
      FormDrawAddComponent.updateSymbol(FormDrawAddComponent.editingExistingSymbol, { color: color.toLowerCase(), });
    }
  },

  // Add a custom handler for color change to update SVG buttons immediately
  handleXHeightChange: function (event) {
    const xHeight = parseInt(event.target.value);

    // Update the symbol with new color if one exists
    if (FormDrawAddComponent.newSymbolObject) {
      FormDrawAddComponent.updateSymbol(FormDrawAddComponent.newSymbolObject, { xHeight: xHeight });
    } else if (FormDrawAddComponent.editingExistingSymbol) {
      FormDrawAddComponent.updateSymbol(FormDrawAddComponent.editingExistingSymbol, { xHeight: xHeight });
    }
  },

  setAngle: function (event) {
    // Store original angle to check if it changes
    const originalAngle = FormDrawAddComponent.symbolAngle;

    // Get current symbol type
    let symbolType = null;
    let currentSymbol = null;
    if (FormDrawAddComponent.newSymbolObject) {
      symbolType = FormDrawAddComponent.newSymbolObject.symbolType;
      currentSymbol = FormDrawAddComponent.newSymbolObject;
    } else if (FormDrawAddComponent.editingExistingSymbol) {
      symbolType = FormDrawAddComponent.editingExistingSymbol.symbolType;
      currentSymbol = FormDrawAddComponent.editingExistingSymbol;
    }

    // If we don't have a symbol type or permitted angles, hide the angle controls
    if (!symbolType || !_objects_template_js__WEBPACK_IMPORTED_MODULE_2__.symbolsPermittedAngle || !_objects_template_js__WEBPACK_IMPORTED_MODULE_2__.symbolsPermittedAngle[symbolType]) {
      FormDrawAddComponent.hideAngleControls();
      return;
    }

    // Use permitted angles for increments
    const permittedAngles = _objects_template_js__WEBPACK_IMPORTED_MODULE_2__.symbolsPermittedAngle[symbolType];
    if (permittedAngles.length > 1) {
      // Find the current index in the permitted angles array
      let currentIndex = permittedAngles.indexOf(FormDrawAddComponent.symbolAngle);
      if (currentIndex === -1) {
        // If current angle is not in the array, find the closest one
        let closestIndex = 0;
        let minDifference = Math.abs(permittedAngles[0] - FormDrawAddComponent.symbolAngle);

        for (let i = 1; i < permittedAngles.length; i++) {
          const difference = Math.abs(permittedAngles[i] - FormDrawAddComponent.symbolAngle);
          if (difference < minDifference) {
            minDifference = difference;
            closestIndex = i;
          }
        }

        currentIndex = closestIndex;
      }

      // Determine next index based on rotation direction
      if (event.currentTarget.id.search('left') > -1) {
        // Rotate left (counterclockwise)
        currentIndex = (currentIndex - 1 + permittedAngles.length) % permittedAngles.length;
      } else {
        // Rotate right (clockwise)
        currentIndex = (currentIndex + 1) % permittedAngles.length;
      }

      // Set the new angle
      FormDrawAddComponent.symbolAngle = permittedAngles[currentIndex];
    }

    // Update the UI regardless
    const angleDisplay = document.getElementById('angle-display');
    if (angleDisplay) {
      angleDisplay.innerText = FormDrawAddComponent.symbolAngle + '°';
    }

    // Update the symbol object only if we actually changed the angle
    if (FormDrawAddComponent.symbolAngle !== originalAngle && currentSymbol) {
      FormDrawAddComponent.updateSymbol(currentSymbol, { angle: FormDrawAddComponent.symbolAngle });
    }

  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
  },

  DrawHandlerOff: function (event) {
    // Use the shared handler for cleanup
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.genericHandlerOff(
      FormDrawAddComponent,
      'newSymbolObject',
      'SymbolOnMouseMove',
      'SymbolOnMouseClick',
      'cancelDraw'
    );

    // Additional draw-specific cleanup
    FormDrawAddComponent.hideAngleControls();
  },

  // Create a new symbol object directly instead of using cursor
  createSymbolObject: function (event) {
    // Get symbol type and parameters from the button or defaults
    const symbolType = event.currentTarget.id.replace('button-', '');
    const xHeight = parseInt(document.getElementById('input-xHeight').value);
    const color = document.getElementById('Message Colour-container').selected.getAttribute('data-value');

    // Toggle angle controls based on symbol type
    FormDrawAddComponent.toggleAngleControls(symbolType);

    // Check and adjust angle based on permitted angles
    FormDrawAddComponent.validateAndAdjustAngle(symbolType);

    // Create a function that returns a symbol object
    const createSymbol = (options) => {
      // Account for any panning that has been done
      return new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: options.type,
        position: {
          x: options.position.x,
          y: options.position.y
        },
        xHeight: options.xHeight,
        symbolAngle: options.angle,
        color: options.color
      });
    };


    // Use the general object creation with snapping function
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createObjectWithSnapping(
      {
        type: symbolType,
        xHeight: xHeight,
        color: color.toLowerCase(),
        angle: FormDrawAddComponent.symbolAngle
      },
      createSymbol,
      FormDrawAddComponent,
      'newSymbolObject',
      'E2',
      FormDrawAddComponent.SymbolOnMouseMove,
      FormDrawAddComponent.SymbolOnMouseClick,
      FormDrawAddComponent.cancelDraw
    );

  },

  // New function to validate and adjust angle based on permitted angles for the symbol
  validateAndAdjustAngle: function (symbolType) {
    // Check if there are restrictions for this symbol type
    if (_objects_template_js__WEBPACK_IMPORTED_MODULE_2__.symbolsPermittedAngle && _objects_template_js__WEBPACK_IMPORTED_MODULE_2__.symbolsPermittedAngle[symbolType]) {
      const permittedAngles = _objects_template_js__WEBPACK_IMPORTED_MODULE_2__.symbolsPermittedAngle[symbolType];

      // If current angle not in the permitted list, find closest
      if (!permittedAngles.includes(FormDrawAddComponent.symbolAngle)) {
        let closestAngle = permittedAngles[0]; // Default to first angle
        let minDifference = Math.abs(FormDrawAddComponent.symbolAngle - closestAngle);

        // Find closest permitted angle
        for (let i = 1; i < permittedAngles.length; i++) {
          const difference = Math.abs(FormDrawAddComponent.symbolAngle - permittedAngles[i]);
          if (difference < minDifference) {
            minDifference = difference;
            closestAngle = permittedAngles[i];
          }
        }

        // Update to the closest permitted angle
        FormDrawAddComponent.symbolAngle = closestAngle;

        // Update the angle display in the sidebar
        const angleDisplay = document.getElementById('angle-display');
        if (angleDisplay) {
          angleDisplay.innerText = FormDrawAddComponent.symbolAngle + '°';
        }
      }
    } else {
      // If no restrictions defined for this symbol, default to allowing only 0 degrees
      if (FormDrawAddComponent.symbolAngle !== 0) {
        FormDrawAddComponent.symbolAngle = 0;

        // Update the angle display in the sidebar
        const angleDisplay = document.getElementById('angle-display');
        if (angleDisplay) {
          angleDisplay.innerText = FormDrawAddComponent.symbolAngle + '°';
        }
      }
    }
  },

  // Helper function to update symbol properties (angle, color, xHeight)
  updateSymbol: async function (symbolObject, options = {}) {
    if (!symbolObject) return;

    const updateExisting = FormDrawAddComponent.editingExistingSymbol === symbolObject;
    const symbolType = symbolObject.symbolType;
    const xHeight = options.xHeight !== undefined ? options.xHeight : symbolObject.xHeight;
    const color = options.color !== undefined ? options.color : symbolObject.color;
    const position = {
      x: symbolObject.left,
      y: symbolObject.top
    };

    // If angle is provided, validate it against permitted values
    if (options.angle !== undefined) {
      FormDrawAddComponent.symbolAngle = options.angle;
      FormDrawAddComponent.validateAndAdjustAngle(symbolType);
    }

    // Create new symbol with updated properties
    const newOptions = {
      x: position.x,
      y: position.y,
      xHeight: xHeight,
      angle: FormDrawAddComponent.symbolAngle,
      color: color,
      symbolType: symbolType
    };

    // clear active vertex
    if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex) {
      //CanvasGlobals.activeVertex.clearSnapHighlight();
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.cleanupDrag();
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex = null;
    }

    // Update the appropriate FormDrawAddComponent reference
    if (updateExisting) {
      FormDrawAddComponent.editingExistingSymbol.updateSymbol(newOptions);
    } else {
      const newSymbolObject = FormDrawAddComponent.newSymbolObject;
      newSymbolObject.updateSymbol(newOptions);
      newSymbolObject.enterFocusMode();
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.setActiveObject(newSymbolObject);
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex = newSymbolObject.controls.E2;
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.isDown = true;
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.isDragging = true;
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.originalPosition = {
        left: newSymbolObject.left,
        top: newSymbolObject.top
      };

      // Store vertex information
      const v2 = newSymbolObject.getBasePolygonVertex('E2');
      if (v2) {
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.vertexOriginalPosition = {
          x: v2.x,
          y: v2.y
        };
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.vertexOffset = {
          x: v2.x - newSymbolObject.left,
          y: v2.y - newSymbolObject.top
        };

      }
    }

  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();

    //return newSymbolObject;
  },

  SymbolOnMouseMove: function (event) {
    // Use shared mouse move handler
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.handleObjectOnMouseMove(FormDrawAddComponent, event);
  },

  SymbolOnMouseClick: function (event) {
    if (event.e.button !== 0 && event.e.type !== 'touchend') return;

    // Use shared mouse click handler
    if (FormDrawAddComponent.newSymbolObject) {
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.handleObjectOnMouseClick(
        FormDrawAddComponent,
        event,
        'newSymbolObject',
        'SymbolOnMouseMove',
        'SymbolOnMouseClick',
        'cancelDraw'
      );

      // Symbol-specific cleanup
      FormDrawAddComponent.hideAngleControls();

      return;
    }
  },

  cancelDraw: function (event) {
    // Use shared escape key handler
    if (event.key === 'Escape') {
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.handleCancelWithEscape(
        FormDrawAddComponent,
        event,
        'newSymbolObject',
        'SymbolOnMouseMove',
        'SymbolOnMouseClick'
      );

      // Symbol-specific cleanup
      FormDrawAddComponent.hideAngleControls();
    }
  },

  createButtonSVG: async (symbolType, length, color = 'white') => {
    const symbolData = (0,_objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.calcSymbol)(symbolType, length, color);

    // Define SVG dimensions for the button
    const svgWidth = 100;
    const svgHeight = 100;
    // Create a temporary canvas to measure and render the symbol
    const tempCanvas = new fabric.StaticCanvas(null, {
      width: svgWidth,
      height: svgHeight,
      enableRetinaScaling: false
    });

    // Create temporary path objects for each path in the symbol
    const pathObjects = [];

    // Process each path in the symbol data
    symbolData.path.forEach(path => {
      // Convert vertex data to path commands
      const pathCommands = (0,_objects_path_js__WEBPACK_IMPORTED_MODULE_4__.convertVertexToPathCommands)(path);

      // Create a fabric.Path object
      const pathObj = new fabric.Path(pathCommands, {
        fill: path.fill || color.toLowerCase(),
        stroke: 'none',
        strokeWidth: 0
      });

      pathObjects.push(pathObj);
    });

    // Process text elements if present
    if (symbolData.text && symbolData.text.length > 0) {
      symbolData.text.forEach(textElem => {
        let fontGlyphs;
        switch (textElem.fontFamily) {
          case 'TransportMedium':
            fontGlyphs = _objects_path_js__WEBPACK_IMPORTED_MODULE_4__.parsedFontMedium;
            break;
          case 'TransportHeavy':
            fontGlyphs = _objects_path_js__WEBPACK_IMPORTED_MODULE_4__.parsedFontHeavy;
            break;
          default:
            fontGlyphs = _objects_path_js__WEBPACK_IMPORTED_MODULE_4__.parsedFontKorean;
        }
        // Access font metrics
        const fontMetrics = {
          unitsPerEm: fontGlyphs.unitsPerEm,
          ascender: fontGlyphs.ascender,
          descender: fontGlyphs.descender,
        };

        // Scale metrics to desired font size
        const fontScale = textElem.fontSize / fontMetrics.unitsPerEm;
        const scaledAscender = (fontMetrics.unitsPerEm - fontMetrics.ascender) * fontScale;

        const yOffset = scaledAscender;
        textElem.y = textElem.y - yOffset;
        // Check for font path
        const charPath = (0,_objects_path_js__WEBPACK_IMPORTED_MODULE_4__.getFontPath)(textElem);
        if (charPath && charPath.commands) {
          // Convert font path commands to fabric.Path format
          const pathCommands = (0,_objects_path_js__WEBPACK_IMPORTED_MODULE_4__.convertFontPathToFabricPath)(charPath.commands, textElem);
          const textPathObj = new fabric.Path(pathCommands, {
            fill: textElem.fill || color.toLowerCase(),
            stroke: 'none',
            strokeWidth: 0
          });

          pathObjects.push(textPathObj);
        }
      });
    }

    // Create a group with all paths
    const group = new fabric.Group(pathObjects, {
      left: 0,
      top: 0
    });
    // Calculate dimensions for scaling
    const bounds = group.getBoundingRect();

    // Special case handling for specific symbols
    let symbolWidth = bounds.width;
    let symbolHeight = bounds.height;

    if (symbolType === 'MTR') {
      symbolWidth = 130;
    }
    if (symbolType === 'Hospital') {
      symbolWidth = color == 'White' ? 80 : 90;
    }

    // Scale to fit within SVG dimensions
    const scaleX = svgWidth / symbolWidth;
    const scaleY = svgHeight / symbolHeight;
    const scale = Math.min(scaleX, scaleY);

    // Set the group's properties for centering and scaling
    group.set({
      left: (svgWidth - symbolWidth * scale) / 2,
      top: (svgHeight - symbolHeight * scale) / 2,
      scaleX: scale,
      scaleY: scale
    });

    // Add the group to the canvas
    tempCanvas.add(group);
    tempCanvas.renderAll();

    // Export as SVG string
    const svgString = tempCanvas.toSVG({
      width: svgWidth,
      height: svgHeight,
      viewBox: {
        x: 0,
        y: 0,
        width: svgWidth,
        height: svgHeight
      }
    });

    // Cleanup
    tempCanvas.dispose();

    return svgString;
  },

  // Toggle angle controls based on symbol type
  toggleAngleControls: function (symbolType) {
    const angleControlContainer = document.getElementById('angle-control-container');

    // If container exists
    if (angleControlContainer) {
      // Only show angle controls if this symbol has permitted angles defined
      const hasPermittedAngles = _objects_template_js__WEBPACK_IMPORTED_MODULE_2__.symbolsPermittedAngle && _objects_template_js__WEBPACK_IMPORTED_MODULE_2__.symbolsPermittedAngle[symbolType] &&
        _objects_template_js__WEBPACK_IMPORTED_MODULE_2__.symbolsPermittedAngle[symbolType].length > 1;

      if (hasPermittedAngles) {
        // Clear any existing angle controls
        angleControlContainer.innerHTML = '';

        // Create a container for angle controls
        var angleContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'angle-picker-container' }, angleControlContainer);

        var btnRotateLeft = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton(`rotate-left`, '<i class="fa-solid fa-rotate-left"></i>', angleContainer, null, FormDrawAddComponent.setAngle, 'click');
        var btnRotateRight = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton(`rotate-right`, '<i class="fa-solid fa-rotate-right"></i>', angleContainer, null, FormDrawAddComponent.setAngle, 'click');

        var angleDisplay = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'id': 'angle-display', 'class': 'angle-display' }, angleContainer);
        angleDisplay.innerText = FormDrawAddComponent.symbolAngle + '°';

        // Show the angle controls
        angleControlContainer.style.display = 'block';
      } else {
        // Hide the angle controls
        angleControlContainer.style.display = 'none';

        // Reset angle to 0 for symbols with no angle options
        FormDrawAddComponent.symbolAngle = 0;
      }
    }
  },

  hideAngleControls: function () {
    const angleControlContainer = document.getElementById('angle-control-container');
    if (angleControlContainer) {
      angleControlContainer.style.display = 'none';

      // Reset angle to default when hiding controls
      FormDrawAddComponent.symbolAngle = 0;
    }
  },

  /**
   * Initialize the draw component settings listener
   * This should be called after all modules are loaded to avoid circular dependencies
   */
  initializeSettingsListener: function () {
    // Use the shared settings listener implementation
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.addListener(
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createSettingsListener(1, function (setting, value) {
        // Symbol-specific updates when settings change
        if (setting === 'xHeight') {
          if (FormDrawAddComponent.newSymbolObject || FormDrawAddComponent.editingExistingSymbol) {
            const targetSymbol = FormDrawAddComponent.newSymbolObject || FormDrawAddComponent.editingExistingSymbol;
            FormDrawAddComponent.updateSymbol(targetSymbol, { xHeight: value });
          }
        } else if (setting === 'messageColor') {
          if (FormDrawAddComponent.newSymbolObject || FormDrawAddComponent.editingExistingSymbol) {
            const targetSymbol = FormDrawAddComponent.newSymbolObject || FormDrawAddComponent.editingExistingSymbol;
            FormDrawAddComponent.updateSymbol(targetSymbol, { color: value.toLowerCase() });
          }

          // Update all symbol buttons with new color
          if (document.getElementById("input-form")) {
            FormDrawAddComponent.addAllSymbolsButton();
          }
        }
      })
    );
  }
}



/***/ }),

/***/ "./js/sidebar/sb-export.js":
/*!*********************************!*\
  !*** ./js/sidebar/sb-export.js ***!
  \*********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   FormExportComponent: () => (/* binding */ FormExportComponent)
/* harmony export */ });
/* harmony import */ var _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./sbGeneral.js */ "./js/sidebar/sbGeneral.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _objects_build_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../objects/build.js */ "./js/objects/build.js");
/* harmony import */ var _modal_md_import_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../modal/md-import.js */ "./js/modal/md-import.js");
/* harmony import */ var _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../i18n/i18n.js */ "./js/i18n/i18n.js");
/* harmony import */ var _exportUtils_export_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../exportUtils/export.js */ "./js/exportUtils/export.js");
/* harmony import */ var _version_upgrades_UpgradeManager_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../version_upgrades/UpgradeManager.js */ "./js/version_upgrades/UpgradeManager.js");
/* Export Panel */








let FormExportComponent = {
  // Export settings for canvas objects
  exportSettings: {
    filename: 'traffic-sign-export',
    quality: 1.0,
    multiplier: 1.0,
    paperSize: 'A3', // Default paper size
    // Paper sizes in mm (width, height)
    paperSizes: {
      'A0': [841, 1189],
      'A1': [594, 841],
      'A2': [420, 594],
      'A3': [297, 420],
      'A4': [210, 297],
      'A5': [148, 210],
      'Letter': [216, 279],
      'Legal': [216, 356],
      'Tabloid': [279, 432]
    }
  },

  exportPanelInit: function (parent) {
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.tabNum = 7;
    var parent = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.PanelInit()
    // Create container for export options
    const exportContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);

    // Create filename input
    const filenameInput = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('export-filename', 'Filename', exportContainer,
      FormExportComponent.exportSettings.filename, (e) => {
        FormExportComponent.exportSettings.filename = e.target.value;
      }, 'input');

    // Create quality selector for raster formats
    const qualitySelect = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createSelect('export-quality', 'Quality',
      ['1.0', '0.9', '0.8', '0.7', '0.5'], exportContainer, '1.0',
      (e) => {
        FormExportComponent.exportSettings.quality = parseFloat(e.target.value);
      });

    // Create paper size selector for PDF export
    const paperSizeOptions = Object.keys(FormExportComponent.exportSettings.paperSizes);
    const paperSizeSelect = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createSelect('export-paper-size', 'PDF Paper Size',
      paperSizeOptions, exportContainer, FormExportComponent.exportSettings.paperSize,
      (e) => {
        FormExportComponent.exportSettings.paperSize = e.target.value;
      });

    // Keep scale multiplier for PNG/SVG exports (hidden from UI for PDF export)
    const scaleInput = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('export-scale', 'Scale Multiplier (PNG/SVG)', exportContainer,
      FormExportComponent.exportSettings.multiplier, (e) => {
        FormExportComponent.exportSettings.multiplier = parseFloat(e.target.value);
      }, 'input');

    // Create toggle for including/excluding grid
  _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Include Grid', ['No', 'Yes'], exportContainer, 'No');

    // Create toggle for including/excluding background
  _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Include Background', ['No', 'Yes'], exportContainer, 'No');

    // Create export buttons
    const buttonContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'export-buttons-container' }, exportContainer);

    // PNG Export
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('export-png', 'Export as PNG', buttonContainer, 'input',
      async () => await FormExportComponent.exportToPNG(), 'click');

    // SVG Export
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('export-svg', 'Export as SVG', buttonContainer, 'input',
      async () => await FormExportComponent.exportToSVG(), 'click');

    // PDF Export
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('export-pdf', 'Export as PDF', buttonContainer, 'input',
      async () => await FormExportComponent.exportToPDF(), 'click');

    // DXF Export
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('export-dxf', 'Export as DXF (Outline Only)', buttonContainer, 'input',
      async () => await FormExportComponent.exportToDXF(), 'click');

    // JSON Export
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('export-json', 'Export as JSON', buttonContainer, 'input',
      async () => await FormExportComponent.exportCanvasToJSON(), 'click');

  // JSON Import
    const importButtonContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent); // Create a new container for import elements, child of the main parent

    const importJsonInput = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('import-json', 'Import JSON file', importButtonContainer, 'input',
      async () => await FormExportComponent.importCanvasFromJSON(), 'click'); // Changed parent to importButtonContainer    // New button for importing JSON from text
    const importJsonTextButton = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('import-json-text', 'Import JSON text', importButtonContainer, 'input',
      () => _modal_md_import_js__WEBPACK_IMPORTED_MODULE_3__.ImportManager.showImportJSONTextModal(FormExportComponent.importCanvasFromJSONText), 'click');

    try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n.applyTranslations(parent); } catch (_) {}

  },

  // Helper function to prepare canvas for export
  prepareCanvasForExport: function () {
    const includeGrid = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Include Grid-container') === 'Yes';
    const includeBackground = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Include Background-container') === 'Yes';

    // Store original canvas state
    const originalState = {
      backgroundColor: _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.backgroundColor,
      width: _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.width,
      height: _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.height,
      viewportTransform: [..._canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.viewportTransform],
      zoom: _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getZoom(),
      objects: _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvasObject
    };

    // Calculate the bounding box that contains all visible objects (excluding grid)
    const visibleObjects = originalState.objects;

    if (visibleObjects.length > 0) {
      // Find the bounds of all objects
      let minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;

      visibleObjects.forEach(obj => {
        // Get the object's bounding rect in canvas coordinates
        const rect = obj.getBoundingRect();
        if (!isNaN(rect.top) && !isNaN(rect.left)){
          minX = Math.min(minX, rect.left);
          minY = Math.min(minY, rect.top);
          maxX = Math.max(maxX, rect.left + rect.width);
          maxY = Math.max(maxY, rect.top + rect.height);
        }
      });

      // Add padding around the objects
      const padding = 20;
      minX -= padding;
      minY -= padding;
      maxX += padding;
      maxY += padding;

      // Calculate dimensions
      const width = maxX - minX;
      const height = maxY - minY;

      // Store the calculated export bounds for later use
      originalState.exportBounds = {
        left: minX,
        top: minY,
        width: width,
        height: height
      };

      // Temporarily resize canvas to fit all objects and center view
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.setDimensions({
        width: width,
        height: height
      });

      // Reset the zoom to 1 (100%)
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.setZoom(1);

      // Center the view on the objects
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.setViewportTransform([1, 0, 0, 1, -minX, -minY]);
    }

    // Set background to transparent if not including it
    if (!includeBackground) {
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.backgroundColor = 'rgba(0,0,0,0)'; // Transparent background
    }

    if (!includeGrid) {
      // Hide grid and grid-related objects
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getObjects().forEach(obj => {
        if (obj.id === 'grid') {
          obj.visible = false;
        }
      });
    }

    // Enforce correct fill colors for Text objects before export
    // This fixes an issue where colors might be reversed or incorrect in SVG export
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getObjects().forEach(obj => {
      if (obj.functionalType === 'Text') {
        let color = obj.color;
        // Normalize color names to hex
        if (color === 'White') color = '#ffffff';
        if (color === 'Black') color = '#000000';
        
        if (obj._objects) {
          obj._objects.forEach(child => {
            // Only update if it's a path (character) and not the frame
            if (child.type === 'path') {
              child.set('fill', color);
            }
          });
        }
      }
    });

  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
    return originalState;
  },

  // Helper function to restore canvas after export
  restoreCanvasAfterExport: function (originalState) {
    // Restore original canvas dimensions
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.setDimensions({
      width: originalState.width,
      height: originalState.height
    });

    // Restore original zoom and viewport transform
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.setViewportTransform(originalState.viewportTransform);

    // Restore background
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.backgroundColor = originalState.backgroundColor;

    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getObjects().forEach(obj => {
      if (obj.id === 'grid') {
        obj.visible = true;
      }
    });

  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
  },

  // Helper function to show loading overlay during export
  showLoadingOverlay: function (exportType) {
    const overlay = document.getElementById('loading-overlay');
    const loadingText = overlay.querySelector('.loading-text');
    if (loadingText) {
      loadingText.setAttribute('data-i18n', 'Exporting');
      try { loadingText.textContent = `${_i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n.t('Exporting')} ${exportType}...`; } catch (_) { loadingText.textContent = `Exporting ${exportType}...`; }
    }
    overlay.style.display = 'flex';

    // Force browser to render the overlay before continuing
    return new Promise(resolve => {
      // Use requestAnimationFrame to ensure DOM updates before continuing
      requestAnimationFrame(() => {
        // Add a small delay to ensure the overlay is visible
        setTimeout(resolve, 50);
      });
    });
  },
  // Helper function to hide loading overlay after export
  hideLoadingOverlay: function () {
    const overlay = document.getElementById('loading-overlay');
    overlay.style.display = 'none';
  },

  // Helper function to show donation overlay after export
  showDonationOverlay: function () {
    // Check if overlay already exists
    let overlay = document.getElementById('donation-overlay');

    if (!overlay) {      // Create the donation overlay if it doesn't exist
      overlay = document.createElement('div');
      overlay.id = 'donation-overlay';

      // Create the message container
      const messageContainer = document.createElement('div');
      messageContainer.className = 'donation-container';

      // Create the title
  const title = document.createElement('h2');
  title.setAttribute('data-i18n', 'Donation Thanks Title');
  title.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n && _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n.t ? _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n.t('Donation Thanks Title') : 'Thank you for using Road Sign Factory!';

      // Create the message
  const message = document.createElement('p');
  message.setAttribute('data-i18n', 'Donation Message');
  message.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n && _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n.t ? _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n.t('Donation Message') : 'If you find this tool helpful, please consider supporting its continued development and new features.';

      // Create button container
      const buttonContainer = document.createElement('div');
      buttonContainer.className = 'button-container';
      // Create the donation link with Buy Me Coffee logo
  const donateButton = document.createElement('a');
      donateButton.href = 'https://www.buymeacoffee.com/g1213123';
      donateButton.target = '_blank';
      donateButton.className = 'donate-button';

      // Add event listener to close overlay when donation link is clicked
      donateButton.addEventListener('click', function () {
        FormExportComponent.hideDonationOverlay();
      });

      overlay.addEventListener('click', function (event) {
        FormExportComponent.hideDonationOverlay();
      });

      // Add Buy Me Coffee logo
      const bmcLogo = document.createElement('img');
      bmcLogo.src = 'https://cdn.buymeacoffee.com/buttons/bmc-new-btn-logo.svg';
      bmcLogo.className = 'bmc-logo';
      bmcLogo.alt = 'Buy me a coffee';

      // Add text after the logo
      const donateText = document.createTextNode('Buy me a coffee');

      // Append logo and text to button
      donateButton.appendChild(bmcLogo);
      donateButton.appendChild(donateText);

      // Create the close button
      const closeButton = document.createElement('button');
      closeButton.textContent = 'No thanks';
      closeButton.className = 'close-button'; closeButton.onclick = function () {
        FormExportComponent.hideDonationOverlay();
      };

      // Assemble the overlay with the new button container
      messageContainer.appendChild(title);
      messageContainer.appendChild(message);
      buttonContainer.appendChild(donateButton);
      buttonContainer.appendChild(closeButton);
      messageContainer.appendChild(buttonContainer);
      overlay.appendChild(messageContainer);

      // Add to document
      document.body.appendChild(overlay);
    } else {
      overlay.style.display = 'flex';
    }
  },

  // Helper function to hide donation overlay
  hideDonationOverlay: function () {
    const overlay = document.getElementById('donation-overlay');
    if (overlay) {
      overlay.style.display = 'none';
    }
  },



  exportToPNG: async function () {
    // Show loading overlay and wait for it to render
    await FormExportComponent.showLoadingOverlay('PNG');

    try {
      const options = {
        format: 'png',
        quality: FormExportComponent.exportSettings.quality,
        multiplier: FormExportComponent.exportSettings.multiplier
      };

      // Prepare canvas for export
      const originalState = FormExportComponent.prepareCanvasForExport();

      // Generate the export
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
      const dataURL = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.toDataURL(options);

      // Restore canvas
      FormExportComponent.restoreCanvasAfterExport(originalState);

      // Create the download link
      const link = document.createElement('a');
      link.download = `${FormExportComponent.exportSettings.filename}.png`;
      link.href = dataURL;
      document.body.appendChild(link);
      link.click();
      document.body.removeChild(link);
    } finally {      // Hide loading overlay and show donation overlay
      FormExportComponent.hideLoadingOverlay();
      FormExportComponent.showDonationOverlay();
    }
  },

  exportToSVG: async function () {
    // Show loading overlay and wait for it to render
    await FormExportComponent.showLoadingOverlay('SVG');

    try {
      // Prepare canvas for export
      const originalState = FormExportComponent.prepareCanvasForExport();

      const includeBackground = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Include Background-container') === 'Yes';

      // Add a temporary background rectangle if background should be included
      if (includeBackground && originalState.exportBounds) {
        // Add a temporary background rectangle that matches the export bounds
        const bgColor = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.backgroundColor || '#ffffff';
        const bgRect = new fabric.Rect({
          left: originalState.exportBounds.left,
          top: originalState.exportBounds.top,
          width: originalState.exportBounds.width,
          height: originalState.exportBounds.height,
          fill: bgColor,
          selectable: false,
          evented: false,
          id: 'temp-export-bg'
        });

        // Insert at the bottom of the stack
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.insertAt(0, bgRect);
        originalState.tempBackgroundRect = bgRect;
      }

      // Generate the SVG data
      const svgData = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.toSVG({
        // SVG-specific options
        viewBox: {
          x: originalState.exportBounds.left,
          y: originalState.exportBounds.top,
          width: originalState.exportBounds.width,
          height: originalState.exportBounds.height
        }
      });

      // Remove temporary background rectangle if it was added
      if (originalState.tempBackgroundRect) {
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.remove(originalState.tempBackgroundRect);
      }

      // Restore canvas
      FormExportComponent.restoreCanvasAfterExport(originalState);

      // Create the download
      const blob = new Blob([svgData], { type: 'image/svg+xml' });
      const url = URL.createObjectURL(blob);
      const link = document.createElement('a');
      link.download = `${FormExportComponent.exportSettings.filename}.svg`;
      link.href = url;
      document.body.appendChild(link);
      link.click();
      document.body.removeChild(link);
      URL.revokeObjectURL(url);
    } finally {      // Hide loading overlay and show donation overlay
      FormExportComponent.hideLoadingOverlay();
      FormExportComponent.showDonationOverlay();
    }
  },

  exportToDXF: async function () {
    // Show loading overlay and wait for it to render
    await FormExportComponent.showLoadingOverlay('DXF');

    // Declare originalState outside the try block so it's accessible in finally
    let originalState;

    try {
      // Prepare canvas for export
      originalState = FormExportComponent.prepareCanvasForExport();

      // Create a new DXF document using our npm module
      const dxf = new DxfWriter();

      // Set document properties
      dxf.setUnits('Millimeters');
      dxf.addLayer('Outlines', 1, 'continuous', 'red');

      // Check if we have any objects to export
      if (!_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvasObject || _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvasObject.length === 0) {
        throw new Error('No objects available for export');
      }

      // Track the bounds to adjust coordinates later
      let minX = Infinity, minY = Infinity;
      const pathObjects = [];

      // Collect all path objects from canvasObject
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvasObject.forEach(obj => {
        try {
          (0,_exportUtils_export_js__WEBPACK_IMPORTED_MODULE_5__.collectPathObjects)(obj, pathObjects);
        } catch (error) {
          console.error(`Error collecting path objects for DXF: ${obj.id}`, error);
        }
      });

      // Process each path object for DXF export
      pathObjects.forEach(pathObj => {
        try {
          (0,_exportUtils_export_js__WEBPACK_IMPORTED_MODULE_5__.processPathForDXF)(pathObj, dxf, 0, 0);
        } catch (error) {
          console.error(`Error processing path for DXF: ${pathObj.id}`, error);
        }
      });

      // Generate the DXF content
      const dxfContent = dxf.toDxfString();

      // Create download link
      const blob = new Blob([dxfContent], { type: 'application/dxf' });
      const url = URL.createObjectURL(blob);
      const link = document.createElement('a');
      link.download = `${FormExportComponent.exportSettings.filename}.dxf`;
      link.href = url;
      document.body.appendChild(link);
      link.click();
      document.body.removeChild(link);
      URL.revokeObjectURL(url);
    } catch (error) {
      console.error("DXF export failed:", error);
      alert("DXF export failed: " + error.message);
    } finally {
      // Ensure canvas is restored even on error (only if originalState was created)
      if (originalState) {
        FormExportComponent.restoreCanvasAfterExport(originalState);
      }
      // Hide loading overlay and show donation overlay
      FormExportComponent.hideLoadingOverlay();
      FormExportComponent.showDonationOverlay();
    }
  },

  exportToPDF: async function () {
    // Show loading overlay and wait for it to render
    await FormExportComponent.showLoadingOverlay('PDF');

    try {
      // Prepare canvas for export first
      const originalState = FormExportComponent.prepareCanvasForExport();

      const includeBackground = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Include Background-container') === 'Yes';

      // Add a temporary background rectangle if background should be included
      if (includeBackground && originalState.exportBounds) {
        // Add a temporary background rectangle that matches the export bounds
        const bgColor = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.backgroundColor || '#ffffff';
        const bgRect = new fabric.Rect({
          left: originalState.exportBounds.left,
          top: originalState.exportBounds.top,
          width: originalState.exportBounds.width,
          height: originalState.exportBounds.height,
          fill: bgColor,
          selectable: false,
          evented: false,
          id: 'temp-export-bg'
        });

        // Insert at the bottom of the stack
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.insertAt(0, bgRect);
        originalState.tempBackgroundRect = bgRect;
      }

      // Use the calculated bounds for content dimensions
      const contentWidth = originalState.exportBounds ? originalState.exportBounds.width : _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.width;
      const contentHeight = originalState.exportBounds ? originalState.exportBounds.height : _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.height;

      // Get the selected paper size dimensions from settings
      const selectedPaperSize = FormExportComponent.exportSettings.paperSize;
      const [paperWidthMM, paperHeightMM] = FormExportComponent.exportSettings.paperSizes[selectedPaperSize];

      // Convert paper dimensions from mm to pixels
      const MM_TO_PX = 2.83; // Approximate conversion from mm to px at 72 dpi
      const paperWidthPx = paperWidthMM * MM_TO_PX;
      const paperHeightPx = paperHeightMM * MM_TO_PX;

      // Determine orientation based on content aspect ratio
      const contentAspectRatio = contentWidth / contentHeight;
      const paperAspectRatio = paperWidthPx / paperHeightPx;

      // Choose orientation that best fits the content
      const isLandscape = contentAspectRatio > paperAspectRatio;

      // Set PDF dimensions based on orientation
      const pdfWidth = isLandscape ? paperHeightPx : paperWidthPx;
      const pdfHeight = isLandscape ? paperWidthPx : paperHeightPx;

      // Calculate scale to fit content within the paper size while preserving aspect ratio
      const scaleX = pdfWidth / contentWidth;
      const scaleY = pdfHeight / contentHeight;
      const scale = Math.min(scaleX, scaleY) * 0.95; // Use 95% of available space to add margin

      // Calculate dimensions of the scaled content
      const scaledWidth = contentWidth * scale;
      const scaledHeight = contentHeight * scale;

      // Calculate position to center the content on the page
      const xOffset = (pdfWidth - scaledWidth) / 2;
      const yOffset = (pdfHeight - scaledHeight) / 2;

      // Create a new jsPDF instance with appropriate orientation
      const pdf = new jsPDF({
        orientation: isLandscape ? 'landscape' : 'portrait',
        unit: 'px',
        format: isLandscape ? [paperHeightPx, paperWidthPx] : [paperWidthPx, paperHeightPx],
        compress: true
      });

  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();

      // Get PNG data URL from canvas
      const dataURL = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.toDataURL({
        format: 'png',
        quality: FormExportComponent.exportSettings.quality,
        multiplier: FormExportComponent.exportSettings.multiplier
      });

      // Remove temporary background rectangle if it was added
      if (originalState.tempBackgroundRect) {
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.remove(originalState.tempBackgroundRect);
      }

      // Add the image to the PDF, centered and scaled to fit the selected paper size
      pdf.addImage(
        dataURL,
        'PNG',
        xOffset,
        yOffset,
        scaledWidth,
        scaledHeight
      );

      // Save the PDF
      pdf.save(`${FormExportComponent.exportSettings.filename}.pdf`);

      // Restore the canvas after PDF creation
      FormExportComponent.restoreCanvasAfterExport(originalState);
    } finally {
      // Hide loading overlay
      FormExportComponent.hideLoadingOverlay();
      FormExportComponent.showDonationOverlay();
    }
  },

  exportCanvasToJSON: function (download = true) {
    if (!_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas) {
      console.error("Canvas is not initialized.");
      return;
    }

    const objectsToSerialize = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvasObject;
    const serializedObjectsArray = [];

    for (const obj of objectsToSerialize) {
      if (typeof obj.serializeToJSON === 'function') {
        serializedObjectsArray.push(obj.serializeToJSON());
      }
    }

    // Prepare metadata including website title and version by reading meta tags
    const appNameMeta = document.querySelector('meta[name="application-name"]');
    const appVersionMeta = document.querySelector('meta[name="application-version"]');

    const metaData = {
      title: appNameMeta ? appNameMeta.getAttribute('content') : 'TrafficSign', // Fallback title
      version: appVersionMeta ? appVersionMeta.getAttribute('content') : '0.0.0', // Fallback version
      exportDate: new Date().toISOString()
    };

    const exportData = {
      meta: metaData,
      objects: serializedObjectsArray
    };

    const jsonString = JSON.stringify(exportData, null, 2);

    if (download) {
      // Create a Blob and download the JSON file
      const blob = new Blob([jsonString], { type: 'application/json' });
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = `${FormExportComponent.exportSettings.filename || 'canvas-export'}.json`;
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      URL.revokeObjectURL(url);
      FormExportComponent.showDonationOverlay();
    }
    return jsonString; // Return JSON string for further processing if needed
  },

  // New private helper function to process and apply JSON data to the canvas
  _applyJSONToCanvas: async function (jsonData) {
    try {
      let objectsToLoad;
      if (jsonData.meta && Array.isArray(jsonData.objects)) {
        console.log("Processing JSON with metadata:", jsonData.meta);
        objectsToLoad = jsonData.objects;
      } else {
        objectsToLoad = jsonData.objects; // Assume old format or direct array
        console.log("Processing JSON (direct array or old format).");
      }

      if (!Array.isArray(objectsToLoad)) {
        // This check can be more specific based on expected structure
        throw new Error("Provided JSON data does not resolve to an array of objects.");
      }

      // Clear existing objects (excluding the grid)
      // This assumes CanvasGlobals.canvasObject is the array of current user-added objects
      // and obj.deleteObject() correctly removes them from canvas and the array.
      if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvasObject && typeof _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvasObject.forEach === 'function') {
        // Iterate backwards if deleteObject modifies the array in place
        for (let i = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvasObject.length - 1; i >= 0; i--) {
          const obj = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvasObject[i];
          if (obj && typeof obj.deleteObject === 'function') {
            obj.deleteObject();
          } else {
            // Fallback for objects without deleteObject or if canvasObject is not what's expected
            console.warn("Object or deleteObject method not found, attempting direct canvas removal for:", obj);
            if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas && obj) _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.remove(obj);
          }
        }
      }
      // If CanvasGlobals.canvasObject is not self-managing, it might need to be reset:
      // CanvasGlobals.canvasObject = []; 
      // Or, if it's just a reference to canvas.getObjects(), then canvas.clear() might be an option,
      // but that would also remove the grid unless handled separately.
      // The current approach relies on the existing pattern of obj.deleteObject().

  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender(); // Render after clearing

      if (typeof _objects_build_js__WEBPACK_IMPORTED_MODULE_2__.buildObjectsFromJSON === 'function') {
        await (0,_objects_build_js__WEBPACK_IMPORTED_MODULE_2__.buildObjectsFromJSON)(objectsToLoad, _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas);

        const version = jsonData.meta ? jsonData.meta.version : null;
        _version_upgrades_UpgradeManager_js__WEBPACK_IMPORTED_MODULE_6__.UpgradeManager.processUpgrades(version);
        
      } else {
        console.error("buildObjectsFromJSON function is not available.");
        throw new Error("Critical function buildObjectsFromJSON is not available to reconstruct objects.");
      }
    } catch (error) {
      console.error("Error applying JSON to canvas:", error);
      throw error; // Re-throw to be handled by the caller
    }
  },
  importCanvasFromJSON: async function () {
    // Warning dialog before proceeding
    const userConfirmed = confirm(
      "Warning: Importing a JSON file will clear the current canvas and replace it with the imported content. " +
      "All current work will be lost.\n\nDo you want to proceed?"
    );

    if (!userConfirmed) {
      console.log("Import cancelled by user.");
      return;
    }

    const fileInput = document.createElement('input');
    fileInput.type = 'file';
    fileInput.accept = '.json'; // Accept only JSON files

    fileInput.addEventListener('change', async (event) => {
      const file = event.target.files[0];
      if (!file) {
        console.log("No file selected or dialog cancelled.");
        // No overlay shown yet, so no need to hide
        return;
      }

      await FormExportComponent.showLoadingOverlay('JSON file'); // Show overlay when processing starts

      const reader = new FileReader();
      reader.onload = async (e) => {
        try {
          const jsonString = e.target.result;
          const jsonData = JSON.parse(jsonString);
          await FormExportComponent._applyJSONToCanvas(jsonData);
          alert('Canvas imported successfully from file!');
        } catch (error) {
          console.error("Error importing canvas from JSON file:", error);
          alert("Failed to import from JSON file. Ensure the file is a valid JSON export from this tool. Error: " + error.message);
        } finally {
          FormExportComponent.hideLoadingOverlay();
        }
      };
      reader.onerror = () => {
        console.error("Error reading file.");
        alert("Error reading the selected file.");
        FormExportComponent.hideLoadingOverlay(); // Ensure overlay is hidden on read error
      };
      reader.readAsText(file);
    });

    fileInput.click(); // Trigger the file dialog
  },
  // Updated method to import canvas from JSON text
  importCanvasFromJSONText: async function (jsonText) {
    if (!jsonText || jsonText.trim() === '') {
      alert('JSON text cannot be empty.');
      return;
    }

    // Warning dialog before proceeding
    const userConfirmed = confirm(
      "Warning: Importing JSON text will clear the current canvas and replace it with the imported content. " +
      "All current work will be lost.\n\nDo you want to proceed?"
    );

    if (!userConfirmed) {
      console.log("Import from text cancelled by user.");
      return;
    }

    await FormExportComponent.showLoadingOverlay('JSON data'); // Show overlay

    try {
      const jsonData = JSON.parse(jsonText);
      await FormExportComponent._applyJSONToCanvas(jsonData);
      alert('JSON text imported successfully and applied to canvas!');

      const modal = document.getElementById('import-json-text-modal');
      if (modal) {
        modal.remove();
      }
    } catch (error) {
      console.error("Error importing JSON from text:", error);
      alert('Failed to import from JSON text. Please ensure the format is correct. Error: ' + error.message);
    } finally {
      FormExportComponent.hideLoadingOverlay(); // Hide overlay in all cases
    }
  },



}

// Export the FormExportComponent for use in other files


/***/ }),

/***/ "./js/sidebar/sb-info.js":
/*!*******************************!*\
  !*** ./js/sidebar/sb-info.js ***!
  \*******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   FormInfoComponent: () => (/* binding */ FormInfoComponent)
/* harmony export */ });
/* harmony import */ var _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./sbGeneral.js */ "./js/sidebar/sbGeneral.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* Info Panel */



let FormInfoComponent = {
  // Adding General settings : e.g. turn off text borders, change background color, show grid, etc.
  InfoPanelInit: function () {
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.tabNum = 9
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.PanelInit() // Call PanelInit to clear the form and set up handlers
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.setActiveComponentOff(FormInfoComponent.InfoHandlerOff); // Register the off handler

    // Find the static info content div
    const staticInfoContent = document.getElementById('info-content-static');
    if (staticInfoContent) {
      // Make the static content visible (it stays in its original place in index.html)
      staticInfoContent.style.display = 'block';
    } else {
      console.error("Static info content div (#info-content-static) not found in HTML.");
    }
  },

  // Function to hide the static info panel when switching tabs or closing panel
  InfoHandlerOff: function () {
    const staticInfoContent = document.getElementById('info-content-static');
    if (staticInfoContent) {
      staticInfoContent.style.display = 'none';
    }
  },
};

// Export the FormInfoComponent for use in other files


/***/ }),

/***/ "./js/sidebar/sb-inspector.js":
/*!************************************!*\
  !*** ./js/sidebar/sb-inspector.js ***!
  \************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   CanvasObjectInspector: () => (/* binding */ CanvasObjectInspector)
/* harmony export */ });
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _property_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./property.js */ "./js/sidebar/property.js");
/* Object Inspector Panel */



let CanvasObjectInspector = {
  createObjectListPanelInit: function () {
    const objectListPanel = document.getElementById('objectListPanel');

    // Clear the existing content
    objectListPanel.innerHTML = '';

    // Loop through the CanvasObject array and append object names to the list
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvasObject.forEach((obj, index) => {
      const div = document.createElement('div');
      div.className = 'object-list-item';
      div.innerText = obj._showName;
      div.id = `Group (${index})`;
      div.addEventListener('click', () => {
        // Remove 'selected' class from all items
        document.querySelectorAll('.object-list-item').forEach(item => item.classList.remove('selected'));
        // Add 'selected' class to the clicked item
        div.classList.add('selected');
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.setActiveObject(obj);
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
        // Scroll the parent container to the clicked item
        div.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
      });
      div.addEventListener('dblclick', () => {
        (0,_property_js__WEBPACK_IMPORTED_MODULE_1__.showPropertyPanel)(obj);
      });
      objectListPanel.appendChild(div);
    });
  },

  SetActiveObjectList: function (setActive) {
    const index = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvasObject.indexOf(setActive);
    // Remove 'selected' class from all items
    document.querySelectorAll('.object-list-item').forEach(item => {
      if (item.id == `Group (${index})`) {
        item.classList.add('selected');
        // Scroll the parent container to the active item
        item.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
      } else {
        item.classList.remove('selected');
      }
    });
  },
  
  initPanelToggle: function() {
    const heading = document.querySelector('.object-list-heading');
    const panel = document.getElementById('objectListPanel');
    const icon = document.getElementById('toggle-object-list');
    
    heading.addEventListener('click', () => {
      // Toggle the contracted class on the panel
      panel.classList.toggle('contracted');
      
      // Toggle the rotated class on the icon
      icon.classList.toggle('rotated');
      
      // Make sure the icon matches the correct state:
      // When panel is contracted (hidden) - icon should point up (rotated)
      // When panel is expanded - icon should point down (not rotated)
      if (panel.classList.contains('contracted')) {
        icon.classList.add('rotated');  // Panel is contracted, icon points up
      } else {
        icon.classList.remove('rotated'); // Panel is expanded, icon points down
      }
    });
  }
}

// Initialize the panel toggle functionality when the script loads
CanvasObjectInspector.initPanelToggle();

// Export the CanvasObjectInspector for use in other files


/***/ }),

/***/ "./js/sidebar/sb-map.js":
/*!******************************!*\
  !*** ./js/sidebar/sb-map.js ***!
  \******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   FormDrawMapComponent: () => (/* binding */ FormDrawMapComponent)
/* harmony export */ });
/* harmony import */ var _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./sbGeneral.js */ "./js/sidebar/sbGeneral.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _objects_mainRoute_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../objects/mainRoute.js */ "./js/objects/mainRoute.js");
/* harmony import */ var _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../objects/sideRoute.js */ "./js/objects/sideRoute.js");
/* harmony import */ var _objects_template_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../objects/template.js */ "./js/objects/template.js");
/* harmony import */ var _utils_hintLoader_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../utils/hintLoader.js */ "./js/utils/hintLoader.js");
/* harmony import */ var _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../i18n/i18n.js */ "./js/i18n/i18n.js");
/* Draw Map Panel */








const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas;
const canvasObject = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvasObject;

let FormDrawMapComponent = {
  MapType: ['Main Line', 'Roundabout'],
  MainEndShape: [{ value: 'Arrow', label: 'Arrow', image: 'arrow.svg' },
  { value: 'Stub', label: 'Stub', image: 'stub.svg' },
  { value: 'RedBar', label: 'RedBar', image: 'redbar.svg' },
  { value: 'LaneDrop', label: 'LaneDrop', image: 'lanedrop.svg' },
  { value: 'T-Junction', label: 'T-Junction', image: 'tjunction.svg' },
  { value: 'Y-Junction', label: 'Y-Junction', image: 'yjunction.svg' }],
  SideEndShape: ['Arrow', 'Stub', 'RedBar', 'Circular Sign', 'Circular Sign (with Arrow)'],
  RoundaboutFeatures: {
    'Conventional': [
      { value: 'Normal', label: 'Normal', image: 'roundabout-normal.svg' },
      { value: 'Auxiliary', label: 'Auxiliary', image: 'roundabout-auxiliary.svg' },
      { value: 'U-turn', label: 'U-turn', image: 'roundabout-uturn.svg' }
    ],
    'Spiral': [
      { value: 'Normal', label: 'Normal', image: 'roundabout-spiral-normal.svg' },
      { value: 'Auxiliary', label: 'Auxiliary', image: 'roundabout-spiral-auxiliary.svg' },
      { value: 'U-turn', label: 'U-turn', image: 'roundabout-spiral-uturn.svg' }
    ],
    'Oval': [{ value: 'Normal', label: 'Normal', image: 'roundabout-oval-normal.svg' }],
    'Double': [{ value: 'Conventional', label: 'Conventional', image: 'roundabout-double-normal.svg' },
    { value: 'Spiral', label: 'Spiral', image: 'roundabout-double-spiral.svg' },
    ]
  },
  permitAngle: [45, 60, 90],
  defaultRoute: [{ x: 0, y: 7, angle: 60, width: 4, shape: 'Arrow' }],
  newMapObject: null,

  /**
   * Initializes the map drawing panel with input fields and buttons
   * @return {void}
   */
  drawMapPanelInit: function () {
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.tabNum = 4
    var parent = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.PanelInit()
    if (parent) {
      parent.routeCount = 0

      // Set up button hint mappings for this panel
      _utils_hintLoader_js__WEBPACK_IMPORTED_MODULE_5__.HintLoader.setButtonHintMappings({
        'Main Road Shape-container': 'route/MainRoadShape'
      });

      // Create a container for basic parameters using the shared function
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createBasicParamsContainer(parent, FormDrawMapComponent);

      // Create a container for route parameters
      var MainRoadParamsContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);

      // Create the toggle
      const roadTypeToggle = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Main Road Type', FormDrawMapComponent.MapType, MainRoadParamsContainer, 'Main Line', FormDrawMapComponent.updateRoadTypeSettings);

      // Create a container for road type-specific settings
      const roadTypeSettingsContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'road-type-settings' }, MainRoadParamsContainer);

      // Initial setup of settings based on default selection
      FormDrawMapComponent.updateRoadTypeSettings(roadTypeSettingsContainer);

      // Create the Draw Map button
      const drawMapButton = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('button-DrawMap', 'Draw Main Road Symbol', MainRoadParamsContainer, 'input', FormDrawMapComponent.gatherMainRoadParams, 'click');

      const existingRoute = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getActiveObjects().length == 1 && _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getActiveObject.functionalType == 'MainRoute' ? _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getActiveObjects()[0] : null
      const routeList = existingRoute ? existingRoute.routeList : FormDrawMapComponent.defaultRoute

      if (routeList) {
        routeList.forEach((route, index) => {
          var SideRoadParamsContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);
          const addRouteButton = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('button-addRoute', '+ Another Route Destination', SideRoadParamsContainer, 'input', FormDrawMapComponent.addRouteInput, 'click');
          if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getActiveObjects().length == 0 || _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getActiveObject.functionalType !== 'MainRoad') {
            addRouteButton.classList.add('deactive'); // Set initial state to deactive
            addRouteButton.disabled = true;
          }
          _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput(`Side Road width`, `Side Road Width`, SideRoadParamsContainer, 4, null, 'input', 'sw')
          _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle(`Side Road Shape`, FormDrawMapComponent.SideEndShape, SideRoadParamsContainer, route.shape, null)

          var angleContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'angle-picker-container' }, SideRoadParamsContainer);
          _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton(`rotate-left`, '<i class="fa-solid fa-rotate-left"></i>', angleContainer, null, FormDrawMapComponent.setAngle, 'click')
          var angleDisplay = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'id': `angle-display`, 'class': 'angle-display' }, angleContainer);
          angleDisplay.innerText = route.angle + '°';
          parent.routeCount += 1
        })
      }
      try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_6__.i18n.applyTranslations(parent); } catch (_) { }
    }
  },

  // Function to show the appropriate settings based on selected road type
  updateRoadTypeSettings: function () {
    // Clear current settings
    const roadTypeSettingsContainer = document.querySelector('.road-type-settings');
    roadTypeSettingsContainer.innerHTML = '';

    // Get selected road type
    const roadType = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Main Road Type-container');

    // Show settings based on road type
    if (roadType === 'Main Line') {
      // Main Line settings
      // Use custom image dropdown for Main Road Shape
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createImageDropdown('Main Road Shape', FormDrawMapComponent.MainEndShape, roadTypeSettingsContainer, 'Arrow', FormDrawMapComponent.onMainRoadShapeChange);

      // Add help icon (need to find the label or container)
      const shapeContainer = document.getElementById('Main Road Shape-container');
      if (shapeContainer) {
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createHelpIconWithHint(shapeContainer, 'route/MainRoadShape');
      }

      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('main-width', 'Main Road Width', roadTypeSettingsContainer, 6, FormDrawMapComponent.drawMainRoadOnCursor, 'input', 'sw');
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('root-length', 'Main Road Approach Length', roadTypeSettingsContainer, 7, FormDrawMapComponent.drawMainRoadOnCursor, 'input', 'sw');
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('tip-length', 'Main Road Exit Length', roadTypeSettingsContainer, 12, FormDrawMapComponent.drawMainRoadOnCursor, 'input', 'sw');

      // Corner radius inputs container - initially hidden
      const cornerRadiusContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'corner-radius-container', 'style': 'display: none;' }, roadTypeSettingsContainer);
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('inner-corner-radius', 'Inner Corner Radius', cornerRadiusContainer, 1, FormDrawMapComponent.drawMainRoadOnCursor, 'input', 'sw');
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('outer-corner-radius', 'Outer Corner Radius', cornerRadiusContainer, 4, FormDrawMapComponent.drawMainRoadOnCursor, 'input', 'sw');

      // Add angle selector for main road using same structure as side road
      var mainAngleContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'angle-picker-container' }, roadTypeSettingsContainer);
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton(`rotate-left`, '<i class="fa-solid fa-rotate-left"></i>', mainAngleContainer, null, FormDrawMapComponent.setMainAngle, 'click');
      var mainAngleDisplay = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'id': `main-angle-display`, 'class': 'angle-display' }, mainAngleContainer);
      mainAngleDisplay.innerText = '0°';
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton(`rotate-right`, '<i class="fa-solid fa-rotate-right"></i>', mainAngleContainer, null, FormDrawMapComponent.setMainAngle, 'click');

      // Initial check for corner radius visibility (default is Arrow, so hide them)
      setTimeout(() => {
        FormDrawMapComponent.onMainRoadShapeChange();
      }, 100);

    } else if (roadType === 'Roundabout') {
      // Roundabout settings

      // 1. Roundel Type Toggle (Conventional vs Spiral)
      const roundelToggle = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Roundel Shape', Object.keys(FormDrawMapComponent.RoundaboutFeatures), roadTypeSettingsContainer, 'Conventional', FormDrawMapComponent.onRoundelTypeChange);

      const roundelType = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Roundel Shape-container');

      // Use custom image dropdown for Roundabout Shape
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createImageDropdown('Main Road Shape', FormDrawMapComponent.RoundaboutFeatures[roundelType], roadTypeSettingsContainer, FormDrawMapComponent.RoundaboutFeatures[roundelType][0].value, FormDrawMapComponent.drawMainRoadOnCursor);

      const shapeContainer = document.getElementById('Main Road Shape-container');
      if (shapeContainer) {
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createHelpIconWithHint(shapeContainer, 'route/Roundabout');
      }

      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('root-length', 'Roundabout Approach Length', roadTypeSettingsContainer, 22.9, FormDrawMapComponent.drawMainRoadOnCursor, 'input', 'sw');

      // Add angle selector for Oval Roundabout (hidden by default unless Oval is selected)
      var mainAngleContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'angle-picker-container', 'style': 'display: none;' }, roadTypeSettingsContainer);
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton(`rotate-left`, '<i class="fa-solid fa-rotate-left"></i>', mainAngleContainer, null, FormDrawMapComponent.setMainAngle, 'click');
      var mainAngleDisplay = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'id': `main-angle-display`, 'class': 'angle-display' }, mainAngleContainer);
      mainAngleDisplay.innerText = '0°';
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton(`rotate-right`, '<i class="fa-solid fa-rotate-right"></i>', mainAngleContainer, null, FormDrawMapComponent.setMainAngle, 'click');

      // Oval 90 position toggle
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Oval Position', ['Left', 'Middle'], roadTypeSettingsContainer, 'Left', FormDrawMapComponent.drawMainRoadOnCursor);
      // Hide initially
      const ovalPosContainer = document.getElementById('Oval Position-container').parentNode;
      ovalPosContainer.style.display = 'none';

      // Trigger visibility check
      FormDrawMapComponent.onRoundelTypeChange();
    }

    try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_6__.i18n.applyTranslations(roadTypeSettingsContainer.parentElement); } catch (_) { }
  },

  // Function to handle Roundel Type changes
  onRoundelTypeChange: function () {
    const roundelType = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Roundel Shape-container');
    const features = FormDrawMapComponent.RoundaboutFeatures[roundelType];

    // Find existing dropdown container
    const container = document.getElementById('Main Road Shape-container');
    if (container) {
      const parent = container.parentNode;
      const nextSibling = container.nextSibling;

      // Remove old container
      parent.removeChild(container);

      // Create new dropdown (appends to parent)
      const newDropdown = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createImageDropdown('Main Road Shape', features, parent, features[0].value, FormDrawMapComponent.drawMainRoadOnCursor);

      // Move to correct position if needed
      if (nextSibling) {
        parent.insertBefore(newDropdown, nextSibling);
      }

      // Re-add help icon
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createHelpIconWithHint(newDropdown, 'route/Roundabout');
    }

    // Toggle Angle Picker for Oval
    const angleContainer = document.querySelector('.road-type-settings .angle-picker-container');
    if (angleContainer) {
      angleContainer.style.display = (roundelType === 'Oval' || roundelType === 'Double') ? 'flex' : 'none';
    }

    // Check angle for position toggle visibility
    const angleDisplay = document.getElementById('main-angle-display');
    const ovalPosContainer = document.getElementById('Oval Position-container');
    if (angleDisplay && ovalPosContainer) {
      const angle = parseInt(angleDisplay.innerText.slice(0, -1));
      const posParent = ovalPosContainer.parentNode;
      posParent.style.display = (roundelType === 'Oval' && angle === 90) ? 'block' : 'none';
    }

    // Trigger draw update
    FormDrawMapComponent.drawMainRoadOnCursor();
  },

  // Function to handle Main Road Shape changes
  onMainRoadShapeChange: function () {
    // Get the selected shape from custom dropdown
    const container = document.getElementById('Main Road Shape-container');
    const selectedShape = container ? container.getAttribute('data-value') : null;

    // Find the corner radius container
    const cornerRadiusContainer = document.querySelector('.corner-radius-container');

    if (cornerRadiusContainer) {
      // Show corner radius inputs only for LaneDrop shape
      if (selectedShape === 'LaneDrop') {
        cornerRadiusContainer.style.display = 'block';
        const tipLengthInput = document.getElementById('tip-length');
        if (tipLengthInput) {
          tipLengthInput.value = 18.45;
        }
      } else {
        cornerRadiusContainer.style.display = 'none';
      }
    }

    // Call the original drawing function
    FormDrawMapComponent.drawMainRoadOnCursor();
  },

  gatherMainRoadParams: function () {
    const xHeight = parseInt(document.getElementById('input-xHeight').value);
    const color = document.getElementById('Message Colour-container').selected.getAttribute('data-value');
    let roadType = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Main Road Type-container');

    const rootLengthElement = document.getElementById('root-length');
    const tipLengthElement = document.getElementById('tip-length');
    const mainWidthElement = document.getElementById('main-width');
    const innerCornerRadiusElement = document.getElementById('inner-corner-radius');
    const outerCornerRadiusElement = document.getElementById('outer-corner-radius');
    const mainRoadShapeContainer = document.getElementById('Main Road Shape-container');
    const roundelContainer = document.getElementById('Roundel Shape-container');
    const mainAngleDisplayElement = document.getElementById('main-angle-display');

    const rootLength = rootLengthElement ? parseFloat(rootLengthElement.value) : null;
    let tipLength = tipLengthElement ? parseFloat(tipLengthElement.value) : null;
    const mainWidth = mainWidthElement ? parseFloat(mainWidthElement.value) : null;

    // Get shape from custom dropdown
    let endShape = mainRoadShapeContainer ? mainRoadShapeContainer.getAttribute('data-value') : null;
    let roundaboutFeatures = null;

    // Handle Roundabout logic
    if (roadType === 'Roundabout') {
      const roundelType = roundelContainer ? _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Roundel Shape-container') : 'Conventional';

      roadType = roundelType + ' Roundabout';
      if (roundelType === 'Oval') {
        const angle = parseInt(mainAngleDisplayElement.innerText.slice(0, -1));
        roundaboutFeatures = 'Normal ' + angle;
        tipLength = 24; // Fixed tip length for Oval
        if (angle === 90) {
          const position = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Oval Position-container');
          roundaboutFeatures += ' ' + position;
        }
        //} else if (roundelType === 'Double') {
        //  const angle = parseInt(mainAngleDisplayElement.innerText.slice(0, -1));
        //  roundaboutFeatures = endShape + ' ' + angle;
      } else if (roundelType === 'Double') {
        if (endShape === 'Spiral') {
          tipLength = 38; // Fixed tip length for Double Spiral
        } else {
          tipLength = 28; // Fixed tip length for Double Normal
        }
        roundaboutFeatures = endShape; 
      }
      else {
        roundaboutFeatures = endShape; // Normal, Auxiliary, or U-turn
      }
    }

    // Only get radius values if LaneDrop is selected and inputs exist
    let innerCornerRadius = null;
    let outerCornerRadius = null;
    if (endShape === 'LaneDrop') {
      innerCornerRadius = innerCornerRadiusElement ? parseFloat(innerCornerRadiusElement.value) : null;
      outerCornerRadius = outerCornerRadiusElement ? parseFloat(outerCornerRadiusElement.value) : null;
    }

    // const roundaboutFeatures = null; // Removed
    const mainAngle = mainAngleDisplayElement ? parseInt(mainAngleDisplayElement.innerText.slice(0, -1)) : 0;

    const mainRoadParams = {
      xHeight: xHeight,
      color: color.toLowerCase(),
      roadType: roadType,
      rootLength: rootLength,
      tipLength: tipLength,
      mainWidth: mainWidth,
      innerCornerRadius: innerCornerRadius,
      outerCornerRadius: outerCornerRadius,
      shape: endShape,
      width: mainWidth,
      RAfeature: roundaboutFeatures,
      mainAngle: mainAngle
    };

    // Use the updated function that uses the general snapping functionality
    FormDrawMapComponent.drawMainRoadOnCursor(null, mainRoadParams);
  },

  /**
   * Adds new route inputs to the panel based on active object's route list
   * @param {Event} event - The triggering event object
   * @return {void} 
   */
  addRouteInput: function (event) {
    const existingRoute = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getActiveObject();
    if (existingRoute && existingRoute.functionalType === 'MainRoad') {
      // Get parameters from DOM
      const xHeight = parseInt(document.getElementById('input-xHeight').value);
      const color = document.getElementById('Message Colour-container').selected.getAttribute('data-value');
      const angle = parseInt(document.getElementById('angle-display').innerText);
      const width = parseInt(document.getElementById('Side Road width').value);
      const shape = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Side Road Shape-container');

      FormDrawMapComponent.drawSideRoadOnCursor(null);
    }
  },

  /**
   * Sets angle for route based on button clicks
   * @param {Event} event - Click event
   * @return {void}
   */
  setAngle: function (event) {
    // Find the parent container element
    const parentContainer = event.currentTarget.parentNode;
    // Find the angle display element within the same container
    const angleDisplay = parentContainer.querySelector('[id^="angle-display"]');
    // Extract the current angle value
    const currentText = angleDisplay.innerText.slice(0, -1); // Remove the degree symbol

    const angleIndex = FormDrawMapComponent.permitAngle.indexOf(parseInt(currentText))
    angleDisplay.innerText = FormDrawMapComponent.permitAngle[(angleIndex + 1) % FormDrawMapComponent.permitAngle.length] + '°';

    // If we have a side road object being placed, update its angle
    if (FormDrawMapComponent.newMapObject && FormDrawMapComponent.newMapObject.functionalType === 'SideRoad') {
      const newAngle = FormDrawMapComponent.permitAngle[(angleIndex + 1) % FormDrawMapComponent.permitAngle.length];

      // Update the object's angle - will be applied during mouse move
      if (FormDrawMapComponent.newMapObject.routeList && FormDrawMapComponent.newMapObject.routeList.length > 0) {
        const side = FormDrawMapComponent.newMapObject.side;
        FormDrawMapComponent.newMapObject.routeList[0].angle = side ? -Math.abs(newAngle) : Math.abs(newAngle);

        // Force an update
        if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex && _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.handleMouseMoveRef) {
          // Get current pointer
          const pointer = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getPointer({ e: window.event });
          _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.handleMouseMoveRef({
            e: window.event,
            pointer: pointer
          });
        }
      }
    }
  },

  /**
   * Sets angle for main road based on button clicks
   * @param {Event} event - Click event
   * @return {void}
   */
  setMainAngle: function (event) {
    // Find the angle display element within the same container
    const angleDisplay = document.getElementById('main-angle-display');

    // Check if angleDisplay exists
    if (!angleDisplay) {
      console.error('Main angle display element not found');
      return;
    }

    // Determine context (Road Type / Roundel Type)
    const roadType = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Main Road Type-container');
    let angleKey = 'Main Line'; // Default

    if (roadType === 'Roundabout') {
      const roundelType = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Roundel Shape-container');
      angleKey = roundelType + ' Roundabout';
    }

    // Guard against types that don't rotate or key not found
    // If double roundabout not in permitted list yet, default or skip
    let mainRoadAngles = _objects_template_js__WEBPACK_IMPORTED_MODULE_4__.routePermittedAngle[angleKey];
    
    // Extract the current angle value
    let currentText = angleDisplay.innerText.slice(0, -1); // Remove the degree symbol
    let currentAngle = parseInt(currentText);
    
    // If current angle is not in the allowed list (e.g. switching types), snap to nearest or 0
    if (mainRoadAngles.indexOf(currentAngle) === -1) {
        currentAngle = 0;
    }

    const angleIndex = mainRoadAngles.indexOf(currentAngle);

    // Determine if this is a left (counter-clockwise) or right (clockwise) button
    const isLeftButton = event.currentTarget.id.includes('rotate-left');
    const isRightButton = event.currentTarget.id.includes('rotate-right');

    let newAngle;
    if (isLeftButton) {
      // Counter-clockwise: go to previous angle (or wrap to last)
      newAngle = angleIndex > 0 ? mainRoadAngles[angleIndex - 1] : mainRoadAngles[mainRoadAngles.length - 1];
    } else if (isRightButton) {
      // Clockwise: go to next angle (or wrap to first)
      newAngle = angleIndex < mainRoadAngles.length - 1 ? mainRoadAngles[angleIndex + 1] : mainRoadAngles[0];
    } else {
      // Fallback to old behavior (clockwise)
      newAngle = mainRoadAngles[(angleIndex + 1) % mainRoadAngles.length];
    }

    if (isNaN(newAngle)) newAngle = 0;

    angleDisplay.innerText = newAngle + '°';

    // Handle visibility of Position Toggle (Left/Middle) for Oval 90 deg
    if (angleKey === 'Oval Roundabout') {
        const ovalPosContainer = document.getElementById('Oval Position-container');
        if (ovalPosContainer) {
            const posParent = ovalPosContainer.parentNode;
            posParent.style.display = (newAngle === 90) ? 'block' : 'none';
        }
    }

    // If we have a main road object being placed, update its angle
    if (FormDrawMapComponent.newMapObject && FormDrawMapComponent.newMapObject.functionalType === 'MainRoad') {
      // Update the object's angle - will be applied during mouse move
      FormDrawMapComponent.newMapObject.mainAngle = newAngle;

      // Force an update if the object is being drawn
      if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex && _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.handleMouseMoveRef) {
        // Get current pointer
        const pointer = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getPointer({ e: window.event });
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.handleMouseMoveRef({
          e: window.event,
          pointer: pointer
        });
      }
      
      // Force redraw to catch shape change (important for Oval position toggle etc)
      FormDrawMapComponent.drawMainRoadOnCursor();
    }
  },


  /**
   * Draws new route cursor on canvas for initial placement
   * @param {Event} event - The triggering event object
   * @param {Object} params - Optional parameters for testing
   * @return {void}
   */
  drawMainRoadOnCursor: function (event, params = null) {
    // Remove existing event listeners first to avoid duplicates
    FormDrawMapComponent.drawRoadsHandlerOff();

    canvas.discardActiveObject();
    document.removeEventListener('keydown', _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.Show);
    document.addEventListener('keydown', FormDrawMapComponent.cancelDraw);

    // Get parameters either from DOM elements or provided params
    let xHeight, rootLength, tipLength, color, width, shape, roadType, RAfeature, mainAngle, innerCornerRadius, outerCornerRadius;

    if (params) {
      xHeight = params.xHeight || 100;
      rootLength = params.rootLength;
      tipLength = params.tipLength;
      color = params.color || 'white';
      width = params.width || 6;
      shape = params.shape || 'Arrow';
      roadType = params.roadType || 'Main Line';
      RAfeature = params.RAfeature || 'Normal';
      mainAngle = params.mainAngle || 0;
      innerCornerRadius = params.innerCornerRadius || null;
      outerCornerRadius = params.outerCornerRadius || null;
    } else {
      return;
    }
    // Determine which vertex to use as the active vertex for tracking
    // For roundabouts, use C1 (center) vertex, otherwise use V1
    const activeVertexLabel = (roadType === 'Conventional Roundabout' || roadType === 'Spiral Roundabout' || roadType === 'Oval Roundabout' || roadType === 'Double Roundabout') ? 'C1' : 'V1';

    // Use the general object creation with snapping function
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createObjectWithSnapping(
      {
        position: {
          x: event ? canvas.getPointer(event.e).x : canvas.width / 2,
          y: event ? canvas.getPointer(event.e).y : canvas.height / 2
        },
        xHeight: xHeight,
        color: color.toLowerCase(),
        rootLength: rootLength,
        tipLength: tipLength,
        width: width,
        shape: shape,
        roadType: roadType,
        RAfeature: RAfeature,
        mainAngle: mainAngle,
        innerCornerRadius: innerCornerRadius,
        outerCornerRadius: outerCornerRadius
      },
      FormDrawMapComponent.createMainRoadObject,
      FormDrawMapComponent, // Pass the component to store the created object
      'newMapObject',
      activeVertexLabel,
      FormDrawMapComponent.mainRoadOnMouseMove,
      FormDrawMapComponent.finishDrawMainRoad,
      FormDrawMapComponent.cancelDraw
    );


  },

  // Create a function that returns a new MainRoadSymbol
  createMainRoadObject: (options) => {
    let tipLength = options.tipLength
    let rootLength = options.rootLength
    // Calculate position based on angle
    let mainAngle = options.mainAngle;
    if (options.shape === 'T-Junction') {
      mainAngle = -90;
    } else if (options.shape === 'Y-Junction') {
      mainAngle = -30;
    } else if (options.shape === 'LaneDrop') {
      mainAngle = -60
      rootLength = 12
    }
    const angleRad = mainAngle * Math.PI / 180;
    let addRootwidth
    switch (Math.sign(mainAngle)) {
      case 0:
        addRootwidth = 0
        break
      case 1:
        addRootwidth = -options.width / 2 * options.xHeight / 4;
        break
      case -1:
        addRootwidth = options.width / 2 * options.xHeight / 4;
        break
    }

    // Create initial route list with tip and preliminary bottom
    const routeList = [
      // tip
      {
        x: options.position.x,
        y: options.position.y,
        angle: mainAngle,
        width: options.shape == 'Y-Junction' ? options.width / 3 * 2 : options.width,
        length: tipLength,
        shape: options.roadType == 'Main Line' ? options.shape : options.RAfeature
      },
      // bottom (preliminary position, will be corrected)
      {
        x: options.position.x - Math.sin(angleRad) * tipLength * options.xHeight / 4 + addRootwidth,
        y: options.position.y + (Math.cos(angleRad) * tipLength + (options.width / 2 * (1 - Math.cos(angleRad)) + options.rootLength)) * options.xHeight / 4,
        angle: 180,
        width: options.width,
        length: options.rootLength,
        shape: options.roadType == 'Main Line' ? 'Stub' : options.RAfeature
      },
    ];

    if (options.shape === 'T-Junction' || options.shape === 'Y-Junction') {
      routeList.push({
        x: options.position.x - 2 * Math.sin(angleRad) * tipLength * options.xHeight / 4 + addRootwidth * 2,
        y: options.position.y,
        angle: -mainAngle,
        width: options.shape == 'Y-Junction' ? options.width / 3 * 2 : options.width,
        length: options.tipLength,
        shape: options.shape
      });
    }

    // Use the extracted function to calculate the correct bottom Y coordinate
    if (options.roadType === 'Main Line') {
      const topRoute = routeList[0];
      const bottomRoute = routeList[1];
      const length = options.xHeight / 4;
      bottomRoute.y = (0,_objects_mainRoute_js__WEBPACK_IMPORTED_MODULE_2__.calculateMainRoadBottomY)(topRoute, bottomRoute, length);
    }

    // Create route options for the MainRoadSymbol
    const routeOptions = {
      routeList: routeList,
      xHeight: options.xHeight,
      color: options.color,
      rootLength: options.rootLength,
      tipLength: options.tipLength,
      routeWidth: options.width,
      roadType: options.roadType,
      RAfeature: options.RAfeature,
      mainAngle: mainAngle,
      innerCornerRadius: options.innerCornerRadius,
      outerCornerRadius: options.outerCornerRadius
    };

    // Create and initialize the MainRoadSymbol
    const routeMap = new _objects_mainRoute_js__WEBPACK_IMPORTED_MODULE_2__.MainRoadSymbol(routeOptions);
    //routeMap.initialize(calcVertexType[options.roadType](options.xHeight, routeList));

    return routeMap;
  },

  /**
  * Handle mouse movement for road symbol placement
  * @param {Event} event - Mouse event
  */
  mainRoadOnMouseMove: function (event) {
    if (!FormDrawMapComponent.newMapObject) return;

    const mainRoad = FormDrawMapComponent.newMapObject;
    if (mainRoad.functionalType !== 'MainRoad') return;

    const pointer = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getPointer(event.e);

    // If we have an active vertex, handle the vertex-based movement
    if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex && _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.handleMouseMoveRef) {
      // Store the original position of the object for delta calculation
      const originalLeft = mainRoad.left;
      const originalTop = mainRoad.top;

      // Let the vertex handle its movement
      const simulatedEvent = {
        e: event.e,
        pointer: pointer
      };
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.handleMouseMoveRef(simulatedEvent);


    } else {
      // Direct positioning for the whole object - THIS PART IS CRITICAL
      // Update the position of the entire object to follow the cursor
      mainRoad.set({
        left: pointer.x,
        top: pointer.y
      });

      // Update the routeList coordinates to match the new position
      // For initial placement, we need to recreate routeList at the new position
      if (mainRoad.routeList && mainRoad.routeList.length >= 2) {
        if (mainRoad.roadType === 'Conventional Roundabout' || mainRoad.roadType === 'Spiral Roundabout' || mainRoad.roadType === 'Oval Roundabout' || mainRoad.roadType === 'Double Roundabout') {
          // For roundabout, use the center point (routeList[1])
          mainRoad.routeList[1].x = pointer.x;
          mainRoad.routeList[1].y = pointer.y;

          // Position the root point relative to center
          mainRoad.routeList[0].x = pointer.x;
          mainRoad.routeList[0].y = pointer.y;
        } else {
          // For main line, position the top and bottom points based on cursor
          mainRoad.routeList[1].x = pointer.x; // Top point (tip)
          mainRoad.routeList[1].y = pointer.y;

          // Position the bottom point (root)
          mainRoad.routeList[0].x = pointer.x;
          mainRoad.routeList[0].y = pointer.y + (mainRoad.rootLength + mainRoad.tipLength) * mainRoad.xHeight / 4;
        }
      }

      // Recalculate the vertices based on updated routeList
      const vertexList = _objects_mainRoute_js__WEBPACK_IMPORTED_MODULE_2__.calcVertexType[mainRoad.roadType](mainRoad.xHeight, mainRoad.routeList, mainRoad.innerCornerRadius, mainRoad.outerCornerRadius);
      if (mainRoad.basePolygon) {
        mainRoad.basePolygon.vertex = vertexList.path[0].vertex;
        mainRoad.basePolygon.path = vertexList.path;
      }

      // Update any side roads if they exist
      if (mainRoad.sideRoad && mainRoad.sideRoad.length > 0) {
        mainRoad.sideRoad.forEach(side => {
          side.onMove(null, true);
        });
      }
    }

    // Update coordinates and render
    mainRoad.setCoords();
    mainRoad.drawVertex(false);
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
  },

  /**
  * Handles mouse click to place main road on canvas
  * @param {Event} event - Mouse event
  * @param {Object} options - Optional parameters
  * @return {Promise<void>}
  */
  finishDrawMainRoad: function (event, options = null) {
    if (event.e.button !== 0 && event.e.type !== 'touchend') return;

    // Use shared mouse click handler for new text objects
    if (FormDrawMapComponent.newMapObject) {
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.handleObjectOnMouseClick(
        FormDrawMapComponent,
        event,
        'newMapObject',
        'mainRoadOnMouseMove',
        'finishDrawMainRoad',
        'cancelDraw',
      );

      return;
    }


  },

  /**
  * Draws side road cursor for adding routes to existing root
  * @param {Event} event - Mouse event
  * @param {Object} option - Optional parameters for testing
  * @return {void}
  */
  drawSideRoadOnCursor: function (event, option = null) {
    document.removeEventListener('keydown', _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.Show);
    document.addEventListener('keydown', FormDrawMapComponent.cancelDraw);

    const mainRoad = canvas.getActiveObject();
    if (!mainRoad || mainRoad.functionalType !== 'MainRoad') return;

    // Clear any existing temp side road
    if (FormDrawMapComponent.newMapObject) {
      // If there's already a temp side road, remove it
      const existingSideRoad = FormDrawMapComponent.newMapObject;
      if (existingSideRoad.functionalType === 'SideRoad') {
        existingSideRoad.deleteObject && existingSideRoad.deleteObject();
        FormDrawMapComponent.newMapObject = null;
      }
    }

    // Declare variables outside the if-else blocks 
    let routeList = [];
    let angle, shape, width, pointer, isSideLeft;

    if (option) {
      // Handle direct parameter input for testing
      if (option.routeList) {
        // Direct route list provided
        routeList = option.routeList;
        angle = option.angle;
      } else {
        // Parameters for creating a route list
        pointer = { x: option.x, y: option.y };
        angle = option.routeParams.angle;
        shape = option.routeParams.shape;
        width = option.routeParams.width;

        // Create route list with parameters
        isSideLeft = pointer.x < mainRoad.left + mainRoad.width / 2;
        if (isSideLeft) {
          angle = -Math.abs(angle); // Make angle negative for left side
        } else {
          angle = Math.abs(angle);  // Make angle positive for right side
        }

        routeList.push({
          x: pointer.x,
          y: pointer.y,
          angle: angle,
          shape: shape,
          width: width,
        });
        mainRoad.tempRootList = JSON.parse(JSON.stringify(routeList));
      }
    } else {
      // Normal DOM operation
      pointer = {
        x: event ? _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getPointer(event.e).x : mainRoad.left - 10 * mainRoad.xHeight / 4,
        y: event ? _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getPointer(event.e).y : mainRoad.top
      };
      // Check if pointer is within the main road bounds
      if (mainRoad.roadType == 'Main Line') {
        if (pointer.x > mainRoad.getEffectiveCoords()[0].x && pointer.x < mainRoad.getEffectiveCoords()[1].x) {
          return;
        }
      } else if (mainRoad.roadType == 'Conventional Roundabout' || mainRoad.roadType == 'Spiral Roundabout') {
        const center = mainRoad.routeList[1];
        if ((pointer.x - center.x) ** 2 + (pointer.y - center.y) ** 2 < (mainRoad.xHeight * 3) ** 2) {
          //return;
        }
      }

      angle = parseInt(document.getElementById(`angle-display`).innerText);
      isSideLeft = pointer.x < mainRoad.left + mainRoad.width / 2;
      if (isSideLeft) {
        angle = -angle;
      }

      shape = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Side Road Shape-container');
      width = document.getElementById(`Side Road width`).value;
      routeList.push({ x: pointer.x, y: pointer.y, angle: angle, shape: shape, width: width, });
      mainRoad.tempRootList = JSON.parse(JSON.stringify(routeList));
    }

    // Use the general object creation with snapping function
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createObjectWithSnapping(
      {
        position: {
          x: pointer.x,
          y: pointer.y
        },
        angle: angle,
        shape: shape,
        width: width,
        mainRoad: mainRoad,
        isSideLeft: isSideLeft,
      },
      FormDrawMapComponent.createSideRoadObject,
      FormDrawMapComponent,
      'newMapObject',
      'V1',
      FormDrawMapComponent.sideRoadOnMouseMove,
      FormDrawMapComponent.finishDrawSideRoad,
      FormDrawMapComponent.cancelDraw
    );


  },

  // Create a function that returns a new SideRoadSymbol
  createSideRoadObject: function (options) {
    // Create the route list for the side road
    const routeList = [{
      x: options.position.x,
      y: options.position.y,
      angle: options.angle,
      shape: options.shape,
      width: options.width || 4
    }];

    const mainRoad = options.mainRoad;

    // Create the branch options
    const branchOptions = {
      routeList: routeList,
      xHeight: mainRoad.xHeight,
      color: mainRoad.color,
      mainRoad: mainRoad,
      side: options.isSideLeft,
      branchIndex: mainRoad.sideRoad.length + 1
    };

    // Create and initialize the side road
    const sideRoad = new _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_3__.SideRoadSymbol(branchOptions);

    return sideRoad;
  },

  sideRoadOnMouseMove: function (event) {
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.handleObjectOnMouseMove(FormDrawMapComponent, event);
  },


  /**
  * Handle mouse movement for side road placement
  * @param {Event} event - Mouse event
  */
  LEGACY_sideRoadOnMouseMove: function (event) {

    const sideRoad = FormDrawMapComponent.newMapObject;
    if (sideRoad.functionalType !== 'SideRoad') return;

    const mainRoad = sideRoad.mainRoad;
    if (!mainRoad) return;

    const pointer = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getPointer(event.e);

    // If we have an active vertex, handle the vertex-based movement
    if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.handleMouseMoveRef) {
      // Let the vertex handle its movement first
      const simulatedEvent = {
        e: event.e,
        pointer: pointer
      };
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.handleMouseMoveRef(simulatedEvent);

      // Find the V1 vertex which corresponds to routeList[0]
      let v1Vertex = sideRoad.basePolygon.vertex.find(v => v.label === 'V1');

      // Calculate offset between active vertex and V1
      // If the active vertex is V1, then no offset is needed
      let offsetX = 0;
      let offsetY = 0;

      if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.label !== 'V1' && v1Vertex) {
        // Calculate where V1 should be based on the active vertex movement
        const activeVertexObj = sideRoad.basePolygon.vertex.find(v => v.label === _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.label);
        if (activeVertexObj) {
          offsetX = v1Vertex.x - activeVertexObj.x;
          offsetY = v1Vertex.y - activeVertexObj.y;
        }
      }

      // Apply the calculated offset to get the correct position for routeList[0]
      sideRoad.routeList[0].x = pointer.x + offsetX;
      sideRoad.routeList[0].y = pointer.y + offsetY;

      // Check which side of the main road we're on now and update side property
      const isSideLeft = pointer.x < mainRoad.left + mainRoad.width / 2;

      // Update the side property if it changed
      if (sideRoad.side !== isSideLeft) {
        // Flip the angle sign when switching sides
        const currentAngle = Math.abs(sideRoad.routeList[0].angle);
        sideRoad.routeList[0].angle = isSideLeft ? -currentAngle : currentAngle;

        // Update the side property
        sideRoad.side = isSideLeft;
      }

      // Apply constraints based on the current pointer position
      const constrainedResult = applySideRoadConstraints(
        sideRoad,
        mainRoad,
        sideRoad.routeList,
        sideRoad.side,
        mainRoad.xHeight
      );

      // Update with constrained position
      sideRoad.routeList = constrainedResult.routeList;

      // In some cases we need to recreate the vertex list
      if (constrainedResult.tempVertexList) {
        // Replace the base polygon vertices with the constrained vertex list
        sideRoad.basePolygon.vertex = constrainedResult.tempVertexList.path[0].vertex;
        sideRoad.basePolygon.path = constrainedResult.tempVertexList.path;
      }
    } else {
      // When no vertex is active, we do direct coordinate updates
      // Calculate where the side road should be based on pointer

      // Update the route coordinate with pointer position
      sideRoad.routeList[0].x = pointer.x;
      sideRoad.routeList[0].y = pointer.y;

      // Apply constraints based on main road type
      const isSideLeft = pointer.x < mainRoad.left + mainRoad.width / 2;
      sideRoad.side = isSideLeft; // Update the side property

      // Update angle based on side
      const currentAngle = Math.abs(sideRoad.routeList[0].angle);
      sideRoad.routeList[0].angle = isSideLeft ? -currentAngle : currentAngle;

      // Apply constraints to position
      const constrainedResult = applySideRoadConstraints(
        sideRoad,
        mainRoad,
        sideRoad.routeList,
        isSideLeft,
        mainRoad.xHeight
      );

      // Update with constrained position
      sideRoad.routeList = constrainedResult.routeList;

      // Set position directly based on constrained result
      if (constrainedResult.tempVertexList) {
        // Calculate proper position for the side road
        const bbox = calculateBoundingBox(constrainedResult.tempVertexList.path[0].vertex);
        sideRoad.left = bbox.left;
        sideRoad.top = bbox.top;

        // Replace the base polygon vertices with the constrained vertex list
        sideRoad.basePolygon.vertex = constrainedResult.tempVertexList.path[0].vertex;
        sideRoad.basePolygon.path = constrainedResult.tempVertexList.path;
      }
    }
    // Update the main road with the side road
    try {
      mainRoad.receiveNewRoute(sideRoad.basePolygon);
    } catch (error) {
      console.error("Error updating main road:", error);
    }

    // Update coordinates
    sideRoad.setCoords();
    sideRoad.drawVertex();
    mainRoad.setCoords();

    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
  },



  /**
  * Completes side road drawing and anchors to root
  * @param {Event} event - Mouse event
  * @return {void}
  */
  finishDrawSideRoad: function (event) {
    if (event.e.button !== 0 && event.e.type !== 'touchend') return;

    const sideRoad = FormDrawMapComponent.newMapObject;
    const mainRoad = FormDrawMapComponent.newMapObject?.mainRoad;
    // Use shared mouse click handler for new text objects
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.handleObjectOnMouseClick(
      FormDrawMapComponent,
      event,
      'newMapObject',
      'sideRoadOnMouseMove',
      'finishDrawSideRoad',
      'cancelDraw',
    );

  },

  /**
  * Cleans up route drawing handlers
  * @param {Event} event - Optional event object
  * @return {void}
  */
  drawRoadsHandlerOff: function (event) {
    // If there's a new road object being placed, remove it unless it's been added to canvas properly
    if (FormDrawMapComponent.newMapObject) {
      const newRoad = FormDrawMapComponent.newMapObject;

      // If it's a side road and not fully added to a main road, remove it
      if (newRoad.functionalType === 'SideRoad' && newRoad.mainRoad) {
        const mainRoad = newRoad.mainRoad;

        // Check if this side road is already part of the main road's side roads
        const isAdded = mainRoad.sideRoad.includes(newRoad);

        if (!isAdded) {
          // Update the main road as if the side road wasn't placed
          mainRoad.receiveNewRoute();
          mainRoad.setCoords();

          // Remove the temporary side road from canvas explicitly
          canvas.remove(newRoad);
          // Then delete the object
          newRoad.deleteObject && newRoad.deleteObject();
        }
      } else if (newRoad.functionalType === 'MainRoad' && !canvasObject.includes(newRoad)) {
        // If it's a main road that hasn't been properly added to canvas
        newRoad.deleteObject && newRoad.deleteObject();
      }

      FormDrawMapComponent.newMapObject = null;
    }

    // Clean up active vertex if there is one
    if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex) {
      if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.indicator) {
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.remove(_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.indicator);
      }
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.cleanupDrag && _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex.cleanupDrag();
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeVertex = null;
    }

    canvas.off('mouse:move', FormDrawMapComponent.mainRoadOnMouseMove);
    canvas.off('mouse:move', FormDrawMapComponent.sideRoadOnMouseMove);
    canvas.off('mouse:down', FormDrawMapComponent.finishDrawSideRoad);
    canvas.off('mouse:down', FormDrawMapComponent.finishDrawMainRoad);
    canvas.off('mouse:move', FormDrawMapComponent.drawSideRoadOnCursor);
    document.removeEventListener('keydown', FormDrawMapComponent.cancelDraw);
    document.addEventListener('keydown', FormDrawMapComponent.ShowHideSideBarEvent);

    // Force a final render to clean up any visual artifacts
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
  },

  /**
  * Cancels route drawing on escape key
  * @param {Event} event - Keyboard event
  * @param {boolean} force - Force cancel flag
  * @return {void}
  */
  cancelDraw: function (event, force = false) {
    // Use shared escape key handler
    if (event.key === 'Escape') {
      if (FormDrawMapComponent.newMapObject) {
        const newRoad = FormDrawMapComponent.newMapObject;
        // If it's a side road, remove it from the main road's side roads
        if (newRoad.functionalType === 'SideRoad' && newRoad.mainRoad) {
          const mainRoad = newRoad.mainRoad;

          // Check if this side road is already part of the main road's side roads
          const isAdded = mainRoad.sideRoad.includes(newRoad);

          if (isAdded) {
            // Update the main road as if the side road wasn't placed
            mainRoad.sideRoad = mainRoad.sideRoad.filter(side => side !== newRoad);
            mainRoad.receiveNewRoute();
            mainRoad.setCoords();

            // Remove the temporary side road from canvas explicitly
            _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.handleCancelWithEscape(
              FormDrawMapComponent,
              event,
              'newMapObject',
              'sideRoadOnMouseMove',
              'finishDrawSideRoad',
            );

            // Symbol-specific cleanup
            // TODO: FormDrawMapComponent.hideAngleControls();
          }
        }
        else if (newRoad.functionalType === 'MainRoad') {
          _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.handleCancelWithEscape(
            FormDrawMapComponent,
            event,
            'newMapObject',
            'mainRoadOnMouseMove',
            'finishDrawMainRoad',
          );
        }
      }
    }
  },
}

// Use the shared settings listener implementation
_sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.addListener(
  _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createSettingsListener(4, function (setting, value) {
    // Map-specific updates when settings change
    if (FormDrawMapComponent.newMapObject || FormDrawMapComponent.newMapObject) {
      const targetObject = FormDrawMapComponent.newMapObject || FormDrawMapComponent.newMapObject;

      // Handle map object updates when settings change
      if (setting === 'xHeight' && targetObject.xHeight !== undefined) {
        targetObject.xHeight = value;

        // Recreate or update the object as needed
        if (targetObject.functionalType === 'MainRoad' || targetObject.functionalType === 'SideRoad') {
          // Update existing parent object
          if (targetObject.mainRoad) {
            targetObject.mainRoad.receiveNewRoute();
          } else {
            // This is a main road - recreate vertex list
            const vertexList = _objects_mainRoute_js__WEBPACK_IMPORTED_MODULE_2__.calcVertexType[targetObject.roadType](value, targetObject.routeList, targetObject.innerCornerRadius, targetObject.outerCornerRadius);
            targetObject.replaceBasePolygon && targetObject.replaceBasePolygon(vertexList);
          }
        }

        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
      } else if (setting === 'messageColor' && targetObject.color !== undefined) {
        targetObject.color = value.toLowerCase();

        // Update the object's color
        if (targetObject.basePolygon) {
          targetObject.basePolygon.set('fill', value.toLowerCase());
        }

        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
      }
    }
  })
);



/***/ }),

/***/ "./js/sidebar/sb-measure.js":
/*!**********************************!*\
  !*** ./js/sidebar/sb-measure.js ***!
  \**********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   FormMeasureComponent: () => (/* binding */ FormMeasureComponent)
/* harmony export */ });
/* harmony import */ var _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./sbGeneral.js */ "./js/sidebar/sbGeneral.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../canvas/keyboardEvents.js */ "./js/canvas/keyboardEvents.js");
/* harmony import */ var _canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../canvas/promptBox.js */ "./js/canvas/promptBox.js");
/* harmony import */ var _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../i18n/i18n.js */ "./js/i18n/i18n.js");
/**
 * Measure Tool Module for Sidebar
 * 
 * This module provides a measurement tool to measure distances between vertices
 * It detects snapping points and displays deltaX and deltaY measurements
 */


 // Import the event handler for sidebar toggling
 // Import prompt box functions


const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas; // Reference to the main canvas
const canvasObject = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvasObject; // Reference to the canvas objects

const FormMeasureComponent = {
  // State tracking
  activeMeasurement: false,
  firstVertex: null,
  newMeasureObject: null,
  snapThreshold: 30, // Distance in pixels for vertex snapping
  snapTarget: null, // Current snap target
  snapHighlight: null, // Visual highlight of snap target
  /**
   * Initialize the Measure panel UI
   */
  measurePanelInit: function (event) {
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.tabNum = 5; // Use a unique tab number for the measure tool
    var parent = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.PanelInit();
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.setActiveComponentOff(FormMeasureComponent.MeasureHandlerOff);
    if (!parent) return;

    // Create the title using consistent styling
  _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createI18nNode("h2", { 'class': 'panel-heading' }, parent, 'Measure Tool', 'text');

    // Create a container for instructions with proper styling
  const instructionsContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);
  const instructionsLabel = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createI18nNode("div", { 'class': 'input-group-label' }, instructionsContainer, 'Instructions', 'text');

    const instructionsContent = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-content' }, instructionsContainer);
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createI18nNode("p", { 'class': 'instruction-text' }, instructionsContent, 'Click on vertices to measure distances. First click selects the starting vertex, second click measures to the end vertex.', 'text');

    // Create a container for the measurement controls with proper styling
  const controlsContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);
  const controlsLabel = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createI18nNode("div", { 'class': 'input-group-label' }, controlsContainer, 'Measurement Control', 'text');

    const controlsContent = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-content' }, controlsContainer);

    // Add the measurement toggle button with consistent styling
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('toggle-measure',
      FormMeasureComponent.activeMeasurement ? 'Stop Measuring' : 'Start Measuring',
      controlsContent, 'input',
      FormMeasureComponent.toggleMeasurementMode, 'click');
    try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n.applyTranslations(parent); } catch (_) {}
  },

  /**
   * Toggle measurement mode on/off
   */
  toggleMeasurementMode: function () {
    FormMeasureComponent.activeMeasurement = !FormMeasureComponent.activeMeasurement; _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.activeMeasurement = FormMeasureComponent.activeMeasurement;

    if (FormMeasureComponent.activeMeasurement) {
      // Enable measuring mode
      FormMeasureComponent.startMeasuring();

      // Update button text
      const toggleButton = document.getElementById('toggle-measure');
      if (toggleButton) { toggleButton.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n.t('Stop Measuring'); toggleButton.setAttribute('data-i18n', 'Stop Measuring'); }
    } else {
      // Disable measuring mode
      FormMeasureComponent.stopMeasuring();

      // Update button text
      const toggleButton = document.getElementById('toggle-measure');
      if (toggleButton) { toggleButton.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n.t('Start Measuring'); toggleButton.setAttribute('data-i18n', 'Start Measuring'); }
    }
  },

  /**
   * Start the measurement mode
   */
  startMeasuring: function () {
    // Clear any existing state
    FormMeasureComponent.clearState();

    // Add mouse move listener to detect vertices
    canvas.on('mouse:move', FormMeasureComponent.MeasureOnMouseMove);
    canvas.on('mouse:down', FormMeasureComponent.MeasureOnMouseClick);

    // Escape key to cancel measuring
    document.removeEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent);
    document.addEventListener('keydown', FormMeasureComponent.cancelMeasure);

    // Change cursor to indicate measure mode
    canvas.defaultCursor = 'crosshair';
  },

  /**
   * Stop the measurement mode
   */
  stopMeasuring: function () {
    // Remove event listeners
    canvas.off('mouse:move', FormMeasureComponent.MeasureOnMouseMove);
    canvas.off('mouse:down', FormMeasureComponent.MeasureOnMouseClick);
    document.removeEventListener('keydown', FormMeasureComponent.cancelMeasure);
    document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_2__.ShowHideSideBarEvent);

    // Reset cursor
    canvas.defaultCursor = 'default';

    // Clear measurement state
    FormMeasureComponent.clearState();
  },
  /**
   * Clear the measurement state
   */
  clearState: function () {
    // Clear first vertex
    FormMeasureComponent.firstVertex = null;

    // Clear any snap highlights
    FormMeasureComponent.clearSnapHighlight();

    // Clear dynamic line
    FormMeasureComponent.clearDynamicLine();

    // Clear dimension lines
    FormMeasureComponent.clearDimensionLines();
  },

  /**
   * Clear the dynamic line between first vertex and cursor
   */
  clearDynamicLine: function () {
    if (FormMeasureComponent.dynamicLine) {
      canvas.remove(FormMeasureComponent.dynamicLine);
      FormMeasureComponent.dynamicLine = null;
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
    }
  },

  /**
   * Clear any dimension lines created for measurements
   */
  clearDimensionLines: function () {
    if (FormMeasureComponent.dimensionLines && FormMeasureComponent.dimensionLines.length > 0) {
      FormMeasureComponent.dimensionLines.forEach(obj => {
        canvas.remove(obj);
      });
      FormMeasureComponent.dimensionLines = [];
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
    }
  },
  /**
   * Handle mouse move during measurement
   */
  MeasureOnMouseMove: function (event) {
    const pointer = canvas.getPointer(event.e);

    // Check for vertices near the pointer
    FormMeasureComponent.checkForSnapTargets(pointer);

    // If we have a first vertex selected, draw a line to the current pointer
    if (FormMeasureComponent.firstVertex) {
      // Remove any existing dynamic line
      FormMeasureComponent.clearDynamicLine();

      // Create a new dynamic line from first vertex to pointer
      FormMeasureComponent.dynamicLine = new fabric.Line(
        [
          FormMeasureComponent.firstVertex.x,
          FormMeasureComponent.firstVertex.y,
          pointer.x,
          pointer.y
        ],
        {
          stroke: '#FF00FF', // Magenta to match first vertex highlight
          strokeWidth: 2 / canvas.getZoom(),
          strokeDashArray: [5, 5], // Dashed line
          selectable: false,
          evented: false
        }
      );

      // Add the line to the canvas
      canvas.add(FormMeasureComponent.dynamicLine);
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
    }
  },
  /**
   * Handle mouse click during measurement
   */
  MeasureOnMouseClick: function (event) {
    if (event.e.button !== 0 && event.e.type !== 'touchend') return;

    // clear selected objects to avoid interference
    canvas.discardActiveObject();

    // If we have a snap target, use that vertex
    if (FormMeasureComponent.snapTarget) {
      // If this is the first click, store the vertex
      if (!FormMeasureComponent.firstVertex) {
        FormMeasureComponent.firstVertex = {
          x: FormMeasureComponent.snapTarget.vertex.x,
          y: FormMeasureComponent.snapTarget.vertex.y,
          label: FormMeasureComponent.snapTarget.vertex.label,
          objectId: FormMeasureComponent.snapTarget.object.canvasID
        };

        // Keep the highlight for the first vertex
        // But create a new one with a different color
        FormMeasureComponent.clearSnapHighlight();
        FormMeasureComponent.addFirstVertexHighlight();
      }      
      // If this is the second click, calculate and display measurement
      else {
        // Clear the dynamic line when second vertex is clicked
        FormMeasureComponent.clearDynamicLine();

        const secondVertex = {
          x: FormMeasureComponent.snapTarget.vertex.x,
          y: FormMeasureComponent.snapTarget.vertex.y,
          label: FormMeasureComponent.snapTarget.vertex.label,
          objectId: FormMeasureComponent.snapTarget.object.canvasID
        };

        // Calculate delta
        const deltaX = Math.round(secondVertex.x - FormMeasureComponent.firstVertex.x);
        const deltaY = Math.round(secondVertex.y - FormMeasureComponent.firstVertex.y);
        const distance = Math.round(Math.sqrt(deltaX * deltaX + deltaY * deltaY));

        // Create dimension lines to visualize the measurement
        FormMeasureComponent.createDimensionLines(
          FormMeasureComponent.firstVertex,
          secondVertex,
          deltaX,
          deltaY,
          distance
        );

        // Display measurement using the context menu text box
        const measurementText =
          ` ${_i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n.t('Measurement Results')}:
          ΔX: ${_sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.formatDimension(deltaX, _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.xHeight)}
          ΔY: ${_sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.formatDimension(deltaY, _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.xHeight)}
          ${_i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n.t('Distance')}: ${_sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.formatDimension(distance, _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.xHeight)}
          
          ${_i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n.t('From')}: ${FormMeasureComponent.firstVertex.label} (Object #${FormMeasureComponent.firstVertex.objectId})
          ${_i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n.t('To')}: ${secondVertex.label} (Object #${secondVertex.objectId})
          
          (${_i18n_i18n_js__WEBPACK_IMPORTED_MODULE_4__.i18n.t('Press Enter to continue')})
        `;        
        // Clear first vertex highlight
        FormMeasureComponent.firstVertex = null;
        
        // Show measurement result and wait for Enter key
        (0,_canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_3__.showTextBox)(measurementText, ' ', 'keydown', function handleKeyPress(event) {
          if (event.key === 'Enter') {
            (0,_canvas_promptBox_js__WEBPACK_IMPORTED_MODULE_3__.hideTextBox)();

            // First clear all dimension lines
            FormMeasureComponent.clearDimensionLines();

            // Then reset for the next measurement after user presses Enter
            FormMeasureComponent.clearState();

            // Make sure dynamic line is fully cleared
            FormMeasureComponent.clearDynamicLine();

            document.removeEventListener('keydown', handleKeyPress);

            // Force a final render to ensure clean canvas
            _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
          }
        });
      }
    }
  },

  /**
   * Cancel measurement with Escape key
   */
  cancelMeasure: function (event) {
    if (event.key === 'Escape') {
      FormMeasureComponent.clearState();

      // If escape is pressed when the tool is active, deactivate it
      if (FormMeasureComponent.activeMeasurement) {
        FormMeasureComponent.toggleMeasurementMode();
      }
    }
  },
  /**
   * Check for vertices near the pointer position
   */
  checkForSnapTargets: function (pointer) {
    // Clear any existing snap highlight
    FormMeasureComponent.clearSnapHighlight();
    FormMeasureComponent.snapTarget = null;

    // Check all canvas objects for potential snap targets
    let closestDistance = FormMeasureComponent.snapThreshold;
    let closestVertex = null;
    let closestObject = null;

    canvasObject.forEach(obj => {
      // Skip objects without basePolygon
      if (!obj.basePolygon || !obj.basePolygon.vertex) return;

      // Check each vertex
      obj.basePolygon.vertex.forEach(vertex => {
        const dx = vertex.x - pointer.x;
        const dy = vertex.y - pointer.y;
        const distance = Math.sqrt(dx * dx + dy * dy);

        if (distance < closestDistance) {
          closestDistance = distance;
          closestVertex = vertex;
          closestObject = obj;
        }
      });
    });

    // If we found a vertex within threshold, highlight it and change cursor
    if (closestVertex) {
      FormMeasureComponent.snapTarget = {
        object: closestObject,
        vertex: closestVertex
      };
      FormMeasureComponent.addSnapHighlight();

      // Change cursor to indicate vertex snap
      canvas.defaultCursor = 'pointer';
    } else {
      // Reset to default measure cursor when not near a vertex
      canvas.defaultCursor = 'crosshair';
    }
  },

  /**
   * Add highlight to the current snap target
   */
  addSnapHighlight: function () {
    if (FormMeasureComponent.snapTarget) {
      // Match the circle size with the vertex control size
      const size = 20 + 5; // Use standard vertex control size plus padding
      const radius = size / 2;
      const zoomFactor = canvas.getZoom() || 1;

      // Create a hollow circle to indicate snap target
      FormMeasureComponent.snapHighlight = new fabric.Circle({
        left: FormMeasureComponent.snapTarget.vertex.x,
        top: FormMeasureComponent.snapTarget.vertex.y,
        radius: radius / zoomFactor,
        fill: 'transparent',
        stroke: '#FFFF00', // Yellow for current snap target
        strokeWidth: 2 / zoomFactor,
        selectable: false,
        evented: false,
        originX: 'center',
        originY: 'center'
      });

      // Add the highlight to the canvas
      canvas.add(FormMeasureComponent.snapHighlight);
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
    }
  },

  /**
   * Add highlight for the first selected vertex
   */
  addFirstVertexHighlight: function () {
    if (FormMeasureComponent.firstVertex) {
      // Match the circle size with the vertex control size
      const size = 20 + 5; // Use standard vertex control size plus padding
      const radius = size / 2;
      const zoomFactor = canvas.getZoom() || 1;

      // Create a hollow circle to indicate first vertex
      FormMeasureComponent.firstVertexHighlight = new fabric.Circle({
        left: FormMeasureComponent.firstVertex.x,
        top: FormMeasureComponent.firstVertex.y,
        radius: radius / zoomFactor,
        fill: 'transparent',
        stroke: '#FF00FF', // Magenta for first vertex
        strokeWidth: 2 / zoomFactor,
        selectable: false,
        evented: false,
        originX: 'center',
        originY: 'center'
      });

      // Add the highlight to the canvas
      canvas.add(FormMeasureComponent.firstVertexHighlight);
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
    }
  },

  /**
   * Clear all snap highlights
   */
  clearSnapHighlight: function () {
    if (FormMeasureComponent.snapHighlight) {
      canvas.remove(FormMeasureComponent.snapHighlight);
      FormMeasureComponent.snapHighlight = null;
    }

    if (FormMeasureComponent.firstVertexHighlight) {
      canvas.remove(FormMeasureComponent.firstVertexHighlight);
      FormMeasureComponent.firstVertexHighlight = null;
    }

  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
  },  /**
   * Create dimension lines to visualize the measurement
   */
  createDimensionLines: function (point1, point2, deltaX, deltaY, distance) {
    // Initialize the array if it doesn't exist
    if (!FormMeasureComponent.dimensionLines) {
      FormMeasureComponent.dimensionLines = [];
    }

    // Clear any existing dimension lines
    FormMeasureComponent.clearDimensionLines();

    // Fixed offset distances for engineering dimension style (matching draw.js)
    const offsetDistance = 30; // Fixed pixel offset for dimension line

    // Scale adjustments based on zoom
    const zoom = canvas.getZoom() || 1;
    const lineWidth = 1 / zoom;         // Thinner lines for engineering style
    const fontSize = 15 / zoom;         // Fixed 15px font size
    const arrowSize = 12 / zoom;        // Size of dimension arrows
    const extensionLineLength = 8 / zoom; // Length of extension lines

    // Add X dimension line if there's a horizontal difference
    if (Math.abs(deltaX) > 0) {
      // Position of dimension line, offset from point1
      const dimLineY = point1.y - offsetDistance / zoom;

      // Extension lines (vertical lines from points to dimension line)
      FormMeasureComponent.dimensionLines.push(new fabric.Line(
        [point1.x, point1.y, point1.x, dimLineY - extensionLineLength],
        {
          stroke: 'green',
          strokeWidth: lineWidth,
          selectable: false,
          evented: false
        }
      ));

      FormMeasureComponent.dimensionLines.push(new fabric.Line(
        [point2.x, point2.y, point2.x, dimLineY - extensionLineLength],
        {
          stroke: 'green',
          strokeWidth: lineWidth,
          selectable: false,
          evented: false
        }
      ));

      // Main dimension line
      FormMeasureComponent.dimensionLines.push(new fabric.Line(
        [point1.x, dimLineY, point2.x, dimLineY],
        {
          stroke: 'green',
          strokeWidth: lineWidth,
          selectable: false,
          evented: false
        }
      ));

      // Add arrow endpoints
      FormMeasureComponent.addArrow(point1.x, dimLineY, 'right', 'green', arrowSize);
      FormMeasureComponent.addArrow(point2.x, dimLineY, 'left', 'green', arrowSize);

      // Calculate the midpoint for dimension text
      const midX = (point1.x + point2.x) / 2;

      // Dimension text
      FormMeasureComponent.dimensionLines.push(new fabric.Text(
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.formatDimension(deltaX, _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.xHeight),
        {
          left: midX,
          top: dimLineY - (25 / zoom),
          fontSize: fontSize,
          fill: 'green',
          fontFamily: 'Arial',
          textAlign: 'center',
          originX: 'center',
          originY: 'bottom',
          selectable: false,
          evented: false,
          stroke: '#fff',
          strokeWidth: 3 / zoom,
          paintFirst: 'stroke'
        }
      ));
    }

    // Add Y dimension line if there's a vertical difference
    if (Math.abs(deltaY) > 0) {
      // Position of dimension line, offset from point1
      const dimLineX = point1.x - offsetDistance / zoom;

      // Extension lines (horizontal lines from points to dimension line)
      FormMeasureComponent.dimensionLines.push(new fabric.Line(
        [point1.x, point1.y, dimLineX - extensionLineLength, point1.y],
        {
          stroke: 'red',
          strokeWidth: lineWidth,
          selectable: false,
          evented: false
        }
      ));

      FormMeasureComponent.dimensionLines.push(new fabric.Line(
        [point2.x, point2.y, dimLineX - extensionLineLength, point2.y],
        {
          stroke: 'red',
          strokeWidth: lineWidth,
          selectable: false,
          evented: false
        }
      ));

      // Main dimension line
      FormMeasureComponent.dimensionLines.push(new fabric.Line(
        [dimLineX, point1.y, dimLineX, point2.y],
        {
          stroke: 'red',
          strokeWidth: lineWidth,
          selectable: false,
          evented: false
        }
      ));

      // Add arrow endpoints
      FormMeasureComponent.addArrow(dimLineX, point1.y, 'down', 'red', arrowSize);
      FormMeasureComponent.addArrow(dimLineX, point2.y, 'up', 'red', arrowSize);

      // Calculate the midpoint for dimension text
      const midY = (point1.y + point2.y) / 2;

      // Dimension text
      FormMeasureComponent.dimensionLines.push(new fabric.Text(
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.formatDimension(deltaY, _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.xHeight),
        {
          left: dimLineX - 15 / zoom,
          top: midY,
          fontSize: fontSize,
          fill: 'red',
          fontFamily: 'Arial',
          textAlign: 'center',
          originX: 'center',
          originY: 'center',
          angle: -90, // Rotate for Y dimension
          selectable: false,
          evented: false,
          stroke: '#fff',
          strokeWidth: 3 / zoom,
          paintFirst: 'stroke'
        }
      ));
    }

    // Add all dimension lines to canvas
    FormMeasureComponent.dimensionLines.forEach(obj => {
      canvas.add(obj);
    });

  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
  },

  /**
   * Helper method to add arrow endpoints to dimension lines
   */
  addArrow: function (x, y, direction, color, size) {
    let points;

    switch (direction) {
      case 'right':
        points = [
          { x: x, y: y },
          { x: x + size, y: y - size / 4 },
          { x: x + size, y: y + size / 4 }
        ];
        break;
      case 'left':
        points = [
          { x: x, y: y },
          { x: x - size, y: y - size / 4 },
          { x: x - size, y: y + size / 4 }
        ];
        break;
      case 'up':
        points = [
          { x: x, y: y },
          { x: x - size / 4, y: y - size },
          { x: x + size / 4, y: y - size }
        ];
        break;
      case 'down':
        points = [
          { x: x, y: y },
          { x: x - size / 4, y: y + size },
          { x: x + size / 4, y: y + size }
        ];
        break;
    }

    // Convert points to fabric.js polygon format
    const fabricPoints = [];
    points.forEach(point => {
      fabricPoints.push({
        x: point.x,
        y: point.y
      });
    });

    // Create arrow polygon and add to dimension lines
    const arrow = new fabric.Polygon(fabricPoints, {
      fill: color,
      stroke: color,
      strokeWidth: 1,
      selectable: false,
      evented: false
    });

    FormMeasureComponent.dimensionLines.push(arrow);
  },

  /**
   * Clean up when panel is closed
   */
  MeasureHandlerOff: function () {
    FormMeasureComponent.stopMeasuring();
  }
};




/***/ }),

/***/ "./js/sidebar/sb-settings.js":
/*!***********************************!*\
  !*** ./js/sidebar/sb-settings.js ***!
  \***********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   FormSettingsComponent: () => (/* binding */ FormSettingsComponent)
/* harmony export */ });
/* harmony import */ var _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./sbGeneral.js */ "./js/sidebar/sbGeneral.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _tests_test_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../tests/test.js */ "./js/tests/test.js");
/* harmony import */ var _sb_export_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./sb-export.js */ "./js/sidebar/sb-export.js");
/* harmony import */ var _objects_build_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../objects/build.js */ "./js/objects/build.js");
/* harmony import */ var _modal_md_font_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../modal/md-font.js */ "./js/modal/md-font.js");
/* harmony import */ var _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../i18n/i18n.js */ "./js/i18n/i18n.js");
/* Settings Panel */








// Define shortcuts in a constant object
const KEYBOARD_SHORTCUTS = {
  "Arrow Keys": "Nudge Selected Object",
  "Delete": "Delete Selected Object",
  "Escape": "Cancel Action / Toggle / Close Panel",
  "Enter": "Confirm Input",
  "Tab": "Switch Vertex / Unit",
  "Ctrl + C": "Copy Selected Object",
  "Ctrl + V": "Paste Object",
  "Ctrl + Z": "Undo",
  "Ctrl + S": "Save",
  "F3": "Toggle Text Border",
  "F4": "Toggle Grid",
  "F2": "Toggle Vertices",
  "F8": "Toggle Dimension Unit",
};

let FormSettingsComponent = {

  // Initialize the settings panel
  settingsPanelInit: function () {
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.tabNum = 10;
    var parent = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.PanelInit()
    if (parent) {
      // Load saved settings if they exist
      //FormSettingsComponent.loadSettings();


      // Create a container for shortcuts
      var shortcutsContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container shortcut-list-container' }, parent);

      // Create details element for collapsible behavior
      const details = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("details", {}, shortcutsContainer);

      // Create summary element for the heading
      const summary = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("summary", { 'class': 'panel-subheading', 'style': 'cursor: pointer;' }, details);
      
      // Add heading text to summary
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createI18nNode("span", {}, summary, 'Keyboard Shortcuts', 'text');

      // Create the list element inside details
      const list = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("ul", { 'class': 'shortcut-list' }, details);

      // Loop through the shortcuts object and create list items
      for (const key in KEYBOARD_SHORTCUTS) {
        if (KEYBOARD_SHORTCUTS.hasOwnProperty(key)) {
          const description = KEYBOARD_SHORTCUTS[key];

          // Create list item
          const listItem = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("li", { 'class': 'shortcut-item' }, list);

          // Create key span
          const keySpan = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("span", { 'class': 'shortcut-key' }, listItem);
          keySpan.textContent = key;

          // Create description span
          const descriptionSpan = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("span", { 'class': 'shortcut-description' }, listItem);
          descriptionSpan.setAttribute('data-i18n', description);
          descriptionSpan.textContent = description;
        }
      }

      // Create a container for visual settings
      var visualSettingsContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);

      // App Language toggle
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('App Language', ['English', 'Chinese'], visualSettingsContainer,
        (_sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.locale === 'zh') ? 'Chinese' : 'English',
        FormSettingsComponent.toggleAppLanguage);

      // Add toggle switches for visibility settings
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Show Text Borders', ['Yes', 'No'], visualSettingsContainer,
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.showTextBorders ? 'Yes' : 'No',
        FormSettingsComponent.toggleTextBorders);

      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Show Grid', ['Yes', 'No'], visualSettingsContainer,
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.showGrid ? 'Yes' : 'No',
        FormSettingsComponent.toggleGrid);

      // Show All Vertices toggle
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Show All Vertices', ['Yes', 'No'], visualSettingsContainer,
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.showAllVertices ? 'Yes' : 'No',
        FormSettingsComponent.toggleShowAllVertices);

      // Dimension Unit toggle
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Dimension Unit', ['mm', 'sw'], visualSettingsContainer,
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.dimensionUnit || 'mm',
        FormSettingsComponent.toggleDimensionUnit);

      //GeneralHandler.createToggle('Snap to Grid', ['Yes', 'No'], visualSettingsContainer, 
      //  GeneralSettings.snapToGrid ? 'Yes' : 'No', 
      //  FormSettingsComponent.toggleSnapToGrid);

      // Create a container for canvas settings
      var canvasSettingsContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);

      // Background color picker
      const bgColorPicker = FormSettingsComponent.createColorPicker(
        'background-color',
        'Background Color',
        canvasSettingsContainer,
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.backgroundColor,
        FormSettingsComponent.changeBackgroundColor
      );

      // Grid color picker
      const gridColorPicker = FormSettingsComponent.createColorPicker(
        'grid-color',
        'Grid Color',
        canvasSettingsContainer,
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.gridColor,
        FormSettingsComponent.changeGridColor
      );

      // Grid size input
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('grid-size', 'Grid Size', canvasSettingsContainer,
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.gridSize,
        FormSettingsComponent.changeGridSize, 'input');

      // Create a container for performance settings
      var performanceSettingsContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);

      // Auto-save toggle
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Auto Save', ['Yes', 'No'], performanceSettingsContainer,
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.autoSave ? 'Yes' : 'No',
        FormSettingsComponent.toggleAutoSave);

      // Auto-save interval
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('auto-save-interval', 'Auto Save Interval (seconds)', performanceSettingsContainer,
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.autoSaveInterval,
        FormSettingsComponent.changeAutoSaveInterval, 'input');

      // Add save/reset buttons
      const buttonContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'settings-buttons-container' }, performanceSettingsContainer);

      // Manual Save button
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('manual-save', 'Save Canvas', buttonContainer, 'input',
        FormSettingsComponent.saveCanvasState, 'click');

      // Clear Saved Canvas button
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('clear-saved-canvas', 'Clear Saved Canvas', buttonContainer, 'input',
        FormSettingsComponent.clearSavedCanvas, 'click');

      // Reset settings button
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('reset-settings', 'Reset Settings', buttonContainer, 'input',
        FormSettingsComponent.resetSettings, 'click');

      // Create a container for testing
      var testingContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);


      // Add Run Tests button
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('run-tests', 'Run Tests', testingContainer, 'input',
        FormSettingsComponent.runTests, 'click');

      // Add Run Tests on Start toggle
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Run Tests on Start', ['Yes', 'No'], testingContainer,
        _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.runTestsOnStart ? 'Yes' : 'No',
        FormSettingsComponent.toggleRunTestsOnStart);

      // Apply translations for all elements created in this panel
      try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_6__.i18n.applyTranslations(parent); } catch (_) { }
    }
  },

  // Helper method to create a color picker input
  createColorPicker: function (id, label, parent, defaultValue, changeCallback) {
    const container = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-container' }, parent);
    const labelEl = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'placeholder', 'for': id }, container);
    // Apply i18n to label
    labelEl.setAttribute('data-i18n', label);
    labelEl.innerText = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_6__.i18n.t(label);

    const input = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("input", {
      'type': 'color',
      'class': 'color-picker',
      'id': id,
      'value': defaultValue
    }, container, changeCallback, 'input');

    return input;
  },

  // Visual settings toggle handlers
  toggleAppLanguage: function (button) {
    const value = button.getAttribute('data-value');
    const locale = (value === 'Chinese') ? 'zh' : 'en';
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.locale = locale;
    FormSettingsComponent.saveSettings();
    try {
      _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_6__.i18n.setLocale(locale);
      _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_6__.i18n.applyTranslations(document);
    } catch (e) {
      console.warn('Failed to apply translations:', e);
    }
  },

  toggleTextBorders: function (button) {
    const value = button.getAttribute('data-value') === 'Yes';
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.showTextBorders = value;
    FormSettingsComponent.applyTextBorderSettings();
    FormSettingsComponent.saveSettings(); // Auto-save settings
  },

  toggleGrid: function (button) {
    const value = button.getAttribute('data-value') === 'Yes';
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.showGrid = value;
    FormSettingsComponent.applyGridSettings();
    FormSettingsComponent.saveSettings(); // Auto-save settings
  },

  toggleSnapToGrid: function (button) {
    const value = button.getAttribute('data-value') === 'Yes';
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.snapToGrid = value;
    // Apply snap to grid setting to canvas
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.snap_pts = value ? FormSettingsComponent.generateSnapPoints() : [];
    FormSettingsComponent.saveSettings(); // Auto-save settings
  },

  // Toggle handler for Show All Vertices
  toggleShowAllVertices: function (button) {
    const value = button.getAttribute('data-value') === 'Yes';
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.showAllVertices = value;
    FormSettingsComponent.applyVertexDisplaySettings();
    FormSettingsComponent.saveSettings(); // Auto-save settings
  },

  // Toggle handler for Dimension Unit
  toggleDimensionUnit: function (button) {
    const value = button.getAttribute('data-value');
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.dimensionUnit = value;

    // Refresh any visible dimension displays
    FormSettingsComponent.refreshDimensionDisplays();

    FormSettingsComponent.saveSettings(); // Auto-save settings
  },

  // Toggle handler for Run Tests on Start
  toggleRunTestsOnStart: function (button) {
    const value = button.getAttribute('data-value') === 'Yes';
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.runTestsOnStart = value;
    FormSettingsComponent.saveSettings(); // Auto-save settings
  },

  // Canvas settings change handlers
  changeBackgroundColor: function (event) {
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.backgroundColor = event.target.value;
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.backgroundColor = event.target.value;
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
    FormSettingsComponent.saveSettings(); // Auto-save settings
  },

  changeGridColor: function (event) {
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.gridColor = event.target.value;
    FormSettingsComponent.applyGridSettings();
    FormSettingsComponent.saveSettings(); // Auto-save settings
  },

  changeGridSize: function (event) {
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.gridSize = parseInt(event.target.value) || 20;
    FormSettingsComponent.applyGridSettings();
    if (_sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.snapToGrid) {
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.snap_pts = FormSettingsComponent.generateSnapPoints();
    }
    FormSettingsComponent.saveSettings(); // Auto-save settings
  },

  // Performance settings handlers
  toggleAutoSave: function (button) {
    const value = button.getAttribute('data-value') === 'Yes';
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.autoSave = value;

    // Start or stop autosave timer
    if (value) {
      FormSettingsComponent.startAutoSaveTimer();
    } else {
      FormSettingsComponent.stopAutoSaveTimer();
    }
    FormSettingsComponent.saveSettings(); // Auto-save settings
  },

  changeAutoSaveInterval: function (event) {
    const interval = parseInt(event.target.value) || 60;
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.autoSaveInterval = interval;

    // Restart timer if enabled
    if (_sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.autoSave) {
      FormSettingsComponent.stopAutoSaveTimer();
      FormSettingsComponent.startAutoSaveTimer();
    }
    FormSettingsComponent.saveSettings(); // Auto-save settings
  },

  // Auto-save timer methods
  autoSaveTimerId: null,

  startAutoSaveTimer: function () {
    if (FormSettingsComponent.autoSaveTimerId !== null) {
      clearInterval(FormSettingsComponent.autoSaveTimerId);
    }

    const interval = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.autoSaveInterval * 1000;
    FormSettingsComponent.autoSaveTimerId = setInterval(() => {
      FormSettingsComponent.saveCanvasState();
    }, interval);
  },

  stopAutoSaveTimer: function () {
    if (FormSettingsComponent.autoSaveTimerId !== null) {
      clearInterval(FormSettingsComponent.autoSaveTimerId);
      FormSettingsComponent.autoSaveTimerId = null;
    }
  },

  simpleStringify: function (object) {
    const simpleObject = {};
    for (const prop in object) {
      if (!object.hasOwnProperty(prop)) {
        continue;
      }
      if (typeof (object[prop]) == 'object') {
        continue;
      }
      if (typeof (object[prop]) == 'function') {
        continue;
      }
      simpleObject[prop] = object[prop];
    }
    return JSON.stringify(simpleObject);
  },

  saveCanvasState: function () {
    // Save the current canvas state to localStorage
    try {
      // Save canvas properties (background color, etc.)
      const canvasJSON = FormSettingsComponent.simpleStringify(_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas);
      localStorage.setItem('canvasState', canvasJSON);

      // Save canvas objects (shapes, texts, etc.)
      const canvasObjects = _sb_export_js__WEBPACK_IMPORTED_MODULE_3__.FormExportComponent.exportCanvasToJSON(false);
      localStorage.setItem('canvasObjects', canvasObjects);

      console.log('Canvas state and objects auto-saved', new Date());
      // Optionally, provide user feedback about the save
      // For example, using a toast notification or a status message
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.showToast("Canvas state saved!");
    } catch (e) {
      console.error('Failed to auto-save canvas state', e);
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.showToast("Error saving canvas state.", "error");
    }
  },

  clearSavedCanvas: function () {
    try {
      localStorage.removeItem('canvasState');
      localStorage.removeItem('canvasObjects');
      console.log('Saved canvas state and objects cleared from localStorage');
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.showToast("Cleared saved canvas data!");
      // Optionally, you might want to reload or reset the canvas view here
      // For example, by reloading the page or resetting the canvas to a default state
      // CanvasGlobals.canvas.clear(); // Example: Clears the canvas
      // FormSettingsComponent.loadSettings(); // Reloads initial settings without saved state
    } catch (e) {
      console.error('Failed to clear saved canvas state', e);
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.showToast("Error clearing saved canvas data.", "error");
    }
  },

  // Settings persistence
  saveSettings: function () {
    try {
      // Save settings
      localStorage.setItem('appSettings', JSON.stringify(_sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings));

      // Also save canvas state
      //FormSettingsComponent.saveCanvasState();

      console.log('Settings and canvas state saved to localStorage');
    } catch (e) {
      console.error('Failed to save settings', e);
    }
  },

  loadCanvasState: async function (externalSignData = null) {
    try {
      if (externalSignData) {
        let objectsToLoad = externalSignData;
        if (externalSignData.meta && Array.isArray(externalSignData.objects)) {
          console.log('Importing Firebase JSON with metadata:', externalSignData.meta);
          objectsToLoad = externalSignData.objects;
        }

        if (typeof _objects_build_js__WEBPACK_IMPORTED_MODULE_4__.buildObjectsFromJSON === 'function') {
          await (0,_objects_build_js__WEBPACK_IMPORTED_MODULE_4__.buildObjectsFromJSON)(objectsToLoad, _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas);
        } else {
          console.error('buildObjectsFromJSON function is not available.');
        }

        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
        console.log('Canvas state loaded from Firebase data');
        return true;
      }

      const savedCanvas = localStorage.getItem('canvasState');
      if (savedCanvas) {
        const parsedCanvas = JSON.parse(savedCanvas);

        // Apply saved canvas properties
        //for (const prop in parsedCanvas) {
        //  if (CanvasGlobals.canvas.hasOwnProperty(prop) && typeof CanvasGlobals.canvas[prop] !== 'function' && CanvasGlobals.canvas[prop] !== 'width' && CanvasGlobals.canvas[prop] !== 'height' ) {
        //    CanvasGlobals.canvas[prop] = parsedCanvas[prop];
        //  }
        //}

        // Reload canvas objects if they're saved separately
        if (localStorage.getItem('canvasObjects')) {
          try {
            const jsonData = JSON.parse(localStorage.getItem('canvasObjects'));
            if (jsonData.meta && Array.isArray(jsonData.objects)) {
              console.log("Importing JSON with metadata:", jsonData.meta);
              const objectsToLoad = jsonData.objects;
              if (typeof _objects_build_js__WEBPACK_IMPORTED_MODULE_4__.buildObjectsFromJSON === 'function') {
                await (0,_objects_build_js__WEBPACK_IMPORTED_MODULE_4__.buildObjectsFromJSON)(objectsToLoad, _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas);
              } else {
                console.error("buildObjectsFromJSON function is not available.");
                // As a fallback, try Fabric's loadFromJSON if custom deserialization isn't critical
                // Note: This will not handle custom object types or re-linking logic.
                // CanvasGlobals.canvas.loadFromJSON(jsonString, () => {
                //   CanvasGlobals.canvas.renderAll();
                //   console.log("Canvas loaded from JSON using Fabric.js default loader.");
                // });
              }
            } else {
              // Assume old format (array of objects) for backward compatibility
              const objectsToLoad = jsonData;
              console.log("Importing JSON in old format (array of objects).");
              if (typeof _objects_build_js__WEBPACK_IMPORTED_MODULE_4__.buildObjectsFromJSON === 'function') {
                await (0,_objects_build_js__WEBPACK_IMPORTED_MODULE_4__.buildObjectsFromJSON)(objectsToLoad, _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas);
              } else {
                console.error("buildObjectsFromJSON function is not available.");
              }
            }
          } catch (e) {
            console.error('Failed to load canvas objects', e);
          }
        }

        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
        console.log('Canvas state loaded from localStorage');
        return true;
      }
      return false;
    } catch (e) {
      console.error('Failed to load canvas state', e);
      return false;
    }
  },

  loadSettings: async function () {
    try {
      const savedSettings = localStorage.getItem('appSettings');
      if (savedSettings) {
        const parsedSettings = JSON.parse(savedSettings);

        // Update existing settings instead of reassigning
        Object.keys(parsedSettings).forEach(key => {
          if (_sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.hasOwnProperty(key)) {
            _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings[key] = parsedSettings[key];
          }
        });

        // Apply loaded settings to the canvas
        FormSettingsComponent.applyAllSettings();
        FormSettingsComponent.updateSettingsUI();

        // Run tests if enabled
        if (_sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.runTestsOnStart) {
          FormSettingsComponent.runTests();
        }

      }
      this.startAutoSaveTimer()
    } catch (e) {
      console.error('Failed to load settings', e);
    }
  },
  resetSettings: function () {
    // Default settings
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.resetSetting();

    // Reset font manager settings
    try {
      _modal_md_font_js__WEBPACK_IMPORTED_MODULE_5__.FontPriorityManager.resetToDefaults();
      console.log('Font manager settings reset successfully');
    } catch (error) {
      console.error('Error resetting font manager settings:', error);
    }

    // Apply all settings
    FormSettingsComponent.applyAllSettings();

    // Update UI
    FormSettingsComponent.updateSettingsUI();

    // Save the default settings and clear canvas objects
    //localStorage.removeItem('canvasObjects');
    FormSettingsComponent.saveSettings();

    console.log('Settings reset to defaults');
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.showToast("Settings reset to defaults!");
  },

  // Apply settings to the canvas
  applyAllSettings: function () {
    FormSettingsComponent.applyTextBorderSettings();
    FormSettingsComponent.applyGridSettings();
    FormSettingsComponent.applyVertexDisplaySettings();

    // Apply background color
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.backgroundColor = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.backgroundColor;

    // Apply snap to grid
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.snap_pts = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.snapToGrid ?
      FormSettingsComponent.generateSnapPoints() : [];

    // Handle auto-save
    if (_sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.autoSave) {
      FormSettingsComponent.startAutoSaveTimer();
    } else {
      FormSettingsComponent.stopAutoSaveTimer();
    }

    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
  },

  applyTextBorderSettings: function () {
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getObjects().forEach(obj => {
      if (obj.functionalType === 'Text') {
        obj.txtFrameList.forEach(frame => {
          frame.set('stroke', _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.showTextBorders ? obj.color : 'rgba(0,0,0,0)');
        })
      }
    });
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
  },


  applyGridSettings: function () {
    // This requires rebuilding the grid with new settings
    (0,_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.DrawGrid)(); // This is the existing function that draws the grid
  },

  applyVertexDisplaySettings: function () {
    // Update the active object if there is one
    const activeObject = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.getActiveObject();
    if (activeObject && activeObject instanceof fabric.BaseGroup) {
      activeObject.drawVertex(false);
    }

    // Also update all BaseGroup objects on the canvas to ensure 
    // they respect the setting when selected later
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvasObject.forEach(obj => {
      if (obj instanceof fabric.BaseGroup && obj.basePolygon) {
        obj.drawVertex(false);
      }
    });

    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
  },

  generateSnapPoints: function () {
    // Generate snap points based on grid size
    const gridSize = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.gridSize;
    const points = [];

    // Generate grid snap points within the current viewport
    const bounds = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.calcViewportBoundaries();
    const xMin = Math.floor(bounds.tl.x / gridSize) * gridSize;
    const xMax = Math.ceil(bounds.br.x / gridSize) * gridSize;
    const yMin = Math.floor(bounds.tl.y / gridSize) * gridSize;
    const yMax = Math.ceil(bounds.br.y / gridSize) * gridSize;

    for (let x = xMin; x <= xMax; x += gridSize) {
      for (let y = yMin; y <= yMax; y += gridSize) {
        points.push({ x, y });
      }
    }

    return points;
  },

  updateSettingsUI: function () {
    // Update UI elements to reflect current settings
    const updateToggle = (id, value) => {
      const container = document.getElementById(id);
      if (!container) return;

      const buttons = container.querySelectorAll('.toggle-button');
      buttons.forEach(button => {
        const isActive = (button.getAttribute('data-value') === 'Yes') === value;
        if (isActive) {
          button.classList.add('active');
          container.selected = button;
        } else {
          button.classList.remove('active');
        }
      });
    };

    // Update toggles
    updateToggle('Show Text Borders-container', _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.showTextBorders);
    updateToggle('Show Object Borders-container', _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.showObjectBorders);
    updateToggle('Show Grid-container', _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.showGrid);
    updateToggle('Snap to Grid-container', _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.snapToGrid);
    updateToggle('Show All Vertices-container', _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.showAllVertices);
    updateToggle('Auto Save-container', _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.autoSave);
    updateToggle('Run Tests on Start-container', _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.runTestsOnStart);

    // Update App Language toggle
    const langContainer = document.getElementById('App Language-container');
    if (langContainer) {
      const buttons = langContainer.querySelectorAll('.toggle-button');
      const desired = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.locale === 'zh' ? 'Chinese' : 'English';
      buttons.forEach(btn => {
        const isActive = btn.getAttribute('data-value') === desired;
        btn.classList.toggle('active', isActive);
        if (isActive) { langContainer.selected = btn; }
      });
    }

    // Handle dimension unit toggle separately (it's not a boolean)
    const updateDimensionUnit = (id, value) => {
      const container = document.getElementById(id);
      if (!container) return;

      const buttons = container.querySelectorAll('.toggle-button');
      buttons.forEach(button => {
        const isActive = button.getAttribute('data-value') === value;
        if (isActive) {
          button.classList.add('active');
          container.selected = button;
        } else {
          button.classList.remove('active');
        }
      });
    };
    updateDimensionUnit('Dimension Unit-container', _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.dimensionUnit);

    // Update color inputs
    const bgColorInput = document.getElementById('background-color');
    if (bgColorInput) bgColorInput.value = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.backgroundColor;

    const gridColorInput = document.getElementById('grid-color');
    if (gridColorInput) gridColorInput.value = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.gridColor;

    // Update number inputs
    const gridSizeInput = document.getElementById('grid-size');
    if (gridSizeInput) gridSizeInput.value = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.gridSize;

    const autoSaveIntervalInput = document.getElementById('auto-save-interval');
    if (autoSaveIntervalInput) autoSaveIntervalInput.value = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.autoSaveInterval;
  },

  // Run tests function
  runTests: function () {
    if (typeof _tests_test_js__WEBPACK_IMPORTED_MODULE_2__.runTests === 'function' && typeof _tests_test_js__WEBPACK_IMPORTED_MODULE_2__.testToRun !== 'undefined') {
      console.clear();
      console.log("Starting tests from settings panel...");
      (0,_tests_test_js__WEBPACK_IMPORTED_MODULE_2__.runTests)(_tests_test_js__WEBPACK_IMPORTED_MODULE_2__.testToRun);
    } else {
      console.error("Test runner not available. Make sure test.js is loaded.");
    }
  },

  // Refresh dimension displays when unit setting changes
  refreshDimensionDisplays: function () {
    // Refresh lock icon dimensions
    if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvasObject) {
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvasObject.forEach(obj => {
        if (obj.lockIcons && obj.lockIcons.length > 0) {
          obj.lockIcons.forEach(lockIcon => {
            if (lockIcon.refreshDimensions && typeof lockIcon.refreshDimensions === 'function') {
              lockIcon.refreshDimensions();
            }
          });
        }
      });
    }

    // Force canvas re-render
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
  }
};


// Export the FormSettingsComponent for use in other files


/***/ }),

/***/ "./js/sidebar/sb-template.js":
/*!***********************************!*\
  !*** ./js/sidebar/sb-template.js ***!
  \***********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   FormTemplateComponent: () => (/* binding */ FormTemplateComponent)
/* harmony export */ });
/* harmony import */ var _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./sbGeneral.js */ "./js/sidebar/sbGeneral.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _objects_text_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../objects/text.js */ "./js/objects/text.js");
/* harmony import */ var _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../objects/symbols.js */ "./js/objects/symbols.js");
/* harmony import */ var _objects_border_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../objects/border.js */ "./js/objects/border.js");
/* harmony import */ var _objects_divider_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../objects/divider.js */ "./js/objects/divider.js");
/* harmony import */ var _objects_template_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../objects/template.js */ "./js/objects/template.js");
/* harmony import */ var _objects_mainRoute_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../objects/mainRoute.js */ "./js/objects/mainRoute.js");
/* harmony import */ var _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../objects/sideRoute.js */ "./js/objects/sideRoute.js");
/* harmony import */ var _objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ../objects/anchor.js */ "./js/objects/anchor.js");
/* Template Signs Panel */











const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas;
const canvasObject = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvasObject;


let FormTemplateComponent = {
  // Template gallery with predefined complex sign layouts
  templates: {
    'Flag Sign': {
      description: 'Standard flag type sign with destinations and chevron',
      thumbnail: function () {
        // Load SVG image from images folder in a standardized thumbnail container
        return `<div class="template-thumbnail"><img src="/images/flag.svg" alt="Flag Sign" /></div>`;
      }
    },
    'Stack Sign': {
      description: 'Stacked road sign with multiple destinations',
      thumbnail: function () {
        // Load SVG image from images folder in a standardized thumbnail container
        return `<div class="template-thumbnail"><img src="/images/stack.svg" alt="Stack Sign" /></div>`;
      }
    },
    'Lane Sign': {
      description: 'Exit sign showing multiple lanes with directions',
      thumbnail: function () {
        // Load SVG image from images folder in a standardized thumbnail container
        return `<div class="template-thumbnail"><img src="/images/lane.svg" alt="Lane Sign" /></div>`;
      }
    },
    'Roundabout Sign': {
      description: 'Sign showing directions at a roundabout',
      thumbnail: function () {
        // Load SVG image from images folder in a standardized thumbnail container
        return `<div class="template-thumbnail"><img src="/images/roundabout.svg" alt="Roundabout Sign" /></div>`;
      }
    }, 'Spiral Roundabout Sign': {
      description: 'Sign showing directions at a spiral roundabout with multiple destinations',
      thumbnail: function () {
        // Load SVG image from images folder in a standardized thumbnail container
        return `<div class="template-thumbnail"><img src="/images/spiral.svg" alt="Spiral Roundabout Sign" /></div>`;
      }
    }, 'Gantry Sign': {
      description: 'Overhead gantry sign with destinations in separate compartments',
      thumbnail: function () {
        // Load SVG image from images folder in a standardized thumbnail container
        return `<div class="template-thumbnail"><img src="/images/gantry.svg" alt="Gantry Sign" /></div>`;
      }
    },
    'Diverge Sign': {
      description: 'Complex interchange direction sign with multiple destinations',
      thumbnail: function () {
        // Load SVG image from images folder in a standardized thumbnail container
        return `<div class="template-thumbnail"><img src="/images/diverge.svg" alt="Diverge Sign" /></div>`;
      }
    },

  },

  // Currently selected template
  selectedTemplate: null,
    /**
   * Initialize the template panel
   */  templatePanelInit: function () {
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.tabNum = 6;
    var parent = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.PanelInit();

    if (parent) {
      // Create heading for templates
      const templateHeader = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createI18nNode("div", { 'class': 'placeholder' }, templateHeader, 'Select Template', 'text');

      // Create templates grid - using FormTemplateComponent as context
      FormTemplateComponent.createTemplatesGrid(parent);
    }
  },
  /**
   * Create a grid of template thumbnails
   */  createTemplatesGrid: function (parent) {
    // Create a container for the templates using the symbols-grid class for consistency
    const templatesContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'symbols-grid' }, parent);

    // Add each template as an SVG button that directly deploys the template
    Object.keys(this.templates).forEach(templateName => {
      const template = this.templates[templateName];

      // Create the SVG content
      const svgContent = template.thumbnail();

      // Create a container for the button
      const buttonContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'template-container' }, templatesContainer);

      // Create the button using GeneralHandler's SVG button creator for consistency
      const templateBtn = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createSVGButton(
        templateName,
        svgContent,
        buttonContainer,
        'template', // Use 'template' style instead of 'border'
        // Direct deployment on click - no selection step needed
        function () {
          FormTemplateComponent.createTemplateSign(templateName);
        },
        'click'
      );

      // Add description tooltip
      templateBtn.setAttribute('data-i18n-tooltip', template.description);
      templateBtn.title = template.description;
    });
  },

  /**
   * Select a template when its button is clicked
   */
  selectTemplate: function (event) {
    // Remove active class from all template buttons
    const templateButtons = document.querySelectorAll('.template-button');
    templateButtons.forEach(button => button.classList.remove('active'));

    // Add active class to clicked button
    event.currentTarget.classList.add('active');

    // Store selected template name
    this.selectedTemplate = event.currentTarget.id;

    console.log(`Selected template: ${this.selectedTemplate}`);
  },

  /**
   * Insert the selected template into the canvas
   */
  insertSelectedTemplate: function () {
    if (!FormTemplateComponent.selectedTemplate) {
      console.log('No template selected');
      return;
    }

    const templateName = FormTemplateComponent.selectedTemplate;
    const xHeight = parseInt(document.getElementById('input-xHeight').value);
    const color = document.getElementById('Message Colour-container').selected.getAttribute('data-value');

    // Clear any active selection
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.canvas.discardActiveObject();

    // Call the appropriate template creation function
    FormTemplateComponent.createTemplateSign(templateName, xHeight, color);
  },
  /**
   * Create a template sign based on the selected template
   */
  createTemplateSign: function (templateName, x = null, y = null) {
    // Each template will specify its own xHeight and color
    console.log(`Creating template: ${templateName}`);

    const vpt = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.CenterCoord();
    const centerX = x || vpt.x;
    const centerY = y || vpt.y; switch (templateName) {
      case 'Flag Sign':
        return this.createBasicGantry(centerX, centerY, 100, 'white');

      case 'Stack Sign':
        return this.createStackSign(centerX, centerY, 100, 'white');

      case 'Roundabout Sign':
        return this.createRoundaboutDirections(centerX, centerY, 100, 'white');

      case 'Diverge Sign':
        return this.createInterchangeDirections(centerX, centerY, 250, 'white');

      case 'Lane Sign':
        return this.createMultiLaneExit(centerX, centerY, 100, 'white');

      case 'Spiral Roundabout Sign':
        return this.createSpiralRoundaboutSign(centerX, centerY, 100, 'white');

      case 'Gantry Sign':
        return this.createGantrySign(centerX, centerY, 250, 'white');

      default:
        console.log(`Template ${templateName} not implemented`);
    }
  },
  /**
   * Create a basic overhead gantry sign
   */
  createBasicGantry: function (centerX, centerY, xHeight, color) {
    try {
      // Create first line text objects
      const Line1English = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Tai Kok Tsui(W)',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'White',
        left: centerX - 300,
        top: centerY - 150
      });

      const Line1Chinese = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '大角咀(西)',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'White',
        left: centerX - 300,
        top: centerY - 50
      });

      // Create WHC symbol for first line
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'WHC',
        left: centerX + 150,
        top: centerY - 100,
        xHeight: xHeight,
        symbolAngle: 0,
        color: 'White'
      });
      const Line1Symbol = canvasObject[canvasObject.length - 1];

      // Create second line text objects
      const Line2English = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Hong Kong(W)',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'White',
        left: centerX - 300,
        top: centerY + 50
      });

      const Line2Chinese = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '香港(西)',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'White',
        left: centerX - 300,
        top: centerY + 150
      });

      // Create Airport symbol for second line
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'Airport',
        left: centerX + 150,
        top: centerY + 100,
        xHeight: xHeight,
        symbolAngle: 90,
        color: 'White'
      });
      const Line2Symbol = canvasObject[canvasObject.length - 1];

      // Create third line text objects
      const Line3English = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Lantau',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'White',
        left: centerX - 300,
        top: centerY + 250
      });

      const Line3Chinese = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '大嶼山',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'White',
        left: centerX - 300,
        top: centerY + 350
      });

      // Anchor text objects in pairs
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(Line1English, Line1Chinese, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(Line1Chinese, Line2English, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(Line2English, Line2Chinese, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(Line2Chinese, Line3English, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(Line3English, Line3Chinese, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(Line2English, Line1Symbol, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E3',
        spacingX: 50,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(Line3English, Line2Symbol, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E3',
        spacingX: '',
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(Line3Chinese, Line2Symbol, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E3',
        spacingX: 50,
        spacingY: ''
      });

      // Create an overall border containing all components
      const allObjects = [
        Line1English, Line1Chinese, Line1Symbol,
        Line2English, Line2Chinese, Line2Symbol,
        Line3English, Line3Chinese,
      ];

      const borderGroup = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.BorderGroupCreate(
        'flagRight',  // Changed from 'stack' to 'flag' type border
        allObjects,
        allObjects,
        null,
        null,
        { xHeight: xHeight, borderType: 'flag', colorType: 'Blue Background' }
      );

      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
      console.log('Basic Gantry template created successfully');

      // Return dimensions and position
      return {
        width: borderGroup.width,
        height: borderGroup.height,
        left: borderGroup.left,
        top: borderGroup.top
      };

    } catch (error) {
      console.error('Error creating Basic Gantry template:', error);
      return null; // Return null on error
    }
  },

  /**
   * Create a gantry sign with separate compartments
   */
  createGantrySign: function (centerX, centerY, xHeight, color) {
    try {
      // Create text objects for the left compartment (Mong Kok)
      const leftEngText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Mong Kok',
        xHeight: xHeight,
        font: 'TransportHeavy',
        color: 'black',
        left: centerX - 500,
        top: centerY - 200
      });

      const leftChiText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '旺角',
        xHeight: xHeight,
        font: 'TransportHeavy',
        color: 'black',
        left: centerX - 500,
        top: centerY - 50
      });

      // Create gantry arrow for left compartment
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'GantryArrow',
        left: centerX - 500,
        top: centerY + 200,
        xHeight: xHeight,
        symbolAngle: 0,
        color: 'white'
      });
      const leftArrow = canvasObject[canvasObject.length - 1];

      // Create text objects for the right compartment (Tai Kok Tsui and Sha Tin)
      const rightEngText1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Tai Kok Tsui',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'white',
        left: centerX + 500,
        top: centerY - 300
      });

      const rightEngText2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Sha Tin',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'white',
        left: centerX + 500,
        top: centerY
      });

      const rightChiText2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '大角咀及沙田',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'white',
        left: centerX + 500,
        top: centerY + 150
      });

      // Create stack arrow for right compartment
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'GantryArrow',
        left: centerX + 500,
        top: centerY + 300,
        xHeight: xHeight,
        symbolAngle: 0,
        color: 'white'
      });
      const rightArrow = canvasObject[canvasObject.length - 1];

      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'Exit',
        left: centerX + 500,
        top: centerY + 300,
        xHeight: xHeight,
        symbolAngle: 0,
        color: 'white'
      });
      const exitSymmbol = canvasObject[canvasObject.length - 1];

      const exitText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '2',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'white',
        left: centerX + 500,
        top: centerY + 150
      });

      // Anchor left side objects vertically
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftEngText, leftChiText, {
        vertexIndex1: 'E2',
        vertexIndex2: 'E6',
        spacingX: 0,
        spacingY: 0
      });

      // Create yellow border for left compartment
      const leftCompartmentObjects = [leftEngText, leftChiText];
      const leftBorder = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.BorderGroupCreate(
        'stack',
        leftCompartmentObjects,
        leftCompartmentObjects,
        null,
        null,
        { xHeight: xHeight, borderType: 'stack', colorType: 'Yellow Background' }
      );

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftBorder, leftArrow, {
        vertexIndex1: 'E2',
        vertexIndex2: 'E6',
        spacingX: '',
        spacingY: xHeight / 4
      });

      // Create an overall green border containing all components (divider will be added after)
      const allObjects = [
        leftBorder,
        rightEngText1, rightEngText2, rightChiText2, rightArrow
      ];

      const borderGroup = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.BorderGroupCreate(
        'stack',
        allObjects,
        allObjects,
        null,
        null,
        { xHeight: xHeight, borderType: 'stack', colorType: 'Green Background' }
      );

      // Create vertical divider AFTER border so sizing logic runs inside border.assignWidthToDivider
      const verticalDivider = new _objects_divider_js__WEBPACK_IMPORTED_MODULE_5__.DividerObject({ dividerType: 'VDivider', borderGroup: borderGroup, xHeight: xHeight, colorType: 'Green Background' });

      // Anchor leftBorder (left side) to divider and divider to one of the right side objects for relative positioning if not already constrained
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftBorder, verticalDivider,
        {
          vertexIndex1: 'V1',
          vertexIndex2: 'E4',
          spacingX: 2.5 * xHeight / 4,
          spacingY: ''
        });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(verticalDivider, rightChiText2,
        {
          vertexIndex1: 'E1',
          vertexIndex2: 'V6',
          spacingX: 2.5 * xHeight / 4,
          spacingY: ''
        });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftBorder, rightChiText2, {
        vertexIndex1: 'E6',
        vertexIndex2: 'E6',
        spacingX: '',
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightChiText2, rightEngText2, {
        vertexIndex1: 'E6',
        vertexIndex2: 'E2',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightEngText2, rightEngText1, {
        vertexIndex1: 'E6',
        vertexIndex2: 'E2',
        spacingX: 0,
        spacingY: 0
      });

      // Anchor Gantry Arrows
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(borderGroup, leftArrow, {
        vertexIndex1: 'E2',
        vertexIndex2: 'C2',
        spacingX: 0,
        spacingY: ''
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(borderGroup, rightArrow, {
        vertexIndex1: 'E2',
        vertexIndex2: 'C6',
        spacingX: 0,
        spacingY: ''
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftArrow, rightArrow, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E1',
        spacingX: '',
        spacingY: 0
      });

      // Anchor exit objects 
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(borderGroup, exitSymmbol, {
        vertexIndex1: 'E7',
        vertexIndex2: 'E1',
        spacingX: xHeight * 1 / 4,
        spacingY: -xHeight * 1 / 4
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(exitSymmbol, exitText, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E3',
        spacingX: xHeight * 0.5 / 4,
        spacingY: -xHeight * 0.2 / 4
      });

      const exitBorder = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.BorderGroupCreate(
        'exit',
        [exitText, exitSymmbol],
        [exitText, exitSymmbol],
        null,
        null,
        { xHeight: xHeight, borderType: 'exit', colorType: 'Green Background' }
      );

      //
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
      console.log('Gantry Sign template created successfully');

      // Return dimensions and position of the main border group
      return {
        width: borderGroup.width,
        height: borderGroup.height,
        left: borderGroup.left,
        top: borderGroup.top
      };

    } catch (error) {
      console.error('Error creating Gantry Sign template:', error);
      return null; // Return null on error
    }
  },

  /**
   * Create a stacked road sign with multiple destinations
   */
  createStackSign: function (centerX, centerY, xHeight, color) {
    try {
      // Create destination texts
      const topEng1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Pok Fu Lam',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'White',
        left: centerX - 300,
        top: centerY - 1150
      });

      const topChi1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '薄扶林',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'White',
        left: centerX - 250,
        top: centerY - 200
      });

      const topEng2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Central',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'White',
        left: centerX - 250,
        top: centerY - 300
      });

      const topChi2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '中區',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'White',
        left: centerX - 250,
        top: centerY - 200
      });

      const midDestinationText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Cyberport',
        xHeight: xHeight,
        font: 'TransportHeavy',
        color: 'Black',
        left: centerX - 250,
        top: centerY
      });

      const midChineseText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '數碼港',
        xHeight: xHeight,
        font: 'TransportHeavy',
        color: 'Black',
        left: centerX - 250,
        top: centerY + 100
      });

      const botDestinationText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Wa Fu Estate',
        xHeight: xHeight,
        font: 'TransportHeavy',
        color: 'Black',
        left: centerX - 250,
        top: centerY + 300
      });

      const botChineseText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '華富邨',
        xHeight: xHeight,
        font: 'TransportHeavy',
        color: 'Black',
        left: centerX - 250,
        top: centerY + 400
      });

      // Create arrows
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'StackArrow',
        left: centerX + 200,
        top: centerY - 750,
        xHeight: xHeight,
        symbolAngle: 0,
        color: 'White'
      });
      const topArrow = canvasObject[canvasObject.length - 1];

      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'StackArrow',
        left: centerX + 200,
        top: centerY + 50,
        xHeight: xHeight,
        symbolAngle: 0,
        color: 'Black'
      });
      const midArrow = canvasObject[canvasObject.length - 1];

      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'StackArrow',
        left: centerX + 200,
        top: centerY + 350,
        xHeight: xHeight,
        symbolAngle: -90,
        color: 'Black'
      });
      const botArrow = canvasObject[canvasObject.length - 1];

      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'Airport',
        left: centerX + 200,
        top: centerY - 664,
        xHeight: xHeight,
        symbolAngle: 0,
        color: 'White'
      });
      const airport = canvasObject[canvasObject.length - 1];

      // Anchor text objects in pairs
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(midDestinationText, midChineseText, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(midDestinationText, midArrow, {
        vertexIndex1: 'E3',
        vertexIndex2: 'E1',
        spacingX: -50,
        spacingY: ''
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(midArrow, botArrow, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E1',
        spacingX: 0,
        spacingY: ''
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(botArrow, botDestinationText, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E3',
        spacingX: 50,
        spacingY: ''
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(botDestinationText, botChineseText, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(botDestinationText, airport, {
        vertexIndex1: 'E3',
        vertexIndex2: 'E3',
        spacingX: 0,
        spacingY: ''
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(airport, topEng2, {
        vertexIndex1: 'E3',
        vertexIndex2: 'E1',
        spacingX: -50,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topEng2, topChi1, {
        vertexIndex1: 'E7',
        vertexIndex2: 'E1',
        spacingX: 0,
        spacingY: 0
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topChi1, topEng1, {
        vertexIndex1: 'E7',
        vertexIndex2: 'E1',
        spacingX: 0,
        spacingY: 0
      });


      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topEng2, topChi2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topEng1, topArrow, {
        vertexIndex1: 'E3',
        vertexIndex2: 'E1',
        spacingX: -50,
        spacingY: ''
      });

      // Create an overall border containing all components
      const topObjects = [
        topEng1, topChi1, topEng2, topChi2, topArrow, airport
      ];
      const botObjects = [
        midDestinationText, midChineseText, midArrow,
        botDestinationText, botChineseText, botArrow
      ];

      const borderGroup1 = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.BorderGroupCreate(
        'stack',
        topObjects,
        topObjects,
        null,
        null,
        { xHeight: xHeight, borderType: 'stack', colorType: 'Blue Background' }
      );

      const borderGroup2 = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.BorderGroupCreate(
        'stack',
        botObjects,
        botObjects,
        null,
        null,
        { xHeight: xHeight, borderType: 'stack', colorType: 'White Background' }
      );

      // Create horizontal divider AFTER border so it is sized by assignWidthToDivider
      const topDivider = new _objects_divider_js__WEBPACK_IMPORTED_MODULE_5__.DividerObject({ dividerType: 'HDivider', borderGroup: borderGroup2, xHeight: xHeight, colorType: 'White Background' });
      // Anchor the objects above and below to the divider to preserve vertical spacing
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(midChineseText, topDivider,
        {
          vertexIndex1: 'V1',
          vertexIndex2: 'E6',
          spacingX: '',
          spacingY: 1.5 * xHeight / 4
        });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topDivider, botDestinationText,
        {
          vertexIndex1: 'E2',
          vertexIndex2: 'V6',
          spacingX: '',
          spacingY: 2.5 * xHeight / 4
        });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(borderGroup2, midArrow, {
        vertexIndex1: 'E8',
        vertexIndex2: 'C3',
        spacingX: '',
        spacingY: 0
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(borderGroup2, botArrow, {
        vertexIndex1: 'E8',
        vertexIndex2: 'C6',
        spacingX: '',
        spacingY: 0
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(borderGroup1, topArrow, {
        vertexIndex1: 'E8',
        vertexIndex2: 'C4',
        spacingX: '',
        spacingY: 0
      });


      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
      console.log('Stack Sign template created successfully');

      // Since there are two main border groups, we might return info about both,
      // or perhaps the bounding box of both. Let's return the bounding box.
      // Note: This assumes borderGroup1 and borderGroup2 are the final groups.
      // A more robust approach might group them again or calculate the overall bounds.
      const combinedBounds = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.getBoundingBox([borderGroup1, borderGroup2]);

      return {
        width: combinedBounds.right - combinedBounds.left,
        height: combinedBounds.bottom - combinedBounds.top,
        left: combinedBounds.left,
        top: combinedBounds.top
      };


    } catch (error) {
      console.error('Error creating Stack Sign template:', error);
      return null; // Return null on error
    }
  },
  /**
   * Create a roundabout directions sign
   */
  createRoundaboutDirections: function (centerX, centerY, xHeight, color) {
    try {
      // Create roundabout symbol at center with three side roads
      const routeOptions = {
        routeList: [
          { x: centerX, y: centerY + 5 * xHeight, angle: 180, width: 6, shape: 'Normal' },
          { x: centerX, y: centerY, angle: 0, width: 6, length: 30, shape: 'Stub' }
        ],
        xHeight: xHeight,
        rootLength: 7,
        tipLength: 12,
        roadType: 'Conventional Roundabout',
        color: color,
      };

      const roundabout = new _objects_mainRoute_js__WEBPACK_IMPORTED_MODULE_7__.MainRoadSymbol(routeOptions);
      roundabout.updateAllCoord(); // Ensure coordinates are set

      // Top side road (Tsuen Wan West Station)
      const topSideRoadParams = {
        xHeight: xHeight,
        color: color,
        roadType: 'Side Road',
        mainRoad: roundabout,
        side: null, // Constructor will determine based on angle and main road
        // Approximate initial position, constructor will adjust
        routeList: [{
          x: centerX,
          y: centerY - 10 * xHeight / 4, // Adjusted for typical side road length
          angle: 0,
          width: 4,
          shape: 'Arrow'
        }],
      };
      const side1 = new _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_8__.SideRoadSymbol(topSideRoadParams);


      // Left side road (Sham Tseng and Tuen Mun)
      const leftSideRoadParams = {
        xHeight: xHeight,
        color: color,
        roadType: 'Side Road',
        mainRoad: roundabout,
        routeList: [{
          x: centerX - 10 * xHeight / 4, // Adjusted for typical side road length
          y: centerY,
          angle: 90,
          width: 4,
          shape: 'Arrow'
        }],
        side: true, // Explicitly left
      };
      const side2 = new _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_8__.SideRoadSymbol(leftSideRoadParams);

      // Right side road (Tsing Yi and Kowloon)
      const rightSideRoadParams = {
        xHeight: xHeight,
        color: color,
        roadType: 'Side Road',
        mainRoad: roundabout,
        side: false, // Explicitly right
        routeList: [{
          x: centerX + 10 * xHeight / 4, // Adjusted for typical side road length
          y: centerY,
          angle: -90,
          width: 4,
          shape: 'Arrow'
        }],
      };
      const side3 = new _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_8__.SideRoadSymbol(rightSideRoadParams);

      // Create destination texts for the top side road (Tsuen Wan West Station)
      const topDestinationText1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Tsuen Wan',
        xHeight: xHeight,
        font: 'TransportHeavy',
        color: 'black',
        left: centerX,
        top: centerY - 250
      })

      const topDestinationText2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'West Station',
        xHeight: xHeight,
        font: 'TransportHeavy',
        color: 'black',
        left: centerX,
        top: centerY - 250
      });

      const topChineseText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '荃灣西站',
        xHeight: xHeight,
        font: 'TransportHeavy',
        color: 'black',
        left: centerX,
        top: centerY - 170
      });

      const topDestinationText3 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Tsuen Wan(C)',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'white',
        left: centerX,
        top: centerY - 250
      });

      const topChineseText2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '荃灣(中)',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'white',
        left: centerX,
        top: centerY - 170
      });

      // Add MTR symbol for the top side road
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'MTR',
        left: centerX - 100,
        top: centerY - 200,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const topSymbol = canvasObject[canvasObject.length - 1];

      // Create destination texts for the left side road (Sham Tseng and Tuen Mun)
      const leftDestinationText1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Sham Tseng',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'white',
        left: centerX - 300,
        top: centerY - 50
      });

      const leftChineseText1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '深井',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'white',
        left: centerX - 300,
        top: centerY + 30
      });

      const leftDestinationText2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Tuen Mun',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'white',
        left: centerX - 300,
        top: centerY + 100
      });

      const leftChineseText2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '屯門',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'white',
        left: centerX - 300,
        top: centerY + 180
      });

      // Add symbol for the left side road
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'Expressway',
        left: centerX - 450,
        top: centerY + 40,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const leftSymbol1 = canvasObject[canvasObject.length - 1];
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'Route4',
        left: centerX - 450,
        top: centerY + 40,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const leftSymbol2 = canvasObject[canvasObject.length - 1];

      // Create destination texts for the right side road (Tsing Yi and Kowloon)
      const rightDestinationText1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Tsing Yi',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'white',
        left: centerX + 300,
        top: centerY - 50
      });

      const rightChineseText1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '青衣',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'white',
        left: centerX + 300,
        top: centerY + 30
      });

      const rightDestinationText2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Kowloon',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'white',
        left: centerX + 300,
        top: centerY + 100
      });

      const rightChineseText2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '九龍',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'white',
        left: centerX + 300,
        top: centerY + 180
      });

      // Add Airport symbol for the right side road
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'Airport',
        left: centerX + 450,
        top: centerY + 40,
        xHeight: xHeight,
        symbolAngle: 90,
        color: color
      });
      const rightSymbol1 = canvasObject[canvasObject.length - 1];

      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'Expressway',
        left: centerX - 450,
        top: centerY + 40,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const rightSymbol2 = canvasObject[canvasObject.length - 1];
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'Route4',
        left: centerX - 450,
        top: centerY + 40,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const rightSymbol3 = canvasObject[canvasObject.length - 1];

      // Anchor top side road objects
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(side1, topChineseText2, {
        vertexIndex1: 'E6',
        vertexIndex2: 'V1',
        spacingX: -484.5,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topChineseText2, topDestinationText3, {
        vertexIndex1: 'E7',
        vertexIndex2: 'E1',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topDestinationText3, topChineseText, {
        vertexIndex1: 'E7',
        vertexIndex2: 'E1',
        spacingX: 62.5,
        spacingY: -37.5
      });

      const topBorderObjects = [
        topDestinationText1, topDestinationText2, topChineseText,
        topSymbol,
      ];

      const whiteBorder = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.BorderGroupCreate(
        'panel',
        topBorderObjects,
        topBorderObjects,
        null,
        null,
        { xHeight: xHeight, borderType: 'panel', colorType: 'White Background' }
      );

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topChineseText, topDestinationText2, {
        vertexIndex1: 'E7',
        vertexIndex2: 'E1',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topDestinationText2, topDestinationText1, {
        vertexIndex1: 'E7',
        vertexIndex2: 'E1',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topDestinationText2, topSymbol, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E3',
        spacingX: 50,
        spacingY: -145
      });

      // Anchor left side road objects
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(side2, leftSymbol1, {
        vertexIndex1: 'E3',
        vertexIndex2: 'V7',
        spacingX: '',
        spacingY: 112.5
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(roundabout, leftSymbol1, {
        vertexIndex1: 'E3',
        vertexIndex2: 'V1',
        spacingX: -350,
        spacingY: ''
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftSymbol1, leftSymbol2, {
        vertexIndex1: 'E2',
        vertexIndex2: 'E6',
        spacingX: 0,
        spacingY: 35
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftSymbol1, leftDestinationText1, {
        vertexIndex1: 'E3',
        vertexIndex2: 'E1',
        spacingX: -50,
        spacingY: 0
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftDestinationText1, leftChineseText1, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftChineseText1, leftDestinationText2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftDestinationText2, leftChineseText2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      const leftBorderObjects = [
        leftDestinationText1, leftChineseText1,
        leftDestinationText2, leftChineseText2,
        leftSymbol1, leftSymbol2,
      ];

      const greenBorder1 = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.BorderGroupCreate(
        'greenPanel',
        leftBorderObjects,
        leftBorderObjects,
        null,
        null,
        { xHeight: xHeight, borderType: 'greenPanel', colorType: 'White Background' }
      );


      // Anchor right side road objects
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(side3, rightDestinationText1, {
        vertexIndex1: 'E1',
        vertexIndex2: 'V2',
        spacingX: '',
        spacingY: 112.5
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(roundabout, rightDestinationText1, {
        vertexIndex1: 'E1',
        vertexIndex2: 'V1',
        spacingX: 500,
        spacingY: ''
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightDestinationText1, rightChineseText1, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightChineseText1, rightDestinationText2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightDestinationText2, rightChineseText2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });


      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightDestinationText1, rightSymbol1, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E3',
        spacingX: 50,
        spacingY: 0
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightDestinationText2, rightSymbol2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E3',
        spacingX: 50,
        spacingY: 0
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightSymbol2, rightSymbol3, {
        vertexIndex1: 'E2',
        vertexIndex2: 'E6',
        spacingX: 0,
        spacingY: 35
      });

      const rightBorderObjects = [
        rightChineseText1, rightDestinationText1,
        rightChineseText2, rightDestinationText2,
        rightSymbol1, rightSymbol2, rightSymbol3
      ];

      const greenBorder2 = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.BorderGroupCreate(
        'greenPanel',
        rightBorderObjects,
        rightBorderObjects,
        null,
        null,
        { xHeight: xHeight, borderType: 'greenPanel', colorType: 'White Background' }
      );



      // Create an overall border containing all components
      const allObjects = [
        roundabout,
        whiteBorder, greenBorder1, greenBorder2,
      ];

      const borderGroup = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.BorderGroupCreate(
        'stack',
        allObjects,
        allObjects,
        null,
        null,
        { xHeight: xHeight, borderType: 'stack', colorType: 'Blue Background' }
      );

      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
      console.log('Roundabout Directions template created successfully');

      // Return dimensions and position of the final border group
      return {
        width: borderGroup.width,
        height: borderGroup.height,
        left: borderGroup.left,
        top: borderGroup.top
      };

    } catch (error) {
      console.error('Error creating Roundabout Directions template:', error);
      return null; // Return null on error
    }
  },
  /**
   * Create an interchange direction sign
   */
  createInterchangeDirections: function (centerX, centerY, xHeight, color) {
    try {
      // Set up test parameters
      const params = {
        xHeight: xHeight,
        rootLength: 7,
        tipLength: 12,
        posx: 250,
        posy: 300,
        width: 6,
        shape: 'Arrow',
        color: color,
        roadType: 'Main Line'
      };

      // Create a MainRoute directly using the updated construction method
      // Create the route list with the main road points
      const routeList = [
        { x: params.posx, y: params.posy + (params.rootLength) * params.xHeight / 4, angle: 180, length: params.rootLength, width: params.width, shape: 'Stub' },
        { x: params.posx, y: params.posy, angle: 0, length: params.rootLength, width: params.width, shape: params.shape }
      ];

      // Create route options for the MainRoadSymbol
      const routeOptions = {
        routeList: routeList,
        xHeight: params.xHeight,
        rootLength: params.rootLength,
        tipLength: params.tipLength,
        routeWidth: params.width,
        color: params.color,
        roadType: params.roadType
      };
      const mainRoad = new _objects_mainRoute_js__WEBPACK_IMPORTED_MODULE_7__.MainRoadSymbol(routeOptions);

      // Create upper destination text (Sheung Shui)
      const upperDestEng1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Sheung',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX - 300,
        top: centerY - 150
      });

      const upperDestEng2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Shui',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX - 300,
        top: centerY - 70
      });

      const upperDestChi = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '上水',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX - 300,
        top: centerY + 10
      });

      // Create lower destination text (Mai Po)
      const lowerDestEng = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Mai Po',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX - 300,
        top: centerY + 300
      });

      const lowerDestChi = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '米埔',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX - 300,
        top: centerY + 380
      });

      // Create expressway symbol in red
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'ExpresswayRed',
        left: centerX - 50,
        top: centerY - 150,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const expressway = canvasObject[canvasObject.length - 1];

      // Create route 9 symbol
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'Route9',
        left: centerX - 50,
        top: centerY - 70,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const route9 = canvasObject[canvasObject.length - 1];

      // Create exit symbol 
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'Exit',
        left: centerX + 150,
        top: centerY + 150,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const exitSymbol = canvasObject[canvasObject.length - 1];

      // Create exit number text for exit panel
      const exitText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '10A',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: 'White',
        left: centerX + 150,
        top: centerY + 50
      });

      // Create airport symbol at 45 degrees
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'Airport',
        left: centerX + 150,
        top: centerY + 150,
        xHeight: xHeight,
        symbolAngle: -45,
        color: color
      });
      const airport = canvasObject[canvasObject.length - 1];


      // Anchor text objects
      // Upper destination text
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(expressway, upperDestEng1, {
        vertexIndex1: 'E3',
        vertexIndex2: 'E1',
        spacingX: -xHeight * 2 / 4,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(upperDestEng1, upperDestEng2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(upperDestEng2, upperDestChi, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });


      // Position lower destination text 300mm below upper text
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(upperDestChi, lowerDestEng, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: xHeight * 12 / 4
      });

      // Lower destination text
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(lowerDestEng, lowerDestChi, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      // Anchor symbols
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(expressway, route9, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: xHeight * 2 / 4
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(route9, mainRoad, {
        vertexIndex1: 'V1',
        vertexIndex2: 'E6',
        spacingX: 0,
        spacingY: xHeight * 4 / 4
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(lowerDestChi, exitSymbol, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: xHeight * 1 / 4,
        spacingY: xHeight * 1 / 4
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(exitSymbol, exitText, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E3',
        spacingX: xHeight * 0.5 / 4,
        spacingY: xHeight * -0.2 / 4
      });

      // Create exit panel border
      const exitPanel = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.BorderGroupCreate(
        'exit',
        [exitText, exitSymbol],
        [exitText, exitSymbol],
        null,
        null,
        { xHeight: xHeight, borderType: 'exitPanel', colorType: 'Green Background' }
      );

      // Position airport symbol below exit panel
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(exitPanel, airport, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: xHeight * 2 / 4
      });

      // Create side road at 60 degrees angle pointing to lower destination
      canvas.setActiveObject(mainRoad);
      const sideRoadParams = {
        xHeight: xHeight,
        color: color,
        roadType: 'Side Road',
        mainRoad: mainRoad,
        side: true,
        // Approximate initial position, constructor will adjust
        routeList: [{
          x: centerX - 300,
          y: centerY,
          angle: -60,
          width: 4,
          shape: 'Arrow'
        }],
      };

      const sideRoad = new _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_8__.SideRoadSymbol(sideRoadParams);

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(exitPanel, sideRoad, {
        vertexIndex1: 'V2',
        vertexIndex2: 'E5',
        spacingX: '',
        spacingY: xHeight * 2 / 4
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(airport, sideRoad, {
        vertexIndex1: 'V1',
        vertexIndex2: 'E4',
        spacingX: xHeight * 2 / 4,
        spacingY: ''
      });

      // Create an overall border containing all components
      const allObjects = [
        mainRoad, sideRoad,
        upperDestEng1, upperDestEng2, upperDestChi,
        lowerDestEng, lowerDestChi,
        expressway, route9, airport
      ];

      const borderGroup = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.BorderGroupCreate(
        'stack',
        allObjects,
        allObjects,
        null,
        null,
        { xHeight: xHeight, borderType: 'stack', colorType: 'Green Background' }
      );

      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
      console.log('Interchange Directions template created successfully');

      // Return dimensions and position of the final border group
      return {
        width: borderGroup.width,
        height: borderGroup.height,
        left: borderGroup.left,
        top: borderGroup.top
      };

    } catch (error) {
      console.error('Error creating Interchange Directions template:', error);
      return null; // Return null on error
    }
  },

  /**
   * Create a multi-lane exit sign
   */
  createMultiLaneExit: function (centerX, centerY, xHeight, color) {
    try {
      // Create lane text objects
      const leftLaneText1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Wan Chai(N)',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX - 300,
        top: centerY - 200
      });

      const leftChineseText1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '灣仔(北)',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX - 300,
        top: centerY - 120
      });

      const leftLaneText2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Kowloon',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX,
        top: centerY - 200
      });

      const leftChineseText2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '九龍',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX,
        top: centerY - 120
      });

      const rightLaneText1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'North Point',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX + 300,
        top: centerY - 200
      });

      const rightChineseText1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '北角',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX + 300,
        top: centerY - 120
      });

      const rightLaneText2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Kowloon(E)',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX + 300,
        top: centerY - 200
      });

      const rightChineseText2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '九龍(東)',
        xHeight: xHeight,
        font: 'TransportHeavy',
        color: color,
        left: centerX + 300,
        top: centerY - 120
      });

      // Create arrows for each lane
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'StackArrow',
        left: centerX - 300,
        top: centerY + 100,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const leftArrow = canvasObject[canvasObject.length - 1];

      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'StackArrow',
        left: centerX,
        top: centerY + 100,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const rightArrow = canvasObject[canvasObject.length - 1];

      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'CHT',
        left: centerX + 300,
        top: centerY + 100,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const chtSymbol = canvasObject[canvasObject.length - 1];

      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'EHC',
        left: centerX + 300,
        top: centerY + 100,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const ehcSymbol = canvasObject[canvasObject.length - 1];

      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'Route4',
        left: centerX + 300,
        top: centerY + 100,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const routeSymbol = canvasObject[canvasObject.length - 1];

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(chtSymbol, rightLaneText1, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E3',
        spacingX: '',
        spacingY: 0
      });

      // Anchor text objects in pairs
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(chtSymbol, leftLaneText1, {
        vertexIndex1: 'E3',
        vertexIndex2: 'E1',
        spacingX: -50,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftLaneText1, leftChineseText1, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftChineseText1, leftLaneText2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftLaneText2, leftChineseText2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftChineseText2, leftArrow, {
        vertexIndex1: 'E2',
        vertexIndex2: 'E6',
        spacingX: '',
        spacingY: 50
      });


      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightLaneText1, ehcSymbol, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E3',
        spacingX: 50,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(ehcSymbol, routeSymbol, {
        vertexIndex1: 'E2',
        vertexIndex2: 'E6',
        spacingX: 0,
        spacingY: 50
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightLaneText1, rightChineseText1, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightChineseText1, rightLaneText2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightLaneText2, rightChineseText2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightChineseText2, rightArrow, {
        vertexIndex1: 'E2',
        vertexIndex2: 'E6',
        spacingX: '',
        spacingY: 50
      });

      // Create an overall border containing all components
      const allObjects = [
        leftLaneText1, leftChineseText1, leftLaneText2, leftChineseText2,
        rightLaneText1, rightChineseText1, rightLaneText2, rightChineseText2,
        leftArrow, rightArrow,
        chtSymbol, ehcSymbol, routeSymbol,
      ];

      const borderGroup = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.BorderGroupCreate(
        'stack',
        allObjects,
        allObjects,
        null,
        null,
        { xHeight: xHeight, borderType: 'stack', colorType: 'Blue Background' }
      );


      // Create lane dividers
      new _objects_divider_js__WEBPACK_IMPORTED_MODULE_5__.DividerObject({ dividerType: 'VLane', borderGroup: borderGroup, xHeight: xHeight, colorType: 'Blue Background', });
      const leftLaneDivider = canvasObject[canvasObject.length - 1];

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(chtSymbol, leftLaneDivider, {
        vertexIndex1: 'V4',
        vertexIndex2: 'E3',
        spacingX: 2.5 * xHeight / 4,
        spacingY: ''
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftLaneDivider, rightLaneText1, {
        vertexIndex1: 'E1',
        vertexIndex2: 'V3',
        spacingX: 2.5 * xHeight / 4,
        spacingY: ''
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(borderGroup, leftArrow, {
        vertexIndex1: 'E2',
        vertexIndex2: 'C2',
        spacingX: 0,
        spacingY: ''
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(borderGroup, rightArrow, {
        vertexIndex1: 'E2',
        vertexIndex2: 'C6',
        spacingX: 0,
        spacingY: ''
      });


      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
      console.log('Multi-Lane Exit template created successfully');

      // Return dimensions and position of the final border group
      return {
        width: borderGroup.width,
        height: borderGroup.height,
        left: borderGroup.left,
        top: borderGroup.top
      };


    } catch (error) {
      console.error('Error creating Multi-Lane Exit template:', error);
      return null; // Return null on error
    }
  },

  /**
   * Create a spiral roundabout sign
   */
  createSpiralRoundaboutSign: function (centerX, centerY, xHeight, color) {
    try {
      // Create spiral roundabout symbol at center with four side roads
      const routeOptions = {
        routeList: [
          { x: centerX, y: centerY + 5 * xHeight, angle: 180, width: 6, shape: 'Auxiliary' },
          { x: centerX, y: centerY, angle: 0, width: 6, shape: 'Stub' }
        ],
        xHeight: xHeight,
        rootLength: 7,
        tipLength: 12,
        roadType: 'Spiral Roundabout',
        shape: 'Auxiliary',
        RAfeature: 'Auxiliary',
        color: color
      };

      const roundabout = new _objects_mainRoute_js__WEBPACK_IMPORTED_MODULE_7__.MainRoadSymbol(routeOptions);

      const baseSideRoute = roundabout.sideRoad[0];

      // Create side roads at top-left, top-right, left and right positions
      canvas.setActiveObject(roundabout);

      // Left side road

      const topSideRoadParams = {
        xHeight: xHeight,
        color: color,
        roadType: 'Side Road',
        mainRoad: roundabout,
        side: null, // Constructor will determine based on angle and main road
        // Approximate initial position, constructor will adjust
        routeList: [{
          x: centerX,
          y: centerY - 600, // Adjusted for typical side road length
          angle: 0,
          width: 4,
          shape: 'Spiral Arrow'
        }],
      };
      const topRoad = new _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_8__.SideRoadSymbol(topSideRoadParams);

      // Set the roundabout as active object to add a side road
      canvas.setActiveObject(roundabout);

      // Right side road
      const rightSideRoadParams = {
        xHeight: xHeight,
        color: color,
        roadType: 'Side Road',
        mainRoad: roundabout,
        side: null, // Constructor will determine based on angle and main road
        // Approximate initial position, constructor will adjust
        routeList: [{
          x: centerX + 600,
          y: centerY,
          angle: 0,
          width: 4,
          shape: 'Spiral Arrow'
        }]
      };
      const rightRoad = new _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_8__.SideRoadSymbol(rightSideRoadParams);

      // Create top text objects
      const topTextEng1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Sheung Tak',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX,
        top: centerY - 400
      });

      const topTextChi1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '尚德',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX,
        top: centerY - 320
      });

      const topTextEng2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Tsueng Kwan O Sports Ground',
        xHeight: xHeight,
        font: 'TransportHeavy',
        color: 'black',
        left: centerX,
        top: centerY - 240
      });

      const topTextChi2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '將軍澳運動場',
        xHeight: xHeight,
        font: 'TransportHeavy',
        color: 'black',
        left: centerX,
        top: centerY - 160
      });

      // Create left side text objects
      const leftTextEng1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Sai Kung',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX - 350,
        top: centerY - 80
      });

      const leftTextChi1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '西貢',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX - 350,
        top: centerY
      });

      const leftTextEng2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Kowloon',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX - 350,
        top: centerY + 80
      });

      const leftTextChi2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '九龍',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX - 350,
        top: centerY + 160
      });

      const leftTextEng3 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '(via TKO Tunnel)',
        xHeight: xHeight * 0.75,
        font: 'TransportMedium',
        color: color,
        left: centerX - 350,
        top: centerY + 240
      });

      const leftTextChi3 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '(經將軍澳隧道)',
        xHeight: xHeight * 0.75,
        font: 'TransportMedium',
        color: color,
        left: centerX - 350,
        top: centerY + 320
      });

      // Create right side text objects
      const rightTextEng1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Kowloon',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX + 350,
        top: centerY - 80
      });

      const rightTextChi1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '九龍',
        xHeight: xHeight,
        font: 'TransportMedium',
        color: color,
        left: centerX + 350,
        top: centerY
      });

      const rightTextEng2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '(via TKO-',
        xHeight: xHeight * 0.75,
        font: 'TransportMedium',
        color: color,
        left: centerX + 350,
        top: centerY + 80
      });

      const rightTextChi2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '(經將軍澳-',
        xHeight: xHeight * 0.75,
        font: 'TransportMedium',
        color: color,
        left: centerX + 350,
        top: centerY + 160
      });

      const rightTextEng3 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: 'Lam Tin Tunnel)',
        xHeight: xHeight * 0.75,
        font: 'TransportMedium',
        color: color,
        left: centerX + 350,
        top: centerY + 240
      });

      const rightTextChi3 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_2__.TextObject({
        text: '藍田隧道)',
        xHeight: xHeight * 0.75,
        font: 'TransportMedium',
        color: color,
        left: centerX + 350,
        top: centerY + 320
      });

      // Create route symbols
      // Route 7 on left side
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'Route7',
        left: centerX - 500,
        top: centerY,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const leftRouteSymbol = canvasObject[canvasObject.length - 1];

      // Route 6 on right side
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'Route6',
        left: centerX + 500,
        top: centerY,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const rightRouteSymbol = canvasObject[canvasObject.length - 1];

      // Tunnel on right side
      new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_3__.SymbolObject({
        symbolType: 'Tunnel',
        left: centerX + 500,
        top: centerY,
        xHeight: xHeight,
        symbolAngle: 0,
        color: color
      });
      const tunnelSymbol = canvasObject[canvasObject.length - 1];

      // Anchor text objects vertically
      // Top text anchoring
      // X-axis
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topRoad, topTextEng2, {
        vertexIndex1: 'E2',
        vertexIndex2: 'V1',
        spacingX: 0,
        spacingY: ''
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topTextEng2, topTextChi1, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E1',
        spacingX: -62.5,
        spacingY: ''
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topTextEng2, topTextEng1, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E1',
        spacingX: -62.5,
        spacingY: ''
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topTextEng2, topTextChi2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E1',
        spacingX: 0,
        spacingY: ''
      });

      // Y-axis
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topRoad, topTextChi2, {
        vertexIndex1: 'E6',
        vertexIndex2: 'V1',
        spacingX: '',
        spacingY: -37.5
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topTextChi2, topTextEng2, {
        vertexIndex1: 'E7',
        vertexIndex2: 'E1',
        spacingX: '',
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topTextEng2, topTextChi1, {
        vertexIndex1: 'E7',
        vertexIndex2: 'E1',
        spacingX: '',
        spacingY: -62.5
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(topTextChi1, topTextEng1, {
        vertexIndex1: 'E7',
        vertexIndex2: 'E1',
        spacingX: '',
        spacingY: 0
      });

      // Left text anchoring
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(baseSideRoute, leftTextEng1, {
        vertexIndex1: 'E3',
        vertexIndex2: 'V1',
        spacingX: -731,
        spacingY: ''
      });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(baseSideRoute, leftTextEng1, {
        vertexIndex1: 'E3',
        vertexIndex2: 'V4',
        spacingX: '',
        spacingY: 37.5
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftTextEng1, leftTextChi1, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftTextChi1, leftTextEng2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftTextEng2, leftTextEng3, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftTextEng3, leftTextChi2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftTextChi2, leftTextChi3, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      // Right text anchoring
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightRoad, rightTextEng1, {
        vertexIndex1: 'E1',
        vertexIndex2: 'V3',
        spacingX: 50,
        spacingY: ''
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightRoad, rightTextEng1, {
        vertexIndex1: 'E1',
        vertexIndex2: 'V2',
        spacingX: '',
        spacingY: 37.5
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightTextEng1, rightTextEng2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightTextEng2, rightTextEng3, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightTextEng3, rightTextChi1, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightTextChi1, rightTextChi2, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightTextChi2, rightTextChi3, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E7',
        spacingX: 0,
        spacingY: 0
      });

      // Anchor text to route symbols
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(leftTextEng3, leftRouteSymbol, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E3',
        spacingX: 50,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(rightTextEng1, tunnelSymbol, {
        vertexIndex1: 'E1',
        vertexIndex2: 'E3',
        spacingX: 337,
        spacingY: 0
      });

      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_9__.anchorShape)(tunnelSymbol, rightRouteSymbol, {
        vertexIndex1: 'E2',
        vertexIndex2: 'E6',
        spacingX: 0,
        spacingY: 50
      });

      const topPanel = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.BorderGroupCreate(
        'panel',
        [topTextEng2, topTextChi2],
        [topTextEng2, topTextChi2],
        null,
        null,
        { xHeight: xHeight, borderType: 'panel', colorType: 'White Background' }
      );

      // Create an overall border containing all components
      const allObjects = [
        roundabout,
        topTextEng1, topTextChi1, topPanel,
        leftTextEng1, leftTextChi1, leftTextEng2, leftTextChi2, leftTextEng3, leftTextChi3, leftRouteSymbol,
        rightTextEng1, rightTextChi1, rightTextEng2, rightTextChi2, rightTextEng3, rightTextChi3, rightRouteSymbol, tunnelSymbol,
      ];

      const borderGroup = _objects_border_js__WEBPACK_IMPORTED_MODULE_4__.BorderUtilities.BorderGroupCreate(
        'stack',
        allObjects,
        allObjects,
        null,
        null,
        { xHeight: xHeight, borderType: 'stack', colorType: 'Blue Background' }
      );

      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_1__.CanvasGlobals.scheduleRender();
      console.log('Spiral Roundabout Sign template created successfully');

      return {
        width: borderGroup.width,
        height: borderGroup.height,
        left: borderGroup.left,
        top: borderGroup.top
      };

    } catch (error) {
      console.error('Error creating Spiral Roundabout Sign template:', error);
      return null; // Return null on error
    }
  },
};

// Use the shared settings listener implementation
_sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.addListener(
  _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createSettingsListener(8, function (setting, value) {
    // Template-specific updates when settings change
    if (setting === 'messageColor') {
      // Redraw the templates grid with the new color
      const parent = document.getElementById('input-form');
      if (parent) {
        const templatesGrid = parent.querySelector('.templates-grid');
        if (templatesGrid) {
          templatesGrid.remove();
          FormTemplateComponent.createTemplatesGrid(parent);
        }
      }
    }
  })
);




/***/ }),

/***/ "./js/sidebar/sb-text.js":
/*!*******************************!*\
  !*** ./js/sidebar/sb-text.js ***!
  \*******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   FormTextAddComponent: () => (/* binding */ FormTextAddComponent)
/* harmony export */ });
/* harmony import */ var _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./sbGeneral.js */ "./js/sidebar/sbGeneral.js");
/* harmony import */ var _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../i18n/i18n.js */ "./js/i18n/i18n.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _objects_text_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../objects/text.js */ "./js/objects/text.js");
/* harmony import */ var _objects_anchor_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../objects/anchor.js */ "./js/objects/anchor.js");
/* harmony import */ var _objects_template_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../objects/template.js */ "./js/objects/template.js");
/* harmony import */ var _modal_md_font_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../modal/md-font.js */ "./js/modal/md-font.js");
/* harmony import */ var _utils_hintLoader_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../utils/hintLoader.js */ "./js/utils/hintLoader.js");
/* harmony import */ var _objects_symbols_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../objects/symbols.js */ "./js/objects/symbols.js");
/* harmony import */ var _objects_border_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ../objects/border.js */ "./js/objects/border.js");
/* Text panel component */











let FormTextAddComponent = {
  textFont: ['TransportMedium', 'TransportHeavy'],
  newTextObject: null,
  textLineInput: 1,
  justification: 'Left',

  // Helper function to find corresponding Chinese text for English text
  findCorrespondingChineseText: function (engText, isEnglishText = true) {
    let returnText = null;
    const searchDestinations = isEnglishText ? _objects_template_js__WEBPACK_IMPORTED_MODULE_5__.EngDestinations : _objects_template_js__WEBPACK_IMPORTED_MODULE_5__.ChtDestinations;

    // Search through all regions in EngDestinations
    for (let i = 0; i < searchDestinations.length; i++) {
      const Region = searchDestinations[i];
      const regionName = Object.keys(Region)[0];
      const engLocations = Region[regionName];

      // Find the index of the English text in this region
      const index = engLocations.indexOf(engText);

      // If found, get the same index from ChtDestinations
      if (index !== -1) {
        const returnDestinations = isEnglishText ? _objects_template_js__WEBPACK_IMPORTED_MODULE_5__.ChtDestinations : _objects_template_js__WEBPACK_IMPORTED_MODULE_5__.EngDestinations;
        const chtRegion = returnDestinations[i];
        const chtLocations = chtRegion[regionName];
        if (chtLocations && chtLocations[index]) {
          returnText = chtLocations[index];
          break;
        }
      }
    }

    return returnText;
  },

  textPanelInit: function (event, editingTextObject = null) {
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.tabNum = 2;
    FormTextAddComponent.textLineInput = 1;
    var parent = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.PanelInit();
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.setActiveComponentOff(FormTextAddComponent.TextHandlerOff);
    if (parent) {
      // Set up button hint mappings for this panel
      _utils_hintLoader_js__WEBPACK_IMPORTED_MODULE_7__.HintLoader.setButtonHintMappings({
        'Text Font-container': 'symbols/TextFont',
        'input-text-container': 'symbols/Text',
      });

      // Create the basic parameters container using the shared function
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createBasicParamsContainer(parent, FormTextAddComponent, null, null, FormTextAddComponent.liveUpdateText, FormTextAddComponent.liveUpdateText);

      // Create a container for text content and font
      const textContentContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);
      const textInput = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('input-text', 'Add Text', textContentContainer, '', editingTextObject ? FormTextAddComponent.liveUpdateText : FormTextAddComponent.TextInputHandler, 'input');
      // Add the info text div for 2Liner mode
      const twoLinerInfo = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createI18nNode('div', { 'id': 'two-liner-info', 'class': 'info-text', 'style': 'display: none;' }, textContentContainer, 'Text input is disabled in 2Liner mode. Select the location in the destination panel.', 'text');
      const fontToggle = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Text Font', FormTextAddComponent.textFont, textContentContainer, 'TransportMedium', editingTextObject ? FormTextAddComponent.liveUpdateText : FormTextAddComponent.TextInputHandler);
      const lineToggle = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Underline', ['Yes', 'No'], textContentContainer, 'No', editingTextObject ? FormTextAddComponent.liveUpdateText : FormTextAddComponent.TextInputHandler);
      const helpIcon1 = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createHelpIconWithHint(textInput.parentElement, 'text/Text',);
      const helpIcon2 = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createHelpIconWithHint(fontToggle.parentElement, 'text/TextFont',);
      const helpIcon3 = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createHelpIconWithHint(lineToggle.parentElement, 'divider/GantryLine',);

      // Add font priority management button for Chinese fonts
      const fontPriorityButton = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton('font-priority-btn', 'Chinese Font Setting', textContentContainer, 'input', _modal_md_font_js__WEBPACK_IMPORTED_MODULE_6__.FontPriorityManager.showModal, 'click');

      // Create a container for location selection
      const locationContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);
      const regionLabel = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createI18nNode('div', { 'class': 'placeholder' }, locationContainer, 'Add New Destination', 'text');

      // Extract region names from destinations array
      const regionNames = _objects_template_js__WEBPACK_IMPORTED_MODULE_5__.EngDestinations.map(region => Object.keys(region)[0]);

      // Create language toggle
      const languageToggle = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Language', ['2Liner', 'English', 'Chinese'], locationContainer, 'English', FormTextAddComponent.updateLocationDropdown);

      // Create Justification toggle - only visible when 2Liner is selected
      const justificationToggle = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Justification', ['Left', 'Middle', 'Right'], locationContainer, 'Left', FormTextAddComponent.handleJustification);
      const justificationContainer = document.getElementById('Justification-container').parentElement;
      justificationContainer.style.display = FormTextAddComponent.textLineInput == 2 ? 'block' : 'none';

      // Create region toggle
      const regionToggle = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createToggle('Region', regionNames, locationContainer, regionNames[0], FormTextAddComponent.updateLocationDropdown);

      // Create location dropdown
      const locationDropdownContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'location-dropdown-container' }, locationContainer);

      // Create the select element for locations
      const locationSelect = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("select", { 'class': 'input', 'id': 'location-select' }, locationDropdownContainer, FormTextAddComponent.locationSelected, 'change');

      // Create a container for Street Name Plate
      const streetNamePlateContainer = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createI18nNode('div', { 'class': 'placeholder' }, streetNamePlateContainer, 'Street Name Plate', 'text');

      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('input-eng-st-name', 'Eng St Name', streetNamePlateContainer, '', null, 'input');
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('input-chin-st-name', 'Chin St Name', streetNamePlateContainer, '', null, 'input');

      // Left numbers row
      const leftNumRow = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-row' }, streetNamePlateContainer);
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('input-left-st-num1', 'Left Num 1', leftNumRow, '', null, 'input', '(optional)');
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('input-left-st-num2', 'Left Num 2', leftNumRow, '', null, 'input', '(optional)');

      // Right numbers row
      const rightNumRow = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createNode("div", { 'class': 'input-row' }, streetNamePlateContainer);
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('input-right-st-num1', 'Right Num 1', rightNumRow, '', null, 'input', '(optional)');
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createInput('input-right-st-num2', 'Right Num 2', rightNumRow, '', null, 'input', '(optional)');

      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createButton(
        'add-street-name-plate-text',
        'Add Street Name Plate Text',
        streetNamePlateContainer,
        'input',
        FormTextAddComponent.createStreetNamePlate,
        'click'
      );

      // Initialize the location dropdown with locations from the first region
      FormTextAddComponent.populateLocationDropdown(regionNames[0], "English");

      // Apply translations so Region toggle options and labels render in the current locale
      try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.applyTranslations(parent); } catch (_) { }
    }
  },

  /**
   * Updates the location dropdown when a region is selected
   */
  updateLocationDropdown: function (selectedButton) {
    const regionName = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Region-container')
    const languageInput = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.getToggleValue('Language-container')
    let language = languageInput == '2Liner' ? 'English' : languageInput
    const twoLinerInfo = document.getElementById('two-liner-info'); // Get the info text div
    if (languageInput == '2Liner') {
      FormTextAddComponent.textLineInput = 2
      // Show justification toggle when 2Liner is selected
      const justificationContainer = document.getElementById('Justification-container').parentElement;
      justificationContainer.style.display = 'block';
      // Lock text input when in 2Liner mode
      document.getElementById('input-text').disabled = true;
      twoLinerInfo.style.display = 'block'; // Show the info text
    } else {
      FormTextAddComponent.textLineInput = 1
      // Hide justification toggle for single line text
      const justificationContainer = document.getElementById('Justification-container').parentElement;
      justificationContainer.style.display = 'none';
      // Enable text input for single line mode
      document.getElementById('input-text').disabled = false;
      twoLinerInfo.style.display = 'none'; // Hide the info text
    }
    FormTextAddComponent.populateLocationDropdown(regionName, language);
    if (FormTextAddComponent.newTextObject && _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.canvas.contains(FormTextAddComponent.newTextObject)) {
      FormTextAddComponent.liveUpdateText();
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.scheduleRender();
    }
  },

  /**
   * Handler for justification toggle
   */
  handleJustification: function (selectedButton) {
    FormTextAddComponent.justification = selectedButton.getAttribute('data-value');
    // If we already have a text object, update its justification
    if (FormTextAddComponent.newTextObject || _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.canvas.getActiveObject()?.functionalType === 'Text') {
      FormTextAddComponent.liveUpdateText();
    }
  },

  /**
   * Populates the location dropdown with locations from the selected region
   */
  populateLocationDropdown: function (regionName, language) {
    const locationSelect = document.getElementById('location-select');
    if (!locationSelect) return;

    // Clear existing options
    locationSelect.innerHTML = '';

    // Add a default empty option (translate immediately and mark for future locale switches)
    const defaultOption = document.createElement('option');
    defaultOption.value = '';
    defaultOption.setAttribute('data-i18n', '-- Select Location --');
    defaultOption.text = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t('-- Select Location --');
    locationSelect.appendChild(defaultOption);

    // Find the selected region in the destinations array
    const languageSet = language == "English" ? _objects_template_js__WEBPACK_IMPORTED_MODULE_5__.EngDestinations : _objects_template_js__WEBPACK_IMPORTED_MODULE_5__.ChtDestinations
    const selectedRegion = languageSet.find(region => Object.keys(region)[0] === regionName);
    if (!selectedRegion) return;

    // Get the locations array for the selected region
    const locations = selectedRegion[regionName];

    // Add an option for each location
    locations.forEach(location => {
      const option = document.createElement('option');
      option.value = location;
      option.text = location;
      locationSelect.appendChild(option);
    });

    // Re-apply translations for the newly created dropdown (safe-guard if locale changed later)
    try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.applyTranslations(locationSelect); } catch (_) { }
  },

  /**
   * Handler for when a location is selected from the dropdown
   */
  locationSelected: function (event) {
    const selectedLocation = event.target.value;
    if (!selectedLocation) return;

    // Update the text input with the selected location
    const textInput = document.getElementById('input-text');
    if (textInput) {
      textInput.value = selectedLocation;

      // Determine if we're editing or creating
      const activeObject = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.canvas.getActiveObject();

      // If we already have a new text object being placed, update it
      if (FormTextAddComponent.newTextObject && _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.canvas.contains(FormTextAddComponent.newTextObject)) {
        FormTextAddComponent.liveUpdateText();
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.scheduleRender();
      }
      // If we're editing an existing text object
      else if (activeObject && activeObject.functionalType === 'Text') {
        FormTextAddComponent.liveUpdateText();
      }
      // Otherwise create a new text object
      else {
        FormTextAddComponent.TextInputHandler(null, {
          text: selectedLocation,
          xHeight: parseInt(document.getElementById('input-xHeight').value),
          font: document.getElementById('Text Font-container').selected.getAttribute('data-value'),
          color: document.getElementById('Message Colour-container').selected.getAttribute('data-value'),
          underline: document.getElementById('Underline-container').selected.getAttribute('data-value') == 'Yes'
        });
      }
    }
  },

  createStreetNamePlate: async function () {
    const engStName = document.getElementById('input-eng-st-name').value;
    const chinStName = document.getElementById('input-chin-st-name').value;
    const leftStNum1 = document.getElementById('input-left-st-num1').value;
    const leftStNum2 = document.getElementById('input-left-st-num2').value;
    const rightStNum1 = document.getElementById('input-right-st-num1').value;
    const rightStNum2 = document.getElementById('input-right-st-num2').value;

    const xHeightEng = 50;
    const xHeightChin = 43.5;
    const xHeightNum = 35;
    const font = document.getElementById('Text Font-container').selected.getAttribute('data-value') || 'TransportMedium';
    const color = 'black';

    // For Chinese text, we should use the Chinese font
    let chinFont = 'TW-MOE-Std-Kai';
    try {
      const fontPriorityList = _modal_md_font_js__WEBPACK_IMPORTED_MODULE_6__.FontPriorityManager.getFontPriorityList();
      const priorityFont = fontPriorityList.length > 0 ? fontPriorityList[0] : 'parsedFontKorean';
      switch (priorityFont) {
        case 'parsedFontKorean': chinFont = 'TW-MOE-Std-Kai'; break;
        case 'parsedFontChinese': chinFont = 'TW-MOE-Std-Kai'; break;
        case 'parsedFontHK': chinFont = 'TW-MOE-Std-Kai'; break;
        case 'parsedFontMedium': chinFont = 'TransportMedium'; break;
        case 'parsedFontHeavy': chinFont = 'TransportHeavy'; break;
        default: chinFont = 'TW-MOE-Std-Kai';
      }
    } catch (e) { }

    await _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createObjectWithSnapping(
      {
        engStName, chinStName, leftStNum1, leftStNum2, rightStNum1, rightStNum2,
        xHeightEng, xHeightChin, xHeightNum, font, chinFont, color
      },
      FormTextAddComponent.createStreetNamePlateObject,
      FormTextAddComponent,
      'newTextObject',
      'E1',
      FormTextAddComponent.TextOnMouseMove,
      FormTextAddComponent.TextOnMouseClick,
      FormTextAddComponent.cancelInput
    );
  },

  createStreetNamePlateObject: function (options) {
    const pos = options.position || _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.CenterCoord();

    // Spread the objects out vertically during creation, similar to 2-liner texts,
    // to give them distinct coordinates initially before anchoring.
    let Y_offset = 0;

    const createText = (text, xHeight, fontOverride, charSpacing = 0) => {
      if (!text) return null;
      const t = new _objects_text_js__WEBPACK_IMPORTED_MODULE_3__.TextObject({
        text: text,
        xHeight: xHeight,
        font: fontOverride || options.font,
        color: options.color,
        left: pos.x,
        top: pos.y + Y_offset,
        charSpacing: charSpacing
      });
      t.isTemporary = true;
      Y_offset += 10; // offset each slightly
      return t;
    };

    const tEngStName = createText(options.engStName, options.xHeightEng);
    const tChinStName = createText(options.chinStName, options.xHeightChin, options.chinFont, 18.25); // Add extra char spacing for Chinese text to make it fulfil standard 40
    const tLeftStNum1 = createText(options.leftStNum1, options.xHeightNum);
    const tLeftStNum2 = createText(options.leftStNum2, options.xHeightNum);
    const tRightStNum1 = createText(options.rightStNum1, options.xHeightNum);
    const tRightStNum2 = createText(options.rightStNum2, options.xHeightNum);

    const createLozenge = (xHeight) => {
      const lozenge = new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_8__.SymbolObject({
        symbolType: 'Lozenge',
        xHeight: xHeight,
        color: options.color,
        left: pos.x,
        top: pos.y + Y_offset
      });
      lozenge.isTemporary = true;
      Y_offset += 10;
      return lozenge;
    };

    // Calculate widths only for existing numbers
    const leftNumWidth = (tLeftStNum2?.width || 0) + (tLeftStNum1?.width || 0);
    const rightNumWidth = (tRightStNum1?.width || 0) + (tRightStNum2?.width || 0);
    const leftEqWidth = leftNumWidth - rightNumWidth > 0 ? 0 : 0.5 * leftNumWidth - 0.5 * rightNumWidth;
    const rightEqWidth = rightNumWidth - leftNumWidth > 0 ? 0 : 0.5 * rightNumWidth - 0.5 * leftNumWidth;

    // Anchor Chinese name below English name
    if (tEngStName && tChinStName) {
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(tEngStName, tChinStName, { vertexIndex1: 'E2', vertexIndex2: 'E6', spacingX: 0, spacingY: 56 - options.xHeightEng * 0.5 - options.xHeightChin * 0.1 });
    }

    // Anchor left numbers to the left of English name
    if (tChinStName && (tLeftStNum1 || tLeftStNum2)) {
      const targetNum = tLeftStNum2 || tLeftStNum1;
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(tChinStName, targetNum, { vertexIndex1: 'E4', vertexIndex2: 'E8', spacingX: -35 + leftEqWidth + options.xHeightChin * 0.25, spacingY: 0 });
    }
    if (tLeftStNum1 && tLeftStNum2) {
      const lozengeLeft = createLozenge(options.xHeightNum);
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(tLeftStNum2, lozengeLeft, { vertexIndex1: 'E4', vertexIndex2: 'E8', spacingX: -10, spacingY: -0.2 * options.xHeightNum });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(lozengeLeft, tLeftStNum1, { vertexIndex1: 'E4', vertexIndex2: 'E8', spacingX: -10, spacingY: 0.2 * options.xHeightNum });
    }

    // Anchor right numbers to the right of English name
    if (tChinStName && (tRightStNum1 || tRightStNum2)) {
      const targetNum = tRightStNum1 || tRightStNum2;
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(tChinStName, targetNum, { vertexIndex1: 'E8', vertexIndex2: 'E4', spacingX: 35 - rightEqWidth - options.xHeightChin * 0.25, spacingY: 0 });
    }
    if (tRightStNum1 && tRightStNum2) {
      const lozengeRight = createLozenge(options.xHeightNum);
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(tRightStNum1, lozengeRight, { vertexIndex1: 'E8', vertexIndex2: 'E4', spacingX: 10, spacingY: -0.2 * options.xHeightNum });
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(lozengeRight, tRightStNum2, { vertexIndex1: 'E8', vertexIndex2: 'E4', spacingX: 10, spacingY: 0.2 * options.xHeightNum });
    }

    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.scheduleRender();


    // Return the base object so createObjectWithSnapping can bind it to mouse
    return tEngStName || tChinStName || tLeftStNum1 || tRightStNum1;
  },

  /**
   * Live update text as the user types or changes parameters
   */  liveUpdateText: function (event) {
    const newXHeight = parseInt(document.getElementById('input-xHeight').value);
    let newFont = document.getElementById('Text Font-container').selected.getAttribute('data-value');
    const newColor = document.getElementById('Message Colour-container').selected.getAttribute('data-value');    // Check if text contains non-English characters and override font if needed
    const txt = document.getElementById('input-text').value;
    const containsNonEnglish = (0,_objects_text_js__WEBPACK_IMPORTED_MODULE_3__.containsNonEnglishCharacters)(txt);

    if (containsNonEnglish) {
      // Get the primary font from FontPriorityManager for Chinese text
      try {
        const fontPriorityList = _modal_md_font_js__WEBPACK_IMPORTED_MODULE_6__.FontPriorityManager.getFontPriorityList();
        // Use the first font in the priority list for Chinese text
        const priorityFont = fontPriorityList.length > 0 ? fontPriorityList[0] : 'parsedFontKorean';
        // Convert font priority names to font family names
        switch (priorityFont) {
          case 'parsedFontKorean':
            newFont = 'TW-MOE-Std-Kai';
            break;
          case 'parsedFontChinese':
            newFont = 'TW-MOE-Std-Kai';
            break;
          case 'parsedFontHK':
            newFont = 'TW-MOE-Std-Kai';
            break;
          case 'parsedFontMedium':
            newFont = 'TransportMedium';
            break;
          case 'parsedFontHeavy':
            newFont = 'TransportHeavy';
            break;
          default:
            newFont = 'TW-MOE-Std-Kai'; // Default for Chinese text
        }
      } catch (error) {
        console.warn('Could not get font from FontPriorityManager, falling back to default Chinese font:', error);
        newFont = 'TW-MOE-Std-Kai'; // Fallback to default Chinese font
      }
    }

    // Make sure we're not in placement mode
    if (FormTextAddComponent.newTextObject) {
      // If in placement mode, update the new object being placed
      let newText = document.getElementById('input-text').value;
      // Check if we're in 2Liner mode
      const isTwoLiner = FormTextAddComponent.textLineInput === 2;
      const justification = FormTextAddComponent.justification || 'Left';

      if (isTwoLiner) {
        // Determine if newTextObject is English or Chinese text
        // We'll check if it's in the EngDestinations or ChtDestinations
        let isEnglishText = !FormTextAddComponent.newTextObject.containsNonAlphabetic;

        if (isEnglishText) {
          // Case: newTextObject is English text (top line)
          const engTextObject = FormTextAddComponent.newTextObject;

          // Get corresponding Chinese text using the helper function
          const chtText = FormTextAddComponent.findCorrespondingChineseText(newText, isEnglishText);

          // Update English text object
          engTextObject.updateText(newText, newXHeight, newFont, newColor);

          // The Chinese text object should be in the anchoredPolygon list of the English text object
          if (engTextObject.anchoredPolygon && engTextObject.anchoredPolygon.length > 0) {
            const chtTextObject = engTextObject.anchoredPolygon[0];
            if (chtTextObject && chtTextObject.functionalType === 'Text' && chtTextObject.containsNonAlphabetic) {
              chtTextObject.updateText(chtText, newXHeight, newFont, newColor);
              // Set the new anchor
              const vertexIndex1 = { 'Left': 'E1', 'Middle': 'E2', 'Right': 'E3' }[justification];
              const vertexIndex2 = { 'Left': 'E7', 'Middle': 'E6', 'Right': 'E5' }[justification];
              const movingPoint = chtTextObject.getBasePolygonVertex(vertexIndex1.toUpperCase())
              const targetPoint = engTextObject.getBasePolygonVertex(vertexIndex2.toUpperCase())
              chtTextObject.set({
                left: chtTextObject.left + targetPoint.x - movingPoint.x,
                lockMovementX: true,
              });
              const anchor = { sourcePoint: vertexIndex1, targetPoint: vertexIndex2, sourceObject: chtTextObject, TargetObject: engTextObject, spacing: 0 }
              chtTextObject.lockXToPolygon = anchor
            }
          } else {
            // Case 1: isTwoLiner is true but no anchored text exists yet - create and anchor Chinese text

            // Create Chinese text object
            const chtTextObject = new _objects_text_js__WEBPACK_IMPORTED_MODULE_3__.TextObject({
              text: chtText,
              xHeight: newXHeight,
              font: newFont,
              color: newColor,
              left: engTextObject.left,
              top: engTextObject.top + newXHeight * 1.2 // Position it lower for the bottom line
            });
            chtTextObject.isTemporary = true;

            // Anchor Chinese text to English text using the justification points
            setTimeout(() => {
              const vertexIndex1 = { 'Left': 'E1', 'Middle': 'E2', 'Right': 'E3' }[justification];
              const vertexIndex2 = { 'Left': 'E7', 'Middle': 'E6', 'Right': 'E5' }[justification];

              (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(engTextObject, chtTextObject, {
                vertexIndex1: vertexIndex1,
                vertexIndex2: vertexIndex2,
                spacingX: 0,
                spacingY: 0
              });
              _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.scheduleRender();
            }, 100);
          }
        } else {
          // Case: newTextObject is Chinese text (bottom line)
          const chtTextObject = FormTextAddComponent.newTextObject;

          // Find corresponding English text
          let engText = FormTextAddComponent.findCorrespondingChineseText(newText, false);

          // Update Chinese text object
          chtTextObject.updateText(newText, newXHeight, newFont, newColor);

          // Check if it has an anchoring parent (English text)
          if (chtTextObject.lockXToPolygon && chtTextObject.lockXToPolygon.TargetObject) {
            const engTextObject = chtTextObject.lockXToPolygon.TargetObject;
            if (engTextObject && engTextObject.functionalType === 'Text') {
              engTextObject.updateText(engText, newXHeight, newFont, newColor);
            }
          } else {
            // If no anchoring parent, create a new English text object
            const engTextObject = new _objects_text_js__WEBPACK_IMPORTED_MODULE_3__.TextObject({
              text: engText,
              xHeight: newXHeight,
              font: newFont,
              color: newColor,
              left: chtTextObject.left,
              top: chtTextObject.top - newXHeight * 1.2 // Position it higher for the top line
            });
            engTextObject.isTemporary = true;

            // Anchor English text to Chinese text using reversed justification points
            setTimeout(() => {
              // Reverse the vertex indices for anchoring
              const vertexIndex1 = { 'Left': 'E7', 'Middle': 'E6', 'Right': 'E5' }[justification];
              const vertexIndex2 = { 'Left': 'E1', 'Middle': 'E2', 'Right': 'E3' }[justification];

              (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(chtTextObject, engTextObject, {
                vertexIndex1: vertexIndex1,
                vertexIndex2: vertexIndex2,
                spacingX: 0,
                spacingY: 0
              });
              _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.scheduleRender();
            }, 100);
          }
        }
        return;
      } else {
        // Check if the text object has an anchor object and trigger its deleteObject method
        if (FormTextAddComponent.newTextObject && FormTextAddComponent.newTextObject.anchoredPolygon) {
          FormTextAddComponent.newTextObject.anchoredPolygon.forEach(anchor => {
            if (anchor && typeof anchor.deleteObject === 'function') {
              anchor.deleteObject();
            }
          });
        }

        // Check if the language toggle value matches the text object type
        const languageToggleValue = document.getElementById('Language-container').selected.getAttribute('data-value');
        const isChineseSelected = languageToggleValue === 'Chinese';
        const isEnglishText = FormTextAddComponent.newTextObject && !FormTextAddComponent.newTextObject.containsNonAlphabetic;
        // If there's a mismatch between text type and selected language, we need to translate
        if ((isEnglishText && languageToggleValue === 'Chinese') || (!isEnglishText && languageToggleValue === 'English')) {
          // Find corresponding text in the other language
          const translatedText = FormTextAddComponent.findCorrespondingChineseText(newText, isEnglishText);
          if (translatedText) {
            // Update the text object with the translated text
            FormTextAddComponent.newTextObject.updateText(translatedText, newXHeight, newFont, newColor);
            document.getElementById('input-text').value = translatedText; // Update the input field with the translated text
          }
          return; // Exit early as the text has been updated
        }
        FormTextAddComponent.newTextObject.updateText(newText, newXHeight, newFont, newColor);
        return;
      }
    }

    // Otherwise update the selected object
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.canvas.off('mouse:move', FormTextAddComponent.TextOnMouseMove);
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.canvas.off('mouse:down', FormTextAddComponent.TextOnMouseClick);
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.canvas.on('mouse:down', FormTextAddComponent.EditOnMouseClick);

    const textObj = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.canvas.getActiveObject();
    if (!textObj || textObj.functionalType !== 'Text') return;
    let newText = document.getElementById('input-text').value;
    if (!newText || newText.trim() === '') newText = textObj.text; // Don't create empty text

    // Check if this is part of a two-liner set
    const isTwoLiner = textObj.anchoredPolygon && textObj.anchoredPolygon.length > 0;

    // Only call updateText if there are actual changes
    if (
      newText !== textObj.text ||
      newXHeight !== textObj.xHeight ||
      newFont !== textObj.font ||
      newColor !== textObj.color
    ) {
      // Update the active text object
      textObj.updateText(newText, newXHeight, newFont, newColor);
    }
  },

  TextHandlerOff: function () {
    // Use shared handler to clean up events and state
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.genericHandlerOff(
      FormTextAddComponent,
      'newTextObject',
      'TextOnMouseMove',
      'TextOnMouseClick',
      'cancelInput'
    );

    // Additional text-specific cleanup
    const textInput = document.getElementById('input-text');
    if (textInput) {
      textInput.removeEventListener('input', FormTextAddComponent.liveUpdateText);
    }

    const xHeightInput = document.getElementById('input-xHeight');
    if (xHeightInput) {
      xHeightInput.removeEventListener('input', FormTextAddComponent.liveUpdateText);
    }

    const colorButtons = document.querySelectorAll('#Message\\ Colour-container .toggle-button');
    colorButtons.forEach(button => {
      button.removeEventListener('click', FormTextAddComponent.liveUpdateText);
    });

    const fontButtons = document.querySelectorAll('#Text\\ Font-container .toggle-button');
    fontButtons.forEach(button => {
      button.removeEventListener('click', FormTextAddComponent.liveUpdateText);
    });

    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.canvas.off('mouse:down', FormTextAddComponent.EditOnMouseClick);
  },

  cancelInput: function (event) {
    if (event.key === 'Escape') {
      // For 2-liner text, we need special handling to remove both English and Chinese texts
      if (FormTextAddComponent.textLineInput === 2 &&
        FormTextAddComponent.newTextObject &&
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.canvas.contains(FormTextAddComponent.newTextObject)) {

        // First, check if the English text has anchored Chinese text
        if (FormTextAddComponent.newTextObject.anchoredPolygon &&
          FormTextAddComponent.newTextObject.anchoredPolygon.length > 0) {

          // Store references to both texts before deletion
          const anchoredTexts = [...FormTextAddComponent.newTextObject.anchoredPolygon];

          // Remove anchored Chinese text first to avoid orphaned objects
          anchoredTexts.forEach(anchoredText => {
            if (anchoredText && typeof anchoredText.deleteObject === 'function') {
              anchoredText.deleteObject();
            }
          });
        }

        // Then continue with the normal escape handling which will remove the English text
      }
    }

    // Use shared escape key handler for standard cleanup
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.handleCancelWithEscape(
      FormTextAddComponent,
      event,
      'newTextObject',
      'TextOnMouseMove',
      'TextOnMouseClick'
    );

    // Additional text-specific cleanup
    if (event.key === 'Escape') {
      document.getElementById('input-text').value = '';
    }
  },
  TextInputHandler: async function (event, options = null) {
    // Get text and parameters
    const txt = options ? options.text : document.getElementById('input-text').value;
    if (!txt || txt.trim() === '') return; // Don't create empty text

    const xHeight = options ? options.xHeight : parseInt(document.getElementById('input-xHeight').value);
    // Check if text contains non-English characters
    const containsNonEnglish = (0,_objects_text_js__WEBPACK_IMPORTED_MODULE_3__.containsNonEnglishCharacters)(txt);

    // Get font - use Chinese font from FontPriorityManager if text contains non-English characters
    let font;
    if (options && options.font) {
      font = options.font;
    } else if (containsNonEnglish) {
      // Get the primary font from FontPriorityManager for Chinese text
      try {
        const fontPriorityList = _modal_md_font_js__WEBPACK_IMPORTED_MODULE_6__.FontPriorityManager.getFontPriorityList();
        // Use the first font in the priority list for Chinese text
        font = fontPriorityList.length > 0 ? fontPriorityList[0] : 'parsedFontKorean';
        // Convert font priority names to font family names

      } catch (error) {
        console.warn('Could not get font from FontPriorityManager, falling back to default Chinese font:', error);
        font = 'Noto Sans KR'; // Fallback to default Chinese font
      }
    } else {
      // For English text, use the UI toggle value
      font = document.getElementById('Text Font-container').selected.getAttribute('data-value');
    }

    const color = options ? options.color : document.getElementById('Message Colour-container').selected.getAttribute('data-value');

    // Check if we're using two-liner mode
    const isTwoLiner = FormTextAddComponent.textLineInput === 2;

    // If we already have a new text object being placed, just update it instead of creating another
    if (FormTextAddComponent.newTextObject && _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.canvas.contains(FormTextAddComponent.newTextObject)) {
      FormTextAddComponent.newTextObject.updateText(txt, xHeight, font, color);
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.scheduleRender();
      return;
    }

    try {
      if (isTwoLiner) {
        // For two-liner mode, we need special handling to create and anchor two text objects
        // Get corresponding Chinese text using the helper function
        const chtText = FormTextAddComponent.findCorrespondingChineseText(txt);

        // Use the general object creation function for two-liner text
        await _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createObjectWithSnapping(
          {
            text: txt,
            translatedText: chtText,
            xHeight: xHeight,
            font: font,
            color: color
          },
          FormTextAddComponent.createTwoLinerText,
          FormTextAddComponent,
          'newTextObject',
          'E1',
          FormTextAddComponent.TextOnMouseMove,
          FormTextAddComponent.TextOnMouseClick,
          FormTextAddComponent.cancelInput
        );

      } else {

        // Use the general object creation function
        await _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createObjectWithSnapping(
          {
            text: txt,
            xHeight: xHeight,
            font: font,
            color: color
          },
          FormTextAddComponent.createTextObject,
          FormTextAddComponent,
          'newTextObject',
          'E1',
          FormTextAddComponent.TextOnMouseMove,
          FormTextAddComponent.TextOnMouseClick,
          FormTextAddComponent.cancelInput
        );
      }

    } catch (error) {
      console.error('Error creating text object:', error);
    }

    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.scheduleRender();
  },

  // For regular single-line text, use our standard pattern
  // Create a function that returns a new TextObject
  createTextObject: (options) => {
    const text = new _objects_text_js__WEBPACK_IMPORTED_MODULE_3__.TextObject({
      text: options.text,
      xHeight: options.xHeight,
      font: options.font,
      color: options.color,
      left: options.position.x,
      top: options.position.y
    });

    FormTextAddComponent.createUnderline(text, options)
    return text
  },

  // Use the general object creation with snapping function
  createTwoLinerText: (options) => {

    const justification = FormTextAddComponent.justification || 'Left';

    // Create English text object (top one)
    const engTextObject = new _objects_text_js__WEBPACK_IMPORTED_MODULE_3__.TextObject({
      text: options.text,
      xHeight: options.xHeight,
      font: options.font,
      color: options.color,
      left: options.position.x,
      top: options.position.y - options.xHeight * 0.6 // Position it higher for the top line
    });
    engTextObject.isTemporary = true;

    // Create Chinese text object (bottom one)
    const chtTextObject = new _objects_text_js__WEBPACK_IMPORTED_MODULE_3__.TextObject({
      text: options.translatedText,
      xHeight: options.xHeight,
      font: options.font,
      color: options.color,
      left: options.position.x,
      top: options.position.y + options.xHeight * 0.6 // Position it lower for the bottom line
    });
    chtTextObject.isTemporary = true;

    // Mark both objects as part of a 2-liner pair and store partner reference
    engTextObject.isTwoLiner = true;
    chtTextObject.isTwoLiner = true;
    engTextObject.twoLinerPartner = chtTextObject;
    chtTextObject.twoLinerPartner = engTextObject;

    // Anchor Chinese text to English text using the justification points
    setTimeout(() => {
      (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(engTextObject, chtTextObject, {
        vertexIndex1: { 'Left': 'E1', 'Middle': 'E2', 'Right': 'E3' }[justification],
        vertexIndex2: { 'Left': 'E7', 'Middle': 'E6', 'Right': 'E5' }[justification],
        spacingX: 0,
        spacingY: 0
      });
    }, 100);

    FormTextAddComponent.createUnderline(chtTextObject, options)

    return engTextObject; // Return the top object as the main one
  },

  createUnderline: (textObject, options) => {
    const isUnderline = options.underline ? options.underline : document.getElementById('Underline-container').selected.getAttribute('data-value') == 'Yes'

    if (isUnderline) {
      // Create underline
      const underlineObject = new DividerObject({
        xHeight: options.xHeight,
        color: options.color,
        dividerType: 'HLine',
        textObject: textObject,
        BorderGroup: null,
      });
      underlineObject.isTemporary = true;

      // Anchor Chinese text to English text using the justification points
      setTimeout(() => {
        (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_4__.anchorShape)(textObject, underlineObject, {
          vertexIndex1: 'V1',
          vertexIndex2: 'E6',
          spacingX: 0,
          spacingY: options.xHeight / 4
        });
      }, 100);

      textObject.underline = underlineObject
    }
  },

  TextOnMouseMove: function (event) {
    // Use shared mouse move handler
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.handleObjectOnMouseMove(FormTextAddComponent, event);
  },

  TextOnMouseClick: function (event, options = null) {
    if (event.e.button !== 0 && event.e.type !== 'touchend') return;

    // Use shared mouse click handler for new text objects
    if (FormTextAddComponent.newTextObject) {
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.handleObjectOnMouseClick(
        FormTextAddComponent,
        event,
        'newTextObject',
        'TextOnMouseMove',
        'TextOnMouseClick',
        'cancelInput'
      );

      // Text-specific cleanup
      document.getElementById('input-text').value = '';
      return;
    }

    // Legacy options handling - should rarely be used now
  },
  EditOnMouseClick: function (event) {
    document.getElementById('input-text').value = '';
    FormTextAddComponent.textPanelInit(null);
  },

  /**
   * Initialize the text component settings listener
   * This should be called after all modules are loaded to avoid circular dependencies
   */
  initializeSettingsListener: function () {
    // Replace the settings listener with the shared implementation
    _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings.addListener(
      _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.createSettingsListener(2, function (setting, value) {
        // Text-specific updates when settings change
        if (FormTextAddComponent.newTextObject || _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_2__.CanvasGlobals.canvas.getActiveObject()?.functionalType === 'Text') {
          FormTextAddComponent.liveUpdateText();
        }
      })
    );
  }
};



/***/ }),

/***/ "./js/sidebar/sb-tracker.js":
/*!**********************************!*\
  !*** ./js/sidebar/sb-tracker.js ***!
  \**********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   CanvasTrackerUI: () => (/* binding */ CanvasTrackerUI)
/* harmony export */ });
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../canvas/Tracker.js */ "./js/canvas/Tracker.js");
/* harmony import */ var _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../i18n/i18n.js */ "./js/i18n/i18n.js");
/* Canvas Tracker UI Component */




class CanvasTrackerUI {
  constructor() {
    this.initialized = false;
    this.trackerContainer = null;
    
    // Listen for history changes
    _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_1__.canvasTracker.addHistoryChangeCallback(() => {
        this.updateHistoryList();
    });
  }

  initialize() {
    if (typeof window.GeneralHandler !== 'undefined' && window.GeneralHandler.PanelInit) {
        window.GeneralHandler.PanelInit();
        window.GeneralHandler.tabNum = 8;
    }

    const form = document.getElementById('input-form');
    if (!form) return;
    
    form.innerHTML = `
        <div class="section-content">
          <div class="history-controls">
            <button type="button" id="clearHistory" class="primary-button" data-i18n="Clear">${_i18n_i18n_js__WEBPACK_IMPORTED_MODULE_2__.i18n.t('Clear')}</button>
            <button type="button" id="undoBtn" class="secondary-button" data-i18n="Undo">${_i18n_i18n_js__WEBPACK_IMPORTED_MODULE_2__.i18n.t('Undo')}</button>
            <button type="button" id="redoBtn" class="secondary-button" data-i18n="Redo">${_i18n_i18n_js__WEBPACK_IMPORTED_MODULE_2__.i18n.t('Redo')}</button>
          </div>
          <div id="historyList" class="history-list"></div>
        </div>
      `;

    const clearBtn = document.getElementById('clearHistory');
    if (clearBtn) {
        clearBtn.addEventListener('click', () => {
            _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_1__.canvasTracker.clearHistory();
        });
    }
    
    const undoBtn = document.getElementById('undoBtn');
    if (undoBtn) {
        undoBtn.addEventListener('click', () => {
            _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_1__.canvasTracker.undo();
        });
    }
    
    const redoBtn = document.getElementById('redoBtn');
    if (redoBtn) {
        redoBtn.addEventListener('click', () => {
            _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_1__.canvasTracker.redo();
        });
    }

    this.initialized = true;
    this.updateHistoryList();
    
    try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_2__.i18n.applyTranslations(form); } catch (_) {}
  }
  
  restoreUI() {
      this.initialize();
  }

  updateHistoryList() {
    const listContainer = document.getElementById('historyList');
    if (!listContainer) return;

    listContainer.innerHTML = '';
    
    // Reverse history to show newest first
    const history = _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_1__.canvasTracker.history;
    const currentIndex = _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_1__.canvasTracker.historyIndex;

    if (history.length === 0) {
        listContainer.innerHTML = `<div class="history-empty">${_i18n_i18n_js__WEBPACK_IMPORTED_MODULE_2__.i18n.t('No history')}</div>`;
        return;
    }

    // Create list items
    [...history].reverse().forEach((entry, reverseIndex) => {
        const realIndex = history.length - 1 - reverseIndex;
        const isCurrent = realIndex === currentIndex;
        const item = document.createElement('div');
        item.className = 'history-item';
        if (isCurrent) {
            item.classList.add('active');
        }
        
        const time = entry.timestamp || '';
        const desc = entry.description || `State ${realIndex + 1}`;
        
        item.textContent = `${time} - ${desc}`;
        
        item.addEventListener('click', () => {
            _canvas_Tracker_js__WEBPACK_IMPORTED_MODULE_1__.canvasTracker.restoreState(realIndex);
        });
        
        listContainer.appendChild(item);
    });
  }
}

const canvasTrackerUI = new CanvasTrackerUI();
window.canvasTrackerComponentInstance = canvasTrackerUI;




/***/ }),

/***/ "./js/sidebar/sbGeneral.js":
/*!*********************************!*\
  !*** ./js/sidebar/sbGeneral.js ***!
  \*********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   GeneralHandler: () => (/* binding */ GeneralHandler),
/* harmony export */   GeneralSettings: () => (/* binding */ GeneralSettings)
/* harmony export */ });
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../i18n/i18n.js */ "./js/i18n/i18n.js");
/* harmony import */ var _sb_inspector_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./sb-inspector.js */ "./js/sidebar/sb-inspector.js");
/* harmony import */ var _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../canvas/keyboardEvents.js */ "./js/canvas/keyboardEvents.js");
/* harmony import */ var _utils_hintLoader_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../utils/hintLoader.js */ "./js/utils/hintLoader.js");
/* General Sidebar Panel */



 // Import the event handler for keyboard events
 // Import the hint loader utility

// Handler registry for active sidebar component off-function
let GeneralHandler = {
  // Currently registered teardown handler for the active sidebar component
  activeComponentOff: null,
  tabNum: 0,
  panelOpened: true,
  ShowHideSideBar: function (event, force = null) {
    if (force === null) {
      if (document.getElementById("side-panel").className.indexOf("open") !== -1) {
        GeneralHandler.HideSideBar()
      } else {
        GeneralHandler.ShowSideBar()
      }
    } else if (force === 'on') {
      GeneralHandler.ShowSideBar()
    } else {
      GeneralHandler.HideSideBar()
    }
  },
  ShowSideBar: function () {
    document.getElementById("side-panel").className = "side-panel"
    document.getElementById("side-panel").className += " open"
    document.getElementById('show_hide').childNodes[0].className = "fa fa-angle-double-left"
    GeneralHandler.panelOpened = true
  },
  HideSideBar: function () {
    document.getElementById("side-panel").className = "side-panel"
    document.getElementById("side-panel").className += " close"
    document.getElementById('show_hide').childNodes[0].className = "fa fa-angle-double-right"
    GeneralHandler.panelOpened = false
    GeneralHandler.PanelHandlerOff()
  },
  /**
   * Invoke and clear the registered component off handler when closing the panel
   */
  PanelHandlerOff: function () {
    if (typeof this.activeComponentOff === 'function') {
      try {
        this.activeComponentOff();
      } catch (err) {
        console.error('Error in activeComponentOff:', err);
      }
      this.activeComponentOff = null;
    }
  },
  /**
 * Register the active component off handler
 * @param {Function} handler
 */
  setActiveComponentOff: function (handler) {
    this.activeComponentOff = handler;
  },
  PanelInit: () => {
    GeneralHandler.ShowHideSideBar(null, "on")
    GeneralHandler.PanelHandlerOff()
    if (document.getElementById("side-panel").className.indexOf("open") !== -1) {
      var parent = document.getElementById("input-form");

      // Clean up any existing general button tooltips before clearing the form
      const buttonsWithTooltips = parent.querySelectorAll('button[data-tooltip], .tooltip-btn');
      buttonsWithTooltips.forEach(button => {
        if (button.cleanupGeneralTooltip) {
          button.cleanupGeneralTooltip();
        }
      });

      // Also clean up any orphaned tooltips that may exist
      GeneralHandler.cleanupOrphanedTooltips();

      // Force hide any visible tooltips (especially useful for mobile)
      GeneralHandler.hideAllTooltips();

      parent.innerHTML = ''
    }
    _sb_inspector_js__WEBPACK_IMPORTED_MODULE_2__.CanvasObjectInspector.createObjectListPanelInit()
    return parent
  },
  createNode: function (type, attribute, parent, callback, event) {
    var node = document.createElement(type);
    for (const [key, value] of Object.entries(attribute)) {
      node.setAttribute(key, value)
    }
    parent.appendChild(node);
    if (callback) {
      node.addEventListener(event, callback)
    }
    return node
  },
  /**
   * Create a DOM node with i18n support and append to parent
   * @param {string} type - Element tag name (e.g., 'div', 'label')
   * @param {Object} attribute - Attributes to set on the element
   * @param {HTMLElement} parent - Parent to append to
   * @param {string} key - Translation key or raw text
   * @param {('text'|'html'|'placeholder'|'tooltip')} mode - How to apply translation; default 'text'
   * @returns {HTMLElement}
   */
  createI18nNode: function (type, attribute, parent, key, mode = 'text') {
    const node = document.createElement(type);
    for (const [attrKey, value] of Object.entries(attribute || {})) {
      node.setAttribute(attrKey, value);
    }

    try {
      switch (mode) {
        case 'html':
          node.setAttribute('data-i18n-html', key);
          node.innerHTML = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(key);
          break;
        case 'placeholder':
          node.setAttribute('data-i18n-placeholder', key);
          node.setAttribute('placeholder', _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(key));
          break;
        case 'tooltip':
          node.setAttribute('data-i18n-tooltip', key);
          const val = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(key);
          node.setAttribute('data-tooltip', val);
          node.setAttribute('title', val);
          break;
        case 'text':
        default:
          node.setAttribute('data-i18n', key);
          node.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(key);
          break;
      }
    } catch (_) {
      // Fallback: set plain text/attrs even if i18n fails
      if (mode === 'html') node.innerHTML = key; else if (mode === 'placeholder') node.setAttribute('placeholder', key); else node.textContent = key;
    }

    parent.appendChild(node);
    return node;
  },
  createButton: function (name, labelTxt, parent, container = 'input', callback = null, event = null) {
    if (container) {
      var inputContainer = GeneralHandler.createNode("div", { 'class': `${container}-container` }, parent)
    }
    var input = GeneralHandler.createNode("button", { 'type': 'button', 'class': `${container ? container : name}-button`, 'id': name, 'placeholder': ' ' }, inputContainer ? inputContainer : parent, callback, event)

    // If label contains HTML (e.g., <i class="fa ..."></i>), render as HTML and skip i18n text replacement
    const containsHTML = /<[^>]+>/.test(String(labelTxt));
    if (containsHTML) {
      input.innerHTML = labelTxt;
    } else {
      input.setAttribute('data-i18n', labelTxt);
      // Set initial text; i18n.applyTranslations may update later
      try { input.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(labelTxt); } catch (_) { input.textContent = String(labelTxt); }
    }
    return input
  },

  createSVGButton: function (name, svg, parent, container = 'input', callback = null, event = null) {
    if (container) {
      var inputContainer = GeneralHandler.createNode("div", { 'class': `${container}-container` }, parent)
    }
    var input = GeneralHandler.createNode("button", { 'type': 'button', 'class': `${container ? container : name}-button`, 'id': name, 'placeholder': ' ' }, inputContainer ? inputContainer : parent, callback, event)

    // Add the SVG
    input.innerHTML = svg;

    // Add a separator line
    const separator = document.createElement('hr');
    separator.className = 'symbol-separator';
    input.appendChild(separator);

    // Add text label
    const textLabel = document.createElement('div');
    textLabel.className = 'symbol-label';
    const labelKey = name.replace('button-', '');
    textLabel.setAttribute('data-i18n', labelKey);
    try { textLabel.innerText = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(labelKey); } catch (_) { textLabel.innerText = labelKey; }
    input.appendChild(textLabel);

    return input
  },
  createInput: function (name, labelTxt, parent, defaultV = null, callback = null, event = null, unit = null) {
    var inputContainer = GeneralHandler.createNode("div", { 'class': 'input-container' }, parent)
    //var labelEdge = GeneralHandler.createNode("div", { 'class': 'cut' }, inputContainer)
    var label = GeneralHandler.createNode("div", { 'class': 'placeholder', 'for': name }, inputContainer)
    label.setAttribute('data-i18n', labelTxt)

    // Create a wrapper div for input field with units if needed
    var inputWrapperClass = unit ? 'input-wrapper' : '';
    var inputWrapper = unit ? GeneralHandler.createNode("div", { 'class': inputWrapperClass }, inputContainer) : inputContainer;

    // Create the input element
    var inputClass = unit ? 'input with-unit' : 'input';
    var input = GeneralHandler.createNode("input", { 'type': 'text', 'class': inputClass, 'id': name, 'placeholder': ' ' }, inputWrapper, callback, event)

    // Add unit span if unit is specified (with i18n support)
    if (unit) {
      var unitSpan = GeneralHandler.createNode("span", { 'class': 'input-unit' }, inputWrapper)
      // Apply i18n translation and attribute; fallback to raw string
      try {
        unitSpan.setAttribute('data-i18n', unit);
        unitSpan.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(unit);
      } catch (_) {
        unitSpan.textContent = String(unit);
      }
    }

    label.innerHTML = labelTxt
    defaultV ? input.value = defaultV : input.value = ''
    return input
  },

  createSelect: function (name, labelTxt, options, parent, defaultV = null, callback = null, event = 'change') {
    var inputContainer = GeneralHandler.createNode("div", { 'class': 'input-container' }, parent)
    var label = GeneralHandler.createNode("div", { 'class': 'placeholder', 'for': name }, inputContainer)
    label.setAttribute('data-i18n', labelTxt)
    var input = GeneralHandler.createNode("select", { 'class': 'input', 'id': name, 'placeholder': ' ' }, inputContainer, callback, event)
    label.innerHTML = labelTxt
    for (var i = 0; i < options.length; i++) {
      var optionDef = options[i];
      var option = document.createElement("option");
      // Support both string and object option definitions
      if (typeof optionDef === 'object' && optionDef !== null) {
        const value = optionDef.value != null ? optionDef.value : optionDef.label;
        const label = optionDef.label != null ? optionDef.label : String(value);
        option.value = value;
        option.setAttribute('data-i18n', label);
        // Use i18n translated text if available, else fallback
        try { option.text = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(label); } catch (_) { option.text = label; }
      } else {
        option.value = optionDef;
        option.setAttribute('data-i18n', optionDef);
        try { option.text = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(optionDef); } catch (_) { option.text = String(optionDef); }
      }
      input.appendChild(option);
    }
    if (defaultV !== null) {
      input.value = defaultV;
    }
    return input
  },

  createToggle: function (name, options, parent, defaultSelected = null, callback = null, maxItemsPerRow = null) {
    // Create a container for the toggle including its label
    var inputContainer = GeneralHandler.createNode("div", { 'class': 'input-container' }, parent);

    // Create the label with i18n (supports HTML like <br>)
    var label = GeneralHandler.createNode("div", { 'class': 'placeholder', 'for': name }, inputContainer);

    const translatedLabel = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(name);
    label.setAttribute('data-i18n-html', name);
    label.innerHTML = translatedLabel;


    // Determine if we need multi-row layout
    const needsMultiRow = maxItemsPerRow && options.length > maxItemsPerRow;
    const containerClass = needsMultiRow ? 'toggle-container multi-row' : 'toggle-container';

    // Create a container for the toggle buttons
    var toggleContainer = GeneralHandler.createNode("div", { 'class': containerClass, 'id': name + '-container' }, inputContainer);

    // If multi-row, set CSS custom property for items per row
    if (needsMultiRow) {
      toggleContainer.style.setProperty('--items-per-row', maxItemsPerRow);
    }

    // Keep track of the buttons to manage their state
    let toggleButtons = [];

    // Create a button for each option
    options.forEach((option, index) => {
      const value = (typeof option === 'object' && option !== null) ? (option.value ?? option.label ?? '') : option;
      const labelKey = (typeof option === 'object' && option !== null) ? (option.label ?? option.value ?? '') : option;

      let buttonId = `${name}-${index}`;
      let button = GeneralHandler.createNode("button", {
        'type': 'button',
        'class': 'toggle-button',
        'id': buttonId,
        'data-value': value
      }, toggleContainer);

      try {
        const translated = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(labelKey);
        if (/<[^>]+>/.test(translated)) {
          button.setAttribute('data-i18n-html', labelKey);
          button.innerHTML = translated;
        } else {
          button.setAttribute('data-i18n', labelKey);
          button.textContent = translated;
        }
      } catch (_) {
        button.textContent = String(labelKey);
      }
      toggleButtons.push(button);

      // Add click event to handle toggle behavior
      button.addEventListener('click', function () {
        // Remove active class from all buttons
        toggleButtons.forEach(btn => {
          btn.classList.remove('active');
        });

        // Add active class to clicked button
        this.classList.add('active');
        toggleContainer.selected = this;

        // Call callback if provided
        if (callback) {
          callback(this);
        }
      });

      // Set default selected button
      if (defaultSelected !== null && value === defaultSelected) {
        button.classList.add('active');
        toggleContainer.selected = button;
      } else if (defaultSelected === null && index === 0) {
        // If no default is specified, select the first option
        button.classList.add('active');
        toggleContainer.selected = button;
      }
    });

    return toggleContainer;
  },

  /**
   * Creates a custom dropdown with image support
   * @param {string} name - Name/ID of the dropdown
   * @param {Array} options - Array of strings or objects {value, label, image}
   * @param {HTMLElement} parent - Parent element
   * @param {string} defaultVal - Default selected value
   * @param {Function} callback - Callback function on change
   */
  createImageDropdown: function (name, options, parent, defaultVal, callback) {
    const container = GeneralHandler.createNode("div", { 'class': 'input-container custom-dropdown-container', 'id': `${name}-container` }, parent);

    // Label
    const label = GeneralHandler.createNode("div", { 'class': 'placeholder', 'for': name }, container);
    label.setAttribute('data-i18n', name);
    try { label.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(name); } catch (_) { label.textContent = name; }

    // Dropdown wrapper
    const wrapper = GeneralHandler.createNode("div", { 'class': 'custom-select-wrapper', 'id': name }, container);

    // Selected item display
    const selectedDisplay = GeneralHandler.createNode("div", { 'class': 'custom-select-trigger' }, wrapper);
    const selectedText = GeneralHandler.createNode("span", {}, selectedDisplay);

    // Arrow icon
    const arrow = GeneralHandler.createNode("div", { 'class': 'arrow' }, selectedDisplay);

    // Options list
    const optionsList = GeneralHandler.createNode("div", { 'class': 'custom-options' }, wrapper);

    // Set initial value
    let currentValue = defaultVal;
    container.setAttribute('data-value', currentValue);
    
    // We need to find the label for the default value to set selectedText correctly
    let defaultLabel = currentValue;

    // Create options
    options.forEach(opt => {
      const val = typeof opt === 'object' ? opt.value : opt;
      const txt = typeof opt === 'object' ? opt.label : opt;

      const optionDiv = GeneralHandler.createNode("div", { 'class': 'custom-option', 'data-value': val }, optionsList);

      // Image placeholder
      const imgPlaceholder = GeneralHandler.createNode("div", { 'class': 'option-image-placeholder' }, optionDiv);

      // If image path is provided, use it
      if (opt.image) {
        const img = GeneralHandler.createNode("img", { 'src': '/images/' + opt.image, 'alt': txt }, imgPlaceholder);
      }

      const textSpan = GeneralHandler.createNode("span", {}, optionDiv);
      textSpan.setAttribute('data-i18n', txt);
      try { textSpan.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(txt); } catch (_) { textSpan.textContent = txt; }

      if (val === currentValue) {
        optionDiv.classList.add('selected');
        selectedText.setAttribute('data-i18n', txt);
        try { selectedText.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(txt); } catch (_) { selectedText.textContent = txt; }
        defaultLabel = txt;
      }

      optionDiv.addEventListener('click', function (e) {
        e.stopPropagation();
        currentValue = val;
        container.setAttribute('data-value', currentValue);
        
        selectedText.setAttribute('data-i18n', txt);
        try { selectedText.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(txt); } catch (_) { selectedText.textContent = txt; }

        // Update selected class
        const allOptions = optionsList.querySelectorAll('.custom-option');
        allOptions.forEach(o => o.classList.remove('selected'));
        optionDiv.classList.add('selected');

        wrapper.classList.remove('open');

        if (callback) callback();
      });
    });
    
    // If selectedText is still empty (defaultVal not in options?), set it
    if (!selectedText.textContent) {
         selectedText.textContent = defaultLabel;
    }

    // Toggle dropdown
    selectedDisplay.addEventListener('click', function (e) {
      wrapper.classList.toggle('open');
      e.stopPropagation();
    });

    // Close on click outside
    document.addEventListener('click', function (e) {
      if (!wrapper.contains(e.target)) {
        wrapper.classList.remove('open');
      }
    });

    return container;
  },

  /**
   * Gets the value of the active button in a toggle container
   * @param {string} containerId - The ID of the toggle container
   * @return {string|null} The value of the active button or null if not found
   */
  getToggleValue: function (containerId) {
    const container = document.getElementById(containerId);
    if (!container) return null;

    // Method 1: Use the selected property we store on the container
    if (container.selected) {
      return container.selected.getAttribute('data-value');
    }

    // Method 2: Find the active button using DOM query (fallback)
    const activeButton = container.querySelector('.toggle-button.active');
    return activeButton ? activeButton.getAttribute('data-value') : null;
  },

  /**
   * Creates the standard basic parameters container with x-Height and Message Color inputs
   * @param {HTMLElement} parent - Parent container
   * @param {Object} componentContext - The sidebar component context (this)
   * @param {number} defaultXHeight - Optional default xHeight to use (defaults to GeneralSettings)
   * @param {string} defaultColor - Optional default color to use (defaults to GeneralSettings)
   * @param {Function} xHeightCallback - Optional callback for xHeight changes
   * @param {Function} colorCallback - Optional callback for color changes
   * @return {HTMLElement} The created container
   */  createBasicParamsContainer: function (parent, componentContext, defaultXHeight = null, defaultColor = null, xHeightCallback = null, colorCallback = null) {
    // Create a container for basic parameters
    const basicParamsContainer = GeneralHandler.createNode("div", { 'class': 'input-group-container' }, parent);

    // Use provided defaults or fall back to GeneralSettings
    const xHeight = defaultXHeight !== null ? defaultXHeight : GeneralSettings.xHeight;
    const color = defaultColor !== null ? defaultColor : GeneralSettings.messageColor;

    // Create wrapper function for xHeight that calls both handlers
    const xHeightHandler = function (event) {
      // Always call the universal handler first
      GeneralHandler.handleXHeightChange(event);
      // Then call the provided callback if it exists
      if (xHeightCallback) {
        xHeightCallback(event);
      }
    };

    // Create wrapper function for color that calls both handlers  
    const colorHandler = function (event) {
      // Always call the universal handler first
      GeneralHandler.handleColorChange(event);
      // Then call the provided callback if it exists
      if (colorCallback) {
        colorCallback(event);
      }
    };

    // Create xHeight input with combined handler and unit (mm)
    const xHeightInput = GeneralHandler.createInput('input-xHeight', 'x Height', basicParamsContainer,
      xHeight, xHeightHandler, 'input', 'mm');

    // Add help icon with hint for x-height guidance (TPDM reference)
    try {
      const xhParent = xHeightInput.parentElement && xHeightInput.parentElement.classList.contains('input-wrapper')
        ? xHeightInput.parentElement.parentElement
        : xHeightInput.parentElement || basicParamsContainer;
      if (xhParent) {
        GeneralHandler.createHelpIconWithHint(xhParent, 'text/XHeight', { position: 'right' });
      }
    } catch (e) {
      console.warn('Failed to attach x-height help icon:', e);
    }

    // Create color toggle with combined handler
    const messageColorToggle = GeneralHandler.createToggle('Message Colour', ['Black', 'White'], basicParamsContainer, color,
      colorHandler);

    // Add help icon with hint for message colour selection (based on panel/background)
    try {
      const toggleParent = messageColorToggle && messageColorToggle.parentElement ? messageColorToggle.parentElement : basicParamsContainer;
      if (toggleParent) {
        GeneralHandler.createHelpIconWithHint(toggleParent, 'text/MessageColor', { position: 'right' });
      }
    } catch (e) {
      console.warn('Failed to attach message colour help icon:', e);
    }

    // Add debounced event listener for real-time updates on xHeight input
    xHeightInput.addEventListener('input', GeneralHandler.debounce(function (e) {
      // The handleXHeightChange function will update GeneralSettings
    }, 300));

    return basicParamsContainer;
  },

  /**
   * Generic handler for xHeight changes that updates GeneralSettings and the relevant component
   * @param {Event} event - The input event
   */
  handleXHeightChange: function (event) {
    const xHeight = parseInt(event.target.value);
    if (!isNaN(xHeight) && xHeight > 0) {
      // Update GeneralSettings
      GeneralSettings.updateSetting('xHeight', xHeight);

      // The specific component update logic will be handled by the listener in each component
    }
  },

  /**
   * Generic handler for color changes that updates GeneralSettings and the relevant component
   * @param {Event} event - The button click event
   */
  handleColorChange: function (event) {
    const color = event.getAttribute('data-value');
    if (color) {
      // Update GeneralSettings
      GeneralSettings.updateSetting('messageColor', color);

      // The specific component update logic will be handled by the listener in each component
    }
  },

  /**
   * Creates a standard settings listener that updates UI elements when settings change
   * @param {number} tabNumber - The tab number for this panel
   * @param {Function} updateCallback - Optional callback for additional updates
   * @return {Function} The listener function
   */
  createSettingsListener: function (tabNumber, updateCallback = null) {
    return function (setting, value) {
      // Only update UI if we're in the correct panel
      if (GeneralHandler.tabNum === tabNumber) {
        if (setting === 'xHeight') {
          const xHeightInput = document.getElementById('input-xHeight');
          if (xHeightInput && xHeightInput.value !== value.toString()) {
            xHeightInput.value = value;

            // Call the custom update callback if provided
            if (updateCallback) {
              updateCallback('xHeight', value);
            }
          }
        } else if (setting === 'messageColor') {
          const messageColorContainer = document.getElementById('Message Colour-container');
          if (messageColorContainer) {
            const buttons = messageColorContainer.querySelectorAll('button');
            buttons.forEach(btn => {
              if (btn.getAttribute('data-value') === value) {
                // Simulate a click on the button if it's not already active
                if (!btn.classList.contains('active')) {
                  btn.click();
                }
              }
            });
          }

          // Call the custom update callback if provided
          if (updateCallback) {
            updateCallback('messageColor', value);
          }
        }
      }
    };
  },

  /**
   * Utility function to handle mouse move events for canvas objects
   * @param {Object} component - The sidebar component context 
   * @param {Event} event - The mouse move event
   */
  handleObjectOnMouseMove: function (component, event) {
    const objectKey = component.newTextObject ? 'newTextObject' :
      component.newSymbolObject ? 'newSymbolObject' :
        component.newMapObject ? 'newMapObject' : null;

    if (!objectKey || !component[objectKey] || !_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex) return;

    const pointer = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.getPointer(event.e);

    // If we have an active vertex, let it handle the movement
    if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.handleMouseMoveRef) {
      // Simulate a mouse move event with the current pointer
      const simulatedEvent = {
        e: event.e,
        pointer: pointer
      };
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.handleMouseMoveRef(simulatedEvent);
    } else {
      // Fallback direct positioning if vertex control isn't active
      component[objectKey].set({
        left: pointer.x,
        top: pointer.y
      });
      component[objectKey].setCoords();
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
    }
  },

  /**
   * Utility function to handle mouse click events for canvas objects
   * @param {Object} component - The sidebar component context
   * @param {Event} event - The mouse click event
   * @param {string} objectKey - Key for the object in the component (e.g., 'newTextObject')
   * @param {string} mouseMoveHandlerName - Name of the mouse move handler function
   * @param {string} mouseClickHandlerName - Name of the mouse click handler function
   * @param {string} cancelHandlerName - Name of the cancel handler function
   */
  handleObjectOnMouseClick: function (component, event, objectKey, mouseMoveHandlerName, mouseClickHandlerName, cancelHandlerName) {
    if (event.e.button !== 0 && event.e.type !== 'touchend') return;

    // Complete the placement if we have an active object
    if (component[objectKey]) {
      // Complete the placement
      if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex) {
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.handleMouseUpRef(event);
      }

      // Clean up
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.off('mouse:move', component[mouseMoveHandlerName]);
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.off('mouse:up', component[mouseClickHandlerName]);
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.discardActiveObject();

      // Reset state
      component[objectKey].isTemporary = false;
      component[objectKey] = null;
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex = null;

      // Reattach default keyboard event listener
      document.removeEventListener('keydown', component[cancelHandlerName]);
      document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_3__.ShowHideSideBarEvent);

  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
    }
  },

  /**
   * Generic cancel handler for escape key
   * @param {Object} component - The sidebar component context
   * @param {Event} event - The keydown event
   * @param {string} objectKey - Key for the object in the component (e.g., 'newTextObject')
   * @param {string} mouseMoveHandlerName - Name of the mouse move handler function
   * @param {string} mouseClickHandlerName - Name of the mouse click handler function
   */
  handleCancelWithEscape: function (component, event, objectKey, mouseMoveHandlerName, mouseClickHandlerName) {
    if (event.key === 'Escape') {
      // If there's a newly created object being placed, delete it
      if (component[objectKey] && _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.contains(component[objectKey])) {
        component[objectKey].deleteObject();
        component[objectKey] = null;
      }

      // Clean up active vertex if there is one
      if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex) {
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.cleanupDrag();
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex = null;
      }

      // Restore event listeners
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.off('mouse:move', component[mouseMoveHandlerName]);
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.off('mouse:up', component[mouseClickHandlerName]);
      document.removeEventListener('keydown', component.cancelInput || component.cancelDraw);
      document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_3__.ShowHideSideBarEvent);

  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
    }
  },

  /**
   * Creates a help icon with hints that displays content based on a toggle value
   * @param {HTMLElement} parent - Parent container to attach the help icon
   * @param {string} toggleContainerId - ID of the toggle container to monitor
   * @param {Object} contentMap - Object mapping toggle values to help content
   * @param {Object} options - Optional configuration {position: 'left'|'right'|'top'|'bottom', showDelay: number, hideDelay: number}
   * @return {HTMLElement} The created help icon element
   */
  createHelpIconWithTooltip: function (parent, toggleContainerId, contentMap, options = {}) {
    // Default options
    const defaultOptions = {
      position: 'left',   // Default hints position
      scrollable: true,   // Make hints scrollable by default
      showDelay: 200,     // Delay in milliseconds before showing hints
      hideDelay: 800      // Delay in milliseconds before hiding hints (longer linger time)
    };
    const config = Object.assign(defaultOptions, options);

    // Create help icon container
    const helpContainer = GeneralHandler.createNode("div", {
      'class': 'help-icon-container'
    }, parent);

    // Create the circled question mark
    const helpIcon = GeneralHandler.createNode("div", {
      'class': 'help-icon',
      'title': 'Hover for help' // Basic fallback hints
    }, helpContainer);
    helpIcon.innerHTML = '?';

    // Ensure the help icon has proper styles for interaction
    helpIcon.style.pointerEvents = 'auto';
    helpIcon.style.cursor = 'help';
    helpIcon.style.zIndex = '1001';

    // Create hints element
    const hints = GeneralHandler.createNode("div", {
      'class': `hints help-hints ${config.scrollable ? 'scrollable' : ''}`
    }, document.body);

    // Store timeout references for showing and hiding hints
    let showTimeout = null;
    let hideTimeout = null;
    let isHintsVisible = false;

    // Function to reposition hints on window resize
    const repositionHints = () => {
      if (isHintsVisible && hints.style.opacity === '1') {
        GeneralHandler.positionTooltip(hints, helpIcon, config.position, true);
      }
    };

    // Add window resize listener for responsive repositioning
    window.addEventListener('resize', GeneralHandler.debounce(repositionHints, 150));

    // Function to get current content based on toggle value
    const getCurrentContent = () => {
      const toggleContainer = document.getElementById(toggleContainerId);
      if (!toggleContainer) return 'Help content not available';

      const activeValue = GeneralHandler.getToggleValue(toggleContainerId);
      return contentMap[activeValue] || 'No help available for this option';
    };

    // Function to show hints with delay
    const showHints = (event) => {
      // Clear any pending hide timeout
      if (hideTimeout) {
        clearTimeout(hideTimeout);
        hideTimeout = null;
      }

      // Clear any existing show timeout
      if (showTimeout) {
        clearTimeout(showTimeout);
      }

      // Set new timeout for showing
      showTimeout = setTimeout(() => {
        const content = getCurrentContent();
        hints.innerHTML = content;

        // Add close button to hints
        GeneralHandler.addCloseButtonToHints(hints, hideHints);

        hints.style.visibility = 'visible';
        hints.style.opacity = '1';
        hints.style.pointerEvents = 'auto';
        isHintsVisible = true;

        // Position the hints
        GeneralHandler.positionTooltip(hints, helpIcon, config.position, true); // true = outsideSidebar
        showTimeout = null;
      }, config.showDelay);
    };

    // Function to hide hints with delay
    const hideHints = () => {
      // Clear any pending show timeout
      if (showTimeout) {
        clearTimeout(showTimeout);
        showTimeout = null;
      }

      // Clear any existing hide timeout
      if (hideTimeout) {
        clearTimeout(hideTimeout);
      }

      // Set new timeout for hiding
      hideTimeout = setTimeout(() => {
        hints.style.opacity = '0';
        hints.style.pointerEvents = 'none';
        isHintsVisible = false;
        // Hide visibility after transition completes
        setTimeout(() => {
          if (hints.style.opacity === '0') {
            hints.style.visibility = 'hidden';
          }
        }, 200); // Match the CSS transition duration
        hideTimeout = null;
      }, config.hideDelay);
    };

    // Function to cancel hide and show immediately (for when mouse enters hints)
    const cancelHideAndShow = () => {
      // Clear both timeouts
      if (hideTimeout) {
        clearTimeout(hideTimeout);
        hideTimeout = null;
      }
      if (showTimeout) {
        clearTimeout(showTimeout);
        showTimeout = null;
      }

      // Also clear the mobile touch system timers if they exist
      if (helpIconListeners && helpIconListeners.timers) {
        helpIconListeners.timers.clearHide();
        helpIconListeners.timers.clearLongPress();
      }

      // Show immediately if not already visible
      if (hints.style.opacity !== '1') {
        const content = getCurrentContent();
        hints.innerHTML = content;

        // Add close button to hints
        GeneralHandler.addCloseButtonToHints(hints, hideHints);

        hints.style.visibility = 'visible';
        hints.style.opacity = '1';
        hints.style.pointerEvents = 'auto';
        isHintsVisible = true;
        GeneralHandler.positionTooltip(hints, helpIcon, config.position, true); // true = outsideSidebar
      }
    };

    // Function to hide immediately (for when mouse leaves hints)
    const hideImmediately = () => {
      // Clear show timeout if pending
      if (showTimeout) {
        clearTimeout(showTimeout);
        showTimeout = null;
      }

      // Also clear mobile touch system timers if they exist
      if (helpIconListeners && helpIconListeners.timers) {
        helpIconListeners.timers.clearLongPress();
      }

      // Hide with the configured delay
      hideHints();
    };

    // Add event listeners for help icon with mobile compatibility
    let helpIconListeners = null;
    let hintsListeners = null;

    try {
      // Add mobile-compatible tooltip listeners to help icon
      helpIconListeners = GeneralHandler.addTooltipEventListeners(
        helpIcon,
        showHints,
        hideHints,
        {
          longPressDelay: 400,  // Slightly shorter for help icons
          hideDelay: 3000,      // Longer display time for help content
          preventScroll: true
        }
      );

      // Add a click event as a fallback test (still useful for debugging)
      helpIcon.addEventListener('click', function (e) {
        e.preventDefault();
        // Show hints on click as fallback
        showHints(e);
      });
    } catch (error) {
      console.error('Error attaching help icon event listeners:', error);
    }

    // Add event listeners for hints itself to prevent hiding when hovering over it (desktop only)
    try {
      if (GeneralHandler.supportsHover()) {
        hints.addEventListener('mouseenter', cancelHideAndShow);
        hints.addEventListener('mouseleave', hideImmediately);
      }
    } catch (error) {
      console.error('Error attaching hints event listeners:', error);
    }

    // Store reference to cleanup function
    helpIcon.cleanupTooltip = () => {
      if (showTimeout) {
        clearTimeout(showTimeout);
        showTimeout = null;
      }
      if (hideTimeout) {
        clearTimeout(hideTimeout);
        hideTimeout = null;
      }

      // Cleanup mobile-compatible event listeners
      if (helpIconListeners) {
        helpIconListeners.cleanup();
      }

      // Remove hints event listeners (desktop only)
      if (GeneralHandler.supportsHover()) {
        hints.removeEventListener('mouseenter', cancelHideAndShow);
        hints.removeEventListener('mouseleave', hideImmediately);
      }

      // Remove resize listener
      window.removeEventListener('resize', repositionHints);
      if (document.body.contains(hints)) {
        document.body.removeChild(hints);
      }
    };

    return helpIcon;
  },

  /**
   * Creates a help icon with hint content loaded via HintLoader
   * @param {HTMLElement} parent - Parent element to append the help icon to
   * @param {string} hintPath - Path to the hint file (e.g., 'symbols/MainRoadShape')
   * @param {Object} options - Configuration options for the tooltip
   * @return {HTMLElement} The created help icon element
   */
  createHelpIconWithHint: function (parent, hintPath, options = {}) {
    // Default options
    const defaultOptions = {
      position: 'right',   // Default hints position
      scrollable: true,   // Make hints scrollable by default
      showDelay: 150,     // Delay in milliseconds before showing hints
      hideDelay: 1000      // Delay in milliseconds before hiding hints (longer linger time)
    };
    const config = Object.assign(defaultOptions, options);

    // Locate input label
    const label = parent.querySelector('.placeholder');
    // Prepare label so i18n updates don't wipe help icon
    if (label) {
      GeneralHandler.prepareLabelForHelpIcon(label);
    }

    // Create help icon container
    const helpContainer = GeneralHandler.createNode("div", {
      'class': 'help-icon-container'
    }, label);

    // Create the circled question mark
    const helpIcon = GeneralHandler.createNode("div", {
      'class': 'help-icon',
      'title': 'Hover for help' // Basic fallback hints
    }, helpContainer);
    helpIcon.innerHTML = '?';

    // Ensure the help icon has proper styles for interaction
    helpIcon.style.pointerEvents = 'auto';
    helpIcon.style.cursor = 'help';
    helpIcon.style.zIndex = '1001';

    // Create hints element
    const hints = GeneralHandler.createNode("div", {
      'class': `hints help-hints ${config.scrollable ? 'scrollable' : ''}`
    }, document.body);

    // Store timeout references for showing and hiding hints
    let showTimeout = null;
    let hideTimeout = null;
    let isHintsVisible = false;
    let hintContent = null;

    // Function to reposition hints on window resize
    const repositionHints = () => {
      if (isHintsVisible && hints.style.opacity === '1') {
        GeneralHandler.positionTooltip(hints, helpIcon, config.position, true);
      }
    };

    // Add window resize listener for responsive repositioning
    window.addEventListener('resize', GeneralHandler.debounce(repositionHints, 150));

    // Function to load hint content
    const loadHintContent = async () => {
      if (hintContent === null) {
        try {
          hintContent = await _utils_hintLoader_js__WEBPACK_IMPORTED_MODULE_4__.HintLoader.loadHint(hintPath);
        } catch (error) {
          console.warn(`Failed to load hint for ${hintPath}:`, error);
          hintContent = 'Help content not available';
        }
      }
      return hintContent;
    };

    // Function to show hints with delay
    const showHints = async (event) => {
      // Clear any pending hide timeout
      if (hideTimeout) {
        clearTimeout(hideTimeout);
        hideTimeout = null;
      }

      // Clear any existing show timeout
      if (showTimeout) {
        clearTimeout(showTimeout);
      }

      // Set new timeout for showing
      showTimeout = setTimeout(async () => {
  const content = await loadHintContent();
  hints.innerHTML = content;
  // Apply i18n translations to dynamically loaded hint content
  try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.applyTranslations(hints); } catch (_) {}

        // Add close button to hints
        GeneralHandler.addCloseButtonToHints(hints, hideHints);

        hints.style.visibility = 'visible';
        hints.style.opacity = '1';
        hints.style.pointerEvents = 'auto';
        isHintsVisible = true;

        // Position the hints
        GeneralHandler.positionTooltip(hints, helpIcon, config.position, true); // true = outsideSidebar
        showTimeout = null;
      }, config.showDelay);
    };

    // Function to hide hints with delay
    const hideHints = () => {
      // Clear any pending show timeout
      if (showTimeout) {
        clearTimeout(showTimeout);
        showTimeout = null;
      }

      // Clear any existing hide timeout
      if (hideTimeout) {
        clearTimeout(hideTimeout);
      }

      // Set new timeout for hiding
      hideTimeout = setTimeout(() => {
        hints.style.opacity = '0';
        hints.style.pointerEvents = 'none';
        isHintsVisible = false;
        // Hide visibility after transition completes
        setTimeout(() => {
          if (hints.style.opacity === '0') {
            hints.style.visibility = 'hidden';
          }
        }, 200); // Match the CSS transition duration
        hideTimeout = null;
      }, config.hideDelay);
    };

    // Function to cancel hide and show immediately (for when mouse enters hints)
    const cancelHideAndShow = async () => {
      // Clear both timeouts
      if (hideTimeout) {
        clearTimeout(hideTimeout);
        hideTimeout = null;
      }
      if (showTimeout) {
        clearTimeout(showTimeout);
        showTimeout = null;
      }

      // Also clear mobile touch system timers if they exist
      if (helpIconListeners && helpIconListeners.timers) {
        helpIconListeners.timers.clearHide();
        helpIconListeners.timers.clearLongPress();
      }

      // Show immediately if not already visible
      if (hints.style.opacity !== '1') {
  const content = await loadHintContent();
  hints.innerHTML = content;
  // Apply i18n translations to dynamically loaded hint content
  try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.applyTranslations(hints); } catch (_) {}

        // Add close button to hints
        GeneralHandler.addCloseButtonToHints(hints, hideHints);

        hints.style.visibility = 'visible';
        hints.style.opacity = '1';
        hints.style.pointerEvents = 'auto';
        isHintsVisible = true;
        GeneralHandler.positionTooltip(hints, helpIcon, config.position, true); // true = outsideSidebar
      }
    };

    // Function to hide immediately (for when mouse leaves hints)
    const hideImmediately = () => {
      // Clear show timeout if pending
      if (showTimeout) {
        clearTimeout(showTimeout);
        showTimeout = null;
      }

      // Also clear mobile touch system timers if they exist
      if (helpIconListeners && helpIconListeners.timers) {
        helpIconListeners.timers.clearLongPress();
      }

      // Hide with the configured delay
      hideHints();
    };

    // Add event listeners for help icon with mobile compatibility
    let helpIconListeners = null;

    try {
      // Add mobile-compatible tooltip listeners to help icon
      helpIconListeners = GeneralHandler.addTooltipEventListeners(
        helpIcon,
        showHints,
        hideHints,
        {
          longPressDelay: 400,  // Slightly shorter for help icons
          hideDelay: 3000,      // Longer display time for help content
          preventScroll: true
        }
      );

      // Add a click event as a fallback test (still useful for debugging)
      helpIcon.addEventListener('click', function (e) {
        e.preventDefault();
        // Show hints on click as fallback
        showHints(e);
      });
    } catch (error) {
      console.error('Error attaching help icon event listeners:', error);
    }

    // Add event listeners for hints itself to prevent hiding when hovering over it (desktop only)
    try {
      if (GeneralHandler.supportsHover()) {
        hints.addEventListener('mouseenter', cancelHideAndShow);
        hints.addEventListener('mouseleave', hideImmediately);
      }
    } catch (error) {
      console.error('Error attaching hints event listeners:', error);
    }

    // Store reference to cleanup function
    helpIcon.cleanupTooltip = () => {
      if (showTimeout) {
        clearTimeout(showTimeout);
        showTimeout = null;
      }
      if (hideTimeout) {
        clearTimeout(hideTimeout);
        hideTimeout = null;
      }

      // Cleanup mobile-compatible event listeners
      if (helpIconListeners) {
        helpIconListeners.cleanup();
      }

      // Remove hints element
      if (hints && hints.parentNode) {
        hints.parentNode.removeChild(hints);
      }

      // Remove resize listener (this should be improved to only remove specific listener)
      // Note: This is a limitation - we can't remove specific debounced listeners easily
      // Consider using a more sophisticated event management system if needed
    };

    return helpIcon;
  },

  /**
   * Ensure label keeps help icon after i18n updates by moving i18n attrs
   * from the label element to an inner span that holds the text/HTML.
   * @param {HTMLElement} label
   */
  prepareLabelForHelpIcon: function (label) {
    if (!label) return;
    // If already prepared, do nothing
    if (label.querySelector('.label-i18n')) return;

    const hasI18n = label.hasAttribute('data-i18n');
    const hasI18nHtml = label.hasAttribute('data-i18n-html');
    const hasI18nPlaceholder = label.hasAttribute('data-i18n-placeholder');

    if (!(hasI18n || hasI18nHtml || hasI18nPlaceholder)) return;

    const span = document.createElement('span');
    span.className = 'label-i18n';

    // Move i18n attributes to the span
    if (hasI18n) {
      const key = label.getAttribute('data-i18n');
      span.setAttribute('data-i18n', key);
      // Set initial text using i18n if available
      try { span.textContent = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(key); } catch (_) { span.textContent = key || label.textContent; }
      label.removeAttribute('data-i18n');
    } else if (hasI18nHtml) {
      const key = label.getAttribute('data-i18n-html');
      span.setAttribute('data-i18n-html', key);
      try { span.innerHTML = _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(key); } catch (_) { span.innerHTML = key || label.innerHTML; }
      label.removeAttribute('data-i18n-html');
    } else if (hasI18nPlaceholder) {
      const key = label.getAttribute('data-i18n-placeholder');
      span.setAttribute('data-i18n-placeholder', key);
      try { span.setAttribute('placeholder', _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.t(key)); } catch (_) { span.setAttribute('placeholder', key || ''); }
      label.removeAttribute('data-i18n-placeholder');
    }

    // Preserve existing textual content if no translation applied above
    if (!span.textContent && !span.innerHTML) {
      span.textContent = label.textContent || '';
    }

    // Clear label content but keep any existing non-text nodes not related to help icon (rare)
    // Safer approach: remove only text nodes
    Array.from(label.childNodes).forEach(n => {
      if (n.nodeType === Node.TEXT_NODE) {
        label.removeChild(n);
      }
    });

    // Insert span as first child for flex layout with help icon
    label.insertBefore(span, label.firstChild);
  },

  /**
   * Positions a hints relative to a target element
   * @param {HTMLElement} hints - The hints element
   * @param {HTMLElement} target - The target element to position relative to
   * @param {string} position - Position preference ('left', 'right', 'top', 'bottom')
   * @param {boolean} outsideSidebar - Whether to position hints outside the sidebar
   */
  positionTooltip: function (hints, target, position = 'left', outsideSidebar = false) {
    const targetRect = target.getBoundingClientRect();
    const hintsRect = hints.getBoundingClientRect();
    const viewport = {
      width: window.innerWidth,
      height: window.innerHeight
    };

    // Clear existing arrow classes (both hint and tooltip patterns)
    hints.classList.remove('hints-arrow-left', 'hints-arrow-right', 'hints-arrow-top', 'hints-arrow-bottom');
    hints.classList.remove('tooltip-arrow-left', 'tooltip-arrow-right', 'tooltip-arrow-top', 'tooltip-arrow-bottom');

    let left, top;

    // Detect if we're in mobile layout (screen width < 768px or sidebar has mobile positioning)
    const isMobile = viewport.width < 768;
    const sidebar = document.getElementById('side-panel');

    // If outsideSidebar is true, position tooltip appropriately for desktop/mobile
    if (outsideSidebar && sidebar) {
      const sidebarRect = sidebar.getBoundingClientRect();

      if (isMobile) {
        // Mobile: sidebar is at bottom, hints should be above it and span most of screen width
        hints.style.maxWidth = `${Math.min(600, viewport.width - 34)}px`; // Larger max width for images

        // Position hints above the sidebar
        left = 2; // 2px from left edge
        const bottom = viewport.height - sidebarRect.top + 48; // Above sidebar with margin from bottom
        hints.style.bottom = bottom + 'px';
        hints.style.top = ''; // Clear any previous top value
        hints.style.height = viewport.height - bottom - 24 + 'px'

        hints.classList.add('tooltip-arrow-bottom'); // Arrow pointing down to sidebar
      } else {
        // Desktop: sidebar is on left, hints to the right
        const height = 450
        const margin = 24
        hints.style.height = height + 'px'
        left = sidebarRect.right + 70; // Reduced margin since hints is larger
        const top = targetRect.top; // Position so hints top aligns with target top
        if (top + height - margin > viewport.height) {
          hints.style.top = '';
          hints.style.bottom = margin + 'px'; // Clear any previous bottom value
        } else {
          hints.style.top = top + 'px';
          hints.style.bottom = ''; // Clear any previous bottom value
        }
        hints.classList.add('tooltip-arrow-left');

        // Reset width for desktop
        hints.style.width = '';
        hints.style.maxWidth = '600px'; // Increased for larger images
      }
    }

    hints.style.left = left + 'px';
  },

  /**
   * Creates a new object and activates the snapping system for it
   * @param {Object} options - Configuration for the object creation
   * @param {Function} createObjectFn - Function to create the object (may be async)
   * @param {Object} component - The sidebar component context
   * @param {string} objectKey - Property name to store the object reference (e.g., 'newTextObject')
   * @param {string} vertexId - ID of the vertex to activate for snapping (e.g., 'V1', 'E1')
   * @param {Function} mouseMoveHandler - Handler function for mouse movement
   * @param {Function} mouseClickHandler - Handler function for mouse clicks
   * @param {Function} cancelHandler - Handler function for cancellation
   * @return {Promise<Object>} - A promise that resolves to the created object
   */
  createObjectWithSnapping: function (options, createObjectFn, component, objectKey, vertexId, mouseMoveHandler, mouseClickHandler, cancelHandler) {
    // Clear any existing object being placed
    if (component[objectKey]) {
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.remove(component[objectKey]);
      component[objectKey] = null;

      if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex) {
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.cleanupDrag();
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex = null;
      }
    }

    // Remove standard event listeners and add component-specific ones
    document.removeEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_3__.ShowHideSideBarEvent);
    document.addEventListener('keydown', cancelHandler);

    // Get the center of the canvas viewport
    const vpt = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.CenterCoord();
    const centerX = vpt.x;
    const centerY = vpt.y;

    // Set position in options
    options.position = options.position || { x: centerX, y: centerY };

    try {
      // Create the object - use await to handle both async and sync functions
      const newObject = createObjectFn(options);

      // Store reference to the object
      component[objectKey] = newObject;

      // Add mouse event handlers
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.on('mouse:move', mouseMoveHandler);
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.on('mouse:up', mouseClickHandler);

      // Set up the object for snapping
      _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.setActiveObject(newObject);
      newObject.enterFocusMode();
      newObject.isTemporary = true;

      // Activate the vertex control for snapping
      if (newObject.controls && newObject.controls[vertexId]) {
        // Get the vertex control
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex = newObject.controls[vertexId];
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.isDown = true;
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.isDragging = true;

        // Store original position info
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.originalPosition = {
          left: newObject.left,
          top: newObject.top
        };

        // Get the vertex coordinates
        const vertex = newObject.getBasePolygonVertex(vertexId);
        if (vertex) {
          // Store vertex positions for snapping
          _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.vertexOriginalPosition = {
            x: vertex.x,
            y: vertex.y
          };

          // Calculate offset from object center to vertex
          _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.vertexOffset = {
            x: vertex.x - newObject.left,
            y: vertex.y - newObject.top
          };

        }
      }

  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
      return newObject;
    } catch (error) {
      console.error('Error creating object with snapping:', error);
      // Clean up event listeners in case of error
      document.removeEventListener('keydown', cancelHandler);
      document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_3__.ShowHideSideBarEvent);
      throw error; // Re-throw to allow caller to handle the error
    }
  },

  /**
   * Generic panel handler off function to clean up when leaving a panel
   * @param {Object} component - The sidebar component context
   * @param {string} objectKey - Key for the object being placed (e.g., 'newTextObject')
   * @param {string} mouseMoveHandlerName - Name of the mouse move handler
   * @param {string} mouseClickHandlerName - Name of the mouse click handler
   * @param {string} cancelHandlerName - Name of the cancel handler
   */
  genericHandlerOff: function (component, objectKey, mouseMoveHandlerName, mouseClickHandlerName, cancelHandlerName) {
    // If there's a new object being placed, finalize its placement
    if (component[objectKey]) {
      if (_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex) {
        _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.activeVertex.finishDrag();
      }
      component[objectKey] = null;
    }

    // Remove event listeners
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.off('mouse:move', component[mouseMoveHandlerName]);
    _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.off('mouse:up', component[mouseClickHandlerName]);
    document.removeEventListener('keydown', component[cancelHandlerName]);
    document.addEventListener('keydown', _canvas_keyboardEvents_js__WEBPACK_IMPORTED_MODULE_3__.ShowHideSideBarEvent);

  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.scheduleRender();
  },

  showToast: function (message, type = 'success', duration = 3000) {
    const toastId = 'toast-notification';
    // Remove existing toast if any
    let existingToast = document.getElementById(toastId);
    if (existingToast) {
      existingToast.remove();
    }

    const toast = document.createElement('div');
    toast.id = toastId;
    toast.className = `toast ${type}`;
    toast.textContent = message;

    document.body.appendChild(toast);

    // Center the toast
    toast.style.left = `calc(50% - ${toast.offsetWidth / 2}px)`;

    // Auto fade out
    setTimeout(() => {
      toast.classList.add('fade-out');
      // Remove the element after fade out animation
      setTimeout(() => {
        if (toast.parentNode) {
          toast.parentNode.removeChild(toast);
        }
      }, 500); // Corresponds to the animation duration
    }, duration);
  },

  /**
   * Utility debounce function to limit rapid successive updates
   * @param {Function} func - Function to debounce
   * @param {number} wait - Wait time in milliseconds
   * @return {Function} Debounced function
   */
  debounce: function (func, wait) {
    let timeout;
    return function (...args) {
      const context = this;
      clearTimeout(timeout);
      timeout = setTimeout(() => func.apply(context, args), wait);
    };
  },

  /**
   * Detects if the current device is mobile/touch device
   * @return {boolean} True if mobile device
   */
  isMobileDevice: function () {
    return (('ontouchstart' in window) ||
      (navigator.maxTouchPoints > 0) ||
      (navigator.msMaxTouchPoints > 0) ||
      /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent));
  },

  /**
   * Detects if the current device supports hover interactions
   * @return {boolean} True if device supports hover
   */
  supportsHover: function () {
    return window.matchMedia('(hover: hover)').matches;
  },

  /**
   * Adds mobile-compatible tooltip event listeners
   * @param {HTMLElement} element - Element to attach listeners to
   * @param {Function} showCallback - Function to call when showing tooltip
   * @param {Function} hideCallback - Function to call when hiding tooltip
   * @param {Object} options - Configuration options
   * @return {Object} Object containing cleanup function and timeout references
   */
  addTooltipEventListeners: function (element, showCallback, hideCallback, options = {}) {
    const config = {
      longPressDelay: 500,    // Long press duration for mobile
      hideDelay: 2000,        // Auto-hide delay for mobile
      preventScroll: true,    // Prevent scrolling during long press
      ...options
    };

    let longPressTimer = null;
    let hideTimer = null;
    let touchStartY = 0;
    let isLongPress = false;
    let isScrolling = false;
    let globalTouchHandler = null;

    const isMobile = GeneralHandler.isMobileDevice();
    const supportsHover = GeneralHandler.supportsHover();

    // Global touch handler to hide tooltip when touching outside
    const handleGlobalTouch = (e) => {
      // Check if the touch is outside the element and not on a tooltip
      if (!element.contains(e.target) && !e.target.closest('.hints')) {
        if (isLongPress) {
          hideCallback();
          isLongPress = false;
        }
        // Remove global handler after hiding
        if (globalTouchHandler) {
          document.removeEventListener('touchstart', globalTouchHandler, true);
          globalTouchHandler = null;
        }
      }
    };

    // Mobile touch event handlers
    const handleTouchStart = (e) => {
      touchStartY = e.touches[0].clientY;
      isScrolling = false;
      isLongPress = false;

      // Start long press timer
      longPressTimer = setTimeout(() => {
        if (!isScrolling) {
          isLongPress = true;
          showCallback(e);

          // Add global touch handler to hide tooltip when touching outside
          if (isMobile || !supportsHover) {
            globalTouchHandler = handleGlobalTouch;
            // Use setTimeout to add the listener after current touch event completes
            setTimeout(() => {
              document.addEventListener('touchstart', globalTouchHandler, true);
            }, 50);
          }
        }
      }, config.longPressDelay);

      // Clear any pending hide timer
      if (hideTimer) {
        clearTimeout(hideTimer);
        hideTimer = null;
      }
    };

    const handleTouchMove = (e) => {
      const touchMoveY = e.touches[0].clientY;
      const deltaY = Math.abs(touchMoveY - touchStartY);

      // Detect scrolling (more than 10px movement)
      if (deltaY > 10) {
        isScrolling = true;

        // Cancel long press if scrolling
        if (longPressTimer) {
          clearTimeout(longPressTimer);
          longPressTimer = null;
        }

        // Hide tooltip if it's visible and user is scrolling
        if (isLongPress) {
          hideCallback();
          isLongPress = false;
          // Remove global handler when hiding due to scroll
          if (globalTouchHandler) {
            document.removeEventListener('touchstart', globalTouchHandler, true);
            globalTouchHandler = null;
          }
        }
      }
    };

    const handleTouchEnd = (e) => {
      // Clear long press timer
      if (longPressTimer) {
        clearTimeout(longPressTimer);
        longPressTimer = null;
      }

      // If it was a long press (tooltip is showing), start hide timer
      if (isLongPress && !isScrolling) {
        hideTimer = setTimeout(() => {
          hideCallback();
          isLongPress = false;
          // Remove global handler when auto-hiding
          if (globalTouchHandler) {
            document.removeEventListener('touchstart', globalTouchHandler, true);
            globalTouchHandler = null;
          }
        }, config.hideDelay);
      }

      isScrolling = false;
    };

    // Desktop mouse event handlers
    const handleMouseEnter = (e) => {
      // Clear any pending hide timer
      if (hideTimer) {
        clearTimeout(hideTimer);
        hideTimer = null;
      }
      showCallback(e);
    };

    const handleMouseLeave = (e) => {
      hideCallback();
    };

    // Add appropriate event listeners based on device type
    if (isMobile || !supportsHover) {
      element.addEventListener('touchstart', handleTouchStart, { passive: false });
      element.addEventListener('touchmove', handleTouchMove, { passive: false });
      element.addEventListener('touchend', handleTouchEnd, { passive: true });
    } else {
      element.addEventListener('mouseenter', handleMouseEnter);
      element.addEventListener('mouseleave', handleMouseLeave);
    }

    // Return cleanup function and state references
    return {
      cleanup: function () {
        if (longPressTimer) {
          clearTimeout(longPressTimer);
          longPressTimer = null;
        }
        if (hideTimer) {
          clearTimeout(hideTimer);
          hideTimer = null;
        }

        // Remove global touch handler if it exists
        if (globalTouchHandler) {
          document.removeEventListener('touchstart', globalTouchHandler, true);
          globalTouchHandler = null;
        }

        if (isMobile || !supportsHover) {
          element.removeEventListener('touchstart', handleTouchStart);
          element.removeEventListener('touchmove', handleTouchMove);
          element.removeEventListener('touchend', handleTouchEnd);
        } else {
          element.removeEventListener('mouseenter', handleMouseEnter);
          element.removeEventListener('mouseleave', handleMouseLeave);
        }
      },
      isMobile: isMobile,
      supportsHover: supportsHover,
      timers: {
        longPress: () => longPressTimer,
        hide: () => hideTimer,
        clearHide: () => {
          if (hideTimer) {
            clearTimeout(hideTimer);
            hideTimer = null;
          }
        },
        clearLongPress: () => {
          if (longPressTimer) {
            clearTimeout(longPressTimer);
            longPressTimer = null;
          }
        }
      }
    };
  },

  /**
   * Gets center coordinates accounting for canvas pan/zoom
   * @return {Object} Center coordinates {x, y}
   */
  getCenterCoordinates: function () {
    // Center the object on the canvas viewport
    const centerX = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.width / 2 / _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.getZoom();
    const centerY = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.height / 2 / _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.getZoom();

    // Account for any panning that has been done
    const vpt = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.viewportTransform;
    const actualCenterX = (centerX - vpt[4]) / vpt[0];
    const actualCenterY = (centerY - vpt[5]) / vpt[3];

    return { x: actualCenterX, y: actualCenterY };
  },

  /**
   * Creates a hover tooltip for general buttons that loads hint content dynamically
   * @param {HTMLElement} button - The button element to attach tooltip to
   * @param {string} hintPath - Path to the hint file (e.g., 'symbols/Airport')
   * @param {Object} options - Optional configuration {position: 'left'|'right'|'top'|'bottom', showDelay: number, hideDelay: number}
   * @return {HTMLElement} The button element with tooltip attached
   */
  createGeneralButtonTooltip: function (button, hintPath, options = {}) {
    // Default options
    const defaultOptions = {
      position: 'top',     // Default position for general button tooltips
      showDelay: 300,      // Delay before showing
      hideDelay: 100       // Quick hide for button tooltips
    };
    const config = Object.assign(defaultOptions, options);

    // Create tooltip element with unified hints class
    const tooltip = GeneralHandler.createNode("div", {
      'class': 'hints',
      'data-button-id': button.id || `button-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`
    }, document.body);

    // Store timeout references
    let showTimeout = null;
    let hideTimeout = null;
    let isTooltipVisible = false;
    let hintContent = null; // Cache the loaded content

    // Function to reposition tooltip on window resize
    const repositionTooltip = () => {
      if (isTooltipVisible && tooltip.style.opacity === '1') {
        GeneralHandler.positionTooltip(tooltip, button, config.position, true);
      }
    };

    // Add window resize listener
    window.addEventListener('resize', GeneralHandler.debounce(repositionTooltip, 150));

    // Function to show tooltip with delay and dynamic loading
    const showTooltip = async (event) => {
      // Clear any pending hide timeout
      if (hideTimeout) {
        clearTimeout(hideTimeout);
        hideTimeout = null;
      }

      // Clear any existing show timeout
      if (showTimeout) {
        clearTimeout(showTimeout);
      }

      // Set new timeout for showing
      showTimeout = setTimeout(async () => {
        try {
          // Load hint content if not already cached
          if (!hintContent) {
            hintContent = await _utils_hintLoader_js__WEBPACK_IMPORTED_MODULE_4__.HintLoader.loadHint(hintPath);
          }

          // If hint content is available, show tooltip
          if (hintContent) {
            tooltip.innerHTML = hintContent;
            // Apply i18n translations to dynamically loaded tooltip content
            try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.applyTranslations(tooltip); } catch (_) {}

            // Add close button to tooltip
            GeneralHandler.addCloseButtonToHints(tooltip, hideTooltip);

            tooltip.style.visibility = 'visible';
            tooltip.style.opacity = '1';
            tooltip.style.pointerEvents = 'auto';
            isTooltipVisible = true;

            // Position the tooltip
            GeneralHandler.positionTooltip(tooltip, button, config.position, true);
          } else {
            // No hint available, show a fallback message
            tooltip.innerHTML = '<p data-i18n-html="No help available for this item."><em>No help available for this item.</em></p>';
            try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.applyTranslations(tooltip); } catch (_) {}

            // Add close button to tooltip
            GeneralHandler.addCloseButtonToHints(tooltip, hideTooltip);

            tooltip.style.visibility = 'visible';
            tooltip.style.opacity = '1';
            tooltip.style.pointerEvents = 'auto';
            isTooltipVisible = true;
            GeneralHandler.positionTooltip(tooltip, button, config.position, true);
          }
        } catch (error) {
          console.warn('Failed to load hint content:', error);
          // Show error message in tooltip
          tooltip.innerHTML = '<p data-i18n-html="Failed to load help content."><em>Failed to load help content.</em></p>';
          try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_1__.i18n.applyTranslations(tooltip); } catch (_) {}

          // Add close button to tooltip
          GeneralHandler.addCloseButtonToHints(tooltip, hideTooltip);

          tooltip.style.visibility = 'visible';
          tooltip.style.opacity = '1';
          tooltip.style.pointerEvents = 'auto';
          isTooltipVisible = true;
          GeneralHandler.positionTooltip(tooltip, button, config.position, true);
        }
        showTimeout = null;
      }, config.showDelay);
    };

    // Function to hide tooltip with delay
    const hideTooltip = () => {
      // Clear any pending show timeout
      if (showTimeout) {
        clearTimeout(showTimeout);
        showTimeout = null;
      }

      // Clear any existing hide timeout
      if (hideTimeout) {
        clearTimeout(hideTimeout);
      }

      // Set new timeout for hiding
      hideTimeout = setTimeout(() => {
        tooltip.style.opacity = '0';
        tooltip.style.pointerEvents = 'none';
        isTooltipVisible = false;
        // Hide visibility after transition completes
        setTimeout(() => {
          if (tooltip.style.opacity === '0') {
            tooltip.style.visibility = 'hidden';
          }
        }, 200); // Match CSS transition duration
        hideTimeout = null;
      }, config.hideDelay);
    };

    // Function to cancel hide and show immediately (for when mouse enters tooltip)
    const cancelHideAndShow = () => {
      // Clear both timeouts from the tooltip system
      if (hideTimeout) {
        clearTimeout(hideTimeout);
        hideTimeout = null;
      }
      if (showTimeout) {
        clearTimeout(showTimeout);
        showTimeout = null;
      }

      // Also clear the mobile touch system timers if they exist
      if (buttonListeners && buttonListeners.timers) {
        buttonListeners.timers.clearHide();
        buttonListeners.timers.clearLongPress();
      }

      // Show immediately if not already visible
      if (!isTooltipVisible) {
        showTooltip();
      }
    };

    // Function to hide with delay for mobile (gives user time to touch again)
    const hideWithDelay = () => {
      // Clear show timeout if pending
      if (showTimeout) {
        clearTimeout(showTimeout);
        showTimeout = null;
      }

      // Also clear mobile touch system timers if they exist
      if (buttonListeners && buttonListeners.timers) {
        buttonListeners.timers.clearLongPress();
      }

      // Start a delayed hide timer - gives user time to touch tooltip again
      // This creates a grace period on mobile before the tooltip hides
      if (hideTimeout) {
        clearTimeout(hideTimeout);
      }

      hideTimeout = setTimeout(() => {
        tooltip.style.opacity = '0';
        tooltip.style.pointerEvents = 'none';
        isTooltipVisible = false;
        // Hide visibility after transition completes
        setTimeout(() => {
          if (tooltip.style.opacity === '0') {
            tooltip.style.visibility = 'hidden';
          }
        }, 200); // Match CSS transition duration
        hideTimeout = null;
      }, 1500); // 1.5 second grace period for mobile users
    };

    // Function to hide immediately (for when mouse leaves tooltip)
    const hideImmediately = () => {
      // Clear show timeout if pending
      if (showTimeout) {
        clearTimeout(showTimeout);
        showTimeout = null;
      }

      // Also clear mobile touch system timers if they exist
      if (buttonListeners && buttonListeners.timers) {
        buttonListeners.timers.clearLongPress();
      }

      // Hide with the configured delay
      hideTooltip();
    };

    // Add event listeners to button with mobile compatibility
    let buttonListeners = null;
    let tooltipListeners = null;

    try {
      // Add mobile-compatible tooltip listeners to button
      buttonListeners = GeneralHandler.addTooltipEventListeners(
        button,
        showTooltip,
        hideTooltip,
        {
          longPressDelay: 500,  // Standard long press duration
          hideDelay: 2500,      // Reasonable display time for button tooltips
          preventScroll: true
        }
      );

      // Add focus/blur events for keyboard navigation (desktop only)
      if (GeneralHandler.supportsHover()) {
        button.addEventListener('focus', showTooltip); // Show on keyboard focus
        button.addEventListener('blur', hideTooltip);  // Hide on blur
      }
    } catch (error) {
      console.error('Error attaching general button tooltip listeners:', error);
    }

    // Add event listeners to tooltip itself to prevent hiding when hovering over it (desktop only)
    try {
      if (GeneralHandler.supportsHover()) {
        tooltip.addEventListener('mouseenter', cancelHideAndShow);
        tooltip.addEventListener('mouseleave', hideImmediately);
      } else if (GeneralHandler.isMobileDevice()) {
        tooltip.addEventListener('touchstart', cancelHideAndShow);
        tooltip.addEventListener('touchend', hideWithDelay); // Use delay for mobile
      }
    } catch (error) {
      console.error('Error attaching tooltip hover listeners:', error);
    }

    // Store cleanup function on button
    button.cleanupGeneralTooltip = () => {
      if (showTimeout) {
        clearTimeout(showTimeout);
        showTimeout = null;
      }
      if (hideTimeout) {
        clearTimeout(hideTimeout);
        hideTimeout = null;
      }

      // Cleanup mobile-compatible event listeners
      if (buttonListeners) {
        buttonListeners.cleanup();
      }

      // Remove focus/blur listeners (desktop only)
      if (GeneralHandler.supportsHover()) {
        button.removeEventListener('focus', showTooltip);
        button.removeEventListener('blur', hideTooltip);
        tooltip.removeEventListener('mouseenter', cancelHideAndShow);
        tooltip.removeEventListener('mouseleave', hideImmediately);
      } else if (GeneralHandler.isMobileDevice()) {
        tooltip.removeEventListener('touchstart', cancelHideAndShow);
        tooltip.removeEventListener('touchend', hideWithDelay);
      }

      // Remove resize listener
      window.removeEventListener('resize', repositionTooltip);
      if (document.body.contains(tooltip)) {
        document.body.removeChild(tooltip);
      }
    };

    return button;
  },

  /**
   * Positions a general button tooltip relative to a target button
   * @param {HTMLElement} tooltip - The tooltip element
   * @param {HTMLElement} target - The target button to position relative to
   * @param {string} position - Position preference ('left', 'right', 'top', 'bottom')
   */
  positionGeneralTooltip: function (tooltip, target, position = 'top') {
    const targetRect = target.getBoundingClientRect();
    const tooltipRect = tooltip.getBoundingClientRect();
    const viewport = {
      width: window.innerWidth,
      height: window.innerHeight
    };

    // Clear existing arrow classes
    tooltip.classList.remove('tooltip-arrow-left', 'tooltip-arrow-right', 'tooltip-arrow-top', 'tooltip-arrow-bottom');

    let left, top;
    const margin = 8; // Space between tooltip and button

    switch (position) {
      case 'top':
        left = targetRect.left + (targetRect.width - tooltipRect.width) / 2;
        top = targetRect.top - tooltipRect.height - margin;
        tooltip.classList.add('tooltip-arrow-bottom');
        break;
      case 'bottom':
        left = targetRect.left + (targetRect.width - tooltipRect.width) / 2;
        top = targetRect.bottom + margin;
        tooltip.classList.add('tooltip-arrow-top');
        break;
      case 'left':
        left = targetRect.left - tooltipRect.width - margin;
        top = targetRect.top + (targetRect.height - tooltipRect.height) / 2;
        tooltip.classList.add('tooltip-arrow-right');
        break;
      case 'right':
        left = targetRect.right + margin;
        top = targetRect.top + (targetRect.height - tooltipRect.height) / 2;
        tooltip.classList.add('tooltip-arrow-left');
        break;
    }

    // Ensure tooltip stays within viewport bounds
    if (left < 10) {
      left = 10;
    } else if (left + tooltipRect.width > viewport.width - 10) {
      left = viewport.width - tooltipRect.width - 10;
    }

    if (top < 10) {
      top = 10;
    } else if (top + tooltipRect.height > viewport.height - 10) {
      top = viewport.height - tooltipRect.height - 10;
    }

    tooltip.style.left = left + 'px';
    tooltip.style.top = top + 'px';
  },

  /**
   * Enhanced button creation that can automatically attach tooltip with dynamic hint loading
   * @param {string} name - Button name/ID
   * @param {string} labelTxt - Button label text
   * @param {HTMLElement} parent - Parent container
   * @param {string} container - Container class name
   * @param {Function} callback - Click callback
   * @param {string} event - Event type
   * @param {Object} tooltipOptions - Tooltip configuration {hintPath: string, position: string, showDelay: number, hideDelay: number}
   * @return {HTMLElement} The created button
   */
  createButtonWithTooltip: function (name, labelTxt, parent, container = 'input', callback = null, event = null, tooltipOptions = null) {
    // Create the button using existing method
    const button = GeneralHandler.createButton(name, labelTxt, parent, container, callback, event);

    // If tooltip options provided, attempt to add tooltip
    if (tooltipOptions && tooltipOptions.hintPath) {
      GeneralHandler.createGeneralButtonTooltip(button, tooltipOptions.hintPath, tooltipOptions);
    }

    return button;
  },

  /**
   * Cleanup utility to remove orphaned hints tooltips from the DOM
   * Call this to clean up any tooltips that may have been left behind
   */
  cleanupOrphanedTooltips: function () {
    const orphanedTooltips = document.querySelectorAll('.hints[data-button-id]');
    orphanedTooltips.forEach(tooltip => {
      // Check if the tooltip's parent button still exists in the DOM
      const buttonId = tooltip.getAttribute('data-button-id');
      const button = buttonId ? document.getElementById(buttonId) : null;

      if (!button || !document.body.contains(button)) {
        // Button no longer exists, remove the orphaned tooltip
        if (tooltip.parentNode) {
          tooltip.parentNode.removeChild(tooltip);
        }
      }
    });
  },

  /**
   * Force hide all visible hint tooltips (useful for mobile when tooltips get stuck)
   */
  hideAllTooltips: function () {
    const visibleTooltips = document.querySelectorAll('.hints[style*="opacity: 1"], .hints[style*="visibility: visible"]');
    visibleTooltips.forEach(tooltip => {
      tooltip.style.opacity = '0';
      tooltip.style.pointerEvents = 'none';
      setTimeout(() => {
        if (tooltip.style.opacity === '0') {
          tooltip.style.visibility = 'hidden';
        }
      }, 200);
    });
  },

  /**
   * Adds a close button to a hints container
   * @param {HTMLElement} hintsElement - The hints container element
   * @param {Function} hideCallback - Function to call when close button is clicked
   * @return {HTMLElement} The created close button element
   */
  addCloseButtonToHints: function (hintsElement, hideCallback) {
    // Add class to hints element for proper padding
    hintsElement.classList.add('with-close-button');

    // Create close button
    const closeButton = GeneralHandler.createNode("button", {
      'class': 'hints-close-button',
      'type': 'button',
      'aria-label': 'Close hint',
      'title': 'Close'
    }, hintsElement);

    closeButton.innerHTML = '&times;'; // × symbol

    // Add click handler to close button
    closeButton.addEventListener('click', (e) => {
      e.preventDefault();
      e.stopPropagation();
      hideCallback();
    });

    // Add touch event handler for mobile
    closeButton.addEventListener('touchend', (e) => {
      e.preventDefault();
      e.stopPropagation();
      hideCallback();
    });

    return closeButton;
  },

  // ...existing code...
}

document.getElementById('show_hide').addEventListener('click', function (event) {
  // Hide any visible tooltips when toggling sidebar
  GeneralHandler.hideAllTooltips();
  GeneralHandler.ShowHideSideBar(event)
}
)

// GeneralSettings object to store shared settings across sidebar panels
const GeneralSettings = {
  // Default values
  showTextBorders: true,
  showObjectBorders: true,
  showGrid: true,
  snapToGrid: true,
  backgroundColor: '#2f2f2f',
  gridColor: '#ffffff',
  gridSize: 20,
  showAllVertices: false,
  autoSave: true,
  autoSaveInterval: 300,
  defaultExportScale: 2,
  runTestsOnStart: false,
  xHeight: 100,
  messageColor: 'White',
  dimensionUnit: 'mm', // 'mm' or 'sw' (sign width units)
  locale: 'en', // UI language code

  // Event listeners for setting changes
  listeners: [],

  // Method to register a listener for setting changes
  addListener: function (callback) {
    this.listeners.push(callback);
  },

  // Method to notify all listeners of setting changes
  notifyListeners: function (setting, value) {
    this.listeners.forEach(callback => {
      if (typeof callback === 'function') {
        callback(setting, value);
      }
    });
  },

  // Update a setting and notify listeners
  updateSetting: function (setting, value) {
    if (this.hasOwnProperty(setting)) {
      this[setting] = value;
      this.notifyListeners(setting, value);
    }
  },

  resetSetting: function () {
    const defaultSetting = {
      showTextBorders: true,
      showObjectBorders: true,
      showGrid: true,
      snapToGrid: true,
      backgroundColor: '#2f2f2f',
      gridColor: '#ffffff',
      gridSize: 20,
      showAllVertices: false,
      autoSave: true,
      autoSaveInterval: 300,
      defaultExportScale: 2,
      runTestsOnStart: false,
      xHeight: 100,
      messageColor: 'White',
      dimensionUnit: 'mm',
      locale: 'en'
    };

    // Apply all settings at once without triggering individual notifications
    // We'll do a single notification at the end
    for (const key in defaultSetting) {
      if (defaultSetting.hasOwnProperty(key) && this.hasOwnProperty(key)) {
        this[key] = defaultSetting[key];
      }
    }

    // Notify listeners about a complete reset instead of individual properties
    this.notifyListeners('settingsReset', null);
  },

  // Method to format dimension values based on the current unit setting
  formatDimension: function (value, xHeight = 100) {
    if (this.dimensionUnit === 'sw') {
      // Convert to sign width units (value / xHeight)
      // Sign width units are calculated as dimension divided by x-height
      const swValue = (value / xHeight * 4).toFixed(1);
      return `${swValue}sw`;
    } else {
      // Return in millimeters (default)
      return `${Math.round(value)}mm`;
    }
  }
};

// Global mobile tooltip management
// Add a global touch handler to help hide tooltips on mobile devices
if (typeof window !== 'undefined' && GeneralHandler.isMobileDevice()) {
  let globalTouchHideTimeout = null;

  const globalTooltipHideHandler = (e) => {
    // Clear any existing timeout
    if (globalTouchHideTimeout) {
      clearTimeout(globalTouchHideTimeout);
    }

    // Set a timeout to hide tooltips if the touch is not on a tooltip or button with tooltip
    globalTouchHideTimeout = setTimeout(() => {
      const target = e.target;
      const isTooltipElement = target.closest('.hints') || target.closest('[data-tooltip]') || target.classList.contains('help-icon');

      if (!isTooltipElement) {
        GeneralHandler.hideAllTooltips();
      }
    }, 100); // Small delay to allow other touch handlers to process first
  };

  // Add the global handler with passive: true for better performance
  document.addEventListener('touchstart', globalTooltipHideHandler, { passive: true });
}

// Export GeneralHandler and GeneralSettings for ES module usage


/***/ }),

/***/ "./js/sidebar/sidebar.js":
/*!*******************************!*\
  !*** ./js/sidebar/sidebar.js ***!
  \*******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   activatePanelFromHash: () => (/* binding */ activatePanelFromHash)
/* harmony export */ });
/* harmony import */ var _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./sbGeneral.js */ "./js/sidebar/sbGeneral.js");
/* harmony import */ var _sb_text_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./sb-text.js */ "./js/sidebar/sb-text.js");
/* harmony import */ var _sb_draw_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./sb-draw.js */ "./js/sidebar/sb-draw.js");
/* harmony import */ var _sb_border_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./sb-border.js */ "./js/sidebar/sb-border.js");
/* harmony import */ var _sb_map_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./sb-map.js */ "./js/sidebar/sb-map.js");
/* harmony import */ var _sb_export_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./sb-export.js */ "./js/sidebar/sb-export.js");
/* harmony import */ var _sb_info_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./sb-info.js */ "./js/sidebar/sb-info.js");
/* harmony import */ var _sb_settings_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./sb-settings.js */ "./js/sidebar/sb-settings.js");
/* harmony import */ var _sb_template_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./sb-template.js */ "./js/sidebar/sb-template.js");
/* harmony import */ var _sb_measure_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./sb-measure.js */ "./js/sidebar/sb-measure.js");
/* harmony import */ var _sb_tracker_js__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./sb-tracker.js */ "./js/sidebar/sb-tracker.js");
/* harmony import */ var _sb_inspector_js__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./sb-inspector.js */ "./js/sidebar/sb-inspector.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ../i18n/i18n.js */ "./js/i18n/i18n.js");
/**
 * Main Sidebar Controller
 * 
 * This file serves as the entry point for the sidebar functionality.
 * It loads component modules on demand when they are first needed.
 */












// Assuming CanvasObjectInspector might be needed, import if necessary





window.GeneralSettings = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralSettings; // Make GeneralSettings globally accessible
window.GeneralHandler = _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler; // Make GeneralHandler globally accessible

// Define a mapping from hash to button ID and its init function
const routeMappings = {
  'text': { buttonId: 'btn_text', initFn: _sb_text_js__WEBPACK_IMPORTED_MODULE_1__.FormTextAddComponent.textPanelInit },
  'draw': { buttonId: 'btn_draw', initFn: _sb_draw_js__WEBPACK_IMPORTED_MODULE_2__.FormDrawAddComponent.drawPanelInit },
  'border': { buttonId: 'btn_border', initFn: _sb_border_js__WEBPACK_IMPORTED_MODULE_3__.FormBorderWrapComponent.BorderPanelInit },
  'map': { buttonId: 'btn_map', initFn: _sb_map_js__WEBPACK_IMPORTED_MODULE_4__.FormDrawMapComponent.drawMapPanelInit },
  'export': { buttonId: 'btn_export', initFn: _sb_export_js__WEBPACK_IMPORTED_MODULE_5__.FormExportComponent.exportPanelInit },
  'info': { buttonId: 'btn_info', initFn: _sb_info_js__WEBPACK_IMPORTED_MODULE_6__.FormInfoComponent.InfoPanelInit },
  'settings': { buttonId: 'btn_settings', initFn: _sb_settings_js__WEBPACK_IMPORTED_MODULE_7__.FormSettingsComponent.settingsPanelInit },
  'templates': { buttonId: 'btn_template', initFn: _sb_template_js__WEBPACK_IMPORTED_MODULE_8__.FormTemplateComponent.templatePanelInit },
  'measure': { buttonId: 'btn_measure', initFn: _sb_measure_js__WEBPACK_IMPORTED_MODULE_9__.FormMeasureComponent.measurePanelInit },
  'tracker': { buttonId: 'btn_tracker', initFn: () => {
    let instance = window.canvasTrackerComponentInstance;
    if (instance && instance.initialized) {
      instance.restoreUI();
    } else if (instance) {
      instance.initialize();
    }
    // Note: This assumes canvasTrackerComponentInstance is globally available or passed appropriately.
    // If not, this specific route might need a different handling strategy for initialization.
  } }
};

function activatePanelFromHash() {
  const hash = window.location.hash.substring(1); // Remove #
  const route = routeMappings[hash];
  if (route) {
    const button = document.getElementById(route.buttonId);
    if (button) {
      // Ensure the panel initialization logic is called.
      // Some panels might already be initialized by default (e.g., settings).
      // Others are initialized on click. We need to ensure the correct state.
      if (typeof route.initFn === 'function') {
        route.initFn(); // Call the panel's initialization function
      }
      // Simulate a click if the initFn doesn't cover all click actions (like UI highlighting)
      // or if direct initFn call isn't sufficient.
      // However, calling initFn is generally preferred to avoid unintended side effects of a raw click.
      // If initFn handles UI updates (like active class), a direct click might not be needed.
      // For simplicity and to ensure UI consistency, we can also trigger a click.
      // Be cautious if initFn and click handler do redundant work or conflict.
      button.click();
    }
  } else if (hash === '') {
    // Default to draw panel or settings if no hash or unknown hash
    const defaultButton = document.getElementById(routeMappings['draw'].buttonId);
    if (defaultButton) {
      defaultButton.click();
    }
  }
}

/*
function updateHash(newHash) {
  if (window.location.hash !== `#${newHash}`) {
    window.location.hash = newHash;
  }
}
*/

document.addEventListener('DOMContentLoaded', function () {
  // Initialize GeneralHandler first if needed (assuming it has an init function)
  _sbGeneral_js__WEBPACK_IMPORTED_MODULE_0__.GeneralHandler.ShowHideSideBar(null, 'on');

  // Store the tracker instance globally or in a way accessible to routeMappings
  window.canvasTrackerComponentInstance = new _sb_tracker_js__WEBPACK_IMPORTED_MODULE_10__.CanvasTrackerUI();
  // Initializing tracker here might be too early if it depends on other DOM elements not yet ready.
  // Consider initializing it within its route activation or ensuring all dependencies are met.

  // Setup button clicks to update hash and call their original functions
  Object.keys(routeMappings).forEach(hashKey => {
    const route = routeMappings[hashKey];
    const button = document.getElementById(route.buttonId);
    if (button) {
      const originalOnClick = button.onclick; // Store original if any (though addEventListener is better)
      const originalAddEventListenerFn = route.initFn; // Assuming initFn is the primary action

      button.onclick = null; // Clear existing onclick to avoid double calls if we use addEventListener
      button.addEventListener('click', function(event) {
        // Call the original initialization/handler function
        if (hashKey === 'tracker') { // Special handling for tracker
            let instance = window.canvasTrackerComponentInstance;
            if (!instance.initialized) { // Initialize tracker only if not already
                instance.initialize();
            } else {
                instance.restoreUI();
            }
        } else if (typeof originalAddEventListenerFn === 'function') {
          originalAddEventListenerFn.call(this, event); // Call the original function
        } else if (typeof originalOnClick === 'function' && hashKey !== 'tracker') {
            // Fallback for any `button.onclick = ` style handlers if not covered by initFn
            originalOnClick.call(this, event);
        }
        //updateHash(hashKey); // Update the hash
        // Apply translations for any newly created panel content
        try { _i18n_i18n_js__WEBPACK_IMPORTED_MODULE_13__.i18n.applyTranslations(document.getElementById('input-form')); } catch (_) {}
      });
    }
  });


  // Settings module button - Initialize directly as it might be a default view
  // And ensure its click updates the hash
  const settingsButton = document.getElementById(routeMappings['settings'].buttonId);
  if (settingsButton) {
    // FormSettingsComponent.settingsPanelInit(); // Initialized by default or on first load
     settingsButton.addEventListener('click', () => { // Ensure hash updates
        _sb_settings_js__WEBPACK_IMPORTED_MODULE_7__.FormSettingsComponent.settingsPanelInit(); // Redundant if already handled by generic setup, but safe
        //updateHash('settings');
    });
  }
  // Tracker needs special handling due to its instance-based setup
  const trackerButton = document.getElementById(routeMappings['tracker'].buttonId);
  if (trackerButton) {
    // The generic setup above should handle the tracker click to call its init/restore.
    // We just ensure the instance is created early enough.
    // window.canvasTrackerComponentInstance.initialize(); // Initialize tracker; might be better done lazily
  }


  // Listen for hash changes to activate panels
  window.addEventListener('hashchange', activatePanelFromHash);

  // Activate panel based on initial hash on page load
  // Ensure this runs after all buttons have their new event listeners attached.
  // activatePanelFromHash();


  // Assuming 'canvas' is a global or accessible variable
  if (typeof canvas !== 'undefined' && canvas.renderAll) {
  _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_12__.CanvasGlobals.scheduleRender();
  }
});



/***/ }),

/***/ "./js/sidebar/tooltip.js":
/*!*******************************!*\
  !*** ./js/sidebar/tooltip.js ***!
  \*******************************/
/***/ (() => {

document.addEventListener('DOMContentLoaded', function() {
    // Create tooltip element
    const tooltip = document.createElement('div');
    tooltip.className = 'tooltip';
    document.body.appendChild(tooltip);

    // Add event listeners to all elements with data-tooltip attribute
    const tooltipElements = document.querySelectorAll('[data-tooltip]');
    tooltipElements.forEach(element => {
        element.addEventListener('mouseenter', showTooltip);
        element.addEventListener('mouseleave', hideTooltip);
        element.addEventListener('mousemove', moveTooltip);
    });

    function showTooltip(e) {
        if (this.id === 'show_hide') { // Check if the element is the #show_hide button
            return; // Do not show tooltip for this button
        }
        const tooltipText = this.getAttribute('data-tooltip');
        tooltip.textContent = tooltipText;
        tooltip.style.opacity = '1';
        moveTooltip.call(this, e);
    }

    function hideTooltip() {
        tooltip.style.opacity = '0';
    }

    function moveTooltip(e) {
        // Reset classes first
        tooltip.className = 'tooltip'; // Keep the base class

        const rect = this.getBoundingClientRect();
        const tooltipHeight = tooltip.offsetHeight;
        const tooltipWidth = tooltip.offsetWidth;
        const mobileBreakpoint = 768; // Define a breakpoint for mobile
        const viewportWidth = window.innerWidth;
        const viewportPadding = 5; // Small padding from viewport edges

        if (window.innerWidth <= mobileBreakpoint) {
            // Mobile: Position tooltip above the element, arrow pointing down
            let left = rect.left + (rect.width / 2) - (tooltipWidth / 2);
            let top = rect.top - tooltipHeight - 10; // 10px spacing above

            // Adjust for viewport overflow
            if (left < viewportPadding) {
                left = viewportPadding;
            }
            if (left + tooltipWidth > viewportWidth - viewportPadding) {
                left = viewportWidth - tooltipWidth - viewportPadding;
            }

            tooltip.style.left = `${left}px`;
            tooltip.style.top = `${top}px`;
            tooltip.classList.add('tooltip-arrow-bottom');
        } else {
            // Desktop: Attempt to position to the right first
            let left = rect.right + 10;
            let top = rect.top + (rect.height / 2) - (tooltipHeight / 2);
            let arrowClass = 'tooltip-arrow-left'; // Arrow points left when tooltip is on the right

            // Check if positioning to the right causes overflow
            if (left + tooltipWidth > viewportWidth - viewportPadding) {
                // If overflows right, try positioning to the left
                left = rect.left - tooltipWidth - 10;
                arrowClass = 'tooltip-arrow-right'; // Arrow points right when tooltip is on the left
            }

            // Adjust for left viewport overflow if positioned to the left
            if (left < viewportPadding) {
                left = viewportPadding;
                // If we had to adjust and it was meant to be on the left, 
                // it might be better to switch back to right if possible, 
                // but that adds complexity. For now, just ensure it's not off-screen.
                // If it was originally trying to be on the right and got pushed here, 
                // it might mean the element itself is too close to the left for left-side tooltip.
            }
            
            // Ensure top position is within viewport (simple check)
            if (top < viewportPadding) {
                top = viewportPadding;
            }
            if (top + tooltipHeight > window.innerHeight - viewportPadding) {
                top = window.innerHeight - tooltipHeight - viewportPadding;
            }

            tooltip.style.left = `${left}px`;
            tooltip.style.top = `${top}px`;
            tooltip.classList.add(arrowClass);
        }
    }
});


/***/ }),

/***/ "./js/tests/test.js":
/*!**************************!*\
  !*** ./js/tests/test.js ***!
  \**************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   runTests: () => (/* binding */ runTests),
/* harmony export */   testToRun: () => (/* binding */ testToRun)
/* harmony export */ });
/* harmony import */ var _objects_text_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../objects/text.js */ "./js/objects/text.js");
/* harmony import */ var _objects_symbols_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../objects/symbols.js */ "./js/objects/symbols.js");
/* harmony import */ var _objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../objects/anchor.js */ "./js/objects/anchor.js");
/* harmony import */ var _objects_lock_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../objects/lock.js */ "./js/objects/lock.js");
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");
/* harmony import */ var _objects_divider_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../objects/divider.js */ "./js/objects/divider.js");
/* harmony import */ var _objects_border_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../objects/border.js */ "./js/objects/border.js");
/* harmony import */ var _objects_mainRoute_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../objects/mainRoute.js */ "./js/objects/mainRoute.js");
/* harmony import */ var _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../objects/sideRoute.js */ "./js/objects/sideRoute.js");
/* harmony import */ var _sidebar_sb_template_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ../sidebar/sb-template.js */ "./js/sidebar/sb-template.js");











const canvasObject = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_4__.CanvasGlobals.canvasObject; // Assuming canvasObject is defined in canvas.js
const canvas = _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_4__.CanvasGlobals.canvas; // Assuming canvas is defined in canvas.js


// MIT http://rem.mit-license.org
// https://stackoverflow.com/questions/33777577/javascript-get-actual-rendered-font-height
function textXHeight() {
  // Create a blank canvas (by not filling a background color).
  var canvasElement = document.getElementById('canvas2');
  var canvas = new fabric.Canvas(canvasElement);
  var ctx = canvas.getContext('2d');

  // Fill it with some coloured text.. (black is default)
  ctx.font = "188px TransportMedium";
  ctx.textBaseline = "hanging";
  ctx.fillText("x", 0, 0);

  // Remove the surrounding transparent pixels
  // result is an actual canvas element
  var result = trim(canvas);

  // you could query it's width, draw it, etc..
  //document.body.appendChild(result);

  // get the height of the trimmed area
  console.log(result.height);

  // Trim Canvas Pixels Method
  // https://gist.github.com/remy/784508
  function trim(c) {

    var ctx = c.getContext('2d'),

      // create a temporary canvas in which we will draw back the trimmed text
      copy = document.createElement('canvas').getContext('2d'),

      // Use the Canvas Image Data API, in order to get all the
      // underlying pixels data of that canvas. This will basically
      // return an array (Uint8ClampedArray) containing the data in the
      // RGBA order. Every 4 items represent one pixel.
      pixels = ctx.getImageData(0, 0, c.width, c.height),

      // total pixels
      l = pixels.data.length,

      // main loop counter and pixels coordinates
      i, x, y,

      // an object that will store the area that isn't transparent
      bound = { top: null, left: null, right: null, bottom: null };

    // for every pixel in there
    for (i = 0; i < l; i += 4) {

      // if the alpha value isn't ZERO (transparent pixel)
      if (pixels.data[i + 3] !== 0) {

        // find it's coordinates
        x = (i / 4) % c.width;
        y = ~~((i / 4) / c.width);

        // store/update those coordinates
        // inside our bounding box Object

        if (bound.top === null) {
          bound.top = y;
        }

        if (bound.left === null) {
          bound.left = x;
        } else if (x < bound.left) {
          bound.left = x;
        }

        if (bound.right === null) {
          bound.right = x;
        } else if (bound.right < x) {
          bound.right = x;
        }

        if (bound.bottom === null) {
          bound.bottom = y;
        } else if (bound.bottom < y) {
          bound.bottom = y;
        }
      }
    }

    // actual height and width of the text
    // (the zone that is actually filled with pixels)
    var trimHeight = bound.bottom - bound.top,
      trimWidth = bound.right - bound.left,

      // get the zone (trimWidth x trimHeight) as an ImageData
      // (Uint8ClampedArray of pixels) from our canvas
      trimmed = ctx.getImageData(bound.left, bound.top, trimWidth, trimHeight);

    // Draw back the ImageData into the canvas
    copy.canvas.width = trimWidth;
    copy.canvas.height = trimHeight;
    copy.putImageData(trimmed, 0, 0);

    // return the canvas element
    return copy.canvas;
  }
}

/**
 * TestTracker: Utility to track objects created during tests
 */
const TestTracker = {
  testSections: {},
  currentTest: "",
  currentLine: 0,
  failedTests: [],
  passedTests: [],

  /**
   * Mark the start of a test section
   * @param {string} name - The name of the test section
   */
  startTest(name) {
    this.currentTest = name;
    this.testSections[name] = {
      startIndex: canvasObject.length,
      objects: {},
      endIndex: null
    };
    console.log(`▶ Starting test: ${name}`);
  },

  /**
   * Get the current line number using stack trace
   * @return {number} The line number
   */
  getLineNumber() {
    const stack = new Error().stack;
    const caller = stack.split("\n")[3]; // Get the caller (3 levels up)
    const match = caller.match(/:(\d+):\d+\)?$/);
    return match ? parseInt(match[1], 10) : 0;
  },

  /**
   * Track the current line being tested
   * @return {number} The current line number
   */
  trackLine() {
    this.currentLine = this.getLineNumber();
    return this.currentLine;
  },

  /**
   * Record a test failure with line information and make it clickable in console
   * @param {string} message - The failure message
   * @param {string} expected - Expected value
   * @param {string} actual - Actual value
   * @param {number} line - Line number where the failure occurred
   */
  recordFailure(message, expected, actual, line = this.currentLine) {
    // Store the failure information
    this.failedTests.push({
      test: this.currentTest,
      message,
      expected,
      actual,
      line
    });

    // Create an error object to get a proper stack trace
    const error = new Error('');

    // Instead of trying to style the Error object directly, use console.error with styling
    console.error(
      "%c ✗ FAILED: %c%s",
      'background: #f44336; color: white; font-weight: bold; padding: 2px 5px;',
      'color: #f44336; font-weight: bold;',
      ` ${this.currentTest} - ${message}\nExpected: ${expected}, Actual: ${actual}`
    );

    // Optional: directly log the error object which creates a clickable stack trace
    console.error(error);
  },

  /**
   * Register a passed test
   * @param {string} testName - Name of the test that passed
   */
  recordSuccess(testName = this.currentTest) {
    if (!this.passedTests.includes(testName)) {
      this.passedTests.push(testName);
    }
  },

  /**
   * Assert that values are equal or almost equal within tolerance
   * @param {any} actual - The actual value
   * @param {any} expected - The expected value
   * @param {string} message - Message to display on failure
   * @param {number} tolerance - Tolerance for numeric comparison
   * @return {boolean} Whether the assertion passed
   */
  assert(actual, expected, message = "", tolerance = 0) {
    this.trackLine();

    // For numeric values, use tolerance
    if (typeof actual === 'number' && typeof expected === 'number') {
      if (Math.abs(actual - expected) > tolerance) {
        this.recordFailure(message || "Assertion failed", expected, actual);
        return false;
      }
    }
    // For non-numeric values, use strict equality
    else if (actual !== expected) {
      this.recordFailure(message || "Assertion failed", expected, actual);
      return false;
    }

    return true;
  },

  /**
   * Assert that condition is true
   * @param {boolean} condition - The condition to check
   * @param {string} message - Message to display on failure
   * @return {boolean} Whether the assertion passed
   */
  assertTrue(condition, message = "") {
    this.trackLine();
    if (!condition) {
      this.recordFailure(message || "Condition is false", true, false);
      return false;
    }
    return true;
  },

  /**
   * Assert that spacing between objects is correct
   * @param {Object} object1 - First object
   * @param {Object} object2 - Second object
   * @param {string} axis - 'x' or 'y' axis
   * @param {number} spacing - Expected spacing
   * @param {string} message - Message to display on failure
   * @param {number} tolerance - Tolerance for comparison
   * @return {boolean} Whether the assertion passed
   */
  assertSpacing(object1, object2, axis, spacing, message = "", tolerance = 1) {
    this.trackLine();

    let actual = 0;
    if (axis === 'x') {
      // For X-axis, measure from right edge of first object to left edge of second
      const rightOfObj1 = object1.left + object1.width;
      actual = object2.left - rightOfObj1;
    } else {
      // For Y-axis, measure from bottom of first object to top of second
      const bottomOfObj1 = object1.top + object1.height;
      actual = object2.top - bottomOfObj1;
    }

    if (Math.abs(actual - spacing) > tolerance) {
      this.recordFailure(message || `${axis}-spacing incorrect`, spacing, actual);
      return false;
    }
    return true;
  },

  /**
   * Assert that two vertices are touching
   * @param {Object} vertex1 - First vertex {x, y}
   * @param {Object} vertex2 - Second vertex {x, y}
   * @param {string} message - Message to display on failure
   * @param {number} tolerance - Max allowed distance
   * @return {boolean} Whether the assertion passed
   */
  assertVertexTouching(vertex1, vertex2, message = "", tolerance = 1) {
    this.trackLine();

    const distance = Math.sqrt(
      Math.pow(vertex1.x - vertex2.x, 2) +
      Math.pow(vertex1.y - vertex2.y, 2)
    );

    if (distance > tolerance) {
      this.recordFailure(message || "Vertices not touching",
        `distance < ${tolerance}`,
        `distance = ${distance.toFixed(2)}`);
      return false;
    }
    return true;
  },

  /**
   * Register an object with a friendly name (silent)
   * @param {string} name - Friendly name to reference the object 
   * @param {number|Object} objectOrIndex - Object or index to register
   * @return {Object} The registered object
   */
  register(name, objectOrIndex = null) {
    if (!this.currentTest) return null;

    const section = this.testSections[this.currentTest];
    let object, index;

    if (objectOrIndex === null) {
      index = canvasObject.length - 1;
      object = canvasObject[index];
    } else if (typeof objectOrIndex === 'number') {
      index = objectOrIndex;
      object = canvasObject[index];
    } else {
      object = objectOrIndex;
      index = canvasObject.indexOf(object);
    }

    if (!object) return null;

    section.objects[name] = index;
    return object;
  },

  /**
   * Get an object by its friendly name from the current or specified test
   * @param {string} name - The friendly name of the object
   * @param {string} testName - Optional test name (defaults to current test)
   * @return {Object} The canvas object
   */
  get(name, testName = null) {
    const section = this.testSections[testName || this.currentTest];
    if (!section) {
      console.error(`Test section "${testName || this.currentTest}" not found`);
      return null;
    }

    const index = section.objects[name];
    if (index === undefined) {
      console.error(`Object "${name}" not found in test "${testName || this.currentTest}"`);
      return null;
    }

    return canvasObject[index];
  },

  /**
   * Get an object by its relative index from test start
   * @param {number} relativeIndex - Index relative to test start (0 for first)
   * @param {string} testName - Optional test name (defaults to current test)
   * @return {Object} The canvas object
   */
  getByIndex(relativeIndex, testName = null) {
    const section = this.testSections[testName || this.currentTest];
    if (!section) {
      console.error(`Test section "${testName || this.currentTest}" not found`);
      return null;
    }

    const index = section.startIndex + relativeIndex;
    if (index >= canvasObject.length) {
      console.error(`Invalid relative index ${relativeIndex} in "${testName || this.currentTest}"`);
      return null;
    }

    return canvasObject[index];
  },

  /**
   * End the current test and log status
   * @param {boolean} passed - Whether the test passed
   */
  endTest(passed = true) {
    if (!this.currentTest) return;

    const section = this.testSections[this.currentTest];
    section.endIndex = canvasObject.length - 1;

    if (passed) {
      this.recordSuccess();
      console.log(`%c ✓ ${this.currentTest} test passed!`, 'color: #4CAF50; font-weight: bold;');
    } else {
      //console.log(`%c ✗ ${this.currentTest} test failed!`, 'color: #f44336; font-weight: bold;');
    }

    this.currentTest = "";
  },

  /**
   * List all registered objects and their indices
   */
  listAll() {
    console.log("All registered test objects:");
    Object.entries(this.testSections).forEach(([testName, section]) => {
      console.log(`[${testName}] Objects: ${Object.entries(section.objects).map(([name, index]) =>
        `${name}=${index}`).join(', ')}`);
    });
  },

  /**
   * Print a summary of test results with clickable links
   */
  printSummary() {
    console.log("\n===== TEST SUMMARY =====");

    if (this.failedTests.length === 0) {
      console.log(`%c ✅ All ${this.passedTests.length} tests passed! `,
        'background: #4CAF50; color: white; font-weight: bold; padding: 2px 5px;');
    } else {
      console.log(`%c ❌ ${this.failedTests.length} of ${this.passedTests.length + this.failedTests.length} tests failed `,
        'background: #f44336; color: white; font-weight: bold; padding: 2px 5px;');

      // Group failures by test
      const failuresByTest = {};
      this.failedTests.forEach(failure => {
        if (!failuresByTest[failure.test]) {
          failuresByTest[failure.test] = [];
        }
        failuresByTest[failure.test].push(failure);
      });

      // Print failures grouped by test with clickable links
      Object.entries(failuresByTest).forEach(([testName, failures]) => {
        console.log(`- Test: ${testName} (${failures.length} failures)`);
        failures.forEach(failure => {
          // Create a synthetic error to get a clickable stack trace
          const error = new Error(`${failure.message} (Expected: ${failure.expected}, Actual: ${failure.actual})`);
          // This will create a clickable stack trace in most browser DevTools
          console.log(`  • %c${failure.message}`, 'color: #f44336;');
        });
      });
    }

    console.log("\nTests run:");
    [...new Set([...this.passedTests, ...this.failedTests.map(f => f.test)])].forEach(test => {
      const passed = !this.failedTests.some(f => f.test === test);
      console.log(`${passed ? '✓' : '✗'} ${test}`);
    });
  }
};

/**
 * Test suite for basic shape creation and positioning
 */
const ShapeTest = {
  /**
   * Create and test basic shapes (text and symbols)
   */
  testShapeCreation() {
    TestTracker.startTest("ShapeCreation");

    // Create text object - Hong Kong in English - Use direct TextObject creation instead
    const englishText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
      text: 'Hong Kong',
      xHeight: 100,
      font: 'TransportMedium',
      color: 'White',
      left: -1050,
      top: -1000
    });
    TestTracker.register("englishText", englishText);

    // Create Chinese text - Use direct TextObject creation
    const chineseText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
      text: '香港',
      xHeight: 100,
      font: 'TransportMedium',
      color: 'White',
      left: -1050,
      top: -800
    });
    TestTracker.register("chineseText", chineseText);

    // Create symbol (unchanged)
    new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_1__.SymbolObject({
      symbolType: 'StackArrow',
      left: -1500,
      top: -900,
      xHeight: 100,
      angle: -45,
      color: 'white'
    });
    TestTracker.register("symbol");

    // Test assertions
    let passed = true;

    // Check English text using tracker
    const englishTextV1 = englishText.getBasePolygonVertex('E1');
    passed = passed && englishText.functionalType === 'Text' && TestTracker.trackLine();

    // Update the position checks to use the base group position instead of vertex position
    passed = passed && TestTracker.assert(englishText.left, -1050, "English text left position incorrect", 20);
    passed = passed && TestTracker.assert(englishText.top, -1000, "English text top position incorrect", 20);

    // Check Chinese text using tracker
    const chineseTextV1 = chineseText.getBasePolygonVertex('E1');
    passed = passed && chineseText.functionalType === 'Text';

    // Update the position checks for Chinese text
    passed = passed && TestTracker.assert(chineseText.left, -1050, "Chinese text left position incorrect", 20);
    passed = passed && TestTracker.assert(chineseText.top, -800, "Chinese text top position incorrect", 20);

    // Check symbol using tracker (unchanged)
    const symbol = TestTracker.get("symbol");
    const symbolV1 = symbol.getBasePolygonVertex('V1');
    passed = passed && symbol.functionalType === 'Symbol';
    passed = passed && TestTracker.assert(symbolV1.x, -1400, "Symbol left position incorrect", 5);
    passed = passed && TestTracker.assert(symbolV1.y, -900, "Symbol top position incorrect", 5);

    // Check that text objects are instances of TextObject class
    passed = passed && TestTracker.assertTrue(
      englishText instanceof _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject,
      "English text should be an instance of TextObject"
    );
    passed = passed && TestTracker.assertTrue(
      chineseText instanceof _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject,
      "Chinese text should be an instance of TextObject"
    );

    // Test text update functionality
    const originalText = englishText.text;
    englishText.updateText("Updated Text", englishText.xHeight, englishText.font, englishText.color);
    passed = passed && TestTracker.assert(englishText.text, "Updated Text", "Text update failed");

    // Restore original text for other tests
    englishText.updateText(originalText, englishText.xHeight, englishText.font, englishText.color);

    TestTracker.endTest(passed);
    return passed;
  },

  /**
   * Run all shape tests
   */
  runAll: function () {
    this.testShapeCreation();
  }
};

/**
 * Test suite for object anchoring functionality
 */
const AnchorTest = {
  /**
   * Create and test anchored objects
   */
  testAnchoringObjects() {
    TestTracker.startTest("AnchorTest");

    // Create three symbols to anchor together using direct object creation instead of FormText methods
    // First symbol (base)
    new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_1__.SymbolObject({
      symbolType: 'Tunnel',
      left: 300,
      top: -1000,
      xHeight: 100,
      color: 'white'
    });
    TestTracker.register("baseTunnel");

    // Second symbol (to right)
    new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_1__.SymbolObject({
      symbolType: 'Airport',
      left: 400,
      top: -400,
      xHeight: 100,
      angle: 0,
      color: 'white'
    });
    TestTracker.register("Airport");

    // Third symbol (below)
    new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_1__.SymbolObject({
      symbolType: 'Hospital',
      left: 300,
      top: -300,
      xHeight: 100,
      color: 'white'
    });
    TestTracker.register("bottomHospital");

    // Anchor second object to first with 50px horizontal spacing
    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(TestTracker.get("baseTunnel"), TestTracker.get("Airport"), {
      vertexIndex1: 'E1',
      vertexIndex2: 'E3',
      spacingX: 50,
      spacingY: 0
    });

    // Anchor third object below first with 30px vertical spacing
    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(TestTracker.get("baseTunnel"), TestTracker.get("bottomHospital"), {
      vertexIndex1: 'E2',
      vertexIndex2: 'E6',
      spacingX: 0,
      spacingY: 50
    });

    // Test assertions
    let passed = true;

    // Check that objects are anchored correctly
    passed = passed && TestTracker.assertSpacing(
      TestTracker.get("baseTunnel"), TestTracker.get("Airport"),
      'x', 50,
      "Horizontal anchor spacing incorrect"
    );

    passed = passed && TestTracker.assertSpacing(
      TestTracker.get("baseTunnel"), TestTracker.get("bottomHospital"),
      'y', 50,
      "Vertical anchor spacing incorrect"
    );

    // Now move the base object and verify that anchored objects move accordingly
    TestTracker.get("baseTunnel").set({ left: TestTracker.get("baseTunnel").left + 100 });
    TestTracker.get("baseTunnel").setCoords();
    TestTracker.get("baseTunnel").updateAllCoord();

    // Check that anchored objects moved with base
    passed = passed && TestTracker.assert(
      TestTracker.get("Airport").left,
      TestTracker.get("baseTunnel").left + TestTracker.get("baseTunnel").width + 50,
      "Horizontal anchor movement failed",
      5
    );

    passed = passed && TestTracker.assert(
      TestTracker.get("bottomHospital").left,
      TestTracker.get("baseTunnel").left + TestTracker.get("baseTunnel").width / 2 - TestTracker.get("bottomHospital").width / 2,
      "Vertical anchor movement failed - horizontal position",
      5
    );

    // Check that the vertical position of the second object didn't change
    passed = passed && TestTracker.assert(
      TestTracker.get("bottomHospital").top,
      TestTracker.get("baseTunnel").top + TestTracker.get("baseTunnel").height + 50,
      "Vertical anchor movement failed - vertical position should not change",
      5
    );

    window.testAnchors = canvasObject.slice();

    TestTracker.endTest(passed);
    return passed;
  },

  /**
   * Create and test delinking of anchored objects
   */
  testDelinkingAnchoredObjects() {
    TestTracker.startTest("DelinkingAnchoredObjects");

    // Create four symbols to anchor together
    // First pair
    new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_1__.SymbolObject({
      symbolType: 'Tunnel',
      left: 1500,
      top: -1000,
      xHeight: 100,
      angle: 0,
      color: 'white'
    });
    TestTracker.register("baseTunnel1");

    new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_1__.SymbolObject({
      symbolType: 'WHC',
      left: 1500,
      top: -1000,
      xHeight: 100,
      angle: 0,
      color: 'white'
    });
    TestTracker.register("airport1");

    // Second pair
    new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_1__.SymbolObject({
      symbolType: 'Hospital',
      left: 1575,
      top: -550,
      xHeight: 100,
      color: 'white'
    });
    TestTracker.register("baseHospital");

    new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_1__.SymbolObject({
      symbolType: 'CHT',
      left: 1575,
      top: -550,
      xHeight: 100,
      angle: 0,
      color: 'white'
    });
    TestTracker.register("stackArrow");

    // Anchor first pair
    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(TestTracker.get("baseTunnel1"), TestTracker.get("airport1"), {
      vertexIndex1: 'E1',
      vertexIndex2: 'E3',
      spacingX: 50,
      spacingY: 0
    });

    // Anchor second pair
    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(TestTracker.get("baseHospital"), TestTracker.get("stackArrow"), {
      vertexIndex1: 'E1',
      vertexIndex2: 'E3',
      spacingX: 50,
      spacingY: 0
    });

    // Test assertions
    let passed = true;

    // Check that objects are anchored correctly
    passed = passed && TestTracker.assertSpacing(
      TestTracker.get("baseTunnel1"), TestTracker.get("airport1"),
      'x', 50,
      "Horizontal anchor spacing incorrect for first pair"
    );

    passed = passed && TestTracker.assertSpacing(
      TestTracker.get("baseHospital"), TestTracker.get("stackArrow"),
      'x', 50,
      "Horizontal anchor spacing incorrect for second pair"
    );

    // Delink first pair by calling onClick of LockIcon
    canvas.setActiveObject(TestTracker.get("airport1"));
    const lockIcon1 = TestTracker.get("airport1").anchorageLink[0]
    lockIcon1.onClick();

    // Check that first pair is delinked
    passed = passed && TestTracker.assertTrue(
      !TestTracker.get("airport1").lockXToPolygon.TargetObject,
      "First pair should be delinked"
    );

    // Delink second pair by deleting the base object
    TestTracker.get("baseHospital").deleteObject();
    TestTracker.testSections[TestTracker.currentTest].objects["stackArrow"] -= 1;

    // Check that second pair is delinked
    passed = passed && TestTracker.assertTrue(
      !TestTracker.get("stackArrow").lockXToPolygon.TargetObject,
      "Second pair should be delinked"
    );

    TestTracker.endTest(passed);
    return passed;
  },

  /**
   * Test the anchor pivoting functionality
   */
  testAnchorPivoting() {
    TestTracker.startTest("AnchorPivoting");

    // Create two text objects
    const topText1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
      text: 'Line 1',
      xHeight: 100,
      font: 'TransportMedium',
      color: 'White',
      left: 2750,
      top: -1000
    });
    TestTracker.register("pivotTopText", topText1);

    // Create two text objects
    const topText2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
      text: 'Line 2',
      xHeight: 100,
      font: 'TransportMedium',
      color: 'White',
      left: 2750,
      top: -1000
    });
    TestTracker.register("pivotTopText", topText2);

    const bottomText1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
      text: 'Line 3',
      xHeight: 100,
      font: 'TransportMedium',
      color: 'White',
      left: 2750,
      top: -800 // Position it roughly below
    });
    TestTracker.register("pivotBottomText", bottomText1);

    const bottomText2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
      text: 'Line 4',
      xHeight: 100,
      font: 'TransportMedium',
      color: 'White',
      left: 2750,
      top: -800 // Position it roughly below
    });
    TestTracker.register("pivotBottomText", bottomText2);

    // Anchor bottomText below topText
    const initialSpacingY = 50;
    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(topText1, topText2, {
      vertexIndex1: 'E2', // Bottom center of topText
      vertexIndex2: 'E6', // Top center of bottomText
      spacingX: '', // No X anchor
      spacingY: initialSpacingY
    });
    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(topText2, bottomText1, {
      vertexIndex1: 'E2', // Bottom center of topText
      vertexIndex2: 'E6', // Top center of bottomText
      spacingX: '', // No X anchor
      spacingY: initialSpacingY
    });
    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(bottomText1, bottomText2, {
      vertexIndex1: 'E2', // Bottom center of topText
      vertexIndex2: 'E6', // Top center of bottomText
      spacingX: '', // No X anchor
      spacingY: initialSpacingY
    });

    let passed = true;

    // Verify initial anchor state
    passed = passed && TestTracker.assertTrue(
      bottomText1.lockYToPolygon && bottomText1.lockYToPolygon.TargetObject === topText2,
      "Initial state: bottomText should be locked to topText in Y"
    );
    passed = passed && TestTracker.assert(
      bottomText1.lockYToPolygon.spacing,
      initialSpacingY,
      "Initial state: bottomText Y spacing incorrect"
    );
    passed = passed && TestTracker.assertTrue(
      bottomText1.lockMovementY,
      "Initial state: bottomText should have lockMovementY=true"
    );
    passed = passed && TestTracker.assertTrue(
      !topText1.lockYToPolygon || !topText1.lockYToPolygon.TargetObject,
      "Initial state: topText should not be locked in Y"
    );

    // --- Perform the Pivot ---
    // Simulate the pivot action on the bottomText
    _objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.globalAnchorTree.reverseAnchorChain('y', bottomText1.canvasID);

    // --- Verify the reversed anchor state ---
    passed = passed && TestTracker.assertTrue(
      !bottomText1.lockYToPolygon || !bottomText1.lockYToPolygon.TargetObject,
      "Pivoted state: bottomText should NOT be locked to topText in Y"
    );
    passed = passed && TestTracker.assertTrue(
      !bottomText1.lockMovementY, // Assuming no other locks
      "Pivoted state: bottomText should have lockMovementY=false"
    );

    passed = passed && TestTracker.assertTrue(
      topText2.lockYToPolygon && topText2.lockYToPolygon.TargetObject === bottomText1,
      "Pivoted state: topText should NOW be locked to bottomText in Y"
    );
    passed = passed && TestTracker.assert(
      topText2.lockYToPolygon.spacing,
      -initialSpacingY,
      "Pivoted state: topText Y spacing should be negative of original"
    );
    passed = passed && TestTracker.assertTrue(
      topText2.lockMovementY,
      "Pivoted state: topText should have lockMovementY=true"
    );

    passed = passed && TestTracker.assertTrue(
      topText1.lockYToPolygon && topText1.lockYToPolygon.TargetObject === topText2,
      "Pivoted state: topText should NOW be locked to bottomText in Y"
    );
    passed = passed && TestTracker.assert(
      topText1.lockYToPolygon.spacing,
      -initialSpacingY,
      "Pivoted state: topText Y spacing should be negative of original"
    );
    passed = passed && TestTracker.assertTrue(
      topText1.lockMovementY,
      "Pivoted state: topText should have lockMovementY=true"
    );

    // Optional: Check relative position remains correct (within tolerance)
    const topBottomEdge = topText2.top + topText2.height;
    const bottomTopEdge = bottomText1.top;
    passed = passed && TestTracker.assert(
      bottomTopEdge - topBottomEdge,
      initialSpacingY, // The visual spacing should remain the same
      "Pivoted state: Visual spacing between objects changed",
      2 // Allow small tolerance
    );


    TestTracker.endTest(passed);
    return passed;
  },

  /**
   * Run all anchor tests
   */
  runAll: function () {
    this.testAnchoringObjects();
    this.testDelinkingAnchoredObjects();
    this.testAnchorPivoting(); // Add the new test here
  }
};

/**
 * Test suite for border creation and functionality
 */
const BorderTest = {
  /**
   * Create and test border around objects
   */
  testBorderCreation() {
    TestTracker.startTest("BorderCreation");

    // Create text objects directly using TextObject constructor instead of FormTextAddComponent
    const borderText1 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
      text: 'Border',
      xHeight: 100,
      font: 'TransportMedium',
      color: 'White',
      left: -1800,
      top: 300
    });
    TestTracker.register("borderText1", borderText1);

    const borderText2 = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
      text: 'Test',
      xHeight: 100,
      font: 'TransportMedium',
      color: 'White',
      left: -1800,
      top: 500
    });
    TestTracker.register("borderText2", borderText2);

    // Capture object dimensions before border creation
    const object1 = TestTracker.get("borderText1");
    const object2 = TestTracker.get("borderText2");

    const minX = Math.min(object1.left, object2.left);
    const maxX = Math.max(object1.left + object1.width, object2.left + object2.width);
    const minY = Math.min(object1.top, object2.top);
    const maxY = Math.max(object1.top + object1.height, object2.top + object2.height);

    const expectedWidth = maxX - minX;
    const expectedHeight = maxY - minY;

    // Create border around the objects
    const borderGroup = new _objects_border_js__WEBPACK_IMPORTED_MODULE_6__.BorderGroup(
      {
        widthObjects: [object1, object2],
        heightObjects: [object1, object2],
        xHeight: 100,
        borderType: 'stack',
        color: 'Blue Background' // Assuming BorderGroup constructor takes color directly or BorderUtilities.BorderGroupCreate handles mapping colorType to color
      }
    );
    TestTracker.register("borderGroup", borderGroup);

    // Test assertions
    let passed = true;

    // Border should be wider and taller than the combined objects due to padding
    passed = passed && TestTracker.assertTrue(
      borderGroup.width > expectedWidth,
      "Border should be wider than combined objects"
    );

    passed = passed && TestTracker.assertTrue(
      borderGroup.height > expectedHeight,
      "Border should be taller than combined objects"
    );

    // Border should contain both objects
    const borderBounds = borderGroup.getBoundingRect();
    const obj1Bounds = object1.getBoundingRect();
    const obj2Bounds = object2.getBoundingRect();

    passed = passed && TestTracker.assertTrue(
      borderBounds.left <= obj1Bounds.left,
      "Border should contain first object (left edge)"
    );

    passed = passed && TestTracker.assertTrue(
      borderBounds.top <= obj1Bounds.top,
      "Border should contain first object (top edge)"
    );

    passed = passed && TestTracker.assertTrue(
      borderBounds.left + borderBounds.width >= obj2Bounds.left + obj2Bounds.width,
      "Border should contain second object (right edge)"
    );

    passed = passed && TestTracker.assertTrue(
      borderBounds.top + borderBounds.height >= obj2Bounds.top + obj2Bounds.height,
      "Border should contain second object (bottom edge)"
    );

    passed = passed && TestTracker.assertTrue(
      borderBounds.width % 25 == 0,
      "Border width is rounded to nearest 25"
    );

    passed = passed && TestTracker.assertTrue(
      borderBounds.height % 25 == 0,
      "Border height is rounded to nearest 25"
    );

    // Check border color scheme
    passed = passed && TestTracker.assert(
      borderGroup.color,
      'Blue Background',
      "Border has incorrect color scheme"
    );

    window.testBorder = borderGroup;

    TestTracker.endTest(passed);
    return passed;
  },

  /**
   * Create and test border around objects with divider
   */
  testBorderWithDivider() {
    TestTracker.startTest("BorderWithDivider");

    // Create text objects directly using TextObject constructor
    const aboveText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
      text: 'Above',
      xHeight: 100,
      font: 'TransportHeavy',
      color: 'Black',
      left: -800,
      top: 250
    });
    TestTracker.register("aboveText", aboveText);

    const belowText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
      text: 'Below',
      xHeight: 100,
      font: 'TransportHeavy',
      color: 'Black',
      left: -800,
      top: 450
    });
    TestTracker.register("belowText", belowText);

    const aboveObject = TestTracker.get("aboveText");
    const belowObject = TestTracker.get("belowText");

    // Create border around all three objects
    const borderGroup = new _objects_border_js__WEBPACK_IMPORTED_MODULE_6__.BorderGroup(
      {
        widthObjects: [aboveObject, belowObject],
        heightObjects: [aboveObject, belowObject,],
        xHeight: 100,
        borderType: 'stack',
        color: 'Yellow Background' // Assuming BorderGroup constructor takes color directly or BorderUtilities.BorderGroupCreate handles mapping colorType to color
      }
    );
    TestTracker.register("combinedBorder", borderGroup);

    new _objects_divider_js__WEBPACK_IMPORTED_MODULE_5__.DividerObject({
      borderGroup: borderGroup,
      xHeight: 100,
      colorType: 'Yellow Background',
      dividerType: 'HDivider',
    });
    TestTracker.register("divider");

    const divider = TestTracker.get("divider");

    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(aboveObject, divider, {
      vertexIndex1: 'V1', // Bottom center of aboveObject
      vertexIndex2: 'E6', // Top center of divider
      spacingX: '', // No X anchor
      spacingY: 1.5 * 100 / 4 // 25px vertical spacing
    });

    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(divider, belowObject, {
      vertexIndex1: 'E2', // Bottom center of divider
      vertexIndex2: 'V1', // Top center of belowObject
      spacingX: '', // No X anchor
      spacingY: 2.5 * 100 / 4 // 25px vertical spacing
    });

    // Test assertions
    let passed = true;

    // Border should contain all three objects
    passed = passed && TestTracker.assertTrue(
      borderGroup.heightObjects.includes(aboveObject) &&
      borderGroup.heightObjects.includes(belowObject) &&
      borderGroup.HDivider.includes(divider),
      "Border should contain both text objects and the divider"
    );

    // Check border contains all elements
    const borderBounds = borderGroup.getBoundingRect();
    const aboveBounds = aboveObject.getBoundingRect();
    const belowBounds = belowObject.getBoundingRect();
    const dividerBounds = divider.getBoundingRect();

    passed = passed && TestTracker.assertTrue(
      borderBounds.top <= Math.min(aboveBounds.top, belowBounds.top, dividerBounds.top),
      "Border should contain all elements (top edge)"
    );

    passed = passed && TestTracker.assertTrue(
      borderBounds.top + borderBounds.height >= Math.max(
        aboveBounds.top + aboveBounds.height,
        belowBounds.top + belowBounds.height,
        dividerBounds.top + dividerBounds.height
      ),
      "Border should contain all elements (bottom edge)"
    );

    // Check divider position is between objects
    passed = passed && TestTracker.assertTrue(
      divider.top >= aboveObject.getEffectiveCoords()[2].y - 1, // Text object height is greater than effective height
      "Divider should be below first object"
    );

    passed = passed && TestTracker.assertTrue(
      divider.top < belowObject.top - 1,
      "Divider should be above second object"
    );

    // Now move the border and check if everything moves together
    const originalDividerTop = divider.top;
    const originalBelowTop = belowObject.top;
    const originalBorderTop = borderGroup.top;
    aboveObject.set({ top: aboveObject.top + 50 });
    aboveObject.setCoords();
    aboveObject.updateAllCoord();

    // Check that all elements moved with the border
    passed = passed && TestTracker.assertTrue(
      Math.abs((divider.top - originalDividerTop) - 50) < 5,
      "Divider should move with border"
    );

    passed = passed && TestTracker.assertTrue(
      Math.abs((belowObject.top - originalBelowTop) - 50) < 5,
      "Below text should move with border"
    );

    passed = passed && TestTracker.assertTrue(
      Math.abs((borderGroup.top - originalBorderTop) - 50) < 5,
      "Below text should move with border"
    );

    // Check border color scheme
    passed = passed && TestTracker.assert(
      borderGroup.color,
      'Yellow Background',
      "Border has incorrect color scheme"
    );

    TestTracker.endTest(passed);
    return passed;
  },

  /**
   * Run all border tests
   */
  runAll: function () {
    this.testBorderCreation();
    this.testBorderWithDivider(); // Replace testDividerCreation with combined test
  }
};

/**
 * Test suite for route.js functionality
 */
const RouteTest = {
  /**
   * Test creation of a Main Road using route.js drawing functions
   */
  testMainRoad() {
    TestTracker.startTest("MainRoad");

    // Set up test parameters
    const params = {
      xHeight: 100,
      rootLength: 7,
      tipLength: 12,
      posx: 250,
      posy: 300,
      width: 6,
      shape: 'Arrow',
      color: 'white',
      roadType: 'Main Line'
    };

    // Create the route list with the main road points
    const routeList = [
      { x: params.posx, y: params.posy + (params.rootLength) * params.xHeight / 4, angle: 180, length: params.tipLength, width: params.width, shape: 'Stub' },
      { x: params.posx, y: params.posy, angle: 0, length: params.rootLength, width: params.width, shape: params.shape }
    ];

    // Create route options for the MainRoadSymbol
    const routeOptions = {
      routeList: routeList,
      xHeight: params.xHeight,
      rootLength: params.rootLength,
      tipLength: params.tipLength,
      width: params.width,
      color: params.color,
      roadType: params.roadType,
      // Ensure left and top are set for consistent positioning in tests
      left: params.posx - (params.width * params.xHeight / 8),
      top: params.posy
    };

    // Create and initialize the MainRoadSymbol
    const mainRoad = new _objects_mainRoute_js__WEBPACK_IMPORTED_MODULE_7__.MainRoadSymbol(routeOptions);
    // Manually call updateAllCoord as it's usually handled by canvas add
    mainRoad.updateAllCoord();


    TestTracker.register("mainRoad", mainRoad);

    // Test assertions using TestTracker
    let passed = true;

    passed = passed && TestTracker.assert(
      mainRoad.left,
      params.posx - (params.width * params.xHeight / 8), // Adjusted for actual calculation in constructor
      "Main Road left position incorrect",
      5
    );

    passed = passed && TestTracker.assert(
      mainRoad.top,
      params.posy,
      "Main Road top position incorrect",
      5
    );

    passed = passed && TestTracker.assert(
      mainRoad.functionalType,
      'MainRoad',
      "Main Road has incorrect functional type"
    );

    TestTracker.endTest(passed);
    return mainRoad;
  },

  /**
   * Test creation of a Side Road route on the left side using route.js drawing functions
   */
  testLeftSideRoad() {
    TestTracker.startTest("LeftSideRoad");

    const mainRoad = TestTracker.get("mainRoad", "MainRoad");
    if (!mainRoad) {
      TestTracker.recordFailure("Main Road not found for LeftSideRoad test", "MainRoad object", "null");
      TestTracker.endTest(false);
      return null;
    }

    // Ensure mainRoad coordinates are up-to-date
    mainRoad.updateAllCoord();


    const params = {
      xHeight: mainRoad.xHeight,
      color: 'white',
      mainRoad: mainRoad,
      side: true, // true for left
      branchIndex: mainRoad.sideRoad.length + 1,
      // Position the side road relative to the main road's root
      // Note: SideRoadSymbol constructor handles precise attachment logic
      routeList: [{
        x: mainRoad.left - 100,
        y: mainRoad.top,
        angle: -90,
        shape: 'Stub',
        width: 4
      }],
    };

    const sideRoad = new _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_8__.SideRoadSymbol(params);

    TestTracker.register("leftSideRoad", sideRoad);

    let passed = true;
    passed = passed && TestTracker.assert(
      sideRoad.functionalType,
      'SideRoad',
      "Side Road has incorrect type"
    );
    passed = passed && TestTracker.assert(
      sideRoad.side,
      true,
      "Side Road should be on left side"
    );

    // Check vertices touching root
    const rootLeft = mainRoad.routeList[0].x - mainRoad.routeList[0].width * mainRoad.xHeight / 8;

    // Find connecting vertices
    const touchingVertices = sideRoad.basePolygon.vertex.filter(v =>
      v.label === 'V3' || v.label === 'V4' || v.label === 'V5' || v.label === 'V6'
    );

    // All vertex should be close to Main Road
    const touchingRoot = touchingVertices.filter(v =>
      Math.abs(v.x - rootLeft) < 1
    );

    passed = passed && TestTracker.assertTrue(
      touchingRoot.length == touchingVertices.length,
      "Left Side Road vertices don't touch Main Road"
    );

    TestTracker.endTest(passed);
    return sideRoad;
  },

  /**
   * Test creation of a Side Road route on the right side using route.js drawing functions
   */
  testRightSideRoad() {
    TestTracker.startTest("RightSideRoad");

    const mainRoad = TestTracker.get("mainRoad", "MainRoad");
    if (!mainRoad) {
      TestTracker.recordFailure("Main Road not found for RightSideRoad test", "MainRoad object", "null");
      TestTracker.endTest(false);
      return null;
    }
    // Ensure mainRoad coordinates are up-to-date
    mainRoad.updateAllCoord();

    const params = {
      xHeight: mainRoad.xHeight,
      color: 'white',
      mainRoad: mainRoad,
      side: false, // false for right
      // Position the side road relative to the main road's root
      routeList: [{
        x: mainRoad.left + 300,
        y: mainRoad.top,
        angle: 60,
        shape: 'Arrow',
        width: 4
      }],
    };

    const sideRoad = new _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_8__.SideRoadSymbol(params);

    TestTracker.register("rightSideRoad", sideRoad);

    let passed = true;
    passed = passed && TestTracker.assert(
      sideRoad.functionalType,
      'SideRoad',
      "Side Road has incorrect type"
    );
    passed = passed && TestTracker.assert(
      sideRoad.side,
      false,
      "Side Road should be on right side"
    );

    // Check vertices touching root
    const rootRight = mainRoad.routeList[0].x + mainRoad.routeList[0].width * mainRoad.xHeight / 8;

    // Find connecting vertices
    const touchingVertices = sideRoad.basePolygon.vertex.filter(v =>
      v.label === 'V3' || v.label === 'V4' || v.label === 'V5' || v.label === 'V6'
    );

    // At least one vertex should be close to main road
    const touchingRoot = touchingVertices.filter(v =>
      Math.abs(v.x - rootRight) < 1
    );

    passed = passed && TestTracker.assertTrue(
      touchingRoot.length == touchingVertices.length,
      "Right Side Road vertices don't touch Main Road"
    );

    // CONSTRAINT TESTS:
    // 1. Test horizontal constraint - branch should be pushed to minimum distance from main road
    const minHorizontalDistance = 13 * mainRoad.xHeight / 4;
    const expectedLeftPosition = rootRight + minHorizontalDistance;

    passed = passed && TestTracker.assert(
      sideRoad.left + sideRoad.width,
      expectedLeftPosition,
      "Side Road not correctly positioned horizontally - constraint failed",
      5
    );

    // 2. Test vertical constraint - branch should not be above main road's tip

    const mainVertex = mainRoad.basePolygon.vertex
    const bottomPivotY = mainVertex[mainVertex.length - 6].y

    const sideRoadTopVertices = sideRoad.basePolygon.vertex[4];
    const branchTopY = sideRoadTopVertices.y;

    passed = passed && TestTracker.assertTrue(
      Math.abs(branchTopY - bottomPivotY) < 1,
      `Side Road top (${branchTopY}) should not be above Main Road tip position (${bottomPivotY})`
    );

    TestTracker.endTest(passed);
    return sideRoad;
  },

  /**
   * Test Side Road route movement constraints
   */
  testSideRoadMovement() {
    TestTracker.startTest("SideRoadMovement");

    // Get left Side Road from TestTracker
    const leftSideRoad = TestTracker.get("leftSideRoad", "LeftSideRoad");
    if (!leftSideRoad) {
      TestTracker.recordFailure("Left Side Road not found", "sideRoad object", "null");
      TestTracker.endTest(false);
      return false;
    }

    const mainRoad = leftSideRoad.mainRoad;
    const originalX = leftSideRoad.left;

    // Try to move Side Road too close to root
    leftSideRoad.left = mainRoad.left; // Too close
    leftSideRoad.updateAllCoord();
    leftSideRoad.onMove();

    // Should have been constrained
    const minDistance = 4 * leftSideRoad.xHeight / 4;
    const rootLeft = mainRoad.routeList[0].x - mainRoad.routeList[0].width * mainRoad.xHeight / 8;
    const expected = rootLeft - minDistance;

    const passed = TestTracker.assert(
      leftSideRoad.left,
      expected,
      "Side Road route not properly constrained from root",
      5
    );

    TestTracker.endTest(passed);
    return passed;
  },

  /**
   * Run all route tests
   */
  runAll: function () {
    this.testMainRoad();
    this.testLeftSideRoad();
    this.testRightSideRoad();
    this.testSideRoadMovement();
    // Roundabout tests moved to dedicated test suite
  }
};

/**
 * Test suite for roundabout functionality (both conventional and spiral)
 */
const RoundaboutTest = {
  /**
   * Test creation of a Conventional Roundabout and adding a side road to it
   */
  testConventionalRoundabout() {
    TestTracker.startTest("ConventionalRoundabout");

    // Set up test parameters for roundabout
    const params = {
      xHeight: 100,
      rootLength: 30,
      tipLength: 12,
      color: 'white',
      width: 6,
      shape: 'Stub',
      roadType: 'Conventional Roundabout',
      posx: 1250,
      posy: 600
    };

    // Create a Roundabout directly using updated construction method
    const routeList = [
      { x: params.posx, y: params.posy + (params.rootLength + params.tipLength) * params.xHeight / 4, angle: 180, width: 6, shape: 'Normal' },
      { x: params.posx, y: params.posy, angle: 0, width: 6, length: 30, shape: 'Stub' }
    ];

    // Create route options for the MainRoadSymbol with Conventional Roundabout type
    const routeOptions = {
      routeList: routeList,
      xHeight: params.xHeight,
      rootLength: params.rootLength,
      tipLength: params.tipLength,
      roadType: params.roadType,
      color: params.color
    };

    const roundabout = new _objects_mainRoute_js__WEBPACK_IMPORTED_MODULE_7__.MainRoadSymbol(routeOptions);
    TestTracker.register("conventionalRoundabout", roundabout);

    // Test assertions
    let passed = true;

    passed = passed && TestTracker.assert(
      roundabout.functionalType,
      'MainRoad',
      "Roundabout has incorrect functional type"
    );

    passed = passed && TestTracker.assert(
      roundabout.roadType,
      'Conventional Roundabout',
      "Roundabout has incorrect road type"
    );

    passed = passed && TestTracker.assert(
      roundabout.left,
      params.posx - 12 * params.xHeight / 4,  // Radius of roundabout is 12
      "Roundabout left position incorrect",
      5
    );

    // Set the roundabout as active object to add a side road
    canvas.setActiveObject(roundabout);

    // Create a side road on the roundabout
    const sideRoadParams = {
      xHeight: roundabout.xHeight,
      color: 'white',
      mainRoad: roundabout,
      side: false, // false for right
      // Position the side road relative to the main road's root
      routeList: [{
        x: params.posx + 120,
        y: params.posy - 50,
        angle: -45,
        shape: 'Stub',
        width: 4
      }],
    };

    // Draw and finalize the side road
    new _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_8__.SideRoadSymbol(sideRoadParams);

    // Create a second side road on the roundabout
    const sideRoadParams2 = {
      xHeight: roundabout.xHeight,
      color: 'white',
      mainRoad: roundabout,
      side: false, // false for right
      // Position the side road relative to the main road's root
      routeList: [{
        x: params.posx,
        y: params.posy - 100,
        angle: -90,
        shape: 'Arrow',
        width: 6
      }]
    };

    // Set the roundabout as active object to add a side road
    canvas.setActiveObject(roundabout);

    // Draw and finalize the second side road
    new _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_8__.SideRoadSymbol(sideRoadParams2);

    // Find the created Side Road route and register it
    const sideRoad = roundabout.sideRoad[roundabout.sideRoad.length - 1];
    TestTracker.register("roundaboutSideRoad", sideRoad);

    // Test side road assertions
    passed = passed && TestTracker.assertTrue(
      sideRoad != null,
      "Side road should be created"
    );

    if (sideRoad) {
      passed = passed && TestTracker.assert(
        sideRoad.functionalType,
        'SideRoad',
        "Side Road has incorrect type"
      );

      // Check side road is correctly positioned relative to roundabout center
      const roundaboutCenter = routeOptions.routeList[1];
      const distanceFromCenter = Math.sqrt(
        Math.pow(sideRoad.routeList[0].x - roundaboutCenter.x, 2) +
        Math.pow(sideRoad.routeList[0].y - roundaboutCenter.y, 2)
      );

      // Side road should be at least the roundabout radius away from center
      const minDistanceFromCenter = 12 * params.xHeight / 4;
      passed = passed && TestTracker.assertTrue(
        distanceFromCenter >= minDistanceFromCenter,
        `Side Road should be at least ${minDistanceFromCenter} units from roundabout center, but is ${distanceFromCenter}`
      );
    }

    TestTracker.endTest(passed);
    return sideRoad;
  },

  /**
   * Create and test a spiral roundabout with multiple arms
   */
  testSpiralRoundabout() {
    TestTracker.startTest("SpiralRoundabout");

    // Create a spiral roundabout
    const params = {
      xHeight: 100,
      rootLength: 7,
      tipLength: 12,
      color: 'white',
      width: 6,
      shape: 'Spiral Arrow',
      roadType: 'Spiral Roundabout',
      posx: 2500,
      posy: 600
    };

    // Create a Spiral Roundabout directly using updated construction method
    const routeList = [
      { x: params.posx, y: params.posy + (params.rootLength + params.tipLength) * params.xHeight / 4, angle: 180, width: 6, shape: 'Auxiliary' },
      { x: params.posx, y: params.posy, angle: 0, width: 6, shape: 'Stub' }
    ];

    // Create route options for the MainRoadSymbol with Spiral Roundabout type
    const routeOptions = {
      routeList: routeList,
      xHeight: params.xHeight,
      rootLength: params.rootLength,
      tipLength: params.tipLength,
      roadType: params.roadType,
      color: params.color
    };

    const spiralRoundabout = new _objects_mainRoute_js__WEBPACK_IMPORTED_MODULE_7__.MainRoadSymbol(routeOptions);
    TestTracker.register("spiralRoundabout", spiralRoundabout);

    // Test assertions for the roundabout
    let passed = true;

    passed = passed && TestTracker.assertTrue(
      spiralRoundabout != null,
      "Spiral roundabout should be created"
    );

    passed = passed && TestTracker.assert(
      spiralRoundabout.roadType,
      'Spiral Roundabout',
      "Roundabout has incorrect road type"
    );

    // Add three arms to the roundabout in different quadrants
    const center = spiralRoundabout.routeList[1];
    canvas.setActiveObject(spiralRoundabout);

    // Add arm in first quadrant (top-right)
    this.addArmAtAngle(center, 300, -90, "arm1");

    // Add arm in second quadrant (top-left)
    this.addArmAtAngle(center, 300, 0, "arm2");


    // Verify arms were added
    passed = passed && TestTracker.assert(
      spiralRoundabout.sideRoad.length,
      3,
      `Wrong number of arms added to spiral roundabout`
    );

    // Test dragging the first arm
    const firstArm = spiralRoundabout.sideRoad[0];
    canvas.setActiveObject(firstArm);

    // Record initial position
    const initialPos = {
      left: firstArm.left,
      top: firstArm.top,
      tipX: firstArm.routeList[0].x,
      tipY: firstArm.routeList[0].y
    };

    // Move the arm
    firstArm.set({
      left: firstArm.left + 50,
      top: firstArm.top + 50
    });

    // Trigger update
    firstArm.onMove();

    // Validate arm position after movement
    const armTip = firstArm.routeList[0];
    const dx = armTip.x - center.x;
    const dy = armTip.y - center.y;
    const angle = Math.atan2(dy, dx) * 180 / Math.PI;
    const roundedAngle = Math.round(angle / 15) * 15;

    // Check angle snaps to 15 degree increments
    passed = passed && TestTracker.assertTrue(
      Math.abs(angle - roundedAngle) < 0.5,
      `Arm didn't snap to 15 degree increment. Actual: ${angle}, Expected: ${roundedAngle}`
    );

    // Check distance from center is maintained
    const distance = Math.sqrt(dx * dx + dy * dy);
    passed = passed && TestTracker.assertTrue(
      distance >= 24 * params.xHeight / 4 - 1,
      `Arm distance from center should be at least ${24 * params.xHeight / 4} units`
    );

    TestTracker.endTest(passed);
    return passed;
  },

  /**
   * Helper function to add an arm at a specific angle from center
   * @param {Object} center - Center point {x, y}
   * @param {number} radius - Distance from center to place arm
   * @param {number} angleDegrees - Angle in degrees
   * @param {string} name - Name to register the arm with in TestTracker
   */
  addArmAtAngle(center, radius, angleDegrees, name) {
    const angleRadians = angleDegrees * Math.PI / 180;
    const x = center.x + radius * Math.cos(angleRadians);
    const y = center.y + radius * Math.sin(angleRadians);

    const roundabout = TestTracker.get("spiralRoundabout", "SpiralRoundabout");
    canvas.setActiveObject(roundabout);

    // Create options for the arm
    const options = {
      xHeight: roundabout.xHeight,
      color: 'white',
      mainRoad: roundabout,
      side: false, // false for right
      routeList: [{
        x: x,
        y: y,
        angle: angleDegrees,
        shape: 'Spiral Arrow',
        width: 4
      }],
    };

    // Add the arm
    new _objects_sideRoute_js__WEBPACK_IMPORTED_MODULE_8__.SideRoadSymbol(options);


    // Register the new arm with TestTracker

    const newArm = roundabout.sideRoad[roundabout.sideRoad.length - 1];
    TestTracker.register(name, newArm);

    return newArm;
  },

  /**
   * Run all roundabout tests
   */
  runAll: function () {
    this.testConventionalRoundabout();
    this.testSpiralRoundabout();
  }
};

/**
 * Test suite for complex traffic sign layouts
 */
const ComplexSignTest = {
  /**
   * Create and test a complex sign with multiple borders and dividers
   */
  testComplexSignLayout() {
    TestTracker.startTest("ComplexSignLayout");

    // ===== LEFT SIDE OF SIGN =====
    // Create first line of 2-liner on left
    const leftTopText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
      text: 'Mong Kok, Hong Kong(W)',
      xHeight: 200,
      font: 'TransportHeavy',
      color: 'White',
      left: -2500,
      top: 1800
    });
    TestTracker.register("leftTopText", leftTopText);

    // Create second line of 2-liner on left
    const leftBottomText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
      text: 'Kowloon City',
      xHeight: 200,
      font: 'TransportHeavy',
      color: 'White',
      left: -2500,
      top: 2000
    });
    TestTracker.register("leftBottomText", leftBottomText);

    // Create destination text on left
    const leftDestination = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
      text: '旺角,香港(西),九龍城',
      xHeight: 200,
      font: 'TransportHeavy',
      color: 'White',
      left: -2500,
      top: 2200
    });
    TestTracker.register("leftDestination", leftDestination);

    // Create stack arrow below on left
    new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_1__.SymbolObject({
      symbolType: 'GantryArrow',
      left: -2400,
      top: 2500,
      xHeight: 200,
      angle: 0,
      color: 'white'
    });
    TestTracker.register("leftArrow");

    // Create Airport on left
    new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_1__.SymbolObject({
      symbolType: 'Airport',
      left: -2700,
      top: 2500,
      xHeight: 200,
      angle: 0,
      color: 'white'
    });
    TestTracker.register("Airport");

    // Create WHC on left
    new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_1__.SymbolObject({
      symbolType: 'WHC',
      left: -435,
      top: 2500,
      xHeight: 200,
      angle: 0,
      color: 'white'
    });
    TestTracker.register("WHC");

    // ===== RIGHT SIDE OF SIGN =====
    // Create first line of 2-liner on right
    const rightTopText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
      text: 'Kowloon Tong & Sha Tin',
      xHeight: 200,
      font: 'TransportHeavy',
      color: 'White',
      left: 3000,
      top: 1800
    });
    TestTracker.register("rightTopText", rightTopText);

    // Create destination text on right
    const rightDestination = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
      text: '九龍塘及沙田',
      xHeight: 200,
      font: 'TransportHeavy',
      color: 'White',
      left: 3000,
      top: 2200
    });
    TestTracker.register("rightDestination", rightDestination);

    // Create stack arrow below on right
    new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_1__.SymbolObject({
      symbolType: 'GantryArrow',
      left: 3000,
      top: 2500,
      xHeight: 200,
      angle: 0,
      color: 'white'
    });
    TestTracker.register("rightArrow");

    // Create second gantry arrow on right
    new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_1__.SymbolObject({
      symbolType: 'GantryArrow',
      left: 3300, // Position roughly to the right
      top: 2500,
      xHeight: 200,
      angle: 0,
      color: 'white'
    });
    TestTracker.register("rightArrow2");

    // Create third gantry arrow on right
    new _objects_symbols_js__WEBPACK_IMPORTED_MODULE_1__.SymbolObject({
      symbolType: 'GantryArrow',
      left: 3600, // Position roughly further right
      top: 2500,
      xHeight: 200,
      angle: 0,
      color: 'white'
    });
    TestTracker.register("rightArrow3");

    // Get all created objects
    const leftTopObj = TestTracker.get("leftTopText");
    const leftBottomObj = TestTracker.get("leftBottomText");
    const leftDestObj = TestTracker.get("leftDestination");
    const leftArrowObj = TestTracker.get("leftArrow");
    const airportObj = TestTracker.get("Airport");
    const whcObj = TestTracker.get("WHC");

    const rightTopObj = TestTracker.get("rightTopText");
    const rightDestObj = TestTracker.get("rightDestination");
    const rightArrowObj = TestTracker.get("rightArrow");
    const rightArrowObj2 = TestTracker.get("rightArrow2"); // Get new arrow 2
    const rightArrowObj3 = TestTracker.get("rightArrow3"); // Get new arrow 3


    // We'll build border first (without dividers), then add dividers tied directly to the borderGroup
    // Anchor the objects in place
    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(whcObj, leftTopObj, {
      vertexIndex1: 'E3',
      vertexIndex2: 'E1',
      spacingX: -100,
      spacingY: 0
    });

    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(leftTopObj, airportObj, {
      vertexIndex1: 'E3',
      vertexIndex2: 'E1',
      spacingX: -100,
      spacingY: 0
    });

    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(leftTopObj, leftBottomObj, {
      vertexIndex1: 'E1',
      vertexIndex2: 'E7',
      spacingX: 0,
      spacingY: 0
    });

    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(leftBottomObj, leftDestObj, {
      vertexIndex1: 'E1',
      vertexIndex2: 'E7',
      spacingX: 0,
      spacingY: 0
    });

    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(leftBottomObj, rightTopObj, {
      vertexIndex1: 'E1',
      vertexIndex2: 'E3',
      spacingX: '',
      spacingY: 0
    });

    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(rightTopObj, rightDestObj, {
      vertexIndex1: 'E2',
      vertexIndex2: 'E6',
      spacingX: 0,
      spacingY: 0
    });

    // Now add horizontal dividers after border so width assignment happens inside border logic
    const leftHDivider = new _objects_divider_js__WEBPACK_IMPORTED_MODULE_5__.DividerObject({
      xHeight: 200,
      colorType: 'Blue Background',
      dividerType: 'HLine',
      textObject: leftDestObj
    });
    TestTracker.register("leftHDivider", leftHDivider);
    // Attach underline reference to the text object for clarity
    leftDestObj.underline = leftHDivider;
    leftHDivider.textObject = leftDestObj;
    // Anchor left side objects to divider (destination above, arrow below)
    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(leftDestObj, leftHDivider, {
      vertexIndex1: 'V1',
      vertexIndex2: 'E6',
      spacingX: '',
      spacingY: 50
    });
    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(leftHDivider, leftArrowObj, {
      vertexIndex1: 'E2',
      vertexIndex2: 'V4',
      spacingX: '',
      spacingY: 50
    });

    const rightHDivider = new _objects_divider_js__WEBPACK_IMPORTED_MODULE_5__.DividerObject({
      xHeight: 200,
      colorType: 'Blue Background',
      dividerType: 'HLine',
      textObject: rightDestObj
    });
    TestTracker.register("rightHDivider", rightHDivider);
    // Attach underline reference to the text object for clarity
    rightDestObj.underline = rightHDivider;
    rightHDivider.textObject = rightDestObj;
    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(rightDestObj, rightHDivider, {
      vertexIndex1: 'V1',
      vertexIndex2: 'E6',
      spacingX: '',
      spacingY: 50
    });
    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(rightHDivider, rightArrowObj2, {
      vertexIndex1: 'E2',
      vertexIndex2: 'V4',
      spacingX: '',
      spacingY: 50
    });

    const overallObject = [leftHDivider, rightHDivider, leftTopObj, leftBottomObj, leftArrowObj, leftDestObj, airportObj, whcObj, rightTopObj, rightArrowObj, rightDestObj, rightArrowObj2, rightArrowObj3];

    const overallBorderGroup = _objects_border_js__WEBPACK_IMPORTED_MODULE_6__.BorderUtilities.BorderGroupCreate(
      'stack',
      overallObject,
      overallObject,
      13400,
      null,
      { xHeight: 200, borderType: 'stack', colorType: 'Blue Background' }
    );
    TestTracker.register("overallBorderGroup", overallBorderGroup);

    overallBorderGroup.set({ left: -6700 });

    // Vertical divider after border
    const vDivider = new _objects_divider_js__WEBPACK_IMPORTED_MODULE_5__.DividerObject({
      borderGroup: overallBorderGroup,
      xHeight: 200,
      colorType: 'Blue Background',
      dividerType: 'VDivider'
    });
    TestTracker.register("vDivider", vDivider);
    // Anchor left/right regions to vertical divider to maintain separation
    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(whcObj, vDivider, {
      vertexIndex1: 'V1',
      vertexIndex2: 'E3',
      spacingX: 100,
      spacingY: ''
    });
    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(vDivider, rightTopObj, {
      vertexIndex1: 'E1',
      vertexIndex2: 'V6',
      spacingX: 100,
      spacingY: ''
    });

    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(overallBorderGroup, leftArrowObj, {
      vertexIndex1: 'E1',
      vertexIndex2: 'E7',
      spacingX: 1550,
      spacingY: ''
    });

    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(leftArrowObj, rightArrowObj, {
      vertexIndex1: 'E2',
      vertexIndex2: 'E2',
      spacingX: 3650,
      spacingY: 0
    });

    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(overallBorderGroup, rightArrowObj2, { // Anchor rightArrow -> rightArrow2
      vertexIndex1: 'E3',
      vertexIndex2: 'E3',
      spacingX: -800, // Add some spacing
      spacingY: ''
    });

    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(rightArrowObj2, rightArrowObj3, { // Anchor rightArrow2 -> rightArrow3
      vertexIndex1: 'E3',
      vertexIndex2: 'E3',
      spacingX: -3650, // Add some spacing
      spacingY: 0
    });

    // reachor the right legend
    (0,_objects_anchor_js__WEBPACK_IMPORTED_MODULE_2__.anchorShape)(overallBorderGroup, rightTopObj, { // Anchor rightArrow2 -> rightArrow3
      vertexIndex1: 'E2',
      vertexIndex2: 'C6',
      spacingX: 0, // Add some spacing
      spacingY: ''
    });


    // Test assertions
    let passed = true;

    // Test 1: Verify that the overall border contains all elements
    passed = passed && TestTracker.assertTrue(
      overallBorderGroup.VDivider.includes(vDivider) &&
      leftDestObj.underline === leftHDivider &&
      rightDestObj.underline === rightHDivider,
      "Border should contain vertical divider; underlines adhere to text objects"
    );

    // Test 2: Verify that dividers do not cross borders
    // Get the vertical divider bounding box
    const vDividerBounds = vDivider.getBoundingRect();

    // Verify vertical divider top and bottom are within the overall border
    const overallBounds = overallBorderGroup.getBoundingRect();
    passed = passed && TestTracker.assertTrue(
      vDivider.top >= overallBounds.top &&
      vDivider.top + vDivider.height <= overallBounds.top + overallBounds.height,
      "Vertical divider should not extend beyond overall border"
    );

    // Test 3: Verify that horizontal dividers do not cross vertical divider
    const leftHDividerBounds = leftHDivider.getBoundingRect();
    const rightHDividerBounds = rightHDivider.getBoundingRect();

    // Left horizontal divider should not extend beyond vertical divider
    passed = passed && TestTracker.assertTrue(
      leftHDividerBounds.left + leftHDividerBounds.width <= vDividerBounds.left + 5,  // Allow 5px tolerance
      "Left horizontal divider should not extend beyond vertical divider"
    );

    // Right horizontal divider should not start before vertical divider
    passed = passed && TestTracker.assertTrue(
      rightHDividerBounds.left >= vDividerBounds.left + vDividerBounds.width - 5,  // Allow 5px tolerance
      "Right horizontal divider should not start before vertical divider"
    );

    // Test 4: Verify that horizontal dividers are positioned correctly between objects
    passed = passed && TestTracker.assertTrue(
      leftHDivider.top > leftBottomObj.top + leftBottomObj.height &&
      leftHDivider.top < leftArrowObj.top,
      "Left horizontal divider should be between 2-liner and destination"
    );

    passed = passed && TestTracker.assertTrue(
      rightHDivider.top > rightTopObj.top + rightTopObj.height &&
      rightHDivider.top < rightArrowObj.top,
      "Right horizontal divider should be between 2-liner and destination"
    );

    // Test 5: Verify that moving the overall border moves all contained elements
    // Record initial positions
    const initialVDividerLeft = vDivider.left;
    const initialLeftTextLeft = whcObj.left;
    const initialRightTextLeft = rightTopObj.left;
    const initialRightArrow2Left = rightArrowObj2.left; // Record initial position
    const initialRightArrow3Left = rightArrowObj3.left; // Record initial position

    // Move the overall border by left top object
    whcObj.set({ left: whcObj.left + 100 });
    whcObj.setCoords();
    whcObj.updateAllCoord();

    // Check that contained elements moved with the border
    passed = passed && TestTracker.assertTrue(
      Math.abs((vDivider.left - initialVDividerLeft) - 100) < 5,
      "Vertical divider should move with overall border"
    );

    passed = passed && TestTracker.assertTrue(
      Math.abs((whcObj.left - initialLeftTextLeft) - 100) < 5,
      "Left text should move with overall border"
    );

    passed = passed && TestTracker.assertTrue(
      Math.abs((rightTopObj.left + rightTopObj.width / 2 - overallBorderGroup.getBasePolygonVertex('C6').x)) < 5,
      "Right text should center horizontally with right half of border"
    );

    TestTracker.endTest(passed);
    return passed;
  },

  /**
   * Run all complex sign tests
   */
  runAll: function () {
    this.testComplexSignLayout();
  }
};


/**
 * Test suite for template sign creation
 */
const TemplateTest = {

  /**
 * Expected dimensions for each template.
 * NOTE: These are placeholders and need to be filled with actual expected values.
 */
  expectedTemplateDimensions: {

    'Flag Sign': { width: 2907, height: 1650, left: -15400, top: 7740 }, // Placeholder values
    'Stack Sign': { width: 1925, height: 1150 + 1275, left: -10602, top: 6749 }, // Placeholder values
    'Lane Sign': { width: 3950, height: 1600, left: -7606, top: 7998 }, // Placeholder values
    'Roundabout Sign': { width: 3800, height: 3250, left: -3488, top: 6019 }, // Placeholder values
    'Spiral Roundabout Sign': { width: 3800, height: 3250, left: 1794, top: 6230 }, // Placeholder values, may be null if not fully implemented
    'Gantry Sign': { width: 7900, height: 2700, left: 7493, top: 7244 }, // Placeholder values
    'Diverge Sign': { width: 2950, height: 5850, left: 17320, top: 7612 }, // Placeholder values
    // Add entries for any other templates
  },

  /**
   * Test creation of all available template signs
   */
  testTemplateCreation() {
    let overallPassed = true;
    // Assuming templateList is globally available or imported from sb-template.js
    const templateList = Object.keys(_sidebar_sb_template_js__WEBPACK_IMPORTED_MODULE_9__.FormTemplateComponent.templates) || []; // Adjust this line based on your actual template list source

    const initialObjectCount = canvasObject.length;
    let createdCount = 0;
    let gridX = -15000;
    let gridY = 8000;

    templateList.forEach((template, index) => {
      const testName = `TemplateCreation_${template || 'UnnamedTemplate'}_${index}`;
      TestTracker.startTest(testName);
      let passed = true;
      let createdSignResult = null;

      try {
        createdSignResult = _sidebar_sb_template_js__WEBPACK_IMPORTED_MODULE_9__.FormTemplateComponent.createTemplateSign(template, gridX, gridY,);
        gridX += template == 'Gantry Sign' ? 11000 : (template == 'Stack Sign' ? 3500 : 5000); // Adjust grid position for next template

        // Get expected dimensions for this template
        const expected = this.expectedTemplateDimensions[template];

        if (createdSignResult && expected) {
          const tolerance = 5; // Tolerance for dimension checks
          passed = passed && TestTracker.assertTrue(createdSignResult != null, "Template sign object should be created");
          // Add more specific assertions if needed, e.g., check functionalType or number of objects created
          // Assert dimensions against expected values
          passed = passed && TestTracker.assert(
            createdSignResult.width,
            expected.width,
            `Template ${template}: Width mismatch`,
            tolerance
          );
          passed = passed && TestTracker.assert(
            createdSignResult.height,
            expected.height,
            `Template ${template}: Height mismatch`,
            tolerance
          );
          // Assert that the returned top-left corner is consistent with the center point and dimensions
          passed = passed && TestTracker.assert(
            createdSignResult.left,
            expected.left,
            `Template ${template}: Left position mismatch (relative to center)`,
            tolerance
          );
          passed = passed && TestTracker.assert(
            createdSignResult.top,
            expected.top,
            `Template ${template}: Top position mismatch (relative to center)`,
            tolerance
          );
          createdCount++;
        } else {
          passed = false;
          TestTracker.recordFailure("Template sign creation returned null or undefined", "Fabric Object/Group", createdSign);
        }
      } catch (error) {
        passed = false;
        TestTracker.recordFailure(`Error creating template: ${error.message}`, "No error", error);
        console.error(`Error details for template ${template.name || index}:`, error);
      }

      TestTracker.endTest(passed);
      if (!passed) {
        overallPassed = false;
      }
    });

    // Final check
    TestTracker.startTest("TemplateCreationSummary");
    let summaryPassed = TestTracker.assert(createdCount, templateList.length, "Number of successfully created templates doesn't match list length");
    TestTracker.endTest(summaryPassed);

    return overallPassed && summaryPassed;
  },

  /**
   * Run all template tests
   */
  runAll: function () {
    this.testTemplateCreation();
  }
};

// Update testToRun to use the new consolidated RoundaboutTest instead of SpiralRoundaboutTest
const testToRun = [
  ShapeTest.runAll.bind(ShapeTest),
  AnchorTest.runAll.bind(AnchorTest),
  RouteTest.runAll.bind(RouteTest),
  BorderTest.runAll.bind(BorderTest),
  RoundaboutTest.runAll.bind(RoundaboutTest), // Replace SpiralRoundaboutTest with RoundaboutTest
  ComplexSignTest.runAll.bind(ComplexSignTest),
  TemplateTest.runAll.bind(TemplateTest),
];



function runTests(tests) {
  console.log("======== RUNNING TESTS ========\n");



  for (const test of tests) {
    test();
  }

  // Print just the final summary
  TestTracker.printSummary();
}


/**
 * Test for TextObject to DXF export
 * This test verifies that a TextObject maintains its position when exported to DXF
 */
function testTextObjectDXFExport() {
  // Create a new DXF document using our npm module
  const dxf = new DxfWriter();

  // Set document properties
  dxf.setUnits('Millimeters');
  dxf.addLayer('Outlines', 1, 'continuous', 'red');

  // Create a test text object at a specific position
  const textPosition = { left: 0, top: 0 };
  const testText = new _objects_text_js__WEBPACK_IMPORTED_MODULE_0__.TextObject({
    text: 'O',
    left: textPosition.left,
    top: textPosition.top,
    xHeight: 100,
    color: '#ffffff',
    font: 'TransportMedium'
  });

  // Add text to canvas temporarily
  canvas.add(testText);
  canvas.renderAll();

  // Export to DXF using the existing FormExportComponent.exportToDXF function
  // This is hronous since the exportToDXF function creates a blob and triggers a download

  const pathObject = [];
  FormExportComponent.collectPathObjects(testText, pathObject);

  const minX = testText.left;
  const minY = testText.top;
  pathObject.forEach(path => {
    FormExportComponent.processPathForDXF(path, dxf, minX, minY);
  })

  // Generate the DXF content
  const dxfContent = dxf.toDxfString();

  verifyTextPositionInDXF(dxfContent, testText)

  // Create download link
  const blob = new Blob([dxfContent], { type: 'application/dxf' });
  const url = URL.createObjectURL(blob);
  const link = document.createElement('a');
  link.download = `${FormExportComponent.exportSettings.filename}.dxf`;
  link.href = url;
  document.body.appendChild(link);
  link.click();
  document.body.removeChild(link);
  URL.revokeObjectURL(url);
}

/**
 * Helper function to verify text position in DXF content
 * @param {string} dxfContent - The DXF content string
 * @param {Object} originalPosition - Original position {left, top}
 * @returns {string} - Test result message
 */
function verifyTextPositionInDXF(dxfContent, originalPosition) {
  // A basic check for coordinates in the DXF
  // DXF format has coordinates after code 10 (X) and 20 (Y)

  // Find position entries in DXF
  const positionEntries = [];
  const lines = dxfContent.split('\n');

  for (let i = 0; i < lines.length; i++) {
    if (lines[i].trim() === '10') { // X coordinate identifier
      const xValue = parseFloat(lines[i + 1]);

      if (lines[i + 2].trim() === '20') { // Y coordinate identifier
        const yValue = parseFloat(lines[i + 3]);
        positionEntries.push({ x: xValue, y: yValue });
      }
    }
  }

  // Check if any position is close to the original text position
  const tolerance = 1; // Allow 1 unit of tolerance for floating point precision
  const positionMatched = positionEntries.some(pos =>
    Math.abs(pos.x - originalPosition.left) <= tolerance &&
    Math.abs(pos.y - originalPosition.top) <= tolerance
  );

  if (positionMatched) {
    return `✅ Text position verified in DXF output! Original: (${originalPosition.left}, ${originalPosition.top})`;
  } else {
    if (positionEntries.length > 0) {
      return `❌ Text position not found in DXF! Original: (${originalPosition.left}, ${originalPosition.top}), Found positions: ${JSON.stringify(positionEntries.slice(0, 3))}...`;
    } else {
      return `❌ No position coordinates found in DXF output!`;
    }
  }
}



/***/ }),

/***/ "./js/utils/hintLoader.js":
/*!********************************!*\
  !*** ./js/utils/hintLoader.js ***!
  \********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   HintLoader: () => (/* binding */ HintLoader)
/* harmony export */ });
/**
 * HintLoader - Utility for loading hint content from standalone HTML files
 */
class HintLoader {
  static cache = new Map(); // Cache loaded hints
  static loadingPromises = new Map(); // Track ongoing loads
  static cssLoaded = false; // Track if CSS has been loaded
  static cssLoadPromise = null; // Track CSS loading promise
  static buttonHintMap = new Map(); // Map button names to hint paths

  /**
   * Map base url suffix for resolving hint paths, useful for development environments where the app might be served from a subdirectory
   */
  static baseUrlSuffix = window.location.pathname ;;


  /**
   * Load hint content from a file
   * @param {string} hintPath - Path to the hint file (e.g., 'symbols/Airport') or button name
   * @return {Promise<string>} Promise that resolves to the hint HTML content
   */
  static async loadHint(hintPath) {
    // Resolve button name to actual hint path if mapping exists
    const resolvedPath = this.resolveHintPath(hintPath);

    // Ensure CSS is loaded first
    await this.ensureCSSLoaded();

    // Check cache first
    if (this.cache.has(resolvedPath)) {
      return this.cache.get(resolvedPath);
    }

    // Check if already loading
    if (this.loadingPromises.has(resolvedPath)) {
      return this.loadingPromises.get(resolvedPath);
    }

    // Start loading
    const loadPromise = this._fetchHint(resolvedPath);
    this.loadingPromises.set(resolvedPath, loadPromise);

    try {
      const content = loadPromise.then(rawContent => this.mapHintSvgPath(rawContent));
      this.cache.set(resolvedPath, content);
      this.loadingPromises.delete(resolvedPath);
      return content;
    } catch (error) {
      this.loadingPromises.delete(resolvedPath);
      throw error;
    }
  }

  static mapHintSvgPath(content) {
    // Implementation for mapping SVG paths in hint content
    const regex = /<img\s+[^>]*src=["']([^"']+)["'][^>]*>/gi;
    return content.replace(regex, (match, src) => {
      const resolvedSrc = `${this.baseUrlSuffix}/${src}`;
      return match.replace(src, resolvedSrc);
    });
  }

  /**
   * Resolve button name to hint path using mapping
   * @param {string} buttonNameOrPath - Button name or direct hint path
   * @return {string} Resolved hint path
   */
  static resolveHintPath(buttonNameOrPath) {
    // Check if it's a mapped button name
    const path = this.buttonHintMap.has(buttonNameOrPath)
      ? this.buttonHintMap.get(buttonNameOrPath)
      : buttonNameOrPath;

    // Return absolute URL with base origin
    return `${this.baseUrlSuffix + '/hints/' + path + '.html'}`;
  }

  /**
   * Ensure hints CSS is loaded
   * @return {Promise<void>}
   */
  static async ensureCSSLoaded() {
    if (this.cssLoaded) {
      return;
    }

    if (this.cssLoadPromise) {
      return this.cssLoadPromise;
    }

    this.cssLoadPromise = this._loadCSS();
    await this.cssLoadPromise;
  }

  /**
   * Load the hints CSS file
   * @private
   * @return {Promise<void>}
   */
  static async _loadCSS() {
    return new Promise((resolve, reject) => {
      // Check if CSS is already loaded
      const existingLink = document.querySelector('link[href*="hints/hints.css"]');
      if (existingLink) {
        this.cssLoaded = true;
        resolve();
        return;
      }

      // Create and load CSS link
      const link = document.createElement('link');
      link.rel = 'stylesheet';
      link.type = 'text/css';
      link.href = `${this.baseUrlSuffix}/hints/hints.css`;

      link.onload = () => {
        this.cssLoaded = true;
        console.log('Hints CSS loaded successfully');
        resolve();
      };

      link.onerror = (error) => {
        console.warn('Failed to load hints CSS:', error);
        // Don't reject - allow hints to work without CSS
        this.cssLoaded = true;
        resolve();
      };

      document.head.appendChild(link);
    });
  }

  /**
   * Internal method to fetch hint content
   * @private
   */
  static async _fetchHint(hintPath) {
    const url = hintPath;

    try {
      const response = await fetch(url);
      if (!response.ok) {
        throw new Error(`Failed to load hint: ${response.status} ${response.statusText}`);
      }

      let content = await response.text();

      // Remove the CSS link from the content since we load it separately
      content = content.replace(/<link[^>]*href[^>]*hints\.css[^>]*>/gi, '');

      return content.trim();
    } catch (error) {
      console.warn(`Failed to load hint from ${url}:`, error);
      return null; // Return null for missing hints rather than throwing
    }
  }

  /**
   * Preload multiple hints
   * @param {string[]} hintPaths - Array of hint paths to preload
   * @return {Promise<void>}
   */
  static async preloadHints(hintPaths) {
    // Ensure CSS is loaded first
    await this.ensureCSSLoaded();

    const promises = hintPaths.map(path => this.loadHint(path));
    await Promise.allSettled(promises); // Use allSettled to not fail if some hints are missing
  }

  /**
   * Clear the cache (useful for development/testing)
   */
  static clearCache() {
    this.cache.clear();
    this.loadingPromises.clear();
    // Note: We don't reset cssLoaded to avoid reloading CSS unnecessarily
  }

  /**
   * Force reload CSS (useful for development)
   */
  static reloadCSS() {
    this.cssLoaded = false;
    this.cssLoadPromise = null;
    // Remove existing CSS link
    const existingLink = document.querySelector('link[href*="hints/hints.css"]');
    if (existingLink) {
      existingLink.remove();
    }
  }

  /**
   * Get cache statistics
   * @return {Object} Cache statistics
   */
  static getCacheStats() {
    return {
      cached: this.cache.size,
      loading: this.loadingPromises.size,
      cssLoaded: this.cssLoaded,
      buttonMappings: this.buttonHintMap.size,
      entries: Array.from(this.cache.keys()),
      mappings: Object.fromEntries(this.buttonHintMap)
    };
  }

  /**
   * Set multiple button hint mappings
   * @param {Object} mappings - Object with button names as keys and hint paths as values
   */
  static setButtonHintMappings(mappings) {
    Object.entries(mappings).forEach(([buttonName, hintPath]) => {
      this.buttonHintMap.set(buttonName, hintPath);
    });
  }

  /**
   * Add a single button hint mapping
   * @param {string} buttonName - The button name or ID
   * @param {string} hintPath - The hint file path
   */
  static addButtonHintMapping(buttonName, hintPath) {
    this.buttonHintMap.set(buttonName, hintPath);
  }

  /**
   * Check if a button hint mapping exists
   * @param {string} buttonName - The button name to check
   * @return {boolean} True if mapping exists
   */
  static hasButtonHintMapping(buttonName) {
    return this.buttonHintMap.has(buttonName);
  }

  /**
   * Get all button hint mappings
   * @return {Object} Object with all button mappings
   */
  static getButtonHintMappings() {
    return Object.fromEntries(this.buttonHintMap);
  }

  /**
   * Remove a button hint mapping
   * @param {string} buttonName - The button name to remove
   * @return {boolean} True if mapping was removed
   */
  static removeButtonHintMapping(buttonName) {
    return this.buttonHintMap.delete(buttonName);
  }

  /**
   * Clear all button hint mappings
   */
  static clearButtonHintMappings() {
    this.buttonHintMap.clear();
  }

  /**
   * Auto-setup button hint mappings by scanning button elements in the DOM
   * @param {string} buttonSelector - CSS selector for buttons (default: 'button[id^="button-"]')
   * @param {string} hintPathPrefix - Prefix for hint paths (default: 'symbols/')
   */
  static autoSetupButtonMappings(buttonSelector = 'button[id^="button-"]', hintPathPrefix = 'symbols/') {
    const buttons = document.querySelectorAll(buttonSelector);
    buttons.forEach(button => {
      if (button.id && button.id.startsWith('button-')) {
        // Extract the symbol name from button ID (e.g., 'button-Airport' -> 'Airport')
        const symbolName = button.id.replace('button-', '');
        const hintPath = `${hintPathPrefix}${symbolName}`;
        this.addButtonHintMapping(button.id, hintPath);
      }
    });

    console.log(`Auto-setup completed: ${this.buttonHintMap.size} button mappings created`);
  }
}


/***/ }),

/***/ "./js/version_upgrades/UpgradeManager.js":
/*!***********************************************!*\
  !*** ./js/version_upgrades/UpgradeManager.js ***!
  \***********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   UpgradeManager: () => (/* binding */ UpgradeManager)
/* harmony export */ });
/* harmony import */ var _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../canvas/canvas.js */ "./js/canvas/canvas.js");


const UpgradeManager = {
    upgrades: [
        {
            targetVersion: '1.3.1', // Applies if save file version is <= 1.3.1
            description: 'Upgrade Roundabouts to have Base Route and remapped anchors',
            apply: (canvas) => {
                const objects = canvas.getObjects();
                objects.forEach(obj => {
                     if (obj.roadType && 
                        (obj.roadType === 'Conventional Roundabout' || obj.roadType === 'Spiral Roundabout') &&
                        typeof obj.upgradeBaseRoute === 'function') {
                         console.log(`Applying 1.3.1 upgrade to object ${obj.id || 'unknown'}`);
                         obj.upgradeBaseRoute();
                     }
                });
            }
        }
    ],

    compareVersions(v1, v2) {
        if (!v1) return -1; // No version means older than any versioned release
        if (!v2) return 1;
        
        const v1Parts = v1.split('.').map(p => parseInt(p, 10));
        const v2Parts = v2.split('.').map(p => parseInt(p, 10));
        
        for (let i = 0; i < Math.max(v1Parts.length, v2Parts.length); i++) {
            const val1 = v1Parts[i] || 0;
            const val2 = v2Parts[i] || 0;
            if (val1 < val2) return -1;
            if (val1 > val2) return 1;
        }
        return 0;
    },

    processUpgrades(fileVersion) {
        console.log(`Checking for upgrades. File version: ${fileVersion || 'Unknown'}`);
        let appliedAny = false;

        this.upgrades.forEach(upgrade => {
            // Apply if fileVersion <= targetVersion
            if (this.compareVersions(fileVersion, upgrade.targetVersion) <= 0) {
                 console.log(`Applying upgrade: ${upgrade.description}`);
                 try {
                    upgrade.apply(_canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas);
                    appliedAny = true;
                 } catch (e) {
                     console.error(`Error applying upgrade '${upgrade.description}':`, e);
                 }
            }
        });

        if (appliedAny) {
            _canvas_canvas_js__WEBPACK_IMPORTED_MODULE_0__.CanvasGlobals.canvas.requestRenderAll();
        }
    }
};


/***/ }),

/***/ "./js/version_upgrades/v1_3_1_roundabout.js":
/*!**************************************************!*\
  !*** ./js/version_upgrades/v1_3_1_roundabout.js ***!
  \**************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   RoundaboutVertexMapping: () => (/* binding */ RoundaboutVertexMapping),
/* harmony export */   remapAnchors: () => (/* binding */ remapAnchors)
/* harmony export */ });

const RoundaboutVertexMapping = {
    'Conventional Roundabout': {
        'V1': 'V2',
        'V2': 'V21',
        'V3': 'V22',
        'V4': 'V23',
        'V5': 'V24',
        'V6': 'V25'
    },
    'Spiral Roundabout': {
        'V1': 'V11',
        'V2': 'V13',
        'V3': 'V14',
        'V4': 'V15',
        'V5': 'V16',
        'V6': 'V17'
    }
};

/**
 * Remaps anchors of children attached to the roundabout to new vertex labels.
 * @param {Object} roundabout - The roundabout object (MainRoadSymbol).
 */
function remapAnchors(roundabout) {
    if (!roundabout || !roundabout.anchoredPolygon || !Array.isArray(roundabout.anchoredPolygon)) {
        return;
    }

    const mapping = RoundaboutVertexMapping[roundabout.roadType];
    if (!mapping) {
        return;
    }

    let remappedCount = 0;

    roundabout.anchoredPolygon.forEach(child => {
        let changed = false;

        // Check Lock X
        if (child.lockXToPolygon && child.lockXToPolygon.TargetObject === roundabout) {
            const oldVertex = child.lockXToPolygon.targetPoint;
            if (mapping[oldVertex]) {
                child.lockXToPolygon.targetPoint = mapping[oldVertex];
                changed = true;
                //console.log(`Remapped X anchor for child ${child.canvasID}: ${oldVertex} -> ${mapping[oldVertex]}`);
            }
        }

        // Check Lock Y
        if (child.lockYToPolygon && child.lockYToPolygon.TargetObject === roundabout) {
            const oldVertex = child.lockYToPolygon.targetPoint;
            if (mapping[oldVertex]) {
                child.lockYToPolygon.targetPoint = mapping[oldVertex];
                changed = true;
                //console.log(`Remapped Y anchor for child ${child.canvasID}: ${oldVertex} -> ${mapping[oldVertex]}`);
            }
        }

        if (changed) {
            // Force update coordinates
            if (typeof child.updateAllCoord === 'function') {
                child.updateAllCoord(); 
            }
            remappedCount++;
        }
    });

    if (remappedCount > 0) {
        console.log(`Remapped anchors for ${remappedCount} children of roundabout ${roundabout.canvasID}`);
    }
}


/***/ }),

/***/ "https://www.gstatic.com/firebasejs/12.15.0/firebase-app.js":
/*!*****************************************************************************!*\
  !*** external "https://www.gstatic.com/firebasejs/12.15.0/firebase-app.js" ***!
  \*****************************************************************************/
/***/ ((module) => {

"use strict";
module.exports = import("https://www.gstatic.com/firebasejs/12.15.0/firebase-app.js");;

/***/ }),

/***/ "https://www.gstatic.com/firebasejs/12.15.0/firebase-auth.js":
/*!******************************************************************************!*\
  !*** external "https://www.gstatic.com/firebasejs/12.15.0/firebase-auth.js" ***!
  \******************************************************************************/
/***/ ((module) => {

"use strict";
module.exports = import("https://www.gstatic.com/firebasejs/12.15.0/firebase-auth.js");;

/***/ }),

/***/ "https://www.gstatic.com/firebasejs/12.15.0/firebase-firestore.js":
/*!***********************************************************************************!*\
  !*** external "https://www.gstatic.com/firebasejs/12.15.0/firebase-firestore.js" ***!
  \***********************************************************************************/
/***/ ((module) => {

"use strict";
module.exports = import("https://www.gstatic.com/firebasejs/12.15.0/firebase-firestore.js");;

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/async module */
/******/ 	(() => {
/******/ 		var webpackQueues = typeof Symbol === "function" ? Symbol("webpack queues") : "__webpack_queues__";
/******/ 		var webpackExports = typeof Symbol === "function" ? Symbol("webpack exports") : "__webpack_exports__";
/******/ 		var webpackError = typeof Symbol === "function" ? Symbol("webpack error") : "__webpack_error__";
/******/ 		var resolveQueue = (queue) => {
/******/ 			if(queue && queue.d < 1) {
/******/ 				queue.d = 1;
/******/ 				queue.forEach((fn) => (fn.r--));
/******/ 				queue.forEach((fn) => (fn.r-- ? fn.r++ : fn()));
/******/ 			}
/******/ 		}
/******/ 		var wrapDeps = (deps) => (deps.map((dep) => {
/******/ 			if(dep !== null && typeof dep === "object") {
/******/ 				if(dep[webpackQueues]) return dep;
/******/ 				if(dep.then) {
/******/ 					var queue = [];
/******/ 					queue.d = 0;
/******/ 					dep.then((r) => {
/******/ 						obj[webpackExports] = r;
/******/ 						resolveQueue(queue);
/******/ 					}, (e) => {
/******/ 						obj[webpackError] = e;
/******/ 						resolveQueue(queue);
/******/ 					});
/******/ 					var obj = {};
/******/ 					obj[webpackQueues] = (fn) => (fn(queue));
/******/ 					return obj;
/******/ 				}
/******/ 			}
/******/ 			var ret = {};
/******/ 			ret[webpackQueues] = x => {};
/******/ 			ret[webpackExports] = dep;
/******/ 			return ret;
/******/ 		}));
/******/ 		__webpack_require__.a = (module, body, hasAwait) => {
/******/ 			var queue;
/******/ 			hasAwait && ((queue = []).d = -1);
/******/ 			var depQueues = new Set();
/******/ 			var exports = module.exports;
/******/ 			var currentDeps;
/******/ 			var outerResolve;
/******/ 			var reject;
/******/ 			var promise = new Promise((resolve, rej) => {
/******/ 				reject = rej;
/******/ 				outerResolve = resolve;
/******/ 			});
/******/ 			promise[webpackExports] = exports;
/******/ 			promise[webpackQueues] = (fn) => (queue && fn(queue), depQueues.forEach(fn), promise["catch"](x => {}));
/******/ 			module.exports = promise;
/******/ 			body((deps) => {
/******/ 				currentDeps = wrapDeps(deps);
/******/ 				var fn;
/******/ 				var getResult = () => (currentDeps.map((d) => {
/******/ 					if(d[webpackError]) throw d[webpackError];
/******/ 					return d[webpackExports];
/******/ 				}))
/******/ 				var promise = new Promise((resolve) => {
/******/ 					fn = () => (resolve(getResult));
/******/ 					fn.r = 0;
/******/ 					var fnQueue = (q) => (q !== queue && !depQueues.has(q) && (depQueues.add(q), q && !q.d && (fn.r++, q.push(fn))));
/******/ 					currentDeps.map((dep) => (dep[webpackQueues](fnQueue)));
/******/ 				});
/******/ 				return fn.r ? promise : getResult();
/******/ 			}, (err) => ((err ? reject(promise[webpackError] = err) : outerResolve(exports)), resolveQueue(queue)));
/******/ 			queue && queue.d < 0 && (queue.d = 0);
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/compat get default export */
/******/ 	(() => {
/******/ 		// getDefaultExport function for compatibility with non-harmony modules
/******/ 		__webpack_require__.n = (module) => {
/******/ 			var getter = module && module.__esModule ?
/******/ 				() => (module['default']) :
/******/ 				() => (module);
/******/ 			__webpack_require__.d(getter, { a: getter });
/******/ 			return getter;
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
/******/ 	
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module used 'module' so it can't be inlined
/******/ 	var __webpack_exports__ = __webpack_require__("./js/main.js");
/******/ 	
/******/ })()
;
//# sourceMappingURL=main.js.map